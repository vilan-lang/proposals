# Vilan Backlog — open items (distilled 2026-07-18)

Distilled from [`backlog.md`](backlog.md), which stays as the historical record (shipped
items, their full context, the lessons recorded alongside them). This file carries **open
work only**, simplified. Ids continue the old file's stable numbering: a carried id is the
same item; ids marked **NEW** are new or split-out work; retired numbers stay retired.
Sizes S/M/L/XL; unordered within a section. Per `CLAUDE.md`: each non-trivial item gets a
formal definition + pinned tests before implementation.

---

## A. Reactive core & UI (`std::reactive`, `std::ui`)

7. **SSR + hydration** (L–XL; proposal first) — server-render the initial UI as HTML
   (first paint before any JS, SEO), then make it live. Needs a render-only `std::ui`
   target (a `View` over an HTML string-builder, legal on `@process`; effects and
   subscriptions never run server-side — server render is create-serialize-discard).
   v1 = string renderer + hydration (the client claims server nodes: first-run-adopts in
   `bind_*`, deterministic node addressing); resumability is the recorded follow-on
   (vilan owns closure conversion, so handler-splitting is a compiler pass — genuinely
   better-placed than the JS frameworks). Streaming SSR / suspense beyond v1.
   Dependencies met. **Sequenced after A13** (user call 2026-07-18): HMR's stable
   identities + state snapshot are shared groundwork.

8. **UI styling — the tail** (M; core SHIPPED 2026-07-10; status in
   `proposal/ui-styling.md`) — `bind_styled`, dark×pseudo composition, the html `<link>`
   scaffold, fmt chain splitting, the property long tail; A7-entangled: critical CSS and
   liveness-tied dead-style elimination (rides G2's liveness-tied emission).

9. **`vilan.toml [build] run` hooks** (S) — run external commands alongside
   `vilan build` / `--watch` (the Tailwind bridge, asset pipelines, codegen sidecars).

13. **NEW — Hot module replacement (HMR)** (L; proposal first; **before A7, and ahead of
    F5/F7** — user calls 2026-07-18) — edit a source file and the running browser app updates without a full
    reload, reactive state preserved. The pieces: watch rebuild (exists) → a dev-server
    push channel (`std::ws` exists) → the in-place swap. The swap is the design core:
    vilan emits one bundle today, so v1 is likely whole-bundle re-eval with **state
    carryover** — which needs **stable identities** for signals/module state across
    builds (compiler-minted, module+binding-keyed) and a snapshot/restore protocol.
    Fine-grained reactive has no component-re-render unit to lean on (Solid's HMR
    lesson), so identity is the real feature; true per-module swap would need
    module-boundary emission — evaluate, don't presume. CSS hot-swaps via the asset
    channel with no reload (needs G2's `run`/`--watch` asset writes). Full-stack: patch
    the client leg, restart the server leg (the K6 reconnect machinery already lets the
    client survive that). The identity + serialization groundwork is shared with A7
    hydration — the reason HMR goes first.

14. **NEW — Reactive residuals** (S–M each; split from shipped A6/A12) —
    - `batch` async-join drain affinity: `batch` deliberately kept its `sync` fence at
      the turn merge; joining an ambient turn from an awaiting body is unresolved.
    - The optimistic-write → reconcile lifecycle (rides turns; `optimistic` covers the
      one-shot case today).
    - `Draft`: auto re-push of dirty drafts on reconnect; a debounced variant.

## B. Type system & the type solver

3. **Variadic-generics tail** (M–L; `variadic-generics.md` §Deferred) — `keyof`; spread
   parameters (`...items: T`); eliding the flat-tuple construction copy;
   trait-typed-value dispatch (→ B4).

4. **Trait objects / dynamic dispatch** (L; own proposal when demanded) — a bare-trait-
   typed value (`let x: Display = …`) is a clean error today; making it work by value
   needs a `(value, vtable)` representation. Nothing demands it yet.

11. **`!` / `?.` tail** (M; `try-and-lift.md`) — the bare-`?` trait path (user `Lift`
    containers; today a clean error steering to `?.`); closure `!` (the RPC-handler
    follow-up; needs the `arg → Result` linkage design); `Signal`/`Promise` `Lift`
    opt-ins.

29. **NEW — Trait conformance is name-only; signatures unchecked** (M; found 2026-07-19
    by C4 S2a's receiver probe) — the required-member check verifies an impl provides
    each member by NAME (`check.declarations.contains_key`); receiver convention,
    parameter types/arity, and return type are never compared against the trait's
    declaration, so `impl R with Drop { fun drop(self) }` and `fun drop(&self)` both
    compile against `fun drop(&mut self)`. C4's S2b ships a *targeted* check for `Drop`
    (its inserted teardown assumes `&mut self` + void return); this item is the general
    fix — full per-member signature conformance with per-case pins (receiver, arity,
    parameter types, return type, generic members). Pre-existing; surfaced, not
    introduced, by C4.

30. **NEW — `lazy`: defer to first demand** (M; **proposal drafted: `lazy.md`
    2026-07-20**, awaiting review) — one semantic in two positions: lazy
    *parameters* (`fun expect(self, lazy message: str)` — call-by-need arguments,
    memoized, transparent to the callee; the compiler-managed form of the
    `unwrap_or_else` callback idiom) and lazy *module bindings* (`lazy let db:
    Database = open(..)` — first-use init, everything else identical to the eager
    global: loan-only, write-frozen, never drops; THE answer to the lazy-resource
    question, which also found and fixed the module-overwrite leak). Surface under
    the closure rule (no new alias kind or epoch event); thunks are sync +
    context-free + data-only in v1 (each the conservative direction). Open calls in
    the proposal §6: poison-vs-retry, the `unwrap_or` retrofit, spelling.

## C. Memory model

*Frame: `claims-and-epochs.md` (2026-07-18) — the one law behind all of it (a claim is
valid while its owner's epoch is unchanged; views = static proof, handles = dynamic
check) and the closure decision: **C4 is the last major change to the memory model** —
everything else here is implementation or refinement within the frame.*

4. **Deterministic destruction** (L; **SHIPPED 2026-07-19, Tier 1 complete S1–S5** —
   ledger in `destruction.md`'s status block; Tier 2 = the counted class, specified in
   §10, builds with the native arc — **was the keystone; the memory model is now
   CLOSED per `claims-and-epochs.md` §4**) — scope-end destructors. The accepted design: a two-world
   partition — data stays rule-1 copying, untouched; a small **affine resource class**
   (declared `resource` + recursive containment) moves on binding/`own`, loans through
   the existing view conventions, and drops at its owner's scope end via
   `Drop { fun drop(&mut self) }` (Tier 1, on JS). `Shared`/`Owner`/`Disposable`
   deliberately stay data in v1; the counted class (`Shared` retain/release, `Weak` +
   `get`, counted closure environments) is Tier 2 — specified in `destruction.md` §10,
   built with the native arc. The design docs supersede this entry's earlier sketch.
   Unblocks C1, J4's free-spawn lint, real teardown in std, and the memory-model half
   of F3/F4.

1. **`Weak<T>`** (M; **blocked on counting** — C4 shipped 2026-07-19, so the blocker is
   now precisely Tier 2's refcounting (the native arc); fully specified in
   `destruction.md` §10, incl. the scoped `get(&self): Option<&T> borrows self` twin
   from `claims-and-epochs.md` §5a) — deterministic
   `upgrade() → None` needs a release event, which only exists once handles are
   refcounted; GC-timing `WeakRef` was rejected (2026-07-07). No JS-side driver today:
   the host GC collects `Shared` cycles, and observe-deletion is `Arena` generations'
   job.

2. **Dynamic rule-4 remainder** (sized 2026-07-18 → **folded into F4's native arc**) —
   cross-handle aliased writes (two `Shared` handles, one cell) need runtime
   generations / poisoned views; semantically empty on JS (nothing dangles, and E3 of
   `view-invalidation.md` already fences the across-`await` window). Build it with the
   native memory story, likely debug-mode-only.

6. **Geometry-effect inference — `bumps`, the twin of `borrows`** (**SHIPPED
   2026-07-19** — `rule4-completion.md`, commits d595ed2 (the effect) + 9b5e3cf (E2
   keys off it; both recorded E2 conservatisms cleared); residues recorded in the
   plan: generic verdicts are instantiation-independent-conservative, view-binding
   arguments under-approximate — both wait on no driver) —
   infer per function whether each `&mut` parameter (receiver included) is
   *content-stable* (field/element writes only) or *bumping* (may resize / reassign /
   drop through it). E2 keys off "`&mut` convention" as a proxy for "may bump the
   root's epoch", and its two recorded conservatisms (scalar-field view under a
   `&mut s` call; generic-typed roots) are exactly where the proxy is coarse; the
   inferred effect makes rule 4 precise (relaxation only — accepts more programs,
   changes no runtime behavior) and is the event classifier Tier 2's trap law and C2's
   generations key off. Fifth verse of the inferred-effect worklist (async, platform,
   contexts, `borrows`).

7. **Wire-blessed handles — the naming layer** (S–M; **decision made 2026-07-18:
   bless**, under `claims-and-epochs.md` §6's conditions) — `[derive(Wire)]` on
   `Handle<T>` + the documented idiom: a server-side arena whose handles flow to
   clients as stable entity references (`Draft` targets, router entities, "update node
   X" RPCs); stale-handle → `None` becomes the distributed staleness story. Conditions:
   per-session arenas as the blessed default, an optional per-arena random `brand` for
   anything cross-tenant, and the derive must tolerate `Handle`'s phantom type
   parameter. Std surface + docs; no semantic change.

9. **NEW — `[service]` stores owning resources** (M–L; found 2026-07-19 by C4 S4a: the
   dispatcher collision) — a `[service]` struct with a resource field (`NotesStore { db:
   Database }`) is a resource by containment, and the generated dispatcher's per-`[rpc]`
   handler closures capture `self` — an R9 reject with no clean affine form (the
   handlers escape into the `Dispatcher`). v1's answer is the §9-blessed module-level
   idiom (walkthrough refactored to it; the collision is pinned as designed rejection).
   A service that truly OWNS its resources needs handler-ownership semantics — closures
   owning the store for the service's lifetime, which is Tier-2 counted-closure
   territory or an explicit service-lifetime owner object. Design when a real driver
   demands it; do not weaken R9 for it.

8. **`Arena.get` view-form migration** (**SHIPPED 2026-07-19** — `get(&self, handle):
   Option<&T> borrows self`; `Slot.value` unwrapped to `T` because rev-1's literal
   pseudocode silently emitted a COPY (`Some(inner)` vs the recognized `Some(&place)`
   leaf); `set` stays; the arena.js golden change authorized + verified; docs updated,
   incl. a phantom `get_mut` in the tour. The migration EXPOSED item 10.)

10. **`borrows`-returned views are invisible to rule 4** (**SHIPPED 2026-07-19** —
    `rule4-completion.md`, commits 144d44e (root-set, + a chain-miscompile fix) +
    9b5e3cf (anchoring: call results + wrapped captures + the ForEach chain; ZERO
    std/corpus/docs/kolt fallout); the arena pin un-ignored; `Weak.get` (C1)
    inherits the anchoring by construction; was found 2026-07-19 by C8's rule-4
    pin as the biggest open static-model hole) — `compute_view_origins`
    tracks only direct `&place` bindings and view→view copies, and `Function.borrows`
    is a bare bool recording no projected root — so a call-returned view
    (`let v = list.at(0)`) or a wrapped-view `match` capture is never anchored, and
    E1/E2/E3 all miss invalidation under it (`list.push(x)` while `v` lives compiles;
    likewise across `await`). Pre-existing since the E-checks shipped (their pins only
    exercised direct views); semantically empty on JS, a hole in the single-conformance
    guarantee for native. Fix: the inferred `borrows` summary carries the projected
    parameter ROOT-SET; `compute_view_origins` seeds call results (mapping roots
    through the call's arguments) and wrapped-view captures; then E1/E2/E3 anchor as
    designed. Overlaps C6's `bumps` machinery (same summary). **C1's `Weak.get` ships
    the identical signature and inherits this until fixed.** `#[ignore]`d pin:
    `arena_mutation_under_a_live_get_view_is_rejected`.

## D. Documentation

2. **NEW — Docs book Phase 3** (S–M; split from D1 — the spec itself shipped, §1–§11;
   plan in `proposal/documentation.md`) — the guided walkthrough expansion + the
   glossary.

## E. LSP & tooling

3. **Per-analysis leak + incremental analysis** (L) — ≈43 KiB leaked per analysis
   (measured 2026-07-17; harness: `measure_per_analysis_leak`, `#[ignore]`d in
   `vilan-lsp`); real, not urgent. True incremental is blocked by the global
   entity/type-id counters; arena or generation-scoped ids.

4. **Sub-file incremental parsing** (L; the largest, lowest-priority LSP item) —
   tree-sitter-style reuse; impractical over chumsky's batch model — rides H6.

7. **Diagnostics audit — batch 7** (S–M; `diagnostics-ledger.md`) — the remaining 51
   unverdicted `diagnostics.push` sites + the "could not be resolved" cascade check.

11. **NEW — Cross-source diagnostic notes** (S; split from the structurally-closed
    boundary-crossing item) — a NOTE that points into `std` for a user-caused condition
    should re-anchor at the user's code (the recorded refinement on the C3 note
    mechanism).

## F. Backend & platform

3. **WASM backend** (L; far future) — emitter, backend-gated layers (`Layer.backend`),
   and the memory-model lowering (linear allocator, C4 scope-end drops, ARC for
   `Shared` + C1 `Weak`) — the heavy part, shared with F4; do it once.

4. **Native backend** (XL; far future) — C-emit vs Cranelift vs LLVM; gate behind a
   stable backend abstraction + a shared lowered IR, proven on F3 first. Absorbs C2's
   runtime check. A bundled tracing GC stays off the table (rev-1 goal #1).

5. **Project-model deferrals** (M) — registry-dependency loading (only `path`
   dependencies resolve today), `[project.dependencies]` inheritance, server-side
   manifest completions.

7. **NEW — Distribution Phase 2** (M; from the release system's recorded plan) — npm
   (then brew / VS Code marketplace) so installing vilan isn't a curl script. Pairs
   with F5 as the adoption slice; sequenced after C4's breaking change **and after A13**
   (user call 2026-07-18: HMR outranks distribution).

## G. Macros & const

2. **Const-eval tail** (S–M; recorded in `const-eval.md`) — `run`/`--watch` asset
   writes (build-only today — **A13 needs this** for CSS hot-swap), liveness-tied
   emission (= A8's dead-style elimination), Tier-2 LSP memoization, deep failure
   spans, the indirect-call conservative gap.

3. **Inferred `const`** (M; v2 of G2; constraints recorded in `const-eval.md` §5) —
   fold `let a = 1 + 2;` without the keyword: silent fallback on any evaluation
   failure, const-only functions never infer, budgets (fuel + serialized-size caps),
   the `[build]` preset split (debug = no inference). Build-only; the LSP never runs
   the sweep.

4. **The `macro_std` output contract** (M–L; renumbered — the old file carried two G2s;
   `proposal/macros-post-parse.md`, design complete, **deferred by decision**
   2026-07-16) — `Output` values over text `Source`; six open questions drafted; full
   normalization recorded as the §7 horizon.

## H. Parser & grammar

6. **Handwritten recursive-descent frontend** (L; take on trigger, not before) — a cold
   compile is still ~95% lex+parse (chumsky's structural overhead); expect 3–5× on
   parse, with the debug binary gaining most. Control is the driver (contextual
   keywords, curated errors, one fast+rich pass); LSP-grade recovery is the true cost
   center; prove with a differential harness over corpus+std+examples. Triggers:
   release builds past ~1s, LSP latency, the next grammar fight. The spec now exists to
   check it against. Unblocks E4.

7. **NEW — Interpolated triple-quoted strings** (S; split from shipped H4) —
   `i"""…"""` needs its escape story (raw braces vs `{expr}` holes conflict); settle
   it, then ship. The macro-authoring payoff.

## I. Collections

2. **Fixed-arrays tail** (M; `fixed-arrays.md` §7) — const-named / const-generic
   lengths (`[u8; SIZE]`, `<const N>`): **blocked on design** — what `const N` means,
   the constraint form (`<const N: i32>`), and the staging fork (const-eval is
   post-analysis; lengths are needed mid-fixpoint) — proposal first. Then: `List` ↔
   `[T; n]` conversions (explicit, no coercion), slicing (wants a range type), generic
   `[T; N].len() → N`.

## J. Concurrency

4. **NEW — Free-spawn lint** (S once unblocked; **blocked on Tier 2 counted closure
   environments** — refined 2026-07-19 by C4 S4b) — the rule ("a spawn happens inside a
   `nursery` extent or an `OwnedNursery.enter` — anything else is a lint") cannot ship
   while std's three legitimate free spawns remain: `Draft.commit`, the RPC SSE pump,
   and streaming `on_open` are owned by capture-based cells or host-lifecycle
   connections, which R9 forbids from holding Tier-1 resources. They become ownable
   with §10's counted captures (or a connection-lifetime owner design); the lint ships
   the same day they migrate, with zero baked-in exceptions.

1. **Part C — the parallelism spine** (L–XL; `async-polymorphism.md` Part C) — workers
   over sendability (value semantics + `Wire` make a send a copy by construction);
   native fork-join later. Part B's scopes were designed not to foreclose it. No driver
   application yet.

3. **Top-level await — the design half** (M) — the diagnostic shipped; *allowing*
   awaited module initializers = TLA on the emitted ESM (available on every host) plus
   an ordering story for dependent bindings. Design before code.

4. **NEW — Async recorded opens** (S each; from Part B's ship record in
   `async-polymorphism.md`) —
   - `Task<Task<T>>` assimilation: thenables flatten, so the type sits one level deeper
     than the runtime (Promise's old divergence, now typed).
   - Per-task cancel handles (deferred until a real need; the race idiom composes from
     nursery-scoped cancel).
   - The **free-spawn lint** (std's spawns audited + comment-marked; **blocked on C4's
     resource-owner story** — object-lifetime spawns need object-owned nurseries).
   - The adapted-instance escape: an adapted instance storing its now-async plain
     parameter into a plain field evades the per-instance divergence checks (A.4's
     recorded call-position-flow-not-escape exclusion).

---

**Recorded proposal tails** (trigger-based; tracked where they live, not re-listed
above): `numeric-types.md` §7 (f32 fround, sized parse family, numeric→`BigInt`, real
widths on native), `macro-engine.md` §11 (beyond-v1 engine tail), `hashable-keys.md`
(tuple keys, a real native hash table, identity keys), `validating-from-json.md` §7
(JSON-pointer error paths, range checks, reader dedup), `fn-coercion.md`
(generic/method coercion), and per-type `MIN`/`MAX` constants (want a static-member
story; revisit with F5/spec work).
