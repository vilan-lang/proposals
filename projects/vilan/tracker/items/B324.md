# B324 — a `context` clause written in a type position that has NO landing rule (`List<(|| i32) context c>`) is admitted and inert: the literal born there captures at creation and the restriction never sees it — sound, silent; a walk-time position gate is the wrong fix (it cannot see through a generic argument, the position B309 wanted)

- status: OPEN — Order 34's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver

A literal-side check in the context pass: 'this literal would be born under a clause the threading cannot follow' — one row; pins for the list element, a map value, and the landing-rule positions as controls.
