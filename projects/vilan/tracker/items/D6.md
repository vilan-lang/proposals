# D6 — the grammar EBNF lists `!` and `?.` but no bare `?` postfix (grammar.md ~334), and spec §5.10's heading still reads "`!` and `?.`"

- status: OPEN — lane b230's find (2026-09-04, Order 27)
- kind: docs
- area: Documentation

The expression lift's bare `?` has been shipped since 2026-07-16 without a production. Add the postfix to the EBNF and retitle §5.10 (B230 added the slot inventory to its body); the `grammar_ebnf` and `grammar_sync` gates and seven cross-references move with it.
