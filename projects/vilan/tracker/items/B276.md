# B276 — a leg that BUNDLES anything is never `Fresh` under `build --watch`: `asset::bundle` records `content_hash_bytes(&bytes)` (const_eval.rs ~487) while `current_source_hash` re-hashes with `read_source` + `content_hash`, and the two never agree, so `leg_is_current` always fails — kolt bundles heavily and its watch reuses nothing today; reproduced under `VILAN_SEQUENTIAL_BUILD=1`, so pre-existing (M22/E12 class)

- status: OPEN — Order 30's finds (2026-09-09)
- kind: bug
- area: CLI & workflow

One hash function for both sides; pin a bundling leg `Fresh` on an unchanged round.
