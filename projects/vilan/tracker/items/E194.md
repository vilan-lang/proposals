# E194 — a HYPHENATED completion label is filtered out by the client's default word pattern: `editors/vscode/language-configuration.json` sets no `wordPattern`, so VS Code's default excludes `-` — at `<svg stroke-w|` the client's word is `w` and `stroke-width` does not match; `Completion` carries no `filter_text` and the server sends no `textEdit` range. Pre-existing since E153's css property vocabulary (`flex-` → `flex-direction`, whose pin asserts only the SERVER's list); E69's attribute table makes it visible at every element head

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: editor
- area: LSP & tooling

Either a `wordPattern` admitting `-` in the language configuration (vscode_extension pin) or a `filter_text`/edit-range field on `Completion` consumed by both front-ends; a pin at the client level if the harness can express one, else on the server's `filter_text`.
