# N113 — the `void`/`()` edges, four holes: (1) `()` is accepted as a TYPE and is uninhabited — nothing can be written at it — where `void` is the unit; refuse it by name pointing at `void`; (2) `void` is missing from the EBNF's literal production though the lexer and spec have it; (3) `let void = 3;` is ACCEPTED — the unit value's name is shadowable; (4) `[extern("document.activeElement")]` emits a CALL to a PROPERTY (the `[extern(get, …)]` form is the spelling — the book does not say so)

- status: OPEN — filed 2026-09-21 (Order 39 sweep; papers-39's LOW finds ×4, found on the way to B363's correction)
- kind: hygiene (spec + parser, four small holes)
- area: Hygiene & rot

(1) parser: `()` in type position → 'the unit type is spelled `void`'; a ledger row. (2) EBNF + `grammar_sync`. (3) `void` joins the reserved words (or the binder refuses it by name); the `specs_reserved_words_are_the_lexers_keywords` pin moves. (4) one paragraph in the extern chapter with the property form. Four pins, one commit each; sized S total.
