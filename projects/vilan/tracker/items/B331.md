# B331 — the loader resolves a module's BODY FILE under two names: `import pkg::a::lib` resolves `a/lib.vl` a second time as the module `a::lib` (`submodules_in_directory` excludes `lib` deliberately; `resolve_module_file`/`longest_module_prefix` do not), so A67's rule had to skip `lib` by hand to avoid a second diagnostic for one root cause

- status: OPEN — Order 34's finds (2026-09-13)
- RULED 2026-09-13 (owner, Order 35 GO, R3): REFUSE `lib` as a segment when the directory is a module's body (one row, a steer to the parent path). BUILD: lane lang-35.
- kind: bug
- area: Type system & the type solver

`resolve_module_file` refuses (or aliases) `lib` as a segment when the directory is a module's body; one row or a silent alias — decide; pin `import pkg::a::lib::x` and `import pkg::a::x` agreeing.
