# N111 — Order 38's smalls: analyzer.rs:28280 still records the walk frame as '~36 KiB / ~4.5 KiB' (N97 re-keyed it to 42,464 B / ~4,650 B everywhere else); `vilan build` exits before `depth_stats::report()` on a parse-bound refusal, so the depth line is missing for exactly the analyses the bound exists for; `vilan run <package>` writes `/tmp/vilan-run-<pid>.mjs` (N103's family); `json_boundary.rs` is a usage census, not a behaviour harness (fix the pin-location list); `Map`/`Set` have no `impl Js` in vilan-rt (rides F20)

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: hygiene
- area: Hygiene & rot

One line each; a lane's first hour.
