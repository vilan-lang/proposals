# B263 — `derive_generics_from_bounds` inside an IMPL body: `impl Held<type T> { fun first(self): T { self.list.get()[0] } }` reports "cannot index this List: its element type is never determined" — the impl binder's `T` is not recovered through the sibling parameter's bound the way B251 now recovers it at the literal

- status: OPEN — lane solver-29's find (2026-09-06, Order 29)
- kind: bug
- area: Type system & the type solver

Run the bound-driven recovery when an impl body reads a field typed by a bounded sibling parameter; pin the exhibit and a generic method control.
