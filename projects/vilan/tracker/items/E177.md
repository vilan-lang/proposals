# E177 — `Document::QuickFix` is SINGLE-FILE, so the visibility warnings' 'Export `S`' / 'Export `hidden`' fixes ship as message steers only (the two same-file fixes — `Import as #leaf`, `Delete the #` — ship); a cross-file fix needs an optional target path + the other file's line index on `QuickFix` and a multi-file `WorkspaceEdit`

- status: OPEN — Order 35's finds (2026-09-13)
- kind: editor
- area: LSP & tooling

Widen `QuickFix` with a target document; the Export fix edits `S`'s declaration wherever it lives; pin the cross-file edit through the code-action handler.
