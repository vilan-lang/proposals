# F44 — native — a CLOSURE pushed into the list it READS is refused by rustc as a RECURSIVE type: `let cell: Shared<List<|| i32>> = Shared::new([]); let same = cell; cell.write().push(|| same.read().len());` (the closure's type contains the list that contains the closure); the emitter needs a boxed closure type at that position

- status: OPEN — filed 2026-09-25 (Order 42, native-42's find)
- kind: bug (native)
- area: Native targets & rendering

Fix: closure-typed list elements lower to `Rc<dyn Fn…>` (a boxed dyn) when the closure captures the container; pin red-first; refused by name meanwhile. Sizing S–M.
