# N80 — N66's other half: `reused_source` and `container_site_visited` are two spellings of ONE predicate in the replay harness; fold them into one with a name that says what it decides

- status: OPEN — Order 34's finds (2026-09-13)
- kind: hygiene
- area: Hygiene & rot

One predicate, one doc comment, the differential pins unchanged.
