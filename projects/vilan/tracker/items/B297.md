# B297 — an unresolved name in an impl SUBJECT is reported TWICE: `impl Source<Option<Nope>>` → two `Error:` lines for one mistake, where `let x: Option<Nope>` reports once — the subject is walked twice in `Node::Impl` (`walk_trait_position_type_node(subject, ..)` and again per argument for `impl_subject_args`) and each walk preps its own `prepped_type_locals` entry; pre-existing (it is how `impl Source<Option<_>>` drew its refusal twice at 65af4be0)

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Type system & the type solver

Walk the subject once and reuse its locals for the argument pass, or dedupe the resolution diagnostic by (span, name) at the second walk. Pin: one error for `impl Source<Option<Nope>>`; the `let` form as the control.
