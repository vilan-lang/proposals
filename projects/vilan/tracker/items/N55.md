# N55 — 96 `.vl` files (7 under std) already differ from their own formatter; there is no repo-wide `vilan fmt --check` gate. Decide: a CI gate after one reformat, or an explicit hand-formatted statement

- status: OPEN — lane parse-fmt-27's find (2026-09-04, Order 27)
- RULED 2026-09-05 (owner): ADD the gate — one reformatting commit (review the diff) and a `vilan fmt --check` leg in ci.yml and in scripts/ci-local.sh (L19). Order 28, L19's lane.
- kind: design
- area: Hygiene & rot

E137's reflows were applied only to the one gated file (the fullstack template); the other six live in files already unformatted. A gate means one reformatting commit (review the diff; the formatter's own contract is 284 in-file asserts + `parse_differential`) and every later lane keeps it; no gate means the formatter's output is advisory and the tree says so.
