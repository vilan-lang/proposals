# B278 — an i-string hole cannot contain an ESCAPED QUOTE: `i"{x.get(\"k\")}"` is a parse error, so a call with a string argument inside a hole must be bound first; cost a lane an iteration

- status: OPEN — Order 30's finds (2026-09-09)
- kind: bug
- area: Type system & the type solver

Let the hole scanner track string literals (lexical.md §3.4's hole grammar already says `expression`); pin the escaped quote and a nested string inside a hole; formatter round-trip.
