# B376 — `[derive(PartialOrd)]` is SILENTLY ACCEPTED and expands to NOTHING — `STD_DERIVE_MACROS` lists six names but `compare.vl` declares only a `PartialEq` macro; a derive name std declares no macro for should be refused BY NAME at the attribute. Likely the shape behind the owner's original E212 report

- status: OPEN — filed 2026-09-21 (Order 39 sweep; editor-39's find 1)
- kind: bug (attributes)
- area: Macros & derives

Repro: `[derive(PartialOrd)] struct P { x: i32 }` → clean; `a < b` on `P` then fails with 'no operator', pointing nowhere near the derive. Fix: the derive expander refuses a name with no registered macro ('`PartialOrd` is not a derivable trait; std derives: …'), anchored on the attribute; a ledger row. `STD_DERIVE_MACROS` and the registry should be ONE list (the test that keeps them in step is the pin). Sized S.
