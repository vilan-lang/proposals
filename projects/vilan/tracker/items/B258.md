# B258 — MISCOMPILE: overriding a trait DEFAULT (`map`) in a sub-trait impl (`impl RemoteSource<type T> with Source<Option<T>>`) reddens A25's owner pins — a call on a `SignalCell` FIELD that INHERITS the default reaches `register_with_owner` with an undefined owner (`self[0].v.push` on undefined); "overriding a default the receiver's own field type inherits" is the breaking shape

- status: OPEN — lane rpc-29's find (2026-09-06, Order 29)
- kind: bug
- area: Type system & the type solver

Reproduce with the mirror's override restored (the doc comment in rpc.vl names the shape), find the dispatch that routes the field's inherited default through the OVERRIDE's body (or the override through the field's owner), fix, and pin both: the field call with the override present, and a user sub-trait impl overriding a default its own field type inherits.
