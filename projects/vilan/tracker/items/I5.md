# I5 — ONE spelling for an index: `usize`. std spells positions, lengths and counts as `i32` almost everywhere (~100 signature sites — `List::len(): i32`, `insert(index: i32, ..)`, `remove(index: i32)`, `get(index: i32)`, `str::len`/`repeat(count: i32)`, `Bytes::slice(from: i32, to: i32)`, `Iterator::take(count: i32)`, `index_of(): Option<i32>`, `Map::len`, `Set::len`) and as `i53` in a dozen more, so a position can be NEGATIVE by type, two halves of std disagree, and ids (`u53`) and indexes meet with casts. The owner's ruling-in-waiting (2026-09-21): switch std to `usize` for every index — one spelling everywhere — with `usize` = `u53` on JS targets unless revisited

- status: OPEN — filed 2026-09-21 (the owner's ask, mid-Order 39); PAPER first — this is the widest breaking surface since A101
- kind: design (breaking)
- area: Collections

Two decisions, then a migration.

**1. What `usize` IS.** (a) a DISTINCT numeric type whose representation is per target — `u53` on the JS backends (the owner's default), the platform word (`u64`/`usize`) on the Rust backend, where `Vec::len` already is one (the native emitter casts every `len()` today: `((xs).len() as i32)`) — with the documented rule R6 of Order 37 gave `i53`/`u53`: the RANGE guarantee is the JS one, so a program round-tripping both backends behaves the same; or (b) a plain ALIAS of `u53` (cheaper; loses the ability to revisit without a second break; type aliases — does the language have them?). Rec (a). It joins the numeric family in number.vl (`Add`/`Sub`/`Ord`/`Display`/`Default`/`Hashable`/`Wire`/`Json`, the `as_*` conversions, literal typing: does an unsuffixed `0` infer to `usize` in an index position? it must, or every `list[0]` needs a suffix).

**2. What is an INDEX.** Positions, lengths, counts, capacities, arities, `take`/`skip`/`repeat` counts, `slice` bounds, `enumerate`'s counter, `index_of`'s answer, `Region`/row positions in std::ui, `KeyedCell.positions`, A112's `Splice(at, ..)` (NOT yet built — it should be BORN `usize`; tell collections-39's S2), the indexing operator `list[i]`. NOT an index: ids (`u53` stays), byte VALUES (`Bytes::get` answers an `i32`-typed byte — its INDEX changes, its answer does not), durations, numeric payloads, scale steps, wire integers.

**The hard parts, which are why this is a paper.** (i) SUBTRACTION: `i - 1` at `i == 0` and the downward loop `for i > 0 { .. i -= 1 }` (kolt lib/storage.vl had one until A73) — unsigned arithmetic needs a ruling: panic on underflow in debug, wrap, or saturate; and `checked_sub`/`saturating_sub` in std. JS's `u53` has no native wrap, so whatever is chosen is EMITTED. (ii) SENTINELS: any `-1` 'not found' (census them — `index_of` already answers `Option`, good) dies. (iii) INFERENCE at the boundary: `let n: i32 = list.len()` and `list[i]` with `i: i32` become type errors in every program ever written — the migration needs a CODEMOD (A101's precedent: the `css` call codemod touched 227 blocks with no golden moved) and a diagnostic that names the conversion (`as_usize()` / `as_i32()`), plus a quick fix. (iv) the WIRE: a length or index that crosses rpc (`KeyedCell`'s positions in a `Patch` frame, `Delta`) has a wire width — `usize` needs a Wire lane or rides `u53`'s; the frames must stay byte-identical or the contract hash moves (say which). (v) the NATIVE backend gets SIMPLER (no casts at `len`), the JS backend's emitted code should not change at all for the common paths (a number is a number) — the corpus goldens are the measure. (vi) the DOCS: every tour/guide fence indexing a list.

**Census owed by the paper** (exact, by parse not grep): every std signature position of index kind, by module; every estate program that names an index type explicitly (std, vilan/test, examples, docs fences, benchmarks, templates, the `.vl` consts in Rust tests, vilan-website, vilan-playground, kolt — a first grep finds kolt naming none explicitly, which suggests inference carries most user code across untouched); every `-1` sentinel; every downward index loop.

**Slices (proposed).** S1 the type + number.vl's family + literal inference + the underflow rule, no std signature moved (S–M). S2 the std signatures + the codemod + the estate, ONE breaking commit merged last in its order (L). S3 the native backend drops its casts; S4 A112's ops and std::ui's positions if not already born so. The v0.41.0 cut is HELD and already carries A101 and A108 — this belongs in the same breaking train, BEFORE the cut, or it waits a whole release.

**Rulings the paper asks.** distinct type or alias; the underflow rule; whether `isize` exists at all (rec: no — a signed offset is `i53`); whether `len()` on the WIRE-visible types changes width; whether an unsuffixed literal in index position infers `usize`.

- 2026-09-21 (the owner's rulings, the same day — sent under the heading "A101", read as this item since they answer its questions one for one):
  1. **DISTINCT TYPE** — `usize` is its own numeric type (`u53` on JS targets unless revisited; the platform word natively), not an alias.
  2. **SUBTRACTION MAY BE UNDEFINED BEHAVIOUR** — no check is emitted for an underflowing `usize` subtraction. Recorded with the integrator's reading, for the paper to state precisely: "undefined" means the RESULT is unspecified (on JS the number simply goes negative and leaves the type's range; natively Rust's own rule applies — a panic in a debug build, a wrap in release), NEVER memory-unsafe (`vilan-rt` forbids `unsafe`; an out-of-range index still meets the bounds check every backend already has). Consequences to write down: the native differential cannot cover a program that underflows (outputs may differ by backend, legitimately); `checked_sub`/`saturating_sub` are still worth having for code that wants a defined answer; a downward loop is written `for i > 0 { i -= 1; .. }`-style and never reaches the edge.
  3. **LITERAL INFERENCE IS A GENERAL LAW, NOT AN EXCEPTION FOR `usize`** — an unsuffixed literal takes its type from context everywhere. Probed the same day: it already does in 14 of 16 positions; the two gaps (a literal on the LEFT of a binary operator; the elements of an annotated list literal) and the census of the rest are filed as **B370**, which this item's S1 depends on.
  4. Everything else in the item as proposed: what counts as an index (and what does not — ids, byte values, wire integers), the codemod + naming diagnostic + quick fix for the migration, A112's `Splice(at, ..)` born `usize`, no `isize` (a signed offset is `i53`), and the timing — in the breaking train BEFORE the held v0.41.0 cut.
  Still the paper's to settle: whether a length or position that crosses rpc keeps its wire width (frames byte-identical) or moves the contract hash. READY for a paper lane in Order 40; S1 (the type + number.vl's family) can start once B370's two gaps are closed.

- 2026-09-22 (the owner's rulings before Order 40): RULED (the last open question) — a length or position that crosses rpc KEEPS its wire width: frames byte-identical, the contract hash does not move; the paper proves it (a `Patch`/`Delta` frame before and after, byte-compared).

- 2026-09-22 (Order 40 sweep): PAPER LANDED (papers-40, proposals 4cde759): `proposal/index-type.md` (785 lines): by parse, 105 index-kind positions (86 signatures + 19 fields), ALL `i32`; `i53` = fs offsets (a STREAM kind, stays); three boundaries the item did not name (WIRE 61, STREAM 10, SEQUENCE 5); estate 251 explicit namings in 56 files, 207 inside Rust-test consts, 0 on website/playground, 3 in kolt; 7 `-1` sentinels; 8 downward loops that break. WIRE PROOF: a naive `u53` moves a `Patch` frame 104 → 108 bytes; `usize` writing via `i32_value` is byte-identical — the rule is load-bearing because the contract hash does NOT see a frame-width change (a user rpc parameter respelled DOES move it — ruling 5 holds with that sentence). Emission: an `i32` loop and a `u53` loop compile byte-identically, so corpus goldens measure S2. FIVE literal-inference positions B370 did not close → B389 (S1 depends on it). Underflow: JS prints -1 and runs on, native panics in debug — the differential cannot cover such a program; `__at`'s `index >= 0` guard is what makes 'never memory-unsafe' true. Q1–Q5 with recs (§11): file offsets stay `i53`; a `usize` in a contract surface lets the hash move; `usize::max_value()` = the JS guarantee on every backend; the native refusal wording (N120); a `42usize` suffix yes. Rec S1 and S2 in SEPARATE orders. Also from the census: A126 (no `Display` on the sized family), E218 (the missing `as_*` steer), B388 (native RHS width — fixed by native-b-40).

- 2026-09-24 (Order 41 GO): R-b RULED as recommended — §11 Q1 `fs.vl`'s STREAM offsets stay `i53`; Q2 a `usize` in a user contract surface LETS the hash move (rebuild both halves); Q3 `usize::max_value()` answers the JS guarantee (2⁵³) on every backend; Q4 the native refusal wording is N120's; Q5 the `42usize` suffix yes. AMENDMENT to ruling 4 (the owner, at GO): `isize` MAY EXIST too if useful — a signed offset is no longer forced to `i53`; index-41 decides whether the family's tables make it free and says which (rec: build it only if it costs nothing beyond the tables; a signed offset stays `i53` where one exists today). R-a RULED: S2 (the breaking train) is Order 42's, merged last, the v0.41.0 cut at its seal; Order 41 lands S1 + A126 + E218 and PREPARES S2 as a codemod diff under sweeps/order41/index-41/.

- 2026-09-24 (Order 41, index-41): S1 BUILT (12750bb0 + 7c746d47, B389 94a648d1 beneath): `usize` in type_.rs's three tables, the numeric rows of analyzer/transformer/bindgen, the native scalar map (Rust `usize`; vilan-rt needed nothing), grammar_sync + the book theme; number.vl's §2.3 family incl. `as_usize` on the eleven others, `checked_sub`/`saturating_sub`, `Hashable`/`Json`/`FromJson`/`Debug`/`Display`; `Wire` on `i32`'s lane (pin reds on the `i53` lane); `usize::max_value()` = 2⁵³ on every backend; the `42usize` suffix needed no lexer change; the SUBSCRIPT admits `usize` beside `i32` (expectation stays `i32` — emitted subscripts unchanged; S2 deletes the admission); 11 book pages incl. the underflow rule. Pins: 20 in `inference/index_type` (19 red before), corpus `usize.vl` (native default suite), `usize-underflow.vl` (named OUT of the differential; JS prints -1, native debug panics), `usize-literals.vl` (21 positions, identical on both backends; at `usize` 19 follow the law, `get`/`len` are S2's boundary). Whole set 123: 85 identical / 38 refused / 0. `isize` NOT built — the tables would accept it but it is not free (its own wire-width and range rulings; nothing in the estate asks); signed offsets stay `i53`. S2 PREPARED at sweeps/order41/index-41/ (`run_s2.sh` over scratch copies; `compiler.patch` flips the subscript + re-keys ledger row 558; the codemod `i5_s2_codemod.rs`; `hand.patch`; NOTES.md): 105 signatures by parse, none unmatched; 214 codemod edits (169 E218 conversions — 60 `.as_i32()` at WIRE positions, none `.as_i53()`; 41 operand conversions; 4 counters); the 7 sentinels + 8 loops by hand + census misses (`Enumerated`'s trait argument, annotated locals, dead `from < 0` clamps, an `index_of` underflow in document.vl); residue ZERO in std both platforms, macro_std, 141 corpus programs; goldens 121 identical / 17 MOVED (§5.4 predicted none — each move a conversion or a hand-rewritten std loop, classified) / 0 fail; examples 11 clean; diff 40 files +379 −332; KOLT reaches FIVE files (rotary.vl first, command_palette.vl + theme.vl underflow on `len() - 1` over an empty list, search.vl, server.vl). Not in the 105 (Order 42 S4): macro_std's `Arguments::len`/`get`, enum payloads `RowStep`/`Delta`. Finds: B406, B407, I6 (RULING), F36.

- 2026-09-25 (Order 42 GO): R-b RULED — `isize` stays UNBUILT (not free: its own wire-width and range rulings; nothing in the estate asks). S2 + I6 + S3 + S4 are index-42's, ONE breaking commit, the train's first car; the cut follows.

- 2026-09-25 (Order 42, papers-42): index-type.md corrected (57d0b6b): §5.4 — 17 of 138 goldens MOVE under S2 (four kinds of cause), which INVALIDATES cut-plan §3.1's 'S2 must move no JS golden' (the merge regenerates and classifies them); §9 gains S1's as-built (re-probed on d65d4e759: the subscript accepts `usize` and `i32`, `len()` still answers `i32`, `isize` is 'cannot find type') and S2's prepared numbers (kolt FIVE files, not three). ⟦INTEGRATOR⟧ placeholder for S2 at the sweep.

- 2026-09-25 (Order 42, index-42 interim): S2 RUN on d65d4e75, NOT LANDED (waits on solver-42's merge to rebase): committed I6 ae3970ef (saturate_unsigned at 14 sites; corpus unsigned-saturate.vl), the naming diagnostic + quick fixes b5d949b2 (2 NEW ledger rows; `vilan_ide::numeric_fix` shared by LSP + CLI; TWO quick fixes; NEW CLI surface `vilan check --fix` = the fixed-point driver; the appendix count word → Eighteen — integrator folds), 6ae45acd; the estate move UNCOMMITTED: 105/105 signatures, 241 codemod edits on this base (+27 drift vs index-41), `pre.patch` applies S4 first; residue ZERO; hand edits: 7 + 1 sentinels (the 8th: ui.vl's focus wrap `0 - 1`), 8 loops, census misses, dead clamps, `len() - n` guards (List::last, index_of ×2), DeltaLog trim/since boundary, corpus sentinels; S4 complete (markers zero; wire frames byte-identical, hash be3fd1fe); S3: `Emitter::intrinsic` casts gone (35 → 0), vilan-rt intrinsics take/answer usize, an emitter fix (a loop inside an arm renders under no expectation — pinned). GOLDENS: 29 + the split fixture move (not 17) — all runtime-identical, classified — ACCEPTED by the integrator. Rust-test sweep: 126 failures fixed except ~90 inference ones a helper agent is migrating. KOLT: five files, 15 errors → `kolt-i5.patch` restores A125's 4; playground 0; website 0 (its two A99 errors pre-existing). reactive.vl carries index edits (reactive-42 told).

- 2026-09-25 (Order 42, index-42): S2 + S3 + S4 LANDED on the branch (tip 98039c83 on eafd61ef; ONE breaking commit 98039c83 carrying every hand edit and the golden classification; I6 c7129152; the diagnostic + quick fixes + `vilan check --fix` 36db895a; 086170ba): 105/105 signatures, 136 E218 + 25 operand + 4 counters, residue 0 in std (both platforms), macro_std, 140 corpus programs; the two rpc.vl `0usize` suffixes KEPT (B406's residual); B407 leaves no `-1` at an unsigned type in the estate (six pins, three docs fences, two corpus programs rewritten; `remove(-1)`/`take(-4)` pin the refusal) — but a CONSTANT `0 - 1` escapes it → B426; the two markdown pins re-derived to ruling 2's bounds-check refusal; A129's `live_ops` `mut last = -1` was a ninth sentinel → `Option<usize>`; goldens: 32 differ = the 30 classified + usize.mjs + unsigned-saturate.mjs, all runtime-identical; split + three censuses regenerated; gates: union 8,631/8,631, natdiff 60/60 env, doc/clippy/fmt 0, grammar/book_sync 79/79. Appendix count word Eighteen. kolt-i5.patch restores A125's 4 on a scratch copy; website/playground 0 I5 lines. MERGING onto next @badf2bfb.
