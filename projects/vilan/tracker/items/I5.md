

- 2026-09-24 (Order 41, index-41): S1 BUILT (12750bb0 + 7c746d47, B389 94a648d1 beneath): `usize` in type_.rs's three tables, the numeric rows of analyzer/transformer/bindgen, the native scalar map (Rust `usize`; vilan-rt needed nothing), grammar_sync + the book theme; number.vl's §2.3 family incl. `as_usize` on the eleven others, `checked_sub`/`saturating_sub`, `Hashable`/`Json`/`FromJson`/`Debug`/`Display`; `Wire` on `i32`'s lane (pin reds on the `i53` lane); `usize::max_value()` = 2⁵³ on every backend; the `42usize` suffix needed no lexer change; the SUBSCRIPT admits `usize` beside `i32` (expectation stays `i32` — emitted subscripts unchanged; S2 deletes the admission); 11 book pages incl. the underflow rule. Pins: 20 in `inference/index_type` (19 red before), corpus `usize.vl` (native default suite), `usize-underflow.vl` (named OUT of the differential; JS prints -1, native debug panics), `usize-literals.vl` (21 positions, identical on both backends; at `usize` 19 follow the law, `get`/`len` are S2's boundary). Whole set 123: 85 identical / 38 refused / 0. `isize` NOT built — the tables would accept it but it is not free (its own wire-width and range rulings; nothing in the estate asks); signed offsets stay `i53`. S2 PREPARED at sweeps/order41/index-41/ (`run_s2.sh` over scratch copies; `compiler.patch` flips the subscript + re-keys ledger row 558; the codemod `i5_s2_codemod.rs`; `hand.patch`; NOTES.md): 105 signatures by parse, none unmatched; 214 codemod edits (169 E218 conversions — 60 `.as_i32()` at WIRE positions, none `.as_i53()`; 41 operand conversions; 4 counters); the 7 sentinels + 8 loops by hand + census misses (`Enumerated`'s trait argument, annotated locals, dead `from < 0` clamps, an `index_of` underflow in document.vl); residue ZERO in std both platforms, macro_std, 141 corpus programs; goldens 121 identical / 17 MOVED (§5.4 predicted none — each move a conversion or a hand-rewritten std loop, classified) / 0 fail; examples 11 clean; diff 40 files +379 −332; KOLT reaches FIVE files (rotary.vl first, command_palette.vl + theme.vl underflow on `len() - 1` over an empty list, search.vl, server.vl). Not in the 105 (Order 42 S4): macro_std's `Arguments::len`/`get`, enum payloads `RowStep`/`Delta`. Finds: B406, B407, I6 (RULING), F36.
