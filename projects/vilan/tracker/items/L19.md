# L19 — CI's verdict takes 30 minutes because both test legs compile the workspace COLD (ubuntu 25 min, windows 30; every other job under 3): a local gate script shared with ci.yml, rust-cache + nextest partitions on GitHub, the Windows leg on the owner's host

- status: OPEN — lane orchestrator's find (2026-09-04, Order 27)
- kind: tooling
- area: Release engineering & beta

The last green run: changes/wasm/fmt/clippy/audit/check all ≤ 3 min; test(ubuntu) 25 min; test(windows) 30 min. The same suite runs in ~4.5 min on the owner's box on a warm build — the wall clock is compile time on a small runner with NO Rust build cache in the workflow. Repo is PUBLIC, so a self-hosted runner is out (GitHub's own guidance). Queued: (1) `scripts/ci-local.sh` — the Linux legs verbatim (nextest --workspace, doc-tests, clippy -D warnings, audit --deny unsound, the x86_64-pc-windows-msvc cross-check) and ci.yml CALLS the same script so the two cannot drift; the orchestrator's seal script becomes it; (3) `Swatinem/rust-cache` on both test legs and nextest split into two partitions per OS (`--partition count:1/2`), expected 25–30 → ~8–12 min; keep GitHub as the seal's final word. The owner's own step (2): the Windows leg natively on the WSL2 HOST — install rustup + the msvc toolchain + cargo-nextest on Windows, then from WSL `powershell.exe -c "cd <repo>; cargo nextest run --workspace"` (a `scripts/ci-windows-host.sh` wrapper once it works) — the one leg that is compile-only locally today. `act` (nektos) is optional on top for a clean-environment run of the ubuntu jobs; it cannot run the Windows job.
