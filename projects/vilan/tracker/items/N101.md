# N101 — two recorded FRAME figures do not reproduce and one doc claim miscounts: `deep_nesting.rs:108`'s `parse` frame says ~71.8 KiB/level unoptimized but `VILAN_DEPTH_STATS` over 30 → 130 parenthesis levels measures 32.8 KiB/level (0.98 MiB at depth 34, 4.18 MiB at 134) — either the frame shrank or the figure predates the counter's move to `parse_nested_as`; `reactive_lifetimes.rs`'s headnote says the mounted exemplar holds 'V4 plus two live-session loops' (three) where the walk measures 2; N97 re-keyed the walk's figure (42,464 B/level, was '~36 KiB') the same way

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: hygiene
- area: Hygiene & rot

Re-measure the parse frame with N97's method and re-key the comment (a canary if the parse walk deserves one); fix the reactive_lifetimes headnote to what the walk records.
