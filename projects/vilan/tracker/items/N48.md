# N48 — std's own dead code: two sites in `rpc.vl` the gray-out's first corpus run found

- status: OPEN — lane e114-rest's find (2026-09-01)
- kind: hygiene
- area: Standard library

`vilan/std/src/rpc.vl:1252` — `let (channel, subscription) = entry;`
whose `channel` is never read (rename `_channel` or drop the
destructure); `rpc.vl:308` — `0 - 1 // unreachable — the panic
throws`, dead after the `panic` (delete; if the checker needs a
value there, that is B204's question). Fix both; run the docs and
corpus gates; std should be clean under its own paint.
