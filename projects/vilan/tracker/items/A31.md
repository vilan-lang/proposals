# A31 — every terminal `Closed` disposes: the third path joins the ruled two

- status: OPEN — RULED 2026-08-29 ("the most correct / most stable option"):
  the uniform law. Build-ready, with plumbing
- kind: feature
- area: Reactive core & UI
- source: lane a30's scope note + the owner's ruling, 2026-08-29

A30 wired dispose onto `reattach_mirrors`' two refusing arms; the THIRD
terminal `Closed` — `handle_drop`'s spent-retry-budget arm — sets the state
and disposes nothing, because no `ReactiveClient` is in scope there. The
ruling makes the law uniform: a connection that reaches terminal `Closed`
takes its client's graph with it, on every path that can say so. The work
is the plumbing (thread the client into `handle_drop`, or hoist the
terminal treatment to one place all three arms reach — prefer the hoist:
one `close_for_good`, three callers, no path forgotten by construction)
plus the pin: the retry-exhaustion path on the A26 harness (a server that
dies and STAYS dead through the whole budget), asserting disposed, beside
A30's redial negative which must stay green.
