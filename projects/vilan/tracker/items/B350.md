# B350 — the `#`-on-an-already-EXPORTED-impl redundancy warning (the impl twin of S2's "the reach marker is redundant — delete the `#`") is not built: `import a::{ #(impl T) }` where `a` exports the block earns no warning; a warning, not a refusal, and the symmetry B318 §14/§15 leave open

- status: OPEN — filed 2026-09-15
- kind: feature
- area: Type system & the type solver

One warning row + the delete-the-# fix widened to the selector; pin.
