# N39 — `ISTRING` is a token class the grammar uses that §2 never declares

- status: OPEN — records lane's find (Order 23)
- kind: process
- area: Hygiene & rot
- source: lane records, 2026-08-31; recorded in grammar_ebnf.rs TOKEN_CLASSES_NOT_IN_LEXICAL

`spec/lexical.md` §2 describes `i"…"` in prose and writes a
production only for its multiline twin, while §3 uses the ISTRING
class normatively. Write the §2 production (the fix widens into §2's
structure, which is why the EBNF gate recorded rather than fixed it)
and drop the class from the gate's exemption list when it lands.
