# B363 — `void` IS vilan's unit value and nobody said so where it matters: `Ok(void)`, `Result<void, E>`, `Option<void>`, a generic at `void` all build and run (spec/types.md:27; std's own option.vl builds `Verdict::Bad(void)`) — only the `()` SPELLING fails (`found ')' expected an expression`), and `()` parses as an UNINHABITED type. FILED ON A FALSE PREMISE ('no unit literal', wire-38 → the integrator → the owner): A107's awaited-void stub answers `Option<RpcError>` because of a probe that tried only `Ok(())`

- status: OPEN — filed 2026-09-21 (Order 38 sweep) as 'no unit literal'; PREMISE FOUND FALSE 2026-09-21 by lane papers-39 (Order 39) and verified by the integrator on c3f7d1a38; REWRITTEN the same day
- kind: documentation + diagnostics + one breaking follow-up
- area: Type system & the type solver

## The correction

`fun remove(ok: bool): Result<void, str> { if ok { Ok(void) } else { Err("refused") } }` builds and prints `ack: Ok` / `ack: refused` (the integrator's own probe). `parsing.rs` calls `void` "the unit value"; spec/types.md:27 declares it; spec/lexical.md:194 lists it under other literals. All nine positions Order 39's brief named work (`proposal/unit-literal.md`, probes at `sweeps/order39/papers-39/probes/b363_*`). What is actually wrong is small:

1. `()` is refused with `found ')' expected an expression` — no mention of `void`; it should be refused BY NAME pointing at `void` (NOT admitted as a synonym);
2. a `()` TYPE parses and is uninhabited, against spec/types.md:15 (zero estate hits — deleting it breaks nothing);
3. `void` is missing from grammar.md:416's `literal` production, so the EBNF describes it as an ordinary identifier (`grammar_sync` goes red against the corrected production — a free non-vacuity proof);
4. `let void = 3;` compiles and the binding is unreadable, against lexical.md §2.2;
5. three places now state the falsehood as a language fact and must be corrected: `rpc.vl` ~1434, `guide/services.md` ~198, CHANGELOG.md ~161 (A107's entry);
6. `void` has no `PartialEq`, which blocks `Option<void>`/`Map<K, void>` equality — `impl void with PartialEq` (the direct-operand refusal fires before dispatch and is not weakened).

## The breaking follow-up (ruling asked)

A107's stub should answer `Result<void, RpcError>` like its siblings, not `Option<RpcError>`: two vilan source lines in `rpc.vl::call_ack`, four comments, one pin, two kolt lines (`model.vl`'s wrapper becomes `get_client().get_safe()!.remove_message(..)` again, `channel.vl` matches `Err`), no wire byte and no hash. It shipped one order ago and is unreleased (the cut is HELD), so the break costs kolt only. Rec: YES, Order 40, with a `breaking` note. `void` gets NO `Wire` impl — there is one awaited-void shape and it is the ack.

## Slices (from the paper)

S1 the two refusals + docs/EBNF + the three corrected sentences (S) · S2 `impl void with PartialEq` (XS) · S3 A107's return type (S, breaking).

## Process note

The false premise travelled through three hands in one day — a lane's probe (`Ok(())` only), the integrator's acceptance of the lane's deviation, the owner's summary — and into kolt's source. The Order 39 Mechanics rule 'your item's text is a HYPOTHESIS' caught it on the first paper written under it.

- 2026-09-21 (the owner, mid-Order 39): "Make A107's stub answer `Result<void, RpcError>`" — RULED YES and BUILT the same hour by the integrator as a one-commit lane (`void-result-39`, 63161d83, merged to next as 0976fd19, all gates green: `service_layer`, `rpc_http`, `corpus`, `native_differential`, `docs`, the std scan, release_scripts, split): `rpc::call_ack` answers `Ok(void)` / `Err(error)`, the generator declares `Result<void, RpcError>`; no wire byte and no contract hash moved; the guide's services page, `call_ack`'s documentation, both pins, two test comments and A107's OWN changelog entry corrected; a `breaking` entry with the migration. **S3 is DONE; items 5's three false sentences are gone from the tree.** Toolchain `vilan 0.40.0 (0976fd194)` installed in both locations; kolt migrated (`model.vl`'s wrapper is `get_client().get_safe()!.remove_message(..)` again; `channel.vl` matches `Err`) — `vilan check` 0/0, build clean, uncommitted (the owner's). REMAINING: S1 (the `()` refusal naming `void`, the uninhabited `()` type, the EBNF's `literal`, `let void = 3`) and S2 (`impl void with PartialEq`).

