# B363 — vilan has NO UNIT LITERAL: `Ok(())` does not parse (`unclosed ( in expression`), so `Result<void, E>`, `Option<void>` and any generic instantiated at nothing have no constructible value — which is why A107's awaited-void stub answers `Option<RpcError>` rather than `Result<void, RpcError>`

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Type system & the type solver

Decide: a unit literal (`()`), `void` as an expression, or nothing (document the limit). If it lands, A107's surface becomes `Result<void, RpcError>` — one line in `rpc.vl::call_ack` plus each caller's match (breaking for callers written against `Option`).
