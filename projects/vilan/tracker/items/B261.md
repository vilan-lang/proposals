# B261 — `Type::Generic`'s pretty-print for an IMPLICIT binder (B186's sugar) is the trait's name, so row 346's head can still read "`Add`'s `add` accepts `Add`" after B218; only the steer could be worked around (B246's refusal names the written-out rewrite instead)

- status: OPEN — lane solver-29's find (2026-09-06, Order 29)
- kind: bug
- area: Diagnostics & the ledger

Give the implicit binder a display (the bound's trait plus a marker, or the parameter's position) and route every operator/dispatch head through it; pin row 346's head on the implicit form and B246's steer.
