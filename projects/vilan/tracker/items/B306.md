# B306 — `bind_callee_own_generics` silently DROPS a failed reconcile: a closure argument whose type contradicts the substituted parameter binds nothing and reports nothing there, so detection depends entirely on a later check — not exploitable after B288 in the shapes probed, but it is why B288's hole was invisible

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Type system & the type solver

Record the failed reconcile as a candidate diagnostic (deduped against the later check's, B5's rule) or assert that a later check always fires for the shape; pin a closure argument contradicting a substituted parameter with NO later consumer.
