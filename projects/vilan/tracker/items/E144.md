# E144 — derive-template double claims: two expansions of one `[derive(..)]` index the same DERIVED_SOURCE offsets (`Ordering` and `JsonKind` both claim 304..312)

- status: OPEN — lane editor-sync-27's find (2026-09-04, Order 27)
- kind: editor
- area: Language server & tooling

Unrelated definitions sharing an address in a text no file holds; E134's co-reference arm excludes them and the dedup collapses them as before, so nothing renders wrong today — but references/rename over a derived member may answer for the wrong type. Key derived occurrences by (template, expansion) or give each expansion its own DERIVED_SOURCE range. Pin: references on a derived member of one enum do not include another enum's.
