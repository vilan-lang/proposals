# B329 — two renderers disagree about a WRITTEN PATH: the `[expose]` element refusal (analyzer.rs ~15364) renders through `pretty_print_type` and says `Note` where the author wrote `models::Note`, while `render_type`'s callers (B302) now say `models::Note` — one annotation, two answers

- status: OPEN — Order 34's finds (2026-09-13)
- 2026-09-13 (Order 35 GO): BUILD, lane solver-35.
- kind: bug
- area: Type system & the type solver

Route the `[expose]` refusal (and any sibling that prints an annotation) through `render_type`; sweep `pretty_print_type` callers that print a user-written annotation; one pin per moved message.
