# A80 — vilan has no MUTABLE PATTERN BINDING: `Some(let mut x)` and `Some(mut let x)` are both parse errors, so `cell.update(|&mut held| match held { Some(let list) => list.push(..) })` is refused with "cannot mutate immutable 'list'"; `Option::take`/`replace` is the working idiom and it is not obvious

- status: OPEN — Order 31's finds (2026-09-09)
- kind: feature
- area: Type system & the type solver

Either the sugar (`Some(let mut x)`, one grammar rule + `RULE_STATEMENT_SITES`) or a line in `SignalCell::update`'s docs naming the take/replace idiom; the refusal's steer could name it too.
