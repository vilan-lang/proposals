# E111 — semantic highlighting and inlay hints drift a few characters after a `Some(let …)` pattern

- status: OPEN — Order 23, editor-health lane
- kind: bug
- area: LSP & tooling
- source: owner report, 2026-08-31

At kolt `interact.vl:129` — `if handler.on_double_click is Some(let
(delay, on_double_click)) {` — the semantic tokens and inlay hints on
the rest of the region sit a few characters off, beginning after the
`Some(let` group. The payload is a TUPLE pattern, which neither B166's
nor B167's probes covered: the offset smells like a span that fails to
account for the `let ` keyword (or the tuple parens) inside an `is`
pattern, shifting every subsequent token by the difference — the same
syntax territory as B167, but in the token-span subsystem instead of
codegen.

Build the minimal repro and discriminate: bare `Some(let x)` vs tuple
`Some(let (a, b))` vs closure payload; semantic tokens AND inlay hints
separately (they may share the one broken span source — find it rather
than patching each consumer). Pin with a token-offset test.
