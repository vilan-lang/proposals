# B286 — a `Source`-generic runtime function is UNCALLABLE from generic code: `expose_dynamic<T: Wire, S: Source<T>>(source: S)` from `reply_source<T: Wire>` fails with "cannot infer 'T' for this call; its bound ': Wire' cannot be checked" — a type parameter is solved from the call's TYPES and never from a bound, so an `S` that is itself a parameter leaves `T` unsolvable; both had to take the concrete `SignalCell<T>` (`expose_keyed` says the same of its key projection)

- status: OPEN — Order 31's finds (2026-09-09)
- kind: bug
- area: Type system & the type solver

Solve a bound-introduced parameter from the bound's application when the argument's type is a parameter with that bound (`S: Source<T>` + `S` known → `T`); pin the generic-to-generic call; measure the solver cost.
