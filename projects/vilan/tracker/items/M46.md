# M46 — `CHECKED_CACHE`'s new table slice is bounded by an 8M-row budget beside the key bound; a real BYTE budget in M24's shape (and M36's on-disk world, whose `World` serialization does not exist yet) is the honest shape

- status: OPEN — lane m19-t1b's find (2026-09-05, Order 28)
- kind: performance
- area: Performance & footprint

Class D is ~10⁵ rows per world; the row count is a proxy. Measure bytes per world with tables, fold into M24's budget, pin eviction under pressure.
