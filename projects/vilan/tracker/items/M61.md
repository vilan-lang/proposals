# M61 — `__with_finally` and `__guarded` (B292) are now emitted into EVERY program that touches `std::reactive` (`Owner::dispose` is always reachable): two small functions of bundle weight, unmeasured

- status: OPEN — Order 32's finds (2026-09-11)
- kind: perf
- area: Performance & footprint

Measure on kolt's browser bundle and the split fixture (bytes, minified and gzipped); if it matters, fold the two into one helper or emit only the one a program reaches.
