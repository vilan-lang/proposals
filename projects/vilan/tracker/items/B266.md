# B266 — a GENERIC `[service]` subject has no curated refusal: it cascades through generated code (first line `'contract_hash' is already defined for 'StoreClient<T>'` plus `` `Store` takes 1 type argument, 0 given ``)

- status: OPEN — lane rpc-29's find (2026-09-06, Order 29)
- kind: bug
- area: Diagnostics & the ledger

One refusal at the attribute naming the subject's parameters and the reason (A53's), ledger `NEW` row; pin the cascade gone.
