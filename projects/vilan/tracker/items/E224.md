# E224 — deprecation and internal-use WARN at import leaves for TYPES (E221's lint, B382's type warnings) but NOT for FUNCTIONS: a `[deprecated]` function's import leaf is silent (pre-existing); one rule for all labelled items at the import leaf

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: editor-41's report)
- kind: diagnostics
- area: LSP & tooling

Discussion: editor-41's find 5 (2026-09-24)

- 2026-09-25 (Order 42, std-42): STOPPED (patch saved at scratchpad/std-42/e224-held.patch): the fix (labels.rs reads type-position rows for every labelled declaration) works — three CLI pins red → green — but it turns the SHIPPED pin `generics::a_std_marked_item_warns_at_its_use` red ('exactly the call site warns; the import line is not a use') and CONTRADICTS proposal/deprecation.md ('an import line alone does not warn … the behavior we want'). OWNER Q: should function import leaves warn (reversing the paper), or should TYPE leaves (E221/B382's deviation) stop warning? Rec: the paper stands — type leaves stop warning (one rule: a use warns, an import does not).
