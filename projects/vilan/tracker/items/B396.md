# B396 — a SECOND bound over `T` on a generic function's `S` (`each<T, S: Source<List<T>> + DeltaFeed<T>>`, in either order) stops the KEY CLOSURE from binding `T` — the a52 pin's refusal grows from two errors to four naming an abstract `T` — so A112 S3 had to put its bound on the `Slot` impls instead of on `each()`

- status: OPEN — filed 2026-09-22 (Order 40 sweep; the s3-40 lane's find)
- kind: bug (inference)
- area: Type system & the type solver

Repro: the two-bound `each` signature with a `|item| key` closure; expected: `T` binds from `S`'s first bound as it does with one bound. Pin red-first. Sizing S–M; solver lane, Order 41 beside B395.
