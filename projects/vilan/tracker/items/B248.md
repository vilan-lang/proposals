# B248 — `match x { .. } + 1` in LEADING statement position is still refused: the block-like dispatch precedes the expression tower (B231 admitted block-likes as operands, not as heads)

- status: OPEN — lane parse-fmt-28's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

A statement beginning with `match`/`if`/`{` is parsed as a block statement and the trailing operator is a parse error. Decide: admit (the tower tries after the block-like) or refuse with a steer to parenthesize; pin both spellings.
