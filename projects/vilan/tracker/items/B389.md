# B389 — INFERENCE — FIVE literal positions B370's ruling covers and B370's fix did not deliver: a literal on the LEFT of a binary operator, the elements of an ANNOTATED list literal, a literal MATCH pattern, a GENERIC call's return, and a bare `let n = 0` used later at the type — all five fail for `i53`/`u32`/`f64` and pass for `i32`. This is I5 S1's real content: `usize` cannot be the index type until an unsuffixed literal takes it in every position

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: bug (inference)
- area: Type system & the type solver

Pins: one per position, red-first, for `i53`, `u32` and `f64`; the 14 passing positions become controls. Sizing M; solver lane, Order 41, ahead of I5 S1.

- 2026-09-24 (Order 41, solver-41): FIXED (94a648d1): the five positions (left literal takes the right operand's type; annotated-list elements take the element type or a typed sibling's; a literal match pattern takes the subject's; a generic parameter fixed only by a literal takes the expected result's numeric type; an unannotated binding built from literals WAITS in the fixpoint for a typed use, then an operator/assignment peer, then the default — BigInt excluded); the native backend reads each literal's settled width from a new `expr_type_ids` record. Census: 13 passing positions (not 14); the other three failures are the index/`len` boundary = I5 S1. 15 + 2 pins red-first; kolt `vilan check` 1.668 → 1.681 s CPU, diagnostics byte-identical; lexical.md + the tour updated (the `stamp + 1000` trap was already untrue).
