# E146 — formatter gaps N55's reformat exposed: `self` sorts LAST in an import group (ASCII order; 37 groups incl. std's prelude.vl/web.vl); an `if`/`match` arm body becomes a block mid-builder-ladder; a declaration's `context` clause prints where written (`fun f(): T borrows p context c` not round-tripped); a comment between a field name and its value relocates below the statement

- status: OPEN — lane l19-ci's find (2026-09-05, Order 28)
- kind: editor
- area: Language server & tooling

Each is a printer rule: `self` first in a group; an arm body that was an expression stays one; the clause normalized after `borrows`; a field's comment kept with the field. Pins per rule; the tree reflows once more (the gate is live now, so one commit).
