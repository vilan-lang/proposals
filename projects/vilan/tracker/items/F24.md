# F24 — the native executor never REUSES a settled task's slab slot (set to `None`), so the slab grows one word per spawn for the program's lifetime — fine for CLI programs, unbounded for a server spawning per request

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: performance
- area: Native targets & rendering

A free list with a generation counter on the handle; a vilan-rt pin that N spawn/settle rounds keep the slab bounded.
