# B361 — `Self<i32>` is an accepted spelling with no use: it parses in type, static-path and literal positions, nothing in std/examples/docs/kolt writes it, and in a type position it means `Cell<i32>` inside `impl Cell<type T>` — coherent, redundant with naming the type

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Type system & the type solver

Refuse it with the steer `write the type's name` (rec), or document it; one row, three position pins.
