# B285 — `[expose(keyed = K2)]` over a `KeyedCell<K, T>` with `K2 ≠ K` is silently IGNORED: the type's `K` wins (it must — `expose_keyed_cell` and the mirror are typed at it), which is the same silent-preference family R6/A56 just closed for the `Map` form

- status: OPEN — Order 31's finds (2026-09-09)
- kind: bug
- area: Reactive core & UI

Refuse at the attribute, span on the argument, reusing row 407's shape (or its row with a `KeyedCell` label); control pin for the agreeing form.
