# B218 — two implicit generics of one trait display identically: `fun swap(a: X, b: X) { c = b }` refuses as `Expected X, but got X instead.`

- status: OPEN — lane b211-b212's find (2026-09-03); the diagnostic face of trait-typed-fields.md revision 2's Q3
- RULED 2026-09-05 (owner, trait-typed-fields.md rev 2 Q3): the hidden argument PRINTS — `Expected C<A>, but got C<B> instead.` over `Expected C, but got C` — a deliberate deviation from B186's display rule at the one place it does not work. Build with B184's sugar (Order 28).
- kind: bug
- area: Diagnostics

B186's rule renders an implicit generic under its trait's name, so
two independent implicit generics of the same trait print the same
word, and B211's (correct) refusal reads as a contradiction. The
written-generic form reads right (`Expected P, but got Q instead.`).
Fix once the owner answers Q3 (how the hidden argument prints):
options — name the parameter it came from (`the type of a`/`the type
of b`), or a numbered spelling (`X#1`, `X#2`), or B186's display
extended with the binder's origin. Pin the swap shape, the three-way
rotate, and a call-argument mismatch through the sugar.
