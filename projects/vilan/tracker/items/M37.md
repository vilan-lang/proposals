# M37 — `collect_unfollowable_loans` is 60% of `LastUse` (450 of 758 ms on kolt's client): three full sweeps of `expr_id_to_expr_map`, the first allocating a `Vec<Convention>` per call only to test `is_some()`

- status: OPEN — lane m28's find (2026-09-04, Order 27)
- kind: performance
- area: Performance & footprint

A non-allocating "is this callee resolved" test plus fusing the three sweeps into one. `compute_view_origins` was 27 ms for n=2 origins — same shape. Goldens byte-identical is the gate; numbers on the exhibit and kolt (quiet box, CPU).
