# N40 — `free_port()` has a bind-release-rebind race two e2e suites share

- status: OPEN — perf lane's find (Order 23)
- kind: process
- area: Hygiene & rot
- source: lane perf, 2026-08-31

`crates/vilan-cli/tests/serve_build.rs:170` (and the same shape at
`ssr_fullstack.rs:81`) binds `127.0.0.1:0`, reads the port, DROPS the
listener, and the server re-binds later — under ~10 parallel suites
the window is real: `a_binary_artifact_reaches_the_wire_as_the_build_
wrote_it` failed once with "the server should bind 45673" after 114s
and passed 10/10 alone. Fix: keep the listener and pass the socket
(or bind in the server and report the port back), in both files. This
is the fleet's second distinct load-flake family after M15's gate
(now fixed) and the directory-watch talking timeout (two strikes).
