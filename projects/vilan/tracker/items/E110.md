# E110 — the web-set steer's comment claims an LSP suppression that does not exist

- status: OPEN — audit run 6's F22; recordable
- kind: bug
- area: LSP & tooling
- source: audit run 6

`web_prelude_steer`'s comment says the LSP's unresolved-name parser keys
on the `; import it first` prefix so the arm's different suffix avoids the
import quickfix; the LSP actually keys on the `cannot find '` PREFIX, so
the quickfix is offered anyway. Decide which behavior is wanted (for a
module-carried name the module import may be a fine quickfix), implement
it, pin it, and make the comment true either way. Cross-ref: the F2 fix
narrowed the steer for module-carried names at the analyzer side.
