# A133 — over an IN-PROCESS transport, `sub`/`effect` on a node over a MIRROR sees the inline SEED twice (over a socket it is correct): the mirror's seed arrives inline under the subscribing turn and the node's pull re-reads it — a std design question (the in-process transport's inline delivery vs the push-pull protocol's one-notify-per-settle)

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's find R1)
- kind: design
- area: Reactive core & UI

Options: (a) the in-process transport delivers the seed through the draining turn like a socket does (one code path — rec); (b) the node dedups a notify whose value equals its last pull (costs `PartialEq`); (c) document it. Rec (a). Sizing S–M; std/reactive, Order 43.
