# N35 — `hmr_css_matrix` reads `dist/client.js` while the watcher writes it

- status: OPEN — seen once under lane load by b148 (pre-existing)
- kind: bug
- area: Testing & CI
- source: lane b148's report, 2026-08-29

`the_css_apply_matrix_holds_in_every_cell` failed on a contended run with
`SyntaxError: Unexpected end of input` on a truncated bundle: the test
reads the file while the watch round writes it, guarded only by two
`contains` checks a partial file can satisfy. Green in isolation and on
the rerun — a load-dependent read-while-writing race. Fix: wait for the
round's completion signal (the watcher narrates; the G14-era watch.log
capture is the pattern) or read-until-parse, never contains-and-hope.
