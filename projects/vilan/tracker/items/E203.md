# E203 — a backtick does not behave as a PAIR inside code comments: the language configuration has no `` ` `` pair at all, and doc prose is where backticks live (`/// Returns `None` when …` — every std doc comment uses them). The owner's ask (2026-09-21): typing `` ` `` in a comment places the closing one; selecting text and typing `` ` `` wraps it

- status: OPEN — filed 2026-09-21 (the owner's ask, mid-Order 38)
- kind: editor
- area: LSP & tooling

VS Code's `autoClosingPairs` can only EXCLUDE scopes (`notIn`), not include them, and the backtick has no meaning in vilan code outside comments (verify: no backtick token in the lexer; i-strings use `i"…"`), so the honest spelling is a global pair `{ "open": "`", "close": "`", "notIn": ["string"] }` plus a `surroundingPairs` entry — it fires in code too, where a backtick is a typo either way. If the lexer ever takes the backtick (raw identifiers, a template form), this item's pair gains a `notIn` or moves server-side (E202's door 2 mechanism). Also check `autoCloseBefore` so a backtick typed before a word character does not pair.

Pins owed (`vscode_extension`, by configuration as E194's are): the pair exists with `notIn: ["string"]`; `surroundingPairs` carries it; a grep pin that the lexer still has no backtick token, naming this item, so the day it gains one the pair is revisited. The markdown fences in the book are not this item.
