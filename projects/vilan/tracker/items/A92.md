# A92 — the SYNC UNLEASED HANDLE STUB (Order 32's P1–P3, RULED 2026-09-11 as recommended): an `[rpc]` method returning `SignalCell<T>` / `Option<SignalCell<T>>` / `KeyedCell<K, T>` (A79) becomes a SYNC stub returning `RemoteSource<T>` / `KeyedSource<K, T>` minted UNLEASED from its `Origin` (the shipped re-mint path is the first mint, so an unleased handle costs nothing — R3 kept); `Status` gains `Absent` (the `Option` form's `None` reply) and `Failed(RpcError)` (the next 0→1 lease retries); `.or(seed)` at the call site, no `remote_signal` bridge; the SERVER dedups dynamic channels by SOURCE IDENTITY with a per-channel client-lease count (revoke at zero, never under another mirror); a std `Memo<K: Hashable, V>` (`get_or`/`forget`, the maker at the call site) is the app's composition tool — the stub does not auto-memo. BREAKING for every handle stub's type

- status: OPEN — RULED 2026-09-11; Order 33 (rpc-33)
- kind: feature
- area: Reactive core & UI

See `briefs32.md` §rpc-32 (steps 1–7, pins, hash pins) — the plain form's hash 78bdada7 must not move; the `Option` form's does (`Option<RemoteSource<T>>` → `RemoteSource<T>` in the surface). `RemoteSource<T>: Source<Option<T>>` unchanged (`None` while Waiting/Absent/Failed). The keyed form (A79) in the same shape, no per-key hop. Paper: transport-rpc §9.2/§9.6 as-built; remote-sources.md §2d (`status()`'s four arms). Kolt migrates at the owner's word: model.vl's memos → std `Memo`, `lib/remote_signal.vl` deleted, `.or()` at the call sites.
