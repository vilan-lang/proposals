# J6 — ASYNC on a native backend: `Task`/`Nursery`/`await` need an executor and a cancellation story where JS supplied the event loop; the F1 paper's largest item F1 did not list — `Task` is an `external struct` over the host promise (task.vl:18), so the native runtime must provide the promise, the microtask queue and the turn model (reactive-turns.md) before any `async fun` compiles natively

- status: OPEN — filed 2026-09-14
- kind: design
- area: Concurrency

Design in F1's S1: a single-threaded executor with a microtask queue matching the JS turn model; cancellation via the Nursery's drop; `Task` stays external over the executor's handle; pins mirror the async corpus programs byte-for-byte on stdout.

- 2026-09-17 (Order 37 GO): R5 RULED as recommended: `Task<T>` stays external over a single-threaded executor in `vilan-rt` matching the JS turn model; S1a builds none — the executor's DESIGN is a section of native-b-37's report; BUILT with F18 (Order 38).

- 2026-09-17 (Order 37 sweep): R5 RULED and DESIGNED in lane native-b-37 (native-apps.md §10): seven primitives read off `transformer.rs::helper_source` — `Task` external over an executor slab, a single-threaded loop with the microtask queue drained before the timer heap, `Nursery` as `Rc<NurseryBody>` with a `Weak` parent, the join drain reproduced exactly, detached nurseries, abortable `sleep`, memoized `Timer`; emitter = Rust `async fn` over a hand-written executor (CPS rejected). Size M; the precondition for F18. Built with F18 (Order 38).
