# B177 — an ARRAY impl's return check refuses with a self-contradicting message

- status: OPEN — operators lane's find (Order 23)
- kind: bug
- area: Type system & the type solver
- source: lane operators, 2026-08-31

`impl [i32; 2] with Add<i32>` is refused: "`type`'s `add` returns
`[i32; 2]`, but `Add` declares `[i32; 2]`" — the two sides are the
same spelling. Array-type identity in the impl return check; tuple
impls pass the same shape, array impls cannot be written at all.
Pre-existing, surfaced while B170 verified the array refusal's advice
was actionable — it is not, until this closes.
