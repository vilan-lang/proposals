# N98 — N94's class SURVIVES outside the rowed surface: swallowed `\`-continuations (a run of ~15–18 spaces mid-sentence) at `vilan-core/src/macros.rs:1535` (a clippy `#[allow(.., reason = "…")]`), `vilan-lsp/src/document.rs:18661` (an assertion), `vilan-core/src/formatter.rs:13388` (a test program const) and ~15 `.vl` program consts in test files (`module_resolution.rs` 1344/5541/5626/5768/5791, `check_scope_differential.rs` 826/833, `hmr.rs` 558, `document.rs` 9312–9434, `inference/generics.rs` 1603, `build_hooks.rs` 1262, `grammar_ebnf.rs` 137/141/145/168); N94's gate covers only what the ledger enumerates

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: hygiene
- area: Hygiene & rot

A second gate over prose literals in `src/` (three consecutive spaces inside a string literal) with an exemption list for the aligned tables (`formatter.rs`'s `StyleMethod` rows, `bindgen.rs`'s report columns); fix the sites it names.
