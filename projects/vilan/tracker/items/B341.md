# B341 — HIGH: the base-cache KEY omits the DESUGAR-SEEDED std modules, so a two-entry package fails to build: `BaseCacheKey.std_seeds` is `collect_module_paths(&nodes.0, "std")` only — the entry's `collect_std_item_modules` seeds (`std::ui` for element syntax, `std::style` for a `css` block) are not in the key — so two entries that differ only in whether they use element/css syntax SHARE a key, and `vilan build` analyzes a multi-entry package in one process: `[entry.aplain]` = `print("plain")`, `[entry.zelement]` = `let _x = <div/>;` → '`view` is not in scope' (rename so the element entry sorts first → both build; the css twin says '`style` is not in scope'). The same shared state reds `cargo test -p vilan-core --test inference -- modules::` on `an_element_needs_no_view_import_at_all` and `b270_…` after any std-free program was analyzed in-process (nextest hides it — N84's third part). Fix, one line: fold `collect_std_item_modules(&nodes.0)` into `entry_seed_names` (analyzer.rs ~53374)

- status: OPEN — filed 2026-09-14; BUILD in lane perf-36 (Order 36)
- kind: bug
- area: Type system & the type solver
- 2026-09-14 (Order 36, perf-36): LANDED 828de17c — `collect_std_item_modules(&nodes.0)` chained into `entry_seed_names` through `seed_module`, so `BaseCacheKey::std_seeds` carries the desugar seeds; verified end to end on hygiene-36's two repro packages (pre-fix binary fails, post-fix compiles both entries); `cargo test -p vilan-core --test inference -- modules::` 285 passed incl. the two formerly-red tests. Pin `an_entrys_desugar_seeds_are_part_of_its_world_key` (asserted on diagnostics, not a hit delta — macro worlds consult the cache on their own account). No ledger row. CLOSE at the sweep.

Fold the desugar seeds into the key; a pin that builds the two-entry package in one process (the repro shape) and one that the two `modules::` tests pass under plain `cargo test`; no ledger row.
