# B182 — a refused bare-trait FIELD cascades ~50 errors through generated code, roots printed last

- status: OPEN — found diagnosing the owner's kolt migration (2026-09-01)
- kind: bug
- area: Type system & the type solver
- source: kolt's store.vl: two `Signal<…>` fields → 53 errors

B161's design promise — a refused annotation resolves to Unknown "so
the one report stands alone instead of cascading" — does not hold
through MACRO-GENERATED code and downstream method calls: two refused
`[expose] … : Signal<…>` fields produced 53 errors (6 curated roots +
15 `set_with` on unknown + 12 generated-code inference failures +
field-access-on-any noise), and the CASCADE PRINTED FIRST — the owner
read "cannot infer 'S'" and never surfaced the root two pages down.
Three parts: (1) suppress unknown-receiver and unknown-inference
follow-ons whose receiver traces to a refused annotation (the b154
stand-down family's move); (2) each root printed 3× — once per entry
— the per-entry dedup the two-seams rule already does for module
errors should cover package-mode check; (3) ordering: roots before
cascade. Part (1) is the substance; (2)/(3) ride it.
