# A53 — a GENERIC source field for `[expose]`/`[service]` (A52's split-off): `ServiceItem` (meta.vl) carries no generics so the expansion cannot spell `impl Store<type T>`; the contract hash is built from WRITTEN types so every instantiation hashes identically (a `Store<Task>` client would connect to a `Store<Note>` server); `Client::connect(url, codec)` has nowhere for the element type to come from — three answers wanted before a build

- status: OPEN — answered by transport-rpc §9 (lane rpc-paper-30, Order 30): the generic subject is REFUSED today (ledger row 406, B266) and §9's return-typed handles make the generic field moot for the sketch; ruling R1–R7 pending the owner
- 2026-09-07 (the owner): FOLDED into the rpc redesign discussion (per-connection service instances, rpc functions returning signal handles, client-declared functions) — the generic source field is decided there, not on its own.
- 2026-09-08 (lane rpc-paper-30, Order 30): ANSWERED in `proposal/transport-rpc.md` §9.2 — a return-typed signal handle writes the element type in the METHOD signature, where the contract hash already reads it, so the motivating case needs no generics on `ServiceItem`; `[service]` on a generic struct stays refused. Recommendation: close as ANSWERED, not built — §9.2, "A53 — the generic source field stops mattering".
- kind: design
- area: Reactive core & UI

Design: generics on `ServiceItem`; the element type INTO the contract hash (per instantiation) and into `connect` (a typed client constructor); or refuse generic services outright and say so (B266). Then build or close.
