# N52 — `infer_differential.rs` has N49's old shape: one `run_node`, one 300 s deadline, one whole-corpus test

- status: OPEN — lane hygiene-26's find (2026-09-03)
- kind: hygiene
- area: Testing & CI

Same `run_node`, same 300 s `NODE_TIMEOUT`, same single-test whole-
corpus loop as `release_differential` had; its `NOT_RUN` has four
entries and does not exclude `watch.vl` or `nursery.vl`. It escapes
the 600 s today only through its run-only-on-difference shortcut —
the moment folding changes `watch.vl`'s emission it inherits the
identical vacuous leg. Split it with N49's `corpus_programs!` macro
(one process per program, the coverage gate in both directions), and
share the declared list with `release_differential` so the two
cannot drift.
