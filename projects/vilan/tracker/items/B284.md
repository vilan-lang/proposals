# B284 — `[expose]` on a `[client_service]`-ONLY struct is a silent no-op: it compiles, contributes an `expose:` entry to that struct's hash, and nothing ever mints the channel (no `__attach`, no reactive session client-side) — `[client_service] struct Handlers { [expose] tally: SignalCell<i32> }` builds and prints a hash

- status: OPEN — Order 31's finds (2026-09-09)
- kind: bug
- area: Reactive core & UI

Refuse at the attribute in the field's vocabulary (a client-side struct has no reactive session to export into); one ledger row; a must-fail pin.
