# E165 — scope completion on a BLANK LINE inside a function offers only globals: `fun main() { let start = 1; ▮ }` does not offer `start` while `st▮` does — `scope_at` falls back to the nearest entity BEFORE the offset and lands on the global scope when the line is empty; pre-existing, and it is the moment a user actually asks for completion

- status: OPEN — Order 33's finds (2026-09-11)
- kind: editor
- area: LSP & tooling

`scope_at` resolves the innermost scope whose SPAN contains the offset (the block's braces), not the nearest entity; pin the blank-line case and a nested block; kolt's `store.vl` as the exhibit.
