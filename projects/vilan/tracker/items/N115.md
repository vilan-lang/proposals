# N115 — `vilan-ide` has NO in-crate tests for the completion engine — every completion pin lives in vilan-lsp's integration tests, so the engine's ranking, filter-text and replace-span logic (E211) is tested only through the protocol layer

- status: OPEN — filed 2026-09-21 (Order 39 sweep; editor-39's find 3)
- kind: hygiene (coverage)
- area: Hygiene & rot

Add `crates/vilan-ide/src/completion/tests.rs` (or a `tests/` dir) with the engine driven directly: member completion detail (E204), filter text and replace span (E211), the prelude/OI cases (E180). Not a big-bang: each future completion item adds its pin here rather than in vilan-lsp. Sized S to seed.
