# E208 — a two-diagnostic cascade after B356: `Option<i32>::Some("y")` refuses at the payload (right line) but the expression's type still settles `Option<str>`, so a later `unwrap_or(3)` reports too — both are type errors, so E189's narrow rule does not suppress the second

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: diagnostics
- area: LSP & tooling

On a refused written type argument, settle the expression's type from the WRITTEN arguments (the user's stated intent), so downstream checks run against `Option<i32>`; a pin asserting exactly one diagnostic.
