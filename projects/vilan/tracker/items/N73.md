# N73 — EIGHT near-copy DOM stubs across `crates/vilan-cli/tests/` (`DOM_STUB` in ui_rows/reactive_lifetimes/source_bindings, `HARNESS` in ssr_differential and router, `BOOT_HARNESS` in ssr_fullstack, `STUB` in split, plus one more): A71's one `insert_before`/`Text::remove` addition had to edit SEVEN of them (six raw strings `r#"` → `r##"`); one shared stub under `tests/support/` would make a std DOM change a std-only diff

- status: OPEN — Order 32's finds (2026-09-11)
- kind: hygiene
- area: Hygiene & rot

One `support/dom_stub.js` (or a Rust const in `support/`) with the per-file extras layered on top (`byId`, `find`/`findAll`, `page()`); each test file includes it. Mechanical; a hygiene lane's.
