# E199 — a refused generic-bound message can name the WRONG `T`: in solver-37's B352 probe a wrong render body was refused as `Swap<str, StorageSignalCell<bool>, View>` does not implement `Slot` — `T` taken from the closure BODY rather than from the SOURCE argument, so the message names a type the author never wrote; the refusal is right, its sentence is not

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: diagnostics
- area: LSP & tooling

Render the bound refusal with the binding the source fixed (the same substitution B347's fill now uses), and name the body's disagreeing type as the second operand; pin the probe's shape.
