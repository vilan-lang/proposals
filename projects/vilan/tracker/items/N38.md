# N38 — two shipped sentences disagree on the reserved-name set

- status: OPEN — records lane's find (Order 23); flagged on ledger row 284
- kind: process
- area: Hygiene & rot
- source: lane records, 2026-08-31

`refused_root`'s parenthetical names three reserved names (`std`,
`pkg`, `macro_std`) where `Manifest::validate` names four (`vilan`
joined at Order 11, vilan 9198d8f9). One shipped sentence is wrong by
omission. Fix is a message change — a corrected sentence gets a
census (the Order 21 rule) — and the lane's recommendation stands:
one shared constant both messages read, so the set cannot fork again.
