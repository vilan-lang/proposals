

- 2026-09-24 (Order 41, native-41): SLICE 3 BUILT, THE EXIT REACHED (cb5b2e5d F33, 6ad60c2c the seams, 39045eca + 52c7d4b2 `Server::builder()`, ac5fc1e7 the rpc server, 629c69a5 the exit; tip 52c7d4b2 before its rebase on reactive-41): `kolt_shape_server.vl` (written from scratch to kolt's shape — builder chain, `parse_path`/`ApiRoute`, `Service::factory().authorize(..)`, fingerprint-aware `cache_build`, the `Document` shell with `const asset::read`, SQLite auth) serves a login AND a keyed subscription over one server byte-for-byte with node (register, login → token, wrong password, malformed → 400, deep link → shell, `/client.js` + ETag; keyed: `who:ada`, `m2:ada:world`, `m2:ada:world again`, never `m1`; bogus token → `err:Unauthorized`); non-vacuous by a planted wrong column reader and a wrong frame byte; contract hash `43077e29` unmoved on both backends; host census 0 on the exit program. The builder's census read 7 gaps (F29 counts createHash/update/digest apart) and behind it rustc refused 14 emitted errors in FIVE classes, each fixed in general (rule 1's copy at every consuming position incl. field reads — native-b-39's find; `&mut` reborrow; a literal takes its partner's width; a host binding's argument no longer inherits the expected type; `Service::authorize`/`authorize_request` were plain closures re-marked async at a `let` — now async closure fields + an installed flag); std's private `external struct Hash` in rpc_server.vl renamed `NodeHash` (else the native backend read it as `std::hash::Hash`); `vilan_rt::fs::stat`; no body/Content-Length on 304/204/1xx; the analyzer records a written closure type's view parameters (`Program::closure_type_parameter_views`, JS never reads it); a closure reading the cell it `update`s is REFUSED by name natively (JS answers the in-progress value; safe Rust cannot — `signal-update.vl` changes refusal reason); four more lowerings (a `const` result serialized in place; a string literal in a pattern → guard; an unannotated function's inferred return; nested closure destructured params not captures). Whole set 121/83/38 → 122 / 89 / 33 / 0 / 0 / 0. KOLT's real `server.vl` on a scratch copy: 4 gaps (was 69) — PBKDF2 + `toString`, `random_bytes`, one generic `map` → F40 (ruling). `guide/native.md` rewritten. Census files in sweeps/order41/native-41/.
