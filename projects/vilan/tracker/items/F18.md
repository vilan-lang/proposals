# F18 — native WEB SERVERS as the first native product after CLI programs (the owner, 2026-09-17: 'more easily achievable in the short term' and 'a huge win in and of itself'): the `@process` family on the Rust backend — `std::fs`, `std::http`, `std::db`, `process/rpc_server.vl`, the service layer — which REVISITS native-apps.md §8 Q2 (`native` does not join `@process` initially): for a server it must, and `check_library_contract`'s base-layer obligation is the cost to size; J6's executor is the precondition (a server is async through and through); the exit test is kolt's server leg built natively and serving its client

- status: OPEN — filed 2026-09-17 (the owner's ask at Order 37 GO); Order 38 candidate
- kind: feature
- area: Native targets & rendering

native-b-37 reports the sizing: what `vilan-rt` needs beyond S1a (the executor J6, sockets/HTTP, SQLite or the `std::db` shape, the file system, the rpc server's wire path), which `@process` modules resolve for `native` unchanged and which need a native twin, and the order S1b → executor → http → db → rpc. Sequenced BEFORE F1 S2's UI-shaped platform work (F15, E182 wait for F17).

- 2026-09-17 (Order 37 GO): SIZED in lane native-b-37's report (no build this order).

- 2026-09-17 (Order 37 sweep): SIZED in lane native-b-37's report (native-apps.md §10): S1b (L) → J6 (M) → http (~25 bindings; M) → db (M) → rpc (S — `process/rpc_server.vl` 1,303 of 1,306 lines extern-free, `ws.vl`/`wire.vl` zero externs: ~2,240 server lines compile natively the day generics work, on 28 host bindings) → fs (M, not on the exit path); `native` MUST join `@process` and it is FREE (`check_library_contract` is structural: joining `@process` + `all_hosts()` costs zero violations); the exit test is kolt's server leg — 3 files, 564 lines, 5 of 9 process modules, no fs. Two to three lane-orders; S1b must not be under-scoped. Order 38's first native slice.

- 2026-09-21 (Order 38 GO): R5 — S1b + J6 are Order 38's; http → db → rpc is Order 39's, briefed from native-a-38's kolt-server-leg refusal list.
