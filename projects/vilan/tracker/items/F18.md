

- 2026-09-21 (Order 38 sweep): Order 39's brief is measured: kolt's server leg has 85 gaps (`sweeps/order38/native-a-38/kolt-server-census.txt`; reproduce with `VILAN_NATIVE_HOST_CENSUS=1 vilan build --backend rust --stdout src/server.vl`) — 55 host bindings, 11 host types, 10 intrinsics, 9 language constructs. J6 is built; F20's three constructs sit on the same path; F24 (the slab free list) before per-request spawns.
