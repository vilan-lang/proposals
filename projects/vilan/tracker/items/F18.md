# F18 — native WEB SERVERS as the first native product after CLI programs (the owner, 2026-09-17: 'more easily achievable in the short term' and 'a huge win in and of itself'): the `@process` family on the Rust backend — `std::fs`, `std::http`, `std::db`, `process/rpc_server.vl`, the service layer — which REVISITS native-apps.md §8 Q2 (`native` does not join `@process` initially): for a server it must, and `check_library_contract`'s base-layer obligation is the cost to size; J6's executor is the precondition (a server is async through and through); the exit test is kolt's server leg built natively and serving its client

- status: OPEN — filed 2026-09-17 (the owner's ask at Order 37 GO); Order 38 candidate
- kind: feature
- area: Native targets & rendering

native-b-37 reports the sizing: what `vilan-rt` needs beyond S1a (the executor J6, sockets/HTTP, SQLite or the `std::db` shape, the file system, the rpc server's wire path), which `@process` modules resolve for `native` unchanged and which need a native twin, and the order S1b → executor → http → db → rpc. Sequenced BEFORE F1 S2's UI-shaped platform work (F15, E182 wait for F17).

- 2026-09-17 (Order 37 GO): SIZED in lane native-b-37's report (no build this order).

- 2026-09-17 (Order 37 sweep): SIZED in lane native-b-37's report (native-apps.md §10): S1b (L) → J6 (M) → http (~25 bindings; M) → db (M) → rpc (S — `process/rpc_server.vl` 1,303 of 1,306 lines extern-free, `ws.vl`/`wire.vl` zero externs: ~2,240 server lines compile natively the day generics work, on 28 host bindings) → fs (M, not on the exit path); `native` MUST join `@process` and it is FREE (`check_library_contract` is structural: joining `@process` + `all_hosts()` costs zero violations); the exit test is kolt's server leg — 3 files, 564 lines, 5 of 9 process modules, no fs. Two to three lane-orders; S1b must not be under-scoped. Order 38's first native slice.

- 2026-09-21 (Order 38 GO): R5 — S1b + J6 are Order 38's; http → db → rpc is Order 39's, briefed from native-a-38's kolt-server-leg refusal list.

- 2026-09-21 (Order 38 sweep): Order 39's brief is measured: kolt's server leg has 85 gaps (`sweeps/order38/native-a-38/kolt-server-census.txt`; reproduce with `VILAN_NATIVE_HOST_CENSUS=1 vilan build --backend rust --stdout src/server.vl`) — 55 host bindings, 11 host types, 10 intrinsics, 9 language constructs. J6 is built; F20's three constructs sit on the same path; F24 (the slab free list) before per-request spawns.

- 2026-09-21 (Order 39 GO): R1 RULED — `vilan-rt` stays dependency-free; slice 1 (HTTP over `std::net`) is lane native-b-39's; `std::db` natively is a separate crate, Order 40's.

- 2026-09-22 (Order 39 sweep): SLICE 1 BUILT (native-b-39, seven commits f6b232a8 … 42985e3d → next fec0b1fa; R1: vilan-rt stays dependency-free). `vilan-rt::http` HTTP/1.1 over `std::net` (~1,050 lines, 15 pins; `IoSource` polled per turn, 1 ms idle bound — the price written at the trait; 431/400/411/413; streaming; the UPGRADE handover); the emitter reaches 23 of 25 raw `node:http` bindings; THE EXIT: a native `std::http` server (the `Server` struct literal) answers `GET /` over a real socket byte-for-byte with node on status/header/length/body (`a_native_std_http_server_answers_a_get_over_a_real_socket`, non-vacuous — the old `run_pending` that asked only the deadline list reds it); whole set 69 identical / 50 refused / 0 / 0 / 0 over 119. Written up as native-apps.md §12. SLICE 2 (Order 40, in order): `std::json` natively (the last two bindings + `serve_build`'s conditional-GET arm → `Server::builder()` works), `std::db` as R1's SEPARATE SQLite crate, the rpc server, kolt's server leg. Still not native by name: the `Number`/`String`/`Boolean` coercions, `now_millis`, `queue_microtask`, `with_finally`, `random_bytes`, the `Bytes`/codec set, six `__db_*`, seven fs. WebSocket: the handover is built; no RFC 6455 accept key; `UpgradeHandler` is sync because `std::http` declares it so (A40's suspending handler is F22's adapted-instance shape). NEW find from the rebase: a destructure of a FIELD read is not copied (`copy_a_consumed_place_read` covers `Local`/`Parameter` only; nothing in the 119 reaches it; widening wants a corpus census — it is on the call-argument path too).

- 2026-09-22 (Order 40 sweep): SLICE 2 as reached (native-a-40 735a0813/a0ac30dc/0e816010/54d42be9/b9f8d3ab): `std::json` natively (`vilan_rt::json`, nine seams, three emitter gaps), the host seams (Math, number labels, charCodeAt, process.env, fs, SHA-256; a void closure that awaits FLOATS), `std::db` through the SEPARATE `vilan-rt-sqlite` crate (rusqlite 0.37 bundled; vilan-rt still dependency-free; linked only when a program reaches `std::db` — pinned), and THE EXIT NARROWER THAN BRIEFED: a login server with kolt's SHAPE built from the `Server` struct literal answers three exchanges over a real socket byte-for-byte with node (`the_kolt_server_shape_answers_a_login_over_a_real_socket`, red with a planted wrong column reader). `Server::builder()` does NOT build natively — so the rpc server natively and the keyed rpc pins against it did not land. Blockers: F22 (native-b-40 f48979cf — now COVERS the `turn(.., || protocol.respond(..))` shape), the mutable `Bytes` RULING (F33), two seams (`now_millis`, SHA-1 `digest`). Whole set after native-a: 119 → 82 identical / 37 refused / 0 / 0 / 0 (native-b adds option-view; the merged number at the seal). SLICE 3 = F33 + the seams + the builder + the rpc server + kolt's `server.vl` as written — Order 41.

- 2026-09-24 (Order 41 GO): SLICE 3 is native-41's: F33 (RULED a) + `now_millis` + SHA-1 `digest` → `Server::builder()` → `std::rpc_server` natively + the keyed pins → kolt's `server.vl` shape as written.
