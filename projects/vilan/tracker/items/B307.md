# B307 — an ELIDED `with` clause over a parameterised trait is PADDED, not checked: `effective_trait_arguments_of` fills a missing argument with the trait's own declared parameter, so `impl DogBox with Holder` (a bounded, non-defaulted parameter left unwritten) is accepted at the clause — B188's erasure in another position; its comment's claim that over-supply 'is already an arity error elsewhere' was false until B273

- status: OPEN — Order 32's finds (2026-09-11); OWNER Q
- RULED 2026-09-11 (owner): REFUSE under-supply as an arity error (a defaulted parameter stays elidable); B273's binder-exempt boundary KEPT. BUILD: Order 33, lane rpc-smalls-33.
- kind: design
- area: Type system & the type solver

Rule: under-supply is an arity error like over-supply (B188's row, the honest reading — a bounded parameter cannot be inferred from nothing), or a documented elision that means 'the trait's own parameter' (then say so on the trait page). Rec: refuse; pin either way.
