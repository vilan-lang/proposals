# M83 — `drain` allocates a fresh dedup `NativeMap` per wave (`turn.queued.write() = NativeMap::new()`); a per-id `remove` measured −35.9 % under the JIT and +11.5 % jitless — under loadavg 8–40, as door 2's control only

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: performance
- area: Performance & footprint

Re-measure on a quiet host (thread CPU + `--jitless` Ir, both); land it only if the JIT direction survives; rides with A110 door 2's bucketed queue.
