# A81 — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere

- status: OPEN — Order 31's finds (2026-09-09)
- kind: feature
- area: Reactive core & UI

Build after B287 is ruled (an awaited call inside a `&mut self` handler is the hazard); the forward stub's `Result<bool, RpcError>` ack is the small first half.
