

- 2026-09-24 (Order 41, solver-41): FIXED (48333165): confirmed with papers-41's probes 16 and 17 (13 was already right); where some element is itself a tuple the comprehension is emitted UNROLLED per element with correct flat offsets, everywhere else the `.map` output is byte-identical; the analyzer records each comprehension's type after the fixpoint for the emitter; native still refuses mapped tuples by name — a differential pin holds 'refused or identical, never different'. 3 pins, 2 red before.
