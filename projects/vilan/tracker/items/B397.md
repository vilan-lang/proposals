# B397 — MISCOMPILE (HIGH) — the tuple COMPREHENSION ignores the FLAT tuple layout, so shipped `combine` returns WRONG VALUES when a source's value is itself a tuple: `combine((SignalCell::new((1, 2)), SignalCell::new("c"))).get()` destructured as `((x, y), l)` reads `x=1,2 y=c l=undefined` (the control — a concrete nested tuple `((1, 2), "c")` — reads correctly, so the layout is sound where the arity is known); reproduced by the integrator on 1265ea5d3 (`sweeps/order41/papers-41/probes/a122_17_combine_tuple_element.vl`, also `a122_16`, `a122_13`). Blocks A122 (`entries()` answers pairs). Fix: emit the comprehension UNROLLED per instance (the JS emitter already specialises generic calls per type)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: papers-41's report + the GO one-liners)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: papers-41's F-a (2026-09-24); solver lane, S–M, any order — added to solver-41's tail if reached

- 2026-09-24 (Order 41, solver-41): FIXED (48333165): confirmed with papers-41's probes 16 and 17 (13 was already right); where some element is itself a tuple the comprehension is emitted UNROLLED per element with correct flat offsets, everywhere else the `.map` output is byte-identical; the analyzer records each comprehension's type after the fixpoint for the emitter; native still refuses mapped tuples by name — a differential pin holds 'refused or identical, never different'. 3 pins, 2 red before.
