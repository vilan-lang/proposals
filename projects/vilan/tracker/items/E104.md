# E104 — b154's R11 stand-down suppresses an independent second offense

- status: OPEN — audit run 5's F2, 2026-08-29; FIX-NOW, in-order lane running
- kind: bug
- area: Diagnostics
- source: audit run 5

`check_instantiation_container_resources` early-returns on the FIRST refused
signature type, dropping every body-container report at that instantiation —
including ones derived from a DIFFERENT type parameter.
`fun two<A, B>(a: Map<str, A>, own b: B) { let xs = [b]; }` at `A := Guard,
B := Other`: 0.39.0 reports the independent `List<Other>` refusal; the
Order 21 union loses it. Contradicts b154's own commit sentence ("a refusal
nobody saw never silences the body's account of it"). Fix shape: the
stand-down is asked PER OFFENDING TYPE, not as one early return; pin the
two-parameter case red-first and keep b154's one-diagnostic pins green.
