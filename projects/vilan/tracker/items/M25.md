# M25 — completion is the keystroke path's entire remaining budget: `auto_import_completions`' per-module sweep and `modules_in_root`'s per-request `read_dir`

- status: OPEN — keystroke-path lane's find (2026-09-01); E121's next keystroke tranche
- kind: performance
- area: Language server & tooling

With tokens and hints at microseconds (E121 §2.1 built), kolt's
5-provider burst is still 12.0 ms — all of it completion: the landed
engine's `auto_import_completions` sweeps every module's
`name_to_id_map` per request, and `modules_in_root` does a
`read_dir` per request. The keystroke path's symbol index retires
only the `module::` arm so far; the seam is marked in
`keystroke_completion`. Fix: the index answers the import-candidate
arm too (names grouped per module, rebuilt only when a module's
stamp moves), and the std/package module listing is cached on the
manifest fingerprint. Gate: the kolt burst under 10 ms hot-from-
edit; the generated exhibit's completion under 0.2 ms. CAUTION from
the lane's near-miss: contributing every loaded module's names at a
scope position turned 125 candidates into 3,144 — a foreign name is
not in scope without an import; the entry module only.
