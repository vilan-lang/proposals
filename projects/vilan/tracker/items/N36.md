# N36 — the diagnostics ledger is three orders stale, and two rows key on dead literals

- status: OPEN — audit run 6's F8+F9+F20
- kind: process
- area: Hygiene & rot
- source: audit run 6

The ledger stops before Orders 20–22: roughly four dozen new user-facing
message forms are unrowed (the B158 refusal, the five `+` refusals, the
sixteen removed-alias arms, the steer arms, E101's rules, the manifest and
const-eval refusals, db.migrate's panics) — C2's textual pins all exist;
the RECORD is the gap. Two rows key on literals that no longer exist (104:
the parameterized arms-mismatch; 149: E100's split site), and F20 records
the family-verdict failure mode (three sites sharing a sentence, one
verdict, two unearned). The L13 mechanical re-key is the fix; run 7's
charter makes the refusal surface a first-class dimension.
