# F17 — DISCUSSION (the owner's, 2026-09-17): how to write UI code ONCE so it works for native and web simultaneously — with named exceptions (native window chrome: close/minimize/maximize buttons, and whatever else the platforms genuinely do not share). The F1 paper's answer (the DOM-shaped `View` as a third twin, Order 37's R2) is the STARTING POSITION, not the conclusion; the conversation decides the shape of the shared surface, the exception mechanism (platform-colored members? a `native`-only module? separate chrome components?), and what the three-layer rule (F15, R4 recorded) has to gate. Nothing in F1 S3–S5 starts before it

- status: OPEN — filed 2026-09-17 (the owner's ask at Order 37 GO); DISCUSSION
- kind: design
- area: Native targets & rendering

Inputs for the conversation: native-apps.md §4.4 (the retained-tree API rejected 'with respect'), §6.2 (the third-twin problem none of the prior art has), F15; the owner's framing: 'drawing windows is much more complicated'; the exceptions list is the first thing to write down. Output: a section of native-apps.md (or its own paper) that F1 S3 reads before it is sized.

- 2026-09-17 (Order 37 sweep): (no build) — F1 S3–S5 and F15/E182 wait for this conversation; native-apps.md §10 records the owner's steer verbatim and the DOM-shaped `View` twin as the starting position.
