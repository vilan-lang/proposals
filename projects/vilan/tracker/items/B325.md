# B325 — `let x = injected_value;` (UNANNOTATED) is still refused by the value-flow restriction although `x`'s type now carries the clause (B309 moved resolution before the fixpoint) and the threading could follow it; the binding landing rule applies only to an annotated `let`

- status: OPEN — Order 34's finds (2026-09-13)
- 2026-09-13 (Order 35 GO): BUILD, lane solver-35 (admit as a forward).
- kind: bug
- area: Type system & the type solver

Admit the unannotated binding as a forward (the clause comes from the initializer's type); pin it beside the annotated form; the mismatched-clause refusal unchanged.
