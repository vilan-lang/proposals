# M64 — glibc never TRIMS what a closed document gives back: closing kolt's 18 documents returned 418 MB to the allocator's free list and 62 MB to the OS; `malloc_trim(0)` on document close would return it, but needs `libc` as a REAL dependency of vilan-lsp (dev-only today) — a dependency ruling

- status: OPEN — Order 33's finds (2026-09-11); OWNER Q
- kind: perf
- area: Performance & footprint

If ruled: `libc` promoted to a normal dependency of vilan-lsp (Linux-gated call, no-op elsewhere), `malloc_trim(0)` after a document close and after an analysis drop; pin RSS after close within 1.2× of before open. Alternative without the dependency: an allocator that trims (mimalloc) — a bigger ruling.
