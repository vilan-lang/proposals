# B403 — MISCOMPILE (native) — B393's real shape: the analyzer ACCEPTS a static call on a bounded impl generic that nothing binds (a bare `Holder::tag()` inside `impl Holder<type T: Label>`); JS then stops with an internal error (loud), NATIVE silently prints `A A` for a `Holder<B>` — one unkeyed instance reads whatever substitution was active when it was first emitted; at top level both backends fail at emission. The fix is in the analyzer's 'cannot infer `T`' check, NOT in the emitters (sharing the four per-emitter functions would copy the wrong answer): solver-41's first attempt was too broad — `Self::` and `Holder<T>::` bind through a channel that check does not read, and std's `FromJson` statics tripped it (reverted). N125's four functions stay per-emitter until this lands

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: solver-41's report)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: solver-41's B393 probe verdict (2026-09-24); solver lane

- 2026-09-25 (Order 42, solver-42): NEEDS A DESIGN RULING (not built): the refusal trips std's JSON statics — `Option::`/`List::`/`Result::from_json_value(value)` called by bare path INSIDE their own impls (json.vl ~278/296/1521) work only because the enclosing instance supplies `T`; the item's text was wrong (std uses bare paths, not `Self::`). OWNER Q: does a bare `Type::f()` inside `Type`'s own impl mean `Self::f()` (rec YES — then the refusal applies only outside the impl, and the native miscompile's shape is exactly the outside case)? Repro scratchpad/solver-42/p/b403.vl. Order 43.

- 2026-09-26 (the owner): RULED as recommended — a bare `Type::f()` inside `Type`'s own impl MEANS `Self::f()` (std's JSON statics are the exhibit); the cannot-infer refusal applies only OUTSIDE the impl — which is exactly the native miscompile's shape. Order 43, solver.
