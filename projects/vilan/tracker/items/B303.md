# B303 — a non-Wire `[rpc]` payload reports THREE times: the signature refusal plus two "in code generated by this attribute: 'X' does not implement trait 'Wire'" from the generated client's `call<T: Wire>`, anchored over the whole `[service]` struct; `[expose]` has B189's stand-down for exactly this, `[rpc]` has none

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Reactive core & UI

The `[rpc]` signature refusal stands the generated stub's bound failures down (B189's mechanism, `expose_refused_key_bounds`' shape from A56); pin: one report for a non-Wire return.
