# B178 — `fun main(p: bool)` compiles and emits broken JS

- status: OPEN — traits-core lane's find (Order 23)
- kind: bug
- area: Type system & the type solver
- source: lane traits-core, 2026-08-31

A parameterized `main` is accepted and the emitted program hoists the
body so the parameter is undefined at runtime (`ReferenceError:
condition is not defined` calling `main(true)`). Nothing in the test
estate uses the shape. Either refuse parameters on `main` with a
message (probably right — nothing can call the entry with arguments),
or emit correctly; ruling decides which. Found while shaping a
fixture; worked around by moving the parameter to a helper.

## RULED 2026-09-01 — REFUSE, and give `main` a real argument door

The owner: "Refuse. Some kind of spelling of `process::args()` is
better. The developer might type the main parameters wrong. The
result type of that function is always correct. More generally,
parameters define what values are accepted, however, the program
doesn't have control over what's passed in. Only the shell owns
that." So: `fun main(…)` with parameters refuses with a message that
steers to `std::process::args()` — and that function ships in the
same lane (a `List<str>` of the argv tail, node leg; the browser leg
refuses it by platform coloring as usual).
