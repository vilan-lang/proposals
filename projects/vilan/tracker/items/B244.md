# B244 — a conditional `Wire` impl reached through two generic parameters at once has no concrete receiver: `ops.describe(s)` on `List<Delta<K, T>>` compiles and fails at EMISSION with the never-silent `internal: a call resolved to Wire's requirement describe, which has no body`

- status: OPEN — lane rpc-28's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

`Delta<K, T>::rebuild` at the same site is fine — it is the `List<Delta<K, T>>` chaining through two parameters. Pin the shape, find the dispatch that loses the concrete receiver, and check `Map<K, V>`'s new Wire impl for the same path.
