# E189 — CONTEXT-REACHABILITY diagnostics CASCADE behind a resolution error: on kolt at d783fbf4 with its seven retired `View` methods still written, `vilan check` reported 180 errors — 7 real (A99's steer) and 173 `context \`owner_scope\` is read here, but this code can be reached without an enclosing \`run\`` (146), `required by …'s \`context\` clause` (10), `reads context …, so it can't be used as a value` (9) and one injected-closure error, 74 of them in the generated lucide module that writes no closure at all; migrating the seven sites took the count to 1. Mechanism: a closure handed to a call that failed to resolve never attaches to the clause-carrying parameter it was written for, so every function it reaches (the pages, the icons, everything under them) is reachable 'without an enclosing run' along that path — one primary error fans out into every context read downstream of the closure

- status: OPEN — filed 2026-09-15
- kind: diagnostics
- area: LSP & tooling

context.rs: the reachability walk (`analyze`'s reach edges) skips edges whose call failed to resolve and closures that are arguments of such calls; then the program-level gate: `context` family diagnostics dropped when the diagnostics list already carries a resolution/type error (a note on the first primary error saying context checks were skipped). Pins: the kolt shape (a retired method taking a closure whose body reaches a context read — one error, not two); the pure missing-`run` program still errors; the broad gate's pin (a type error elsewhere + a genuine missing run → the run error is deferred, the note says so).

- 2026-09-17 (Order 37 GO): BUILD in lane diagnostics-37 (the narrow rule, then the broad gate as backstop; verified over a kolt copy with the seven A99 sites restored).
