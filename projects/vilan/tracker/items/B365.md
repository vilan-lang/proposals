# B365 — a VACUOUS arm in the drop planner's resource-reaching gate: analyzer.rs:11946 `Some(Expr::StructInitializer(struct_id, _)) => nominals.contains(struct_id)` compares the INITIALIZER's id against struct DEFINITION ids — always false (the `EnumVariant` arm beside it is right); the type check above it catches the common case, so the live hole is 'type not yet recorded' — a body reaching a resource can be skipped

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Type system & the type solver

`self.struct_initializer_to_def.get(initializer_id).is_some_and(|def| nominals.contains(def))`; a pin that reaches the arm (a struct literal of a resource-holding type whose type slot is unrecorded at the gate) planted red.
