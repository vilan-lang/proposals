# B379 — a generic parameter reachable ONLY through a BLANKET impl's trait argument does not bind through a STRUCT-RETURNING constructor call: '`T` is missing the bound `: PartialEq`' — while the nominal-impl route binds, and a non-struct-returning call binds (with a control)

- status: OPEN — filed 2026-09-21 (Order 39 sweep; collections-39's FIND 2)
- kind: bug (inference, HIGH — blocks A112 S3)
- area: Type system & the type solver

Shape: `impl type S: DeltaSource<List<T>, SeqOp<T>> { fun rows(self): Rows<T> { Rows::new(..) } }` — `Rows::new` returns a struct generic in `T`; inside the blanket body `T` is known only via the bound's trait argument, and the constructor call reports `T` missing a bound the blanket's own where-clause carries. Replace the struct-returning call with a plain function returning `T` and it binds; write the impl nominally and it binds. Fix: the blanket body's binder environment must be the one `bind_subject_bound_binders` grounds, at CONSTRUCTOR calls too (the struct-literal/constructor path apparently re-derives from the nominal environment). Pins: the lane's repro + control; the held S3 patch compiles.
