# B277 — `std::time::Debounce`'s driving loop runs under the ambient nursery, so a nursery cancellation unwinds it with `running` still true and leaves that `Debounce` value INERT (every later `run` is swallowed); not pinned

- status: OPEN — Order 30's finds (2026-09-09)
- kind: bug
- area: Reactive core & UI

Either a cleanup that resets `running` when the loop unwinds, or a stated scope rule (a debounce lives in the nursery that made it); pin the cancelled-then-run shape.
