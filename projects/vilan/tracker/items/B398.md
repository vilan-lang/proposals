# B398 — MISCOMPILE — `(U in T: dyn Source<U>)` (a mapped tuple over a `dyn` element) TYPE-CHECKS and then fails at runtime with `s[1].get is not a function` — the `dyn` coercion at a mapped-tuple element position emits no object (dyn-40's explicit-coercion rule has no arm for the comprehension's element type); probe `sweeps/order41/papers-41/probes/a122_09_mapped_dyn_source.vl` (`a122_09b` the controls)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: papers-41's report + the GO one-liners)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: papers-41's F-b (2026-09-24)
