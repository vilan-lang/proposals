

- 2026-09-24 (Order 41, reactive-41): CONFIRMED by reactive-41 as its defect 5 on 9ffa397e: a `(U in T: dyn Source<U>)` position does not coerce its elements and every `combine((a, b))` under the prepared S2c `combine` patch throws at run time — BLOCKS the flip's `combine`. Repro also in `sweeps/order41/reactive-41/finds/`.
