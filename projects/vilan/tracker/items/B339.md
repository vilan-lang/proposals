# B339 — the macro-world derive corner after N79: a macro world registers no macros, so a `[derive(..)]` written into one of the eleven std modules a world force-loads (`boolean/list/null/promise/compare/default/debug/json/hash/number/string`) would be refused with the load-ordering text for a perfectly good std; reach EMPTY today (documented at `IN_MACRO_WORLD`) — a `!in_macro_world()` guard is one condition when wanted

- status: OPEN — Order 35's finds (2026-09-13)
- kind: design
- area: Type system & the type solver

Add the guard with a pin that plants a derive into a force-loaded module fixture, or record the trap as a rule in the std authoring page.
