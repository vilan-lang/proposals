# E191 — a diagnostics CASCADE from one arity error in an element head: kolt login.vl:181/286 — `href(href())` (the attribute call given `href()` with its `Route` argument missing) 'caused a ton of error noise'; one wrong call inside `<a href(..) on:click(..) .styled(..)>` should report ONE error at the call, not fan out through the head's desugar (the head lowers to a chain; a failed argument may leave the chain's type unresolved and every later link reports)

- status: OPEN — filed 2026-09-17 (the kolt sweep)
- kind: diagnostics
- area: LSP & tooling

Reproduce (count the errors on the twenty-line shape); if E189's broad gate covers it (a type error present → the dependent family suppressed with the note) record that and pin the count; else fix the head's recovery so a failed attribute argument leaves the element typed (`View`) and the chain continues; one error, pinned.

- 2026-09-17 (Order 37 GO): BUILD in lane diagnostics-37 (reproduce; E189's gate or the head's recovery).

- 2026-09-17 (Order 37 sweep): BUILT the head's share in lane diagnostics-37 (b9ce70e2): on a kolt copy with `href(href())` restored, 194 on the base → 139 with E189's narrow rule — the 55 that went are the element head's own; pin `e191_an_arity_error_in_an_element_head_reports_alone`. The remaining 138 do NOT trace through the head: they enter at `app_shell`'s body because the failed attribute poisons the RETURN TYPE of the component the route arm calls — E189's family by a route the narrow rule cannot name; the broad gate closes it (same copy → 1) and waits on B355 (B279's non-diagnostic face). STAYS OPEN, re-pointed at B355 / the poisoned-return route.
