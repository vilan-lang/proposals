# N34 — the normative EBNF has no gate: grammar.md is review-only prose

- status: OPEN — the smalls lane's N32 finding (Order 22)
- kind: process
- area: Hygiene & rot
- source: lane smalls' report, 2026-08-29

Nothing in the tree reads `spec/grammar.md`'s productions: `grammar_sync.rs`
gates the two syntax-HIGHLIGHTING grammars against the compiler's word
tables, and the anchors golden pins headings only — the normative EBNF is
kept true by review alone, which is how `extern-args` went three forms
stale (and one form WRONG) across two releases with nothing red. The ask:
a gate that holds at least the attribute/production lists mechanically
derivable from parser tables (the extern-args set first, as the proven
rot site), or a recorded decision that the EBNF is documentation-tier
and the reference surface is the appendix.
