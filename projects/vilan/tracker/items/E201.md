# E201 — A101's breaking diagnostic (`A_CSS_DECLARATION_IS_A_CALL`, the flagship row at the `:`) carries NO quick fix, although the single-argument and plain-text cases are mechanical — exactly the codemod's own rules (`property: value;` → `property(value);`, typed where E167's converter types, string otherwise); it is the message every migrating program hits

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: editor
- area: LSP & tooling

A quick fix on the row rewriting one declaration (and a code action rewriting the whole block); the `!important` and glued-value shapes decline; pins in vilan-lsp document.rs.
