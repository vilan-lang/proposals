# N107 — E69 carries TWO implementations of one arrow: the python generator's default mode (TSV → Rust table) and the Rust sync gate `html_attributes_sync.rs`, which re-renders and diffs byte for byte — held equal by the gate, so drift reds on the first `cargo test`, but duplication; and `Error.note` is ONE location by contract, with no channel for a program-level footnote (what made 'a note on the first primary error' unbuildable for E189's broad gate when that error already had a note)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: process
- area: Hygiene & rot

Demote the python default mode to `--check`/`--fetch` only (the Rust gate is the authority); give `Error` a `footnotes: Vec<Note>` when the second footnote arrives (B355's gate is the first candidate).
