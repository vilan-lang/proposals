# B301 — `[derive(Wire)]` still EMITS `Json`/`FromJson` beside Wire (`derive_impl_source`'s `"Json" | "Wire"` arm — "additive beside the JSON impls until the codec re-plumb consumes it"), so a Wire FIELD must also be Json and the message says nothing about Wire: `[derive(Wire)] struct Row { tags: Map<str, i32> }` fails "cannot find 'from_json_value' in Map<str, i32>" / "has no method 'to_json'" on stock 65af4be0 (`Map` has been Wire since A39); after B289/A82 the same residue reaches `Result` and every hand-written `impl .. with Wire` — `Result` works as an `[rpc]` RETURN (kolt store.vl:184's case) but not yet as a derived FIELD

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Reactive core & UI

Finish the codec re-plumb the arm's comment promises: `[derive(Wire)]` emits the Wire visitor impls alone (the JSON pair only under `[derive(Json)]`), and the derive's field check reads the Wire predicate (B289's) — so a `Map`, a `Result`, a user Wire type all sit in a derived struct. Flip the two current-behaviour pins to the admit face; a `[derive(Json, Wire)]` control keeps both.
