# B431 — a B412 erasure of a generic parameter that is INSTANTIATED at a resource is not refused — trait-objects Q5 ('a `dyn` may hold no resource') is checked at the coercion of a concrete type and cannot see through a generic `S` erased to `dyn` when `S` is later bound to a resource

- status: OPEN — filed 2026-09-25 (Order 42, solver-42's find)
- kind: bug (soundness)
- area: Type system & the type solver

Fix: the Q5 check runs at monomorphisation as well (or the erasure of a rigid parameter requires a `S: !Resource`-shaped negative bound the language lacks — so the mono check). Pin: a generic erasing to `dyn` instantiated at a `[resource]` type is refused by name. Sizing S–M; solver.
