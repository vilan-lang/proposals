# M50 — M24's byte budget is denominated ~24× LOW: `base_cache_retained_bytes` records 195,087 B for a retained world that weighs 4,722,688 B resident (eight distinct keys minted via `macro_limits`, minus a same-key control, byte-identical across three runs) — 512 MiB of recorded bytes is on the order of 12 GB resident; the cache is bounded, but not where M24 thinks

- status: OPEN — lane m36's find (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Measure what the retained-bytes counter misses (the leaked ASTs, the interned tables, the macro worlds hanging off the registry), correct the denomination or the budget, pin the world's weight against the counter within a factor.
