# A77 — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel

- status: OPEN — Order 31's finds (2026-09-09)
- kind: feature
- area: Reactive core & UI

`ClientProxy::detached()` (a proxy that says so when called) or make the proxy field `Option`; pin the browser-side construction.
