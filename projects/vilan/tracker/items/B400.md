# B400 — MISCOMPILE — COPY ELISION on JS is wrong when a `Shared::read()` TEMPORARY is live while the cell is written: `for x in cell.read() { cell.write().push(..) }` iterates the GROWING array (JS b=4, native b=3); and a CLOSURE whose parameter is `&List<T>` accepts `cell.read()` where a `fun`'s `&` parameter refuses the same argument, and sees the live list (JS seen=3, native seen=2) — the checker gap is the closure parameter's `&` admitting a temporary read. `ListCell::peek` (M86) is written with that closure spelling and documents that its closure must not write the cell; when the gap closes, `peek` re-spells through `Weak::get`, which the Rust backend cannot emit yet (`WeakGet`)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: collections-41's report)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: collections-41's find 1 (2026-09-24); repros in `sweeps/order41/collections-41/finds/`; solver + native

- 2026-09-25 (the owner's rulings after Order 41's seal): `peek` KEEPS its name (the owner, 2026-09-25) — and the owner notes that not every keyword needs banning as an identifier (`with` should be usable; declaration keywords `let`/`mut`/`fun`… stay reserved): filed B414 (contextual keywords).

- 2026-09-25 (Order 42, solver-42): STOPPED (stop condition): the closure `&`/`&mut` view check refuses legitimate std code — rpc.vl `KeyedCell::update`'s `mutate(list[at])` at a `|&mut T|` parameter (~line 4130) — failing `check_scope_differential::every_std_module_is_clean_under_full_scan` and `vilan-wasm a_server_program_checks_clean_for_node`. OWNER Q: respell std as `&mut list[at]`, or scope the check (rec: scope the check — a `|&mut T|` parameter fed an lvalue subscript is not the temporary-read hazard; the hazard is `cell.read()` as the argument). The full patch (check, pins, AND collections-42's peek re-spelling, applied with -p0, M86 green, shared_census 2/2) is saved at scratchpad/solver-42/b400-closure-view-check.patch — copy it into proposals before the scratchpad is cleaned. The `for x in cell.read()` half is a copy-POLICY question (shared.vl promises reads copy nothing). Order 43.

- 2026-09-26 (the owner): RULED as recommended — SCOPE THE CHECK: a `|&mut T|` parameter fed an lvalue subscript (`mutate(list[at])`) is not the temporary-read hazard; the hazard is `cell.read()` as the argument. The saved patch (sweeps/order42/solver-42/b400-closure-view-check.patch, incl. peek's re-spelling) lands with the check narrowed. Order 43, solver.
