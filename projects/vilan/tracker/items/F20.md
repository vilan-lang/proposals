# F20 — the path to `board.vl` (Order 38's unreached native exit): three constructs are 29 of the backend's 68 refusals — the host type `Hash` (10 programs; `std::reactive`'s `CanonicalHash`, board.vl's one remaining wall), `lazy` parameters (12; the whole `Option`/`Result` combinator surface since A103 — a thunk at the call, `Rc<OnceCell>`-shaped force in the callee; M81's eager set shrinks it), overloaded operators (7; `binary_op_dispatch` is already read by the refusal); then backed enums (5), `for` over an `Iterator` impl (5), the `?` lift behind `lazy`

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: feature
- area: Native targets & rendering

In that order; exit: `board.vl` byte-identical on both backends and the whole set at 0 differing / 0 rustc-refused; `JSON.stringify` needs its OWN renderer (`Js` is `console.log`-shaped); `Map`/`Set` gain `impl Js`.
