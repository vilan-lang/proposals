# A98 — A91's region reconciler costs +1 node per row and roughly DOUBLE the DOM mutation calls per reconcile pass (1,000 rows: nodes 1,005 → 2,005; insertions ~4,000 → ~8,000), because the order pass cuts every live row by span and re-inserts it whether or not its index moved; the plan knows `Keep(index)` but not whether the index moved — skip the cut/re-insert for an unmoved row

- status: OPEN — Order 35's finds (2026-09-13)
- kind: perf
- area: Reactive core & UI
- 2026-09-14 (Order 36 GO): BUILD in lane lang-a-36.
- 2026-09-14 (Order 36, lang-a-36): LANDED af9e859a — `settled_steps` (two greedy scans, forward and backward, the better wins — LIS-equal on every pinned shape) + `row_references`; `Region::open_row_before`; 1,000-row harness: append/prepend cut 1000 → 0, remove/change/move cut 1000 → 1, reverse 999 (control); Ir −61.2 % whole harness (stub-dominated, stated). Pin `a98_the_order_pass_leaves_an_unmoved_row_alone` (cost AND tree per edit; non-vacuity proved). OPEN Q built as recommended: greedy scans, not a patience-sort LIS — `settled_steps` is the one fn to replace if ever wanted. CLOSE at the sweep.

Track the previous index in the plan; cut/re-insert only rows whose position changed; measure with the same 1,000-row harness (nodes, insertions, Ir under `--jitless`).
