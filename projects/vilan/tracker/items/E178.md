# E178 — vilan-ide's two completion filters (`OriginListing::completions`, `auto_import_completions`) do not consult the visibility bit — the third tooling consumer B318 S1 named, left because the file was another lane's; and `module_impl_blocks` / the selector member arm should filter unexported subjects once `export impl` is enforced

- status: OPEN — Order 35's finds (2026-09-13)
- kind: editor
- area: LSP & tooling
- 2026-09-14 (Order 36 GO): BUILD in lane editor-36.
- 2026-09-14 (Order 36): editor-36 880f92aa filtered THREE consumers (`OriginListing::completions`, `module_member_completions` — unnamed by the item, `impl_selector_completions`); visibility-36 efa55a2f exposed `Program::exported_entities` + `Program::curated_modules` (= `Analyzer::is_exported_in`; pin `e178_the_program_visibility_fields_agree_with_module_importables`) for the FOURTH consumer `auto_import_completions`, which cannot call `module_importables` outside `owned_modules::collecting()` (a parse-cache leak per keystroke). The wiring of the fourth consumer is E184 (filed). CLOSE at the sweep as landed-with-follow-up.

Filter on `Importable.exported` under the uncurated-module exemption; pins for a curated and an uncurated module.
