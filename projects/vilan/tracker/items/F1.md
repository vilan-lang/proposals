

- 2026-09-21 (Order 38 sweep): S1b BUILT in lane native-a-38 (835c29d2) with F19 first: monomorphisation by reproducing the JS emitter's mechanism (its instance SET cannot be shared), generic structs/enums one Rust item per instantiation, generic-parameter dispatch + trait-default specialization, module-level bindings as `thread_local!`, `Weak`, `if let`. WHOLE SET (117 platform-free): byte-identical 29 → 47 → 49 after J6's rebase; differing 1 → 0; rustc-refused 2 → 0; broken 4 → 0 — the base was RED and §10's '0 differing' was the ten-program default suite's view; the seal runs the whole set from now on. THE ORDER'S NATIVE EXIT IS NOT REACHED: `board.vl`'s recorded wall (an `is`-capture) was a FALSE POSITIVE over a context-threaded hidden parameter; its real wall is the host type `Hash` → F20. Paper §11 as-built.
