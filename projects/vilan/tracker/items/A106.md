# A106 — the style vocabulary is ambient only inside a css HOLE (`std::style::prelude`, names.md §4.7's one-position rule): a `fun` body building a chain imports nine names per function (kolt styles.vl:54/103 — `style as s`, `Length`, `Cursor`, `TextAlign`, `AlignItems`, `active`, `attribute`, `hover`, `pseudo` — under a `// FIXME: Add a prelude to std.`). Doors: (1) document `import std::style::prelude::*;` at file level as the idiom (the module is ordinary outside a hole — but the TYPES `Length`/`Cursor`/`TextAlign`/`AlignItems` are not in it); (2) the `std::web` prelude `export import`s a widened style prelude — ambient in every web file (E180's R9 rule fades it cleanly), gated on a name-collision census over the estate; (3) nothing. Rec: widen the prelude with the types (S), document (1) now, take (2) after the census

- status: OPEN — filed 2026-09-17 (the kolt sweep); OWNER QUESTION
- kind: design
- area: Reactive core & UI

Census first: every name the widened prelude would bind against every identifier declared in std, the examples, the docs, the website and kolt (read-only); then door (1) + the widened module, door (2) only at zero collisions; the tour's styling page names the idiom.

- 2026-09-21 (Order 38 GO): R7 RULED as filed — widen the prelude with the four types + document the idiom now; door 2 only at a zero-collision census (lane std-38 runs it).

- 2026-09-21 (Order 38 sweep): R7's door 1 LANDED in lane std-38 (9f6a174c): `AlignItems`, `Cursor`, `TextAlign` joined the prelude (`Length` was ALREADY reachable — this item's text was wrong). CORRECTION to R7: `import std::style::prelude::*;` is NOT valid vilan (no wildcard import) — the documented idioms are a brace list and `import std::style::prelude as tokens;`. DOOR 2's census is NOT zero: 6 file-level shadows (kolt lucide `divide`/`focus`/`space`; docs `struct Cursor`, `scale_step`, `space`), 40 local, 122 redundant imports — door 2 NOT taken; re-census after A117 hides the prelude's 21 alias spellings. Stays OPEN for door 2.

- 2026-09-22 (Order 39 sweep): door 2 RE-CENSUSED by std-39 with A117's 46-name prelude: file-level shadows 7 → 5, scope-local 22 → 22, redundant imports 75 unchanged — only ONE colliding name was an alias (`scale_step`); what collides is words (`space`, `divide`, `focus`, `first`, `last`, `s`, `active`, `within`, `hex`). Door 2 stays untaken; the item stays open on it.
