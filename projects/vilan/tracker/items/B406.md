# B406 — B389's remaining gap — a literal passed to a GENERIC CONSTRUCTOR inside a STRUCT LITERAL FIELD is not typed from the field: `struct S { count: Shared<u53> }` then `S { count = Shared::new(0) }` refuses with `Expected Shared<u53>, but got Shared<i32>`; std's `rpc.vl` hits it (index-41's `b389-gap-literals.patch` carries two suffixes until fixed)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: index-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: index-41's find 1 (2026-09-24); solver lane, before I5 S2

- 2026-09-25 (Order 42, index-42): the gap is WIDER than the field case — an ANNOTATED LET refuses too on d65d4e75: `let a: Shared<u53> = Shared::new(0);` → 'Expected Shared<u53>, but got Shared<i32>' (kolt's `let selected_index: SignalCell<usize> = Signal::new(0);`); solver-42 told to pin the let shape under B406. Sibling find → B423 (an `if` arm's literal not typed from its sibling arm).

- 2026-09-25 (Order 42, index-42 on next @eafd61ef): RESIDUAL after the fix (68c05c30): still mistyped when an EARLIER field of the same struct literal holds a `Shared<i32>` — `Rs { channel = Shared::new(5), count = Shared::new(0) }` with `count: Shared<usize>` gives Expected Shared<usize>, got Shared<i32>; swapped fields compile; same with a binding or `Shared::new(channel)` (the first field's instantiation leaks into the second). std's rpc.vl RemoteSource/KeyedSource are this shape → index-42 KEEPS the two `0usize` suffixes. Repro sweeps/order42/index-42/b406-gap-repro.vl; solver-42 told (a follow-up after B409).

- 2026-09-25 (Order 42, solver-42): FIXED (68c05c30 + 54acf502): the literal rule bound only the function's own generics — `Shared::new`'s `T` belongs to the IMPL, so annotated lets and declared returns were refused too (premise widened as index-42 found); impl generics now bind from the expectation and each field's type seeds its expectation when closed; the follow-up seeds EVERY field before any is checked (the order-dependent residual gone); a B225 regression (kolt's shape) caught and fixed; pins over u53/usize/u8/f64, let/return, `Signal::new(0)`, both field orders. index-42's two `0usize` suffixes can now drop (Order 43 hygiene).
