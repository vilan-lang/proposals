# I4 — `Default` is implemented for the numeric family only (number.vl): `Map`, `List`, `Set` (and `str`, `bool`, `Option<T>` — verify each) have no `Default`, so a `T: Default` bound cannot be met by a container and kolt writes `impl Map<type K, type V> with Default` itself (prefs.vl:100 under `// FIXME: Implement with std.`)

- status: OPEN — filed 2026-09-17 (the kolt sweep)
- kind: feature
- area: Collections

The empty-container impls beside the numeric ones (`export impl Map<type K, type V> with Default`, `List<type T>`, `Set<type T>`; `str` → `""`, `bool` → `false`, `Option<T>` → `None` if absent); a pin per type in `inference/traits.rs` or `std_surface.rs`; the `Default` doc row; kolt's impl goes at the owner's word.
