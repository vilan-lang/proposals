# B405 — NUMERIC MIXING is silent: `let y: i32 = 3; print(y / 2.0)` prints `1` — an `i32` operand meets an `f64` literal and the expression is integer division with no diagnostic (the literal takes the LEFT operand's type under B389's rule, which is right for `i32 / 2` and wrong for `2.0`: a fractional literal cannot be an `i32`); the deferred numeric-mixing family — rule whether a fractional literal at an integer-typed position is REFUSED (rec: refuse with an `as_f64()` steer; B402 covers the literal-only case)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: solver-41's report)
- kind: bug (inference) + ruling
- area: Type system & the type solver

Discussion: solver-41's find 2 (2026-09-24)

- 2026-09-25 (the owner's rulings after Order 41's seal): RULED — REFUSE a fractional literal at an integer-typed position, with an `as_f64()` steer (rec accepted). Order 42, solver.
