# M73 — an open module's world is stored UNRESOLVED, so ~150 ms of `resolve_world` is paid on every keystroke in M70's six kolt files (theme.vl build 161 ms vs views.vl 11 ms with the same checks phase); the naive import-fixpoint split moved 0.4 ms of it; a fix has to defer only the ALIAS-reaching work rather than the whole pre-entry pass — scope-chain territory, size after B318 S4 (landed)

- status: OPEN — filed 2026-09-15
- kind: perf
- area: Performance & footprint

Split `resolve_world` at the alias boundary (preludes/use-drain/conformance/constraint fixpoint for non-alias scopes first); measure the six.

- 2026-09-17 (Order 37 GO): BUILD in lane perf-37.
