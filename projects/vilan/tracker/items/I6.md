# I6 — RULING — NEGATIVE-to-UNSIGNED conversion differs by backend: `(-5i8).as_usize()` answers `-5` on JS and `0` natively (`u53` has always done this; `usize` mirrors `u53` on purpose). Rule which answer is the language's: (a) saturate to 0 on both (native's answer; JS emits a `max(0, ..)`), (b) wrap (neither today), (c) unspecified like underflow (I5 ruling 2's class — then the native differential excludes it by name). Rec (a): a conversion is an explicit call and should have one defined answer

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: index-41's report)
- kind: design (ruling)
- area: Collections

Discussion: index-41's find 3 (2026-09-24); I5's family

- 2026-09-25 (the owner's rulings after Order 41's seal): RULED as recommended — `as_usize()`/`as_u53()`/… of a negative SATURATES to 0 on every backend (the owner: many numeric operations stay unspecified for performance, but this one is fair and cheap). Order 42, with I5 S2.
