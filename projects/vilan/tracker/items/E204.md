# E204 — `///` annotations on struct FIELDS do not 'work' (the owner, 2026-09-21): measured on 0fa109eb7 — a `///` above a field PARSES and `vilan fmt` keeps it, and ONE consumer reads it (the struct-initializer completion's `documentation`, pinned by `struct_initializer_completion_details_the_field_type_and_doc`); the HOVER on a field read or a field declaration shows the fenced name and type only (`hover_on_a_field_shows_the_fenced_name_and_type` asserts nothing about docs), and the other consumers are unverified

- status: OPEN — filed 2026-09-21 (the owner's ask, mid-Order 38)
- kind: editor
- area: LSP & tooling

Census first, then make every consumer carry it — one row per consumer, each answering 'does a field's `///` reach it': hover on `p.x` (a field read), hover on the declaration, hover on a struct-initializer field name (`Point { x = … }`), MEMBER completion after `p.` (detail + documentation), the struct hover's declaration block (E9 — should a documented field show its first paragraph?), signature help for a struct literal if any, the docs generator (`vilan doc`/the book's std pages — do std's documented fields render?), enum VARIANT payload fields and tuple-struct positions (can they carry a `///` at all?), `[derive]`-generated members (they should inherit nothing), and the wasm playground's hover. `doc_comment_of(id)` (vilan-lsp document.rs ~3020/3185/3293) is the reader hover already uses for functions/bindings — the likely defect is that a FIELD's id is never asked, or that the doc is attached to the struct's field list under a key hover does not look up. Also confirm `//!`-style or trailing `// …` comments are NOT meant to count.

Pins owed: a hover pin per census row that lands; the first-paragraph rule matches functions'; a documented field in std (pick one) renders in the book; fmt idempotency over a documented field (exists? name it).
