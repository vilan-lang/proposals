

- 2026-09-21 (the owner, after the paper): §13 RULED, all as recommended — Q1 `map_each`/`filter_each`; Q2 two hand-written default lists, no macro; Q3 `Move` is an op from S1 (the wire translator lowers it to `Delta::Reset`); Q4 `KeyedCursor` → `DeltaCursor` in std::reactive with a deprecated re-export in std::rpc for one release. The paper's two decisions stand (`Splice` carries removed VALUES; `set(whole)` records `Reset`, the diff door is `reconcile_to`). Slice order S1 (the lift) → S3 (delta-driven `each`) first; S2 (`map_each`) after. READY for Order 39.
