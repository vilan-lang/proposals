

- 2026-09-21 (Order 38 sweep): the PAPER is `proposal/incremental-collections.md` (lane papers-38; eleven probes kept at `sweeps/order38/papers-38/probes/`): the law holds over 555 randomized checks; a delta-driven `each` is 16,040× cheaper at 1,000 rows and today's `reconcile` scan is QUADRATIC (M82). Corrections to this item as filed: `Splice`'s `removed` is `List<T>` (values, not a count) and `SetAt` carries both; `Move` is admitted at S1; a per-element OWNER is not enough — the element needs a stable per-element cell. §13 RULED (above). READY: S1 → S3 → S2.
