# E150 — fmt-29's residuals: E147's break threshold is a COUNT (two operators) — `std/src/markdown.vl:537` stays at 118 columns because its last operand is a two-operand `||`; a MEASURED threshold (break when the widest resulting line is narrower) reaches it but flips `a_single_link_right_operand_leaves_the_line_long` (116 → 105, still over budget); a `match` leg body written `=> { expr }` still expands where an `if` arm now stays inline; `for`/`while` conditions and a `match` subject do not take the split permission an `if` condition now has (no over-budget instance in the tree)

- status: OPEN — lane fmt-29's finds (2026-09-06, Order 29); OWNER Q
- RULED 2026-09-07 (the owner): recommendation accepted — the COUNT threshold stays; the `match`-leg inline rule and the split permission for `for`/`while`/`match` subjects build.
- kind: editor
- area: Language server & tooling

Rule on count versus measured; extend the inline-arm rule to a `match` leg and the split permission to the other conditions with pins; one reformat commit if the tree moves.
