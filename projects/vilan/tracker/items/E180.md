# E180 — Organize Imports BREAKS kolt's generated `src/lucide/lib.vl`: under `prelude = "std::web"` its two imports (`std::option::Option`, `std::ui::{ View, view }`) are redundant with the prelude (rule 0 — correct, the file checks clean with both deleted), but E168's module rescue then rewrites them to `import std::option;` + `import std::ui::{ (impl View) };`, and `import std::option;` binds the module name `option` over the file's own `fun option()` icon, so the organized file fails to check ('`option` is a module, not a value' at `Option::Some(option())`, lib.vl:17453). Two defects: (1) rule (2) `module_import_brings_a_use` counts every occurrence DECLARED in the module's file minus the file's own leaves, but not minus the PRELUDE's bindings and not minus what is reached by qualification through a bound head — `Option::Some`'s variants are option.vl declarations reached through the prelude-bound `Option`, so the module 'brings' them, `rescuing_subject` finds no impl member and answers `ModuleRescue::Module`; (2) a rescue INTRODUCES a binding (`import <module>;` binds `<module>`) with no check that the name is free in the file — the organizer must never write a statement whose new name collides with a declaration or another import; keep the statement verbatim (leaves unfaded) rather than break a green build

- status: OPEN — filed 2026-09-13
- kind: editor
- area: LSP & tooling

Where: `Document::import_leaf_is_used` rule (0) (document.rs ~5010, `prelude_bindings`), `module_import_brings_a_use` (~5140), `rescuing_subject` (~5215), `import_module_is_used` (~5185), `rewritten_import_statements` (~4580); `formatter::module_only_import_branch` (formatter.rs:1792) and `ModuleRescue` (1771). Fix (1): rule (2) subtracts `program.prelude_bindings` (the same `Definition::Entity` address space as `bound_by_leaves`) and counts only what a module import ALONE carries in today's program-global impl model — an impl member resolved by RECEIVER syntax (`x.child(..)`), never a definition spelled as a path segment behind a head (`Option::Some`, `Type::new`): the head is a leaf's, the prelude's, or the module leaf's own (rule 1). The lane finds the analyzer's method-call resolution record to tell receiver resolution from a path segment (the reference index alone cannot; a text check for a preceding `::` is the fallback). Optional third subtraction: a module the prelude module itself `export import`s (web.vl:47–48 — `pkg::style`, `pkg::ui`) is loaded by every file of the package, so rescuing it as `{ (impl View) }` is harmless but redundant; under B318 S4 (per-importer namespace) the prelude carries impls and this predicate is re-pointed anyway (B318's own note) — take it only if cheap. Fix (2): the collision guard — before `Module` (and before `Selector`, which binds nothing and needs none), the module SEGMENT's name must be free in the file: no top-level declaration, no other import leaf or alias binds it (shadowing an ambient prelude name is fine: an explicit import beats the prelude). Not free → a new `ModuleRescue::Keep` arm: the statement is printed verbatim and its leaves do NOT fade (E114/E173's contract — what fades is what the action removes, and the action removes nothing). Fade text: a rule-(0) leaf currently says 'unused import'; say what it is — 'redundant: the prelude already binds `Option`' — so a user looking at a heavily-used name faded is told why (E145 territory). Pins: (a) a file under `prelude = "std::web"` importing `std::option::Option` and spelling `Option::Some(..)` prunes the statement outright — no `import std::option;` rescue — and the organized text checks; (b) the same file declaring `fun option()`: no rewrite that binds `option` ever lands; (c) `import std::ui::{ View, view };` there prunes (or rescues as the selector if the third subtraction is not taken) and the organized text checks; (d) E168's pins (an impl-only module use keeps `import pkg::a;`) and E169's (a module leaf beside its own leaf prunes) still hold; (e) the rule-(0) fade names the prelude. Verify over kolt: organize `src/lucide/lib.vl` in a copy and `vilan check` it.
