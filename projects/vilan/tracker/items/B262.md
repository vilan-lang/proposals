# B262 — B251's written-argument bound check is STRUCT-only: enums and traits reach the same `prepped_type_locals` drain with the same unchecked written arguments (`Held<i32, SignalCell<List<str>>>`'s enum twin is still accepted)

- status: OPEN — lane solver-29's find (2026-09-06, Order 29)
- kind: bug
- area: Type system & the type solver

Extend `check_written_nominal_bounds` over `enums` and `traits`; census the estate (expect zero as for structs); pins per kind plus a satisfied control.
