# B191 — a `let`-bound self-call read in the tail of an unannotated recursive body cannot be resolved (B126's residue, orphaned)

- status: OPEN — re-owned 2026-09-01; found by N27's cross-check on its FIRST run
- kind: bug
- area: Type system & the type solver
- source: `tests/inference/returns.rs:864`, `#[ignore]` reason "B126 residue, 2026-08-22" — B126 is tombstoned, the N31 anti-pattern

`fun g(n: i32) { if n == 0 { 0 } else { let x = g(n - 1); x + 1 } }`
— a self-call bound by a `let` and read in the tail still fails
"could not be resolved": a `let` binding is not read through its
initializer on the inference path for an unannotated recursive body,
where the direct-tail form (`g(n - 1) + 1`) resolves. B185 gave
`unfilled_closure_parameter` exactly this initializer-following move
for closure parameters; the recursive-return fixpoint wants the same
hop. Retarget the pin's reason to B191.
