# N88 — std doc comments outside lang-a-36's ownership still say `bind_each` after A99 — `reactive.vl` (7 lines), `rpc.vl` (7), `math.vl` (1) — plus `browser/ui.vl:537`'s `child_relation` mention after A95 S2, `ui.vl:1187`'s now-redundant `context owner_scope` annotation (B333 adopts it), and formatter.rs's two element-head layout pins that spell `.bind_each(` (syntactic, pass, name a method that no longer exists)

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

One doc sweep; a grep pin that std's comments name no retired method.

- 2026-09-17 (Order 37 GO): BUILD in lane hygiene-37 (comment lines only; merges before native-a-37).
