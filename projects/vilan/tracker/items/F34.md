# F34 — native — the two reactive COMBINATORS are refused: `SignalCell::new(1).map(|n| n * 10)` answers 'unbound generic type parameter (parameter 1 of `map`)' and `outer.flatten()` over any `Source` is refused on a `?` lift — so no derived signal builds natively today, which the A124 pipeline (S2) will make the common case

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: bug (native)
- area: Native targets & rendering

Sizing M; the `map` half is a generic-parameter binding through a trait default with a closure argument (B379's neighbour); the `flatten` half is the `?` on an `Option` payload inside a blanket. Both differential-pinned when fixed.

- 2026-09-24 (Order 41, native-41): BUILT on today's `reactive.vl` (83f4db67) — premise partly off: the analyzer DOES record `U` for `map`; the native backend passed `&[]` on the receiver-type dispatch path and keyed a default instance on its receiver alone; `flatten`: `?` lifts and lifting regions over `Option`/`Result` now lower to a `match`. Pins: map/flatten probes + `reactive-on-change.vl`, `reactive-flatten.vl` in the default suite; `reactive.vl` and `iterator-adapters.vl` flipped. The `.cell()` chain re-run waits on the lane's rebase over reactive-41's S2b (its one rebase).
