# B320 — a MALFORMED import reports `found 'import' expected an expression` at COLUMN 1 of the statement, whatever the typo (six probes, identical): the import grammar's failure falls back to the expression parser, which points at the keyword — acute once B318's new import forms (`#`, `only`, `(impl T)`, `::*`) multiply the ways an import can be mistyped

- status: OPEN — Order 34's finds (2026-09-13)
- 2026-09-13 (Order 35 GO): BUILD, lane visibility-a-35.
- kind: bug
- area: Type system & the type solver

Make the import parser report at the token it stopped on with a curated rule naming the import grammar (one row); pin the six probe shapes; the expression fallback stays for everything that is not `import`-led.
