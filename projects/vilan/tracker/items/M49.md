# M49 — m19-t1c's residuals: the resource-reaching nominal FINGERPRINT includes entry-declared nominals, so two entries in one package that differ in whether they declare a resource invalidate each other's enrolment record (split world-declared from entry-declared); and `LastUse::compute` still costs 258–558 ms warm after T1b — the restore may not reach as much as assumed (re-price)

- status: OPEN — lane m19-t1c's find (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Two small slices under M19: split the fingerprint; profile `LastUse::compute` warm on kolt's client and say what the restore misses.
