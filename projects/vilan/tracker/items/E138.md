# E138 — hover on `PartialEq::eq` and `PartialOrd::lt` returns nothing through both routes (default body, generic bound) where `Add::add`/`Sub::sub`/`Mul::mul` resolve

- status: OPEN — lane cascade-27's find (2026-09-04, Order 27)
- kind: editor
- area: Language server & tooling

E128 pins the `PartialEq` SHAPE on a user trait because the std traits' members do not resolve as hover targets at all. Localize in the ide's target resolution (operator traits with a `= Self` parameter vs the comparison traits' derive-provided members?). Pins: hover on each of the four, through a default body and through a bound.
