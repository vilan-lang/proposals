# G22 — a `.vl` file reached under two names collects twice

- status: OPEN — symlinks lane's residual (Order 23)
- kind: bug
- area: Macros & const
- source: lane symlinks, 2026-08-31

G18's cycle guard keys the visited set on DIRECTORY identity only, so
a FILE symlink lets one file into the collection under both names —
`fmt --check` can print two `would reformat` lines for one file.
Pre-existing, out of G18's charge. Fix: extend the identity check to
files at collection, same `DirectoryIdentity` device+inode shape (and
the Windows arm rides audit run 7's symlink charter).
