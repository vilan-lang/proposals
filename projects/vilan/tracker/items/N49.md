# N49 — `release_differential` is the union's critical path at 615 s (one test): split per corpus program

- status: OPEN — hygiene-25 lane's find (2026-09-01)
- kind: hygiene
- area: Testing & CI

`the_release_preset_is_observationally_neutral_over_the_corpus`
runs the whole corpus in one test — 615.5 s under load, priority
100 already, the union's last finisher. Split it into one test per
corpus program (a macro or a generated list), so nextest schedules
them across cores and a single program's regression names itself.
Keep the corpus-wide neutrality claim as the SUM of the parts.
