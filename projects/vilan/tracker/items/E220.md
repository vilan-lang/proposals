# E220 — the MISSING-MEMBER sentence for a supertrait reached with a CONSTRUCTED argument shows the wrong argument: `trait Feed<T> with Base<List<T>>` + `impl Cell with Feed<i32> {}` says 'declare `fun read(self): i32`' where it must say `List<i32>`

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: diagnostics
- area: LSP & tooling

Substitute the supertrait's arguments before rendering the missing member. Pin red-first. Sizing S.
