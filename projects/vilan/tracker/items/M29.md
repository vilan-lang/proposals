# M29 — completion's third tranche: a whole-buffer `tokenize` + `ParsedSource::parse` per request, and the member-completion resolution walk (now the most expensive single request)

- status: OPEN — lane m25-e125's finds (2026-09-03); the keystroke path's remaining completion budget
- kind: performance
- area: Language server & tooling

After M25 (the captured `CompletionIndex`), kolt views.vl's five-
provider burst is 2.64 ms and completion still pays, per request: a
whole-buffer `tokenize` (`completion.rs`'s first line) and a whole-
buffer `ParsedSource::parse` (~0.47 ms — what every auto-import edit
is computed against); member completion is 2.63 ms — a resolution
walk M25 does not touch. Build: §2.1.4's incremental lex of the
current line (the keystroke path's anchor already knows the edit
window); the auto-import edit computed against the CAPTURED parse
with the anchor's mapping, not a fresh one; member completion served
from the symbol index's per-type member tables (rebuilt when the
type's module stamp moves). On-demand paths left as they are:
`import_candidates` (the add-import quickfix) and
`module_member_completions` (parses the module through the shared
cache). Gate: kolt burst under 1 ms hot-from-edit; the candidate-
identity pin extended to member positions.
