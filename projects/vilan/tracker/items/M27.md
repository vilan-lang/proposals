# M27 — `lsp-index` is a fifth per-keystroke cost nobody budgeted: the editor tables at `document.rs:1257` cost 110–584 ms wall on kolt, outside `analyze` and every tranche

- status: OPEN — paper lane m19-paper's find (2026-09-03); E121's diagnostics path
- status update 2026-09-04 (Order 27, lane e126 @e27dee56): the MEASUREMENT half landed — and the premise was stale: `lsp-index` has been on the LSP phase line since E106 (document.rs ~1395); the gap was that the print ran BEFORE `capture_landed`, so E121's landed whole-program walk was outside the line. Now printed after, with `lsp-landed`, and on the session trace (`Document::index_time` → `AnalysisTally`). kolt per keystroke: `lsp-index` 110–292 ms wall at loadavg 31–33 (494–1,058 + landed 232–350 at 150–166); the view exhibit 691–785 + 187–230 at 166. A cheap fold exists: `entity_spans` + `landed_symbol_index` are one declaration sweep, `ReferenceIndex::build` + `semantic_tokens()` one occurrence walk. The per-module FIX half stays open.
- status update 2026-09-05 (Order 28, lane perf-28 @799c897f): `entity_spans` is per MODULE (0.014 ms vs the whole-program scan's 8–11 ms, 591–798×, same rows); the rest of `lsp-index` is still whole-program, and the per-module half needs entity ids stable across analyses — M19 T1's world gives them; the fold e126 named (one declaration sweep, one occurrence walk) is still open. NARROWED to that.
- kind: performance
- area: Language server & tooling

After `analyze`, the LSP builds its editor tables (`lsp-index`:
references, symbols, the keystroke path's `capture_landed` is a
separate, measured item) — 110–584 ms wall on the full kolt copy vs
65–88 ms with lucide stubbed to 4 icons. It is outside the phase line
and outside M19's tranches. Measure it on the phase line (N43's
labels gain `lsp-index`), then make it per-module like the analysis
it indexes (a cached world's modules keep their index; only the
edited buffer's rebuilds) — or fold the tables into
`capture_landed`'s single walk. Pin: `lsp-index` ∝ the edited
buffer's size, not the program's.
