# N130 — Windows clippy over TESTS is red and nothing gates it: `cargo clippy --target x86_64-pc-windows-msvc -p vilan-cli --tests` fails on `split.rs` (an unused `Instant` import under the cfg) — pre-existing on 1265ea5d; the seal's Windows cross-check runs without `--tests`. Fix the import (one line, any lane) and decide whether the seal's Windows leg adds `--tests`

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: hygiene-41's report)
- kind: hygiene (gates)
- area: Hygiene & rot

Discussion: hygiene-41's find 6 (2026-09-24); integrator one-liner

- 2026-09-25 (Order 42, std-42): FIXED (0cd28e75) with a correction: the seal's Windows line already had `--tests` — the gap is `cargo check` (no `-D warnings`); the Windows cross-check is now `cargo clippy --target x86_64-pc-windows-msvc --workspace --exclude vilan-rt-sqlite --all-targets -- -D warnings` in `scripts/ci-local.sh windows` (the old leg was red on every Linux box — vilan-rt-sqlite's bundled C build has no msvc compiler); seal.sh line 17 changed by the integrator to the same command.
