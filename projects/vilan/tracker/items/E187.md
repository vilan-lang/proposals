# E187 — a cross-file quick fix (E177's Export fixes) and E175's sibling reads take the declaring file from DISK, so an edited-but-unsaved sibling is edited at its SAVED offsets — the same bargain go-to-definition makes; a server-side document-map lookup (the open buffer's text when the sibling is open) closes it

- status: OPEN — filed 2026-09-15
- kind: editor
- area: LSP & tooling

Consult the server's open-document map before the filesystem in both readers; pin: an unsaved sibling edit still lands the `export` at the right line.
