# E206 — a function HOVER shows only one signature; for a GENERIC function the developer wants both — the declaration as written AND the signature computed at this use. The owner's ask (2026-09-21): `fun get_or(self, key: K, make: || V): V` then a blank line then `fun get_or(self, key: UserId, make: || SignalCell<Option<User>>): SignalCell<Option<User>>` — the definition, and the exact function this call reached

- status: OPEN — filed 2026-09-21 (the owner's ask, mid-Order 38)
- kind: editor
- area: LSP & tooling

Scope: a hover on a CALL SITE or a method reference whose callee is generic — by its own `<T>` parameters, by its impl's binders (`impl Memo<type K, type V>`), or by a trait's (`Source<T>::map`). The first line is today's `function_signature_label` (declared types, clauses); the second is the same label rendered UNDER THE CALL'S SUBSTITUTION (the bindings the analyzer solved for this site — `method_call_substitution` / `own_generic_call_bindings` are the record; `pretty_print_type_at` already takes a substitution). Non-generic functions, and a generic one hovered at its DECLARATION (no site, no substitution), show one line as today. When the substitution changes nothing (every parameter still open — inside another generic body) show one line; when it is PARTIAL show the second line with the open parameters left as written.

Design points: both lines in ONE fenced `vilan` block separated by a blank line (the owner's spelling), or two blocks with a `resolved at this call:` caption? — rec the owner's; long signatures wrap per the hover's existing rule; clauses (`borrows`, `bumps`, `context`) render on both lines (E207 must land first or the doubled clause doubles again); `Self` renders as the subject on line two; a `lazy`/spread marker is unchanged; signature HELP (the parameter popup while typing) gets the computed line too — arguably more useful there than in hover; inlay hints are NOT this item. The wasm playground's hover shares the function.

Pins owed (vilan-lsp `mod tests`): the owner's `get_or` shape through an impl's binders; a function-level `<T>`; a trait-default through `Source<T>`; a partial substitution; a non-generic function shows one line; hovering the declaration shows one line; a closure-typed parameter's computed form; the two lines are IDENTICAL-modulo-substitution (no clause lost on line two).
