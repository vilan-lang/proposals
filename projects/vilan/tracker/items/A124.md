

- 2026-09-24 (Order 41, reactive-41): S2a BUILT (bb798cbc) — premise corrected: `on_settle(self, observer: || void)` cannot carry one id per leaf chain; it takes the leaf's `Subscriber` RECORD (id, class, liveness) and is a DEFAULTED `Source` member (a requirement would break every impl); `observe` = mint (`mint_subscriber`, the one place the door-2 mark is spent) + attach; the default is a bridge over `on_change` waking through the draining turn only (reading the ambient turn would give the member a hidden context parameter a native `dyn` slot refuses); the S1 `Cold` trait retired. A diamond IN A TURN fires once (pins red with a fresh id per registration); INLINE with no turn it still fires twice — the paper does not say that. S2b BUILT (29c0f2bb): `Map<S, T, U>`, `Switch<S, T, I, U>`, `Combine<T>` over `(U in T: dyn Source<U>)`, `Distinct<S, T>`, `.cell()` (no compare; owner-tied; a derivation; adds NO `Shared` site), `.distinct()`, `Resource<T>`/`ResourceState<T>` with `.or` (resets)/`.latest`/`.optional`/`.is_pending`; the paper's `Map<S, U>`/`Switch<S, I>` do not compile as written (the transform is a field so `T` must be a parameter — B411); combinators still return `SignalCell`; node constructors exported but `[internal]` (`map_node`, `switch_node`, `combine_node`, `resource_node`, `Resource::pending`); the probe file deleted, its four pins on the real nodes; shared census 140 → 143 (+3: Switch, Distinct, Resource ×2, −the probe — NOT the `.cell()` count); `dispose` → `release_under`, nodes `detach` context-free (8 goldens move, runtime-identical). Guide (b3fa0a47): 'Where a derivation lives'. S2c PREPARED at sweeps/order41/reactive-41/ (`s2c-flip.patch` applies to b3fa0a47 and BUILDS: map/switch/both flattens/and_then return nodes, `FlattenOption`/`AndThen` new, `map` a blanket so dyn-40's refusal pin passes; `s2c-combine.patch` on top builds but throws until B398) — 19 tests RED under it: 5 on B408, 8 A25 pins awaiting a decision, 4 behaviour pins to re-derive, 1 diagnostic, the NO-CYCLE gate (a module-level `.cell()` keeps a strong root → record → node → root loop while subscribed — §2.4's shape). CENSUS under the flip: std ui/router 0, rpc 4 (the mirrors' own `map` must go; `or` returns a node), corpus 0 errors / 4 goldens move, docs 0, examples 9, split 1, Rust-test `.vl` consts 30 (11 fixed by `.cell()` in the patch), website 0, playground 1 (client.vl:22 hands a derivation to a writable `Signal<Route>` bound), `update` on a derivation 0, KOLT 17 sites + 6 cascades (11 return types, 1 local, `Searchable.table`, 3 `combine`). OWNER DECISIONS for Order 42: (1) B408 FIRST in the solver lane (the flip cannot land without it) and B398 for `combine`; (2) A25 under the flip — the mirrors lose their owner-strict `map` and the owner is asked at the SUBSCRIBING LEAF instead (rec: yes — the law moves to the leaf; the 8 pins re-derive); (3) the no-cycle gate: a module-level `.cell()` holds a strong loop while subscribed — rec: the exemplar's `.cell()` moves under an owner, and §2.4 states 'a `.cell()` at module level is a leak by design unless released'; (4) `Resource`'s PUBLIC name — `resource` is a KEYWORD; rec `Pending<T>` for the type and `.or`/`.latest`/`.optional` unchanged, or keep `Resource<T>` as the TYPE name (types are not keywords) and only the constructor needs another spelling. Seven defects filed: B408–B412, B398 confirmed, F37.
