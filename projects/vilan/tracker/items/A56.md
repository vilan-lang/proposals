# A56 — two rpc rulings after A51/A52: `[expose(keyed = K)]` over a `Map<K2, V>` whose argument disagrees with `K2` — today the argument silently wins and the mismatch surfaces as a plain type error at the mirror (refuse at the attribute?); and A48's `authorize_timeout` still answers 429 now that `Reject::Unavailable` is 503 (std refusing on the app's behalf is not the app's judgement — re-rule or leave)

- status: OPEN — lane rpc-29's questions (2026-09-06, Order 29); OWNER Qs
- 2026-09-07 (the owner): FOLDED into the rpc redesign discussion with A53; the keyed-Map ruling and the 429/503 question follow whatever the redesign keeps of `[expose]`.
- 2026-09-08 (lane rpc-paper-30, Order 30): ANSWERED in §9.2 — (i) refuse a `[expose(keyed = K)]` whose argument disagrees with the `Map`'s own key type, at the attribute (one new ledger row; today the written argument silently wins, `std/src/rpc.vl:3093-3098`); (ii) KEEP 429 for `authorize_timeout` — 503 is the app's judgement (`Reject::Unavailable`, A52) and a std timeout is std's limit. Owner ruling R6 in §9.5.
- kind: design
- area: Reactive core & UI

Recommend: refuse the disagreeing key at the attribute (one ledger row); keep 429 for the timeout (it is not the app's decision) and say so on A48's tombstone.
