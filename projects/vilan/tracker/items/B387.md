# B387 — COMPILER STACK OVERFLOW — a trait whose supertrait is the bound of its own blanket impl (`trait DeltaFeed<T> with Source<List<T>>` + `impl type S: Source<List<type T>> with DeltaFeed<T>`) sent `provides_trait → subject_applies → provides_trait` into infinite recursion; with the shape in std EVERY program importing `std::reactive` aborted, and a std-free sibling was refused wrongly ('missing read'). Pre-existing on 49de39157; the wall A112 S3 stopped on

- status: CLOSED 2026-09-22 (Order 40, solver-40 a6bf7c43, filed at the sweep for the record)
- kind: bug (inference)
- area: Type system & the type solver

Four pins abort with the guard planted out; three fail with the conformance rule removed. No refusal added, no ledger row. After the fix A112 S3's held supertrait form checks and builds (`logged=true plain=false ops=1`).
