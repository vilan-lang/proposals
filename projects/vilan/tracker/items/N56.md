# N56 — `vilan check <absolute path>` with the cwd inside ANOTHER vilan checkout resolves that checkout's std: 37 `macro PartialEq's definition did not compile` cascades on kolt's sidebar.vl from the vilan tree, 0 from kolt's own directory

- status: OPEN — lane b239's find (2026-09-04, Order 27)
- kind: hygiene
- area: Hygiene & rot

Project and std resolution should start from the FILE's location (its manifest, its toolchain's std), not the process cwd; at minimum the CLI names which std it resolved when a cascade of this shape appears. Pin: `check <abs path to kolt-shaped fixture>` from a cwd inside a different package gives the same verdict as from the fixture's directory.
