# B382 — there is NO TYPE ALIAS — `type KeyedCursor = DeltaCursor;` cannot be written, `import … as` cannot re-export under another name, and `[deprecated]` is a function attribute — so A112's RULED 'deprecated `KeyedCursor` re-export' was inexpressible and the lane did a HARD RENAME with a `breaking` note instead

- status: OPEN — filed 2026-09-21 (Order 39 sweep; collections-39's FIND 7 / Q4)
- kind: feature (type alias)
- area: Type system & the type solver

Three missing pieces, any one of which would have served: (1) a `type Alias = Existing;` declaration (transparent, no new nominal identity — the everyday one); (2) `export { Existing as Alias }` / `import X::Y as Z;` at module level; (3) `[deprecated("use …")]` admitted on types and re-exports, rendered by the editor (E213's greying is the same mechanism). Rec: (1) first as the smallest general feature, sized M (parser, a `Type::Alias` resolved at name lookup, printer, EBNF, the formatter, hover shows the target); (3) rides on E213. Pins: alias to a generic with parameters; alias in an impl subject; alias across a re-export; hover renders `= Existing`.
