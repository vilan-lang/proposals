# B382 — there is NO TYPE ALIAS — `type KeyedCursor = DeltaCursor;` cannot be written, `import … as` cannot re-export under another name, and `[deprecated]` is a function attribute — so A112's RULED 'deprecated `KeyedCursor` re-export' was inexpressible and the lane did a HARD RENAME with a `breaking` note instead

- status: OPEN — filed 2026-09-21 (Order 39 sweep; collections-39's FIND 7 / Q4)
- kind: feature (type alias)
- area: Type system & the type solver

Three missing pieces, any one of which would have served: (1) a `type Alias = Existing;` declaration (transparent, no new nominal identity — the everyday one); (2) `export { Existing as Alias }` / `import X::Y as Z;` at module level; (3) `[deprecated("use …")]` admitted on types and re-exports, rendered by the editor (E213's greying is the same mechanism). Rec: (1) first as the smallest general feature, sized M (parser, a `Type::Alias` resolved at name lookup, printer, EBNF, the formatter, hover shows the target); (3) rides on E213. Pins: alias to a generic with parameters; alias in an impl subject; alias across a re-export; hover renders `= Existing`.

- 2026-09-22 (Order 40 sweep): PREMISE TWO-THIRDS FALSE (papers-40, `sweeps/order40/papers-40/b382-type-alias.md`, probes/b382_aliases.sh): `export import pkg::inner::DeltaCursor as KeyedCursor;` COMPILES, runs, is transparent (a `KeyedCursor` value satisfies a `DeltaCursor` parameter) and works over a generic struct, a trait, an enum with variant paths, a function and an `impl` on the alias — so A112's ruled deprecated re-export WAS expressible and the hard rename was avoidable. Genuinely missing: `type X = Y;` (no customer) and `[deprecated]` on a TYPE (admitted only on a function). REC: build `[deprecated]` on a type; leave the alias open with no customer. Re-titled by this stamp; the next lane must not build aliasing the language already has.

- 2026-09-24 (Order 41 GO): R-c RULED as recommended — build `[deprecated("…")]` on a TYPE and on a re-export, greyed by E213's mechanism; NO type alias (no customer). editor-41.

- 2026-09-24 (Order 41, editor-41): BUILT (b79db3bc, R-c): `[deprecated("…")]` on structs, enums, traits, module bindings, and a re-export written `export [deprecated("…")] import … as …;` (the steer lives on the `export` node — on `Import` it pushed `Node` to 104 bytes past node_size's 96); an unexported deprecated import refused; `[deprecated]`/`[internal]` on an `impl` refused; a type warns at each use in ANOTHER module (the declaring module silent — OWNER to confirm); a re-export warns at each other module's import through it; the editor's `deprecated` modifier (functions too) + a hover lead. No alias. FIND: the module-name guard is not independently provable in the tested shapes.
