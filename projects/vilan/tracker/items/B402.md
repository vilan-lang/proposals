# B402 — MISCOMPILE (fixed for the record) — a literal-only expression with a FRACTIONAL literal divided as integers on JS: `1 / 4.0` printed `0` and `3 / 2.0` printed `1` (native rustc refused them); found by solver-41 while pinning B389 — the expression now defaults to `f64` as a whole

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: solver-41's report)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: solver-41 (2026-09-24), FIXED at e0dd9c51 in the same lane — filed so the miscompile has an id; closes at the sweep
