# F46 — native — `source.attach(watching(source))`: the RECEIVER is borrowed while the ARGUMENT moves it (rustc E0505); reactive-42 wrote the node subscription as two statements to dodge it; the emitter should evaluate such an argument first (native-42's rule for a `&mut` argument whose place a later argument reads, applied to a by-value argument that moves the receiver's place)

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's find N2)
- kind: bug (native)
- area: Native targets & rendering

Pin: the one-statement form builds and matches node. Sizing S.
