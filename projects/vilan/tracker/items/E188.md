# E188 — E177's exposure fix ("Export `S`") DECLINES when a file names two different types with one spelling: it resolves the exposed name through this file's `type_references` and refuses on disagreement; an analyzer-recorded (warning site → exposed entity) pair would make it exact — the same `Program` surface E184 reads

- status: OPEN — filed 2026-09-15
- kind: editor
- area: LSP & tooling

Record the exposed entity on the exposure warning; the fix reads it; pin the two-`S` file.

- 2026-09-17 (Order 37 GO): BUILD in lane editor-37.
