# N45 — the ignored-pins backstop runs ubuntu-only, so a `cfg(windows)` `#[ignore]` can never expire through it

- status: OPEN — audit7-windows lane's find (2026-09-01)
- kind: process
- area: Hygiene & rot
- source: lane audit7-windows; the B198 pin is the first such

`ignored-pins.yml` (N27's weekly self-test + the CI format gate) runs
on `ubuntu-latest` only; a `#[cfg(windows)]` pin compiles away there,
so its `#[ignore]` reason is invisible to the automated route — when
B198 is ruled and fixed, only a human notices the pin should be
un-ignored. Fix: the pins script enumerates `#[ignore]` attributes
TEXTUALLY (it already reads source, not the compiled test list —
check), so the ubuntu run sees the cfg(windows) pin's reason; if it
does not, give the workflow a windows leg for the enumeration. Also
from the lane: `modules.rs:3406`'s helper is dead code for the
Windows target (used only by a `cfg(unix)` test) — the one thing
blocking a Windows clippy leg; and pin 2's junction-reads-as-symlink
assumption is unverified locally — a red there on CI's Windows leg is
a TRUE finding (the CLI following junctions inside declared trees),
not a broken fixture.

2026-09-01: the first Windows CI run with the pins (Order 24's sealed
tip 33692bb2) — SUCCESS; the seven live `cfg(windows)` pins passed
(pin 2's junction-as-symlink assumption held), the B198 pin ignored
as intended. The backstop's blindness to that ignore is still this
item.
