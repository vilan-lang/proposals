# A78 — a handle-returning service on the CONNECTIONLESS leg (`{mount}rpc` POST, `local_rpc`) fails at RUNTIME with `RpcError::Remote("..arrived on none..")`; a `[service]` with a handle method is WebSocket-only and could be refused at compile time or at mount

- status: OPEN — Order 31's finds (2026-09-09)
- kind: design
- area: Reactive core & UI

Refuse at `Service::new(..).at(..)` for a service whose surface carries a `RemoteSource` return when the mount has no socket, or document the runtime failure in the guide's status table.
