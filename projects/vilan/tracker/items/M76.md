# M76 — the CHECKS phase, not the resolve, is the largest remaining per-keystroke cost on M70's six kolt files: theme.vl at loadavg 40 reads `load+walk 460.7 / base 0.0 / build 632.4 / checks 1,103.2` ms — M19's per-module checks record is deliberately WITHHELD from an entry-shaped world (`analyze_inner`'s store site says why: module checks through the alias may depend on the entry), and `[vilan phase] reused 0/69` on every one of the six confirms nothing is reused; worth MORE than M73 (checks are 1.7× the resolve)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: perf
- area: Performance & footprint

Decide which checks an entry-shaped world CAN reuse (those not reaching the alias — the same per-file deferral set M73's design needs) and store that record; measure the six with the paired harness.
