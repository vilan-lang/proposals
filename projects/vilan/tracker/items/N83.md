# N83 — `vilan build` over a corpus program IN PLACE emits four asset artifacts into `vilan/test/` (`.vilan-bundled`, `icons/close.svg`, `logo.<hash>.svg`, `robots.txt`) that `git add -A` sweeps into a commit; none is gitignored

- status: OPEN — Order 35's finds (2026-09-13)
- kind: hygiene
- area: Hygiene & rot
- 2026-09-14 (Order 36 GO): BUILD in lane hygiene-36 (first; N81's fixed tools handed back through the lane's scratch for the integrator to land).

Gitignore the emitted asset paths under `vilan/test/` (or have the corpus harness build into a temp dir); a hygiene pin that the tree stays clean after `--test corpus`.
