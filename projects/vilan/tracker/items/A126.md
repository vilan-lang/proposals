# A126 — the SIZED numeric family has NO `Display`: `display.vl` covers `str`/`i32`/`f64`/`bool`/`u32`/`BigInt` only, so `fun show<T: Display>(v: T)` refuses a `u53` while `print(i"{n}")` works — `i53`, `u53`, `u8`, `i8`, `i16`, `u16`, `i64`, `u64` want the impls, and `usize` (I5) must not inherit the gap

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: feature (std)
- area: Standard library

One impl per type in display.vl, both twins where they exist; a pin per type through a `T: Display` bound. Sizing S.

- 2026-09-24 (Order 41, index-41): BUILT (c936f3d7): premise corrected — `i64`/`u64` do not exist (renamed to `i53`/`u53`); the missed width is `f32`. `Display` for `i8`, `u8`, `i16`, `u16`, `i53`, `u53`, `f32` (each the interpolation itself; display.vl has no twin); `usize` in S1; std/strings.md documents the coverage; seven pins through `T: Display` + `format` + `join`, all red before; identical on both backends.
