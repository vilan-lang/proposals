# B421 — MISCOMPILE — the `dyn` coercion accepts a VOID closure body as its value: `show(root.mapped(|n| { n * 2; }))` with a `dyn Src<i32>` parameter checks clean and prints `NaN` (the block's trailing `;` makes it void; the coercion at the `dyn` position does not check the closure's result type against `U`) — on d65d4e75

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's interim find M1)
- kind: bug (miscompile)
- area: Type system & the type solver

Fix: the coercion typechecks the node's `U` against the closure's body type (a void body is a mismatch with the usual steer). Pin red-first. Sizing S; solver.

- 2026-09-25 (Order 42, solver-42): FIXED (76d3affe): dyn erasure checks the object's trait ARGUMENTS as well as the trait, so the `NaN` program is refused; 2 pins.
