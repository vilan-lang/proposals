# B174 — an unbounded generic LEFT operand of `+` still concatenates

- status: OPEN — operators lane's residual (Order 23); pinned #[ignore]d
- kind: bug
- area: Type system & the type solver
- source: lane operators, 2026-08-31; pin `platform::an_unbounded_generic_left_operand_of_addition_is_rejected`

B169 closed the RIGHT operand; the left survives: a trait default
written over the trait's own unbounded parameter (`self.once() +
self.once()`, macros.rs:3513's inherited-default pin) is legal today,
so refusing the shape is a bound requirement on every such default —
the breaking generics change b148's SCOPE note deferred. `bump(Point
{…})` still concatenates the tuple meanwhile. Closing this needs the
deliberate breaking step (census the estate's trait defaults first).

## CENSUS 2026-09-01 (proposal/generic-left-operand-census.md) — the number is ONE

Breaking surface: 1 site — `tests/inference/macros.rs:3522`, `trait
Doubler<T>`'s `twice`. Zero in std, corpus, docs fences, examples,
benchmarks, templates; kolt and the website declare no traits at all.
The `#[ignore]`d refusal pin turns green. Total migration: one `<T>`
→ `<T: Add>` edit. Strongest lever: spec types.md:653 ALREADY
prescribes the bound — the change makes the spec true. The census's
recommendation: TAKE THE BREAKING STEP, requiring a bound that
PROVIDES the operator's method (not any bound). Awaits the owner's
word; a smalls lane can close it in an hour once ruled.

## Widened 2026-09-01 (audit run 7)

All 16 operators escape on an unbounded generic LEFT operand, not
just `+`, and the LOGICAL half is worse: `either<T>(p: P, true):
bool` returns `1` — a struct out of a `bool` return; `&&`/`||` with
a generic left draw no refusal (B181 closed the right half). Two
squares produce plausibly-wrong booleans (`same<T>(M{7}, M{7})` →
false; `less<T>(M{10}, M{9})` → true via lexicographic "10" < "9").
The census still says ONE trait-default site; the breaking step is
still one edit. Awaiting the owner's word.
