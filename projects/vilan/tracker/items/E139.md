# E139 — hover at the end of a bare USE (`let _ = count|`) still hovers the enclosing function: `vilan_ide::analysis::entity_at` is separately end-exclusive (shared with completion's receiver resolution)

- status: OPEN — lane editor-sync-27's find (2026-09-04, Order 27)
- kind: editor
- area: Language server & tooling

E133 made the reference index end-inclusive (rename, references, hover on declarations). `entity_at` in vilan-ide has its own end-exclusive containment and serves hover on uses and completion's receiver arm. Extend the same end-inclusive fallback there; pin hover at `count|` on a use, and completion's receiver at `x|.` stays right.
