# B332 — after a STRUCT segment the import walk keeps the MODULE scope (a module-level `rem` in style.vl would shadow `Length::rem`, because the type's namespace is asked SECOND — B317 preserved every resolving path), while an ENUM segment REPLACES the scope with the variants; the two are inconsistent

- status: OPEN — Order 34's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver

Decide: a type segment replaces the walk's namespace (breaking for a path that resolved to a module member through a type-named prefix — census first) or the enum walk keeps the module scope too; pin the shadowing pair either way.
