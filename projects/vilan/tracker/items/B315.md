# B315 — the duplicate-inherent-member rule now reads a bound's ARGUMENTS only for two BARE binders (A86's `subjects_collide`), so a blanket pair whose bounds OVERLAP but are not identical (`impl type S: Source<type I>` and `impl type S: Source<Option<type I>>` on a hypothetical `Option: Source`) would both be admitted, and tier 1 of method resolution takes the first inherent candidate UNRANKED — 'no silent pick' is weaker than it was; not reachable today (no `Option: Source` in std)

- status: OPEN — Order 33's finds (2026-09-11)
- kind: bug
- area: Type system & the type solver

Either rank inherent candidates by specificity at tier 1 (the trait-impl tiers already do) or refuse two blankets whose bounds overlap (`instantiation_agrees` in both directions = overlap); pin the hypothetical pair with a local trait. Rec: refuse overlap — the cheaper rule and the one the doc comment already wants.
