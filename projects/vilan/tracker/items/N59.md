# N59 — corpus programs inherit the test process's cwd (`crates/vilan-core`), which is how `file-corpus-<n>/` trees strand there on a crash; a per-run `current_dir` in `run_node_within` would end the pollution at the source but changes relative-path resolution for all three gates on the shared harness

- status: OPEN — lane hygiene-29's find (2026-09-06, Order 29)
- kind: hygiene
- area: Hygiene & rot

Give `run_node_within` a per-run scratch cwd and pass the program's own directory explicitly where a gate resolves a relative path; run the three gates; drop the ignore line.
