# A83 — a 2-D vector helper in std: kolt's `interact.vl:127` hand-writes `magnitude((x, y): (f64, f64))` for pointer-drag distances and asks for "a vector library for std" — the smallest useful surface is a `Vec2 { x, y }` value with `add`/`sub`/`scale`/`length`/`distance`/`dot` (and `Length`-free: pixels are `f64` here), or the same over `(f64, f64)` tuples

- status: OPEN — kolt marker sweep (2026-09-10)
- kind: feature
- area: Standard library (`std::math`)

Rule the shape (recommend the `Vec2` value type with operator impls — `Add`/`Sub` exist, and a named type reads at a call site where a tuple does not), land it in `std::math`, migrate kolt's `magnitude` and its drag arithmetic; the drag/threshold idiom in kolt's `interact.vl` is the exhibit.
