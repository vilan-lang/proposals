# B413 — DISSOLVE the `resource` KEYWORD into a struct attribute (`[resource] struct Handle { … }`), and settle the word: 'resource' vs 'droppable' vs 'temp' — the keyword bit A124's `Resource<T>` (the pending family's obvious public name), and a kind that changes destruction rules already has precedent as an attribute (`[platform(..)]` changes analysis)

- status: OPEN — filed 2026-09-25 (the owner's question at the Order 41 rulings)
- kind: design (breaking)
- area: Type system & the type solver

Rec: dissolve the keyword into `[resource]` on the struct and KEEP the word 'resource' — it names the meaning (a thing the environment lent you, which must go back) where 'droppable' names Rust's mechanism and 'temp' names a lifetime; the attribute frees the identifier for `Resource<T>` and shrinks the grammar. Cost: every `resource struct` in std and the estate respells (mechanical, a codemod); the formatter, EBNF (`grammar_ebnf` + `grammar_sync`), the LSP's painting and completion tables, and destruction.md follow. BREAKING — rides the v0.41.0 train if the codemod is ready, else v0.42.0.

- 2026-09-25 (Order 42 GO): R-c RULED — rides THIS train IF syntax-42's codemod + estate sweep are green by index-42's merge; otherwise HELD for v0.42.0 with the deprecation ANNOUNCED in v0.41.0's notes (the cut refuses a removal whose deprecation never shipped). syntax-42 builds it either way.

- 2026-09-25 (Order 42, syntax-42): PREMISE CORRECTED — `resource enum` is a SHIPPED form (parse_enum takes `resource? enum`; the analyzer reads the flag at ~5 sites; corpus resource_take.vl declares two; B117's pins declare a payload-less `resource enum Handle { Open = 1, Closed = 2 }` that containment cannot express; no doc mentions it). The brief said struct only — the integrator's under-count. RULED by the integrator within the ruling's meaning: `[resource]` on `struct` AND `enum`, a pure respelling, every shipped meaning kept, the codemod moves both; refused everywhere else.

- 2026-09-25 (Order 42, syntax-42): BUILT (f39ff833, its own commit, BREAKING; R-c decides merge-in-train vs hold): `resource` REMOVED from `KEYWORDS` and `Token::Resource` gone (lexing.rs, token.rs); `[resource]` closes the label prefix ahead of `external`, on a struct AND an enum (the premise correction); the old `resource struct|external struct|enum` refused with the steer (one NEW ledger row, still parses); `[resource]` on fun/impl/trait/local/field/variant gets row 206's message re-keyed; the analyzer unchanged except row 184's steer text; formatter prints `[resource] `; both highlighting grammars regenerated; keyword completion drops `resource`, a NEW `ATTRIBUTE_DOCS` table in vilan-ide carries the hover (`book_sync` checks its links); spec §2.2/§A.2/the tour drop `resource` from the reserved lists and the contextual lists gain `as`, `only`, `internal`, `resource` (D14 done on the way); EBNF `resource-attr`; §6.8, the Resources tour, the errors appendix. CODEMOD `sweeps/order42/syntax-42/b413_codemod.py` (`--check` = the residue gate; declaration positions only) — ESTATE 405 sites: std 5 (task.vl, process/db.vl, process/fs.vl ×3), corpus 8, docs fences 16 (5 pages), Rust `.vl` programs + comments 376 (resources.rs 262, borrows.rs 34, backed_enums.rs 31), examples/benchmarks/templates 0, kolt 0, playground 0, website programs 0 (but its editor keyword list → K24). No golden moved. `destruction-md-b413.patch` (papers) and `b413-held-announcement.md` (the v0.41.0 `deprecates:` entry ONLY if R-c holds it) saved. bindgen `RESERVED` left alone (E225).
