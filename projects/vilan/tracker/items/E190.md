# E190 — the never-silent body-less-emission refusal (B55, `internal: a call resolved to \`Slot\`'s requirement \`place\`, which has no body …`) carries NO usable span: its span is the trait requirement's name in std, which the CLI never renders, so kolt showed one bare `Error:` line with no file, no line and no hint which of 19 files to look in; the emitter at that point does not know the call site either (`function_with_name` records the bodyless id when the requirement is EMITTED, long after the call that asked for it). The integrator had to instrument `ensure_function_emitted` to learn the emitting stack (`app_shell` → `place`) and bisect kolt by hand

- status: OPEN — filed 2026-09-15
- kind: diagnostics
- area: LSP & tooling

Record, with the bodyless id, the function being emitted when the requirement was first requested (`ensure_function_emitted`'s caller: `self.emitting`'s innermost + `current_admitting_file`) and the call expression id if the resolver has it; the message names the function and its file, and the span is the CALL's when known, the enclosing function's name otherwise; a pin over B351's reproduction asserts the file and line.
