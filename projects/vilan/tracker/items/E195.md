# E195 — `Analysis::nominal_id_by_name` (vilan-ide completion.rs) still scans the program for the FIRST name match — the shape E193 fixed for the struct-initializer head (which now resolves through the scope chain and, qualified, through the namespace); its callers (`element_view_nominal_id`'s neighbours, receiver typing) answer the wrong struct in the same two-module fixture (a sibling's twin `Dot` beside this file's own)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: editor
- area: LSP & tooling

Route it through the same scope-chain/namespace resolution `path_struct_id` uses; pin with the two-module fixture at each caller.
