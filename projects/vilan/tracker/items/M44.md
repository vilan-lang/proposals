# M44 — M35's measured shortfall: a multi-entry `check` starts its second member cold and recomputes the base world (1.13× one entry at +63% CPU if all start cold; the shipped design keeps CPU flat at −16% wall); a base-cache "under construction" claim so a second cold member WAITS for the first's world (M23's territory); `build` and `check_single`'s per-platform loop are still sequential

- status: OPEN — lane compile-perf-28's find (2026-09-05, Order 28)
- kind: performance
- area: Performance & footprint

Numbers on the item; the claim is the fix that gets both wall and CPU. `build` writes files and chunk sinks and needs its own audit before going parallel.
