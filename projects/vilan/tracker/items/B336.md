# B336 — `export(in <general PATH>)` (`export(in pkg::a)`) parses, stores, formats and reprints, but the visibility test ADMITS it — the subtree test needs source PATHS, which live on `Program`, not the analyzer; only `mod` and `pkg` are enforced; zero estate occurrences

- status: OPEN — Order 35's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver
- 2026-09-14 (Order 36 GO): BUILD in lane visibility-36 beside B318 S4.
- 2026-09-14 (Order 36, visibility-36): LANDED — TWO bugs: `export(in pkg::a)` was READ as `export(in pkg)` (reserved heads matched on `scope.first()`), and the general path was unenforced; `check_scoped_exports` post-build over banked `ScopedReachCheck` rows, subtree root from the DECLARING file; pin `b336_a_general_export_scope_narrows_to_its_own_subtree`. CLOSE at the sweep.

Enforce the general path at the post-build family (where `Program`'s paths are in hand); pin `export(in pkg::a)` reached from `pkg::b` warning and from `pkg::a::c` silent.
