# A66 — a boolean-attribute binding on `View`: `toggle_attr(name, source: Source<bool>)` (presence, not value — `inert`, `disabled`, `hidden`, `open`) beside `bind_attr(name, Source<str>)`; kolt hand-writes it over its own `Element::remove_attribute` extern (views.vl:371–384) to make the app shell `inert` while a modal is up

- status: OPEN — the owner's ask (2026-09-07)
- kind: feature
- area: Reactive core & UI

`Element::remove_attribute` (A59) plus `View::toggle_attr<S: Source<bool>>(self, name, source)` in browser/ui.vl (an effect under the row's owner: set the attribute to the empty string when true, remove it when false) and the process/ui.vl twin (render the attribute when the initial value is true). Pin `inert` toggling on and off, the SSR twin's markup, and that the effect dies with the owner.
