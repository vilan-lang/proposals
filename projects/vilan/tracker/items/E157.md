# E157 — a block-like head followed by `::` takes B248's parenthesize steer, which is not a fix there (`(match x {..})::foo` is not valid either); `::` is a `Token::Op` and needs its own arm

- status: OPEN — Order 30's finds (2026-09-09)
- kind: editor
- area: Language server & tooling

A `::`-specific refusal naming the real problem (a path cannot start at a block); pin.
