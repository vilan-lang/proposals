# B428 — `let x: i8 = 128` is ACCEPTED: the positive signed bound admits 2^(n-1) (off by one — `i8` tops at 127); the same check B407 extended for unsigned types reads the signed maximum wrong

- status: OPEN — filed 2026-09-25 (Order 42, solver-42's find)
- kind: bug (inference)
- area: Type system & the type solver

Fix: the signed maximum is 2^(n-1) − 1; pins per width incl. `i8 = 127` accepted and `i8 = 128` refused. Sizing XS; solver.
