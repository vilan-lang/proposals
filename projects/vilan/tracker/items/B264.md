# B264 — TYPE-position import aliases are not fully split after E145: `import Foo as Bar; let x: Bar` records `type_references` against the target (the alias span catches it by spelling, so references narrow correctly) while go-to-definition and hover at such a use resolve THROUGH the target — a ruling on what go-to-definition at an alias should answer

- status: OPEN — lane editor-29's find (2026-09-06, Order 29); OWNER Q
- RULED 2026-09-07 (the owner): TypeScript's behaviour is the reference — go-to-definition at an aliased use RESOLVES THROUGH to the target's declaration; hover shows the ALIAS name with the target's signature (`(alias) …`); rename at a use renames the alias, rename at the original's name renames the original. The lane double-checks each against tsserver before pinning (the owner asked for the check).
- kind: design
- area: Language server & tooling

Decide: go-to-definition at an alias use lands on the alias declaration (one hop, then the target) or on the target; and whether definition-side references list alias-reached uses. Then record type-position aliases as alias entities like value-position ones and pin hover, go-to-definition and references at a type-position alias.
