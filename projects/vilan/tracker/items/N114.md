# N114 — vilan-lsp's `hover_at_cursor` test helper MANGLES its fixture — it strips every `|` to find the cursor, so a fixture containing a closure (`|x| …`) or a union-shaped comment is analyzed as a DIFFERENT program; E9's pin was VACUOUS for this reason (it asserted a clause only the deleted E207 append could produce). `hover_at_marker` was added beside it; retire `hover_at_cursor` or make it refuse a fixture with a second `|`

- status: OPEN — filed 2026-09-21 (Order 39 sweep; editor-39's find 2)
- kind: hygiene (test trap)
- area: Hygiene & rot

Sweep every `hover_at_cursor` caller: fixtures with one `|` migrate mechanically to `hover_at_marker`; the helper then asserts exactly one `|` or panics with the fixture's name. Pin: the helper's own guard. Sized S.
