# N47 — the docs gate compiles fences but asserts no output: a `vilan,run` tag with a transcript block

- status: OPEN — docs-law lane's proposal (2026-09-01)
- kind: process
- area: Hygiene & rot
- source: lane docs-law: two resources.md fences promised the REVERSE of what they printed, and nothing could catch it

`crates/vilan-core/tests/docs.rs` extracts each fence and runs
analyze + transform only; the tags (`vilan`, `vilan,browser`,
`vilan,norun`, `vilan,fragment`) assert nothing about output. Build:
a `vilan,run` tag paired with an adjacent ```text block holding the
claimed transcript, executed through `corpus.rs`'s build-and-run
path, so a timing or ordering claim in prose fails the docs gate when
the law moves (c8609287 moved it; 32 sentences and 2 outputs rotted
unseen). Start with the resources chapter's six runnable fences,
already hand-witnessed by docs-law; the corpus `// witness:` gate is
the sibling for goldens.
