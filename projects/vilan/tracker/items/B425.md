# B425 — the CONTEXT PASS under-threads an argument: `blanket-impl.vl`'s `badge("static")` passes FEWER context arguments than its callee declares (rustc E0061 natively — 'broken' — now refused by name by native-42); the analyzer's context-threading plan (B146's class) misses a call through a blanket

- status: OPEN — filed 2026-09-25 (Order 42, native-42's find)
- kind: bug (context pass)
- area: Type system & the type solver

Fix in the analyzer's context plan, not the emitter; pin: `blanket-impl.vl` flips to identical. Sizing S–M; solver, Order 43.
