# N127 — the playground's exhibits ROT unchecked: `vilan-playground/todo` on 1265ea5d3 had FOUR dead lines (a removed `import std::print`, `.bind_each` retired by A99, a bare-trait field `Signal<List<Note>>` refused by dyn-40 — whose `dyn` spelling cannot serve it because `update` is INHERENT on `SignalCell`, not a trait member — and the missing `each_by` import), fixed by the integrator at Order 41 GO; the estate sweep counts the playground's six programs for BREAKING lanes but nothing CHECKS them — wanted: the playground's programs in the estate check (a `vilan check` per exhibit in the sweep script, or a corpus copy of each)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: papers-41's report + the GO one-liners)
- kind: hygiene (estate)
- area: Hygiene & rot

Discussion: Order 41 GO (2026-09-24); the playground is not a git repository — the owner's

- 2026-09-25 (Order 42, std-42): the script BUILT (`scripts/integration/estate_check_playground.sh` — `vilan check` per package and loose .vl in vilan-playground; honours VILAN/VILAN_STD; non-zero on red) and placed in proposals by the integrator; its first run found the playground RED — `db.vl` and `fib.vl` still `import std::print` (removed) — fixed by the integrator 2026-09-25 (the playground is not a git repo). Wire it into the sweep at the seal.
