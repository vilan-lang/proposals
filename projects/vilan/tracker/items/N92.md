# N92 — `vilan check` writes `dist/.cache` into the checked package's directory: a read-only-sounding command mutates the tree (and `split.rs:63` `read_to_string`s every entry of its fixture directory, so a `dist/` left there fails 10 tests with `IsADirectory` instead of a readable message)

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

`check` keeps its cache under the target/user cache dir (or `--no-cache` default); the split loader skips directories; pins for both.
