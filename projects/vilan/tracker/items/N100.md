# N100 — the ledger's check (2) searches only the LONGEST fragment of a key: 283 of 558 rows carry more than one searchable fragment, so a reword of a non-longest run does not red its row. Measured: widening the search to EVERY fragment ≥ `MIN_FRAGMENT` would red exactly two rows today (494, 499), both for one cosmetic cause — their keys spell `{{`/`}}` where `normalized()` collapses them

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: hygiene
- area: Hygiene & rot

Fix the two keys, then widen check (2) to every fragment; a pin that a reword of a short fragment reds.
