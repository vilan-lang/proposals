# E153 — css-block editor smalls: `:hover {` inside a block is refused with the bare `found ':' expected a declaration …` where a STEER to `.hover { … }` is owed (the dotted rule is deliberate — one name-blind rule covers pseudo-classes, breakpoints, `within`, `divide`); and PROPERTY-NAME completion inside a block from the closed CSS property list (values stay token runs — css-block.md §10's non-goal is value completion, not names)

- status: OPEN — the owner's ask (2026-09-07); css-30
- kind: editor
- area: Language server & tooling

Ledger row EDIT for the `:` case (a steer naming `.hover`), a completion source of CSS property names (a generated list, versioned with std) offered at declaration position inside a block; pins for both.
