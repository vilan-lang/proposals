# F35 — native — `ListCell` and `map_each` do NOT BUILD on the Rust backend: 'generic type instantiated at `any`' (pre-existing before Order 41's collections work; the delta path is JS-only today) — the A112 family joins F34's list of reactive shapes the native emitter must reach before A124 S2c makes them common

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: collections-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: collections-41's find 4 (2026-09-24); native lane, with F34

- 2026-09-25 (Order 42, native-42): BUILT (c562e514) — premise corrected: the `any` wall had already lifted; behind it three lowering fixes (a named function as a value; a `&mut` argument whose place a later by-value argument reads evaluates first — E0502; a `&mut` over a loaned binding reborrows — E0596) and a MISCOMPILE fixed: Rust evaluates an assignment's value before its place, so `list[next()] = next()*10` paired the draws wrong — subscripts hoisted into `let`s in source order; `list-cell.vl` identical, in the default suite.
