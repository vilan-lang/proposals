# B377 — MISCOMPILE — a mapped-tuple comprehension whose element is a BLOCK expression is DROPPED from the emitted JS, statements and side effects included, clean at check: `(value in values => { note(1); value })` emits NOTHING (`block=0`) where the call form `(value in values => f(value))` emits `values.map(..)`

- status: OPEN — filed 2026-09-21 (Order 39 sweep; reactive-39's FIND-1, reproduced by the integrator on 0976fd194)
- kind: bug (MISCOMPILE, HIGH)
- area: Emitter / mapped tuples

Repro is the probe: two comprehensions over one mapped tuple, one with a call element, one with a block element that prints; the block one's output is absent from the `.mjs` and nothing runs. The check passes. Fix direction: the comprehension lowering has an element-shape match that lacks a `Block` arm (or the desugar discards a block's statements and keeps only its tail, then drops an 'empty' tail). Pins: a corpus program with a block element whose side effect is witnessed; the emitter's arm coverage for every element shape (call, block, literal, closure, `match`, `if`). Priority: FIRST in Order 40's solver lane — a silent drop with a clean check is the worst class we have.
