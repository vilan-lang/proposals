# B247 — an i-string hole containing an OPERATOR is silently not a hole: `print("{1 + 2}")` emits the literal `{1 + 2}`

- status: OPEN — lane rigid-28's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

Either the hole grammar admits an expression (recommend) or a hole that fails to parse as one is refused, never printed verbatim. Pins: `{1 + 2}`, `{a.b()}`, `{x}` control, and the refusal or the value under node.
