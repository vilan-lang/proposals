# F23 — `context.rs` mints a needs-context function's hidden parameter RECORD-LESS, so the Rust emitter INFERS its `Option`-vs-bare flavour from call sites (a worklist, extended through clause-typed parameters for `nursery.vl`) and defaults to the strict reading

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: hygiene
- area: Native targets & rendering

Record the flavour where the parameter is minted (one bool beside `context_hidden_parameters`, ~5 lines); `context_parameter_type`/`context_clause_type` become lookups and `compute_context_flavours` is deleted.
