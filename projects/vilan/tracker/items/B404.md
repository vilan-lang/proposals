# B404 — an UNSUFFIXED literal typed `BigInt` is emitted as a JS NUMBER and crashes at runtime: `fun tb(v: BigInt): BigInt { v + 1n }` then `tb(3)` throws 'Cannot mix BigInt and other types' — the literal law (B370/B389) types `3` as `BigInt` from the parameter but the emitter writes `3`, not `3n`; pre-existing before solver-41 (B389's stall step EXCLUDES BigInt for this reason)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: solver-41's report)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: solver-41's find 1 (2026-09-24)
