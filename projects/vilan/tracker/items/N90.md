# N90 — HIGH: `vilan fmt --check` cannot distinguish "clean" from "BAILED": a file the printer cannot render is reported as already-formatted, so a printer gap is invisible to N55's gate — `export let x = 1;` bailed, `fmt --check vilan/std` was green over it, and only `formatter::idempotency::reactive_vl` caught it; `[deprecated(..)]` bailed the same way until style-36 caught it through `formatter_never_silently_bails`

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

`--check` reports a bail as a distinct non-zero outcome naming the file; the CI fmt job reads it; pin with a construct the printer declines.
