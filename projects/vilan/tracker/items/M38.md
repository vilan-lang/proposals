# M38 — `collect_resource_bindings` is ~100 ms on kolt's client: `type_is_resource` over every variable and parameter type with field recursion

- status: OPEN — lane m28's find (2026-09-04, Order 27)
- kind: performance
- area: Performance & footprint

A per-nominal "can any instantiation of this nominal be a resource" pre-filter (M28's RN plus generic holders) bails non-candidates before the field recursion, provably answer-identical. Also fold the duplicate `any_declared_resource`/`declares_a_resource` (three identical lines twice). Gate: goldens byte-identical; the resource pins.
