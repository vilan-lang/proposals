# E152 — a pure-namespace module entity (a directory with no body, A65) is attributed to `SourceId(0)` at `EMPTY_SPAN` because it names no file, so go-to-definition on such a segment lands at the ENTRY's top; hover and references on the segment have the same nowhere to point

- status: OPEN — lane a65's find (2026-09-08)
- kind: editor
- area: Language server & tooling

Give the namespace a location: the directory itself (a `file://` URI to the directory, or its first child's file at line 1), and answer go-to-definition, hover (the children it holds) and references from it; pin all three.
