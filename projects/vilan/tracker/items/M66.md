# M66 — every `SignalCell` pays `fresh_id()` for `.id` (A92's server dedup key), so twelve corpus goldens moved and every program mints an id per cell whether or not it is ever exported — a LAZILY-STAMPED identity intrinsic (`Shared::identity`: `o.__id ??= next++`) gives reference identity at zero cost until asked and zero golden churn

- status: OPEN — filed at Order 34's GO (2026-09-13); not queued
- kind: perf
- area: Performance & footprint
- 2026-09-14 (Order 36 GO): R8 RULED as recommended: BUILD (the lazily-stamped identity intrinsic; twelve goldens return). Lane perf-36.
- 2026-09-14 (Order 36, perf-36, R8): LANDED 091762e5 — `Shared::identity(self): i32` (`external fun`, `Intrinsic::SharedIdentity`, `__shared_identity` helper `cell.__id ??= next++` from 1 — never the rpc sentinel −1; interpreter arm with a per-run counter); `SignalCell` loses the eager `id`; `cell_identity(cell)` in std::reactive reads it; rpc.vl's four dedup sites use it. Ir per `SignalCell::new` 3,556.6 → 3,043.2 (−513.4, −14.4 %; callgrind, noise 0.016 %). kolt's client emits NO `__shared_identity` at all. ELEVEN goldens returned (A92 moved eleven, not twelve): nine byte-identical to pre-A92, two differ by unrelated drift. Pins in std_surface.rs. One unrowed interpreter `Failure::unsupported` ('`Shared::identity` over {type}') for N85's class. CLOSE at the sweep.

Replace the eager `id` field with an intrinsic that stamps on first read; the server's dedup and `LiveForward.holds` read the intrinsic; the `.mjs` goldens return to their pre-A92 shape except where an export actually asks. Measure: Ir per `SignalCell::new` before/after, on kolt's client warm.
