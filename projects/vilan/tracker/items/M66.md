# M66 — every `SignalCell` pays `fresh_id()` for `.id` (A92's server dedup key), so twelve corpus goldens moved and every program mints an id per cell whether or not it is ever exported — a LAZILY-STAMPED identity intrinsic (`Shared::identity`: `o.__id ??= next++`) gives reference identity at zero cost until asked and zero golden churn

- status: OPEN — filed at Order 34's GO (2026-09-13); not queued
- kind: perf
- area: Performance & footprint

Replace the eager `id` field with an intrinsic that stamps on first read; the server's dedup and `LiveForward.holds` read the intrinsic; the `.mjs` goldens return to their pre-A92 shape except where an export actually asks. Measure: Ir per `SignalCell::new` before/after, on kolt's client warm.
