# N81 — the two item-merge tools (`fold_tests_by_name.py`, `merge_items_by_name.py`) mis-split a MULTI-LINE STRING CONST whose first line ends in `"\` (a line continuation): the header lines were dropped inside a `use { }` block and the 32-line body vanished (visibility-a-35's merge, lexer errors at the gate's build); they also resurrect a test HEAD RENAMED under its old name and drop an EDITED existing const's changes (the fold carries new consts only)

- status: OPEN — Order 35's finds (2026-09-13)
- kind: hygiene
- area: Hygiene & rot

Teach the splitter string continuations (a `"\` line ends inside the string until the closing `";`), treat a lane fn absent from HEAD but present in BASE as DELETED-on-HEAD (never resurrect), and fold an edited const when `--allow-edit` names it; pins over the three shapes.
