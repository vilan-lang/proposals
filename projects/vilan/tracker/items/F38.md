# F38 — native — `result-combinators.vl`: `Result::and_then`'s `U` stays UNBOUND at the native call — the analyzer records no value for it (F34's `map` half was the same class on `Source::map`, fixed by reading the recorded `U`; here nothing is recorded); the program stays refused by name

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: native-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: native-41's find 2 (2026-09-24)

- 2026-09-25 (Order 42, native-42): BUILT (61d94fd9): the emitter binds a call's own unbound generics from its ARGUMENTS (the declared closure type against the literal's parameters and body tail; a variant-constructor tail through its arguments; an arithmetic operand through its left side; a concatenation as str); `result-combinators.vl` now stops at `or_else<F>` where nothing constrains `F` (the analyzer records `any`) → B424 (ruling); refused by name.
