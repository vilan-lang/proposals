# F38 — native — `result-combinators.vl`: `Result::and_then`'s `U` stays UNBOUND at the native call — the analyzer records no value for it (F34's `map` half was the same class on `Source::map`, fixed by reading the recorded `U`; here nothing is recorded); the program stays refused by name

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: native-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: native-41's find 2 (2026-09-24)
