# A90 — `Color::var` / `Length::var` (style.vl:200/:128) render the name VERBATIM, so `var("button-color")` emits `var(button-color)`, which no browser resolves, and nothing says so until the page is wrong; the docs spell `var("--w")`. Refuse a name without the leading `--` at const time (curated, one row) — and `view.style_var(name, ..)` (browser/ui.vl:160) takes the same check if it writes the property name verbatim

- status: OPEN — filed at Order 33's GO (2026-09-11); styles-33
- kind: feature
- area: Reactive core & UI

`check_attribute_name`'s shape (style.vl): `check_custom_property_name(name)` panics at const time unless `name` starts with `--` and carries no spaces/quotes/`:`; called by both `var`s and by `style_var`. Message: "a custom property is written with its dashes — `var(\"--button-color\")`". Pins: the refusal, the `--` control, `style_var` with a dashless name (if it checks). Docs: one sentence at the `var` rows.
