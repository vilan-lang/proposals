# G21 — the watcher gives a const-recorded directory the HOOK reading, not the const key's

- status: OPEN — audit run 6's F23; recordable, safe direction
- kind: bug
- area: Macros & const
- source: audit run 6

`insert_watched_input` expands a const-listed directory to the whole
tree's files + nested dirs (the hook declaration's reading) while the
const gate keys on the IMMEDIATE SORTED NAMES only — so editing a listed
file's content, or an unlisted nested file, wakes rounds the const key
provably did not move for. Spurious rounds only. Align on the recorder:
each RECORDED_INPUT_PATHS entry should carry which reading it wants (the
kind row G16 gave the stamp is the pattern). HMR-skip half unconfirmed.
