# B300 — a BLANKET over a NESTED bound does not resolve: `impl type S: Source<type I: Source<type U>> { fun flatten(self): SignalCell<U> }` (MaybeSignal's shape, one level deeper) — (a) at ONE level the trait's own argument is not bound from the receiver (`let joined = outer.flatten()` → "never fully determined"; annotating the result makes it compile and RUN correctly), and (b) with the NESTED bound the receiver resolves to no concrete impl at all: "internal: a call resolved to `Source`'s requirement `get`, which has no body" — even for a fully concrete `SignalCell<SignalCell<i32>>` with the result annotated; the same one-level gap leaves `T` uninferred for `impl type S: Source<Option<type T>> { fun or(self, initial: T) }` although an ARGUMENT fixes it

- status: OPEN — Order 32's finds (2026-09-11); A86 waits on it
- kind: bug
- area: Type system & the type solver

Two defects in impl selection over a bounded subject: the bound's ARGUMENT (`I`, `T`) is not solved from the receiver's instantiation when the subject is `type S: Trait<arg>` (B268 fixed the dropped-argument matching; this is the binding half), and a bound whose argument is itself bounded (`type I: Source<type U>`) resolves the requirement call to the trait's bodiless declaration (B296's shape from the other side). Un-ignore the two pins; then A86 lands as written.
