# F39 — native — a REENTRANT cell read through an ALIAS of the cell being `update`d still PANICS at run time (a `RefCell` double borrow): native-41 refuses `todos.update(|&mut l| todos.get())` at compile time by NAME only when the read names the same place; `let t2 = todos; todos.update(|..| t2.get())` escapes the check. JS answers the in-progress value there. Wanted: the check follows aliases (B267's walk) or the runtime answers JS's value

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: native-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: native-41's find 4 (2026-09-24)

- 2026-09-25 (Order 42, native-42): DECIDED and BUILT (fb6c0ac9): the compile-time check FOLLOWS ALIASES — a `let` whose initializer is a place is followed back to the binding it copies (copying a value holding a `Shared` copies the handle); `let t2 = todos; todos.update(|..| t2.get())`, the reverse and alias chains refused by name; a value read out before the update is not an alias (control identical). Why not the runtime option: safe Rust has no second view while `update` holds the only `&mut`; answering the pre-update value would differ from node. An alias arriving through PARAMETERS is invisible statically — it stops at run time with vilan-rt's `REENTRANT_READ` message and exit 1.
