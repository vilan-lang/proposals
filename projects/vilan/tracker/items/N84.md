# N84 — two flakes under lane load: `keystroke::gate::the_entity_table_costs_the_edited_buffer_not_the_program` (32.2 s once, green after) and `rpc_http.rs::a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method` ('note err Waiting' — a lease answer read before the first lease failed); and `cargo test -p vilan-core --test inference -- modules::` fails two prelude-shaped tests that `cargo nextest` passes (threaded-process shared state)

- status: OPEN — Order 35's finds (2026-09-13)
- kind: hygiene
- area: Hygiene & rot

A load-aware bound for the cost gate; a settle wait in the rpc pin; find the shared state behind the two `modules::` tests (a global the prelude seeding leaves behind).
