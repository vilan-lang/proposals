# E209 — `reprint` compares TOKEN STREAMS and never re-parses its own output: N108's bug printed a `const { … }` with no `;` and was caught only because the token count differed — a lost-and-gained pair would print a file that does not parse

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: tooling
- area: LSP & tooling

`reprint` parses its output (one more parse per format) and declines with a distinct `DeclineReason` on failure; a pin with a planted printer bug that keeps the token count.
