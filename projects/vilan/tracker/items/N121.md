# N121 — N119's other half — a STACK OVERFLOW aborts the whole `vilan-lsp` process (exit 134) and no `join` `Err` can observe it, so the analysis fence N119 pinned catches panics only; wanted: a stack-remaining probe in the analyzer's recursive walks (`stacker`-style remaining-stack check turning a deep recursion into a diagnostic) and a pin at the playground's fence `lib.rs:472` (ledger row 293), which N119 could not reach (vilan-ide runs no analysis)

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: hygiene (LSP)
- area: LSP & tooling

Sizing M (the probe touches every recursive walk in analyzer.rs — or one `ensure_sufficient_stack` at the four entry points B385/B387 went through).
