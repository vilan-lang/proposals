# E215 — `[fmt] comment_width` — std's comment width (~80–88) is not the code width (100), so `wrap_comments = true` over std REFLOWS 6,719 → 5,573 comment lines to a width nobody chose; THE thing between the key and a default flip is either a `comment_width` key or a ruling that std never opts in. Also: `wrap_comments = true` does not mean 'no comment over the budget' (hanging-indent lists and the ten never-reflow classes stay) — the book must say so

- status: OPEN — filed 2026-09-21 (Order 39 sweep; fmt-39's finds 1 and 3)
- kind: ruling + feature (formatter)
- area: LSP & tooling

Rulings asked: R1 a `comment_width` key (default = the code width) vs a fixed ratio vs none; R2 does std opt in, and at which width (the measured ~84 keeps the prose as written); R3 when, if ever, `wrap_comments` flips on by default (rec: not before std has run under it for one order). Pins: the key parsed and completed (the manifest schema/LSP `TABLES` — the four-file set fmt-39 named); a width other than 100 respected; the book's `[fmt]` section states the ten never-reflow classes and 'over-budget comments can remain'. Sized S once ruled.

- 2026-09-22 (the owner's rulings before Order 40): RULED R1–R3 as recommended — R1 a `[fmt] comment_width` key, default = the code width; R2 std opts in at the measured 84 (prose stays as written); R3 `wrap_comments` does not flip on by default before std has run under it for one order.
