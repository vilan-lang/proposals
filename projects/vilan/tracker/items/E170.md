# E170 — in an element head a PLAIN attribute name paints as a function call (`<div class("row")>` → `class` is `entity.name.function.vilan`) and a KEYWORD-named attribute paints as the declaration keyword (`<input type("checkbox")>` → `storage.type.vilan`); only `on:` forms and hyphenated names get `entity.other.attribute-name`; E164's head region makes it a one-rule fix (`\b([a-z][a-zA-Z0-9_]*)(?=\()` inside the head, before `$self`)

- status: OPEN — Order 34's finds (2026-09-13)
- kind: editor
- area: LSP & tooling

One rule inside `#element-head`; scope pins for `class(..)`, `type(..)`, `for(..)`, an `on:` control and a real call control outside a head.
