# N64 — three notes from a65: `own` is a vilan keyword documented nowhere in the tour (cost a lane a debug cycle on a fixture); loading `pkg::lib::util` also loads `lib`'s body when it has one (Rust's model — a child import can pull a heavy parent), intended but unstated in the docs; build-hooks.md:1035's "a subdirectory cannot be imported today" is now false, so the generated root (kolt's `src/lucide`) may move under `root` as an ordinary module path — the build-hooks paper's follow-up

- status: OPEN — lane a65's finds (2026-09-08)
- kind: hygiene
- area: Hygiene & rot

Two doc lines (the tour's keyword list; names.md's parent-body sentence) and one paper note; the generated-root move is its own decision on the build-hooks paper.
