# B395 — a DEFAULT member of a supertrait, called on a type that implements the supertrait at OTHER arguments, is resolved through a SUBTRAIT's blanket and then refused on that blanket's bound: `trait Base<T> { fun read(self): T; fun twice(self): i32 { 2 } }  trait Feed<T> with Base<List<T>> {..}  impl type S: Base<List<type T>> with Feed<T> {..}  impl Cell with Base<i32> {..}` → `cell.twice()` fails "'Cell' does not implement trait 'Base<List<T>>'" (prints 2 without the supertrait clause, and with `Base<T>` — the ARGUMENT mismatch is the trigger). With A112 S3's ruled `DeltaFeed<T> with Source<List<T>>` in std it breaks `reactive-on-change.vl` (`Stored<i32>`'s `sub`/`map`), `reactive_channels::a_keyed_mirror_is_a_source_over_the_shipped_counted_lease` (a `KeyedSource`'s `effect_on_change`) and the native differential's corpus leg. Likely the B268 id-vs-arguments gap `impl_select.rs` documents at `subject_applies`: the blanket is taken as applicable at the trait-ID level (Cell provides `Base`, at `i32`)

- status: OPEN — filed 2026-09-22 (Order 40, the s3-40 lane's blocker #2; the THIRD solver gap on A112 S3's ruled shape — S3 shipped on the two-bound fallback)
- kind: bug (inference)
- area: Type system & the type solver

Fix: member selection through a blanket must check the blanket's bound at the ARGUMENTS the supertrait is implemented at, not the trait id; pins: the repro prints 2; the three reds above turn green with the supertrait form in std (a fixture with the shape, since std ships the fallback). Then A112's declaration switches to the ruled spelling (one std commit). Sizing S–M; solver lane, Order 41, FIRST.
