

- 2026-09-24 (Order 41, solver-41): FIXED (d476202a) — the item's cause was WRONG: `subject_applies` was fine (the emitter already drops the blanket); the defect was the analyzer's `inherited_default_candidates` keeping the FIRST registered provider of a default (the blanket) — an impl whose bounds do not hold at their arguments no longer carries the default. A SECOND defect surfaced with `DeltaFeed` switched in: `impl_bounds_hold` checked a bound's arguments without substituting the impl's other binders (`Keyed<K>`) — fixed. 6 pins (4 red before; the keyed-mirror one reds with only the substitution removed); B394's and B387's pins green; spec §5.5 one clause. The ruled switch proven green on this branch with std reverted — collections-41 applied it (8994d395).
