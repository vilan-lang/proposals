# E218 — `Expected u53, but got i32 instead.` carries NO `as_*` steer, while the binary-operator path (ledger 357) does — under I5 every `let n: i32 = list.len()` will meet this message, so it needs the conversion named (`as_i32()` / `as_usize()`) and a quick fix

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: diagnostics
- area: LSP & tooling

Sizing S; lands with I5 S2 or before it.
