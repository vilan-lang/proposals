

- 2026-09-24 (Order 41, index-41): BUILT (97acd1b3): every declared-type position (call, method, closure-call and variant arguments, `let`, reassignment, return, struct field, container slot) goes through one helper; when both sides are numeric widths the message appends 'There are no implicit numeric conversions; convert with `.as_X()`' (`BigInt` excluded as a target); LSP quick fix 'Convert with `.as_u53()`' (parenthesizes unless a name/field path/call chain); one NEW ledger row; 9 + 2 + 3 pins. Touches vilan-lsp/src/document.rs (editor-41's file: one dispatch arm + two functions).
