# N65 — ledger hygiene: parser CURATED rule statements (`CSS_BLOCK_IS_BRACE_INITIAL`, `IMPORTANT_HAS_NO_PLACE`, E153's `:hover` rule) are not enumerated by the ledger gate, so E153's "row EDIT" had no row to edit; and `resource_derive_refusal`'s message carries no ledger row at all (a helper-built `msg:` the ledger's checks cannot reach or miss) — unrowed messages, not red ones

- status: OPEN — Order 30's finds (2026-09-09)
- kind: hygiene
- area: Hygiene & rot

Enumerate the curated rule constants in the ledger's population check (row 229's twin for the css/parse constants) and row the helper-built message; pin both directions.
