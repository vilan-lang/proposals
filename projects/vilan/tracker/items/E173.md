# E173 — an unused import LEAF whose module Organize Imports will keep for its impls (E168's rewrite) fades with the generic 'unused import' text; per-leaf text ('unused; the module's impls are in use — Organize Imports rewrites this to `import pkg::a;`') needs `unused_import_spans` to return `Vec<(Span, String)>` and `publish::diagnostic_groups` to carry a per-span message

- status: OPEN — Order 34's finds (2026-09-13)
- RULED 2026-09-13 (owner, Order 35 GO, R8): BUILD — per-leaf fade text through `Vec<(Span, String)>` and publish.rs's group API. Lane editor-35.
- kind: editor
- area: LSP & tooling

Widen the return type and the group API; one pin on the text; E114's contract (fade == what the action edits) holds either way.
