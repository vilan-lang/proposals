# E148 — the "declare the inferred contexts" quickfix fires only on the SUBSET refusal (a declared-but-narrow clause); on an UNDECLARED function it needs a signature insertion span the parser does not record, and the triggering diagnostic sits in a different function

- status: OPEN — lane context-28's find (2026-09-05, Order 28)
- kind: editor
- area: Language server & tooling

Record the signature end span (after the return type, before `{`) on `Func`; offer the quickfix at the caller's "reachable without an enclosing run" refusal naming the callee; pin it inserting `context a, b` on a bare `fun`.
