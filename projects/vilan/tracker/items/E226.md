# E226 — the regime-3 `;` steer (a missing return value) is not reached through `.map(..).cell()`: the expectation stops at the receiver, so a `;`-ended closure body inside `map` followed by `.cell()` gets the plain mismatch instead of the steer; B421 (the `dyn` coercion accepting a void body — a MISCOMPILE) is the same blind spot at a coercion

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's find)
- kind: diagnostics
- area: LSP & tooling

Fix: the expected-type walk carries the steer through a method chain whose receiver is a closure-taking call. Sizing S.
