# B367 — B318 S4's ADMISSION MISS may be unreachable: a four-module reproducer (trait in A, `impl` in B, a generic body in C importing only the trait, the entry importing `(impl _)`) compiled CLEAN, so the generic body resolved under a file that does not admit the impl — either the refusal is dead code or visibility.md §3.5's rule is looser than written; and neither source-carrying transformer note (the admission miss, B55) has any pin

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Type system & the type solver

Re-derive the reproducer against B354's three-file fixture (which DOES refuse); if the four-module shape is a hole in the rule, it is a visibility soundness item; if the refusal is reachable only via derives, say so in §3.5. An HMR end-to-end pin for E196 rides on whichever reproducer refuses.
