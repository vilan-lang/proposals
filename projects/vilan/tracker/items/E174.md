# E174 — `Vilan: Show Language Server Status` prints the CLIENT's tally and only opens the channel the server's summary goes to, so E166's memory numbers arrive with the 500-request summary rather than on demand; a `workspace/executeCommand` (or custom request) that makes the server log its summary now would finish the item

- status: OPEN — Order 34's finds (2026-09-13)
- kind: editor
- area: LSP & tooling

Add the command on both sides; the summary line E166 built is the payload; a unit pin on the request handler.
