# B429 — unary `-x` on an UNSIGNED VALUE is accepted (`let u: usize = 3; let n = -u;`) — B407 refuses a negative literal at an unsigned type but not negation of an unsigned value, which on JS goes negative (unspecified under I5 ruling 2) and natively refuses to build

- status: OPEN — filed 2026-09-25 (Order 42, solver-42's find)
- kind: bug (inference)
- area: Type system & the type solver

Rec: refuse `-` on an unsigned operand with a steer to `as_i53()`/`checked_sub` (the native backend refuses it anyway, so this only aligns the JS answer). Sizing XS; solver.
