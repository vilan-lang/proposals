# B416 — `[derive(Wire)]` binds a LOCAL per field, so a field named `deserializer` (or any name the expansion itself uses) fails with 5 errors (`json.vl:995`, `rpc.vl:5692`) — the derive's hygiene: its own locals must not collide with field names

- status: OPEN — filed 2026-09-25 (Order 42, papers-42's probe)
- kind: bug (macro)
- area: Type system & the type solver

Fix: the expansion's locals take a spelling no user identifier can (a reserved prefix), or the derive reads fields through positional accessors. Pin: a `deserializer` field round-trips. Sizing S.
