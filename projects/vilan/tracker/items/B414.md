# B414 — CONTEXTUAL keywords: not every keyword needs banning as an IDENTIFIER — `with` (bit M86's borrowing read, now `peek`), `as` (only in imports), `in`, `layer`, `sync`?… should be usable as names where the grammar cannot confuse them; DECLARATION keywords (`let`, `mut`, `fun`, `struct`, `enum`, `trait`, `impl`, `import`, `export`, `type`…) stay reserved

- status: OPEN — filed 2026-09-25 (the owner's note at the Order 41 rulings)
- kind: design
- area: Type system & the type solver

Rec: a short probe-paper classifies the reserved list into hard (declaration/structural) and contextual (admitted as identifiers at binding, field, parameter and method-name positions where no production starts with them), with the parser lookahead each contextual word needs and the editor's painting rule (a contextual keyword used as a name paints as a name). Not in the train; Order 42 papers or later.

- 2026-09-25 (Order 42, papers-42): PAPER LANDED (`proposal/contextual-keywords.md`, ad60764): a 44-word × 9-position probe matrix; 28 of 35 keywords stay HARD (14 declaration words, 8 expression heads, `in`/`is`, 3 literals, `css` — ruled hard in css-block Q3); CONTEXTUAL: `with`, `borrows`, `own`, `dyn`, `lazy`, and `resource` once B413 lands; premise corrections: `as` and `sync` are ALREADY contextual, `layer` is not a word the compiler knows, `lazy` was made hard by a grep, not a ruling. Cheap in vilan: no expression puts two names side by side. Q1–Q6 with recs (OWNER): Q1 `jump` contextual (yes); Q2 any word — even hard ones — as a FIELD, METHOD or PATH-SEGMENT name (yes, slice S4; a JSON `"type"` key cannot be decoded today — 'missing field type_'); Q3 a raw-identifier escape (no); Q4 `dyn` as a type name (no); Q5 keyword hover on demoted words (yes); Q6 B413 deletes the `resource` KEYWORDS row (yes — relayed to syntax-42). Finds → F41, B416, B417, D14, E225. Slices S1–S4.
