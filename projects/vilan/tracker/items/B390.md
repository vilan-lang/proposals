# B390 — a REFUSED impl subject (one type argument too many) is refused at its own spelling AND still enters the candidate set with an `unknown` subject, so every later dispatch of that trait fails with 'both `Root` and `unknown` provide it and neither impl subject is more specific', pointed at an innocent call site while the real error sits LAST in the list. The field analogue is already pinned (`a_refused_field_does_not_cascade_through_its_uses`); an impl subject wants the same

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: bug (inference)
- area: Type system & the type solver

Fix: a refused impl subject does not enter the candidate set (or enters as poisoned and is skipped by specificity). Pin: the repro reports ONE error at the impl. Sizing S.
