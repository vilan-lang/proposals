# C15 — spec §6.9's closure rule ('captures BINDINGS, not values') makes every mutably-captured binding a SHARED CELL on a native backend: the F1 probe (R-2) had to box `let mut count` as `Rc<RefCell<_>>` because two closures assign it — on JS the host boxes the place for free; natively the compiler must decide per binding whether it is captured by value (copied), by counted reference, or refused. Larger than `Shared` (C14) — no item existed

- status: OPEN — filed 2026-09-14
- kind: design
- area: Memory model

A capture-mode analysis: a binding assigned after capture (or captured by two closures that write it) lowers to a counted cell; a binding never written after capture is copied (spec §6.1); the refusal class if any. Pins over the probe's shapes.

- 2026-09-17 (Order 37 GO): R3 RULED: v1 boxes every mutably-captured binding (`Rc<RefCell<_>>`) and native-b-37 COUNTS them over the exit corpus; the by-value capture optimisation is a later item on that measurement.
