# A99 — RETIRE the parent-method forms `View::when` / `View::swap` (and `View::swap_split`, the split recognizer's retarget twin) in favour of the free `when` / `swap` of `std::ui` that A85 shipped in Order 35: `<div>{swap(value, |x| match x { Some(_) => <span>"Exists"</span>, None => <span>"Empty"</span> })}{when(condition, || <span>"Is true"</span>)}</div>`. The methods are one-line sugar (`self.child(Conditional { .. })` / `self.child(Swap { .. })`) kept in lockstep across the browser and process layers; inside their own `impl` block they SHADOW the free function of the same name (which is why `split_route` exists under a third name and why the methods build the literal instead of calling the value form), and `chunks.rs` recognizes BOTH spellings of `swap` (lines 102/105 and 243/247) — one spelling halves it. BREAKING: the rewrite is mechanical, `parent.swap(s, r)` → `parent.child(swap(s, r))` or `{swap(s, r)}` in a child hole; census on 9b22ec36: kolt 6 `.swap` + 1 `.when` (theme.vl:270, views.vl:86/114/158/162/394, overlay.vl:502), std 0 live sites (router.vl:9 and ui.vl doc lines only), docs 7/6, examples 2/3, vilan-core tests 24/6, vilan-cli tests 11/9 (the `.when(` counts include `Style::when`, which STAYS). The owner's example spells `swap`/`when` BARE, so the `std::web` prelude gains the five slot functions (`when`, `swap`, `each`, `each_values`, `each_by`) beside `View`/`view`

- status: OPEN — filed 2026-09-14; RULED 2026-09-14 (hard removal; the `bind_each` trio goes too)
- kind: design
- area: Reactive core & UI
- 2026-09-14 (Order 36 GO): BUILD in lane lang-a-36 (six methods, hard removal, the steer + quick fix, the five slot fns into the `std::web` prelude, the estate rewrites). R7 RULED: kolt's nine sites are the integrator's at the sweep at the owner's word.
- 2026-09-14 (Order 36, lang-a-36): LANDED e535baa8 — six methods deleted from browser/ui.vl and FIVE from process/ui.vl (it never had `swap_split`); `split_route` folded into the free `swap_split`; chunks.rs recognizes the free `swap` only (`view_method` deleted); web.vl prelude `export import pkg::ui::{ View, each, each_by, each_values, swap, view, when }`; steer `retired_slot_method_steer` (row 212 re-keyed, no NEW row) + the ONE editor arm `Document::retired_slot_method_fix` ('Rewrite as `child(each(…))`'); estate rewritten: docs 9 pages, examples 5 packages / 9 sites, corpus ssr-render.vl, vilan-core tests 7 files, vilan-cli tests 6 files; kolt untouched (R7). Goldens: ssr-render.mjs, split golden app.js, markdown_anchors. FIND: `each_by`'s `T` freezes abstract when the render closure's parameter is unannotated (pre-existing; the retired METHOD bound `T` through the receiver, the free call does not — A99's rewrite is NOT type-preserving there; kolt has no `bind_each_by`, so clear). KOLT nine sites re-censused (views.vl:77, 118, 162, 166 (nested in 162's arm), 399; theme.vl:270; overlay.vl:502; theme.vl:216; views.vl:736) — at the owner's word.

std: delete `View::when`/`View::swap`/`View::swap_split` in browser/ui.vl (411, 441, 589) and process/ui.vl (300, 316); fold `split_route` back into the free `swap_split` once nothing shadows it; the free forms' doc lines stop saying 'the value form of `View::swap`'. Compiler: `chunks.rs` recognizes the free `swap` only (`view_method(program, "swap")` arms go; the retarget pair becomes `std_free_function` only); B268's note at ui.vl:965 updates. Prelude: web.vl `export import pkg::ui::{ when, swap, each, each_values, each_by };` (E145's rule 0 then fades an explicit import of them — fine). Editor: a quick fix on the removed method ('no method `swap` on `View` — write `child(swap(..))` or `{swap(..)}`') via the curated-rule steer, rewriting in place. Estate: docs 7+6 sites, examples 2+3, tests 24+6 / 11+9 rewritten; kolt's 7 sites at the owner's word (views.vl:158's route swap becomes `{swap(get_route(), \|route\| match route { .. })}` with the inner `.swap` at :162 wrapped the same way — every arm then yields a `View` as today). Pins: the method form is refused with the steer; the free form in a child hole places at the anchor (A85's pins); the split recognizer still retargets a recognized `swap` VALUE; goldens for the split fixtures regenerate over the merged tree.

**RULING 2026-09-14 (owner):** "Hard removal of the methods and move the `bind_each` trio over too." So the order deletes SIX parent methods — `View::when`, `View::swap`, `View::swap_split`, `View::bind_each`, `View::bind_each_values`, `View::bind_each_by` (browser/ui.vl 345/367/398/411/441/589; process/ui.vl 261/274/288/300/316) — with no `[deprecated]` window; the editor's steer on the vanished method rewrites the call to the value form (`parent.child(each(..))` / `{each(..)}`). Trio census on 9b22ec36 (method sites): std 0 live; kolt 2 (`.bind_each_values` at views.vl:731 and theme.vl:216 — kolt's other rows already moved to `ui::each_values` in the Order 35 migration); docs 12 + 3 + 2; examples 4; vilan-core tests 11 + 2 + 1; vilan-cli tests 8 + 9 + 3. The free `each`/`each_values`/`each_by` join `when`/`swap` in the `std::web` prelude. One breaking order, one migration note in the changelog listing the six rewrites.
