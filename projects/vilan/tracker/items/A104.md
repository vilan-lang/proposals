# A104 — `std::json` has no `Json`/`FromJson` for `Result<T, E>`: `Option` has both (json.vl:23 and :264) and kolt hand-writes the pair at shared.vl:65/80 as a `[kind, value]` array (0 = Ok, 1 = Err); the WIRE twin landed with A82 (variant-name tags) — the JSON twin should match `Option`'s spelling and the wire tags, not kolt's integers

- status: OPEN — filed 2026-09-17 (the kolt sweep)
- kind: feature
- area: Reactive core & UI

`export impl Result<type T: Json, type E: Json> with Json` and the `FromJson` twin beside `Option`'s in json.vl (a variant-tagged object, the same tag names the wire codec uses so a value round-trips both codecs by the same spelling); pins in `json_boundary.rs` for both directions and a malformed tag; kolt's two impls go at the owner's word.
