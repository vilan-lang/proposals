# D7 — `SignalCell::update`'s doc does not name the write-back the cell-growing idiom needs: A80's `Some(mut list)` binds a COPY (rule 1), so `list.push(..)` inside the arm grows the copy; the working spellings are `held = Some(list)` through the `&mut` subject, or `take`/`replace` — one sentence at the method, and A80's refusal steer could name it

- status: OPEN — Order 34's finds (2026-09-13)
- kind: docs
- area: Documentation

One doc sentence + the steer; `-p vilan-core --test docs`.
