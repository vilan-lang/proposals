# B249 — the missing-member diagnostic's suggested declaration renders the TRAIT's parameter names unsubstituted: `impl Counted with Source<i32>` is told to `declare fun on_change(self, observer: |T| void)` for a `T` it does not have

- status: OPEN — lane std-28's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

The refusal is right; the copyable line is wrong for concrete impls. Substitute the impl's arguments into the suggested signature (B206's written-name label is the renderer to reuse); pin `Source<i32>` and a two-parameter trait.
