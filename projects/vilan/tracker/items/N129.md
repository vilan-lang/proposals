# N129 — `native_differential` FAILS under plain `cargo test` (23/30): it shares one per-process staging directory across threads; nextest's process-per-test hides it — and CLAUDE.md's sentence that `cargo test --workspace` is equivalent to nextest is FALSE for this binary. Fix: a per-test staging directory (tempdir per test), or the sentence names the exception

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: hygiene-41's report)
- kind: hygiene (tests)
- area: Hygiene & rot

Discussion: hygiene-41's find 5 (2026-09-24)
