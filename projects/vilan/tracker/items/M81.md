# M81 — every `unwrap_or(<literal>)` now allocates a memo cell: 111 of A103's 114 `__lazy` thunk emissions carry an INERT argument (`0`, `-1`, `"?"`, `false`, `[]`, an enum constant); an 'inert argument stays eager' elision in `record_lazy_arguments` would keep most of the 16 moved corpus goldens byte-identical, remove the allocation and the `__force` on the hot path, and is unobservable by construction (a literal has no effects)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: perf
- area: Performance & footprint

A literal/constant argument in a lazy position is passed eagerly (no thunk, no cell); the callee's `__force` accepts a plain value; pin the elision and the non-elision (a call argument still thunks); regenerate the goldens back.
