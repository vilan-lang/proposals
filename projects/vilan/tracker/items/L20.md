# L20 — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word

- status: OPEN — lane l19-ci's find (2026-09-05, Order 28)
- kind: process
- area: Release engineering & beta

Install rustup + the msvc toolchain + cargo-nextest on the Windows side; the wrapper mirrors `ci-local.sh windows` but runs the suite instead of the cross-check; the seal script calls it when present.
