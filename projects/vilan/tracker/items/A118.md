# A118 — HIGH-ish: `std::random::range(low, high)` can return `high` — `random::range(1, 7)` answered 7 in 1 of 8 runs while the doc says `[low, high)`; docs/std/numbers.md's own fence prints `false` intermittently and no gate can see it (the fence prints a bool, it asserts nothing)

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Reactive core & UI

Read the implementation against the doc and fix whichever is wrong (rec: the implementation — half-open is the documented contract); a pin over a seeded or exhaustive small range; the docs fence becomes output-asserting when N47 lands.
