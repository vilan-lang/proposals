# B259 — B248's siblings: a block-like head of a binary expression OUTSIDE statement position (`let y = match x {..} + 1;`) reports "expected `;`" plus B248's refusal (same steer, different path); a POSTFIX chain on a block-like head (`match x {..}.to_str()`) is still the bare `found '.' expected an expression`

- status: OPEN — lane smalls-29's find (2026-09-06, Order 29)
- kind: bug
- area: Type system & the type solver

One refusal for both spellings (the steer is the same: parenthesize), one diagnostic each, no cascade; pin the `let` form, the argument form, and the postfix form beside B248's.
