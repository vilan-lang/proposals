# B392 — a LET-BOUND closure whose unannotated parameter feeds a generic position is refused: `let f = |m| wrap(m); f(4)` and `let f = |m| Holder { value = m * 2 }; f(4)` both answer 'type of variable f could not be resolved' — pre-existing on 49de3915, in the bare-parameter call and in B288's struct literal alike; the root cause is that a closure's type waits on its body while the body waits on the parameter

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: bug (inference)
- area: Type system & the type solver

Fix candidate: a let-bound closure whose parameter is unannotated takes its type from its first call site (one round after the fixpoint stalls). Pins: both shapes, plus a control where the parameter is annotated. Sizing M.
