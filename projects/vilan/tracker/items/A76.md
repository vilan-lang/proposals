# A76 — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there

- status: OPEN — Order 31's finds (2026-09-09)
- kind: feature
- area: Reactive core & UI

Either register a client channel on the SSE leg (the frame rides the event stream) or refuse `client = H` on a service mounted SSE-only; pin.
