# L21 — `~/.vilan/std-cache` is pruned ONLY by `vilan upgrade` (`prune_stale`, upgrade.rs:383, a seven-day age guard), so a dev toolchain refreshed from source — every lane binary, every order's release build — materializes a new `<CONTENT_HASH>/` tree and nothing ever deletes the old ones: 374 entries, 316 MB on the owner's machine since July 19. Prune at MATERIALIZATION too — after `materialize_into` lands a new hash, run the same seven-day `prune_stale` over the root minus the current hash (one readdir, atomic entries, safe by construction: an entry's mtime is its creation time and nothing touches it after) — and give the toolchain a `vilan cache prune` for the explicit gesture

- status: OPEN — filed 2026-09-13
- kind: tooling
- area: Release engineering & beta

`vilan_embedded_std::materialize_into`: after a successful atomic rename of a NEW entry (the only moment the set grows), call `prune_stale(root, 7 days)`; the current hash is never older than its own creation, so it survives; a concurrent older binary still reading a week-old tree is the same race `upgrade` already accepts. `vilan cache prune [--all]` in the CLI (and `--dry-run` printing what would go, with sizes). Pins: a fresh materialization prunes a >7-day sibling and keeps a fresh one; the current hash survives; the staging-leftover case (`.staging-*`) already covered by `prune_stale`'s pin. Docs: the toolchain page's cache paragraph (releases.md §3) gains the sentence.
