# B208 — a watch round's second trigger is LOST: round 1 fires in ~0.6 s, round 2 never fires in 300 s despite 15 re-touches (N46's undiagnosed mechanism)

- status: OPEN — hygiene-25 lane's find (2026-09-01); the recorded strike behind N46, still unexplained
- kind: bug
- area: CLI & build

N46's three strikes were read as load slowness; the recorded strike
is NOT: round 1 fires in ~0.6 s and round 2 never fires in 300 s
while the harness re-touches the file 15 times — a lost wake-up. The
serial group and the raised bounds (N46, closed) make the pin honest
under load; they do not find this. Diagnose: the watcher's event
coalescing/debounce window around a round in flight (an event
arriving during round 1's compile dropped instead of queued?), the
notify backend under WSL2 (inotify on a 9p/drvfs path?), or the
harness's touch not changing mtime/content. Reproduce with a
watcher log that timestamps every filesystem event and every round
decision; pin the queued-during-round case.
