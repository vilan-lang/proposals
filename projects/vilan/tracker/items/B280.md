# B280 — `freshen_list_element_slots`' EXTERNAL-function path is unguarded: B263 guarded only the declared-function return, so an external method returning `List<T>` reached through a bounded receiver would still erase the caller's rigid `T`; no exhibit found

- status: OPEN — Order 30's finds (2026-09-09)
- kind: bug
- area: Type system & the type solver

Apply `generic_is_enclosing_binder` on the external path too, with an exhibit (an external `fun items(self): List<T>` on a bounded receiver) pinned red first.
