# M68 — the release+trim at the analysis-landing seam costs roughly 100 ms per BACKGROUND document at kolt scale (estimated: 17.4 s vs 15.5 s over 19 opens) and runs on the runtime thread after publish; make the trim conditional on how much was released and measure it properly (CPU time)

- status: OPEN — Order 34's finds (2026-09-13)
- kind: perf
- area: Performance & footprint

Threshold the `malloc_trim` on released bytes; measure per-landing cost before/after; pin nothing new (the M64 pin holds the trim's effect).
