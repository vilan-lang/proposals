# E186 — Organize Imports' whole-run deletion leaves the paragraph gap behind: pruning a run followed by a blank line deletes the run plus one line ending, not the separator, so kolt's organized lucide shows two blank lines before `export *;` (pre-existing, E168-era; cosmetic; `vilan fmt` repairs a non-generated file)

- status: OPEN — filed 2026-09-15
- kind: editor
- area: LSP & tooling

Delete the run's trailing separator when the run was a whole paragraph; pin over the lucide shape.
