# E197 — format-on-save cannot say it DECLINED: `vilan-lsp/src/main.rs` (`formatting`) and `vilan-wasm/src/lib.rs` (`format_program`) both call `formatter::format`, which answers the original bytes on every way out, so a file the printer cannot render returns 'no edit' — indistinguishable from an already-canonical file. N90 closed exactly this silence for the CLI (`reprint` returns a `Decline` with reason, construct and line; `vilan fmt` exits 2 and names it)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: editor
- area: LSP & tooling

Both sites call `reprint`; the LSP surfaces a `window/showMessage` (or a log line under the session trace) naming the construct and line; the playground shows a status note; one pin each.
