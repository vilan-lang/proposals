# B399 — the ELEMENT bound in a tuple-family bound `T: (2..: PartialEq)` does not reach `U` inside a comprehension body: `a == b` on two elements is refused as unbounded although the pack declares the bound; probe `sweeps/order41/papers-41/probes/a122_12_element_bound.vl` (`a122_12b` the control that a pack WITHOUT the bound is refused — that half is right)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: papers-41's report + the GO one-liners)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: papers-41's F-c (2026-09-24); A122's `TupleKey<T, U>` typing needs it
