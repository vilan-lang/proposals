# B356 — explicit TYPE ARGUMENTS on an associated-function path were INERT for a TRAIT subject: `Signal<i32>::new("x")` compiled at d783fbf4 because only `Struct`/`Enum` subjects seeded `static_subject_bindings` (fixed for the static path by solver-37's `seed_trait_static_subject_bindings`, B352); whether EVERY path shape checks its written arguments deserves a sweep — B323's family ('an annotation-only generic argument is silently inert')

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: bug
- area: Type system & the type solver

Enumerate the path shapes that carry written type arguments (static call, struct literal, enum variant, trait static, qualified `pkg::T<..>::f`, `impl`-bound `Self`) and pin each against a contradicting argument; fix the inert ones at their seed.
