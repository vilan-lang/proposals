# F36 — native — two pre-existing gaps independent of `usize`: (a) INTERPOLATING a generic method's result is refused; (b) `encode_json` on a `List<i32>` produces Rust that rustc refuses (E0596, a `&mut` through an immutable binding)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: index-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: index-41's find 4 (2026-09-24); native lane

- 2026-09-24 (Order 41, native-41): (a) CONFIRMED by native-41 on the way to the exit: interpolating a generic call's result (`i"{cell.get()}"`) is refused ('its own render'); it also blocks `blanket-impl.vl`, `reactive-selector.vl`, `resource_take.vl` — repro: interpolate `SignalCell::new(1).map(|n| n).get()`.
