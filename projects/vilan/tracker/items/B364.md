# B364 — a CLOSURE RECORD cannot be built over a `&mut self` receiver (`a view cannot escape its scope`): 'erase a stateful thing behind a bag of closures' has one spelling — own a `mut` local in the frame that mints the record (spec §6.9's binding capture) — which is what deleted `JsonWriter::serializer()` and its three siblings under A108

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Type system & the type solver

Record the limit in lifetimes.md with A108's example; decide whether a `dyn`-style erasure over a `&mut` receiver is wanted (trait-objects.md's territory). Low priority; no build.
