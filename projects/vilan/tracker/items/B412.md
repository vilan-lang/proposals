# B412 — a generic `S: Source<X>` VALUE cannot be ERASED to `dyn Source<X>` — the explicit coercion at a `dyn`-typed position (dyn-40's rule) does not admit an abstract `S` that satisfies the bound, only a concrete type; needed by `Combine`'s `(U in T: dyn Source<U>)` in generic callers and by A124 §3's 'store as dyn'. Repro in `sweeps/order41/reactive-41/finds/`

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: reactive-41's defect 6 (2026-09-24); with B398 (the mapped-tuple element coercion)
