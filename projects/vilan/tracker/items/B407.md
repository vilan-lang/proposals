# B407 — NEGATIVE literals are ACCEPTED at UNSIGNED types: `let n: usize = -1;` (and `u53`, `u8`) compiles and prints `-1` — with B389 in place every `-1` sentinel compiles SILENTLY as a `usize` after I5 S2, which is exactly the class the migration must catch. Refuse a negative literal at an unsigned type (a diagnostic naming the type's range); the seven sentinels of index-type.md §3.4 are the pin

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: index-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: index-41's find 2 (2026-09-24); solver lane, before I5 S2

- 2026-09-25 (Order 42, solver-42): FIXED (d4cb70a1): a negative literal at an unsigned type refused naming the range (the check reads B389's recorded literal types); 12 pins incl. the seven §3.4 sentinel shapes; one ledger row; errors.md. Remainders: B426 (a constant `0 - 1`), B428 (`i8 = 128` accepted), B429 (`-x` on an unsigned value).
