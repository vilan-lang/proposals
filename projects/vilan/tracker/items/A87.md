# A87 — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it

- status: OPEN — Order 32's finds (2026-09-11); OWNER Q
- RULED 2026-09-11 (owner): NOT built — declined until a caller appears (the pattern is what fresh owners are for); stays open as the record.
- kind: design
- area: Reactive core & UI

If wanted: `fun release(self)` runs and empties the cleanups WITHOUT raising `disposed`; `dispose` = release + the flag. Doc the difference at `Owner`. Pin: release-then-take keeps the item alive; dispose-then-take releases it.
