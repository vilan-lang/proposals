# B408 — a BLANKET METHOD is UNREACHABLE through an abstract BOUND: `fun f<S: Src<i32>>(s: S) { s.twice() }` fails 'S has no method twice' when `twice` is a blanket over `S: Src<T>` — blocks `.cell()`/`.distinct()` in generic code TODAY and blocks A124 S2c (every generic `s.map(..)` once `map` is a blanket). Repro in `sweeps/order41/reactive-41/finds/`; reproduces on next @9ffa397e

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: reactive-41's defect 1 (2026-09-24); SOLVER, FIRST in Order 42 — the flip cannot land without it

- 2026-09-25 (Order 42, solver-42): FIXED (ca0dd51f): for a bounded parameter, method lookup falls back to blanket impls whose bounds are ENTAILED by the parameter's declared bounds (arguments, supertraits, nested-binder bounds); an inherent blanket member wires statically (B359's rule), a trait member a blanket provides re-dispatches to the most specific impl at monomorphisation; types.md; 11 pins (8 red-first). The flip's `map` blanket reaches through abstract bounds and `dyn`.
