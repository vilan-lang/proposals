# M55 — module-level SHARING across the platform axis: M52's union key was refused because kolt's browser leg is 82% of all base time and no seed union crosses the platform; the node legs load 48 and 49 sources from disjoint seeds but the FILES overlap — sharing a module's analyzed world across legs (and platforms where coloring allows) is where kolt's remaining base-cache headroom is

- status: OPEN — Order 30's finds (2026-09-09)
- 2026-09-13 (M67's ruling): QUEUED as the real fix for the base cache's footprint (13 worlds / 270 MiB for kolt) — the next perf order.
- kind: performance
- area: Performance & footprint
- 2026-09-14 (Order 36 GO): R6 RULED as recommended: MEASURE first (per-module overlap of kolt's three legs); BUILD the key only if the second node leg's `base` phase drops ≥ 50 %; otherwise the numbers go here. Lane perf-36.
- 2026-09-14 (Order 36, perf-36, R6): MEASURED AND STOPPED — kolt has TWO legs, not three (`[entry.client]` browser, `[entry.server]` node; the manifest's header comment still says three): browser 77 modules / base 619 ms, node 49 / 87 ms; 47 of the node leg's 49 modules are the browser leg's files (Jaccard 0.595) — the overlap is real but unreachable on R6's terms: there is NO second node leg to halve, the two legs sit across the platform axis (M52's refusal; `std::ui` resolves to a DIFFERENT file per leg), and the ceiling if sharing were sound and free is ≤ 83 ms ≈ 3.4 % of a 2.45 s build. REC (lane + integrator): CLOSE as measured-and-declined for kolt's shape at the sweep; re-queue only behind a package running two same-platform legs.

Measure the per-module overlap of kolt's three legs; design the key (module + platform + seed closure) against M19's record; pin the second node leg's `base` phase at ~0 on kolt's shape.
