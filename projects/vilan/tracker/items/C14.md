# C14 — `SignalCell` (and `Owner`) off `Shared` boxes and onto `Rc` or OWNER-ATTACHED arena `Handle`s — the necessary step for native apps with native code and NO garbage collector (F1). Today `SignalCell<T> { value: Shared<T>, subscribers: Shared<List<Subscriber>>, identity }` and `Owner { cleanups: Shared<..>, disposed: Shared<bool> }`; `Shared` is a compiler primitive (`external struct`, `primitive_struct_ids` 'Shared', 8 analyzer sites) whose doc says 'reference-counted' but whose JS backing is a plain object box the HOST GC reclaims — nothing counts, cycles (cell ↔ subscriber closures capturing cells) are the GC's problem, and a disposed owner's cells stay readable forever. Neither property survives a native backend. Two candidate representations, to be DECIDED in a paper: (a) a real `Rc<T>` — counted box with deterministic drop (`destruction.md`, `drop.vl`), cycles broken by the owner's disposal or by `Weak` (C1, OPEN); (b) owner-attached `Arena` + `Handle` (std::arena already exists; the owner holds the arena, a cell is `Handle<Slot<T>>` + its owner, disposal frees the arena wholesale — no counting, no cycles, and a stale handle reads `None` instead of a ghost value, which is a BEHAVIOUR change from today for a cell that escapes its owner: module-level cells, `std::memo` caches, rpc mirrors, `Task` results). Blast radius: reactive.vl 47 `Shared` mentions, 13 std files use `Shared`, kolt 4 (store, lib/input_system, lib/overlay, lib/rotary)

- status: OPEN — filed 2026-09-14
- kind: design
- area: Memory model
- 2026-09-14 (Order 36 GO): PAPER this order — `proposal/signal-cell-representation.md`, lane paper-native-36 (census, the two representations, a recommendation, slices; no build).
- 2026-09-14 (Order 36, paper-native-36): PAPER LANDED — `proposal/signal-cell-representation.md` (727 lines; census 154 std sites / 13 kolt sites — root 5.8 % / owner-scoped 63.0 % / escaping 18.8 % / frame-scoped 12.3 %; the two representations probed in Rust beside the paper, `native-apps-probe/`). RECOMMENDS (a): `Shared<T>` becomes the counted resource destruction.md §10 already specifies (retain on clone, release at last use on Tier 1's dataflow, `Weak` (C1) at exactly two std sites: `observe`'s captured cell and `Subscription`'s alias), `Arena`/`Handle` unchanged — because NO owner owns a cell today (only four types are `Disposable`), so (b) would invent a relation at 126 sites and turn `Source::get` into `Option<T>`. Slices S1 (frame-scoped subtraction, M71) → S2 (`Weak`, C1) → S3 (std's two back edges weak) → S4 (counted representation, native only) → S5 (JS counted mode as an instrument, optional). CORRECTIONS to this item: the exit test is the SCC gate `a_disposed_exemplar_holds_no_reactive_cycle` + `heap_cycles.js`, not the leak-soak harness (which measures the Rust process); `Task` holds no `Shared`; a `set` after `owner.dispose()` still COMMITS and notifies (the cell is fully live, not stale); lifetimes.md §9's non-goal ('changing `Shared`'s JS representation') is reopened and the paper proposes the amendment. OWNER RULING wanted on the recommendation before Order 37 builds S2–S4.

Paper first (`proposal/signal-cell-representation.md` or a section of `memory-management.md`): (1) census every `Shared` use in std and kolt and classify each — owner-scoped (dies with a boundary), root-scoped (module-level, lives for the program), escaping (handed across owners: memo caches, rpc mirrors, `Task`); (2) the two representations against that census, with the JS backend as the FIRST target — an `Rc` on JS is a box whose counts are no-ops, an arena handle on JS is two integers plus a lookup, so either lands and is verified on JS (leak-soak.md's harness, `heap_cycles.js`) before any native backend exists; (3) the semantics of a read after the owner's disposal (today: the ghost value; arena: `None`/refusal; Rc: the value while any holder lives) and what `ambient-owner.md`'s `owner_scope` context guarantees; (4) subscriber lists — the cell → subscriber → closure → cell cycle is the one that forces `Weak` or the arena; (5) `Owner` itself (its `cleanups`/`disposed` boxes) and `Signal`/`Memo`/`RemoteSource`/`Task` in the same pass; (6) the `[derive(Wire)]` identity stamp (A92) survives either. Decide, then slice: representation type + std's reactive core on it + the JS pins green + kolt unchanged at the surface. Not before the paper: no `Shared` deletion — `Shared` stays for genuinely diffuse ownership (its own doc's case) and gains real counting under a native backend.

- 2026-09-17 (Order 37 GO): R1 RULED as recommended — (a), the counted `Shared` with `Weak` at two std sites, `Arena`/`Handle` unchanged: S1 (M71), S2 (C1 `Weak`, JS-lowered), S3 (the two back edges weak) BUILT in lane native-a-37 with the SCC gate as the exit; S4 waits for F1's backend; S5 queued (optional).

- 2026-09-17 (Order 37 sweep): S1–S3 BUILT in lane native-a-37 (c5b98491, 1379ccdd, 7d9427dc) under R1 = (a): eight of the nineteen frame-scoped cells retired (the eleven json/binary cells are A108 — the Wire visitor's by-value receivers; fs.vl's cursor is forced by an await), `Weak<T>` with a no-wrapper JS lowering and the one missing analyzer seam for a bodiless `Option<&T>`, the two back edges weak; a match-capture aliasing MISCOMPILE in B267's walk found and fixed. §10 S3's exit test is unachievable on JS by construction (the SCC count is unchanged: 243/2 mounted, 122/0 unmounted) — S5's counted mode is the first instrument that can see it; paper §13. S4 waits for F1's backend (S1a landed the same order); S5 optional, queued.

- 2026-09-21 (Order 38 sweep): A108 (lane wire-38) retired the TEN cells §13 filed as blocked (json 6, binary 4): std `Shared::new(` 128 → 118, and 121 on the merged tree beside A110 door 1's three liveness cells; the F class is one file, `process/fs.vl`'s cursor. S4 is unblocked by S1b; S5 with it.

- 2026-09-24 (Order 41 GO): R-d RULED — S4 (the counted `Shared` natively) is native-41's droppable TAIL behind the rpc-server exit; S5 stays queued.

- 2026-09-24 (Order 41, native-41): S4 NOT BUILT (native-41, R-d's tail): natively `Shared`/`Weak` are already a real `Rc`/`Weak` count — retain on clone and release on drop hold BY CONSTRUCTION; what S4 still names touches reactive.vl (reactive-41's) and the rpc-server path (a stop condition), and needs a native leak/SCC gate that does not exist. Rec: re-scope S4 to 'the native leak gate' (a counted-`Shared` census over the exit program) and close the representation half as built by F1 S1a.

- 2026-09-25 (Order 42, papers-42): RE-SCOPED (signal-cell-representation.md §15, 5c481c2): the representation half is CLOSED by F1 S1a — natively `Shared` is `Rc<RefCell<T>>` and `Weak` a real weak count (probed: an orphaned cell upgrades to `None` natively, `7` on JS); §10 S4 done by construction item by item except 'Owner's two cells become one body' (dropped: an allocation saving, not correctness); the NATIVE LEAK GATE needs three things (a live-cell counter in vilan-rt; zero live owner-scoped cells asserted at exit, root-scoped ones reported; red by a planted strong cycle) — native-42's; S5 (the JS counted mode) rec DROP: 117 of std's 143 `Shared::new(` sites build natively and fall under the native gate, the 26 browser-only ones run under a GC where the cycle gate + subscriber-list pins already catch what matters (OWNER Q: confirm the drop). Stale doc: vilan-rt's `Shared` comment still says real counting is a later slice.
