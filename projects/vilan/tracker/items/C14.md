

- 2026-09-24 (Order 41, native-41): S4 NOT BUILT (native-41, R-d's tail): natively `Shared`/`Weak` are already a real `Rc`/`Weak` count — retain on clone and release on drop hold BY CONSTRUCTION; what S4 still names touches reactive.vl (reactive-41's) and the rpc-server path (a stop condition), and needs a native leak/SCC gate that does not exist. Rec: re-scope S4 to 'the native leak gate' (a counted-`Shared` census over the exit program) and close the representation half as built by F1 S1a.
