# M75 — `refuse_imported_member_collisions` is O(collisions × files × statements) through `statement_for`'s linear scan — zero on the estate (gated on a banked collision), but the first real two-package collision in a large program pays it; an index `(importer) → [(span, sources)]` is the fix

- status: OPEN — filed 2026-09-15
- kind: perf
- area: Performance & footprint

Build the index once per program; pin a 500-statement two-package collision under a time bound.

- 2026-09-17 (Order 37 GO): BUILD in lane perf-37.
