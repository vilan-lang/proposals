# A93 — `child_relation` is a PUBLIC RAW-SELECTOR HATCH and kolt uses it as one: `views.vl:165` writes `child_relation("not([hidden])", "display", ..)` — the token is neither `^…` nor `>…`, so it lands in the pseudo slot, skips the `@layer` wrap and the `> *` suffix, and renders `.sX:not([hidden])`, a rule about the element itself from a child-relation method; A89 gives it a real spelling (`attribute("hidden", None, not(..))` emits the identical selector, verified). Fence the token to `>*` / `>*+*` (the two shipped relations) once kolt's site migrates

- status: OPEN — Order 33's finds (2026-09-11)
- kind: feature
- area: Reactive core & UI

`child_relation(self, token, name, inner)` (style.vl ~1432) accepts any token string; the two callers (`children` = `>*`, `divide` = `>*+*`) are the whole legitimate set. Check the token at const time against those two (curated, one row) after kolt's one site moves to `attribute("hidden", None, not(style().flex_col()))`; pin the refusal and the two controls.
