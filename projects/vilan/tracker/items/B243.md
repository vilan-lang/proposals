# B243 — a supertrait's default body does not substitute the impl's trait argument into a closure PARAMETER type when reached through a single-block sub-trait impl: `impl X<type T> with Signal<T>` types `x.map(|v| ..)`'s `v` as the trait's abstract `T`

- status: OPEN — lane std-28's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

Splitting into `with Source<T>` + `with Signal<T>` resolves it, which is the recipe on A49's tombstone. Localize the substitution the sub-trait impl skips for closure parameter types (B216's `supertrait_position_type` is the neighbour) and pin the one-block impl with `map`, `effect_on_change` and a user default body.
