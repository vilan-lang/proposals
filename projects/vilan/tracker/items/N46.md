# N46 — the watch pins' 301 s talking timeout reds under lane load: strike three

- status: OPEN — 2026-09-01, the third strike (Order 20 close, Order 24 hygiene lane, Order 24 pre-seal union)
- kind: process
- area: Hygiene & rot
- source: `build_hooks::an_edited_hook_input_starts_a_watch_round_and_reruns_the_hook` (301.3 s in the union at loadavg ~25 with a sibling suite running; 0.7 s alone, 53/53) and its sibling `a_file_added_under_a_declared_directory_input_starts_a_watch_round` (Order 24's records lane)

The pins are correct and the hardening (talking timeout + re-touch,
Order 20) does its job — it names the wait — but a wall-clock wait
for a watcher round is exactly the shape M15 fixed for the const-pass
gate: preemption accrues to it. Directions, pick one with numbers:
(1) a nextest test GROUP for the watch/HMR/e2e pins with
`max-threads = 1` and `slow-timeout` raised, so they never contend
with a full-suite interleave (the union's own load); (2) make the
round's wait CPU-time-aware the way M15 did (the watcher process's
own CPU time, not wall); (3) M22's fix (the watch round recompiles
every leg — 3.9 s CPU on kolt-sized trees) shrinks the round enough
that the timeout stops binding. N40 (free_port) was this family's
other member and is closed; this is the last named flake source in
the union.

2026-09-01, the family widens to the LSP harness: docs-law's union
saw `vilan-lsp::package_recolor_tests::a_package_import_edit_recolors_
the_open_file_it_reaches` red twice (23.6 s / 29.9 s, "the error must
clear without a restart") under an 11-lane load; alone at loadavg 86
it passed in 13.8 s, and CI passed it on the sealed tip — the pin's
recolor WAIT is wall-clocked. Same remedy as the watch pins: the
nextest group / a CPU-aware wait.
