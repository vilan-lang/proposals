# A67 — A65's ruling left open: `import pkg::a::b` where `a.vl` DECLARES an item `b` and `a/b.vl` also exists takes the FILE (longest module prefix wins, documented in `longest_module_prefix`), not an ambiguity error — unlike `a.vl` + `a/lib.vl`, which is one

- status: OPEN — lane a65's find (2026-09-08); OWNER Q
- kind: design
- area: Type system & the type solver

Decide and pin: either the ambiguity error (recommended — a directory is the author's statement, but so is the item, and a silent winner is how a rename goes wrong) or the documented longest-prefix rule with a note in names.md §4.2.
