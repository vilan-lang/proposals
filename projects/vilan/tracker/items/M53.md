# M53 — compile-perf-29's smalls on a cold kolt check: `infer_type_path` clones `constraint` on entry 180,670 + 148,029 times (a `Cow` at twenty use sites inside a 1,300-line function, ~0.23%); path handling ~3% (`Components::next` 2.15% + `Path::_starts_with` 0.70%); `const_eval::State::new` 0.69% + 0.68% (a per-evaluation allocation); `Node`'s next ceilings 136 (`Trait`, `StructInitializer`) then 120 (`Element`, `Struct`, `Enum`), boxing all five lands at 96, below which `Node::Call` at 88 is an expression where boxing trades the memcpy for an allocation

- status: OPEN — lane compile-perf-29's finds (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Four slices, each with a callgrind profile diff on the E121 exhibit and kolt's client; stop at `Node::Call`.
