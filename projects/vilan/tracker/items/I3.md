# I3 — Iterator adapters — the remainder

- status: OPEN (remaining: S6/Iterable under B4)
- kind: feature
- area: Collections
- source: S–M

_Original title parenthetical: (S–M)_

   STATUS: OPEN (remaining: S6/Iterable under B4)
   The arc SHIPPED 2026-08-06; §4 option (ii) REFUSED by owner ruling.
   Live remainder: S6/`Iterable` waits on B4 (trait-typed-value
   dispatch). Record: iterator-adapters.md §11. History:
   backlog-2026-07-18.md §I item 3.

- 2026-09-17 (the kolt sweep at Order 37 GO): `filter_map` is the adapter kolt hand-writes as `List::map_filter` (server.vl:92 under `// FIXME: Implement with std.`) — the smallest slice of this item; Order 38 candidate.

- 2026-09-17 (Order 37 sweep): (kolt sweep note stands; no Order 37 lane) — `filter_map` is Order 38's smallest slice.

- 2026-09-21 (Order 38 sweep): `filter_map` LANDED in lane std-38 (594d2e6a) — the adapter (`FilterMapped<I, T, U>`) and the eager `List` twin in option.vl beside `find`. The remainder (S6/`Iterable`) still waits on B4.

- 2026-09-21 (the kolt sweep before Order 39): kolt's two hand `List::map_filter` impls (server.vl, channel.vl — the second was unused) are RETIRED onto std's `filter_map` in kolt's working tree.
