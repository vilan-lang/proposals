# B386 — MISCOMPILE — a list subscript did NOT type-check its index: `xs[at]` with `at: f64` (or a `str`, a `bool`; the write form too) passed `vilan check` and crashed at run time (`Cannot read properties of undefined`); the native backend refused it with an unrelated rustc error so the differential could not see it

- status: CLOSED 2026-09-22 (Order 40, solver-40 8258b73c, filed at the sweep for the record)
- kind: bug (inference)
- area: Type system & the type solver

Landed as solver-40's add-on: the subscript operator requires the positional index type for a `List` and a fixed array, reads and writes; six pins in `inference/std_surface.rs`; one ledger row (558). Filed closed so the ledger row and the CHANGELOG entry have an item.
