# A115 — element syntax does not admit an `Option` attribute: `AttrValue` has arms for `str` and `Source<str>` only, so `<div data-dragging(maybe)>` is refused while `bind_attr` (A105) takes `Source<Option<str>>` — A105's own text claimed A89 had built it (A89 gave `Option<str>` to `Style::attribute`/`within`)

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: feature
- area: Reactive core & UI

`AttrValue` arms for `Option<str>` and `Source<Option<str>>` (`None` omits/removes), both twins, an element-syntax pin beside A105's; kolt's `bind_attr_proper` call site then becomes element syntax.
