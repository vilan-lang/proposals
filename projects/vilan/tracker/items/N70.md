# N70 — `analyzer::service_impl_source`, the Rust FALLBACK `[service]` generator, is a stale twin: no `turn` wrapper, no keyed exposures, still `fun dispatcher(self)`, no `connect_with`, no reconnect hook, no handle mapping, and its `service_contract_hash` disagrees with the macro's for any keyed service; it runs only for fixture stds without `rpc.vl` (the `inference` harness uses the real std), and the file's own comments insist the two must not disagree

- status: OPEN — Order 31's finds (2026-09-09)
- kind: hygiene
- area: Hygiene & rot

Delete it (the fixtures that need it get a minimal `rpc.vl`) or gate it behind a comment that says it is a stub; a pin that the fallback is unreachable from any shipped path.
