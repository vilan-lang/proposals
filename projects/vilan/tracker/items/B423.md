# B423 — a literal in an `if` ARM is not typed from its SIBLING arm in an unannotated `let`, either order: `let n: u53 = 5; let m = if n > 2 { n } else { 0 };` (and the reverse) → '`if` arms have mismatched types: expected u53, but got i32'; with a declared return type (`fun pick(flag: bool, n: u53): u53 { if flag { 0 } else { n } }`) it passes — not among index-type.md §4's positions; std needs it nowhere (I5 S2's residue is zero) but kolt hits it

- status: OPEN — filed 2026-09-25 (Order 42, index-42's find)
- kind: bug (inference)
- area: Type system & the type solver

Fix: an `if`/`match` whose arms are a typed value and a literal takes the typed arm's type for the literal (the same peer step B389 gave a stalled binding). Pins: both orders, the `match` twin, a control with two literals (default). Sizing S; solver.
