# A121 — no std FOCUS MANAGEMENT for an overlay: kolt's panel focuses `[x-autofocus]` or else the panel itself (lib/overlay.vl:536 `FIXME: Focus the panel in such a way that tab moves to the first tab-able thing. Or even better, make the overlay the only tab-able container (opt-in/opt-out), even for menus, without making the top-level container inert`) — a focus TRAP and a first-tabbable query are both hand-work today

- status: OPEN — filed 2026-09-21 (the kolt sweep before Order 39)
- kind: feature
- area: Reactive core & UI

`std::dom`: `Element::tabbable(): List<Element>` (the standard tabbable selector + visibility/disabled filter), `Element::focus_first()`; `std::ui`: `View::focus_scope()` — Tab/Shift+Tab cycle within the subtree while mounted, restoring focus to the previously focused element on disposal (the owner's `defer`), WITHOUT `inert` on the rest of the page (kolt's menus need the page live). Paper section first (papers-39) — the trap's interaction with nested overlays and with `autofocus` is the design.
