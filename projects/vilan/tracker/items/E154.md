# E154 — E150's third: a `for x in <iterable>` head lacks the split permission rule B gave `for` conditions and `match` subjects (a 117-column iterable is left untouched); the obvious one-line sibling

- status: OPEN — Order 30's finds (2026-09-09)
- kind: editor
- area: Language server & tooling

Grant the permission to the iterable position; pin an over-budget iterable breaking operator-leading and a short one inline; one reformat commit if the tree moves.
