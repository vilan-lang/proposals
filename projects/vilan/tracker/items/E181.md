# E181 — `vilan fmt` gives the module-level `export *;` marker a canonical place: just below the file's imports — module comment, blank line, the import block, blank line, `export *;`, blank line, then the items. Today the printer keeps the marker wherever it was written (before the imports, in the middle of the file, glued to the next item without a blank line — probed on 9b22ec36: all four placements survive `vilan fmt` unchanged) because it prints statements in source order and only sorts imports WITHIN their run; B318 S1 introduced the marker and S6's estate sweep is about to write it into most of std, so the rule should land before the sweep prints hundreds of them

- status: OPEN — filed 2026-09-13
- kind: tooling
- area: LSP & tooling
- 2026-09-14 (Order 36 GO): BUILD in lane editor-36.

Formatter (`formatter.rs`, `Node::ExportAll` printed at ~3429; the statement walk that coalesces import runs ~2943): (1) a bare `export *;` anywhere at the top level moves to the slot after the file's LEADING import run — the run that begins the file after the module comment and any leading comments — with a paragraph gap on both sides; a file with no imports puts it after the module comment, before the first item; a comment attached directly above the marker travels with it; (2) `export import …` re-exports, `export(in PATH) item`, and `export item` are declarations and stay where they are — only the bare marker moves; (3) a second `export *;` (the analyzer's business — refuse or ignore, check what S1 does) is left in place so the formatter never hides a diagnosis; (4) idempotent — the canonical shape reprints byte-identical. Insertion points that must respect the slot: the add-import quickfix (E54) and Organize Imports' first-import insertion put a NEW leading import ABOVE the marker, never below it; the organizer's `import <module>;` rescue (E168/E180) lands inside the run as today. Pins: marker before the imports → below them; marker mid-file → below them, its comment along; glued marker → paragraph gaps; no imports → after the module comment; a file already in the shape is unchanged; the add-import fix on a file whose only header line is `export *;` inserts above it. Order: before B318 S6's `export *;` estate sweep, so the sweep's output is already canonical and `vilan fmt --check` (N55's gate) stays green across it.
