# B240 — file mode's residues after B239: B226's refusal is pushed once PER IMPORTED NAME at one span (raw LSP diagnostics do not dedup); file mode cannot see that ANOTHER file is a declared entry (`views.vl` importing `pkg::client::helper` is clean in file mode, refused by `check .`); a DEPENDENCY file opened as the entry would lose its own derives (`base_cacheable` pre-marks SourceId(0), undone only under `!entry_is_module`)

- status: OPEN — lane b239's find (2026-09-04, Order 27)
- kind: bug
- area: Type system & the type solver

Three small holes on the open-file path. (1) Push B226's error once per import statement. (2) Give the open-file analysis the manifest's declared-entry set so a sibling's import of a declared entry is refused in file mode as it is in package mode. (3) The latent dependency case: pin it (open a dependency's file as the entry, assert its derive holds) and route it through the OpenFile order.
