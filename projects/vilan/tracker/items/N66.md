# N66 — harness notes from Order 30: the replay harness drives kolt's client clean only with `pkg_root = <repo>/src` (the manifest's `root`) — the repo root gives 22 spurious "cannot find … in the imported path" errors and 39 sources instead of 69, a trap for anyone measuring a real package; `reused_source` and `container_site_visited` are two spellings of one predicate at two granularities

- status: OPEN — Order 30's finds (2026-09-09)
- kind: hygiene
- area: Hygiene & rot

A doc line on the harness's `std_root`/`std_spec` helpers (or a manifest-reading helper that finds `root`); one predicate taking either granularity.
