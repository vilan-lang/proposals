# B355 — E189's BROAD GATE (drop the `context`-family diagnostics when the list already carries a resolution/type error, with a note) is buildable only after `b279_an_unresolvable_dispatch_site_still_fences_its_candidates` gets a NON-DIAGNOSTIC observation of the fence: that pin observes a SOUNDNESS property (the refined-dispatch fallback widening to the whole candidate list — without it the program compiles and prints `undefined`) only THROUGH the coverage fence, in a program that carries another diagnostic by construction. diagnostics-37 built the gate, measured it (kolt's `href(href())` shape 139 → 1), and removed it because it blinds that pin (AGENTS.md's stop condition). The narrow rule landed; E191's remaining 138 wait on this

- status: OPEN — filed 2026-09-17 (Order 37 sweep); OWNER Q
- kind: feature
- area: Type system & the type solver

Give the pass a non-diagnostic face for the fence (the dispatch fallback's candidate set is computed either way — expose it on `Program` or a test-only accessor) and re-pin B279 on it; then land the broad gate behind it, restating `b229_only_…`/`b241_…` as deferrals; E191 closes with it (re-run the kolt copy: expect 1).

- 2026-09-21 (Order 38 GO): R3 RULED — the third option: a non-diagnostic face for B279's fence, then the broad gate; lane solver-38; E191 closes on the kolt-copy number.
