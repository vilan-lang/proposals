# A117 — `std::style::prelude` publishes 21 internal ALIAS spellings nobody designed as surface: `export *;` re-exports an ALIASED import, so `open_style`, `scale_step`, `value_piece`, `custom_property` and seventeen `*_condition` names resolve from outside — 21 of the module's 67 exports, and A106 door 2's largest objection

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Reactive core & UI

Hide them (`#hidden`, or move the aliasing into each one-line body); a pin that the module's export set is the designed 46; then re-run std-38's census script (kept in its report) for A106 door 2 — it read 6 file-level / 40 local shadows at 67 names.
