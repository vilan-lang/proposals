# B427 — `combine((a, b)).map(|pair| { let (x, y) = pair; x + y })` leaves `map`'s `U` OPEN: a destructuring `let` inside the closure does not carry the tuple's element types into the body's tail; annotating `pair` fixes it — under the flip `map` is a blanket, so this is the everyday spelling

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's find S3)
- kind: bug (inference)
- area: Type system & the type solver

Fix: a destructuring `let` of a closure parameter binds the pattern's names from the parameter's inferred tuple type before the tail is typed. Pin red-first. Sizing S; solver.
