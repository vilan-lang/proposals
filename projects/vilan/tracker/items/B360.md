# B360 — MISCOMPILE-class: a user-written `external fun` with no `[extern]` binding and no compiler lowering emits a DANGLING mangled name at an ORDINARY call site — `external struct Thing; impl Thing { external fun poke(&mut self, v: str); }` then `t.poke("x")` emits `$b(t, "x")` with `$b` undefined, clean at check

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Type system & the type solver

REFUSE it: an `external fun` with no binding is meaningful only inside std (B265's rule) — a row at the declaration naming `[extern(..)]`; pins for the method and the free-function forms; census std for bindingless externals that are NOT lowered.
