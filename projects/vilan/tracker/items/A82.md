# A82 — `std::wire` has no `Result<T, E>` (kolt hand-writes forty lines at `store.vl:27`, variant-tagged `Ok`/`Err` exactly as `Option` is) and its scalar row stops at `i53`: no `u53` (`store.vl:143` wants an unsigned id; `u53` IS a scalar, `u64`'s rename), nor `i8`/`u8`/`i16`/`u16`/`f32` — in `wire.vl` AND in the analyzer's allowlist (`str | i32 | u32 | i53 | f64 | bool`), so the two must move together

- status: OPEN — kolt marker sweep (2026-09-10)
- kind: feature
- area: Reactive core & UI

`impl Result<type T: Wire, type E: Wire> with Wire` beside `Option`'s (take kolt's impl; a hash-stable `Ok`/`Err` tag); `impl u53 with Wire` and the small scalars in one row; both allowlists (`is_wire_type`, `resolved_type_is_wire`) and the refusal text updated in the same commit; the contract-hash goldens re-checked (a new impl moves no hash — pin it). Docs: `std/wire.md`'s table.
