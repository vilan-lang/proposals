# B238 — `disabled` is not usable as a binding name (parse error): a reserved-word leak

- status: OPEN — lane std-27's find (2026-09-04, Order 27)
- kind: bug
- area: Type system & the type solver

`let disabled = …` fails to parse. Find which lexer/parser table claims `disabled` (an element-attribute keyword? a style token?) and scope it to that position; census the estate for other leaked words (grep the keyword tables against identifier use). Pins: the binding, a field named `disabled`, the attribute position still working.
