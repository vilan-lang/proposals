# N43 — the phase-timing line mislabels two buckets

- status: OPEN — editor-perf lane's find (2026-09-01)
- kind: process
- area: Hygiene & rot
- source: lane editor-perf ("both mislabels cost real time to see through")

`VILAN_PHASE_TIMING` labels `context::thread_contexts` as
`call-graph` and `const_eval::check_const_only` as `const-eval`;
both were the same `dispatch_refine::refined_edges` call, and the
"const-eval" 1,189 ms was NOT const evaluation (the real const
passes were 11 ms). Rename the buckets after what they time, and add
the `dispatch-refine` bucket explicitly so the next perf lane reads
the split without a detour.
