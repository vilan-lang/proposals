# F42 — native — `KeyedCell` does not build natively at all, before or after M87: 'unbound generic type parameter (parameter 2 of struct `Map`)' at `positions = Shared::new(Map::new())` — probably B406's struct-literal-field shape (a generic constructor inside a field not typed from the field); the keyed rpc pins cannot run natively until it does

- status: OPEN — filed 2026-09-25 (Order 42, collections-42's find)
- kind: bug (native)
- area: Native targets & rendering

Re-probe after B406 lands; if it still fails it is the native emitter's own binding of a struct-literal field's generic. Sizing S to probe.
