# A120 — a `[service]` cannot be used as a PLAIN HTTP API: kolt's login/register must run BEFORE a socket can be authorized, so it hand-writes the whole path — `mod KoltAuth` (store.vl:51 `FIXME: Make into a http api service (no real-time mechanics)`), a hand `match parse_path(request)` building JSON responses (server.vl:31 `FIXME: Support basic http apis as services`), and `fetch::post("/api/login", body)` + `Result<AccountToken, str>::from_json` on the client (comp/login.vl:77/181). The connectionless `POST {mount}rpc` leg EXISTS on the server (transport-rpc; `rpc_http.rs`) — what is missing is a CLIENT that speaks it without a socket, and a story for a service that is ONLY that

- status: OPEN — filed 2026-09-21 (the kolt sweep before Order 39)
- kind: design
- area: Reactive core & UI

PAPER first (Order 39, lane papers-39): `KoltAuthClient::connect_http("/auth/")` — a generated stub whose calls are `fetch` POSTs to the existing leg (no handles, no notifications, no `client =`; refused by name at the declaration if the service declares any); `[service(http)]` or a mount-time choice; how `authorize` composes (the token a login returns is what the socket's `rpc_protocols` then carries); status codes for `RpcError`; CSRF/same-origin posture; what kolt's three sites become.

- 2026-09-22 (Order 39 sweep): papers-39's section SPLICED into `transport-rpc.md` as **§9.7** (thirteen sub-sections, slices S1–S5, Q1–Q5 with recs). HALF WRONG as filed: a hand-built `AuthClient<HttpTransport>` ALREADY WORKS over `POST {mount}rpc` (the transport is a type parameter); what is missing is `over_http(mount, codec)` (not `connect_http` — no connection), four refusals (`client = H` compiles clean today — the one hole), a status table (200 for everything the protocol decides; 401 typed envelope; 501 for a factory service is PLAIN TEXT), and the bugs first. S1 (the decode gate) is BUILT — std-39's add-on 2321790e (the reader poisons; the long-list half is B383). The two bugs: `HttpTransport` crashes the process on an unreachable host → B374; `[service]` over an `export impl` → zero routes silently → B375. Also from the probes: `HttpTransport` sends `text/plain;charset=UTF-8` (CORS-safelisted — Q3's posture: std does no CORS but REQUIRES a non-safelisted content type); `GET {mount}rpc` is answered 200 + Decode (nothing reads the method — S3's 405). Rulings Q1–Q5 asked (recs: `[service(http)]` an opt-in marker; a second hook `authorize_request`; the content type; `over_http`; an application `Err` is never a non-200). Order 40 candidate: S2+S3 after B374/B375.
