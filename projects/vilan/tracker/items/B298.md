# B298 — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's

- status: OPEN — Order 32's finds (2026-09-11); OWNER Q (trait-typed-fields.md rev 2 §Q3)
- 2026-09-11: not this order — stays behind trait-typed-fields.md rev 2's Q3.
- kind: design
- area: Type system & the type solver

Rule Q3 (how two `impl X` faces are told apart in a message — by position, by an ordinal, by the bound's argument), then route `_` binders through `implicit_binder_bound`. Until then the three-`_` message stands; pin the current text so the change is visible.
