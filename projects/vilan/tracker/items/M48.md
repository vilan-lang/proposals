# M48 — M19 tranche 1d: R10 `check_container_resource_arguments` is now the largest single item on the phase line (477–1,113 ms warm on kolt's client, comparable to the enrolment gate T1c removed); its whole-program state `reported_container_structures` is already a `HashSet<String>` structural key, id-free and recordable — the blocker is that its diagnostics sit OUTSIDE T1's Class A recording window, so restoring it means moving that window

- status: OPEN — lane m19-t1c's find (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Widen the Class A window to cover R10's diagnostics (or record R10's under a second window keyed the same way), record `reported_container_structures` per module, restore per reused module; the replay differential and the 58/58 pin are the gate; measure the leg before/after with loadavg.
