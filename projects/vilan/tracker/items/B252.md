# B252 — a refused RETURN annotation does not stand its uses down (`cannot call method 'who' on unknown` follows), where a refused closure parameter does (B182's stand-down)

- status: OPEN — lane b184-b218's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

The call result is not the annotation's slot, so B182's covered set misses it. Extend the stand-down to a call whose callee's return annotation was refused; pin the cascade gone and an unrelated miss still reporting.
