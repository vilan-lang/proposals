# A72 — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate`

- status: OPEN — Order 30's finds (2026-09-09)
- kind: feature
- area: Reactive core & UI

`current_url(): Source<str>` beside `current_path()` (one `popstate` listener); rule on `segments` decoding (recommend: decode, one form); pins.

- 2026-09-17 (the kolt sweep at Order 37 GO): two more faces from kolt: no REPLACE-state navigation (client.vl:39/44 hand-writes `history.replaceState` + `navigate_in_place` over `path_signal`; `navigate` pushes only) and routes.vl:25's ask for "a canonical url parsing format" (A62's `FromPath` answers the parse; kolt not yet migrated).

- 2026-09-21 (the kolt sweep before Order 39): STILL OWED — kolt client.vl:39/44 still hand-writes `history.replaceState` + `navigate_in_place` over `path_signal` (`FIXME: Implement with std (A72)`); Order 39 lane std-39 takes the replace-navigation face (`navigate_replace` / `navigate(path, replace = true)`).

- 2026-09-22 (Order 39 sweep): ONE face BUILT by std-39 (a66fed94, BREAKING) — `navigate_replace(path)` (not a parameter: no overloading, no defaults); kolt's hand `history.replaceState` extern + `navigate_in_place` deleted, two call sites. The item's other two faces stay open.
