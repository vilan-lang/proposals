# E200 — a DOUBLE report for one mistake, pre-existing and not widened by B344: a lazy argument reaching a resource through a FIELD of a resource binding (`hold(flag, holder.conn)`, and the same through a `&Holder` loan) gets both R9's capture message (naming the binding) and the lazy-argument message (naming the field's type, ledger row 475) — two diagnostics for one shape; closing it means deciding which owns it

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: diagnostics
- area: LSP & tooling

Rec: the lazy-argument message owns the shape (it names the type and the position); R9 stands down when the argument check has spoken; pin both forms at one diagnostic; row 475's vacuity pin unchanged.
