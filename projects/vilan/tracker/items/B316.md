# B316 — `reconcile_type(Trait, Concrete)` REFUSES what every value-first position accepts (B4 / method-resolution.md §10): measured at 18 correct programs when a pass reported on the parameter-first drop (B306's rejected backstop — `Doubler`'s supertrait defaults read "Expected Add, but got Doubler"), so any future 'report where the binder gave up' instrument is unbuildable until the arm is settled; and B304's real discriminator was receiver-resolution TIMING (a chain receiver defers, a parameter receiver freezes the one-shot slot) — the other `is_unknown_closure_parameter` doors may be schedule-dependent the same way

- status: OPEN — Order 33's finds (2026-09-11); OWNER Q (the arm's reading)
- kind: design
- area: Type system & the type solver

(1) Rule the arm: a bare-trait PARAMETER type reconciled against a concrete argument is the universal reading (accept when the concrete implements the trait) — then B306's backstop becomes buildable and the 18 programs are the pin set. (2) A sweep of the one-shot-slot doors for schedule dependence, each pinned with a chain receiver and a parameter receiver (B304's pair is the template).
