# C16 — C12 PROMOTED to a native-backend precondition: a captured VIEW of a dead local (C12's closed-as-limit shape) is a use-after-free the moment the host stops boxing places — on JS the escaped view reads a boxed ghost; on the emit-Rust backend it is a dangling borrow rustc will refuse or a freed slot the runtime will read. The limit C12 accepted as 'spec §6.9's honesty limit' must become a refusal before F1's S1

- status: OPEN — filed 2026-09-14
- kind: bug
- area: Memory model

Make the escape a refusal (the analysis exists as C13's pinned-ignored case); plant the probe's shape red first; sequence before F1 S1.

- 2026-09-17 (Order 37 GO): R7 RULED: blocks F1 S1 — the refusal lands FIRST in lane native-b-37 (C13's pinned-ignored case is the first exhibit).
