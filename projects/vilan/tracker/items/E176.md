# E176 — a PARENLESS head item has no attribute vocabulary in the TextMate grammar: `disabled` in `<input type("checkbox") disabled />` and the `hidden` of `hidden.show(flag)` paint as nothing; one more rule inside the head region, which must decline the tag name and a `{hole}`

- status: OPEN — Order 35's finds (2026-09-13)
- kind: editor
- area: LSP & tooling
- 2026-09-14 (Order 36 GO): BUILD in lane editor-36.

One rule after E170's; scope pins for both shapes and the tag-name control.
