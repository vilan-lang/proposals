

- 2026-09-24 (Order 41, editor-41): BUILT (cbc31fe4): `vilan.autoClosing.generics` (default on) + the `type` override — after `<` the extension asks `vilan/opensAGenericList` and inserts `>` in the same undo step; a `>` typed onto one it placed moves the caret; keystrokes queue behind a pending answer (≤ 150 ms); placed only before `autoCloseBefore` characters; the extension reports the override in `initializationOptions` (re-sent on setting change) and `onTypeFormatting` stands down for `<` while set; if another extension (Vim) owns `type` the override stays off and says so. Pins: server + the TS half textually (`vscode_extension.rs`, `book_sync`).
