# D8 — `shared.vl`'s headnote says 'prefer an `Arena` + `Handle` whenever an owner exists', and 0 of the 167 `Shared` sites in std and kolt follow it — no cell has an owner today (only `Subscription`, `Owner`, `ReactiveServer`, `ReactiveClient` are `Disposable`); the sentence describes a relation the estate does not have. Rewrite it to what the C14 paper found: `Shared` is the counted box for diffuse ownership, `Arena`/`Handle` is the tier for graphs and stable identities

- status: OPEN — filed 2026-09-14
- kind: documentation
- area: Documentation

One headnote rewrite in std/src/shared.vl + the memory-model tour's sentence; lands with C14's S1 or alone.

- 2026-09-17 (Order 37 GO): BUILD in lane native-a-37 (with C14 S1).
