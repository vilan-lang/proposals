# N97 — the analyzer's expression walk overflowed libtest's 2 MiB test thread ON WINDOWS at NINE levels of nesting (`b250_a_declared_leg_with_no_manifest_is_unchanged` ABORTED the Windows shard with 0xc00000fd at Order 36's seal; Linux passes at 2 MiB and fails at 1 MiB) — `walk_expr_node_inner`'s frame GREW this order (the lazy, const, callable and visibility arms all landed in it) while the recursion did not; the binaries are safe (the compiler runs on a 128 MiB thread), so the seal fix raised the suite's test threads to 8 MiB (`.cargo/config.toml` `[env] RUST_MIN_STACK`). Left: MEASURE the frame (`walk_expr_node` + `_inner` per level, both codegens), bound it, and plant a CANARY pin — a 30-level nested fixture analyzed on a thread of a fixed size — so the next bloat is caught on Linux instead of by the Windows shard

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

Measure with gdb's frame deltas (the integrator's method: ~110 KB per level on Linux debug); split the walk's giant match into per-arm fns if the frame is the cause (boxing large locals); the canary pin at 2 MiB over a deep fixture; note the config in AGENTS.md's test section.

- 2026-09-17 (Order 37 GO): MEASURE + canary pin in lane hygiene-37; the walk's SPLIT is not this order (five lanes share analyzer.rs) — a FIND if the measurement says so.
