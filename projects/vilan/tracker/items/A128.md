# A128 — A121 S3 (LOW) — the `focusout` path: `Event::related_target` is bound and pinned (ui-40), so a `Contain` scope can learn 'focus left me, and to there' and a `Wrap` scope can dismiss on Tab-out if an app wants it; deferred by focus-scope.md §S3 and by ui-40, `Contain` is correct without it

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: feature (std::ui)
- area: Reactive core & UI

Sizing S.

- 2026-09-24 (Order 41, hygiene-41): STOPPED, not built — the brief's "`Wrap` may dismiss on Tab-out" CONFLICTS with focus-scope.md Q2 ('a third arm for Tab-out-dismisses-me is not needed: under `Wrap` focus never leaves by Tab'), and "`Contain` learns focus left me, and to there" names no API. PROPOSAL for the owner: `FocusScope::on_leave(|to: Element|)`, fired from a capture-phase `focusout` when `relatedTarget` is outside the scope and every scope above it; a null target (focus left the document) does nothing (paper §S3); both twins; Q2 re-ruled or confirmed first. OWNER Q.

- 2026-09-25 (the owner's rulings after Order 41's seal): RULED as recommended — `FocusScope::on_leave(|to: Element|)` from a capture-phase `focusout` when `relatedTarget` is outside the scope and every scope above it; a null target does nothing; Q2 CONFIRMED (no Tab-out arm under `Wrap`). Order 42, std/ui.
