# N124 — F26's impl-ADMISSION parameter is UNPINNED: the shared `resolve_inherited_default` takes the admitting file as a parameter (JS passes `current_admitting_file`, native `None`) because the half-work had silently passed `None` on both — and planting `None` on the JS side passes the WHOLE estate, since nothing in it restricts an import. Wanted: a B354-style multi-file pin in `module_resolution.rs` where an `only` import declines one of two traits' same-named defaults

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: hygiene (tests)
- area: Type system & the type solver

Sizing S.
