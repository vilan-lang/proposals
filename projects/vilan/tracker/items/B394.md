# B394 — UNSOUND — a blanket whose bound is a trait's own supertrait satisfied that bound for ANYTHING: `trait Feed<T> with Base<T>` + `impl type S: Base<type T> with Feed<T>` made `fun takes<T, S: Base<T>>(s: S)` accept a plain struct (`satisfies_trait_bound` found the blanket providing `Base` through `Feed`, bound `S := Other`, asked itself the same question until the 32-level depth cap answered YES); with A112 S3's `DeltaFeed` shape in std every `S: Source<T>` bound accepted an `i32`. Exposed by B387's fix a6bf7c43 (before it the blanket itself was refused) and found by the s3-40 lane before the seal

- status: CLOSED 2026-09-22 (Order 40, solver-40b 55464670, filed at the sweep for the record)
- kind: bug (inference)
- area: Type system & the type solver

Five pins in `inference/bounds.rs`, red on 3e4e6c51 and green with either half alone. LESSON for the solver: a cap that answers 'yes' when it gives up is a soundness hole waiting for the next cycle — every such leniency in analyzer.rs is worth a census (N-item candidate).
