# E141 — the keystroke gate's per-request completion budget is a RELEASE figure and the gate has no profile guard: red at 289e2a2b under debug (0.705–0.813 ms vs 0.2), invisible because `#[ignore]`d

- status: OPEN — lane completion-27's find (2026-09-04, Order 27)
- RULED by measurement 2026-09-05: in RELEASE on a quiet box (loadavg 1.4) completion is 0.038–0.041 ms and the burst 0.042–0.043 ms against the 0.2 ms budget on BOTH exhibits, and `diagnostics_budget` PASSES on the view-shaped exhibit too — the slowness was the debug profile entirely, nothing to resolve. Build: the gate skips under debug naming the release figure (cfg!(debug_assertions)); the debug numbers stay informational. S; Order 28.
- kind: editor
- area: Language server & tooling

M25's tombstone measured 0.126 ms in release; two Order 27 lanes measured 0.7–0.8 ms in debug and read it as drift. Either the gate skips (with its reason) under `cfg!(debug_assertions)`, or the budget is re-derived per profile with both recorded. Also: PR CI never runs the ignored instruments — a "known-red gates" leg that asserts they stay red until the named item lands (E126's question) would make drift visible. Pins: the gate names its profile in its reason string.
