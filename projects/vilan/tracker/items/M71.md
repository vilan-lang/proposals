# M71 — 19 FRAME-SCOPED `Shared` cells in std want a `mut` local: cells constructed, mutated and dropped inside ONE function frame (json/binary/fs/rpc_server/rpc — 12.3 % of std's 154 `Shared` declaration sites) pay a box allocation and an indirection for state that never escapes the frame; each is a `mut` local (or a `&mut` parameter) in the language as it stands. The C14 paper's S1 ('the frame-scoped subtraction', size S)

- status: OPEN — filed 2026-09-14
- kind: perf
- area: Performance & footprint

Rewrite the 19 sites to `mut` locals; measure Ir on the rpc codec paths before/after; a census pin (the classifier in `native-apps-probe/census.py`) that the frame-scoped class stays at zero.
