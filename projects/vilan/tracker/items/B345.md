# B345 — `scan_closure_view_captures`'s `Expr::Call` arm never walks the call SUBJECT (only the arguments) — pre-existing, and now also on the lazy path, so a view named inside a nested call subject in a closure or a lazy argument is missed by the view-capture scan; one line, but it changes an existing check's firing

- status: OPEN — filed 2026-09-15
- kind: bug
- area: Type system & the type solver

Walk the subject; pin the shape; a breaking note if an estate program fires.
