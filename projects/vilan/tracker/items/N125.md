

- 2026-09-24 (Order 41, native-41): DONE (3d59e59e): the rename `vilan-embedded-std` → `vilan-embedded`, 27 files incl. Cargo.lock, the notices' coverage line, one word in AGENTS.md; `RELEASE_FILES` → `release_files()` derived from `crates/*/Cargo.toml` (the cut plan's F1), the printed `git add` line likewise, a pin comparing it with the workspace members both ways (red with the old list). The four per-emitter resolution functions STAY per-emitter (solver-41's B393 verdict → B403).
