# E179 — the session summary line carries `memory:` and `programs=` but nothing about the base cache; `base_cache_worlds/weight/budget` belong beside them now that M67 bounds the cache (session_trace's pin text is E174's payload)

- status: OPEN — Order 35's finds (2026-09-13)
- kind: editor
- area: LSP & tooling

Three fields on the retained-state line; the E166/E174 pins extended.
