# B347 — `each_by`'s `T` freezes abstract when its render closure's parameter is UNANNOTATED: `view("nav").child(each_by(handles, |h| h.id, |h| … h.map(|c| c.title)))` reports `cannot access field 'title' on type T` plus a spurious `owner_scope … can be reached without an enclosing run` cascade on every sibling chain link; pre-existing, but A99 makes it reachable at every site — the retired `bind_each_by` METHOD bound `T` through the receiver path and the free call does not (the one place A99's rewrite is not type-preserving); `each`/`each_values` unaffected; kolt has no `bind_each_by`

- status: OPEN — filed 2026-09-15
- kind: bug
- area: Type system & the type solver

Bind `T` from the source argument before the closure parameters are typed (the receiver path did it for free); pin the unannotated form; drop the four annotations.

- 2026-09-17 (Order 37 GO): BUILD in lane solver-37 (bind `T` from the source before the closure parameters; B352 carries three sibling exhibits).
