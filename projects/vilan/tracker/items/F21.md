# F21 — `Option<&mut T>` — a VIEW inside an enum payload — is a latent wrong TYPE natively: `returns_mut_view` is applied to the outer return type only, so `fun inner_mut(&mut self): Option<&mut Inner>` would emit `-> Option<Inner_…>`; refused BY NAME since native-b-38 (`option-view.vl`, `transparent-references.vl`)

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Native targets & rendering

Carry viewness into the payload type the way it is carried into a return type — one slice; it is what a view-returning `Arena::get` needs natively.
