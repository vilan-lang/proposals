# K15 — the playground does not say when the formatter DECLINED: vilan-wasm now exports `format_checked` → `{ text, declined }` (E197), but the deployed glue calls the String-shaped `format`

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: feature
- area: Web presence

Call `format_checked` where present (feature-detect by name) and show the decline sentence in the status note.
