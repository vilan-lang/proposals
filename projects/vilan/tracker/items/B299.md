# B299 — a BARE-TRAIT IMPL SUBJECT parses and then refuses every use of `self`: `impl Source<type I: Source<type U>> { fun flatten(self) .. }` is accepted at the head, but inside the body `self` is a bare trait type — "cannot call 'sub' on a value of bare trait type 'Source': a trait is not a value type (vilan has no trait objects)" — so the spelling is a trap: it means nothing a body can use, and the working spelling is `impl type S: Source<..>` one token away

- status: OPEN — Order 32's finds (2026-09-11)
- RULED 2026-09-11 (owner): DESUGAR — `impl Trait<..> { .. }` means `impl type S: Trait<..> { .. }`, the universal reading B186/B184 already give a bare trait. BUILD: Order 33, lane solver-33.
- kind: bug
- area: Type system & the type solver

Either REFUSE the head (a trait in impl-subject position with a steer: "write `impl type S: Source<..>` to implement for every source" — one row) or make it MEAN that (desugar `impl Trait<args> { .. }` to `impl type S: Trait<args> { .. }`, the universal reading every other bare-trait value position already has — B186 parameters, B184 fields). The second is the consistent one and costs no new spelling; the first is smaller. Owner's call; pin either.
