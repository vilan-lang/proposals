# M51 — `build` in parallel: the AUDIT (perf-29, no code) — the obstacle is one `dist/` and two order-dependent accumulators: `leg_schedule` is a real producer→consumer DAG (parallelism caps at the widest tier); `bundled_names` is mutated per leg and read by every later one (collision blame becomes scheduler-dependent — lock + member-order blame); `explain::leg_facts` is process-global and loop-ordered (M35's capture/replay); `write_assets`/`write_bundled`/`write_chunks` share one directory and `write_chunks` sweeps a namespace (per-leg staging + atomic move, or a `dist/` lock); `watch_state: &mut` must become per-leg results merged after join; `?` aborts at the first failing leg. Smallest honest step: parallelize `compile_unit` within a tier, keep every writer serial in schedule order. `check_single`'s per-platform loop's blocker is the process-global B182 `RoundReports` ledger

- status: OPEN — lane perf-29's audit (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Build the smallest step with the audit as the checklist; measure a three-leg workspace (kolt read-only) CPU and wall with loadavg; pin that collision blame is deterministic under parallel compile.
