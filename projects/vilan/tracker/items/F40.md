# F40 — RULING — OS RANDOMNESS natively: kolt's real `server.vl` is 4 host gaps from native (PBKDF2 + `toString`, `random_bytes`, one generic `map`); `random_bytes` and PBKDF2's salt need OS randomness and `vilan-rt` is dependency-free with `forbid(unsafe_code)` — no `getrandom` without either a dependency or `unsafe`. Options: (a) a second optional crate `vilan-rt-crypto` (like `vilan-rt-sqlite`: `getrandom` + PBKDF2/SHA-512, linked only when reached — rec); (b) read `/dev/urandom` through std::fs (Linux/macOS only; Windows needs an API); (c) refuse by name for good

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: native-41's report)
- kind: ruling (native)
- area: Native targets & rendering

Discussion: native-41's find 5 (2026-09-24); the last wall before kolt's server leg builds natively AS WRITTEN

- 2026-09-25 (the owner's rulings after Order 41's seal): RULED (a) as recommended — a second optional crate `vilan-rt-crypto` (OS randomness via `getrandom`, SHA-512 + PBKDF2), linked only when reached, `vilan-rt` itself stays dependency-free and `forbid(unsafe_code)`. The owner asked where the unsafe comes from: OS randomness is a SYSCALL — `getrandom(2)` on Linux, `BCryptGenRandom` on Windows, `SecRandomCopyBytes` on macOS — reached only through FFI (`extern "C"` calls are `unsafe` in Rust) or a crate that wraps that FFI; Rust's std exposes no stable safe random source; the one safe route is reading `/dev/urandom` through `std::fs`, which is unix-only; SHA-512 and PBKDF2 themselves are pure computation with no unsafe (SHA-1/SHA-256 are already hand-written in vilan-rt). Order 42, native lane — the last wall before kolt's server.vl builds natively as written.

- 2026-09-25 (Order 42, native-42): BUILT (28bf86d2 + f1511065), THE EXIT REACHED: kolt's REAL `src/server.vl` builds natively with 0 host gaps (premise corrected: 3 gaps on d65d4e75, not 4 — `pbkdf2_sync`, `random_bytes`, `to_string_encoded`; the generic `map` gap was already gone) and its login exchange matches node byte for byte bar the masked random tokens (nine exchanges on each backend over a fresh kolt.db identical; cross-backend over ONE database file: an account registered on node logs in on native and vice versa; a planted PBKDF2 defect makes both answer 'wrong password'). `crates/vilan-rt-crypto`: `getrandom` 0.3, `forbid(unsafe_code)`; `random_bytes`, `random_uuid`, SHA-384/512, HMAC-SHA-512, PBKDF2 (std's four async WebCrypto twins) + node:crypto's `pbkdf2Sync` and its `NodeBuffer` (`toString` hex/base64/base64url/utf8) — SHA-512/HMAC/PBKDF2 HAND-WRITTEN (the RustCrypto route adds six crates; the ruling asked for one dependency at the syscall), pinned to FIPS 180-4, RFC 4231 and node's own output incl. kolt's 100,000-round call; the crate named in the manifest only when reached (`reaches_sqlite` → `OptionalCrates`); the embedded toolchain materialises both optional crates; the crypto crate at opt-level 3 in dev (2.6 s → 0.14 s per hash); the host-table walk deduplicated (`lowered_host_binding`). vilan-rt CHANGED despite the brief: `Shared::identity` returned an i64 address where std declares i32 (5 `cell_identity` sites refused) — now an i32 counter stamped at first ask, as JS; vilan-rt still dependency-free + forbid(unsafe). THIRD-PARTY-NOTICES regenerated (getrandom, r-efi, wasip2, wit-bindgen; Cargo.lock +4). Records in `sweeps/order42/native-42/`. Whole set 124 / 93 / 31 / 0 (was 91/33).
