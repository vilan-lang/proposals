# B282 — an `[rpc]` method with a parameter named `request` does not build: `route_block` emits `|request| { .. let request: str = arg(request, 0); .. }`, so the author's parameter shadows the closure's — `[rpc] fun send(self, request: str): str` fails with "Expected RpcRequest, but got str" spanned on the STRUCT; only `request` collides (`reason`, the decode arm's binding, is safe)

- status: OPEN — Order 31's finds (2026-09-09)
- kind: bug
- area: Reactive core & UI

Gensym the route closure's parameter and thread it into `arg(..)`/`decode_failed(..)`; pin the `request`-named parameter.
