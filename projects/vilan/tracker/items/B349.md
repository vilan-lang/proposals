# B349 — a MODULE-LEVEL (top-level statement) body resolves its members under NO file scope: the transformer's `current_admitting_file` is set only by `enter_instance` (a function-body seam), so module-level initializers and top-level statements resolve unrestricted — harmless today (they sit in the file that declares them, which admits its own blocks) but a hole in §3.5's rule

- status: OPEN — filed 2026-09-15
- kind: bug
- area: Type system & the type solver

Set the admitting file at the module-body seam too; pin a module-level call through a selector-restricted import.
