# E184 — E178's FOURTH completion consumer: `auto_import_completions` (`AutoImportOrder::build`, vilan-ide) still offers every importable, because it runs outside `owned_modules::collecting()` and calling `module_importables` there parses open buffers into the process-global parse cache once per keystroke (four `overlay_module_reclaim` pins red, measured by editor-36); visibility-36 exposed `Program::exported_entities` + `Program::curated_modules` (= `Analyzer::is_exported_in`) for exactly this — wire the filter on them

- status: OPEN — filed 2026-09-15
- kind: editor
- area: LSP & tooling

One filter in `AutoImportOrder::build` reading the two Program fields; a pin that a curated module's private item is not auto-import-offered and an uncurated module's is; the four reclaim pins stay green.

- 2026-09-17 (Order 37 GO): BUILD in lane editor-37.
