# G17 — a `generated` root declared through a symlink fails OPEN

- status: OPEN — audit run 6's F5; order-material, reopens §12.1's loop
- kind: bug
- area: Macros & const
- source: audit run 6, repro verified

`generated_root_problem` refuses lexically; `generated_root_covering`
canonicalizes both sides and climbs from the file's canonical directory —
a symlinked `src/icons` puts the file outside the package's canonical
tree, the manifest is never found, the exclusion silently stops, and the
fmt↔hook ping-pong returns with the honesty note never printing. Both
consumers affected (fmt and format-on-save). Fix: one resolution rule for
both functions (the G15 shape: resolve the declared root as the walk
does); pins with the symlinked-root repro.
