# B353 — a `set` with a TYPE MISMATCH reconciled without error (kolt views.vl:222, commented): `channels: Signal<List<i53>>`, `incoming: Option<List<i53>>` from a `RemoteSource` effect; `channels.set(incoming)` without the `if incoming is Some(..)` guard 'reconciled without error even though there's a type mismatch'. If it reproduces on d783fbf4 it is UNSOUND — an `Option` stored where a `List` is read, a miscompile class — and the likely mechanism is B352's: the effect closure's parameter left abstract, so `set`'s argument check had nothing to compare

- status: OPEN — filed 2026-09-17 (the kolt sweep)
- kind: bug
- area: Type system & the type solver

Probe first with the minimal shape (`Signal<List<i53>>::new([]).set(Some([1i53]))` — expected: refused) and then the exhibit's shape (a `RemoteSource<Option<List<i53>>>`'s `effect(|incoming| channels.set(incoming))`); if the second passes, the closure's parameter is abstract and every call inside it is unchecked — that is the finding, and B352's fix must make the pin red; if neither reproduces, record the probe and close.

- 2026-09-17 (Order 37 GO): PROBE + fix in lane solver-37 (HIGH until probed).
