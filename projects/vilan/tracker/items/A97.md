# A97 — HOLD-COUNTING vs remote-sources §3: the server counts a client's leases per channel and revokes at zero (A92), so a RAW client that sends `Subscribe` twice and `Unsubscribe` once holds its channel open until disconnect; the alternative — client-side demand coordination that never double-subscribes — is plumbing the generated client already does and a raw client would have to repeat. Rule whether the wire contract is 'one Unsubscribe per Subscribe' (documented, as built) or the server forgives

- status: OPEN — filed at Order 34's GO (2026-09-13); OWNER Q, not queued
- kind: design
- area: Reactive core & UI

Rec: keep the contract as built (one `Unsubscribe` per `Subscribe`; the generated client keeps it by construction) and document it at the wire page; a forgiving server would need per-client-per-channel dedup that A92 deliberately did not add (a mirror held by two demands must not be revoked under one). Pin the raw double-subscribe as the documented behaviour.
