# E171 — the book's highlight.js tag rule (`vilan/docs/theme/vilan.js:123`) carries E164's closing-tag refusal unreachably: `(?<=(?<![A-Za-z0-9_])</?)` declines a closing tag after an identifier character; no doc fence reaches it today (bare text children are a parse error), so it is latent — the one-regex fix is `(?<=</|(?<![A-Za-z0-9_])<)`

- status: OPEN — Order 34's finds (2026-09-13)
- kind: editor
- area: LSP & tooling

Apply the regex; a book-side pin if the theme has any (else a note in the file).
