# N106 — `f64` PRINTING is a latent differential hazard the JS side never pins: node prints `Infinity`/`0` where Rust prints `inf`/`-0`, and switches to exponential outside `1e-6..1e21`; `vilan_rt::js_number` documents and implements that boundary for the Rust backend, but no corpus program prints `1e21`, `-0.0` or `Infinity`, so the JS emitter's own behaviour at the boundary is unrecorded; and `str::len` is UTF-16 on JS while the native backend's `str_len` matches it only for the BMP (surrogate pairs diverge — `vilan_rt::str_len` says so)

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: hygiene
- area: Hygiene & rot

Two corpus programs (numeric boundaries; a non-BMP string's `len`) with goldens on JS; the native differential names them until S1b answers the surrogate case.

- 2026-09-21 (Order 38 sweep): HALF done in lane native-a-38: the ESCAPE half of the class was a live native miscompile, fixed and pinned (`a_string_literals_escapes_mean_the_same_thing_on_both_backends`); the f64-boundary and non-BMP `str::len` corpus programs are not written. Stays OPEN.
