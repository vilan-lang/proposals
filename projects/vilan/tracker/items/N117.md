# N117 — `std::binary`'s READER is UNAUDITED for the kind-mismatch class the JSON reader just closed (2321790e: a typed read that never poisoned on the wrong kind or a short list) — the same audit, the same ten pin shapes, over the binary codec

- status: OPEN — filed 2026-09-21 (Order 39 sweep; std-39's find 4)
- kind: hygiene (audit)
- area: Hygiene & rot

Audit each typed read in binary.vl: short buffer, wrong tag, a list read past its declared count, a string length past the buffer; poison naming both sides as the JSON reader does. Pins planted red per shape; the wire goldens unchanged. Sized S–M.
