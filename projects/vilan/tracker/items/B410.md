# B410 — MISCOMPILE — an OVERRIDE is not selected through a bound when the trait's ARGUMENT is a TUPLE: JS prints 'default' where the impl's override must run; worked around in S2b by each node calling its own `on_settle`. Repro in `sweeps/order41/reactive-41/finds/`; reproduces on 9ffa397e

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (miscompile)
- area: Type system & the type solver

Discussion: reactive-41's defect 3 (2026-09-24); solver, Order 42

- 2026-09-25 (Order 42, solver-42): FIXED (3332632f): `impl_select::instantiation_agrees` compared tuple and array arguments by type ID — element-wise now, both backends; 3 pins. The nodes' per-node `on_settle` retired on it.
