# N72 — flake: `tuples::nested_nurseries_join_inside_out` failed once under full-suite load (got `inner-body/outer-child/inner-child/inner-done`, wanted `inner-body/inner-child/inner-done/outer-child`), passed in isolation and on the final full run — a nursery-join ordering race the pin's expected order does not tolerate

- status: OPEN — Order 32's finds (2026-09-11)
- kind: hygiene
- area: Hygiene & rot

Decide whether the order is promised (then the runtime has a race to fix) or not (then the pin asserts the set and the inner-before-outer-done invariant only). N35's class.
