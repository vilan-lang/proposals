# M40 — M19 tranche 1b: Class D (`LastUse`, the ten `compute_*`, `infer_bumps`, the hover render — 34% of the phase) and the drop planner produce tables the emitter reads, so freezing them is cache-and-restore, not skip

- status: OPEN — lane m19-t1's find (2026-09-04, Order 27)
- kind: performance
- area: Performance & footprint

T1 froze Class A checks and replays diagnostics; the rest of `checks` builds tables (`LastUse`, view origins, drop glue, sink argument types, `check_resource_generic_instantiations` which is instantiation-driven) that must be restored per reused module rather than skipped. Also: R10 populates R11's dedup set (carry it in the replay unit so R10 can freeze); `check_hmr_transfer_bounds` re-infers through `&mut self`. Gate: the corpus-as-modules differential (its 60–170 s cost is the owner's question).
