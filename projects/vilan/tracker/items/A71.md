# A71 — a reactive element child APPENDS on replacement, like `when`/`swap`: `std::dom` has no `insertBefore`, so a `Source<View>` child's position is lost after its first change — a marker node and `insert_before` would keep it; is "put it last" the contract?

- status: OPEN — Order 30's finds (2026-09-09)
- 2026-09-11 (integrator): A85 (positional slots) is the design this brick serves — Order 32's ui-32 builds the anchor + `insert_before` as a reusable `Region` the value forms can take; P5 asks the owner to confirm KEEP POSITION.
- kind: design
- area: Reactive core & UI

`Element::insert_before` + an anchor comment node per reactive child (the row's owner disposes it); `when`/`swap` take the same anchor; pins for a middle child replaced twice.
