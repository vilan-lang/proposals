# B327 — the `[derive(Hashable)]` boundary cannot see a HAND-WRITTEN `impl .. with Hashable`: `is_hashable_type` reads scalars + `hashable_names` (derives and backed enums) only, so `[derive(Hashable)] struct Key { inner: Custom }` with `impl Custom with Hashable` is refused 'field `inner` … is `Custom`, which is not `Hashable`' — B289's class for Wire; it only rejects valid programs

- status: OPEN — Order 34's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver

`check_hashable_boundary` asks the impl table through `resolved_type_is_hashable`; pin the hand-written impl inside a derive and the missing-impl control.
