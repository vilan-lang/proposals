# B330 — two blanket binders bounded DIFFERENTLY (`impl Source<type I: Debug>` vs `impl Source<type J: Clone>`) declaring one member name are still admitted: whether a third type satisfies both bounds is not answerable from the declarations, so B315's duplicate family refuses only what it can see; a type implementing both traits then has two candidates

- status: OPEN — Order 34's finds (2026-09-13)
- RULED 2026-09-13 (owner, Order 35 GO, R5): DOCUMENT the admission and pin it now (lane solver-35); the call-site refusal is B318 S4's (Order 36).
- kind: bug
- area: Type system & the type solver

Either refuse at the CALL when a receiver satisfies both (the per-importer namespace of B318 is where such a check would live) or document the admission; pin the pair and a receiver that satisfies both.
