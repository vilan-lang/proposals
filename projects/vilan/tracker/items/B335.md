# B335 — `source_of` on an import's MODULE segment lies when the module owns both a file and a directory: `pkg::x` with `x.vl` beside `x/y.vl` resolves its segment to the directory NAMESPACE, an entity minted in the IMPORTING file's id range, so `program.source_of(segment_id)` answers the importer, not `x.vl`; E168/E169's `import_module_is_used` makes the same lookup, so Organize Imports is probably wrong for such a module

- status: OPEN — Order 35's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver
- 2026-09-14 (Order 36 GO): BUILD in lane visibility-36 beside B318 S4.

Resolve the segment to the module's BODY entity (or record the file on the namespace entity); pin `import pkg::x;` with `x.vl` + `x/y.vl` through Organize Imports and `source_of`.
