# B417 — a METHOD named `Self` is accepted at its declaration but cannot be called (`x.Self()` parses `Self` as the type); refuse the name at the declaration with a steer

- status: OPEN — filed 2026-09-25 (Order 42, papers-42's probe)
- kind: bug
- area: Type system & the type solver

One parser rule + ledger row. Sizing XS.
