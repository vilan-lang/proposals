# B342 — A67's rule cannot see a collision NOBODY walks into: `import pkg::a;` alone, with `a.vl` declaring `b` beside `a/b.vl`, reports nothing — a child's scope exists only for a child something loaded (the doc comment says so), while the section heading above the pins claims the collision is reported 'whether or not anything imports the path' (true only of paths that walk THROUGH the child). Closing it means listing each loaded module's directory (a `read_dir` per module per analysis) — a perf decision

- status: OPEN — filed 2026-09-14
- kind: bug
- area: Type system & the type solver

Either list the directory once per loaded module (measure the cost on kolt: 24 modules, lucide's directory excluded by the generated root) or correct the heading to the rule as it is; pin whichever.

- 2026-09-17 (Order 37 GO): MEASURE first in lane dx-37 (list the directory under 1 ms per analysis on kolt, else correct the heading).
