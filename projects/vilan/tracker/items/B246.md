# B246 — `fun bump<P: Add>(a: P) { a + 1 }` (a concrete right operand against a bounded parameter) still compiles and is UNSOUND for a struct whose `Add` declares `B = Self`; pinned as the boundary — close it, at what census cost?

- status: OPEN — lane rigid-28's find (2026-09-05, Order 28)
- kind: design
- area: Type system & the type solver

B233 refused two rigid parameters meeting on an operator; a rigid parameter against a CONCRETE right operand is the remaining hole, and the refusal would say `<P: Add<i32>>` is what the author meant. Census the estate for bounded-parameter operators with concrete right operands before deciding; the sugar's refusal currently renders implicit binders as trait names (B218's face in new text — B184's `C<A>` display should reach the operator steer).
