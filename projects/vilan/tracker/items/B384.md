# B384 — B365's repaired arm (`struct_initializer_to_def`) is UNREACHABLE from a clean `analyze_source` — seven shapes tried, `literal_evidence()` = 0 — the type check refuses first; it fires only on the WARM path (E-family analysis reuse), so its counter is wired but its pin is vacuous from cold

- status: OPEN — filed 2026-09-21 (Order 39 sweep; solver-39's find 2)
- kind: note (analysis reuse)
- area: Type system & the type solver

Either the arm is dead (the cold refusal always wins → delete it and the counter, with a pin that the refusal fires) or it is live only under retained-program re-analysis (→ a warm-path pin in the LSP test harness that drives an edit through the retained program and asserts the counter moved). Rec: write the warm-path pin first; if it cannot be made to fire in three shapes, delete the arm. Sized S.
