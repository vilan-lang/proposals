# B391 — a SPURIOUS refusal on an exported service impl: a `[service]` with `client = Peer` written as `export impl Door`, beside a plain `impl Peer` holding an `[rpc] fun ping`, reports "'ping' is provided by an `impl` in module `main` that `main` does not export" — and the error goes away when `export` is removed from `impl Door`. Pre-existing on 49de39157

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: bug
- area: Type system & the type solver

Repro in std-40's report; pin red-first; the fix is in the exported-impl admission of the peer's members, not in `[service]`. Sizing S.
