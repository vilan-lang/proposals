# E102 — the module-parse-error dedup dropped the MESSAGE from its key

- status: OPEN — audit run 4 finding 8
- kind: bug
- area: Diagnostics
- source: audit run 4, 2026-08-29

E100's fix dedups module parse errors on `(PathBuf, Span)` where the code it
replaced deduped on the full rendered message. Two DISTINCT lex/parse errors
at the same offset in one module now collapse to one, and the doc comment
justifies adding the path without addressing dropping the reason. Cheap fix:
key `(PathBuf, Span, message)`; pin the two-distinct-errors-at-one-offset
case red-first.
