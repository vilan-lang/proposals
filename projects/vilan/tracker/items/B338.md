# B338 — an impl SELECTOR that admits NO implementation is silent (`import a::{ (impl Nothing) }` — almost certainly a typo); a bad selector only surfaces as the admission refusal at the first call; a second row would say so at the import

- status: OPEN — Order 35's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver
- 2026-09-14 (Order 36 GO): BUILD in lane visibility-36 beside B318 S4.
- 2026-09-14 (Order 36, visibility-36): LANDED — two rows at the selector (subject reaches nothing; `::` tail names no member) with the `(impl _)` control; two pins. CLOSE at the sweep.

One row at the import when the selector's subject unifies with no impl in the module; pin it and the `(impl _)` control.
