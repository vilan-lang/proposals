# F26 — the monomorphisation RESOLUTION half now exists twice: `resolve_type_id`, `type_key`, `collect_type_generics`, `inherited_substitution`, `call_substitution`, `trait_parameter_substitution`, `resolve_inherited_default`, `trait_default_member` are `&self`-over-`Program` in transformer.rs and re-written in vilan-rust, held together only by the differential; and `analyzer::RENDER_MEMBER` + `transformer::unescape_string` are `pub(crate)`, so the backend crate carries copies

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Native targets & rendering

Lift the eight into a shared vilan-core module both emitters call; widen the two to `pub` and delete the copies; corpus + the whole-set differential byte-identical is the gate. Also: rename `vilan-embedded-std` → `vilan-embedded` (it embeds std, macro_std and vilan-rt).

- 2026-09-22 (Order 40 sweep): PARTIAL (native-b-40 e03d7e98): `vilan_core::mono` shares `collect_type_generics`, `signature_generics`, `function_has_body`, `trait_default_member`, `resolve_inherited_default`, `trait_parameter_substitution` (the last two take impl ADMISSION as a parameter — the inherited half-work had passed `None` on BOTH backends, silently changing JS; JS passes its file again, native `None`; unpinned → N124); `RENDER_MEMBER` + `unescape_string` now `pub`, the backend's copies deleted; 384/384 emits byte-identical before/after on both backends and a planted drift IS detected. Left per-emitter with reasons in the module header: `type_key`, the active-substitution resolvers, `inherited_substitution`, `call_substitution` (they DIFFER — B393 probes whether JS is wrong). The `vilan-embedded-std` → `vilan-embedded` rename NOT done → N125. Stays open on those.
