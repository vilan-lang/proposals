# F26 — the monomorphisation RESOLUTION half now exists twice: `resolve_type_id`, `type_key`, `collect_type_generics`, `inherited_substitution`, `call_substitution`, `trait_parameter_substitution`, `resolve_inherited_default`, `trait_default_member` are `&self`-over-`Program` in transformer.rs and re-written in vilan-rust, held together only by the differential; and `analyzer::RENDER_MEMBER` + `transformer::unescape_string` are `pub(crate)`, so the backend crate carries copies

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Native targets & rendering

Lift the eight into a shared vilan-core module both emitters call; widen the two to `pub` and delete the copies; corpus + the whole-set differential byte-identical is the gate. Also: rename `vilan-embedded-std` → `vilan-embedded` (it embeds std, macro_std and vilan-rt).
