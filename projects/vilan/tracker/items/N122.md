# N122 — the shared DOM test stub's `focus()` accepted ANY element and dispatched nothing (ui-40 fixed it: the focusable-area rule, the `visibility` rule, a `focusin` with `relatedTarget`) — B271's own suite still carries a PRIVATE focus override that can now retire onto the shared stub; and `crates/vilan-cli/tests/support/dom/stub.js` is shared by eight suites, so its two new fidelity rules are a behaviour change every focus pin should know

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: hygiene (tests)
- area: Hygiene & rot

Retire the override; one pin that the stub refuses a `focus()` the platform would refuse. Sizing S.
