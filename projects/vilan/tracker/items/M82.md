# M82 — `reconcile`'s key-matching scan is O(N²) per change, so EVERY `each` over a `List`-valued source pays a quadratic pass whatever changed: 95.1 M / 358.5 M / 1,369.9 M Ir at 500 / 1,000 / 2,000 rows (3.77× and 3.82× per doubling; callgrind, `node --jitless`) against 22,347 Ir on the delta path — A98 fixed the DOM half of the pass and left the scan

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: performance
- area: Performance & footprint

Either A112 S3 deletes it (the delta-driven `each`) or a position index inside `reconcile` closes it for non-delta sources (a plain `SignalCell<List<T>>` keeps the scan) — rec both: the index now (S), S3 on A112's schedule.
