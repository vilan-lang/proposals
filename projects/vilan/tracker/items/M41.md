# M41 — `type_id_sources` adds ~4 bytes per `TypeId` to every stored world, invisible to `base_cache_world_bytes` (text-proportional): M11/M24's tally under-reports

- status: OPEN — lane m19-t1's find (2026-09-04, Order 27)
- kind: performance
- area: Performance & footprint

T0's dirty-bit census stamps each TypeId's minting source. Account for it in the world-bytes estimate (or measure the real allocation) so M24's LRU budget stays honest; pin that the tally moves when a world gains types.
