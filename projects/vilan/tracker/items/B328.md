# B328 — `transformer::expr_type_id` is BLIND to a parameter spelled `Expr::Local`: `self.program.variables.get(binding)` misses it (the gap `drop_argument_type_id` documents); harmless after B310 (the `tuple_element_types` fallback answers) but latent for any other consumer

- status: OPEN — Order 34's finds (2026-09-13)
- 2026-09-13 (Order 35 GO): BUILD, lane solver-35.
- kind: bug
- area: Type system & the type solver

Resolve a parameter's type through the function's parameter table in `expr_type_id`; pin a consumer that reads a parameter's type without the fallback.
