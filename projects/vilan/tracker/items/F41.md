# F41 — native — a FIELD named `self` or `super` is emitted as `r#self`/`r#super`, which rustc REFUSES (raw identifiers cannot spell those two) — live today; the emitter must mangle instead of raw-quoting

- status: OPEN — filed 2026-09-25 (Order 42, papers-42's probe)
- kind: bug (native)
- area: Native targets & rendering

Pin: a struct with a `self` field builds and runs natively byte-identical with JS; the mangling rule written where the emitter names fields. Sizing S; native lane.
