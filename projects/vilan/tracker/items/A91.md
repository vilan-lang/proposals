# A91 — a RENDER CLOSURE may yield a `Slot`: today `swap`'s and `bind_each`'s render closures (and `when`'s body) must return a `View`, so the two kolt wrappers A85's value forms do not remove (views.vl:127 a swap render's result, views.vl:204 a bind_each row) exist only because a `List<View>`, a `str` or a positional value cannot be what a row or a page IS — the closure type becomes `|T| C` with `C: Slot` (a fragment, a value form, a text node), the region owning whatever the child placed

- status: OPEN — A85's Q3 (2026-09-11); after slots-33
- 2026-09-11: inherits B309's wall (a value form holding a render closure needs a context clause on a field); after B309.
- kind: feature
- area: Reactive core & UI

Generalise the render parameter from `View` to `C: Slot` on `swap`/`swap_split`, `bind_each` ×3, `when`'s body and the value forms; the row's `Region` (A71) already owns a run, so a `List<View>` row is a run placed before the row's anchor and moved as a unit by the reconciler's order pass; the process twin renders whatever the Slot renders. Costs: the reconciler's per-row `element.remove()`/move becomes per-region; `bind_each`'s `T: PartialEq` unchanged; the split gate's `swap_split` keeps its render type. Pins: a fragment row, a text row, a value-form row, reorder/remove of fragment rows; `ssr_differential`.
