# B267 — B256's third option: rule 2 (last-use elision) is LOCAL-only and cannot see a `Shared` slot, yet both of std's hot per-notify reads (`bind_each`'s `row_views.read()`/`row_owners.read()`, `drain`'s `turn[0].v`) are reads whose source dies or is only walked — a CELL-AWARE last-use would make the B256 ruling nearly free (+12–25% CPU on the reactive pins today, +344% on a raw `get()`)

- status: OPEN — lane rule1-29's find (2026-09-06, Order 29); the B256 unblocker
- RULED 2026-09-07 (the owner): recommendation accepted — B267 first, then re-measure and land B256. QUEUED for Order 30.
- kind: design
- area: Memory model (rule 1 / rule 2)

Extend liveness to a `Shared` slot read whose cell is not written again before the binding's last use (the cell's own writes are the aliasing hazard; a `write()` or `set`/`update` in between refuses the elision), then re-run rule1-29's three ignored B256 pins and its CPU table; land B256 if the reactive pins come back under ~5%.
