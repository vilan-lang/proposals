# M52 — M44 is INERT wherever members' std seed sets differ: kolt's three legs have three distinct seed sets and so three base-cache keys, so its residual multi-entry cost is not duplicated worlds — a coarser key (the union of the package's seeds) or M19's module-level sharing is the follow-up that reaches kolt

- status: OPEN — lane perf-29's find (2026-09-06, Order 29)
- kind: performance
- area: Performance & footprint

Measure kolt's three legs' seed-set overlap; decide between a package-union key (one world, superset) and module-level sharing; pin the second leg's `base` phase at ~0 on kolt's shape.
