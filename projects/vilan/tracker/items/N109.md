# N109 — `List::new` / `List::push` are the only compiler-lowered externals OUTSIDE the `intrinsics` map (two `Program` fields keyed by function id) — which is why B359's emit hole existed and why solver-38 added `Dispatch::ListNew`/`ListPush`

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: hygiene
- area: Hygiene & rot

Make them `Intrinsic` rows; delete the two fields and the two `Dispatch` variants; zero output change (corpus + whole-set native differential byte-identical).
