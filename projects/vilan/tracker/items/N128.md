# N128 — N121's remainder — (a) SYNTACTIC visitor walks are unprobed: `collect_module_paths::walk` over `Node::for_each_child` recurses once per method-chain link (a 450-link chain needs > 2 MiB in debug before any probe is reached) — a probe in `for_each_child` (node.rs; gates `node_size`) is the fifth site; (b) UNDECLARED threads: the CLI's three compiler threads (`COMPILER_STACK_SIZE`) and the vilan-wasm entry declare no stack, so their probes are inert — one `with_declared_stack` line each turns a SIGABRT into a panic naming the walk; (c) the 11.3 KiB optimized walk-frame figure in `COMPILER_STACK_SIZE`'s comment is STALE (release measures ~2.4 KiB per level; 1.05 MiB at 453 levels)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: hygiene-41's report)
- kind: hygiene (compiler)
- area: Hygiene & rot

Discussion: hygiene-41's finds 2–4 (2026-09-24)
