# B419 — a BLANKET over a supertrait is not found on a type that implements the supertrait only through a ONE-BLOCK subtrait impl (`impl Cell with Sig<T>` — B243's form): under the flip `map` is a blanket over `Src`, and `traits::b243_a_one_block_signal_impl_reaches_source_map_and_effect_on_change` stays red ('Cell<i32> has no method map') even with B408 applied — pre-existing on d65d4e75

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's interim find S1)
- kind: bug (inference)
- area: Type system & the type solver

Fix: blanket-method lookup walks the supertraits an impl block provides through its subtrait (the one-block form registers `Src` as provided — B243's rule — but the blanket candidate set does not consult it). Pin: the b243 pin under the flip. Sizing S–M; solver-42's tail — if unlanded by reactive-42's merge, that pin is re-derived to the defaults and the blanket case waits for Order 43.

- 2026-09-25 (Order 42, solver-42): ANALYSED, not built: needs the subtrait closure in `type_implements_trait`, `trait_args_for`, `type_implements_trait_at` and impl_select's `provided_trait_arguments`. Order 43; reactive-42 parked its b243 pin under #[ignore = B419].
