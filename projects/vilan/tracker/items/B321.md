# B321 — `export (helper);` and `export * helper;` compile CLEAN and mean nothing: `parse_export` takes any statement, so an export of a parenthesised expression (or of `*` followed by a name) is accepted silently; zero occurrences in the estate; one curated refusal, shippable today, and the rule B318's `export *;`/`export(in PATH)` grammar needs anyway

- status: OPEN — Order 34's finds (2026-09-13)
- 2026-09-13 (Order 35 GO): BUILD, lane visibility-a-35 (S1's `export <expression>;` refusal).
- kind: bug
- area: Type system & the type solver

`parse_export` admits a declaration, an `import`/`use`, and (after B318) `*;` / `(in PATH)` — anything else is refused with a steer naming those; one row; pins for the two nonsense forms and the legal controls.
