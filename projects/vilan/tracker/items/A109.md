# A109 — `Result::expect_err` is NOT retrofitted to `lazy` while `Result::expect`, `Result::unwrap_or`, `Option::expect` (new) and `Option::unwrap_or` are (A103, Order 37): the same position with the same argument shape, now asymmetric — a trap; the differential shows zero estate call sites, so it is free

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: feature
- area: Reactive core & UI

`fun expect_err(self, lazy message: str)` beside `expect`; a pin that the message is built only on the `Ok` path; the docs' surface listing.
