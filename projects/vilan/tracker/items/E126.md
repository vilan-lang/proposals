# E126 — the E121 diagnostics gate's generated exhibit does not exercise the diagnostics path: 104 call substitutions vs kolt's 18,540, 19× less warm CPU

- status: OPEN — paper lane m19-paper's find (2026-09-03); a prerequisite of M19 tranche 1's gate (per-module-analysis-reuse.md Q3)
- kind: editor
- area: Testing & CI

The keystroke gate's exhibit (1,791 functions of `i32` arithmetic)
met the 10 ms keystroke budget honestly — tokens/hints/completion
never type-check — but `diagnostics_budget` (`#[ignore]`d at ~925
ms) would PASS at 50 ms on it today, on a program that never had the
problem. Real lucide icons return `View` and chain `.child(…)`: 18,540
call substitutions, 97,070 entities. Build a VIEW-SHAPED generator
(functions returning a view type through a chained builder, a
generic-heavy std surface, one imported module the size of lucide)
whose warm-keystroke CPU tracks kolt's to within 2×; the diagnostics
gate runs on it. kolt itself never enters the tree (the owner's rule).
