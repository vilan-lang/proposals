# B302 — `render_type` renders a PATH annotation as `_`: `[rpc] fun note(self, id: i32): models::Note` reported "return type of `[rpc]` method `note` is `_`, which is not Wire" on 65af4be0 (B289 makes that program compile — the path type is now read for what it is — but the `_` rendering presumably still reaches every other message that renders a written path type)

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Type system & the type solver

`render_type` gains the path arm (`a::b::C<..>` rendered as written); sweep its callers for a message that shows `_` for a path; pin one.
