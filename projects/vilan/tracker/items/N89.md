# N89 — `Node::Const` peels in only 2 of ~24 item-shape sites (`item_visibility`, `unwrap_item`); the other `Node::Export(..)` match sites (formatter, macros.rs, lib.rs, css.rs, elements.rs, lift.rs, parsing.rs) do not peel `Const`, so `export const fun f()` parses and resolves but is unaudited across the export-marker consumers

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

Audit every export-marker consumer for a `Const` wrapper, or refuse `export const` with a row; pin `export const let`/`export const fun` through import, format and docs.

- 2026-09-17 (Order 37 GO): BUILD in lane hygiene-37 (rec: peel everywhere).
