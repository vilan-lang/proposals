# N57 — M19 T1's corpus-as-modules differential runs inside vilan-core's inference binary and adds 60–170 s to every `-p vilan-core` run; give it its own binary in the slow group

- status: OPEN — orchestrator's ruling on the owner's open question (2026-09-05)
- kind: hygiene
- area: Testing & CI

`corpus_agrees_between_replayed_and_rederived_module_checks` re-hosts every corpus program as a package module and analyzes it warm both ways — the right standing gate for diagnostics replay, and the wrong place for it: targeted `-p vilan-core` runs and every lane's whole-crate gate pay it. Move it to its own test binary (`replay_differential`), declare it in the nextest slow/wall-clock group beside the other differentials, run it in the union and in CI's test legs. Pin: the coverage assertion both ways stays.
