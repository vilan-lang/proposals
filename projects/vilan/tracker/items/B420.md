# B420 — an abstract `S: Src<i32>` does not SATISFY a bound `Up<i32>` when `Up` is provided by a blanket IMPL over `Src` — the trait-satisfaction twin of B408 (B408 fixed method LOOKUP through the bound; satisfaction of a blanket-provided trait at an abstract parameter still fails): keeps the four b371 pins red after B408 ("'Map<S, T, I>' does not implement trait 'Source<I>'")

- status: OPEN — filed 2026-09-25 (Order 42, reactive-42's interim find S2)
- kind: bug (inference)
- area: Type system & the type solver

Fix: `satisfies_trait_bound` at an abstract parameter consults blanket impls whose bound the parameter's own bounds entail (the same entailment B408 added to lookup), with B394's in-progress guard. Pins: the repro; the b371 four under the flip. Sizing S–M; solver.
