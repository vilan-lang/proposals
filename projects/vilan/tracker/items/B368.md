# B368 — a TUPLE PATTERN in a `for` header does not parse, and the refusal is unreadable: `for (i, item) in list.iter().enumerate() { .. }` → `found 'for' expected a statement or '}'` anchored on the `for` keyword — no mention of the pattern; kolt writes a manual counter around it (lib/search.vl:26) and destructures inside the body elsewhere (`let (index, message) = pair;`)

- status: OPEN — filed 2026-09-21 (the kolt sweep before Order 39)
- kind: bug
- area: Type system & the type solver

Admit a PATTERN where the `for` header takes a binder (tuple first; a struct pattern if the grammar's `let` already takes one — read `parse_let`'s pattern production and share it); EBNF + grammar_sync + formatter; the refusal for a still-unsupported pattern names the pattern and says 'bind the element and destructure in the body'. Native backend: the Rust emitter's `for` arm takes the same pattern (F20 owns the `Iterator` `for`).

- 2026-09-21 (Order 39 GO): R5 RULED — the tuple pattern (and `let`'s) admitted in a `for` header, others refused by name; lane solver-39.
