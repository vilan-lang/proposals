# B409 — the B395 CLASS again — a SAME-TRAIT bound is checked at the WRONG arguments: a default inherited through `impl M<type S: Src<type X>, X, type U> with Src<U>` is checked at `Src<U>` instead of the upstream's `Src<X>`; worked around in S2b with a private `Upstream` blanket. Repro in `sweeps/order41/reactive-41/finds/`; reproduces on 9ffa397e (after B395's fix)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: reactive-41's defect 2 (2026-09-24); solver, Order 42 with B408

- 2026-09-25 (Order 42, solver-42): FIXED (49b1c930): a `type X` in a trait's HEAD was registered as the trait's own parameter id, so `with Src<U>` rebound it — a trait head now passes on its parameter's BOUND, not its identity; two more found on the way (a trait default's JS instance dropped the enclosing instance's bindings; emission left a blanket provider's bound binders ungrounded) fixed; native: `bind_subject_and_bounds` called at one place in vilan-rust's `dispatch_to_member`. reactive.vl's `Upstream` retired on it.
