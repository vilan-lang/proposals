# E123 — `did_open` analyzes INLINE on the async handler, blocking a tokio worker for the whole analysis

- status: OPEN — editor-latency paper's find (2026-09-01)
- kind: bug
- area: LSP & tooling
- source: `crates/vilan-lsp/src/main.rs:2131`

Opening a large file runs its first analysis on the request thread
(1.1 s on views.vl), stalling every other request on that worker.
Route it through the same analysis scheduling `did_change` uses
(spawned, stamped, cancellable). Pin with the harness: a request
issued during a fresh open answers before the open's analysis lands.
