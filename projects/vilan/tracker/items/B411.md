# B411 — a default's CLOSURE PARAMETER is left UNTYPED when the impl reads its value type from a NESTED binder (`impl Map<type S: Src<type T>, T, type U>`); worked around in S2b with phantom type parameters (which is why the nodes are `Map<S, T, U>` / `Switch<S, T, I, U>`, not the paper's `Map<S, U>`). Repro in `sweeps/order41/reactive-41/finds/`

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: reactive-41's defect 4 (2026-09-24); solver, Order 42

- 2026-09-25 (Order 42, solver-42): FIXED (43b6d46e): inherited-default bindings ground a nested binder's bound binders; `mono::trait_parameter_substitution` likewise (native needed it for the node shape); 3 pins. The phantom parameters can retire for a node that reads its value type from the source it selects (`Switch`-shaped); `Map` keeps `T` (its `transform` field names it) — reactive's call, Order 43.
