# A8 — UI styling — the tail

- status: OPEN (blocked: A7/G2 — liveness-tied emission)
- kind: design
- area: Reactive core & UI (`std::reactive`, `std::ui`)
- source: entangled pieces only

_Original title parenthetical: (entangled pieces only)_

   STATUS: OPEN (blocked: A7/G2 — liveness-tied emission)
   The 2026-08-04 slices closed everything unentangled. Remainder:
   critical CSS and liveness-tied dead-style elimination, riding A7/G2's
   liveness-tied emission — nothing else. Adjacent open find: A22
   (same-family rule ordering). History: backlog-2026-07-18.md §A item 8.

- 2026-09-17 (the kolt sweep at Order 37 GO): kolt styles.vl:48 declares three raw rules the builder cannot spell — `scrollbar-color` (no method) and `cursor: … !important` under `[data-dragging]` (`!important` is refused in the builder, so `raw` carries it) — candidates for the tail.

- 2026-09-21 (the kolt sweep before Order 39): STILL OWED — kolt styles.vl:46 `FIXME: Implement with std (A8)`: three `style::declare(..)` rules by hand (`[data-dragging] *` cursors with `!important`, `* { scrollbar-color }`).
