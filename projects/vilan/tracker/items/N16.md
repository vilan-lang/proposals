# N16 — NEW — the recurring codebase audit

- status: RECURRING — one audit lane every other work order. Run 3
- kind: process
- area: Hygiene & rot — NEW SECTION
- source: RECURRING; the owner's standing ask, filed 2026-08-25

_Original title parenthetical: (RECURRING; the owner's standing ask, filed 2026-08-25)_

    STATUS: RECURRING — one audit lane every other work order. Run 3
    (Order 18, 2026-08-28) took Debugging + Tests and EXHAUSTED the
    rotation — all seven dimensions covered across runs 1–3, so run 4
    (due Order 20) is the first DELTA run: re-take the earliest
    dimensions over what changed since their run. Run 3 filed
    M11–M13, N27/N28, L18, E99, B149; grades Debugging C+, Tests B−.
    focus. Track here: last run Order 11 2026-08-26 (security +
    diagnostics; L13's re-key carried); **RUN 2 CLOSED in Order 16
    (2026-08-27), three orders late** — 13 findings filed (A26, B144, B145,
    E97, N19–N25) plus one S-sized tidy landed inline with its own gate;
    grades: error handling B+, dead code A, tidy & structure B−, stale-path
    class B. Its sharpest structural find: the documents agents read first
    (`AGENTS.md`, `.claude/**`) are the only documents in the tree with no
    gate, which is why a crate went five days missing from the repo map and
    why three agent definitions pointed at a path that did not exist. It was — the line said "next due Order 13"
    and Orders 13, 14 and 15 all shipped without an audit lane, which is
    the failure this line exists to make visible and did not. Run 2's
    dimensions: error handling, dead code, tidy & structure. Next due
    **Order 18** (update this line each run — and if it goes stale again,
    the fix is a gate, not a bigger note).
    A standing audit over all four repos (vilan primary; website,
    proposals, pages secondary), SURVEY-FIRST like the 2026-08-18 rot
    survey: findings are FILED as tracker items with evidence, never fixed
    inline — except S-sized tidies the lane can land with pins. Each run
    picks 2–3 dimensions from the rotation and states which; the report
    grades what it swept so coverage is honest across runs. The rotation:
    - **Security**: `cargo audit`/dependency advisories; the `unsafe`
      inventory (each block's SAFETY comment against reality — the
      BASE_CACHE transmute class); injection/escaping surfaces (the
      website's served HTML, the playground share codec, the HMR
      endpoints); secrets/tokens in tracked files; the release pipeline's
      supply chain (pinned actions, checksum gates).
    - **Tidy & structure**: directory/module layout drift, files that
      outgrew their name, orphaned assets/fixtures, stale scratch or
      generated files tracked by mistake.
    - **Dead code**: unreferenced items the compiler cannot see (feature-
      gated, test-only helpers gone stale, cfg'd-out paths), dead exports,
      the `#[cfg_attr(not(test), allow(dead_code))]` inventory re-argued.
    - **Error handling**: `unwrap`/`expect`/`panic!` in non-test code
      audited against the panic-fence and Result discipline; swallowed
      errors; error messages that lost their context.
    - **Diagnostics**: a ledger sweep (with L13's mechanical re-key),
      heads that drifted from house rules, missing C3 notes/steers on
      newer refusals.
    - **Debugging**: VILAN_PHASE_TIMING/debug-dump coverage of newer
      passes; leak_tally site coverage; whether the perf baseline still
      reproduces.
    - **Tests**: vacuous pins (plant-audit a sample), #[ignore]d pins
      whose reason expired, per-case gaps on newer features, goldens
      whose regeneration story rotted, suite wall-time creep.
    Each run ends by updating this item's last-run/next-due line and
    filing its findings; a dimension with zero findings says so
    explicitly (absence of evidence recorded, not implied).

**Run 4 — 2026-08-29 (Order 20, wave 2): the first DELTA run, and the method
holds.** Scope was v0.38.0..wave-1 (49 commits, both orders' changes read as
diffs against the sentences that shipped with them). Twelve findings: two
order-material bugs (G13's unswept bundled assets; B154's NativeMap
use-after-free, surfaced by b153's sweep and living only in an ignore
string), two fix-now (the unpinned LSP format gate; the untracked-pin
process gap, now N31), and eight recordable — six of them one species,
**"the shipped sentence"**: prose in the CHANGELOG, a std doc comment or a
fuel-price rationale the code beside it does not support (the
bundle/bundle_as identity, the readdir no-stat claim, the nine-vs-eight
retains census, the shortest-chain comment, the unit-less index_of docs).
The sentence-sized six plus the LSP pin were fixed in the same cycle
(vilan efb7e67c); the rest filed as B154, G12, G13, N29, N30, N31, E102.
Seven dimensions swept with zero findings, recorded in the run report
(notably: every new e2e suite survived a vacuous-negative audit, and all
three cross-lane seams — estate↔watch re-hash, retains↔widened predicate,
generated-root↔watcher — held). NOT covered: the transformer's S3 lowering
and the 51 reordered pins (the largest behavioural surface in the delta),
A29's SCC gate (a measurement instrument, refused beside a compiling
sibling), runtime verification generally.

**Run 5 charter (next due Order 21):** delta again, weighted to the
transformer half run 4 could not reach (S3's lowering, the moved goldens —
a golden-diff audit is work an audit lane can do that a feature lane will
not); adopt **"the shipped sentence"** as a named standing angle rather
than a species of drift; and the two house-rule gates it kept refinding go
as items, not angles (N31 filed; the LSP-pin rule is CLAUDE.md's, its gap
closed this run).

**Run 5 — 2026-08-29 (Order 21, wave 2): the transformer-weighted delta,
and the emitted program is where the bodies were.** Eleven findings, the
sharpest three all found by RUNNING the artifact beside its claim: F1 a
RELEASED miscompile (S3's widening dead in every nested scope — the
keying mismatch between the extents map and the widener; fixed same-day
as B159, five shape pins red-first), F2 b154's stand-down swallowing an
independent second offense (fixed same-day as E104), F3 the symlinked
directory input's forever-stale hook (G15). Also: G16 (watcher-stamp
empty-nested-dir asymmetry), M14 (the unpriced extern-to-vilan cost
cliff), E105 (unterminated css completion), N33 (the ignore-gate's loose
predicate + fixture self-parse), and four sentence-sized (fixed in-order
@167d4567) — including run 4's own bundle-identity finding SURVIVING IN A
SECOND COPY four lines from where the fix commit was editing: when a
sentence is corrected, census the sentence. The golden-diff centrepiece:
four of five moved goldens verified pure reorderings; the fifth
(resource_exit) is the drop-at-declaration shape whose header had rotted
— fixed, with the pending-finally-at-exit pin gap recorded. The
integrator's own hand-merges audited clean (line-multiset, zero invented
lines). Twenty dimensions swept to zero findings, recorded. Count
reconcile: the chronicle's "51 pins moved" vs c8609287's 38 moved + 15
new = 53 — corrected in Order 21's chronicle entry. NOT covered: the 38
moved pins one at a time, the interpreter/node differential for the new
to_lowercase, A29's SCC gate (instrument), the secondary repos.

**Run 6 charter (next due Order 22):** delta weighted to the EMITTED
PROGRAM — (1) the lowering differential: the S3/C11 scope-shape matrix
(function body, if arm, match arm, loop body, closure, tail-position,
own-param split, nested try) against the two laws (no name read out of
scope; every path releases exactly once) — F1 proves the matrix has
holes; (2) std's emitted cost as a standing angle (a function that stops
being an extern changes its price, M14); (3) the shipped sentence
carries, amended: a corrected sentence gets a census; (4) the symlink
axis across the build's path handling (F3/F4 are two faces of one
unswept dimension); (5) diagnostics, unswept since run 2, now carrying
three orders of new refusals.
