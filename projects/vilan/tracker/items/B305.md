# B305 — a NON-EMPTY list literal whose element type is `Unknown` types as an ERASED `List` (no arguments) that reconciles with anything: the `Expr::List` arm's `if matches!(element_type, Type::Unknown) { Vec::new() }` — the EMPTY-literal path deliberately mints a slot instead (B16: a zero-argument `List` made every method call on it vacuous), the non-empty path still erases; B288 closed its struct-literal door by deferring upstream, but the erasure is still manufactured and any other consumer of an unfilled closure parameter's literal reaches it

- status: OPEN — Order 32's finds (2026-09-11)
- kind: bug
- area: Type system & the type solver

Mint a slot for the non-empty path exactly as the empty path does (one arm), then sweep for the consumers that relied on the erasure (the infer differential and the corpus are the net). Pin: a `[x]` over an unfilled `x` never types as a bare `List`.
