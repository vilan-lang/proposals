# B275 — `satisfies_trait_bound` drops trait ARGUMENTS on a SUPERTRAIT match (v1, documented in-source): a program with only `Source<str> with Place` still ADMITS `slot(signal_of_panel)` at the analyzer, and only the emission filter's never-empty fallback keeps it from an internal-error report — closing the gap turns it into a real bound refusal at the call site

- status: OPEN — Order 30's finds (2026-09-09)
- kind: bug
- area: Type system & the type solver

Thread the bound's arguments through the supertrait walk in `satisfies_trait_bound` (the `instantiation_agrees` machinery B268 used), pin the refusal, keep the fallback as an internal guard only.
