# A103 — `lazy` S3, the std RETROFIT (lazy.md §6b): `Option::expect(self, lazy message: str)` and `unwrap_or(self, lazy fallback: T)` / `Result`'s twins become lazy, GATED on the corpus/std/docs differential (any observable difference is stop-and-decide); the `Option`/`Result` doc page says today that the retrofit has NOT happened

- status: OPEN — filed 2026-09-15, READY
- kind: feature
- area: Reactive core & UI

The sweep with the differential; docs; the page's note retired.

- 2026-09-17 (Order 37 GO): BUILD in lane lazy-37 (S3; the corpus/std/docs differential is the GATE — any observable difference is stop-and-decide).
