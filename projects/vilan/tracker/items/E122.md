# E122 — `semantic_tokens_range` computes the full token stream, then filters by line

- status: OPEN — editor-latency paper's find (2026-09-01); the one item there needing no design decision
- kind: bug
- area: LSP & tooling
- source: `crates/vilan-lsp/src/main.rs:2440`

A viewport request costs a whole-file one. Compute the range directly
(walk only the lines asked for, or slice the cached stream by line
index). Pin: the range request's cost is proportional to the range,
not the file (thread clock).
