

- 2026-09-24 (Order 41, editor-41): R1 BUILT (caaf6620): the spelling is `[platform("browser")];` as the file's FIRST statement (the `;` after `export *;`'s precedent — separates it from a fence on the first function; elsewhere refused by a parser rule); it is the platform the file is ANALYZED under (outranks inference and `default-entry`), everything in the file requires it (a node entry gets the colouring chain: '… requires the `browser` platform its file declares … main → render → when_value'), one promise for the whole file; on an `impl` likewise for its members; a FUNCTION fence and a label on a nominal now take part in choosing the analysis platform (only when the package's colour admits none of them). Editor: a status-bar entry 'analyzed as: <platform> — <kind>' (new `vilan/analysisPlatform` request — R6 had never built the status line), the R6 note names the attribute, a quick fix inserts it. Resolver: `crates/vilan-core/src/platform_color.rs`. OWNER to confirm: the `;` spelling; labels on nominals affect resolution only; item fences decide only where the package's colour admits none. FIND: R3's invariant — a server leg loading a browser-declared module still type-checks it under node (the wrong-twin error beside the chain) — R3's paper (§8, landed) owns it.
