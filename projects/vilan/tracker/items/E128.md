# E128 — hover renders a `= Self` parameter as the TRAIT's name: `fun add(self, b: Add): Add`

- status: OPEN — lane cascade-26's find (2026-09-03); B206's out-of-scope half
- kind: editor
- area: Language server & tooling

`declaration_labels` calls `function_signature_label` with no impl,
so hover on `Add::add` shows `fun add(self, b: Add): Add`. Decide the
rendering of `Self` in a TRAIT declaration (recommend the literal
`Self`: `fun add(self, b: Self): Self` — `B = Self` shows its default)
and route hover through B206's written-name label. Pin hover on
`Add::add`, `PartialEq::eq`, a user trait with a defaulted parameter.
