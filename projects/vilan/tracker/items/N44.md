# N44 — the resources chapter still states the pre-amendment scope-end law; memory.md wants per-sentence adjudication

- status: OPEN — audit7-records lane's find (2026-09-01)
- kind: process
- area: Hygiene & rot
- source: lane audit7-records (found repairing resource.vl's claims — the docs twin of F7/F8)

`vilan/docs/tour/resources.md:10` says resources "are torn down
deterministically when their owner's scope ends" — the law BEFORE
c8609287 moved disposal to last use (Order 19); `spec/memory.md`'s
"Drop timing and order" (~682) IS amended, but ~12 other "scope end"
sentences in that file need per-sentence adjudication (several are
legitimately about R2/`own` framing). The docs gate compiles fences,
not prose — the corpus now has the `// witness:` gate (audit7-records)
for claims; prose claims about timing have no gate at all. Sweep both
files sentence by sentence; a lane's worth, with the reader-facing
chapter first.
