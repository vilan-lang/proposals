# E210 — `DeclineReason::WouldChangeTheCode` names the WRONG construct: the net has no span, so it reports the file's FIRST item — which sent N108's investigation thirty lines off and into the wrong grammar (kolt client.vl:7 was just the first import)

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: tooling
- area: LSP & tooling

Bisect the two normalized token streams for the first divergence and report the source span at that token (the lane did it by hand in ~50 lines of scratch); pin on N108's repro naming the `};`.
