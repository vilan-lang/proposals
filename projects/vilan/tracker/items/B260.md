# B260 — the missing-member diagnostic's HEAD names the trait UNPARAMETERIZED: `does not implement trait 'Counted'`, never `Counted<i32>`, even where the arguments are what the refusal is about (B249 fixed only the suggested declaration)

- status: OPEN — lane smalls-29's find (2026-09-06, Order 29)
- kind: bug
- area: Diagnostics & the ledger

Render the `with` clause's arguments in the head through the same `trait_argument_substitution`; the ledger row's text moves (an edit in place, not a NEW row); pin `Source<i32>` and a two-parameter trait.
