# B430 — MISCOMPILE — a tuple VARIABLE passed whole into a `(dyn A, dyn B)` position is not erased element-wise: `p[0][1].get is not a function` at run time — B398 erases the elements of a tuple LITERAL at a mapped-tuple position; a tuple-typed binding at the same position escapes the erasure

- status: OPEN — filed 2026-09-25 (Order 42, solver-42's find)
- kind: bug (miscompile)
- area: Type system & the type solver

Fix: the element-wise `dyn` coercion applies to any expression of tuple type at a mapped-`dyn` position, not only a literal (re-wrap by projection). Pin red-first; native holds 'refused or identical'. Sizing S; solver.
