# M84 — `Ord::clamp` over an integer costs two `compare` calls since B359 (R1: a default body reaches the TRAIT's `min`/`max`, not the inherent `Math.min`/`Math.max` externs) — semantics identical, JS larger, every integer type

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: performance
- area: Performance & footprint

If the host intrinsics are wanted back: `min`/`max` overrides in each `impl <int> with Ord` (~12 types × 2, number.vl) — the override wins inside a default body by R1's own rule; `number-math.mjs` moves back.
