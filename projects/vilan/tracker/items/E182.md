# E182 — `infer_platform` (vilan-core lib.rs:93) is BINARY browser/not-browser: a third `std::ui` twin makes the editor report the PROCESS twin's `View` against native source — the E113 shape its own doc comment describes; latent until F1's S2 adds the `native` platform

- status: OPEN — filed 2026-09-14
- kind: bug
- area: LSP & tooling

Make `infer_platform` answer the manifest's platform for the entry (the E113 lane's own note); sequence with F1 S2.
