# E219 — fmt — a LONG ATTRIBUTE LINE splits the signature below it: `[deprecated("…110 chars…")] fun one(a: i32, b: i32): i32` reprints one parameter per line because the declaration's budget counts the attribute line that precedes it. Pre-existing (not E213's); std's `[internal]` reasons were kept short to dodge it

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: formatter
- area: LSP & tooling

The attribute line and the signature are separate lines with separate budgets. Pin: the repro reprints unchanged. Sizing S.

- 2026-09-24 (Order 41, editor-41): BUILT (125b8210): a declaration is measured from its OWN line after any attribute lines (nested statements save/restore the mark); SECOND BUG: the same flaw HID over-budget signatures under a short `[extern]` line — four estate spots reformatted (std::fs `raw_read`/`raw_write`, std::rpc `reply_promise`, one canvas + one walkthrough example).
