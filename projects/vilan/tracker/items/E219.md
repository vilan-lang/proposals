

- 2026-09-24 (Order 41, editor-41): BUILT (125b8210): a declaration is measured from its OWN line after any attribute lines (nested statements save/restore the mark); SECOND BUG: the same flaw HID over-budget signatures under a short `[extern]` line — four estate spots reformatted (std::fs `raw_read`/`raw_write`, std::rpc `reply_promise`, one canvas + one walkthrough example).
