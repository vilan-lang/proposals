# B219 — `compare_type_rigid` is `reconcile_type`'s read-only twin and does not consult `rigid_binder_scope`; §1.4's table says they agree exactly, and now they do not

- status: OPEN — lane b211-b212's find (2026-09-03)
- promoted 2026-09-04: B225 is this hole's first REPRODUCED silent unsoundness — the struct-literal door reopens an impl's own rigid parameter and the rigid field passes through `compare_type_rigid`'s empty-rigid-list fallthrough; landing this is B225's backstop
- kind: bug
- area: Type system & the type solver

B211 decides rigidity in `reconcile_type` via `rigid_binder_scope`
(recorded per constraint anchor in `resolve_constraints`) and
`inferable_generics`; `compare_type_rigid` — the read-only comparison
`trait-objects.md` §1.4 documents as agreeing with `reconcile_type`
exactly — still binds-by-shape. Nothing in the estate depends on the
difference today. Fix: one rigidity predicate shared by both (or
`compare_type_rigid` implemented over `reconcile_type` in a dry-run
mode); a pin that runs both over the B211 garbage shapes and asserts
the same verdict; §1.4's table re-checked. Also: `reconcile_type` has
60 call sites — a refactor candidate for the next audit.
