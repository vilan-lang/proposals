# F37 — native — a PARTIAL MOVE is emitted where rustc refuses it: `let h = r.live; push(copy_of(r))` moves a field out of `r` and then uses `r`; the attach path of the reactive nodes pays one extra copy to dodge it (native copy census: dyn-objects 1/4 → 5/12, board probe 0/4 → 1/7). Repro in `sweeps/order41/reactive-41/finds/`

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: reactive-41's defect 7 (2026-09-24); native lane, with F34/F35
