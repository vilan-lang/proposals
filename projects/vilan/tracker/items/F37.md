# F37 — native — a PARTIAL MOVE is emitted where rustc refuses it: `let h = r.live; push(copy_of(r))` moves a field out of `r` and then uses `r`; the attach path of the reactive nodes pays one extra copy to dodge it (native copy census: dyn-objects 1/4 → 5/12, board probe 0/4 → 1/7). Repro in `sweeps/order41/reactive-41/finds/`

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: reactive-41's report)
- kind: bug (native)
- area: Native targets & rendering

Discussion: reactive-41's defect 7 (2026-09-24); native lane, with F34/F35

- 2026-09-25 (Order 42, native-42): BUILT (6626e54c) — premise corrected: the repro already built (native-41 copies a consumed field conservatively); what landed is PRECISION in the last-use pass — a field read judged by its PATH (moves when every later read touches a disjoint field; a read inside a closure/spawn/loop counts as the whole binding), the pass forks at if/else chains and guard-less match; reactive-41's attach-order workaround builds and matches node. Copy census (copied/moved): dyn-objects 9/21 → 3/27, board 5/15 → 1/19, delta-law 28/46 → 21/53, reactive-on-change 9/24 → 3/30, reactive-flatten 10/23 → 4/29, list-cell 74/136 → 64/146, reactive-selector 15/20 → 10/25.
