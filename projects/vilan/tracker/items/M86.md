# M86 — a `ListCell` WRITE still copies the whole list: `size()` goes through `SignalCell::get`, which deep-clones (the emitted `$y` in `list-cell.mjs`), and every notify hands each observer a copy — so A112 S2's `map_each` saves calls of `g`, not copies, and any per-push Ir claim S3 makes must subtract this first

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: performance
- area: Performance & footprint

Measure a 1,000-row push's Ir split between the derivation and the copies; then a read path that borrows (`with(|list| ..)`) or a `DeltaSource` consumer that never reads the whole. Sizing M.

- 2026-09-24 (Order 41 GO): R-e RULED — a borrowing read on `ListCell` (a new public name) is built after the measurement confirms the copies dominate; collections-41.
