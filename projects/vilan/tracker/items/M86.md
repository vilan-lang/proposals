# M86 — a `ListCell` WRITE still copies the whole list: `size()` goes through `SignalCell::get`, which deep-clones (the emitted `$y` in `list-cell.mjs`), and every notify hands each observer a copy — so A112 S2's `map_each` saves calls of `g`, not copies, and any per-push Ir claim S3 makes must subtract this first

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: performance
- area: Performance & footprint

Measure a 1,000-row push's Ir split between the derivation and the copies; then a read path that borrows (`with(|list| ..)`) or a `DeltaSource` consumer that never reads the whole. Sizing M.

- 2026-09-24 (Order 41 GO): R-e RULED — a borrowing read on `ListCell` (a new public name) is built after the measurement confirms the copies dominate; collections-41.

- 2026-09-24 (Order 41, collections-41 b979c0af): BUILT — premise corrected: the per-observer notify copy is ELIDED on JS (~4 K Ir); the copies were `size()` reading the length through `SignalCell::get` (5.8 M of 6.3 M), `set_at` and `reconcile_to` copying to read/diff. `ListCell` now holds its list in its own `Shared` beside the log and a `SignalCell<i32>` that carries only the notification; the borrowing read is `peek<U>(self, read: sync |&List<T>| U): U` (`with` is a KEYWORD — the owner may rename). Ir per push at 1,000 rows: the write 6,274,618 → 39,744; with `each` 6,305,104 → 102,546; `each_by` 6,412,457 → 154,816; `map_each` 7,474,915 → 115,700. Pin `m86_a_list_cell_write_copies_no_whole_list` (red on the old tree at `push wholes=3`). OWNER Q: the name `peek`. Finds: B400 (peek's closure spelling rides a checker gap), M87 (`KeyedCell::locate`).
