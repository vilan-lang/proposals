# M70 — SEVEN of kolt's 19 files can NEVER be served by the analyzer's base cache (`app_context, app_overlay, prefs, sidebar, store, styles, theme`): an import cycle reaches the entry's own module, so `entry_alias_module` is set and in `EntryMode::OpenFile` `entry_is_open_module` suppresses the pre-entry resolve AND the store — nothing else stores their key; each pays 0.42–1.43 s of CPU per analysis, per keystroke (paired probe: 0 hits in 7 tries with an unbounded cache). Dwarfs M67's budget question

- status: OPEN — Order 35's finds (2026-09-13)
- kind: perf
- area: Performance & footprint
- 2026-09-14 (Order 36 GO): BUILD in lane perf-36 (HIGH).
- 2026-09-14 (Order 36, perf-36): LANDED d963a119 — `BaseCacheKey.entry_open_module: Option<PathBuf>` (set in `EntryMode::OpenFile` when the entry can reach a sibling; `None` elsewhere so every pre-existing world keys as before); B239's store refusal replaced by the key; the world is stored UNRESOLVED with `entry_is_open_module` travelling; M19's checks record withheld for entry-shaped worlds on both legs. Pin `an_open_modules_world_is_stored_under_a_key_that_excludes_it` (base_cache.rs; red-proved). CORRECTIONS: it is SIX files, not seven — `store.vl` is a `[service]` entry and bypasses the cache for its own reason (M72 filed); the item's premise (storing recovers the per-keystroke cost) is WRONG — an open module's world is stored unresolved, so a hit saves the load walk only: measured 8–14 % per keystroke on the six (app_context 10.7 %, app_overlay 7.9 %, prefs 9.2 %, sidebar 13.6 %, styles 9.8 %, theme 10.4 %; controls 38–45 %); the ~150 ms `resolve_world` is paid inside `build()` every keystroke (M73 filed; the naive import-fixpoint split moved 0.4 ms — tried and reverted). Memory: kolt's session 12 → 18 worlds, +9.5 MiB in the budget's currency (M50's 24× factor over-charges these — M74). The 0.42–1.43 s figure was the owner's machine (174–608 ms here at loadavg 3). New paired harness `base_cache_keystroke_cost_across_a_sibling_checkout` (vilan-lsp session_growth).

Either store the world for an entry-aliased open module under a key that excludes the entry's own module (the world minus the alias), or resolve the cycle at store time; measure the seven files' per-keystroke CPU before/after with dx-35's paired harness (`base_cache_miss_cost_across_a_sibling_checkout`).
