# N99 — two user-visible messages the ledger's widened walk still cannot reach: (1) `asset::staged`'s registry refusal — `const_eval.rs`'s helpers return `Err(String)` and are re-wrapped at `Err(message) => Err(Failure::new(.., message))`, a variable not a literal; (2) the derive LOAD-ORDERING message (`[derive({name})]` expanded before std's `{module}` declared its `{name}` macro: a compiler load-ordering bug (B21's class); please report how this module is reached`) lives in a `let msg = if … else …` ladder handed over by field shorthand — its sibling arm (row 434) and both `[service]` arms (rows 161, 418) are rowed, this one is not, and it is not in `ROWS_THE_ENUMERATION_CANNOT_REACH` either, so nothing reds

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: hygiene
- area: Hygiene & rot

Row both through `ROWS_THE_ENUMERATION_CANNOT_REACH` (dx-37's report carries the derive message's exact text); the vacuity pin for each.
