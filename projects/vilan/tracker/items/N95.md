# N95 — `Program::hidden_impls` is DRAINED by `build_impl_admission`, so a consumer reading it after the post-passes sees an empty set; documented at the field, but a `debug_assert` or a rename (`hidden_impls_pending`) would be safer

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

Rename + a debug_assert at the drain; one line each.
