# N82 — the inference harness writes its `.mjs` runs into `std::env::temp_dir()`, and `/tmp` is a 12 GiB tmpfs shared by every lane: three `No space left on device` failures in one full run under nine lanes (also seen by slots-35), green on re-run — a spurious red indistinguishable from a real one

- status: OPEN — Order 35's finds (2026-09-13)
- kind: hygiene
- area: Hygiene & rot
- 2026-09-14 (Order 36 GO): BUILD in lane hygiene-36 (first; N81's fixed tools handed back through the lane's scratch for the integrator to land).

Write under `target/` (or `CARGO_TARGET_TMPDIR`) and clean eagerly; a guard that names the free space when a run fails with ENOSPC.
