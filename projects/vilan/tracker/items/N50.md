# N50 — the same exemption-rot shape N42 closed for the ledger has no inverse check in three other gates

- status: OPEN — hygiene-25 lane's find (2026-09-01)
- kind: hygiene
- area: Testing & CI

N42 gave the ledger's hand-rowed/fragmentless/token-class exemptions
inverse checks ("still needs exempting"). The same shape without one:
`grammar_ebnf.rs`'s `NON_KEYWORD_TERMINALS` / `PROSE_DEFINED` /
`PROSE_RIGHT_HAND_SIDES` / `UNREACHED_PRODUCTIONS`;
`diagnostics_ledger.rs`'s `APPENDIX_HEADS_NOT_HELD` /
`HEADS_THAT_ARE_NOT_MESSAGES`; `ci_ignored_pins.rs`'s
`DELIBERATE_NON_BUG_IGNORES`. Each: an inverse check that fails when
an entry's REASON stops holding, red-proved by removing the reason.
