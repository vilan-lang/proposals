# E106 — the language server slows down over a session

- status: OPEN — the owner's report, 2026-08-29 evening; measure first
- kind: bug
- area: LSP & tooling
- source: the owner's kolt hand-migration session

The owner observes the LSP "slowing down quite a bit" over a working
session. Prime suspect, already on the record: every analysis LEAKS its entry
text and AST deliberately (the known `leak_measurement`-bounded leak the
perf harness documents — "a 25 KB file at 2000 keystrokes is hundreds of
megabytes of deliberate garbage"), which a long editing session accumulates
without bound. Second suspect: S5's per-request raw re-parses (semantic
tokens + completion share one parse per request — per-REQUEST cost, not
growth, but additive). The item: instrument first (a session-length memory
and per-request latency trace against a scripted editing session), attribute
the growth, then design the reclaim (bounded retention of analyses, or the
fix M11's tally work implies). Cross-refs: M11 (retentions invisible to the
tally), M14 (the std cost angle). THE OWNER'S DATAPOINT (2026-08-29 late): "Vilan: Restart Language
Server" does NOT fix the slowness; restarting VS Code DOES. That moves
the prime suspect to the EDITOR SIDE: either the extension host's client
state accumulates (providers/disposables re-registered per restart, a
leaking middleware, a diagnostics collection growing), or the restart
command does not actually replace the server process (the stale-inode
pattern already on record for this extension). The per-analysis server
leak stays a suspect only if the restart command is a no-op. The
investigation now starts at editors/vscode: instrument what the restart
command really does, then attribute.

## Order 23 editor-health lane, 2026-08-31 — one leak fixed, instruments shipped, STAYS OPEN

The owner's datapoint (LSP restart didn't help; VS Code restart did)
was made rigorous: the restart command VERIFIABLY replaces the server
process (checked against vscode-languageclient 10.1.0's source, not
assumed), so the accumulation is client-side. The leak that matches
the datapoint exactly: `synchronize.fileEvents` created a workspace-
recursive FileSystemWatcher PER START and nothing ever disposed it —
every restart leaked one for the life of the window. FIXED (created
once, in subscriptions, reused). Server side CLEARED by measurement:
a scripted open/edit/close loop returns documents/token-cache/
manifests/pending to baseline, pinned red-first.

NOT closed: the watcher leak is restart-shaped and does not explain
accumulation within one uninterrupted session. Instruments now
standing: request-timing middleware (>400ms named), profile logged
before each restart, `Vilan: Show Language Server Status` command,
server-side session_trace (>250ms named, cardinalities every 500
requests). NEXT DATAPOINT: the owner's next slow session + the status
command's output.

## 2026-09-01 — the second datapoint, and a hypothesis with a timeline

The owner: slow in many files — views.vl, theme.vl, client.vl in
particular — and HMR "has slowed down quite a bit too". Both lists
share a property: every client-leg analysis in kolt now includes
the 636 KB generated lucide module (1791 functions) reachable from
views.vl, and the three named files are the const-heaviest
(declarations, const styles). Filed as M19 (package-module analysis
caching); E117 (the ghost diagnostic) is the ordering hole the
slowness widened. The editor-perf lane measures with the shipped
instruments on kolt read-only before touching anything.

## 2026-09-01 — editor-perf: 2.2× shipped, STAYS OPEN with the residue sized

The owner's three files attributed (see M19): lucide, not const, not
the walk. Keystroke-to-diagnostics on views.vl 3.7 → 1.8 s. What
remains is M19 + M21 + M22, each filed with numbers; the instruments
gained the LSP's phase line (`lsp-context`/`lsp-analyze`/`lsp-index`/
`lsp-legs`). E117's ghost window measured at 2.3 s pre-fix / 1.1–1.6 s
post — closed by editor-fixes' version stamping.

## 2026-09-01 — the owner's third datapoint, and the measurement that sizes it

The owner: HMR on views.vl took ~1 MINUTE. Measured on a copy of
kolt with the owner's installed 0.40.0 (d9532a9e — WITHOUT
editor-perf's 2.2×, which is on next) at loadavg 0.2: one views.vl
edit = a **4.0 s** watch round; the lucide hook prints `Fresh` inside
the round (not re-fetching); all THREE legs recompile (M22). The 15×
gap to the owner's minute is LOAD — the order ran 13 lanes + full
suites at loadavg 20–150 for hours while the owner was editing. Path
down: the next toolchain (2.2× → ~1.8 s), M22 (client leg only →
~0.6 s), then M19/M21.

## Re-measure after Order 26 (2026-09-03, lane m23-m24's note)

M11's finding — the base cache retaining one world per (std seeds ×
sibling set × package root), overwrite-only — is now BOUNDED by M24's
byte-budgeted LRU (512 MiB default, `VILAN_BASE_CACHE_BUDGET_MIB`),
and M23's claim sets per retained world are bounded by the same
budget; the session-leak loop pin returns every retained map to
baseline. NOT measured, not touched: `parse_clean_cached`'s growth for
disk-served content, and `line_indices` (documented as bounded by
project file count). Re-measure a long session (200 keystrokes across
the three kolt files, RSS sampled per 20) on the Order 26 tip before
closing or re-scoping this item.
