# M72 — `[service]` ENTRIES never reach the base cache (`base_cacheable` requires `!contains_service`), and on kolt that is the single most expensive file per keystroke after M70's six: `store.vl` pays 290–605 ms of CPU per keystroke with 0 hits and 0 misses (it never consults the cache); the bypass was written when a service entry was exotic — kolt has one and edits it

- status: OPEN — filed 2026-09-15
- kind: perf
- area: Performance & footprint

Store the world before the service expansion (the expansion runs inside the world-building loop — split it) or key the service's expanded form; measure store.vl with the paired harness.
