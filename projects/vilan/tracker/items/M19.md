# M19 — an unchanged package module is re-analyzed on every LSP analysis and every HMR round

- status: OPEN — filed 2026-09-01 as E106's prime hypothesis
- kind: perf
- area: Performance & footprint
- source: the owner's datapoint (views.vl, theme.vl, client.vl slow; HMR slowed "quite a bit") + the lucide timeline

The BASE_CACHE reuses std worlds keyed by import set; nothing
comparable exists for PACKAGE modules. Since kolt gained the 636 KB
generated `src/lucide/lib.vl` (1791 functions, reachable from
views.vl), every analysis of any client-leg file types all 1791
again — the LSP on each keystroke, HMR on each round — and the
timeline of "slow in many files" matches the day the module landed.
The const-heavy files (theme.vl's declarations, views.vl's const
styles) add the const pass per analysis on top. Direction: cache the
analysis of an unchanged module (content-keyed, like the parse
cache) across analyses within a session — the module's declarations
and typed bodies — invalidated when its content or anything it
imports changes; emission reachability already prunes the OUTPUT,
this prunes the WORK. Measure first with the E106 instruments
(`Vilan: Show Language Server Status`) on kolt read-only; the
numbers decide whether lucide is the whole story.

## SIZED 2026-09-01 (editor-perf)

Lucide was 90% of a client-leg analysis (views.vl 2.22 s vs 0.22 s
with a 4-icon stub); const-heaviness REFUTED (const passes 11 ms;
theme.vl the fastest file); E113's walk REFUTED (10–20 ms). The hot
constant — `refined_edges`' impl selection, 17,895 selections per
pass for 32 distinct answers — is now memoized on the resolved Type
(2.2×: views.vl 1.00 s, build 2.46→1.22 s, keystroke-to-diagnostics
3.7→1.8 s). RESIDUE, which is this item: whole-program work over
1,791 UNCHANGED functions — checks ≈ 950 ms (63% of it
`check_generic_bound_satisfaction`) + load/walk/base ≈ 480 ms; lucide
still 4.7× of an analysis. Siblings: M21 (base-cache bypass on
`pkg::` imports), M22 (watch recompiles every leg).

## Tranche 1 BUILT 2026-09-01 (lane perf-25) — item stays OPEN for tranche 2

`check_generic_bound_satisfaction` memoizes `satisfies_trait_bound`
and `unrankable_bound_impls` on the RESOLVED `Type` (ids are per-
occurrence — id keys memoize nothing), nested maps so a hit allocates
nothing; counter `analyzer::generic_bound_checks()`; slope pin red-
first via a planted memo-disable (4→20 call sites: 12→28 evaluations
without, 12→12 with). kolt `checks` 2284→769 ms; maxrss 176→205 MB.
TRANCHE 2 — reuse of an unchanged module's analysis ACROSS analyses —
is a rewrite the paper must sequence: one Analyzer mints entity ids
sequentially by load order, so a module's declarations and typed
bodies are not addressable independently of what loaded before; the
frozen-source seam (`std_sources` → `frozen_ranges` → `frozen_entity`)
assumes a DIAGNOSTIC-CLEAN source, which a package module is not —
its diagnostics must be cached and replayed keyed on (module content
× transitive import contents). Sequence after M23.

## Paper landed 2026-09-03 (lane m19-paper, `per-module-analysis-reuse.md`) — FIVE QUESTIONS for the owner

Recommendation: NOT per-module id spaces, NOT content-hashed ids —
M21's base-cached `World` is CLONED on a hit, so module entities
already occupy a byte-identical id PREFIX and the edited buffer the
suffix (option (c) is the shape the tree has). Generalize the
frozen-source seam from `std_sources` to every source in the cached
world — for the diagnostic checks AND the table builders — and pay
for a package module not being diagnostic-clean by caching its
diagnostics beside the world and REPLAYING them (span-addressed,
attributed by `diagnostic_sources` — a splice). Gate like S1: the
corpus differential (byte-identical diagnostics/warnings/JS) plus a
per-source dirty bit set in `write_type_slot` refusing reuse for a
module whose type slots the entry's `build()` moved.
NUMBERS: 83% of a warm kolt keystroke's analyzer CPU is ONE unchanged
module (lucide): 965/1,033 ms with it, 162/177 stubbed; the generated
exhibit 50/57 ms (0.05×). `checks` 90% the module's; 96% per-body
walking; `check_generic_bound_satisfaction` now 5.6% (tranche 1
worked). Projection: T1 (freeze module-local checks + tables, replay
diagnostics) → 330–420 ms; T2 (post-passes seeded from the world) →
230–320 ms; floor 162 ms. THE KEY `(module × transitive imports)` is
WRONG: no orphan rule (impls in unimported modules change resolution),
whole-program predicates (`declares_a_resource()`), four passes run in
the REVERSE dependency direction — the right key is `BaseCacheKey ×
every loaded source's content × "the entry didn't move this module's
type slots"`.
Q1 all-or-nothing per world as the reuse granularity? rec yes. Q2 may
a package module's cached diagnostics be REPLAYED (S1 skipped std's
because known absent; T1 makes them remembered)? rec yes, the
differential the standing gate. Q3 a view-shaped exhibit for the
diagnostics gate (E126)? rec yes, prerequisite. Q4 `plan_resource_
drops` (M28) filed separately? done. Q5 the `Note.source` index
invariant as an assertion? rec assertion.
Found: E126, M27 (`lsp-index`), M28. Correction: the BASE_CACHE/M9/
frozen-source material is leak-soak.md §7.9, not editing-dx.md.
