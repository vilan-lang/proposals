# D14 — the lexical spec §2.2's contextual-keyword list OMITS `as` and `only`, which the compiler already treats as contextual (accepted as names at every position probed); `layer` is listed nowhere and is not a word the compiler knows

- status: OPEN — filed 2026-09-25 (Order 42, papers-42's probe)
- kind: docs
- area: Documentation

Add `as`/`only` to §2.2's list; drop `layer` from any list that names it. Gates grammar_ebnf/grammar_sync if the EBNF carries the list. Sizing XS; may ride syntax-42's EBNF pass.
