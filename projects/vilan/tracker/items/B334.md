# B334 — a METHOD NAME SHADOWS a same-named FREE FUNCTION inside its own `impl` block: `fun when(self, ..) { self.child(when(condition, body)) }` resolves `when` to the method ('`when` expects 3 arguments, but got 2'), and there is no qualified escape from inside std (`pkg::ui::when(..)` → '`pkg` is a namespace, not a value'; a self-import is a cycle) — the value forms' methods had to construct the struct literal inline instead of being the sugar the paper promised

- status: OPEN — Order 35's finds (2026-09-13)
- kind: design
- area: Type system & the type solver

Decide the spelling (rec: the free function wins when the call has no receiver — a method needs one); pin the shape; positional-slots §3b's 'sugar' becomes literal.
