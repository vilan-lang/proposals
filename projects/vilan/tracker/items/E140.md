# E140 — E124's residue: paper §6.1 pins 4, 10, 11, 16, 19 unbuilt; withdrawal is package-wide, not the depends_on cone; the union analyses ignore M26's per-document scheduler

- status: OPEN — lane e124-build's find (2026-09-04, Order 27)
- kind: editor
- area: Language server & tooling

Not pinned: `[rpc]` reached via the dispatcher in both states (4), a `[platform]` fence across a browser+node package (10), 11's two-state form, lateness allowed (16), a library as a WORKSPACE MEMBER (19). Withdraw-on-edit clears the whole package's paint on any keystroke (safe, but a large package loses grays constantly) — withdraw the inverted `depends_on` cone instead. A package with many entries pays its union analyses serially; route them through M26's scheduler. Also: trait-impl members are unpainted in v1 (per-instantiation) — decide whether they ever paint.
