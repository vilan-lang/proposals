# E211 — E194's non-VS-Code half: `Completion` carries no `filter_text` and the server sends no `textEdit` range, so every other LSP client filters a hyphenated candidate (`stroke-width`, `--card-gap`) by its own word rules

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: editor
- area: LSP & tooling

`filter_text` + an edit range on `Completion`, consumed by both front-ends — a server-side fix that also makes E194's client-side `wordPattern` pin redundant (keep it).
