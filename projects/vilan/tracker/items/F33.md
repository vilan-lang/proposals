

- 2026-09-24 (Order 41, native-41): BUILT (cb5b2e5d, RULED (a)): `vilan_rt::bytes::Bytes` = `Rc<RefCell<Vec<u8>>>` in its own module (moved out of `http` with the two text codecs); a copy is an alias as a `Uint8Array` is; `fill` answers the same buffer, stores the low 8 bits, takes `slice`'s range rules; `copy_into` reads its source first (self-copy is not a double borrow), past the end aborts with the host's `RangeError`; `slice`/`get` unchanged; equality is identity. Corpus `bytes-aliasing.vl` (native default suite; the interpreter excludes it by name) + 4 unit pins; red on the seal compiler and with copy-on-write planted.
