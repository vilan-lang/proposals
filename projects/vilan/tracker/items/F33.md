# F33 — RULING — MUTABLE `Bytes` natively: `std::ws`'s codec needs `alloc`, `fill` and `copy_into` (the three `Bytes` bindings that mutate) but native `Bytes` is an immutable refcounted buffer while JS's `Uint8Array` is mutable; with F22 landed this is the LAST wall between `Server::builder()` and a native rpc server (plus two seams: `now_millis` and SHA-1 `digest` for the WebSocket accept key)

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: ruling (native)
- area: Native targets & rendering

Options: (a) a shared `Rc<RefCell<Vec<u8>>>`-style buffer — JS's semantics (rec); (b) copy-on-write with the three bindings returning a new `Bytes` (breaks aliasing programs); (c) refuse the three natively for good. Rec (a), with the two seams landed alongside, as F18 slice 3 in Order 41.

- 2026-09-22 (the owner's rulings after Order 40's seal): RULED (a) — a shared `Rc<RefCell<Vec<u8>>>`-style mutable buffer natively (JS's semantics), landed with the two seams (`now_millis`, SHA-1 `digest`) as F18 slice 3 → `Server::builder()` natively → the rpc server. Order 41, native lane.

- 2026-09-24 (Order 41 GO): Order 41, native-41 (as ruled).
