# B348 — a GENERIC `Callable` does not coerce into a closure-typed position (by design: `impl Box<type T> with Callable` writes `call` in the impl's own binders — fn-coercion rule 2's reason); `held()` calls, `let f: |…| T = held;` is the ordinary mismatch; needs the expected-type-driven instantiation rule 2 is waiting on

- status: OPEN — filed 2026-09-15
- kind: design
- area: Type system & the type solver

With rule 2's instantiation: bind the impl's binders from the slot's closure type; pin.

- 2026-09-17 (the kolt migration, post-seal): a second exhibit of the same missing instantiation, on a plain GENERIC FUNCTION rather than a `Callable`: `current_path().map(from_path)` with std::router's `fun from_path<R: FromPath>(path: str): R` is refused — `Expected |str| Route, but got fn from_path<R of FromPath>(str): R instead` — while the eta-expanded `|path| from_path(path)` compiles (kolt client.vl:74 spells the latter). The slot's closure type names `R`; the expected-type-driven instantiation rule 2 waits on would bind it. Same item.
