# B401 — the analyzer's INHERITED-DEFAULT lookup ignores impl ADMISSION: `b.vl` exports `Box`; `t.vl` has traits `One`/`Two` each with a default `describe`; `p1.vl`/`p2.vl` each `export impl Box with One/Two {}`; `main.vl` with `import pkg::p1 only;` still compiles `box.describe()` and emits "one" — and with p2 loaded in any form (`only`, or a non-exported impl) the call is 'ambiguous on Box' whatever the file admits. B318's `only`/`(impl …)` selectors decide which impls a FILE admits for a method call; the inherited-default path does not consult them (N124's pin covers the shared `mono` lookup, which is right — the analyzer's own resolution is what ignores it)

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: hygiene-41's report)
- kind: bug (inference)
- area: Type system & the type solver

Discussion: hygiene-41's find 1 (2026-09-24); solver lane
