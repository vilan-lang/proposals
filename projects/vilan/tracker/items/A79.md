# A79 — the keyed RETURN mapping: `[rpc] fun tasks_in(workspace): KeyedCell<i32, Task>` → a stub returning `Result<KeyedSource<i32, Task>, RpcError>` — §9.2's row, UNBLOCKED by A54 (the cell exists) and A74 (the mapping machinery, `handle_element` + `reply_source`), unbuilt

- status: OPEN — Order 31's finds (2026-09-09)
- RULED 2026-09-11 (owner, P3): the keyed RETURN in A92's sync unleased shape, no per-key hop. BUILD: Order 33, lane rpc-33.
- kind: feature
- area: Reactive core & UI

Add the `KeyedCell` spelling to `handle_element` and a `reply_source_keyed` over `expose_keyed_cell` marked DYNAMIC; the keyed lease gets the same origin/re-mint path (owner Q: the per-key hop); pins mirroring A74's.
