# B354 — HIGH: a `[derive(..)]`-SYNTHESIZED generic body resolves its members under the SENTINEL source (`current_admitting_file` → `source_of` → `DERIVED_SOURCE`), not the file the derive was written in — so B318 S4's export gate hides every non-exported impl from every derived visitor. This is WHY E185's placeholder message fired sixteen times at Order 36's sweep merge and why std had to export its codec blocks (`JsonWriter`/`JsonReader`/`BinaryReader`) that it had kept private. E185 fixed the NAME in the message (diagnostics-37); the resolution rule is this item. Reproducible: an exported `Counter` + a non-exported `impl Counter with Serialize` in `w.vl`, `[derive(Wire)]` in `thing.vl`, `.describe(counter)` in a third file

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: bug
- area: Type system & the type solver

A derived body's admitting file is the file carrying the `[derive(..)]` attribute (the importing body `AdmissionMiss` now records — E185's plumbing is the seam); pin the three-file shape; then re-examine the codec blocks the sweep exported under the old rule (sweep-36's curation re-run) and re-privatize what only the sentinel rule needed.
