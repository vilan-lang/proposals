# B233 — `fun sum<P: Add, Q>(a: P, b: Q): P { a + b }` compiles and `sum(1, "two")` runs: the operator-dispatch site retains away the free binders, so two different rigid parameters meet on an operator (UNSOUND)

- status: OPEN — lane b225-b219's find (2026-09-04, Order 27)
- kind: bug
- area: Type system & the type solver

The operator-dispatch site (analyzer ~14825) builds its own `rigid` list via `collect_generics` then `retain`s away `free_binders`, so P and Q reconcile as if open. B211's rule (a body cannot bind its own parameters) and B225's fresh-ids door do not reach this path. Fix: route the operator site through `generic_is_rigid_here` (B219's shared predicate) and refuse. Census the estate for operator uses over two distinct parameters first (expect zero). Pins: the `sum` shape, the same-parameter control `fun twice<P: Add>(a: P, b: P)`, a call-site mismatch through the sugar.
