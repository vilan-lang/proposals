# N116 — TWO WALL-CLOCK pins red under load and must lose their wall half: `m75_a_collisions_refusal_does_not_scale_…` (perf-38 made its COUNT half; the wall half remains — '-10 ms' under a parallel lane) and `native_differential::the_spawn_and_sleep_ordering_…` (a sleep-ordering RACE in the executor under load, native-b-39's). Rule for the suite: no assertion reads a clock

- status: OPEN — filed 2026-09-21 (Order 39 sweep; collections-39's FIND 6 + solver-39's find 1)
- kind: hygiene (flaky pins)
- area: Hygiene & rot

(1) delete the wall half of the M75 pin (the count is the claim). (2) the spawn-and-sleep pin: drive the executor with a virtual clock (vilan-rt's timers already take a deadline — inject `now`) so the order is asserted with no real sleep; if the runtime pin must sleep, the tolerance is ≥ 10× the interval. Pins: both green under `cargo nextest run -j 32` with a CPU burner beside them. Sized S.
