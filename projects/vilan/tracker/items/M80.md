# M80 — `push_or_share`'s body-sharing key does not normalize LOCAL GENSYMS: `canonical_instance_body` normalizes only the instance's own name, so two monomorphizations whose bodies differ only in gensym numbering stop sharing — C14 S3 gave `observe` two compiler-minted temporaries and `observe` is now emitted TWICE in `reactive-flatten.mjs` and `reactive-on-change.mjs` where it was once (`$D`/`$H` identical modulo `$E$F` vs `$I$J`); +2,857 bytes over the corpus goldens, ~+640 each in those two

- status: OPEN — filed 2026-09-17 (Order 37 sweep)
- kind: perf
- area: Performance & footprint

Normalize local gensyms in the canonical key (or number them per body); the two goldens return to one `observe`; a pin that two bodies differing only in gensyms share.
