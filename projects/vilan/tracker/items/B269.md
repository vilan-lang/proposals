# B269 — the dead-code paint (E124) marks a function used ONLY under `const` as unused: kolt's `fun page_defaults()` is called by `const page_defaults();` (build-time side effects — `declare`, `preflight`, `bundle`) and reads as dead because the paint follows the pruner's RUNTIME reachability, under which a const-evaluated call never runs; a `const` call is a use

- status: OPEN — the owner's find (2026-09-07)
- kind: bug
- area: Language server & tooling

PREMISE TO VERIFY FIRST: `dead_items.rs:25-27` already follows "a callee reached only from a `const` module-binding initializer" (`CallGraph`'s paint-only const edges) and `:91` names kolt's `let _page_defaults = const …` as the exhibit the `_` keeps quiet — so the owner's paint on `fun page_defaults()` is either an older `vilan-lsp` in the editor (the a5473227 pair was installed 2026-09-07 15:00; reproduce after a restart) or a shape the edge misses. The shape that certainly has no edge is the BARE STATEMENT `const page_defaults();` at module level (not a binding initializer), which is the spelling recommended over the `_`-binding hack: give it the same paint-only edge. Pin: a function called only from a module-level `const f();` statement is not painted; one called from nowhere still is; the emission still omits it.
