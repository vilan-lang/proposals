# B236 — the entry-cycle refusal (B226) still cascades three follow-ons: `cannot find 'X' in the imported path` and two `cannot find type`

- status: OPEN — lane checker-27's find (2026-09-04, Order 27)
- kind: bug
- area: Type system & the type solver

After B226's error on a sibling importing `pkg::<entry>`, the unresolved import leaves its names dangling and every use reports again. Root the follow-ons under the primary (the stand-down that B189/B229 use), so the cycle reports once at the import. Pin: the cycle program reports exactly one error.
