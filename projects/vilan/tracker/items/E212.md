# E212 — a `$VILAN_STD` pointing at a `std` with no `macro_std` sibling is not refused by name: the derive silently fails to expand and std's own compare.vl is blamed (`'Instant' does not implement trait 'PartialOrd': missing 'eq'`) — B346's promise ('a root that carries only half a toolchain is refused by a message naming BOTH paths') unkept on the one path a user actually configures

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: diagnostics
- area: LSP & tooling

Refuse at toolchain resolution naming the std root and the `macro_std` path it looked for; a `relocated_std.rs` pin with a half copy (two commands reproduce it).
