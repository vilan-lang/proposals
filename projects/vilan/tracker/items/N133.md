# N133 — N129's per-test staging (`target/tmp/native-differential-src-<pid>-<counter>`) is NEVER REMOVED: 2,788 stale directories after one day of Order 42's runs (each a copy of the corpus), and CI's `vilan-fmt` leg run locally walks `./target` and DECLINED 5,556 of their stale `resource struct` copies ('it does not parse' after B413) — two fixes: the staging directory is a tempdir removed at the test's end (or on the next run's start), and the formatter leg's walk excludes `target/`

- status: OPEN — filed 2026-09-26 (the Order 42 seal)
- kind: hygiene (tests)
- area: Hygiene & rot

Sizing XS–S; hygiene lane, Order 43. Meanwhile the integrator removed the stale directories by hand.
