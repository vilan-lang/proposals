# A129 — RULING — a `Splice` is applied BY POSITION on the delta path, so ROW IDENTITY is lost across it: a splice that removes rows and re-inserts the SAME keys rebuilds them where the keyed pass keeps them (`set_all` over the same three keys: `cut=3 built=3` vs `set`'s `cut=0 built=0`; `reconcile_to` with one edit + an append rebuilds the tail; in the 300-turn walk `each_by` builds 1,078 rows on the op path against 557 on the pass) — and `set_all`'s doc claim ('cheaper than `set`') is FALSE for keyed consumers. Rule: does a splice's span get a KEYED match (the pass's identity rule inside the span — rec yes, bounded to the span, so identity survives a re-insert and the op path never builds more than the pass), or is `set_all`'s doc corrected to say so

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: collections-41's report)
- kind: design (ruling)
- area: Reactive core & UI

Discussion: collections-41's find 2 (2026-09-24); repros in `sweeps/order41/collections-41/finds/`; incremental-collections.md §13 did not price it

- 2026-09-25 (the owner's rulings after Order 41's seal): RULED as recommended — a `Splice`'s span gets a KEYED match bounded to the span, so identity survives a re-insert and the op path never builds more than the pass; `set_all`'s doc corrected with it. Order 42, collections.

- 2026-09-25 (Order 42, collections-42): BUILT (c64bf317) as ruled: the whole-list pass is now `reconcile_span(at, count, list)` over the whole run (so the two cannot drift); a Splice that both removes and inserts runs the SPAN pass (bounded on the right by the first row after it or the anchor; a key inside the span never claims a row outside it); a one-sided splice stays positional (push still `built=1 keys=1`); a DRAIN RULE — ops before the last `Reset` in one drain are dropped; `held_span` copies only the span's elements (M86's `move_range wholes=11` unmoved); the server twin has no op path. `set_all` over the same keys `cut=3 built=3` → `cut=0 built=0`; `reconcile_to` `cut=2 built=3` → `cut=0 built=1`; the walk's `each_by` on the op path 1,078 → 575 against the pass's 557 — the ≤-pass target NOT fully met: the remaining 18 are rows built and cut inside one drain (→ A131, a deferred-build redesign). `set_all`'s doc corrected (not cheaper than `set`, no longer costlier); docs ×2. Pin `a129_a_splice_keeps_the_rows_whose_keys_survive_it` (cost + row identity per line; red on the old tree and under four plants); the S3 walk pin now carries two pass-control runs (`renders=2061 by=575 … pass=653 pass_by=557`). No golden moved; census unchanged.
