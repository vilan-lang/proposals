# B287 — once B281 lets one connection's handlers INTERLEAVE, two routes share the one `self` binding and an `async fun f(&mut self)` `[rpc]` method that awaits can have its instance mutated underneath it — Q9's "a `&mut self` method is itself a promise that it does not await" is now load-bearing and NOTHING enforces it (an async `&mut self` rpc compiles today and its write persists; the no-view-across-await rule is still deferred)

- status: OPEN — Order 31's finds (2026-09-09)
- RULED 2026-09-11 (owner): REFUSE `async` + `&mut self` on an `[rpc]` method at expansion until the no-view-across-await rule lands. BUILD: Order 33, lane rpc-smalls-33.
- kind: design
- area: Reactive core & UI

Rec: REFUSE `async fun f(&mut self)` on an `[rpc]` method at the attribute (one row) until the no-view-across-await rule lands; the guide sentence lands with Order 31's seal either way.
