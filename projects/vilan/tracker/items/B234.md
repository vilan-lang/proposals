# B234 — `fun f<T>(x: T) { if x { } }` compiles: a rigid parameter satisfies a `bool` condition (UNSOUND)

- status: OPEN — lane b225-b219's find (2026-09-04, Order 27)
- kind: bug
- area: Type system & the type solver

The condition check reconciles the parameter with `bool` and, with T open at that site, binds it. Under B211 T is rigid inside its own body; the condition site does not consult rigidity. Fix through the shared predicate; pins: the `if x`, a `for x` condition, a `!x`, a `T: Into<bool>`-shaped positive control if one exists, a concrete-`bool` control.
