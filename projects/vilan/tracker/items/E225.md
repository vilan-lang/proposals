# E225 — bindgen's `RESERVED` list has DRIFTED from the lexer: it lacks `css`, `dyn` and `lazy`, so a TypeScript member named one of those produces bindings that do not parse — the list should be derived from the lexer's `KEYWORDS` (one source), and after B413 it must not name `resource`

- status: OPEN — filed 2026-09-25 (Order 42, papers-42's probe)
- kind: bug (bindgen)
- area: LSP & tooling

Fix: `RESERVED` = the lexer's table (a shared constant or a pin that they agree); a pin per drifted word. Sizing S.
