

- 2026-09-21 (Order 38 sweep): DOOR 1 BUILT in lane reactive-38 (2f93d7a2): one liveness cell shared by a `Subscriber` and its `Subscription`, lowered before any removal; THREE faces closed — the inline snapshot, the WAVE COPY (`drain` takes a wave out of `pending` before running it) and the scrub's blind spot (papers-38's FIND-2, fixed at 2b06202c: `dispose` resolves its turn as `notify` does; the scrub is KEPT, fixed). Cost +9.1 % Ir per 12-notification wave, std `Shared::new(` +3. FOUND: face 2 in the DOM is a HARD CRASH on the base (`Cannot read properties of null (reading 'insertBefore')`) — kolt's `page_content` latch is what keeps the app from throwing, not an optimisation. DOOR 2 measured twice (reactive-38 bucketed +23.5 % / naive 2.2×; papers-38 2.15× / 5.33×; zero corpus output changes; the nested-`swap` pin improves 3/3 → 2/2) and RULED (above); the section is reactive-turns.md §7. Both lanes: this item's own probe is SIBLINGS, not nesting — door 2's pin is the nesting shape. Stays OPEN for door 2 (Order 39).
