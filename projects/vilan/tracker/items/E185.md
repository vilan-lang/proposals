# E185 — S4's monomorphization refusal prints a PLACEHOLDER where the module's name belongs: "'begin_struct' is provided by an `impl` in module `json`, and module `that module` does not admit it … Widen `that module`'s own import of `json`" (seen at the sweep merge, sixteen times); the declaring file's module name is in hand (`current_admitting_file`) and the row's `{}` slots want it

- status: OPEN — filed 2026-09-15
- kind: bug
- area: LSP & tooling

Thread the declaring module's spelled name into the message and the steer; re-key the row; the §3.5 pin asserts the name.
