# E163 — the TextMate scope tests assert REGEXES, not scopes: E161's five pins beside `grammar_sync` check each rule's regex and the two rule ORDERS that decide the outcome, but nothing runs VS Code's tokeniser over the exhibit — that needs `vscode-textmate` + `vscode-oniguruma` as extension devDependencies (neither is in `editors/vscode/package-lock.json`; 41 packages, none textmate), a NEW dependency the lane stopped on

- status: OPEN — Order 32's finds (2026-09-11); OWNER Q (a new dependency)
- RULED 2026-09-11 (owner): the two devDependencies are ADDED. BUILD: Order 33, lane editor-33.
- 2026-09-11 (integrator, from lane editor-33): STOPPED on the CI half, not the ruling — the notices gate is FREE (Cargo.lock only) and the cost is +2 lockfile packages, +1 MB node_modules, ~1 s warm `npm ci`, 0 advisories; but NOTHING creates `editors/vscode/node_modules` in ci.yml's test job, release.yml's gate job or ignored-pins.yml (only the vsix job and install-dev.sh run `npm ci`), so the scope pins would run nowhere; making them run = an `npm ci` step in two workflows (Windows included) and a fresh clone's `cargo nextest run --workspace` depending on npm — release machinery. The tokeniser was PROTOTYPED off-tree and found two real grammar defects the regex pins cannot see (E164). RULING ASKED: add the `npm ci` step (rec yes).
- kind: editor
- area: LSP & tooling

Add `vscode-textmate` and `vscode-oniguruma` to `editors/vscode/package.json` devDependencies (lockfile updated, THIRD-PARTY-NOTICES if the notices gate reads devDependencies — check), a `tokenize.js` helper under `crates/vilan-cli/tests/support/` that loads the grammar with the wasm onig and prints scopes per token; rewrite E161's five pins as scope assertions over the exhibit (`<div>` and `a < b` as controls).
