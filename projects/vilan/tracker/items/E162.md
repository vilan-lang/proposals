# E162 — a FIXED-ARRAY generic argument ends the TextMate generic list early: `SignalCell<[i32; 4]>` hits E161's `;` bail-out and the tail falls back to top-level rules (today's behaviour before E161); zero occurrences in std, the corpus, the examples or the book; documented at the rule

- status: OPEN — Order 32's finds (2026-09-11)
- kind: editor
- area: LSP & tooling

A nested `[`…`]` sub-rule inside `meta.generic.vilan` that consumes the array type (and its `;` length) closes the gap; one regex pin beside E161's.
