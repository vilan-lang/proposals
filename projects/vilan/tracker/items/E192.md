# E192 — the unused-import fade on a TYPE import whose only use is a SELECTOR import's subject: kolt views.vl:2–4 — `import std::map::Map;` fades as unused while `import std::map::{ (impl Map<_, _>) };` names `Map` (the owner: 'considered unused, but it is used for the type below'). Verify which is true: the selector resolves `Map` through the file's import (then the fade is a false positive — count the selector subject as a use) or through the module's own items (then the type import IS unused, the fade is right, and the owner deletes the line)

- status: OPEN — filed 2026-09-17 (the kolt sweep)
- kind: editor
- area: LSP & tooling

Delete the import in a copy and `vilan check`; then either the fade's use-count gains the selector subject (pin: a type import used only by a selector is not faded) or the item records the answer for the owner.

- 2026-09-17 (Order 37 GO): VERIFY + fix in lane editor-37.
