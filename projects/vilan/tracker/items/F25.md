# F25 — native EXIT and PRINT corners: a failing `async fun main` (and a sync panic) exits 101 with Rust's panic banner where node exits 1 with its own; printing a host handle (`Task`, `Nursery`, `CancelSignal`, `TimerHandle`) or a value holding a function PANICS at runtime with the reason rather than inventing node's inspection text

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: design
- area: Native targets & rendering

Rule the native exit code + panic rendering (rec: exit 1, the message without Rust's banner); make the two print cases an emitter-side COMPILE-TIME refusal (the standing preference). `settle_all` raising the first failure in ORDER where `Promise.all` raises the first to SETTLE is documented as accepted.
