# N42 — the gates' exemption lists have no stale-exemption check

- status: OPEN — hygiene lane's find (2026-09-01)
- kind: process
- area: Hygiene & rot
- source: lane hygiene (N39/N41)

Two exemption lists can rot silently: `TOKEN_CLASSES_NOT_IN_LEXICAL`
in grammar_ebnf.rs (a class listed there that IS declared in
lexical.md stays green — N39 just removed `ISTRING` by hand) and
`KEYS_WITHOUT_A_FRAGMENT` / `ROWS_THE_ENUMERATION_CANNOT_REACH` in
diagnostics_ledger.rs (a row that later becomes reachable stays
exempt). Each list gets its inverse check: an exemption that no
longer needs to exist reds the gate naming it, so exemptions expire
the way ignored-pin reasons now do (N27). Also from the lane: the
mdBook version pin lives in FOUR in-repo places plus the pages repo's
docs.yml with only two held against each other — fold the other two
into the same cross-check.
