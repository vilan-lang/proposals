# F32 — RULING — `BigInt` has NO native representation: the `n` suffix is arbitrary precision on JS, and it had been emitting `…993i32` (a WRONG value, silently) until native-a-39 made it a refusal BY NAME (2 programs). Rule: a bignum in `vilan-rt` (dependency-free — its own limbs), or a documented limit (`i128`, refused past it), or 'not on native'

- status: OPEN — filed 2026-09-21 (Order 39 sweep; native-a-39's find 3)
- kind: ruling (native)
- area: Native targets & rendering

Options: (a) a small dependency-free bignum in `vilan-rt` (add/sub/mul/div/compare/format — a few hundred lines; the corpus uses BigInt for two hashing-shaped programs); (b) `i128` with a compile-time refusal for a literal past it and a runtime trap on overflow — a LIMIT the book states; (c) BigInt stays JS-only and the refusal is permanent. Rec (b) now, (a) if a real program needs it: the two programs are contrived. Pins: whichever is ruled, the two programs flip or their refusal names the rule.

- 2026-09-22 (the owner's rulings before Order 40): RULED (b) — an `i128` LIMIT natively: a literal past it is refused at compile time, an overflow traps at runtime, the book states the limit; (a) a bignum only if a real program asks. The two programs flip or their refusal names the rule.
