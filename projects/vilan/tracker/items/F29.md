# F29 — the native HOST CENSUS (`VILAN_NATIVE_HOST_CENSUS=1`) UNDER-REPORTS: a refused call's ARGUMENTS are never walked, so `createServer(..)` hid NINE bindings behind it and the brief's '55 host bindings' was low; the census must walk into a refused call's arguments (and its closure bodies) anyway

- status: OPEN — filed 2026-09-21 (Order 39 sweep; native-b-39's find)
- kind: tooling (native census)
- area: Native targets & rendering

Fix: on a refusal, the emitter records the name and CONTINUES into the call's arguments and any closure bodies among them, recording their bindings too (emit nothing for them; the program is refused regardless). Pin: a program whose only host bindings sit inside a refused call's arguments reports all of them; the whole-set census total moves up and is re-baselined once. Sized S.
