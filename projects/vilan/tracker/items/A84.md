# A84 — HOW a `Style::when(..)` variant should be EMITTED: today every combination of flags (`selected`, `!disabled`, `!selected && !disabled` in kolt's `styles.vl:46`) computes its own merged style and so its own class set per call; the owner's TODO asks whether variants should instead be CSS custom properties or `data-` attributes selected by static rules (`[data-selected]`), for both performance (one class per variant vs one rule set) and tidiness — the emission strategy the A36 combinator left open

- status: OPEN — kolt marker sweep (2026-09-10)
- kind: design
- area: Reactive core & UI

Measure first: count the classes and rule bytes kolt's button emits across its flag space under the current computed form, then the same surface as (a) `data-` attribute rules and (b) custom properties; rule on one or on a per-site choice (a `Style::variant(name, delta)` that emits an attribute-selected rule beside `when`'s computed merge would let both coexist). No build before the ruling.
