# E175 — the css-block converter stops at the PACKAGE boundary: kolt's `button_style` converts the prefix its own file's `flex_row` opens and splits at `.script_label()`, declared in `theme.vl`; widening needs the analyzed impl table plus each declaring file's text, not one raw parse

- status: OPEN — Order 35's finds (2026-09-13)
- kind: editor
- area: LSP & tooling
- 2026-09-14 (Order 36 GO): BUILD in lane editor-36.

Feed `StyleSurface::extend` from the program's impl table (each `impl Style` block's source and text); pin the sibling-file case converting.
