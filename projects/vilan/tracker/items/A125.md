# A125 — RULING — add `K: Hashable` to `reconcile` and the `each*` family so REORDERS go linear too (M82 made the common path linear with a `PartialEq` position index; a coarse `==` still scans `[low, candidate)` on a reorder). BREAKING: kolt's two `each_values` over structs need `[derive(Hashable)]`

- status: OPEN — filed 2026-09-21 (Order 39 sweep; collections-39's open Q-A)
- kind: ruling (breaking)
- area: Reactive core & UI

M82's plan is identical for every `K` and needs no bound; what it cannot do without a hash is find an old position for a key that MOVED — that case scans `[low, candidate)` with `==`, which is empty for every key type in the estate today (keys are ids, rows append or delete) and quadratic the day a list is sorted in place. Rec: rule YES and land it in Order 40 beside I5, since both touch every `each` caller's signature once. Alternative: leave it and pin the scan's cost so the day arrives with a number. Pins: a 1,000-row reverse is linear in Ir; kolt's two struct keys derive `Hashable` and build.

- 2026-09-22 (the owner's rulings before Order 40): RULED YES — `K: Hashable` on `reconcile` and the `each*` family, Order 40 beside I5 (one signature move per `each` caller); kolt's two `each_values` over structs derive `Hashable`; pin the 1,000-row reverse linear in Ir.
