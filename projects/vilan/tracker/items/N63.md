# N63 — `vilan check` now CREATES `dist/` (M33's `dist/.macro-expansions` lives there), where it previously wrote nothing; compile-perf-29 gitignored `vilan/benchmarks/dist/` and `vilan/test/dist/` after a cache file was staged by accident — where the on-disk caches live (`dist/.cache/`, a per-package `.vilan/`, or the build dir only for `build`) is the owner's call

- status: OPEN — lane compile-perf-29's question (2026-09-06, Order 29); OWNER Q
- kind: process
- area: CLI & workflow

Recommend `dist/.cache/` for every on-disk cache (one ignore line, one place to delete), `check` allowed to create it; pin that `check` writes nothing else.
