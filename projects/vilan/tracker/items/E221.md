# E221 — E213 S2 — `[internal("reason")]` on the remaining positions (nominals, enum variants, traits, module bindings), the docs generator's collapsed 'Internal' heading, and the opt-in `[lints] internal_use = "warn"` (a new `[lints]` manifest section). S1 shipped functions/methods/externals and struct fields

- status: OPEN — filed 2026-09-22 (Order 40 sweep)
- kind: feature (editor)
- area: LSP & tooling

Sizing M.

- 2026-09-24 (Order 41, editor-41): BUILT (ae541ba1) — premise partly false: there is NO docs generator (the book is hand-written; E204 found the same), so the collapsed 'Internal' heading was not built. `[internal("reason")]` on structs, enums, variants, traits, module bindings (a labelled `let`; a local-binding label refused); one reader `vilan_core::labels::internal_of` feeds completion hiding, the dimmed modifier, the hover lead; enum variants through a path (`Side::Auto`) are now painted at the leaf (they were dropped); NEW `[lints] internal_use = "allow" | "warn"` (manifest, LSP tables, schema, book) — at `warn` every import/use outside the declaring module warns. OWNER to confirm: the declaring module is exempt; warns at imports. Three NEW ledger rows across E221/B382.
