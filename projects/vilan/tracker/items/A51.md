# A51 — A39's residue: the `List<T>` keyed exposure has NO macro form (`[expose(keyed)]` reads two written types; vilan has no associated types), refused with a steer to `Map<K, V>` or the hand-wired `expose_keyed`; `keyed_diff` is O(N) CPU per change per connection (bytes are O(change))

- status: OPEN — lane rpc-28's find (2026-09-05, Order 28)
- kind: design
- area: Reactive core & UI

Options for the list form: an attribute argument (`[expose(keyed = str)]`), a std wrapper type carrying `K`, or associated types (`T::Key`) if they ever land. For the CPU: an incremental diff keyed on the source's own change (a `Delta`-producing cell) instead of re-keying two snapshots; measure before a channel gets large.
