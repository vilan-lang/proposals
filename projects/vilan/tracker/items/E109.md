# E109 — E101's `pub` rule ships a cascade its own pin claims is gone

- status: OPEN — audit run 6's F10
- kind: bug
- area: Diagnostics
- source: audit run 6, repro verified

`pub fun helper() {}` emits TWO errors, the useless one first — `cannot
find 'pub' in this scope`, then the curated rule — while the pin's comment
says the misleading message is "GONE, not merely joined by a better one"
and asserts only the absence of the semicolon text. The `let mut` sibling
is clean; fix the ordering/suppression (the curated rule should consume
the scope miss) and make the pin assert the count. Also F21: `public fun`
is refused in a word the author never wrote — the fixed string should
substitute the marker actually written.
