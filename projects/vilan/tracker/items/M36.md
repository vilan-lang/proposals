# M36 — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s)

- status: OPEN — lane hygiene-27's find (2026-09-04, Order 27)
- status update 2026-09-05 (Order 28, lane perf-28 @0ce9b060): MEASURED and DESIGNED, not built — cold 370 / warm 180 / floor 190 ms (51% of a cold analysis) at loadavg 106; the build needs `World` serialization, which does not exist; the design is in the `#[ignore]`d measurement's doc and the changelog. A lane of its own.
- kind: performance
- area: Performance & footprint

Both differentials now run one process per program (N52), each paying a cold std analysis. A cross-process on-disk base cache keyed by M21's key (std sources + toolchain hash) would halve both; the LSP's M24 budget and M9's leak-soak rules apply to its eviction. Measure first: per-program floor with and without a warm on-disk world.
