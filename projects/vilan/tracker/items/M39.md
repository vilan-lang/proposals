# M39 — completion's remaining cost is documentation rendering: `doc_first_paragraph` reads the declaring module's whole text per request (0.415 → 0.324 ms with it stubbed)

- status: OPEN — lane completion-27's find (2026-09-04, Order 27)
- kind: performance
- area: Performance & footprint

Offering a name imported from a large module costs that module's text per request (cached per query only). Cache the first paragraph per declaration in the captured index (rebuilt with the module's stamp), or render lazily on `completionItem/resolve`. Gate: the keystroke completion budget under its profile (E141).
