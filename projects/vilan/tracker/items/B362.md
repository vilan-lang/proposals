# B362 — UNSOUND: a function with a `lazy` parameter called INDIRECTLY — `record_lazy_arguments` skips dispatched/indirect callees, so the call passes a plain value while the callee emits `__force(param)` → `cell.state` on a non-object, a TypeError at runtime; M81 made the NO-direct-call case correct (the parameter becomes eager) and left the MIXED case as it was

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: bug
- area: Type system & the type solver

Refuse at the COERCION (a `lazy`-parameter function is not a closure value — lazy.md's rule to write) or thunk at indirect sites through the callee's recorded convention; pins: a function value, a trait-object dispatch, the mixed direct + indirect case.

- 2026-09-21 (Order 39 GO): R4 RULED — refused at the coercion; lane solver-39.

- 2026-09-22 (Order 39 sweep): HALF built by solver-39 (b1bb6884 + 08cd75f2, R4) — the COERCION door is CLOSED (`coercible_function_signature` declines; `lazy` prints as part of a function type). STOP-AND-REPORT on the other half: refusing `lazy` on a TRAIT member contradicts the lane's own reading of the lazy paper; the owner rules. Stays open on that half.

- 2026-09-22 (the owner's rulings before Order 40): RULED (the dispatched half) — THUNK at the dispatched call site through the callee's recorded convention (a `lazy` parameter is part of the signature and impls must match, so the convention is known at the bound); the direct-call pins stay true; un-ignore `b362_a_lazy_parameter_reached_through_a_bound_is_still_unsound` red-first.
