# F31 — `clone_sites`' LAST-USE ELISION does not transfer to the native backend — the JS emitter elides a copy at a binding's last use; the Rust emitter must copy conservatively (four such copies in the exit program), so a native LIVENESS pass is C15's neighbour: compute last uses per binding and `move` there instead of `clone`

- status: OPEN — filed 2026-09-21 (Order 39 sweep; native-a-39's find 2)
- kind: perf (native)
- area: Native targets & rendering

Build: a per-function liveness over the lowered body (bindings are few and blocks are structured, so a backward walk suffices); at a use that is the last, emit a move; a NATIVE copy census beside the JS one so the number is watched. Pins: the four sites in `board.vl` become moves with the program still byte-identical; a program where the last use is inside a loop keeps its clone (the classic trap); the census file. Sized M.
