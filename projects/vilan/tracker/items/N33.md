# N33 — the ignore-gate's id shape is trivially satisfiable, and its scanner mis-parses its own fixture

- status: OPEN — audit run 5's F5+F6; the pin half fixed in-order (borrows.rs names C13 now)
- kind: process
- area: Testing & CI
- source: audit run 5

Two weaknesses in N31's gate. (1) `names_a_tracker_item` accepts any
capitals-then-digit run — `UTF8`, `ES6`, `ARM64`, `ISO8601` all pass — and
borrows.rs:7029 passed by naming P4c (a proposal slice label, not an item)
and C12 (closed) while the OPEN owner of the defect, C13, went unnamed
(fixed in-order; this item keeps the predicate question: e.g. require the
reason to LEAD with the id, and/or restrict the letter set to the tracker's
families). (2) The gate's own string-literal fixture is scanned by the
whole-repo sweep and passes by coincidence — `read_string_literal` swallows
the fixture and `read_attribute` picks the next quoted string as the
reason. Teach the scanner to skip the gate's own fixture block, plant-
proven.
