# B251 — a struct parameter reachable ONLY through another parameter's bound never grounds (`struct Held<T> { list: Signal<List<T>> }` leaves `T = any`), and the written form ACCEPTS `Held<i32, SignalCell<List<str>>>`: the bound is not checked against the written arguments at a struct's parameter list (UNSOUND)

- status: OPEN — lane b184-b218's find (2026-09-05, Order 28)
- kind: bug
- area: Type system & the type solver

Two halves: ground `T` through the bound at the literal (the recovery B186 built for calls, at struct literals), and check a struct's written arguments against its parameters' bounds. Pins: the hidden-parameter form grounding `T`, the written form refusing `Held<i32, SignalCell<List<str>>>`, a bounded-and-satisfied control.
