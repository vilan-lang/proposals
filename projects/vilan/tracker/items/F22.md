# F22 — ADAPTED INSTANCES are unmodelled natively: `adapt.vl` calls one `map` with an async closure at one site and a sync one at another (async-polymorphism.md A.1); the Rust emitter monomorphizes on TYPES and asyncness is not one — refused by name as 'an async closure as a VALUE'

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: feature
- area: Native targets & rendering

Key an emitted instance on `(id, type key, async bits)` reading `adapted_instances`; an async closure type spelled `Rc<dyn Fn(P) -> vilan_rt::executor::Boxed<R>>` with `pin_future` at the literal (both exist).

- 2026-09-22 (Order 39 sweep): the declared-async closure TYPE (native-b's Q1, 'split from F22?') was FIXED by native-a-39 ee9edd4e via `expects_async`/`expects_async_value` (asyncness per SITE, carried to the field) — so that half is NOT this item's; what remains for F22 is the adapted-instance shape proper, of which A40's suspending upgrade handler is now the named customer.
