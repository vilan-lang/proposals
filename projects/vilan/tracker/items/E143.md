# E143 — rename at a struct-init shorthand refuses (E134); should it EXPAND to `A { new = old }` instead?

- status: OPEN — lane editor-sync-27's find (2026-09-04, Order 27)
- kind: design
- area: Language server & tooling

Every edit the rename module emits is a span the caller rewrites with `new_name`; an expansion needs per-span replacement text and a signature change through `rename_edits_across`, `rename_spans` and every pin. The refusal names the expansion so the user does it by hand. Decide whether the expansion is worth the module's shape change.
