# B351 — REGRESSION (Order 36): a block hole whose tail is `context.run(v, || { <element .child(swap(s, |x| match x { .. => <e/>, .. => <div .child(swap(t, |y| match y { .. })) /> })) /> })` leaves `run<U>`'s `U` unresolved, and the emitter reaches `Slot::place` with no body — the B55 internal refusal, on a shape kolt compiled at 9b22ec36 (its `app_shell`). Twenty-line reproduction at the integrator's scratch `kolt-migrate/run-repro/src/client.vl` (case h): the same program with `run` replaced by a user generic `ident<U>(body: || U): U` PASSES, with an expression-bodied closure PASSES, with a single (un-nested) `swap` PASSES, outside a `{{ }}` block hole PASSES, with both closure parameters annotated PASSES — the trigger needs the EXTERNAL generic `run`, a block-bodied closure, and a nested `swap` whose `C` is decided by `match` arms. Workaround in kolt (views.vl:89, FIXME): `let shell: View = user_context.run(..); shell`

- status: OPEN — filed 2026-09-15
- kind: bug
- area: Type system & the type solver
- 2026-09-15 (bisect): NOT an Order 36 regression of the solver — the module-qualified reproduction (`ui::swap`, so it spells the same at both ends) FAILS on the Order 35 seal 9b22ec36 exactly as on d783fbf4. The shape was always broken in the VALUE form; kolt compiled at 9b22ec36 because its `app_shell` wrote the METHOD form (`.swap(..)`), which bound the closure's types through the receiver path — A99's removal of the methods makes the value-form weakness reachable (B347's family: `each_by` there, the nested `swap` under an external generic's block closure here). Culprit: none of Order 36's lanes; the fix is in the value forms' closure typing (A85-era), and the item's title word REGRESSION should be read as 'newly reachable'. Bisect cost: two builds (49611256, 9b22ec36).

Bisect to the commit; the fix is where the closure's return type is seeded for an external generic's closure parameter (B333's `adoptable_closure`/`call_return_clause` or B340's `note_callable_coercion` gate in `infer_type_inner`); pin the reproduction and the four passing variants; drop kolt's FIXME at the owner's word.
