# N118 — `std_twin_parity` COUNTS PRIVATE DECLARATIONS — a helper `fun` added in one ui twin and inlined in the other reddens the gate, so std-39 had to NAME the server twin's body (`place_when_some`) to match; the gate's contract must be written at its head (and F27's 'the parity gate learns fields' must not make it worse)

- status: OPEN — filed 2026-09-21 (Order 39 sweep; std-39's find 5)
- kind: hygiene (doc a gate's semantics)
- area: Hygiene & rot

Either the gate compares EXPORTED surface only (rec — a private helper is an implementation choice) or its header says 'every declaration, private ones included, so the twins read alike'. Decide, write it at the test's head, and add a pin that shows the choice (a private helper in one twin passes or reds by design). Sized S.
