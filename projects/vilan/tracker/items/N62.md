# N62 — two accuracy gains left unobserved by any pin: B254's subject narrowing removes bogus `call_graph`/`init_order` edges (strictly more accurate, nothing asserts an edge that should disappear); B244's `current_substitution` now ACCUMULATES outer bindings across nested instance emissions (constraint ids are per-declaration so the sets are disjoint; the cost of a deep monomorphization chain is unmeasured)

- status: OPEN — lane checker-29's find (2026-09-06, Order 29)
- kind: hygiene
- area: Hygiene & rot

One pin asserting a removed edge on the a49 shape; one measurement of the substitution depth on the deepest corpus instantiation, with a bound if it matters.
