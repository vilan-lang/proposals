# M62 — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it

- status: OPEN — Order 32's finds (2026-09-11)
- kind: perf
- area: Performance & footprint

Per-width visitor methods with defaults that ride the wide lane (so a codec opts in); the binary codec writes the narrow width; the JSON codec unchanged. Measure a frame-heavy exhibit before building.
