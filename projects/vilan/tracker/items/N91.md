# N91 — `macro` declarations are UNFORMATTABLE: `macro M { fun m() { } }` bails the printer, so std's `hash.vl`, `json.vl` (6) and `rpc.vl` (8) are silently unformatted files — and after the curation 15 of them are `export macro`, so the printer fix must handle the wrapper too

- status: OPEN — filed 2026-09-15
- kind: hygiene
- area: Hygiene & rot

A `print_macro` arm; idempotency pins over the three std files.

- 2026-09-17 (Order 37 GO): BUILD in lane dx-37.
