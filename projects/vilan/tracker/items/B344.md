# B344 — a GENERIC lazy parameter instantiated at a resource can slip the type check: the declaration check reads the DECLARED type (`lazy fallback: T` is not a resource) and the argument check reads `expr_id_to_type_id_map`, which a bare `Expr::Local` argument does not populate (transformer.rs's B328 comment records the same gap); a resource LOCAL is still caught by the R9 capture scan, a resource reached another way is not

- status: OPEN — filed 2026-09-15
- kind: bug
- area: Type system & the type solver

Resolve a bare binding's type through `variables`/`parameters` in the argument check, and the generic instantiation through `check_resource_generic_instantiations`'s family; pin both.
