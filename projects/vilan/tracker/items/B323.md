# B323 — an ANNOTATION-ONLY generic argument carrying a `context` clause is silently INERT at the construction site: `let held: Held<(|| void) context current> = Held { body = || print(..) }` inside `current.run(9, ..)`, then `current.run(3, || (held.body)())`, prints `saw 9` (measured) — the clause binds `T` from the VALUE's type and a literal is born clause-less, so the annotation never reaches B309's landing rule; not a miscompile (ordinary capture) but silent; the working spelling is a constructor `fun` whose parameter carries the clause (pinned as `b309_a_generic_argument_carries_the_clause_into_the_field_it_binds`)

- status: OPEN — Order 34's finds (2026-09-13)
- kind: bug
- area: Type system & the type solver

Make the struct-literal field landing consult the annotated EXPECTED type (`seed_tail_expectations` already carries it for an annotated `let`); pin the probe, keep the constructor shape as the control.
