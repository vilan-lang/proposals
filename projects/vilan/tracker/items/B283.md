# B283 — `at_settle` reached from a subscription's RELEASE HOOK resolves the turn the closure captured AT CREATION, so a `RemoteSource`/`KeyedSource` lease taken outside a turn and disposed-and-rebuilt INSIDE one gets NO same-turn cancellation — the plant run measured `same-turn:up=2 down=1` (two frames up, one down, for a rebuild the guide promises churns nothing); the guide's "re-renders in place churn nothing" is true only for subscriptions born inside that turn

- status: OPEN — Order 31's finds (2026-09-09)
- kind: bug
- area: Reactive core & UI

Resolve the deferral against the turn AMBIENT AT RELEASE (the draining turn, else the ambient one) rather than the captured one, or document the capture rule and make `lease` capture at the right site; pin the outside-in shape.
