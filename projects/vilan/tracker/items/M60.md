# M60 — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update`

- status: OPEN — Order 31's finds (2026-09-09)
- kind: performance
- area: Performance & footprint

A census of `get()`/`set()` pairs over containers in std (the pattern `let x = cell.get(); .. cell.set(x)`), each moved to `update`; a note in the memory-model tour naming the clone at a bound read; consider an editor hint for the pair.
