# E159 — a doc-facing enum arm can ship UNDOCUMENTED: `RpcError::Unavailable` (A52) reached no reader-facing page because `--test docs` compiles fragments and a stale enum LISTING still compiles (the guide counted five variants, the reference listed five, the status mapping named two codes)

- status: OPEN — Order 31's finds (2026-09-09)
- kind: editor
- area: Language server & tooling

A parity gate: every `[derive(Wire, Debug)] enum` fragment the docs show for a std enum lists every arm std declares (the `style_table_sync` idiom); pin on `RpcError` and `Reject`.
