# M87 — `KeyedCell::locate` (rpc.vl) COPIES the whole list to read one element — the same shape M86 fixed on `ListCell` (`size()` through `SignalCell::get` deep-cloned 5.8 M of 6.3 M Ir per push); a borrowing read (`peek`-shaped) or a `Shared`-held list beside the notification cell

- status: OPEN — filed 2026-09-24 (Order 41, mid-order: collections-41's report)
- kind: performance
- area: Performance & footprint

Discussion: collections-41's find 3 (2026-09-24); std's rpc lane

- 2026-09-25 (Order 42, collections-42): BUILT (01eb9b00): `locate` reads `self.elements.value.read().get(at)` (what `SignalCell::get` reads) and copies ONE element — 2,881,649 → 21,889 Ir per locate into 1,000 keys (the empty loop 41–62); the list not moved into its own `Shared` (touches all of `KeyedCell` — a `SignalCell::peek` in reactive.vl would be the cleaner surface, reactive-42's call); pin `m87_keyed_cell_locate_copies_no_whole_list` (red first: `wholes=1` per hit); keyed rpc pins and the wire unchanged. FIND: `KeyedCell` does not build natively at all → F42.
