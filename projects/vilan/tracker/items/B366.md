# B366 — a nominal declaration's GENERIC PARAMETER arrives in two spellings: a generic ENUM's payload records the constraint id itself (typed `Any`), a generic STRUCT's fields record `Generic(..)` nodes — invisible on JS, ~40 lines of double handling in the Rust emitter

- status: OPEN — filed 2026-09-21 (Order 38 sweep)
- kind: hygiene
- area: Type system & the type solver

One spelling (rec: `Generic(..)` everywhere); the Rust emitter's `bind_parameters` loses a branch; corpus byte-identical is the gate.
