# F15 — the THREE-LAYER `std::ui` rule and an N-way twin-parity gate: today two `ui.vl` twins (browser DOM, process SSR) share one public surface and the parity gate is structurally TWO-valued; a native twin makes it three, and nothing today states the rule that every twin exposes the same surface (`ssr.md`'s 'same public surface' is a sentence, not a gate). F1's S2 cannot be sized without the rule

- status: OPEN — filed 2026-09-14
- kind: design
- area: Native targets & rendering

State the rule (one surface, N twins; a member added to one twin is added to all in the same commit or refused by the gate); generalize the parity gate from a pair to a list of layers; the docs' 'same public surface' sentence becomes the gate's citation.

- 2026-09-17 (Order 37 GO): R4 RECORDED, not ruled (2026-09-17): the three-layer rule is part of the owner's F17 conversation; F1 S2's sizing waits with it.
