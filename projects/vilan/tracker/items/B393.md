

- 2026-09-24 (Order 41, solver-41): PROBED — JS and native AGREE on `Self::tag()`, `Holder<T>::tag()`, method calls, recursion with a written turbofish and calls through a generic instance; the gap opens only where the analyzer accepts a bare `Holder::tag()` whose bounded impl generic nothing binds — NATIVE is the wrong side (silent `A A`), JS loud. Not an emitter fix: filed as B403 (the analyzer's cannot-infer check). N125's four functions stay per-emitter. Closes at the sweep with B403 as the follow-on.
