# Vilan tracker - archive

Append-only; one tombstone paragraph per closed item, the file deleted,
the INDEX row dropped, the ID retired. The eras before this file, frozen
with their tombstones in place: `archive/backlog-2026-08-28.md` (the
2026-08-18 -> 2026-08-28 tracker, closed by the N17 migration) and
`archive/backlog-archive.md` (that era's tombstone file, terminal);
before them, `projects/vilan/proposal/backlog-2026-07-18.md` and the alpha-era
`projects/vilan/proposal/backlog.md` (both moved with the papers, 2026-08-28). A `backlog <ID>` citation resolves here or in that
chain when it does not resolve in `items/`.

- **N17. Per-item tracker files — the full-repo restructure — CLOSED 2026-08-28** (EXECUTED in its serial window, the same day the owner ruled it should happen: runbook `proposal/tracker-migration.md` (Order 18, lane n17-plan), execution per its §8 — the freeze-and-stub commit, the materialization (44 items, verbatim IDs, INDEX, chronicle, archive chain-head), and the fix list (the index gate learns projects; the dangling-cite rule learns code spans, plant-proven; the pointer notes). Verification passed: 44 = 44 census with the tombstone-HEAD refinement, hygiene green, 40 sampled body lines lossless, narrative preserved. Two of its own tools needed live corrections in their first minutes — the cite rule's TOML false positive and the census's mention-vs-head coarseness — both fixed and plant-proven, which is the gates working. This tombstone is the first item closed under the discipline it created.)

- **C11. A resource born and consumed as an expression temporary was never dropped — CLOSED 2026-08-29** (Order 19, wave 2, lane lastuse-s3, vilan 4c476ad1 within the S3 arc: a resource temporary is owned by its STATEMENT — the lift both lowering options had to invent, built once; the fd staircases from temporary-drop.md's tables are pins (straight-line back-to-baseline after the statement; the loop peak collapses); the conditional temporary (a constructor on the right of `&&`/`||`) is REFUSED with a fix-naming message — the one place the arc makes a compiling program an error, exactly as ruled; `File::open(p).read_at(b, 0)`'s receiver now closes after its statement. The wider S3 shipped around it: disposal at LAST USE per the ruling (the join-placement and dead-binding calls recorded for ratification), the `[extern(…, retains)]` contract with the std audit, the ordering amendment (family breaking). temporary-drop.md is CLOSED with its §12 — statement-end carried forward as the special case, its measurements now pins. The lane's semantics-level finding: `Option::replace` declares a bare loan and keeps the value — filed as B153; C11's temporary predicate stays narrowed to views and bare self receivers until that fix.)

- **G10. `[[build.hook]] inputs` declared to the stamp, not the watcher — CLOSED 2026-08-29** (Order 20, wave 1, lane hook-watch, vilan 893e69a6: the wake-up set extends to every declared hook input through the SAME recorded-inputs mechanism `asset::read` already rode — `RECORDED_INPUT_PATHS`, one recording door in `BuildHooks::run`, recorded before anything runs so a fresh or failing hook still tells the watcher what it reads; a declared directory means its tree, the stamp's own reading; declared `outputs` deliberately excluded and the negative pinned. Red-first the honest way: both new watch-round pins waited out the full 300 s liveness bound against the unfixed tree. The banner now names both halves of its set — its understatement is how the defect was first diagnosed backwards. Recorded, not fixed: an unconditionally-rewritten declared input can spin mtime rounds forever, the same exposure `asset::read` inputs and `.vl` sources already carry, documented at `RECORDED_INPUT_PATHS`.)

- **G11. `vilan build --explain` — every output names its contributors — CLOSED 2026-08-29** (Order 20, wave 2, lane explain, vilan d2a56243, filed and shipped the same day on the owner's word: builds exactly as `vilan build` does then prints the story — one block per `dist/` output (role, emitting const sites, bundle source, hook verdict in the terminal's own words) and one per tracked input with what it invalidates; line-oriented, greppable, docs-as-spec in `appendix/cli.md`. One minimal record extension where none existed: `ConstFact` stamps the enclosing `const` site beside each channel fact — the granularity the compiler actually keeps, resolved through the same map errors render against, paid only under the flag. `--stdout` is refused with it. Five e2e pins over one project exercising the whole surface, seven plants proven. Honest cut recorded: no `generated shell` role — nothing in this tree writes one, and the report does not invent roles for files the build does not produce.)

- **B153. `Option::replace` declares a loan and keeps the value — CLOSED 2026-08-29** (Order 20, wave 1, lane b153, vilan e1d28ac3, family MISCOMPILE: `external fun replace(&mut self, value: T)` spelled its stored value BARE, so `slot.replace(r)` destroyed the resource twice and left `r` readable after the move — the honest declaration is `own value: T`, the one std surface where an `external` has no body for the move-clean check to read. With it corrected, C11's temporary predicate WIDENED back to the ruled rule — a resource temporary in ANY loan position is its statement's to destroy — with one new spec exemption: a `[extern(…, retains)]` argument is left to leak rather than freed under a host still reading it. Six pins each red-first, including the pre-existing replace pin that reddens on the widening WITHOUT the declaration fix — the mechanical proof the original narrowing was correct at the time. Zero corpus goldens moved. The same-shape sweep cleared `Shared::new` (R10-fenced) and `Context::run` (an honest loan) and found the internal `NativeMap::insert` genuinely broken — filed as B154, pinned `#[ignore]`d with the two candidate fixes and why each is a ruling.)

- **E98. A browser build constructing a `@process` resource draws the coloring diagnostic twice — CLOSED 2026-08-29** (Order 20, wave 1, lane diags, vilan 5a04aca2: two general causes, not one symptom — the synthetic destruction edge carried no site (its diagnostic anchored inside the library that seeded the requirement) and coloring had no dedup at all. The edge now carries the CONSTRUCTION it answers to, threaded from the drop planner across all five shapes; violations collapse on (anchor, layer, origin), which also closed a third unfiled face: a family fence walked per host drew three identical diagnostics for one broken promise. Ten pins with the negatives beside them — distinct mistakes still each report.)

- **E100. Module-load parse errors carry no span — CLOSED 2026-08-29** (Order 20, wave 1, lane diags, vilan f84ed971: the loader had `error.span` in hand and rendered it into prose with an empty span — 798 errors at line 1 of an 18k-line generated file, measured. A loaded module now carries (span, reason) pairs like the entry always did, both loader paths together, so `vilan build` points at `util.vl:2:11` with a caret. One consumer keeps prose deliberately: `vilan check <library>` registers no sources. Pinned per case, red on the unfixed tree. Audit run 4's E102 records the one narrowing the fix introduced — the dedup key dropped the message — left open.)

- **E101. `str::index_of`/`find` + three cause-less diagnostics — CLOSED 2026-08-29** (Order 20, wave 1, lane diags, vilan 98c32078: shipped as `index_of`/`last_index_of` — the house vocabulary settled it, `List::index_of` exists and `find` takes a predicate here — in `std::option` beside `strip_prefix`, absence as `None` per `substring`'s doctrine, no `from` offset (it would reintroduce the clamp question). The three curated refusals: `pub` names what vilan does not have and what `export` is not; `let mut` names the two binding forms; a literal `{` in an i-string names `\{` — each recognized structurally, each with the misleading message proven GONE. The audit's follow-ups: the unit sentence added same-cycle; `std::markdown`'s hand-rolled O(n·m) `index_of_from` helper recorded as wanting its own item when the `from`-bound question is ruled.)

- **M13. The perf baseline is stale, and its rows carry no provenance — CLOSED 2026-08-29** (Order 20, serial close step, vilan 46415bf2 + proposals e2efa3b: both legs in order. Rows gain `load` (1-minute loadavg at summarize time) in BOTH binaries' harnesses and each run ends with a `section:"run"` provenance row naming its subject count — run 3's finding closed verbatim. Then the quiet-machine full re-record, all five subjects, zero skips: 57 rows + 2 provenance rows at load 0.53–0.87, header naming date/tree/machine. The field proved itself on its first outing — the first recording run's own release build polluted the loadavg window (8.39→3.36) and the rows said so, so the recorded baseline is the settle-then-measure re-run.)

- **A29. `DuplexEnd.me` never cleared — a session's server and wire retained per disconnect — CLOSED 2026-08-29** (shipped in Order 19's S1 (lane graph-repairs) exactly as filed — `clear_on_frame` called from `dispose` and `drop_session`, one line each — and pinned by the standing SCC heap-snapshot gate, whose post-disposal zero-cycle contract is what holds the repair; recorded in v0.39.0's A29 entry. The item outlived its fix by one close: Order 19's records missed it, and Order 20's detail-pull for the owner's ratification queue found the stale-open row — the 022 lesson's small recurrence, caught by reading rather than by a gate.)

- **A28. `map`/`combine`/`flatten` derivations undetachable — CLOSED 2026-08-29** (the leak shipped FIXED in Order 19's S1: derivations register with the ambient owner and die with the boundary that built them, the 25→0 pin holding; the carried remainder — refuse the OWNERLESS derivation outright — was RULED 2026-08-29: **DEFERRED**, leak-as-today stands. The owner's reasoning, recorded: cases that work fine today would need explicit handling under refusal, and the footguns are so far hypothetical — so defer, and REFUSE later if the footguns become common or hard to diagnose. The trigger is stated; `register_with_owner`'s doc comment is the living record; a future refusal is a new item with its evidence.)

- **B150. `drop(x)` is not exception-safe — CLOSED 2026-08-29** (shipped in Order 19's S3, v0.39.0's miscompile entry: `drop(x)` no longer takes its scope's teardown with it — the sink's statement IS the binding's last use, so the `finally` net closes there and a panic between acquisition and the explicit drop releases on the way out. §6.5 records that the planned exception was unnecessary. Row outlived the fix; found in Order 20's ratification detail-pull.)

- **B151. mR2 overwrite double-drops when the RHS throws — CLOSED 2026-08-29** (shipped in Order 19's S3, v0.39.0's miscompile entry: an overwrite that destroys a resource computes the new value FIRST — a panicking right-hand side no longer leaves the scope's `finally` to destroy the same value twice. Row outlived the fix.)

- **B152. bare `ret <expr>` emits `return return` — CLOSED 2026-08-29** (shipped in Order 19, v0.39.0's tooling entry — the doubled keyword produced a bundle that does not parse. Row outlived the fix.)

- **C12. The view-capture ban documented but unenforced — CLOSED 2026-08-29** (shipped in Order 19's S5 as v0.39.0's third breaking entry: a closure may no longer capture a view binding, refused at the CAPTURE with both fixes named, the capture rule itself finally spec §6.9, the estate census ZERO. The recorded residue — the view-parameter-through-storing-callee escape — is C13, filed and pinned ignored as §6.9's honesty limit. Row outlived the fix.)

- **G13. `dist/` never sweeps a bundled asset the build no longer names — CLOSED 2026-08-29** (Order 21, lane g13, vilan 0b065062: a parallel `.vilan-bundled` record keyed per leg — a kind row and a bundled row name their files by different grammars, so one file would need two row grammars and a guessing pruner — with the record primitives generalized to one format and one pruning law; the prune removes only what NO leg's record names, so the two-legs-one-target case survives. Both exhibit cases pinned dead: the deleted-from-a-listed-tree copy and the digest-orphan under a fingerprint change. The lane's own plant audit caught a vacuous test it had just written — the later-leg re-copy hiding a wrong prune — and flipped it. Deferred, recorded: emptied subdirectories are left behind (their removal wants its own ruling); E92's general sweep unchanged.)

- **G14. A watch round lost to a transient action failure is lost for good — CLOSED 2026-08-29** (Order 21, lane g14, vilan 00fe7a84, per the ruling: a failed round keeps the OLD snapshot so the same difference re-fires on the next poll, retried ONCE; a second consecutive failure adopts and waits — never a third attempt, never a hot loop. Threading the verdict made it readable at all: ExitCode was write-only, so the build/check/test rounds now return RoundOutcome. The `run --watch` path is honestly exempt — its rounds have no legible verdict and the lane refused to invent one, with the reasoning at the closure. Both pins red at the full liveness bound; the retry pin deliberately rides `wait_for_in`, not the nudging wait, so only the retry can produce its second run.)

- **A30. `ReactiveClient` disposes itself at terminal `Closed` — CLOSED 2026-08-29** (Order 21, lane a30, vilan 41467dec, per the ruling: one `close_for_good` — state set BEFORE dispose so a `connection_state` observer never sees a half-torn client — on both of `reattach_mirrors`' refusing arms; the redial arm untouched and negatively pinned; double-dispose pinned harmless. The connect-time contract mismatch has no client yet, so two paths is the true count there; the THIRD terminal `Closed` (handle_drop's spent retry budget) has no client in scope and is A31's question. The routes half is pinned at unit level — reaching it at e2e needs the generated client to expose its ReactiveClient, a public-surface decision declined.)

- **B154. The internal `NativeMap::insert` frees the caller's value while the table holds it — CLOSED 2026-08-29** (Order 21, lane b154, vilan 83039f08, per the ruling: one word — `NativeMap` joins `resource_rejecting_containers` — and the un-ignored pin is the red-first proof. The two-head shape collapsed the principled way: R11's in-body check stands down when the callee's instantiated signature is already refused AND reported, so `stash<T>` still reports; negatives held by diffing eight probe programs' full diagnostic output against a pre-fix baseline binary, byte-identical. The stand-down's first draft was too eager — audit run 5 caught it swallowing an INDEPENDENT second offense the same day, fixed as E104. Breaking census: a deliberate zero (the only program that stops compiling IS the use-after-free, via a non-public import).)

- **F14. `to_lowercase_ascii` is not ASCII — CLOSED 2026-08-29** (Order 21, late lane f14, vilan daac93ba, per the ruling: the rename (`to_lowercase`, the full-Unicode mirror of `to_uppercase`) PLUS genuinely-ASCII siblings implemented as plain vilan — keeping the compiler surface at the rename and const-eval agreeing for free — with doc comments on all four naming the difference. Eleven std call sites migrated by intent (ten `_ascii`, one full: markdown's heading_id), and the migration FIXED TWO LATENT BUGS: U+212A KELVIN SIGN satisfied `is_name_start` through the full fold, and `skip_raw_text`'s index-preservation claim was false under one-to-many mappings. The break is behavioral and undiagnosable — the old name keeps compiling and starts meaning what it says — acceptable on the zero-app-caller census alone, and the entry says so twice. Cost note filed as M14 (the skip_raw_text cliff).)

- **E102. The module-parse-error dedup dropped the message from its key — CLOSED 2026-08-29** (Order 21, lane smalls, vilan 3840f89c: key widened to (path, span, reason); the two-distinct-errors-at-one-offset case proven CONSTRUCTIBLE by fuzzing — a stray `"` gets the lexer and the parser refusing at the same span — and both directions pinned red-first.)

- **N29. `retains` documented only where no extern author looks — CLOSED 2026-08-29** (Order 21, lane smalls, vilan 2b3c7fd9: the flag named at every extern form reference — grammar, types spec, tour, cli appendix — pointing at spec/memory.md for the semantics. The sweep found the production omits two MORE accepted forms, filed as N32.)

- **N30. An empty directory appearing where a listed one was missing starts no watch round — CLOSED 2026-08-29** (Order 21, lane smalls, vilan 5e459c57: a recorded directory contributes its own snapshot entry (existence + membership); reproduced live against a `--watch` session before any edit; the e2e pin waited out the full liveness bound on the unfixed tree. Audit run 5's G16 records the residual stamp-vs-watcher asymmetry on empty NESTED directories — safe direction, spurious round only.)

- **N31. An `#[ignore]` reason must name a tracker item — CLOSED 2026-08-29** (Order 21, lane smalls, vilan d1ab6e4f: the gate beside ci_ignored_pins' existing checks, four exact-string allowlisted non-bugs (the sweep found the mdbook gate the brief did not know), plant-proven both branches. Its first catch closed by its own rule at integration: the async-assimilation ignore's item existed all along — B149, "pinned but never itemed" in its own title — and the reason now names it. Audit run 5's N33 records the predicate's looseness (capitals-then-digit passes ARM64) and the scanner's fixture self-parse; the borrows.rs reason was re-pointed at open C13 the same day.)

- **B159. S3's teardown-region widening dead in every nested scope — CLOSED 2026-08-29** (Order 21, FIX-NOW lane b159 off the audit's same-day finding, vilan 7170b530: the extents map now keys on chain.last() — the INNERMOST statement, the only key that ever answers the widener's question — which also removes the old key's spurious over-widening at top level. Five shape pins each red-first with real ReferenceErrors; the tail-vs-statement differential pinned both ways; the audit's seven probes re-ran with exactly the predicted one-line diffs; zero corpus goldens; the spec gained the missing widening paragraph. RELEASED in v0.39.0 (the ReferenceError is loud, not silent); the fix ships with the next cut.)

- **E104. b154's R11 stand-down suppresses an independent second offense — CLOSED 2026-08-29** (Order 21, FIX-NOW lane e104 off audit run 5's F2, vilan df2b18d7: the stand-down asked PER OFFENDING TYPE — the covered set of constraint ids from refused-and-reported signature types, with a non-empty guard so nothing is vacuously covered. The suppression lived one union-tip day; both halves of b154's rule pinned: seen-once stays once, unseen never silent. The lane reshaped two of its own draft pins after catching one vacuous and one asserting around a pre-existing multiplicity.)

- **B160. raw/typed 1:1 interchangeability — CLOSED 2026-08-29, REFUSED by ruling** (the owner, with the downsides in hand: "Keeping `raw` the same as its method seems unnecessary… The methods are higher-level than `raw`. They shouldn't have to conform." The layering is the ruling: `raw` is the low-level verbatim escape, the typed methods are a higher-level surface free to normalize, and the block↔chain refactor's inverse stays PARTIAL BY DESIGN over typed-method chains — the refusal-to-offer is the honest report, permanently. The mixed-spelling census was never needed; the fence on std evolution is never written; css-s5's recorded partiality note is the standing text.)

- **A27. `std::dom` cannot listen on `window` at all — CLOSED 2026-08-31** (Order 22, lane pointer: `Window` is now a real handle — `window()` returns it, it carries `listen` like any element — and `listen` returns a `Subscription` everywhere instead of void, so pointer capture can end. `pointer_x`/`pointer_y` land beside it. Kolt's drag handler drops both workarounds; 037's std gaps all shipped.)

- **A31. every terminal `Closed` disposes: the third path joins the ruled two — CLOSED 2026-08-31** (Order 22, lane a31, ruling (b) the safer option: `close_for_good(socket)` is now the SOLE writer of `Closed` — all three terminal paths route through it — and `on_terminal: Shared<List<|| void>>` is the one hook; `dispose_on_close` rides it. `reattach_mirrors` lost its client parameter as fallout. Paper amended; red-first pins per path.)

- **A33. the Source-not-Signal audit: bindings demand a `Signal` they never write — CLOSED 2026-08-31** (Order 22, lane a33, the owner's charge verified whole: 8 std ui bindings widened to `<S: Source<T>>` bounds — none of them wrote. AttrValue/Slot impls stay concrete pending B158's blanket collapse, now buildable. The audit's side-find filed as B168.)

- **B148. `str + <any struct>` type-checks and renders the runtime tuple — CLOSED 2026-08-31** (Order 22, lane b148: `+` gained an ADMITTED SET — str + renderable concatenation (deliberately NOT B=Self: the i-string desugar rides it) and T+T numerics; everything else refuses with the operand named. The lane also confessed a `pkill -f` that killed three sibling suites — the ban now leads every brief.)

- **B158. a blanket impl (`impl type T with Trait<T>`) is accepted, then ICEs at the first dispatch — CLOSED 2026-08-31** (Order 22, lane b158, the owner's MaybeSignal design ruled support-with-specificity: `impl_select` orders candidates — a constructor impl beats the blanket, unorderable pairs refuse at the call with both named. spec/types.md §5.4. The union-type proposal dissolved into this pattern.)

- **B163. `if`-expression arms are never unified — CLOSED 2026-08-31** (Order 22, lane b163: `Constraint::IfArms` at priority 12 — match's own unification rule, arms unify FIRST then meet the annotation, per the owner's constraint-annotation semantics. The owner's legal/illegal branch examples are the pins.)

- **B164. a supertrait's type argument is not substituted through a sub-trait bound — CLOSED 2026-08-31** (Order 22, lane b163: `trait_with_supertraits_at` now substitutes the sub-trait's arguments into the supertrait's, so `Signal<T>: Source<T>` reaches consumers typed.)

- **B166. struct-field ASSIGNMENT is entirely unchecked against the declared field type — CLOSED 2026-08-31** (Order 22, FIX-NOW lane b166 off the owner's kolt report: `Constraint::FieldAssignment` routes assignment through the SAME rule as literal construction — `check_field_value` extracted, one rule both doors; compound rides `stored_value_id`; nested/indexed same path. Estate census 149/149 both ways via shadow-migrated copies; zero newly-refused — kolt's DragHandler already wrote `Some(handler)` by hand. Side-find: no compound assignment can currently land a wrong type (operator traits all return Self) — recorded in the pin, no item.)

- **B167. an `if`-`is` pattern binding of CLOSURE type is never emitted — CLOSED 2026-08-31** (Order 22, FIX-NOW lane b166, the DIAGNOSIS CORRECTED MID-FLIGHT by the owner's falsification — any name reported undefined, so not shadowing: the `is_bindings` alias table had ONE reader while locals become JS in TWO places; the `Expr::Call` named-callee fast path never consulted it, which is exactly why closure payloads (uses are calls) broke while i32 reads and `match` worked. Fix: the fast path falls through for aliased locals and emits `$a[1]()`. 9 pins incl. the struct-payload control. The stale spec sentence it exposed is B171.)

- **E103. the `Some`-import steer is lost when the `Option` arrives through a `List` method — CLOSED 2026-08-31** (Order 22, lane smalls: `scope_miss_steer` gained the 3-door shape — value, pattern, type each steered from their own index. Pins retargeted twice under the prelude (Some went ambient); audit run 6's F13 caught the first retarget landing on the wrong door — value control now uses `format`, pattern pin uses `Ordering::Less`.)

- **E105. an unterminated `css` block completes with ordinary scope where CSS is promised — CLOSED 2026-08-31** (Order 22, lane smalls: the completion boundary now respects the css block's promise; css-context items only.)

- **G15. a `[[build.hook]]` directory input that is a symlink re-runs the hook every build, forever — CLOSED 2026-08-31** (Order 22, lane watcher: `insert_watched_input` stats through `fs::metadata` (follows the link) so the stamp and the watch agree on the target. The symlink FAMILY the audit found around it is Order 23's lane: G17/G18/G20/G21 + G19 ruled a supported spelling.)

- **G16. the watcher and the stamp disagree on empty nested directories — CLOSED 2026-08-31** (Order 22, lane watcher: directories record their OWN entry in the snapshot, so an empty directory's appearance/disappearance moves the stamp both ways. Pinned with the two-way fixture.)

- **M14. a std function that stops being an extern changes its price, and nothing prices it — CLOSED 2026-08-31** (Order 22, lane m14: the windowed compare — 182,015 candidate strings narrowed to 5 real movers, each priced and recorded in the perf ledger. The method is the deliverable; reruns are cheap.)

- **N32. the grammar's `extern-args` production omits two forms the parser accepts — CLOSED 2026-08-31** (Order 22, lane smalls: grammar.md gained both forms; parser unchanged — the text was behind, not the code.)

- **N33. the ignore-gate's id shape is trivially satisfiable, and its scanner mis-parses its own fixture — CLOSED 2026-08-31** (Order 22, lane smalls: the id shape now requires a tracker-real id; the scanner's fixture parse fixed and pinned against itself.)

- **A32. custom signals with compatibility: `Source` is the trait, the question is reach — CLOSED 2026-09-01** (Order 23, the arc's spine: traits-core shipped B162+B161, reactive-replumb built the rest — `trait Signal<T>: Source<T>` with `set`, `update` LOCKED to `SignalCell` per the ruling, `Signal::new` a default body returning the cell, the clamping-setter exhibit pinned four ways and RUNNING through both ui layers. Three general fixes forced en route, each pinned: trait-assoc fns bind the TRAIT's generics from the call; bound-site calls no longer inherit context requirements from same-named members on unrelated traits; impl_select grounds a bound's binder before its instantiation tier. The paper's §14 as-built section records all eight divergences; the parameter-position sugar (§7.3) is UNBUILT and unruled.)

- **B156. a manifest-configurable prelude — CLOSED 2026-09-01, ARC COMPLETE** (built Order 22, closed when the templates adopted per the stated condition: Order 23's templates lane set `prelude = "std::web"`/`"std::prelude"` in all three scaffolds with scaffold-and-build proof, module-carried `style::`/`ui::` spellings demonstrated. §15 census amendment: `SignalCell` joined the web set beside trait-`Signal`. B172 (qualified type paths don't parse) is the standing wart the adoption exposed.)

- **B157. static-or-dynamic component values: the trait-bound pattern — CLOSED 2026-09-01** (Order 23, reactive-replumb: std `MaybeSignal<T>` with both blankets — `impl type T` and `impl type S: Source<type T>` (Source not Signal: `bind` only reads; a writing component asks for `Signal<T>` directly). AttrValue/Slot collapsed to blankets on both ui twins, `str`'s own impls outranking pinned. The owner's union-type proposal fully dissolved into this pattern.)

- **B161. a trait annotation as a checked constraint on bindings — CLOSED 2026-09-01** (Order 23, traits-core, the owner's exact semantics: `let count: Signal<i32> = SignalCell::new(1)` keeps the concrete type, the annotation CHECKS it; branches unify first (B163's rule) then meet the annotation; universal across traits. §12.2's destructor hole now shut by SEMANTICS not the ban — the binding IS its concrete type, so the destructor runs, pin converted. Admitted at the LET position only; the parameter sugar stays unbuilt, question 11 open on the paper.)

- **B162. trait-associated functions — CLOSED 2026-09-01** (Order 23, traits-core, per the ruling: `Trait::func` resolves to the TRAIT's own default body, ranked above impl statics; `Type::func` reaches the impl's declaration; body-less-on-trait-path and no-override-on-type-path both get curated refusals with steers. Type::func does NOT inherit the trait's default — traits-core's deliberate boundary, one-filter change if ever wanted. The trampoline emission cost recorded in the paper's §14.)

- **B165. `type` binders do not resolve inside an impl head's bounds — CLOSED 2026-09-01** (Order 23, reactive-replumb's first commit: `register_subject_binders` recurses a binder's bounds for NESTED binders before the binder registers. 5 pins, 4 red-first. Unblocked B157's generic blanket — and exposed the impl_select grounding miscompile, fixed in the same lane.)

- **B169. an unbounded generic right operand still escapes `+`'s typing — CLOSED 2026-09-01** (Order 23, operators lane: TWO carriers found — the grounded() gate skip AND compare_type(i32, Generic) answering true; `unbounded_generic` now forces refusal. Pre-fix: `total + value` returned the string "11,2" typed i32. Superseded on the bounded side by B176/B179's fuller family.)

- **B170. binary operators skip the check for non-nominal LEFT operands — CLOSED 2026-09-01** (Order 23, operators lane, audit 6's F3: the dispatch loop continued unless Struct|Enum, so tuples/arrays/closures/void reached no check at all — `(1,2) + 1` emitted "1,21", `(1,2) == (1,2)` ran false, a closure concatenated its own source text. Fixed AT THE GUARD; the sibling operators closed with it, and non-nominal impl dispatch (`impl (i32,i32) with PartialEq`) works for the first time — it had resolved correctly all along and was never reached. Type::Trait skip-listed under B175's inference wart.)

- **B176. a bounded generic right operand of `str + x` never calls its `Display` impl — CLOSED 2026-09-01** (Order 23 FIX-NOW, same day as the find: there was NO routing to repair — concrete structs are refused at `+`, so nothing had ever routed Display through it. Built: `string_render_bound` admits a bound promising `to_string(self): str`, the transformer rewrites the OPERAND only, a never-silent guard makes unresolvable dispatch a compile error. The i-string hole was byte-identical and `+=` had it too — one rewrite covers all three. 10 pins run emitted JS; spec §5.7 names the render bound as the fourth admitted operand.)

- **E107. no completion at a builder chain's dot on its own line — CLOSED 2026-09-01** (Order 23, editor-health — the reported cause was WRONG: the line break was innocent; member completion was silent on any call receiver whose callee had no DECLARED return type. The analyzer had inferred and memoized the answer and never exported it; now `Program::inferred_return_types` + one fallback. 7 pins, both spellings, incl. the generic-builder specialization guard.)

- **E108. a type-position unresolved name in a dependency attributes to std's lib.vl — CLOSED 2026-09-01** (Order 23, diag-smalls: the prepped_type_locals drain runs after every walk and inherited the LAST walk's source id. Both pushes routed through push_in_source; the bare-trait sibling had the identical defect and was fixed red-first in the same move. Simpler repro than filed: a plain same-package module.)

- **E109. the `pub` rule's cascade; `public` refused as `pub` — CLOSED 2026-09-01** (Order 23, diag-smalls: `TerminatorRecovery::{Declined,Kept,Dropped}` — the marker statement is reported-and-DROPPED so the analyzer stops finding 'pub' unbound beside a rule that already refused it. F21's fixed string parameterized; `public` had already reached the curated rule since E101 — the filing's wording was the defect. 4 pins red-first + control.)

- **E110. the web-set steer's claimed LSP suppression does not exist — CLOSED 2026-09-01** (Order 23, diag-smalls, door taken: make it HONEST — and the suppression was rejected on merit, not cost: the quickfix it would have suppressed produces a working program (F2's fix removed the module-carried names). Non-vacuity plant-proven: wiring the claimed suppression reds the quickfix pin 1→0.)

- **E111. semantic tokens and inlay hints drift after a `Some(let …)` pattern — CLOSED 2026-09-01** (Order 23, editor-health, the owner's live site at kolt interact.vl:129: drift = +4, the length of `let `, rebuilt arithmetically under a flag that CANNOT distinguish binder tuples from keyword-covering pattern spans — B167's shape, one datum recomputed by a consumer. `Pattern::Binding` now CARRIES the name span from the parser; flag and arithmetic deleted. 6 absolute-offset pins red-first; the `Some(let [a, b])` array case found and covered en route.)

- **G17. a `generated` root through a symlink fails open — CLOSED 2026-09-01** (Order 23, symlinks lane: the containment check climbed the canonical path only, so a linked generated root's products fell outside it — fmt rewrote a product and the hook re-staled, §12.1's loop LIVE and measured. Two ladders now: spelled first, resolved second, every containment canonical both sides.)

- **G18. fmt/watch follow directory symlinks unguarded — CLOSED 2026-09-01** (Order 23, symlinks lane: `TreeWalk` with a device+inode visited set, scope = resolved project + declared generated roots. The tracker's single-link hang was OVERSTATED — ELOOP stops one self-link after 41 duplicate reports; TWO links genuinely hang, and the cycle pin uses that fixture with the red proven via timeout guard. Residual: file-level double-collect filed as G22.)

- **G19. the const channel's lexical fence vs symlink escape — CLOSED 2026-09-01, RULED a supported spelling** (the owner: "Why would it be? Do symlinks cause problems for the watcher?" — doctrine written at spec const.md §9.2 and echoed in three docs pages; both fence messages now speak about paths AS WRITTEN instead of implying symlinks are illegitimate; F24's canonicalize-both-sides settled under the ruling, not fenced. Windows half stays on audit run 7's charter.)

- **G20. file-mode `check` ignores the manifest — CLOSED 2026-09-01** (Order 23, symlinks lane: `file_project` + shared `owning_package` adopt source root, workspace, prelude, options, platform — NOT [build] hooks, stated in the doc. All three lies reproduced before, gone after, 4 pins red-first.)

- **G21. the watcher gives const directories the hook reading — CLOSED 2026-09-01** (Order 23, symlinks lane: `RECORDED_INPUT_PATHS` now maps path → InputReading (Listing|Tree, Tree wins on overlap). The 'HMR-skip half unconfirmed' note RESOLVED confirmed harmless — the stamp re-key catches it.)

- **M11. the compiler's largest retentions are invisible to the leak tally — CLOSED 2026-09-01** (Order 23, perf lane, and the open question ANSWERED: not a leak. The BASE_CACHE keys on the REFERENCE SET, not the text — 22 keystroke states of one import line retain THREE worlds. Two tally sites added (BaseCacheWorld 449,301 B for 3 worlds, MacroFailureText), record-on-insert release-on-evict, `base_cache_retained()`; the growth pin is what says so the day it stops being true.)

- **M15. the const-pass scaling gate is wall-clock and reds under lane load — CLOSED 2026-09-01** (Order 23, perf lane, option (b) THREAD CPU TIME: the gate asserts a ratio, and preemption never accrues to a thread clock — measured across loadavg 8→107, CPU ratio spread 5% where wall spread >100% (one wall reading at 5.30 against the 6× bound). Both directions proven: green under 72 recorded CPU hogs, red at 12.45× on the planted quadratic. Every run prints both clocks + loadavg. The light plant (linear per-site work, 3.11×, passes) corroborates the pin's standing honesty note — it guards the entry-size asymptote only.)

- **M16. T-independent generic bodies emit per-monomorphization copies — CLOSED 2026-09-01** (Order 23, perf lane, audit 6's F18: T-independence decided by the EMITTED body compared name-stripped through rename_node (never text substitution — a literal containing another instance's name would fake a match), shared only when the walk pushed no keyed emission of its own (the mutual-recursion refusal for free). 37 bodies merged; corpus −1.11%, split fixture −9.38% (105→88 functions, 78 distinct bodies before and after); ALL 124 corpus programs byte-identical under node. B95/B102's twelve pins re-instrumented onto instance KEYS — four would have gone red and eight quietly vacuous. Cross-subject sharing filed as M17.)

- **N21. `cargo fmt` is gated; the clippy and cargo-audit legs remain — CLOSED 2026-09-01** (Order 23's SOLO serial slot: clippy 217→0 (180 fixed incl. 102 let-chain collapses with a byte-identical comment-multiset proof; 4 workspace-level allows with reasons, DELETING 24 scattered per-site allows; dead code deleted incl. an empty `if !compiling_std {}` sitting since July's dd59c007). cargo-audit 0.22.2, 137 deps, zero advisories, `--deny unsound` chosen with the argument written down. Both wired into ci.yml feeding the required check; clippy component pinned in rust-toolchain.toml; local ritual in CLAUDE.md + AGENTS.md rule 6. The vault-lock stop honored mid-lane; commits landed on unlock. release.yml's gate carries none of the three — flagged, owner's call.)

- **N34. the normative EBNF has no gate — CLOSED 2026-09-01** (Order 23, records lane: grammar_ebnf.rs — keyword coverage both directions, extern-args' form words against the parser's OWN source, internal closure; header states what it does NOT verify. Found and fixed four real gaps: `operator-expr` named but never written (so `await` and `is` appeared nowhere normative), `place`/`literal` prose-only, `struct-init` unreachable, three positions saying secondary-expr where §3.8 says condition-expr. ISTRING's missing §2 production filed as N39.)

- **N36. the diagnostics ledger is three orders stale — CLOSED 2026-09-01** (Order 23, records lane: 89 rows backfilled (256–344), F9's dead literals re-keyed — row 149 NOT dead, contrary to the filing: E100 split the site — F20's sentence-per-row rule adopted with 7 splits. The gate: key-liveness, coverage-as-prefix, well-formedness, red-proven seven ways; prefix matching chosen because loose fragments had silently accepted B158's reworded sentence. Day-one customer b179 found the catch-all row 291 hole + the helper blind spot — N41. Row 345 added same day.)

- **N37. the errors appendix is ungated, flagship messages missing — CLOSED 2026-09-01** (Order 23, records lane: the appendix declares its three curation criteria; five flagship entries backfilled, six heads re-quoted to what the compiler prints; gated on live-message + flagship-quoted (111 rows), five composed heads recorded unheldable with reasons.)

- **B179. a bounded generic in `+`'s numeric position still miscompiles — CLOSED 2026-09-01, ruled and fixed the same day** (the owner's operand-role semantics: the `+` belongs to the LEFT operand; membership in its admitted set is provable by bound only where a trait names the set — str's renderables yes, i32's numerics no, so EVERY generic right operand of a native-left operator refuses whatever its bound promises. FIX-NOW lane b179 closed the whole family — `+` `+=` `- * / %` bitwise comparisons equality, str-left equality too — 8 refusal pins red-first with the pre-fix garbage run and recorded (`total & value` → 0, `"a" == value` → false). Ledger row 345 flagship. Two finds: B180 (the dispatch path never checks a nominal left's declared B — the ruling's second steer is broken until it closes) and B181 (`&&`/`||`).)

- **B168. a bound's bare generic argument loses its constraints in a generic body — CLOSED 2026-09-01** (Order 23, lane b168: `trait_args_for` recovered bound arguments through an UNDIRECTED unifier, so two generic sides kept pairs oriented caller-side and the impl's own unbound binder came back as the bound's argument — errors naming a `U` appearing nowhere in the program. `bindings_for_binders` orients pairs at the impl's own binders; impl selection untouched. The un-ignored pin green, the three waiting std signatures widened (`View::swap`, `swap_split`, `chunk_preload` + the process twin), the refusal now names the CALLER's parameter, one split golden verified as a pure duplicate-dedup. Side-find B173 filed with the pin retargeted before this closed — the N31 anti-pattern averted. MISSED by the first close batch and caught by the changelog-vs-INDEX sweep, the Order 19 lesson doing its job.)

- **E113. file-mode and the LSP color a module by `default-entry`, not by the entry that reaches it — CLOSED 2026-09-01, the pre-cut FIX-NOW** (the owner's kolt report: `impl View`'s `self.element` drew "struct 'View' has no field 'element'" — correct code under the wrong overlay. Fixed at ONE rule both surfaces share: `file_platforms` colors by the loader's own reachability (reused, not reinvented — overlay-served parses reclaimed per M9's discipline); a shared module checks under EVERY reaching leg (the build's own workspace-lowering rule, matched exactly); unreached falls back to default-entry. VINTAGE: both surfaces PREDATE Order 23 — file mode mis-colored every path-addressed module since file mode existed (G20 NARROWED it), the LSP since ≤0.39.0 whenever a module carried no browser-exclusive import — which is why the owner's own `prelude = "std::web"` adoption surfaced it: the prelude deleted the browser-evidence imports the inference keyed on. 10 pins red-first incl. the mirror-image control and the package-mode invariant; a same-shape pre-existing bug fixed en route (`vilan check <module>` demanded a `main` — CompileGoal::CheckModule). Residuals noted, not taken: `check .` prints no success verdict for multi-entry; `build <shared file>` deliberately first-color-only, documented.)

- **B185. a rebinding of an unfilled closure parameter grounds on `Unknown` — CLOSED 2026-09-01, the second cut-holding FIX-NOW** (found by the website migration's clean-build gate at playground.vl's set_with; root cause NOT set_with/traits/mut/containers — `resolve_variable` was the ONE resolver whose first_ready gate did not defer on an unfilled closure parameter's Unknown, so `mut next = values` ground permanently before the call-site fill landed. Family MISCOMPILE, tested: the untyped binding read as scalar and DROPPED the `__clone` a rebinding owes — `2 2` where the annotated spelling prints `1 2`, silent sharing. Fixed with the Variable arm in `unfilled_closure_parameter` (chains of rebindings count; a ground binding stops the walk) + the starved-parameter sweep naming the PARAMETER + a cycle guard the module-binding graph made necessary (two soak pins hung without it). 16 pins red-first incl. both miscompile runs, the website-verbatim shape, kolt's field shape, sub/update/plain-generic controls; the brief's discrimination corrected on all four axes (str loses too — the scalar control was saved by numeric defaulting; plain let loses; inherent and required methods reproduce; kolt was clean by constraint-TIMING, not shape). Website check 4→0 errors; suite 5242/5242.)

- **B180. a nominal left operand's declared `B` is never checked at dispatch — CLOSED 2026-09-01** (Order 24 top slot, lane b180: ONE general check at the dispatch site — the declared `B` resolved three ways (written, the impl's own parameter substituted, `Self` through `Add<B = Self>`'s default) and compared RIGID, since the ordinary comparison treated a parameter as a hole answering true to anything. Every operator trait through it, `+=` via the desugar, `PartialOrd`'s inherited defaults included. Thirteen pre-fix miscompiles run and recorded (`Counter{n=1} + Point{…}` → 2; `impl Bag<type T> with Add<T>` at `Bag<i32>` stored a str → `oops`). B179's second steer now names a spelling that WORKS, pinned at two instantiations. Ledger row 346.)

- **B172. a module-qualified type path is a parse error in every type position — CLOSED 2026-09-01** (Order 24, lane b172: one production `type-path = IDENT { "::" IDENT } [ generic-args ]` at the type cycle's single door, resolution through the existing path machinery (`Node::StaticAccessor` gained the last segment's generic arguments). Fifteen pins red-first across every type position; the misattributed return-type diagnostic re-anchored (a located demand for the body's `{`). Templates stripped of their workaround imports; examples-adopt's last one stripped at integration. The owner's kolt sites reported, not touched. Ledger row 347 (a non-type member steer). Found: B190 (the qualified struct LITERAL).)

- **B186. `fun f(x: Trait)` as an implicit generic — CLOSED 2026-09-01** (Order 24, lane b186, owner-ruled WANTED the same day: the parameter's annotation mints a FRESH constraint id appended after the written generics — B161's machinery reused, two same-trait parameters two independent generics, the trait's arguments bind the bound — under NO name (the trait's name displays). `&Trait` accepted as the erased view convention, pinned as the one deviation for the owner's eye. Emission census: the sugar emits BYTE-IDENTICAL JS to the written generic. `Optimistic<T, S: Signal<T>>`'s arity change shipped (four std spellings, zero estate), which forced a real solver fix — a generic reachable only through another's bound is now recovered over the whole bindable set. Found: B192.)

- **B175. `T::default()` under a multi-bound infers as the BOUND, not `T` — CLOSED 2026-09-01** (Order 24, lane operators-tail: `Expr::Call`'s `Self`-return specialization keyed on a RECEIVER an associated function does not have; a second branch keyed on the bound-path accessor's recorded dispatch fixes it, single-bound too (the filing's multi-bound framing corrected). `Type::Trait` then came OFF the operator skip list with its own refusal — and the exposed miscompile ran: `List<Money>::sum()` over 40 and 2 printed 4 (host `+` over lowered structs); now 42. Found: B193.)

- **B181. `&&`/`||` accept a generic operand and emit the value as the boolean — CLOSED 2026-09-01** (Order 24, lane operators-tail: the `And | Or` arm's grounded gate excluded every generic; the right operand now refuses outright (no operator trait to consult). Pre-fix `both(true, Point{…})` printed `[ 1, 2 ]`. The LEFT half pinned #[ignore]d under B174 — its second exhibit. Ledger row 361, caught unrowed at integration by N41's hardened gate.)

- **B177. an array impl's return check refuses with a self-contradicting message — CLOSED 2026-09-01** (Order 24, lane operators-tail: `compare_type_rigid` had a structural arm for every composite shape EXCEPT `Type::Array`, so two arrays fell to id equality and an array was unequal to itself. Arm added; `[1,2] + 3` runs; the message's other half fixed too — conformance named everything non-struct the literal word "type".)

- **B182. a refused bare-trait FIELD cascades ~50 errors through generated code, roots printed last — CLOSED 2026-09-01** (Order 24, lane b182: the refusal's `Unknown` slot now carries PROVENANCE (`refused_annotation_slots`), and three consumers stand down on it — method lookup, field access, the generic-bound invariant — per receiver / per call, E104's grain. Per-entry dedup via a per-round ledger keyed exactly as E102's two-seams dedup; roots-first ORDER by an observed-stand-down rank. kolt-shaped fixture 21 → 2; the papers lane's smallest form 3 → 1. Found: B189 (three siblings with other provenances).)

- **B188. an under-supplied type argument is ERASED, not refused — CLOSED 2026-09-01** (Order 24 FIX-NOW off the papers lane's probe, same day: the `prepped_type_locals` drain attached arguments only when some were written, so bare `Holder` fell through to the declaration's empty vector — `fun read(h: Holder): i32` accepted a `Holder<str>` and printed `seven1`; the ICE twin had the same root. `written_application_arity_error` + a declared-parameters side table: refuse under AND over supply naming the fix spelling, defaults fill. Twenty pins; estate census zero — and a STOP: generated code spells subjects bare (`[derive(Wire)]` on `Handle<T>`), so the check is gated off DERIVED_SOURCE and B194 files the generator half. Ledger row 358.)

- **B173. a blanket impl never satisfies a bound for a GENERIC value — CLOSED 2026-09-01, REFUSED by ruling** (the owner: "It sounds like no? Can you think of a situation where … would be desired?" — none exists; the hazard is over-approximation against a more specific impl at instantiation. The #[ignore]d wish became three pins (the refusal naming both `T` and `Wrap<T>`, the concrete caller, the declared-bound spelling); rule recorded in spec §5.4 beside the blanket-impl rule.)

- **B171. the spec's `is`-binding scope sentence is false — CLOSED 2026-09-01, RULED and the compiler fixed too** (the owner: then-branch YES, `&&`-right YES, `||` arms NO, else NO — "exactly correct". The lane found the compiler wrong on one boundary: `if maybe is Some(let n) || n > 1` COMPILED, reading a payload slot that isn't there. Fixed at the root — positional declarations carry a visibility RANGE, a capture inside a `||` operand ends with it. Seven pins; spec §5.7 rewritten. The negated arm's twin is B195 (FIX-NOW); the guard clause is B187.)

- **B178. `fun main(p: bool)` compiles and emits broken JS — CLOSED 2026-09-01, RULED refuse** (the owner: "the shell owns what is passed in". Refusal steers to `std::process::args()`, which ships in the same lane with a runnable fence and seven pins. Found and fixed en route: `arguments` and `eval` were missing from the emitter's RESERVED_NAMES — strict-mode-forbidden bindings — so the corpus program exercising `args()` was byte-identical to its golden and UNRUNNABLE; one golden moved and now runs. Ledger row 359.)

- **A35. the element desugar's `view` is capturable, the shadowed case has no diagnostic — CLOSED 2026-09-01, RULED name it** (the curated shadowed-case twin of the absent-case steer, detection keyed on the markup span so a hand-written `view(1)` keeps the arity message; lands on `ui::view(…)` as the escape. No hygiene change. Ledger row 360.)

- **E119. a field miss on an overlaid std type should name the overlay and why — CLOSED 2026-09-01** (Order 24, rulings-smalls: `file_platform_choices` returns each color with a `PlatformReason` (package target / reached-by / default-entry / flag), threaded as `Workspace::platform_reason` outside the base-cache key, printed as a NOTE under the type's declaration at field- and method-miss sites — the ruling's sentence verbatim. LSP re-stamps per leg. Five pins incl. the user-type control.)

- **E117. a ghost diagnostic lingers after a comment/uncomment round-trip — CLOSED 2026-09-01** (Order 24, editor-fixes: both out-of-order guards compared TEXT, which a DEPENDENT's buffer does not change when its import is edited — two byte-identical in-flight analyses landed in whichever order they finished. A world revision stamps every analysis; older stamps are dropped at land and at plan; plan+send under one gate. Two pins red-first.)

- **E116. a file's platform color does not update when it becomes reachable — CLOSED 2026-09-01** (Order 24, editor-fixes: color is decided by which entry REACHES a file — the opposite direction from the dependency edge the sweep gated on. An analysis now records the modules it reached; when that set moves, every open document under the same `pkg::` root re-analyzes. Found and fixed in the same stroke: a manifest SAVE had been re-analyzing nothing at all since the edge gate landed.)

- **E115. an element tag's closing `>` on its own line is not highlighted — CLOSED 2026-09-01** (Order 24, editor-fixes: the grammar's element rule was a single-line match AND the semantic pass recorded only tag names; the parser now records all four brackets where it consumes them and the semantic pass paints them as their tag. Multi-line head == one-line head token stream, pinned.)

- **E118. the formatter lays out an inline element closure argument oddly — CLOSED 2026-09-01** (Order 24, editor-fixes, the item's recommended rule: break after `|| `, element on its own line, children +1, close aligned to the open — `print_block`'s shape; width still decides first so `|t| <li>{t}</li>` stays inline. Four pins, all 274 formatter tests incl. idempotence green.)

- **E112. `line_indices` caches a closed workspace file forever — CLOSED 2026-09-01** (Order 24, editor-fixes: an entry carries length + mtime and is served only while both match — subsumes both offered remedies (a did-close hook misses `git checkout`; std-only goes stale on std development). Residual: a same-second same-length rewrite on a coarse-mtime filesystem; a content hash would defeat the cache.)

- **N40. `free_port()`'s bind-release-rebind race — CLOSED 2026-09-01** (Order 24, hygiene, done FIRST: `std::http` has reported its bound port through `on_start` since E19 — the suites had never taken it up. Both stage `.port(0)`, one shared announcement, the port read off stdout; `free_port`/`wait_for_port` gone. Load run: 5× both suites under 8 recorded spinners at loadavg ~23, 55/55, zero bind failures.)

- **N41. the ledger gate's catch-all row and helper blind spot — CLOSED 2026-09-01** (Order 24, hygiene: check (3) takes the fragment-less exclusion refined to "own site only"; five accidentally-held sites rowed (346-349 in the lane's numbering, 348-351 at integration — the item was one site off: `platform_color.rs` was row 180's all along). The helper blind spot answered by HAND-ROWING with a stated exemption (`ROWS_THE_ENUMERATION_CANNOT_REACH`) rather than widening into 18 other `let msg` ladders. The hardened gate then caught operators-tail's unrowed B181 message at merge — its first sibling catch. Found: N42.)

- **N39. `ISTRING` is used normatively, never declared in §2 — CLOSED 2026-09-01** (Order 24, hygiene: the production written beside its multiline twin, two prose facts stated, the EBNF gate's exemption dropped — red-proven by deleting the production.)

- **N27. nothing detects an expired `#[ignore]` reason — CLOSED 2026-09-01** (Order 24, hygiene: the format half had shipped with N31/N33; the missing cross-check is `scripts/ignored-pins.py --tracker <path>` reading the archive CHAIN, run at close. Its FIRST live run found a real fault: `returns.rs:864` named B126, tombstoned — re-owned as B191 at integration. Weekly `--self-test` in the workflow; two pins hold the script's interface.)

- **N28. `book_sync`'s mdBook backstop runs whatever `mdbook` is on PATH — CLOSED 2026-09-01** (already shipped in base at 2acb0c04 — tracker drift; the residue closed: the refusal's version spelling now reads off `PINNED_MDBOOK`. The pin still lives in four in-repo places + the pages repo — N42 carries the cross-check.)

- **N38. two shipped sentences disagree on the reserved-name set — CLOSED 2026-09-01** (Order 24, hygiene: one table of name+reason both refusals read, one `RESERVED_PACKAGE_NAMES_CLAUSE`, a unit test holding sentence against table both ways; the corrected-sentence census re-keyed ledger rows 248/284.)

- **B195. a negated `is` capture is readable in its then-branch and reads `undefined` — CLOSED 2026-09-01** (Order 24 FIX-NOW, the day's third: a `ConditionPolarity` PAIR (the visibility end and else-membership at even and odd negation parity) carried down the condition's boolean spine — `!` is a plain swap of the halves, so `!(!(x is P))` cancels with no depth counter; `&&` cuts only the flipped half; `||` stays B171's cap. A negated capture is INVISIBLE in the then-branch and VISIBLE in the `else` (and down an `else if` chain) — the ruled scope sentence extended in spec §5.7. Pre-fix recorded: `if !(maybe is Some(let n)) { print(n) }` over None printed `undefined`. 15 pins, 9 red-first; two latent unsoundnesses closed beyond the charge (`!(!(x is P) && n > 1)` and `!(x is P) && n > 1` both read the missing payload). Found: B199 — a capture through a CALL ARGUMENT keeps B171's then-branch answer; the guard-clause continuation stays B187's.)

- **B196. every native operator except `+` is unchecked for a `bool`/`str`/backed-enum LEFT operand — CLOSED 2026-09-01, audit run 7's F1, a RELEASED miscompile fixed the same day** (lane b196: the gate's own comment scoped the check to `+` because 'the other operators emit arithmetic on numbers' — true only for a NUMERIC left; a binary's static type is the left's, the host returns a number, so `true - 3` was a `bool` holding -2 that took the true branch and failed `== true`, `"12" - "3"` a `str` holding 9, `Level::High ^ Level::Low` a `Level` matching no variant. Now: the nine arithmetic/bitwise operators refuse on a non-numeric native left with the ADMITTED SET named per type and per-shape steers pinned compiling (`&`→`&&`, `str *`→`.repeat`, enum→match); `f64 * i32` stays the deliberate carve-out, said so in the rewritten SCOPE note. Census ZERO across 293 files and 390 fences; 48 of the audit's 216-program matrix flip RUN→REFUSE, none the other way. 13 pins, 10 red-first with the pre-fix garbage recorded. Ledger row 362. Found: B200 (the unary `-` twin); numeric-left/non-numeric-right coercion deliberately left with the strictness family.)

- **N44. the resources chapter states the pre-last-use scope-end law; memory.md wants adjudication — CLOSED 2026-09-01** (Order 25, lane docs-law: 32 STALE sentences across seven pages corrected to the last-use law (the owner's rule quoted in the reader-facing chapter: declaration counts as a use, unread resources drop where created), 3 HELD recorded; two resources.md fences had promised the REVERSE of their output — the programs adjusted so the claims are witnessed, all six runnable fences hand-run and matched. Found: the docs gate asserts no output — N47 proposes a `vilan,run` + transcript form; `destruction.md` §5 in the design record carried the old law too, amended at integration.)

- **E120. every std import steer is silently dead in the playground — CLOSED 2026-09-01** (Order 25, lane playground-steers: the inventory is derived at build time from the embedded std (`modules_in_root` over `vilan_embedded_std::FILES`, K9's own listing), never hand-written; the web-set steer gained a `PreludeRepair::Toggle` arm for front ends with no manifest ('switch the playground's prelude to the web set, or import it'). FILING CORRECTED: the removed-alias steer's module-path arm was never dead — it is a curated table. The silence pin rewritten (it had recorded a defect as a guarantee). Ledger rows 363/364 assigned at integration — both arms were unrowed, N41's helper blind spot. K14's page gap noted: no `Prelude: web` option in node mode.)

- **M20. a chunk snapshots its dependencies by value; wrong-order evaluation binds `undefined` forever — CLOSED 2026-09-01** (Order 25, lane m20, the owner's 'M20 first': sibling-chunk references are now CALL-SITE registry reads (a scope-correct rewrite modelled on `rename_node`), eager names keep the preamble snapshot — sound by construction. The paper's D5 CORRECTED: the shipped emitter never reached the `TypeError` — it emitted no binding at all for a sibling's name and dangled with `ReferenceError`. Wrong-order pin resolves; zero cross-chunk references on today's route partition, pinned; no golden moved. Removes the ordering hazard only — the fetch is M18's. Incident: a shared-stash collision with a sibling lane, recovered fully — the no-stash lane rule minted.)

- **B192. a partial generic-argument list ICEs at emission ('resolved to a requirement, which has no body') — CLOSED 2026-09-01** (Order 25, lane cascade-25: `transformer.rs::call_substitution` RACED its three binding channels instead of merging them — a non-empty written list zipped positionally against the parameters and returned, discarding the analyzer's recorded bindings for the parameters the list never reached, so the unwritten `U` reached `emit_instance` abstract. Now the written prefix is overlaid on the recorded substitution; an over-long list and a list on a non-generic function are REFUSED (they used to compile and silently drop the extra). 3 pins red-first, 2 controls incl. a precedence pin. Ledger rows 365/366 assigned at integration.)

- **B189. B182's provenance misses three siblings: the unresolved-name arm, derive-templated paths, `[expose]`'s two shapes — CLOSED 2026-09-01** (Order 25, lane cascade-25: (1) the `prepped_type_locals` drain's unresolved-name arm wrote `Unknown` with no provenance, one arm below B182's; (2) derive templates from the annotation's SPELLING, so the generated path's type id never matched the slot — a per-trait provenance keyed on the refused spelling; (3) `[expose]`'s subject is a well-typed field with a refused EXPOSURE, whose expansion fails the same bound twice (the call carries the field, the client mirror names only the element AS RENDERED — fresh annotation ids defeat Type equality) — a covered set consulted under `DERIVED_SOURCE`. Exhibits 2→1, 4→1, 3→1; the kolt-shaped fixture 4→2 pinned as a count. 5 pins red-first; two existing pins re-shaped when their own non-vacuity guards fired. Residual, deliberate: a HAND-WRITTEN miss on the same refused trait is silent under spelling-level keying. Found: B201 (derive inside a mod), B202 (rpc.vl's `_` placeholder).)

- **B190. `pkg::mod::Type { … }` — the qualified struct literal — is refused; the one spelling B172 left off `type-path` — CLOSED 2026-09-01** (Order 25, lane cascade-25: `parse_struct_initializer` keyed on `IDENT [generic-args] {`; now the head is B172's `type-path` across the parser, `node.rs` (`StructInitializer` carries the namespace + the name span), the css/elements/lift pass-throughs, the formatter, the analyzer (`resolve_initializer_head` / `namespace_body_scope`, the reference record reads the parser's span) and the normative grammar. 5 pins red-first with src reverted, the condition-position control green in both states, a formatter round-trip, two AST snapshots updated. The fixture's hand-written impl stands in for B201.)

- **N43. the phase line's buckets are mislabelled: `call-graph` times contexts, `const-eval` times the whole pass, `dispatch_refine` is invisible — CLOSED 2026-09-01** (Order 25, lane perf-25: `contexts+graph` (thread_contexts builds its own graph when it rewrites), `const-pass` beside its `const-lower`/`const-interp` sub-split, and `dispatch-refine` — reached from both — accumulates its own wall in a thread-local, printed as a deliberate SLICE through the two buckets it explains. The pin refuses the old spellings; red-first. Live: `contexts+graph 171.7ms … const-pass 64.9ms … dispatch-refine 41.0ms` on kolt.)

- **M21. a `pkg::`-importing entry never hits BASE_CACHE: two bypasses — CLOSED 2026-09-01** (Order 25, lane perf-25: `base_cacheable` no longer requires an empty `pkg::` set — `BaseCacheKey` gains `entry_pkg: Option<(canonical package root, sorted deduped interned sibling names)>`, `None` for a sibling-free entry so every pre-M21 key is byte-identical and std-only worlds still share across packages; hits stay content-validated per source (E12); and `expand_entry_over_world` no longer rebuilds the world on a generated `pkg::` reference — `World` carries `pkg_module_names` and the hoist asks the same question it asks of `std::`. Pin: hit on the second analysis, observation-identity vs a cleared cache, a different sibling set misses, an edited sibling evicts by content, retained-world delta == 1 — red-first ('it missed instead'). kolt views.vl keystroke: `base` 156–586 ms miss-every-time → 0.0 ms from analysis 2. Found: M23 (M9's overlay rule refuses the store where a session lives — client.vl never hits), M24 (no eviction).)

- **M22. `build --watch` recompiles every leg on any change — CLOSED 2026-09-01** (Order 25, lane perf-25: the watch round now keeps the HMR round's three facts (per-leg compiled-from source hashes incl. const inputs, the manifest fingerprint, the prior round's failure) and decides with the SAME two functions, `hmr::round_forces_full` / `hmr::leg_is_current` — content, never mtime, so a hook's regenerated outputs restale by content with no second rule. A skipped leg prints `Fresh <entry> -> <artifact>` and still occupies its output names in the collision check; `--explain` forces a full round. Content re-hash chosen over E113's syntactic `pkg::` walk deliberately: it subsumes reachability and covers std/dep/asset inputs. kolt views.vl round: 2.31 s three legs Compiled → 1.55 s `Compiled client` + `Fresh probe` + `Fresh server`. New pin file `watch_leg_reuse.rs`. Workspace-only — `Project::Single` and `check --watch` get none. Found: B203 (the skip set is decided before any leg compiles — cross-leg artifact inputs).)

- **E114. gray out unused imports, unused declarations and unreachable code — CLOSED 2026-09-01** (thirds: imports in Order 24 (lane editor-fixes, HINT + `Unnecessary`); Order 25's lane e114-rest built the rest. DECLARATIONS RE-SCOPED by the language: vilan has NO visibility marker (`pub` is a parse error — 'a module's items are importable as they stand'), so every top-level item is module surface an unanalyzed importer may use and a single-entry analysis cannot call it dead; shipped is the one scope the language closes — FUNCTION-LOCAL bindings, read from the reference index the imports third built, exempting `_`-led names, parameters, module-level bindings, any binding whose index dropped a use, and the stale/broken-file gate. UNREACHABLE: the analyzer's three private divergence methods became `analyzer::Divergence`, one analysis for checker and paint; paint adds two leaves the checker deliberately lacks — a `panic(…)` call and an endless `for { … }` nothing breaks (the language has no `while`/`loop`; `jump` binds to the nearest loop by span, ambiguous nesting excluded). One faded range per dead block. Both producers run ONLY on the debounced diagnostics path — the first cut scanned whole-program `entity_map` at 118 ms and broke the recolor pin's settle deadline under load; `Program::entities_of` + `id_ranges_of` took it to 6.2 ms (locals 2.8 ms, imports 106→65 ms). 17 pins red-first (8 of 9 unreachable pins failed first: a synthesized Void tail records the closing brace). Found: E124 (top-level gray for single-entry packages — the owner's decision), N48 (std's own dead code), B204 (panic as checker divergence). Incident: the SECOND shared-stash collision of the order (e114-rest ↔ e122), recovered fully.)

- **B174. an operator on an UNBOUNDED generic left operand compiles and dispatches to whatever the runtime value happens to be — CLOSED 2026-09-01, the owner's 'take it'** (Order 25, lane b174: the rule from the generic-left census — `T + T` needs `T: Add`; a parameter promises only what its bounds promise, and `+` dispatches through its LEFT operand, exactly where a bound decides the implementation. All 16 trait-modelling binaries via `operator_trait_method`, `&&`/`||` refusing on both sides (row 361 unchanged); supertrait bounds count — load-bearing, std's `minmax<T: Ord>` uses `<=` from `PartialOrd`. Census re-derived by hand: 482 traits / 126 default-bodied members / 35 operator lines; ONE live site in the whole estate (`macros.rs` `trait Doubler<T>` → `<T: Add>`; the impl binder does not restate the bound — satisfaction is checked where the parameter is grounded, pinned both ways); kolt/website/playground 0/0/0. 6 pins red-first with the pre-fix garbage run through emitted JS (`bump(Point{1,2})` → `1,21`, `both(Point,true)` → `true`, `same(M{7},M{7})` → `false`). Ledger row 367 assigned at integration. Found: a THIRD surface the census never priced — derive-generated code over a `T` field (`[derive(PartialEq)]` on a generic struct) — exempted on B188's `DERIVED_SOURCE` boundary until B194 bounds it (estate: generics.rs:5460, tuples.rs:131, borrows.rs:1072, std arena.vl:23).)

- **B200. unary `-` and `!` are unchecked: `-true` is `-1` typed bool, `!Point{…}` is `false` — CLOSED 2026-09-01, the operator family's last miscompile** (Order 25, lane operators-25: `Expr::Unary` returned the operand's type with NO operand rule — unaries were never prepped, so they reached neither B196's binary gate nor any other. Now `prepped_unary_ops` + a post-solve check before the binary loop: `-` admits the numeric primitives, `!` admits `bool` — stated, not read off an impl (no `Neg`/`Not` trait exists); `Type::Trait` operands refused too (nothing can promise `-`/`!`; B175's which-refusal rule gives the default body and the bare trait different steers). Pre-fix, all RUN: `-"12"` → `-12` typed str; `-Level::High` → `-5` matching no variant; `-Point{1,2}` → NaN with `.x` undefined; `!5`/`!"hi"`/`!Point{…}` → false; `negate<T: Sub>(v: T) { -v }` compiled. Census ZERO across 223 .vl + 391 fences + 19 kolt files. 15 pins, 11 red with the prep line planted out. Ledger rows 368–370, 371–376, 379–380 assigned at integration.)

- **B193. a trait default's `self <op> self` emits native JS on the specialization's payload — CLOSED 2026-09-01** (Order 25, lane operators-25: ONE defect, two halves — `self` in a default is `Type::Trait`, the analyzer's operator loop skipped that shape, and the transformer's binary arm demanded `OnType(Some(..))`, so the anything-goes native emission survived: `function $a(self) { return self + self; }` on `Money{cents=21}.twice()` printed 2; `self - self`/`self * self` undefined; `self == self` emitted `===` → true for a `PartialEq` whose `eq` answers false. Now the transformer's arm takes `.or(self.current_self_type)` mirroring `Expr::Call`'s (B55), and the analyzer records `OnType(None, method)` when the trait or a supertrait declares the operator's method — else a refusal naming the supertrait to declare. Post-fix 42/0/9/false. 6 pins, 4 red with the emitter case planted out; the platform pin un-ignored; native specializations keep native JS via `compares_natively`. Ledger row 377. Found: B205 — the explicit `self.add(self)` spelling still cannot be written (supertrait `Self` not specialized).)

- **B197. an operator trait's method is not required at impl time: `impl P with Add { }` type-checks and panics at runtime — CLOSED 2026-09-01, the owner's 'required at impl time'** (Order 25, lane operators-25: the conformance check's `required` list excludes members with a default body, and `operators.vl`'s ten traits carry `panic("not implemented yet")` bodies. Now `operator_trait_required_method` (the ten traits + symbols) adds one disjunct to the `required` filter, with a dedicated refusal arm that renders its own signature (`fun add(self, b: Feet): Meters`) because `function_signature_label` prints `b: Add` for a `B = Self` parameter — B206. `operators.vl` untouched; `+=` still derives; ordinary defaults still inherit; `PartialOrd`'s `lt`/`le`/`gt`/`ge` defaults untouched. Census: 63 operator-trait impls across the estate, every one provides its method. BREAKING family (a runtime panic in waiting becomes a refusal; migration: write the method). 9 pins, 6 red. Ledger row 378.)

- **N46. the watch/LSP wall-clocked pins go red under lane load — strike three — CLOSED 2026-09-01** (Order 25, lane hygiene-25, direction (1) chosen and (2) REJECTED WITH NUMBERS: a watcher waiting with nothing to do burns 1.0 ms CPU per second set by its 300 ms poll rate, not by load, so a CPU-denominated deadline is a wall deadline in other units and makes starved and stuck LESS distinguishable — M15's shape does not transfer to an event delivered by a sleeping poll loop. Built: nextest group `wall-clock-waits` (`max-threads = 1`) with 38 members — hmr ×14, build_hooks ×9, assets ×3, serve_build ×3, watch_lifecycle ×2, asset_bundle, hmr_swap, hmr_css_matrix, split, and vilan-lsp `package_recolor_tests` ×3 — with an anti-rot gate that found `split` and `serve_build` by itself; two bounds raised to 300 s by E39/E40's rule (`package_recolor_tests::settled`'s 10 s was GRAZED at 9.58 s on a quiet box; `release_differential`'s NODE_TIMEOUT 30 s); `priority = 100` for the stragglers. After: 5 runs at loadavg 106–143, 106/106 green; a planted hang still red at 300.2 s. Cost: the group serializes to 273 s inside a 702 s union (not the critical path — `release_differential` at 615 s is, N49); at idle +37 s (+58%). `dependent_edit_measurement`'s wall clocks are PRINTED, never asserted — long, not fragile. THE MECHANISM IS NOT THIS: the recorded strike is round 1 in 0.6 s and round 2 never in 300 s despite 15 re-touches — a lost wake-up, filed B208.)

- **N42. the ledger gate's exemption lists can rot silently — CLOSED 2026-09-01** (Order 25, lane hygiene-25: three inverse checks — `every_recorded_token_class_still_needs_recording`, `every_fragmentless_key_still_has_no_fragment`, `every_hand_rowed_row_is_still_out_of_the_enumerations_reach` (asked with check (3)'s own predicate) — each red-proved by removing the exemption's REASON (declare `INTEGER` in lexical.md; re-key row 291 onto a real literal; make row 352 reachable → 'now enumerated at analyzer.rs:20754'). The mdBook pin now held in five places (README, docs README, markdown.vl, markdown_golden.rs, the regen script) — red-proof 'README.md names mdBook 0.4.40, and the pin is 0.5.4'. Found: N50, the same shape in three other gates.)

- **N45. `ignored-pins.py` cannot see a `#[cfg(windows)]` pin — CLOSED 2026-09-01** (Order 25, lane hygiene-25: TEXTUAL, confirmed — the script and `ci_ignored_pins.rs` both enumerate by line scan without compiling, so a cfg-gated pin IS listed; the blind half is the weekly `--run-ignored only` leg, ubuntu-only. Written down beside the enumerator; `--self-test` gained a `#[cfg(windows)]` fixture pin read by reason, red-proved by a cfg-blind scanner. WINDOWS CLIPPY: `cargo clippy --target x86_64-pc-windows-msvc --workspace --all-targets -- -D warnings` exit 0 over all six crates after ONE fix — `modules.rs`'s `entry_functions_with_a_requirement` gated `cfg(unix)` (red-proof: 'function is never used'); a Windows clippy leg needs nothing else.)

- **B198. a generated product not yet on disk under a symlinked root is not covered — CLOSED 2026-09-01, the owner's 'canonical-or-fail'** (Order 25, lane hygiene-25: `util::canonical_path_of_unwritten` canonicalizes the deepest ancestor on disk and re-attaches the tail as spelled; BOTH sides of `covering_from`'s containment test use it; `canonical_path` untouched, nothing folds, nothing on disk → both degrade lexically together (G17's spelled ladder). Unix twin `a_product_the_generator_has_not_written_yet_is_covered_through_a_link` red-proved (reverting gives `None` while the written control stays green). The Windows pin is LIVE but unverified locally — the cross-check compiles it. Found: B207, the same shape at document.rs:287 on an unsaved buffer.)

- **E122. `semanticTokens/range` computes the whole file's stream and filters by line — CLOSED 2026-09-01** (Order 25, lane e122: the handler ran the whole-file computation — every entity table, `member_name_spans`, `type_references`, a fresh raw parse for markup/CSS spans, a sort and a de-overlap — then filtered with one line-index lookup PER TOKEN IN THE FILE; the 20-line viewport cost 1.01× the whole file, §1.6's claim as a number. Now `document::SemanticTokenStream` holds the tokens ordered by start offset plus a `line_starts` table, built once per landed analysis; a window is a clamp and two indexes; full and delta ride the same memo. Invalidation: the stream is a pure function of the analyzed snapshot (`program`, `analyzed_index`, B38's `retained_tail`), which move together in exactly ONE place, `adopt_analysis` — a live edit does not invalidate it (S1). Pins: byte-identity vs the filter-the-full-stream form over mid-file/past-end/token-less/whole windows (planting an ignored end bound → red); cost ∝ range on a 12,000-token stream, thread CPU: no memo 0.923×, fixed 0.006× (2.67 vs 480 ms). kolt views.vl at the paper's load band: range 9.0 → 0.3 ms wall, 10 → 0 ms CPU; the five-provider burst 19.1 → 12.8 ms. Overlaps the keystroke path's `LandedSnapshot` — reconciled at integration.)

- **E123. `did_open` analyzes inline on the async handler — CLOSED 2026-09-01** (Order 25, lane e122: `Document::analyze` joins a 128 MiB thread, so the notification held its tokio worker for the whole first analysis — didOpen-then-request 1,271 / 1,059 ms. Now `did_open` inserts `Document::unanalyzed` synchronously (a query still finds the document; `land`'s 'a missing entry can only mean closed' untouched) and schedules through the same `analyze_and_publish` every other path uses — `spawn_blocking`, stamped with the world revision it read (E117), landed only if still newest — with a `semanticTokens/refresh` after the landing. Pins on a current-thread runtime (ordering exact): a request during a fresh open's analysis is answered before it lands (planting the inline analyze reddens by construction); diagnostics still publish; an edit during a fresh open is the analysis that settles; E117's ordering pins green. After: 7.9 / 1.5 ms; the `slow request: didOpen` trace line gone. THREE PINS HAD GONE SOFT under the scheduled open and were repaired: the two `package_recolor_tests` read a diagnostic straight after didOpen; `session_leak_tests`' open/edit/close loop dropped from tens of seconds to 88 ms the moment its twelve opens stopped analyzing — a gate quietly vacuous, now waits per open. Found: M26 (superseded analyses run to completion; did_open registers no pending generation).)

- **B194. `[derive]` on a generic type generates erased impls — CLOSED 2026-09-03, the owner's 'rec accepted': REACHABILITY** (Order 25's lane b194, merged after the seal at 635e3728: the derived trait binds every parameter the generated body REACHES — one some field type or variant payload mentions, at any depth — and a PHANTOM parameter takes a bare binder; undecomposable forms (tuples, closures, references) read as reaching every parameter, so the fallback is Rust's rule, never a bare binder over a body that won't compile. The lane STOPPED at the ruled default (Rust's every-parameter rule) because it broke C7's naming layer — `impl Handle<type T: Wire> with Wire` would make `Handle<Session>` un-sendable against the pinned doctrine that a name is not the thing it names — measured the amendment (919/919 across the census incl. the C7 pin), committed it unmerged, and wrote the owner's question; the owner accepted. Reflection: `StructItem`/`EnumItem` gain `generics: List<GenericParameter>` + `subject()`/`binders(trait)`; Rust mirror `DerivedSubject`; B188's arity gate REMOVED. 8 pins each claimed by a planted wrong rule (bind-everything reddens phantom/mixed/reflection/C7; never-bind reddens the other five). `[derive(Json)] struct Box<T> { value: T }` round-trips at two instantiations; bound-unmet anchors at the use site. Family FEATURE (phantom derives round-tripped correctly under the erasure; reached ones were refused). Left for the next order's landing lane: lift b174's operator `DERIVED_SOURCE` exemption (measured redundant); B189's covered set stays — dedup, not a bound. Union 5580/5580 at the merge.)

- **B214. `ret` inside `main` emits an illegal top-level `return` — CLOSED 2026-09-03, MISCOMPILE** (Order 26, lane b214-b215: `main` is inlined at module scope, so `Expr::FunctionReturn` lowered to a bare `return` and node refused the whole module at parse time. The LABELED BLOCK chosen over wrapping `main` in a function: measured, (a) moves 115 of 124 corpus goldens (+5,282 bytes); (b) wraps `main`'s body in `main: { … }` only when a `ret` is there and each module-scope `ret` becomes `break main` — the corpus byte-identical. Two new emitted forms (`js::Node::Labeled` / `BreakLabel`), the walkers, the emitter, and the `js::Node` interpreter (a labeled break propagates outward through every loop until its block swallows it). Async `main` untouched — §7.1 already runs it inside an invoked async closure where `return` is legal. Three seams: a valued `ret` assigns the exit-code temp so the `process.exit` after the block sees it; a resource-owning `main` keeps its `try`/`finally` and the break runs teardown; a divergent tail is emitted as the statement it is (B152's rule, which the no-drops path had no guard for). 7 pins in `returns.rs`, 5 plant-proved red; a new corpus program `main-ret.vl` + golden exercises the two node forms through the interpreter equivalence gate. Spec §7.1 states it.)

- **B215. a bare `let b = x is Some(let n);` binds `n` afterwards — CLOSED 2026-09-03, MISCOMPILE, the symptom corrected** (Order 26, lane b214-b215: `condition_polarity` was `None` in two different situations — outside every condition, and inside one that installs no frame (a `while`-shaped `for`, a `match` guard); B199 fixed the sibling conflation one layer in, this is the outer one. Rule: a capture bound in EXPRESSION position reaches the rest of THAT expression and nothing after it — the `&&` half kept, since `&&` short-circuits wherever it is written (`let big = x is Some(let n) && n > 1;` binds its own right operand). Three parts: `ConditionPolarity::expression(span)` at the spine root outside every condition; an `in_condition` marker for the `for`/guard (preserving today's behavior — B223); and a declaration with a real END is positional even in a module scope (B33 keeps none there, which is how a module-level `let present = slot is Some(let n);` handed `n` to `main`). Message: `'n' is bound only inside the `is` test that captured it: outside a condition that test is an ordinary `bool`, and nothing after it proves the payload is there. Put the test where a branch depends on it — `if maybe is Some(let n) { … }` — and read 'n' inside` — the steer quoting the author's own test. THE FILED SYMPTOM DID NOT REPRODUCE: every expression-position shape emits a live payload read (`$a[1]`), not a dangling name — the pre-fix result was a SILENTLY WRONG VALUE (`undefined` for an `i32`, `NaN` from `n + 1`). 12 pins plant-proved against all three parts; two corpus programs (`derive-enum.vl`, `derive-json.vl`) had been written in the refused shape — rewritten to read inside the branch, stdout byte-identical. Ledger row assigned at integration. Found: B223.)

- **B204. should `panic(…)` diverge for the checker as it does for paint? — CLOSED 2026-09-03, the owner's ruling built: `panic` is `never`, and `never` erases** (Order 26, lane b204: the bottom type already existed — `Type::Never`, already `panic`'s result, already yielding in `reconcile_type` — so nothing new was minted; what disagreed was DIVERGENCE. `Divergence`'s two constructors (checker = `ret`/`jump`; paint = those plus a `panic(…)` call and an endless `for { … }`) collapsed into ONE walk; the two leaves a walk cannot read off an expression's shape live in a `DivergenceLeaves` settled once per resolution phase — in `resolve_world`, after names resolve, before the constraint fixpoint — carried on both the analyzer and the `Program`, so the editor reads the checker's own answer and classification is no longer entry-file-only. Erasure: `never` yields in every join — if/else arms, match arms, a block tail, operator operands (`never + i32 = i32`), a call argument, return position — and removes ONLY the diverging participant: `let v: str = if c { 1 } else { panic("no") }` still reports at the `1`; a `for` with a bound `break`, the conditional `for`, an unspanned loop and a plain tail still owe their value. A REAL BUG found mid-lane: the first cut read the callee from `function_calls`, which fills AS CALLS RESOLVE, so 'does this body diverge' depended on fixpoint order — a `ReturnType` constraint resolved before the panic call it sat after saw a non-diverging body; the leaf now reads `call_subjects`, a walk-time record settled after names land. 12 pins, four plants (leaves emptied; every call a leaf; `Never`'s yield stripped; every block diverging) each reddening its own set; the std-side proof is the pre-existing `malformed_json_frames_fail_sticky` pin, red without the fix once N48's idiom is gone. Spec types.md §5.1 states `never` and erasure, §5.6 the diverging tail, memory.md R4 names a diverging leg; the tour gains two sentences. Found: B221 — a diverging NON-last statement still owes the tail (pre-existing, B124's list).)

- **N48. std's own dead code: two rpc.vl sites — CLOSED 2026-09-03** (Order 26, lane b204: `rpc.vl:308`'s `0 - 1 // unreachable — the panic throws` deleted now that a panic tail type-checks (B204); `rpc.vl:1252`'s unread `channel` → `_channel`, the idiom style.vl already uses at five sites. std compiles under its own paint.)

- **B209. `for x in tuple` exists undocumented and binds `x` as `any`: a write through `&mut` is silently discarded — CLOSED 2026-09-03, MISCOMPILE, the conservative fix; the unroll stays the owner's Q2** (Order 26, lane b209-b210: `iterable_element_type` had no `Type::Tuple` arm, so a tuple took the `Any` give-up default written for empty lists. Now REFUSED at the loop head — 'cannot iterate `(i32, str, bool)`: a tuple is a fixed sequence of independently typed elements, not a container of one element type, so a `for` binder has no single type to take and one body cannot be checked for every element' — with the steer that knows which spelling exists WHERE: positional reads or destructuring on a concrete tuple ('the value-level mapping form is not yet available over a concrete tuple'), the comprehension on a MAPPED tuple (whose elements have no positions yet — verified `t.0` is refused there). The give-up default narrowed, not deleted: a tuple/mapped source gets `Unresolved` (so exactly ONE diagnostic — `any` absorbed inconsistently and reported the one broken loop twice), the empty-never-pushed list keeps `any`. Census: ONE hit in the estate — an iterators pin that ran `for item in (5, 6)` beside List/str/array/Set/Bytes asserting only that it ran, relying on the `any` binder; removed with the reason written. 9 pins plant-proven red. Ledger row assigned at integration (the lane taught the gate NEW rows a third way; cascade-26's shape kept). Spec grammar.md §3.5 states the rule and records the unroll as open. Shaped to be replaced: every program it rejects, an unroll would accept.)

- **B210. a tuple receiver resolves no methods at any arity — CLOSED 2026-09-03** (Order 26, lane b209-b210: `resolve_method_call` dispatches on the receiver's SHAPE and its arms were Struct/Enum, bare-trait self, bounded generic, and the deferring Unknowns — a tuple fell to `_ => NotCallable` and never asked; the lookup itself needed nothing: B170's `operator_method` and `impl_member_candidates` compare the receiver against each impl subject with the SAME function over the SAME table at the same identity (arity, then elements structurally). Three nominal-only receiver sets sat below it, each reachable only once the one above was fixed: the lookup arm; the transformer's `resolve_member_on_type` re-dispatch (a specialized trait default's inherited `label` walked past the impl to the trait's bodyless requirement — the emitter's never-silent check caught it); and `reconcile_type`'s 'a value satisfies a trait-typed slot when it implements the trait' arm (`(1, 2) < (3, 4)` ran while `(1, 2).lt((3, 4))` was refused — the operator/method asymmetry one level below). 8 pins with per-site red-proofs; B170's and B177's operator pins green. Spec types.md §5.7 now states shape-independence for methods too. Found: B220 — an ARRAY receiver has the emission-side hole, pre-existing, a decision.)

- **B211. `reconcile_type`'s generic arm binds a generic the body has no right to bind: `swap(A, B)` prints A's impl reading a B's field — CLOSED 2026-09-03, MISCOMPILE, pre-existing** (Order 26, lane b211-b212, on the trait-typed-fields paper's bycatch: a generic parameter is RIGID inside its own body. Rigidity decided by `rigid_binder_scope` — `resolve_constraints` records the constraint anchor's scope before each `try_resolve`, one place rather than a parameter threaded through sixty call sites — and `generic_is_rigid_here` asks the pre-existing `generic_declared_by_enclosing_scope`; both binding arms guarded, a new arm accepts a rigid parameter against itself; rigidity is ASYMMETRIC (the other side still binds TO a rigid parameter, which is how `helper(x)` inside `fun f<T>(x: T)` binds `helper`'s `U := T`). The counterweight `inferable_generics` — the set an inference step may bind — set by the callee-binder paths, the enum-constructor paths, the struct-literal door, and a new `reconcile_declaration` wrapper at the ~11 impl-subject sites: an impl binder INHERITS the subject declaration's constraint id (B77), so inside `impl Cell<Cell<type U>>` the `T` of `impl Cell<type T>` IS `U` — without the carve-out `self.get()` returned `U` instead of `Cell<U>`, the hardest find. Two narrower rules std needed: a generic whose declared bound carries the trait SATISFIES a trait-typed position without binding (§1.4: a trait may satisfy a bound, never BE the binding — `List::sort`/`contains`); an identity mapped type `(U in T: U)` reduces to its source (`combine`). Six garbage runs recorded and refused: `swap` through B186's sugar and written `<P: X, Q: X>` (`A/7`), the three-way rotate (`A/7B/undefined`, now two reports), `f(x: X) { need_a(x) }` through sugar, `<T: X>`, `<T>`, and an impl method (`9`) — each an `i32` escaping a declared `str`. 13 pins, eight refusals red-proved against the pre-fix reconciler; CENSUS across 254 corpus programs, 10 examples, every docs fence and std: exactly ONE verdict change, and a real one — a tuple-comprehension pin had declared `: T` for a body producing `(U in T: i32)`, true only because the leak bound `T` to the mapped type; now written honestly, the old spelling pinned refused. Spec §5.6 gains the rigidity rule. Found: B218 (two implicit generics of one trait print alike — Q3's face), B219 (`compare_type_rigid` does not consult `rigid_binder_scope`).)

- **B212. duplicate top-level type declarations unrefused (resolving by declaration order); a struct written as a bound gets no sort refusal — CLOSED 2026-09-03** (Order 26, lane b211-b212: `check_duplicate_module_declarations` reads `declaration_order` per module scope, reports at the SECOND site with a note at the first, skips frozen std entities, and treats one entity re-declared under its own id (macro expansion) as one declaration; `fun`+`fun` had NOT refused either (it ran the second body) — covered by the rule. The bound refusal keys on `bound_position_type_ids`, populated in `register_binder` only — an impl SUBJECT is a trait position where a struct legitimately belongs, excluded; the steer runs `closest_name` over traits in scope (`Did you mean 'Show'?`). 13 pins: struct+struct, trait+trait, struct+enum, trait+struct (note asserted), fun+fun, three-copies-two-reports, an import-beside-declaration control, struct/enum bounds, the steer, an impl-binder bound, a trait-bound control, a cross-module same-name control and an on-disk duplicate through the real loader; all 11 refusals red-proved. Two ledger rows minted `NEW` — the gate's `Row.number` became a String validated as a number or `NEW`, exempt from the ascending check only (cascade-26 made the same change its own way; reconciled at integration); ids assigned at integration. Spec names.md §4.4 gains the one-declaration rule with the cross-module carve-out.)

- **B201. `[derive(…)]` on a type declared inside an inline `mod` generates its impl at file top level — CLOSED 2026-09-03** (Order 26, lane cascade-26: expansion is unified and `ExpansionOutput.items` carried only an origin span, so every generated list walked into the FILE's module scope whatever scope the deriving item was written in. Now `GeneratedItems { origin, module_path, nodes }`: the expander tracks the inline-`mod` path across `Node::Module`, the Rust-fallback text is bucketed per declaring scope with its own prelude imports, and `declaring_module_scope` steps the path down before the three generated walks — PLACEMENT, not spelling, so B188's anchoring holds. 6 pins in `modules.rs`, 5 red with the placement removed; B190's fixture dropped its hand-written impl. Found: B217 — two of the pre-fix diagnostics came through the `prepped_*` route the derived-range redirect does not cover.)

- **B202. `[expose]` of a non-Source field leaves the macro's `_` placeholder as a type — CLOSED 2026-09-03** (Order 26, lane cascade-26: `rpc.vl`'s `service` macro and its Rust twin `service_impl_source` pushed the literal `"_"` into `exposed_elements` when they could not read a field's element; both now generate NOTHING for such a field — no surface entry, no mirror, no `expose` call: 5 errors → 1. Scope note: the skip alone would have turned a `Source` impl whose element is not a written type argument (`impl Feed with Source<Note>` on a non-generic `Feed`) from loudly broken into silently compiling with no mirror, so `check_expose_fields` gained a curated refusal for that shape — the accepted set unchanged, the message actionable. 5 pins red-proven in two halves. The ledger gate was taught to hold a `NEW` id (`Row.number` as written plus an ordinal) — the first lane able to follow 'never mint a number' to the letter; the row assigned at integration.)

- **B205. `self.add(self)` in a trait default over a supertrait: supertrait `Self` not specialized to the subtrait — CLOSED 2026-09-03** (Order 26, lane cascade-26: inside a default body `self` is `Type::Trait(Doubler, [])` and `Add::add` is declared in `Add`'s terms (`B = Self`, both `Type::Trait(Add, [])`); the `Type::Trait` receiver arm of method lookup now records (declaring trait, sub-trait type), `resolve_method_arg_check` rebinds the parameter through `substitute_member_type`, and the `Self`-return specialization counts a sub-trait receiver — structural, because types are not interned. GATED to an argument-less `with` clause on purpose: with `Add<i32>` written, `b: B` and the `Self` return part company and only the written name separates them — B216. 4 pins, 3 red without the rebinding.)

- **B206. the conformance steer renders `B = Self` as the trait's name (`b: PartialEq`) — CLOSED 2026-09-03** (Order 26, lane cascade-26: `= Self` and `Self` resolve to one type, so `function_signature_label` printed the trait's name for both; `function_signature_label_for` renders by the WRITTEN name (B29's rule); B197's duplicate rendering deleted, both arms read the one label; hover deliberately unchanged (E128). 5 pins; the two B197 signature pins red without the fix, proving the routing; neither ledger row re-keyed.)

- **M23. M9's overlay rule refuses the base cache for an entry importing an OPEN buffer — CLOSED 2026-09-03** (Order 26, lane m23-m24: neither interning a copy nor a proof for a bare borrow — the stored world SHARES the analysis's overlay-served copies by reference count: every party that can read through an overlay allocation holds a `ModuleClaim` (an `Arc`), and `Arc::into_inner` is the whole protocol; like `Leaked`, a claim has no `Drop`, so reclaiming stays an explicit `unsafe` act and M7's unwound-analysis story survives. The analysis holds its own claims (drained into `AnalyzedEntry`, released by `AnalyzedProgram::drop`); each stored world claims inside `base_cache_store` while the storer provably still holds its own, and releases on every removal path (displacement, staleness, M24's budget, `clear`); a hit hands the world's claims to the receiver under the cache lock before the clone escapes; an analysis with nowhere to keep a claim (CLI, wasm, a transient reader) or a macro-world compile is served a counted MISS. §7.9.2's five hazards each land on a claim, not an ordering rule. Plants both red (M9's gate restored; store-without-claims → the corrupted resolution `cannot find 'greeting'`, the use-after-free as a wrong answer, exactly M9's own plant). ASan: 7 pins, zero reports, in the adversarial shape. kolt 3-file session: client.vl `base` 4457–6766 ms per round → 0 on every round, keystroke CPU 1,580 → 1,105 ms (−30%); views.vl/theme.vl unchanged (already hitting). Gate pin: the third file hits from its second analysis with both siblings overlaid, observation identity vs a cleared cache. Residual: a scope-less analysis can displace a claimed world (served a miss, stores a claim-free world) — no production path does it, self-heals on the next opted-in analysis.)

- **M24. BASE_CACHE has no eviction — CLOSED 2026-09-03** (Order 26, lane m23-m24: `BaseCacheState` under one lock — worlds, retained bytes (the store's own figure, carried on the world), a monotonic tick; a HIT refreshes `last_hit`, so eviction is least-recently-USED; a store evicts LRU until the total fits, the world just stored exempt (an oversized world bounds the cache at one, never switches it off); default 512 MiB in the compiler so any front end is bounded, `VILAN_BASE_CACHE_BUDGET_MIB` overrides at server start (`0` honoured, garbage reported and ignored); every eviction path through one routine, so an evicted world returns its M11 bytes AND its M23 claims. Pins on a six-entry package generated in the test: three retained under the default (vacuity guard), the budget evicting down, one world's bytes released, the untouched world the victim, a hit surviving a later eviction, a one-byte budget still serving the world just stored — planted two ways, both red; plus a budget-evicted world releasing its claims while the live analysis served from it keeps answering. E106 re-scoped: the base cache is now bounded, the parse cache's growth unmeasured.)

- **M26. superseded analyses run to completion (dropped at land, never cancelled); `did_open` registers no pending generation — CLOSED 2026-09-03** (Order 26, lane m26: a one-way `Arc<AtomicBool>` token (`vilan-core/src/cancel.rs`) installed on the analysis thread and read from a thread-local, checked after parse, before the entry walk, before `build()`, between every call of the ~40-call check sequence, at the post-pass boundaries, and INSIDE the two hot loops (per call site in `check_generic_bound_satisfaction`, per function in `scan_bodies_for_moves`); every checkpoint downstream of every process-global store (a partial world never reaches BASE_CACHE; a macro world is exempt); a cancelled `analyze_over_world` answers `None`, never a truncated program (a first draft that fell through panicked in the extraction tail — a real find). Cancel latency 13–69 ms on the exhibit. Bursts of 10 keystrokes: started 10, landed 1, cancelled 9 — per keystroke 79.8 → 13.2 ms CPU on the exhibit (6.0×), 988.7 → 127.2 ms on kolt views.vl (7.8×), measured against WARM analyses so M21 is not credited. `did_open` registers its generation; a dependency edit cancels the dependents' in-flight analyses and re-lands once; the keystroke path's `dependency_moved` seam wired — a moved dependency degrades the verdict to Stale (pinned through `semantic_tokens_full`). A starvation hazard the design introduced was closed: superseding a dependent takes its own debounce out of the running, so each dependent's text is read AFTER its supersede, `Schedule::start`'s stale-generation refusal closing the residual race. `diagnostics_budget` now CPU-clocked and HOLDS at 52–59 ms on the exhibit vs the 500 ms mandate — kept `#[ignore]`d for subject size with its reason rewritten; E126 stands (kolt's last keystroke 6.1 s wall). 8 server pins + 11 scheduler + 4 token pins, each red-proved by planting the mechanism out.)

- **B199. an `is` capture through a call argument reads the missing payload — CLOSED 2026-09-03, MISCOMPILE** (Order 26, lane bindings-26: `if always(maybe is Some(let n)) { print(n) }` compiled and printed `undefined`. B195 made the branch bookkeeping travel the condition's boolean spine and DROP the frame off it — but 'dropped' was `None`, the same state as 'not in a condition at all', so the capture fell back to B171's plain answer, visible to the end of the `if`'s scope. Now `ConditionPolarity::off_spine` NARROWS the frame (min-ing both visibility ends, clearing both else flags) where it used to be taken — narrowing rather than clearing keeps `&&`'s short-circuit rule alive inside the off-spine subtree. 8 pins; the exhibit, the `&&` written around the call, and two-calls-deep red before the fix. Found: B215, the bare `let b = x is Some(let n)` shape has no frame at all and dangles.)

- **B187. a negated `is` whose then-branch diverges binds the continuation — CLOSED 2026-09-03, the owner's Order-24 'yes'** (Order 26, lane bindings-26: B195 already computed the capture set a condition binds on its FALSE path — what it hands an `else` — and nothing read it for the else-less shape; `walk_branch` now returns that set beside the branch, declared into the ENCLOSING scope from the `if`'s end when there is no `else` and `Divergence::checker` says the then-block diverges; codegen needed nothing — the `is` subject temp already hoists into the enclosing JS block. 15 pins: `ret`, `jump break`, `jump continue`, a nested all-paths-diverging block, two captures from one pattern and two from a negated `&&`, shadowing both ways, scope ending with the block, four controls (non-diverging branch, `else` present, unnegated `is`, `||`-composed); ten red before. The `panic` guard is `#[ignore]`d naming B204 — the spec carries the note. Found: B214, `ret` inside `main` emits an illegal top-level `return`.)

- **B191. a let-bound self-call in a recursive tail cannot resolve (B126's orphaned residue) — CLOSED 2026-09-03** (Order 26, lane bindings-26: a DEADLOCK — `infer_type_path`'s `Expr::Variable` arm answered `Unresolved` for an `Unknown`-typed binding instead of reading through what it was bound to, while that binding's own constraint waited on the self-call whose type was the answer under construction. Now an UNANNOTATED binding with no type yet is inferred through its initializer, read undirected (as `resolve_variable` grounds it); an annotation wins; `exprs_seen` ends binding cycles. 8 pins in `returns.rs`, the old `#[ignore]` retargeted and un-ignored, five red before. Cost: the initializer hop is ~8% user CPU over the corpus (66.2 → 71.4 s A/B); `deep_nesting`'s linear ceilings hold.)

- **B208. a watch round's second trigger is LOST: round 2 never fires in 300 s despite 15 re-touches — CLOSED 2026-09-03, DIAGNOSED: not a lost wake-up** (Order 26, lane watch-26: the watcher has no event backend to lose a wake-up in — `--watch` is a 300 ms poll over a path→mtime map read BEFORE the round and consumed only on success, so a mid-round edit is still a difference at the next poll. What survived fifteen re-touches was the FRESHNESS STAMP: until 53c3b8bf a hook's stamp re-digested its inputs AFTER the hook ran, so an edit landing between the hook's last command and that re-hash was recorded as consumed and every later round called the hook `Fresh`; re-touching could not rescue it by construction — `wait_nudged`'s nudges rewrote IDENTICAL bytes, which move the mtime (rounds fire) but not the digest (the hook stays fresh). Reproduced twice: at the code (old stamp order + a slow hook → rounds 7, ran 1; fixed tree rounds 2, ran 2) and at the suite (14 iterations green on the fixed tree, then red the moment the old order went in — two tests, both at 306 s with `rounds.txt: 2, ran.txt: 1`, the recorded signature). Eliminated with numbers: mtime granularity (2000 rewrites, 2000 distinct mtimes, smallest gap 9.7 µs), the seeding swallow (a file's mtime is always AHEAD of a `now()` taken before the write), event coalescing/notify-under-WSL2 (no event backend). Landed: `watch_log.rs` (`VILAN_WATCH_LOG`) tracing every poll difference, verdict and round with clock and pid, printed on a harness timeout; the Linux regression pin 53c3b8bf never had (`an_edit_landing_while_a_round_runs_starts_exactly_one_more_round`, red-proved); nudges now vary content by attempt.)

- **B203. the watch/HMR leg-skip set is computed before any leg compiles — CLOSED 2026-09-03** (Order 26, lane watch-26: both watch loops decided freshness for every leg before any compiled, so a consumer of another leg's `dist/` artifact was measured against the artifact about to be overwritten. One shared schedule — `hmr::legs_in_artifact_order` (stable, cycle-tolerant, three unit tests) orders producers first, `LegSchedule::downstream_of_a_recompile` answers at each leg's turn; `build --watch` re-hashes at the turn (exact — it writes `dist/` per leg), the HMR round relies on the edge (it writes `dist/` after the loop). Pin red-proved: hoisting the decision back gives `rebuilt: false` for the server leg.)

- **B207. the LSP's containment test canonicalizes an unsaved buffer lexically — CLOSED 2026-09-03** (Order 26, lane watch-26: `document.rs`'s `is_within` and its sibling `same_file` use `util::canonical_path_of_unwritten` on BOTH sides (B198's function); pins: a symlinked-root unsaved buffer (unix) red-proved on its first assertion, an everywhere control.)

- **G22. a `.vl` file reached under two names collects twice — CLOSED 2026-09-03** (Order 26, lane watch-26: `TreeWalk`'s guard keyed on DIRECTORY identity only, so a file symlink handed one file to the collector twice; `EntryIdentity` now covers files from the metadata the walk already followed (one fewer stat); an unidentifiable FILE is still visited, opposite of the directory arm, deliberately. Pin `fmt_counts_a_file_reached_under_two_names_once`, red-proved at 3 lines instead of 2. Found: B213 — `fmt` walks once per root, so overlapping roots still double-report.)

- **M25. completion is the keystroke path's entire remaining budget — CLOSED 2026-09-03** (Order 26, lane m25-e125: two gatherers derived whole-program tables inside every keystroke — `auto_import_completions` walked both root scopes, classified every name every directly-loaded std/pkg module declares (2,239 candidates on kolt views.vl to offer 20, 1.05 of a 2.19 ms request) and an import path's origin arm did a `read_dir` per source root on a path §2.1 says may not touch the disk. Both moved into `vilan_ide::CompletionIndex`, built once where the program is (`capture_landed` on the analysis thread; `Retained::new` in the playground), held on `SymbolIndex::completion`: a per-module-grouped `AutoImportOrder` sorted by (tier, name) — a stable sort of a subsequence is that subsequence of the stable sort, so filtering after the sort answers the same first 20 — and a per-origin `OriginListing`. The item's manifest-fingerprint cache deliberately NOT built: the server has no watched-files handler to hang it on (E127) and `modules_in_root` also feeds the compiler's own B4 steer, so a process-global cache would put a staleness class INSIDE the compiler; per-analysis capture can never be staler than the analysis the request already serves. Exhibit: completion 0.628 → 0.126 ms, burst 0.568 → 0.103 (thread CPU); kolt views.vl burst 3.76 → 2.64 ms interleaved at loadavg 80–86; candidate counts identical at every position. Pin corpus vs the old mechanism kept as `keystroke_completion_rebuilding_index`; non-vacuous when the capture is built without import roots. Union stalled at loadavg 115 — taken on 588/588 across lsp/wasm/ide plus the dependency argument. Found: M29 (whole-buffer tokenize/parse per request; member completion now the costliest request), E127.)

- **E125. `semanticTokens/range` answers in analyzed coordinates while `full` answers live — CLOSED 2026-09-03** (Order 26, lane m25-e125: `semantic_tokens_range` now calls `Document::keystroke_tokens_in_lines` and encodes against the LIVE index — a live window's tokens come from at most two contiguous stretches of the capture's `token_lines` (the window itself, identity, and the window shifted by `tail_line_shift`), each span mapped by `Anchor::map_span` and kept if its live start line is in the window, plus the edit window's syntax tokens lexed only when its lines meet the request; Stale/Unusable degrade to whole-file syntax filtered to the window, exactly as `full`. Pin `a_viewport_follows_an_unlanded_edit_above_it`: one line typed above a 20-line viewport, equal to `full`'s window byte for byte AND asserted to differ from the capture sliced by analyzed line — non-vacuity built in; the cost pin unmoved.)

- **N49. `release_differential` is the union's critical path at 615 s, one test — CLOSED 2026-09-03** (Order 26, lane hygiene-26: a `corpus_programs!` macro over a declared list writes one test per corpus program, so nextest schedules one process each and a regression names its program; held to `vilan/test/` in both directions by a coverage gate that also re-asserts the old loop's non-vacuity bounds. THE FINDING: 600 of the 607 seconds were `watch.vl` ALONE — it blocks forever on `flat.next()`, both builds hit the 300 s deadline, and the gate compared two identical timeout strings and PASSED — a verdict that could not come out any other way, invisible while every program shared one clock; now in `NOT_RUN` with that reason (N51 asks the owner what the corpus should hold). After: the binary's longest unit 607 → 14 s in the union; the union's floor can no longer be 607 s; the new critical path is `dependent_edit_measurement` at 269 s, pre-existing. Found: N52, `infer_differential.rs` has the same shape.)

- **N50. three more exemption tables without N42's inverse check — CLOSED 2026-09-03** (Order 26, lane hygiene-26: SEVEN inverse checks, one per table — `NON_KEYWORD_TERMINALS`, `PROSE_DEFINED`, `PROSE_RIGHT_HAND_SIDES`, `UNREACHED_PRODUCTIONS` in the grammar gate; `APPENDIX_HEADS_NOT_HELD`, `HEADS_THAT_ARE_NOT_MESSAGES` in the ledger gate (their two-pass 'is the head held' logic unified into one `appendix_head_is_held` used by both directions so they cannot drift); `DELIBERATE_NON_BUG_IGNORES` in the ignored-pins gate — each asking its question with the predicate of the gate it widens, each red-proved by removing the exemption's reason and restored. Every table found clean; nothing had rotted yet. Plus the seal's lesson written into CLAUDE.md in two sentences: a `#[cfg(windows)]`-only pin is compiled here and verified only by CI, and a report that lands one must say so by name; a pin that waits on a process is a race until its ordering is proven by the harness's own sequencing, never by a sleep.)

- **B224. a condition's `&&`/`||` short-circuit is lost in codegen: the right operand's statements hoist before the `if`; an `else if` condition hoists above its whole chain — CLOSED 2026-09-04** (Order 27, lane b224, vilan c7bce95a: the emitter had no statement slot for a condition, so any right operand of `&&`/`||` that lowered to statements (an `is` subject temp, an if-expression, a side-effecting `is`) was hoisted before the `if` and the short-circuit was lost; `else if` conditions hoisted above their whole chain. Now `And`/`Or` walk the right operand into a scratch: empty → byte-identical emission; else temps declared UNINITIALIZED in the enclosing block and the work evaluated inline in a new `js::Node::Sequence` (`$a[0] === 0 && (($b = $a[1][0]), $b[0] === 0)`) — the hoisted declaration keeps B187's continuation reads nameable; a non-expressible operand falls back to `let $r = lhs; if ($r) {..}`; `walk_branch` gives a non-root condition its own prelude (`else { prelude; if (..) }`), B59's match-guard shape. `Sequence` got an arm in every js-tree walk. 14 RUN pins, 9 red-first; goldens byte-identical (the estate has no right-operand `is` site); spec §5.7 states the evaluation half. One self-inflicted regression caught by an existing resource pin (a root `if` lifting a resource temporary emitted a bare `else`) — fixed with an `is_root` guard and pinned. Finds: B230 (filed same day and overturned by its lane), B231.)

- **B225. the struct-literal door reopens the enclosing impl's OWN rigid parameter: `impl Pair<type T> { fun make(x: T) { Pair { b = "s", a = x } } }` silently returns `Pair<str>` (B219's first live consequence) — CLOSED 2026-09-04** (Order 27, lane b225-b219, vilan 8c21e6b6: the struct-literal door B211 installed reopened the enclosing impl's OWN rigid parameter (an impl binder inherits the subject's ids, B77), so `impl Pair<type T> { fun make(x: T) { Pair { b = "s", a = x } } }` bound T := str on one field while the rigid-T field passed through B219's hole. Census of 52 struct literals inside generic impls across std, tests, docs, kolt and the website: 26 same-struct sites, 16 `map<U>`-style, 10 controls — NO program stops compiling (kolt's committed search.vl is concrete). The restrict-to-non-rigid option was built and measured — it breaks `map<U>` (the method's own U is rigid too) — so `instantiate_literal_parameters` mints FRESH ids for the struct's parameters the enclosing declaration also owns and lets the impl's rigid T meet them one way, cached per literal. 8 pins in generics.rs (4 red-first) + the rigidity-agreement unit pin. Two already-refused forms now report in the body instead of the call site (pinned as better). §1.4 edit list for trait-objects.md in the lane's report (applied at seal).)

- **B219. `compare_type_rigid` is `reconcile_type`'s read-only twin and does not consult `rigid_binder_scope`; §1.4's table says they agree exactly, and now they do not — CLOSED 2026-09-04** (Order 27, lane b225-b219, vilan 8c21e6b6: one rigidity predicate, `generic_is_rigid_here`, now asked by both `reconcile_type` and `compare_type_rigid`, whose new arms mirror the reconciler's asymmetry; the B29 conformance-only `rigid` list is untouched; one caller (`resolve_pattern`'s literal arm, B82) deliberately stays off the answer and opens the subject's own parameters instead. Pin `both_predicates_give_one_verdict_on_b211s_garbage_shapes` (red-proved on P-against-Q). Promoted the same day by B225, its first reproduced consequence. Finds: two further unsound accepts filed (B233 operator dispatch with two rigid parameters; B234 a rigid parameter as a `bool` condition); `reconcile_type`'s ~60 call sites remain the audit candidate.)

- **B226. a self-import (or a cycle back) into the ENTRY module makes it load as a module and skips its walk and derive expansion: `main` and every `[derive]` vanish — CLOSED 2026-09-04** (Order 27, lane checker-27, vilan 24d4377f: `is_entry_module` was origin-blind (a canonical-path equality meant for a std file opened directly), so a `pkg::<entry>` self-import or a cycle back through the entry made the entry load as a module and skipped its program walk and derive expansion — `main` and every `[derive]` vanished ("SidebarTab does not implement PartialEq"). Now gated on `Origin::Std | Origin::Dep`; a pkg re-entry aliases the module name onto the entry's own scope; a direct self-import WARNS and a cycle/cross-module import of the entry is an ERROR (unsatisfiable by construction — a sibling resolves its imports before the entry walks). Ledger rows 386, 387. Four pins + a CLI telling pin; two existing fixtures that imported from the entry were reshaped with a third file — evidence the construct only ever "worked" by costing the entry its walk. kolt's sidebar.vl carries the silent non-entry form (the control).)

- **B227. an `any` parameter FILLS an unfilled closure parameter instead of coercing: one `print(x)` types `x` as `any` and silences every later check on it (`on:keydown(|event| ..)`'s `event: any` is this) — CLOSED 2026-09-04** (Order 27, lane checker-27, vilan 3c249aed: B13's "adopt a concrete declared parameter" rule let `Type::Any` through its guard, so `print(v)` — or any `fun sink(m: any)` — WROTE `any` into an unfilled closure parameter's slot before the enclosing call typed it; `on:keydown(|event| { print(event); .. })` typed `event` as `any`. `Type::Any` now has its own arm: fall through, slot unfilled, no `Deferred` (a closure whose only use is an `any` call would deadlock). 8 pins (6 plant-proved). Census: kolt (3 entries) and the website (4) clean before and after — no verdict changed; `any` now behaves as a generic `fun sink<T>` always did.)

- **B228. a ZERO-argument method call anchors its arity diagnostic on the DECLARATION, so a std callee's error renders against std's file at the caller's offsets (`<div .styled() />` looks undiagnosed) — CLOSED 2026-09-04** (Order 27, lane checker-27, vilan 1cd12461: `Constraint::MethodArgCheck` anchored a ZERO-argument call on `member_id` (the declaration), so the arity error for `<div .styled() />` rendered against std's browser/ui.vl at the caller's offsets, quoting a comment, with the author's file unnamed. Anchored on the constraint's own `call_id`: primary label in the caller's file, "declared here" as its own std block; the too-many control byte-unchanged. 3 pins asserting the primary label's FILE.)

- **B222. a guard ending in `panic(..)` does not bind the continuation: B187's decision is made during the walk, B204's divergence leaves are settled after it — CLOSED 2026-09-04** (Order 27, lane divergence-27, vilan 880ad531: B187 decided a negated guard's continuation binding during the walk while B204's `panic`/endless-`for` leaves settled after it in `resolve_world`. The walk now records the candidate (false-path captures, then-block, scope, `if` end) and one pass in `resolve_world` decides every guard once the leaves exist; the queued bare-name drain runs in two parts (only a use a guard in an enclosing scope might publish is held back) and `declare_scope_value_until` orders a scope's record by `visible_from`. Moving the leaves EARLIER was tried and regressed `io::panic(..)` (a module-qualified subject becomes `Expr::Local` only in the member-path pass) — pinned. B187's pin un-ignored + 4 pins; the spec's tracked-gap note replaced by "every leaf counts".)

- **B221. a diverging statement that is not the LAST statement does not exempt the tail: `fun g(): i32 { for { … } print("dead"); }` is refused — CLOSED 2026-09-04** (Order 27, lane divergence-27, vilan 1f95343b: `check_return_position` asked only the tail and the LAST statement, so a body diverging earlier (`for {..}`, `panic`, `ret 1; print("dead")`) still owed a tail value — the one shape paint and the checker described differently. It now asks `Divergence::block` about any statement; the same widening lands in `return_evidence` and both closure `tail_dead` sites, so the four askers agree; `Function::last_statement_id` and `Analyzer::expr_diverges` fell out dead. 6 pins, 5 red-first; one spec clause.)

- **B223. the `for` condition and the `match` guard install no polarity frame: a capture under a NEGATION there still binds where B195 refuses it in an `if` — CLOSED 2026-09-04** (Order 27, lane divergence-27, vilan f17b682c: the `for` condition and the `match` guard install `root(condition_end)` polarity frames (with the else collector swapped out beside them), `in_condition` retired; a negated capture no longer binds in a `for` body or a guard's leg. CENSUS: ZERO affected sites in std, tests, examples, docs, kolt, the website (two `is` tests exist in either position, neither negated-with-capture). Family breaking, entry states the census. The closure-in-a-condition case fixed at the root (a closure body starts from no frame → expression position → B215's rule via a second arm of `expression_position_capture`); B199's pin re-voiced to B215's steer. 5 pins, 3 red-first. `condition_polarity` stays an `Option` (None = no spine yet).)

- **B229. an unresolved value argument silently deletes the `run` site: every context read then fences, burying the real initializer error; and the 'flows through this call' note spans the whole receiver chain — CLOSED 2026-09-04** (Order 27, lane context-27, vilan ec6a20ac: (b) an unresolved value argument left `run` unselected, so the site was never recorded, `bound` stayed empty and every strict `get` fenced — three "can be reached without an enclosing run" errors burying the real missing-field error, reported last. New `Program::unresolved_method_calls` (filled once after the fixpoint from leftover `MethodCall`s); the context pass skips the coverage verdict and rewrite recording for exactly the contexts an unresolved `run`-shaped call names, and only when the program already carries diagnostics — a clean program's genuinely uncovered read still fences (pinned). (a) the "flows through this call" hop note anchors on `member_name_spans` (the callee name) with `span_of` as fallback — a method hop no longer spans the whole receiver chain. 6 pins, 5 red-first; E78's dispatch-hop pin re-anchored on purpose. Find: a leftover `MethodCall` has no residual diagnostic of its own (B232).)

- **B216. a PARAMETERIZED supertrait clause loses `Self` in a default body: `trait Bumper with Add<i32> { fun bumped(self): Self { self.add(1) } }` → `Expected Bumper, but got Add instead.` — CLOSED 2026-09-04** (Order 27, lane cascade-27, vilan c4c6d8f4: B205's argument-less gate lifted: `supertrait_self_at_call` records the clause's arguments beside the sub-trait and `supertrait_position_type` decides each ambiguous position by its own annotation's SPELLING (B206's written-name rule, B29's residue, at method resolution) — `Self` → sub-trait, a declaring-trait parameter name → the clause argument, unreached → the sub-trait, unrecoverable → as declared. 6 pins, all red-first (`Expected Bumper, but got Add instead.` gone; two-parameter clause; per-specialization dispatch). Find: a `= Self`-defaulted parameter is read as a BOUND through a parameterized clause (B235).)

- **B217. a generated-code type miss resolved in `build()` (the `prepped_*` route) is not anchored at the `[derive(..)]`: it lands at the declaring `mod` or the type — CLOSED 2026-09-04** (Order 27, lane cascade-27, vilan 2257224a: a generated walk now records the TYPE-id range it minted (`derived_type_origins`) beside the entity range; `push_at_written_type` re-anchors through it and both `prepped_*` type-reference drains raise every refusal that way. Pin: a test-owned derive template emitting `fun planted(self): NoSuchType` — pre-fix the span was ten characters into the file's leading whitespace with no provenance; post-fix "in code generated by this attribute: cannot find type 'NoSuchType'" on the `[derive(Planted)]`. Residual: the resource-container refusal (~8155) keeps the entry fallback (no entity or type id).)

- **E128. hover renders a `= Self` parameter as the TRAIT's name: `fun add(self, b: Add): Add` — CLOSED 2026-09-04** (Order 27, lane cascade-27, vilan 6e9974b6: `SignatureSubject` gains the side it renders for; `declaration_labels` renders a trait member for the declaration side, where `Self` and a `= Self` default are one promise and print as the literal `Self` (`fun add(self, b: Self): Self`). 6 pins, 5 red-first. Find: hover on `PartialEq::eq`/`PartialOrd::lt` returns nothing through either route — a target-resolution gap (E138).)

- **E132. linkedEditingRange answers in ANALYZED coordinates; the client mirrors keystrokes into unrelated live text (E125's twin; CORRUPTS working code) — CLOSED 2026-09-04** (Order 27, lane editor-sync-27, vilan fee45662: `linked_editing_range` converted through `analyzed_offset` and `linked_tag_ranges` re-parsed `analyzed_text()` — a raw parse handed the stale text; during the debounce the client mirrored keystrokes into whatever sat at the old offsets (the owner's "unrelated text deleted"). Now parses the LIVE text and converts through `document.line_index`; three pins driving the real Backend (the mirrored range landing on live code was red: open=3:2–3:5 over `let counter = 1;`). The interim `editor.linkedEditing: false` is no longer needed.)

- **E133. the reference index is end-exclusive: rename and find-references miss with the caret at `name|` — CLOSED 2026-09-04** (Order 27, lane editor-sync-27, vilan 92b4eb58: `ReferenceIndex::at` was end-exclusive, so `name|` gave `prepare_rename` → `Ok(None)` (VS Code's "can't be renamed"); it now falls back to the row ending at the offset (rows are identifier-exact and non-nesting), and `offset_touches_a_token` is end-inclusive so hover agrees. 4 pins red-first. Residual: `vilan_ide::analysis::entity_at` is separately end-exclusive for a bare USE (E139).)

- **E134. a struct-init shorthand's two references share one span; the index dedup drops the variable's use (unused-local fade + find-references), and rename at that span serves only one name — CLOSED 2026-09-04** (Order 27, lane editor-sync-27, vilan 26680c46: a struct-init shorthand `A { x }` produced two references at ONE span (the synthesized local read and the field key) and the index dedup dropped one by declaration order — the local faded as unused and find-references missed it, or the field lost its rename site. `Occurrence` gains `co_definition`: a Field + Binding pair at one span keeps one row carrying the other name; `by_definition` indexes both; `unused_local_spans` and references see the use; rename at a shorthand REFUSES from either side naming the `x = x` expansion (an expansion needs per-span replacement text — the owner's call, E143). The std double claim INVARIANT 2 had been hiding: three field shorthands in iterator.vl (`Mapped { upstream = self, fn }` and siblings) were losing a row every analysis. Pins over both declaration orders. Find: derive-template double claims across expansions (E144).)

- **E127. the server has no `did_change_watched_files` handler: the client sends them, tower-lsp drops them, and the disk-read paths (`modules_in_root` for the B4 import steer and `check_library_contract`) go stale until the next analysis — CLOSED 2026-09-04** (Order 27, lane editor-sync-27, vilan 14a3e7e6: the server registered no `did_change_watched_files` handler (tower-lsp dropped the client's notifications). Now: `.vl`/`vilan.toml` events outside open buffers invalidate the parse cache entry, bump the revision and `reanalyze_dependents` from a `watched_sweep_root` (manifest → its directory; source → the deepest open package root); the client glob widened to `**/{*.vl,vilan.toml}`. Four pins (two in the wall-clock group, red-first by burning the liveness bound with the handler stubbed). The group's build_hooks mirror constant had to follow at integration.)

- **E129. a code `::`-path completes only its LAST segment: `style::FlexDirection::` and `style::Color::` offer nothing — CLOSED 2026-09-04** (Order 27, lane completion-27, vilan b79f49ba: `code_path_completions` read only the last `::` segment and looked it up in SCOPE, so `style::FlexDirection::` and `geo::Shape::` offered nothing while the import arm descended fine. It now scans the whole chain, resolves the head in scope (E53 untouched) and walks the rest through `namespace_member`; the keystroke index (keyed by module name) declines a nested path instead of offering an unrelated module. 4 pins, 3 red at 289e2a2b.)

- **E130. member completion is blind on a call whose DECLARED return is a type parameter: `SignalCell<T>::get()`'s `.` offers nothing (E107's other half) — CLOSED 2026-09-04** (Order 27, lane completion-27, vilan 8b99fda6: `call_result_type_id` returned a declared `Type::Generic` verbatim, so `SignalCell<T>::get()`'s `.` offered nothing (E107's other half). `substituted_return_type_id` resolves it through `Program::method_call_substitution` — keyed by the constraint id the Generic wraps, populated mid-edit (verified). The positional generic records added no reach and were removed. 4 pins, 2 red-first.)

- **E131. member completion resolves a CALL receiver in ANALYZED coordinates: a receiver typed since the landing answers the OLD expression's type (`<div .styled(const style::style().` offers View's members) — CLOSED 2026-09-04** (Order 27, lane completion-27, vilan 0c8424bf: a CALL receiver was resolved in ANALYZED coordinates through a clamping line/character round-trip, so a stale request answered the OLD expression's type (`<div .styled(const style::style().` → View's 24 members). The receiver's identity now comes from the LIVE token stream (`live_receiver_type_id`: bare binding, `f(..)`, `a::b::f(..)`, `x.m(..)`, `x.f`, typed against the landed program); what it cannot type falls back to the analyzed arm only OUTSIDE the edit window (`analyzed_agrees_at`, a two-sided anchor). 5 pins, 4 red at 289e2a2b, the stale answer asserted equal to the settled one.)

- **M29. completion's third tranche: a whole-buffer `tokenize` + `ParsedSource::parse` per request, and the member-completion resolution walk (now the most expensive single request) — CLOSED 2026-09-04** (Order 27, lane completion-27, vilan 351f0f3f: completion's third tranche: a `MemberTable` grouped by nominal id (was a per-request walk over every implementation and trait), auto-import edits computed against the analyzed text at build time and re-mapped through the anchor (a request now parses the buffer ZERO times — pinned by count), the whole-buffer raw parse behind the element/css classifier paid only when the token stream shows `<` or `css` before the cursor, `doc_comment_of` walking backwards. E121 exhibit thread CPU: 0.813 ms completion / 0.814 burst → 0.415–0.485 / 0.530–0.665 (debug; loadavg 79 → 124). Find: the 0.2 ms budget was already red in this profile at 289e2a2b — it is a RELEASE figure and the gate has no profile guard (E141); ~half the remainder is documentation rendering per request (M39).)

- **E135. an incomplete `a::` path rolls the `::` back: the failure surfaces as a missing `;`, or the path re-binds to the NEXT line's identifier and swallows that statement — CLOSED 2026-09-04** (Order 27, lane parse-fmt-27, vilan 813823a6: `parse_static_accessor`'s `None` arm rolled the `::` back, so `let x = style::` surfaced as `expected ';'` on the `::` plus a bogus type error. The arm now notes `a name after `::`` and DECLINES (the `Node::Error` stand-in was tried and rejected: it leaves the cursor on the next line's `<` and moves the error to the `let`); `farthest_failure` survives the rollback so statement recovery surfaces it once, located. 3 pins; ledger row 388. NOT fixed: the "print after" face — `style::` ⏎ `print(..)` is the legal path `style::print`; only a "`::` does not cross a line break" rule catches it (E142, the owner's).)

- **E136. a multi-value element attribute declines the WHOLE element; its curated diagnostic is discarded and the span lands on the tag — CLOSED 2026-09-04** (Order 27, lane parse-fmt-27, vilan 7dfe84da: the comma arm of `parse_element_head_item` hard-declined (`return None`), failing the whole element; with a paired `</div>` the statement never parsed and `attempt`'s error truncation discarded the curated message, so the span landed on the tag. Now mirrors the `.`-chain arm: report at the comma, `scan_balanced` past the attribute, `Some(None)`. Pins: the paired-tag fixture beside `<div name(a, b)/>`.)

- **E137. the formatter never breaks a ONE-link chain: a single `.map(closure)` stays inline at any width (220 columns) — CLOSED 2026-09-04** (Order 27, lane parse-fmt-27, vilan 97b0d679: `is_breakable_chain` required ≥ 2 call links, so `combine(x).map(|..| <150 cols>)` never broke. The one-link door now opens only when the link's last argument is a CLOSURE and the split is MEASURED (inline first; the link rolls onto its own line only if the first line is still over budget); a closure's expression body inherits the split. A blanket ≥1 threshold reflowed four shapes worse and was narrowed away. Seven reflows reviewed by diff against a 289e2a2b binary (four `.on_request(|r| ..)` chains, ui.vl's `.on_event`, fs.vl's `raw.map(|e| Entry {..})` staying inline, the fullstack template — the only gated one, rewritten). Find: 96 tree files already differ from the formatter; there is no `vilan fmt --check` gate (N55, the owner's).)

- **B213. `vilan fmt` builds a fresh `TreeWalk` per command-line root, so overlapping roots double-report one file — CLOSED 2026-09-04** (Order 27, lane parse-fmt-27, vilan c46df96e: `fmt` ran one `TreeWalk` per command-line root, so G22's identity set did not span roots and `vilan fmt --check src src/pkg` printed a file twice. `collect_vl_files_across(roots)` walks every root with one walk, re-rooting between them. Pins beside G22's (both root orders, the rewrite path, a disjoint control). `check`/`build` take one entry and `watch_roots` hands `scan_vl` one root — neither has the shape.)

- **E126. the E121 diagnostics gate's generated exhibit does not exercise the diagnostics path: 104 call substitutions vs kolt's 18,540, 19× less warm CPU — CLOSED 2026-09-04** (Order 27, lane e126, vilan e27dee56: the view-shaped exhibit generator (1,791 `View`-returning icon functions each chaining `.child(..)` ×4, a `Panel<T>` state module over `SignalCell`/`combine`/`map`, 40 element-syntax components over a `Style` impl) tracks kolt's client leg within the 2× band both ways: entities 104,430 vs 99,522, substitutions 26,000 vs 18,745, implementations 341 vs 433, warm keystroke CPU ~1.1×. `diagnostics_budget` re-anchored on it and RED HONESTLY at 1,053 ms CPU (loadavg 121; 1,138 at 166) against 500 ms — kept `#[ignore]`d naming the number and M19-t1; the keystroke-half budgets green on both exhibits (burst 0.181 ms, 108× non-vacuity). M27's measurement half: `lsp-index` was already on the phase line (E106) but printed BEFORE `capture_landed`; moved after, with `lsp-landed`, and onto the session trace. Four constants crossed from kolt, with provenance; nothing copied. Open: the 2× band's durability per box/day.)

- **M28. `plan_resource_drops` costs 350–600 ms per analysis and switches on WHOLE-PROGRAM the moment any type is `resource` — CLOSED 2026-09-04** (Order 27, lane m28, vilan eba51e4f: the predicate half: `resource_reaching_roots` selects the bodies whose types can reach a resource (RN = declared resource nominals closed under containment over declared fields, generic parameters excluded; `type_reaches_resource` over nominal/args/tuples/arrays/closures; seven routes each pinned; unresolved callee → selected), under the kept `declares_a_resource()` gate; `drop_sink_types_by_root` moved below the gate. Goldens byte-identical (the proof of no under-selection); kolt client.vl −8% CPU (1.775 → 1.642 s median; the 285 ms of `plan_resource_drops` the predicate removes: 81 ms sink-types walk, 86 ms body walk over 27,016 candidates, 114 ms `infer_type` on none-resources); headline pin: 1,000 resource-free functions do not change enrolment (a thread-local instrument). The SECOND half of this item is WITHDRAWN as unsound: `LastUse` has a non-resource consumer (copy elision rule 2 reads `is_last_use` for every variable), so narrowing it changes elision and corpus bytes. Where its time goes is filed instead (M37 `collect_unfollowable_loans` allocating per call; M38 `collect_resource_bindings`).)

- **E124. gray TOP-LEVEL dead items in a single-entry `[package]`? (owner's decision) — CLOSED 2026-09-04** (Order 27, lane e124-build, vilan b9022a67: dead-code paint, built on the accepted paper with the owner's five answers: (exemptions first, red-proved) const-module-binding initializer edges collected into paint-only maps beside the emission graph, `context::thread_contexts` recording `Program::context_bindings`, `dead_items::paintable_items` offering top-level `fun` + module-`let` only; `main`, `_`-led names, trait members, trait-IMPL members (unpainted in v1, stated), DERIVED_SOURCE exempt by construction. Step 1 the module slice (`unreached_module_entries` from `file_platform_choices`' answer — free for a multi-entry package). Step 2 the union across the manifest's entries keyed on (canonical path, name span), one analysis per entry in `spawn_blocking` on a 600 ms package clock, cancellable; `[library]` and `generated` roots get nothing. Step 3 withdraw-on-edit (`did_change` drops the union). Cost pinned on a generated 3-entry exhibit: union walk 10.6 ms CPU (budget 150); the keystroke gate did not move adversely (`diagnostics_budget` 433.6 → 262.0 ms in debug). Residue filed as E140: paper §6.1 pins 4/10/11/16/19 unbuilt, withdrawal package-wide, union analyses serial.)

- **A38. the rpc service is one process-wide instance; transport-rpc Q9 says the instance IS the connection's session (per-connection factory + injected globals) — CLOSED 2026-09-04** (Order 27, lane rpc-27, vilan 9b96c740: `Service::factory(build: |Connection| S, codec)` beside the unchanged `Service::new`; `Connection { id, session, remote_addr }`, `Session { identity, credential }`, `trait RpcService { dispatcher_for }` implemented by `[service]` alongside the inherent `dispatcher()` (additive — every existing service compiles); `accept_socket` builds the connection's own protocol and threads it into every `respond`; `on_disconnect` drops it; factory services refuse the connectionless `POST {mount}rpc` with 501. Contract hash pinned identical to a 289e2a2b build. Per-instance `[expose]` mirrors pinned (a build-once factory plant cross-talks: `b-mirror:2`/`a-mirror:9`). kolt migration recipe in the lane's report (ungated `/auth/` mount for register/login; shared module-level cells handed to every instance; drop the token parameters).)

- **A40. the rpc WebSocket upgrade authorizes nothing and never echoes a subprotocol; a pre-upgrade `authorize` hook + a token subprotocol (transport-rpc Q4's transport half) — CLOSED 2026-09-04** (Order 27, lane rpc-27, vilan 302d0b97: `Handshake { path, headers, protocols, remote_addr }`, `Reject { Unauthorized | Forbidden | TooMany }` → 401/403/429; `Service::authorize(async |Handshake| Result<Session, Reject>)` run in `fold_service_upgrades` BEFORE the upgrade (no socket, no session on refusal); `max_connections`, `handshake_rate`, `handshake_timeout`; client `new_web_socket(url, protocols)`, `rpc_protocols(credential)` (the `token.` convention), generated `Client::connect_with(url, codec, protocols)` — additive (`connect` untouched); the offer re-presented on reconnect; authorized services refuse the SSE/POST legs with the first-ever `RpcError::Unauthorized` producer. TWO FIXES inside: the 101 never echoed `Sec-WebSocket-Protocol` (a browser closes such a connection) — it now selects `vilan-rpc` or the first non-credential offer, bare handshake byte-identical; and teardown rode `close`, which node never fires for an upgraded socket a peer half-closes — every connection lost to a dead network LEAKED its session and wire entry for the process's life; `end` tears down too. Finds: a refused client burns the retry budget and cannot distinguish 401 from unreachable (A47); `authorize` awaited in the upgrade handler wants a verification timeout (A48).)

- **A41. the dynamic-expose path leaks and goes dead: `ReactiveServer::stop` never removes the `sources` entry; `reattach_mirrors` rebinds only `__attach`'s positional list — CLOSED 2026-09-04** (Order 27, lane rpc-27, vilan 98c7e11d: both holes on the runtime-minted channel path — BUT the item's first fix (`stop` removing the `sources` entry) was WRONG and is recorded so: an `Unsubscribe` is client-local demand hitting zero and `RemoteSource::acquire` re-subscribes on the same id when a view remounts; removing the entry made every remount silently dead, plain `[expose]` mirrors included (the pin showed `second:3` vanish). Withdrawal is its own verb, `ReactiveServer::revoke(channel)`. For `reattach_mirrors` the refuse-clearly branch: a fresh session cannot rebind a channel an application method minted (that is A39), so `invalidate_dynamic()` sets the mirror to `Waiting` and the app re-runs the rpc (`ReactiveClient::source` vs `attached_source`). Pins in a new in-process `reactive_channels.rs` and one unix-only reconnect pin across a real server restart.)

- **A43. `Source` has no lazy subscribe: land the `observe`-shaped member (`on_change`) that reactive-traits Q5 owes, rather than flipping `sub`/`effect` — CLOSED 2026-09-04** (Order 27, lane std-27, vilan f92fcc4b: `Source::on_change(observer)` and `effect_on_change` — landed as members with a DEFAULT body (wrapping `sub` and swallowing its one immediate call, honest against `sub`'s contract), not requirements: a requirement breaks nine `impl .. with Source` in shared tests, three doc examples and kolt's `StorageSignalCell`; `SignalCell` overrides with `observe`. `sub`/`effect` stay eager (every UI binding is the initial paint; zero sites guard the first call). reactive-traits Q5's `map` widening fell out free: `map` moved from `SignalCell` onto `Source`; `combine` attaches with `on_change` (its n redundant sets are not observable from vilan code — pinned as a declaration gate). Corpus `reactive-on-change.vl` + 3 gates, red with the eager form planted. Open (A49): tighten to a requirement later, migration paid.)

- **A44. `Source::selector()`: O(2) per-key selection notification (Solid's `createSelector`); the owner's `sub_condition` is O(n) and lands inside the notify loop — CLOSED 2026-09-04** (Order 27, lane std-27, vilan 3f617699: `selector<T: Hashable + PartialEq, S: Source<T>>(source): Selector<T>` with `Selector::of(key): SignalCell<bool>` — one `on_change` on the source, a per-key map, two cell writes per change (Solid's `createSelector`); a struct method rather than a returned closure because `sync` is refused on a return type and a returned closure captures the creation site's owner (probed) — `of` inside a `bind_each` row frees its entry with the row. `enqueue`'s linear dedup became a `NativeMap` keyed by `id.hash()` (`Turn.queued`). Pins: 1,000 rows → 2 notifications per change (red with a walk-all-cells plant); a scaling ratio in one process, 4× observers < 8× (80 → 229 ms keyed; 483 → 10,083 ms, 13.8×, with the scan back). `sub_condition` not added — it buys no complexity class.)

- **A42. `bind_each` demands both `T: PartialEq` AND a key fn; split the value-keyed and key-fn forms (kolt writes the key twice and gets a stale row) — CLOSED 2026-09-04** (Order 27, lane std-27, vilan 07218676: `bind_each_values<T: PartialEq, S>(source, render)` (identity key, `==` unchanged-check) and `bind_each_by<T, K: PartialEq, S>(source, key, render: (sync |SignalCell<T>| View) context owner_scope)` with NO bound on T — a surviving key keeps its row and owner and std writes the new item into the row's own cell; `render` takes `SignalCell<T>` because a closure parameter cannot be typed by a trait (no trait objects — probed). `reconcile<T, K>` parameterized on `same`; the three-argument `bind_each` unchanged (it cannot be renamed: ten call sites). SSR twins. Pins in `ui_rows.rs`: a changed value rebuilds under the two value-checked forms only, a reorder rebuilds nothing, `bind_each_by` keeps element identity while its text follows. kolt: its two hand-written `PartialEq { id == id }` impls become optional and the latent stale `Theme` row is gone under `bind_each_by`.)

- **A45. no element mount hook: `View::on_mount(|el| ..)` and `View::autofocus()`; kolt focuses three modal inputs with three UUIDs, three timers and a private `focus` extern — CLOSED 2026-09-04** (Order 27, lane std-27, vilan 161e9bc7: `Element::focus` extern; `View::on_mount(action: sync |Element| void)` on a microtask; `View::autofocus()`; SSR twins as generic no-ops. The plain microtask holds at all four attachment sites including a `when` body and a `bind_each` row appended in a later drain wave (a DOM harness walks element→root; red inline) — kolt's "queue_microtask runs too early" note is not reproducible against the shipped path, so its three UUID+timer focus dances and private `focus` extern collapse to `.autofocus()`.)

- **A36. `Style::when(condition, delta)` — the conditional-merge combinator — CLOSED 2026-09-04** (Order 27, lane std-27, vilan bb80ec4f: `Style::when(condition, delta)` exactly the ruling's five lines with its doc guidance (own-flag chains; a compound condition is the tell for `match`; later delta wins); `when` joins `STYLE_BARRIER_METHODS` beside `add` (chain position is precedence — the table-completeness gate named it). Pin: four-cell class-list equivalence against the `+`-built twin plus a precedence cell (red with `delta + self`); corpus `style-when.vl` with its own `.css` golden.)

- **N52. `infer_differential.rs` has N49's old shape: one `run_node`, one 300 s deadline, one whole-corpus test — CLOSED 2026-09-04** (Order 27, lane hygiene-27, vilan 27f0b402: both differentials (in vilan-core, not vilan-cli as the item said; infer's timeout was 30 s, not 300) now share one `corpus_harness` module: a `corpus_manifest!` x-macro, `NOT_RUN`, one `NODE_TIMEOUT` (300 s), `run_node`, and the two-direction coverage assertion each gate runs against `vilan/test/` — so the lists cannot drift (they already HAD: release excluded nursery.vl, infer ran it). The corpus-wide non-vacuity count (61 s in its own test) became `FOLDS`, the 32 programs the sweep changes, asserted both ways inside each per-program test at zero extra compiles. 255/255 in 110.8 s, longest 21.5 s. Find: the split's real cost is the process-global in-memory base cache (M36).)

- **N51. `watch.vl` is a corpus program that cannot run to completion: nothing in the suite ever EXECUTES it (owner's question) — CLOSED 2026-09-04** (Order 27, lane hygiene-27, vilan c15c48ba: on the recommendation (the owner's answer never came): `watch.vl` makes its own change — scratch dir with a random suffix (the two differentials run it in mutually blind processes), flat watch → write → `Created`, recursive watch → rewrite → `Modified`, teardown, exit 0 — a corpus program whose OUTPUT is the claim; 0.85–1.14 s at loadavg 126, eight concurrent copies byte-identical, no bounded-wait branch needed; run by both differentials for the first time. The endless form moved to `vilan/examples/watch/`; the rule paragraph beside `// witness:`; stale reasons in interpreter.rs and nextest.toml fixed. Finds: interpreter.rs still has the 30 s timeout and a whole-corpus test (N53); file.vl writes a fixed CWD path under both differentials (N54).)

- **B230. `?` inside a `let` initializer double-wraps: `let v = probe()? > 0; Ok(v)` returns `Ok(Ok(true))` and an `Err` becomes `Ok(Err(..))`, never propagated (RELEASED MISCOMPILE) — CLOSED 2026-09-04** (Order 27, lane b230, vilan 5b825900: THE ITEM'S PREMISE WAS WRONG, and the lane said so before building: bare `?` is the expression LIFT (expression-lifting.md, 2026-07-16) — a `let` initializer is a slot and receives the CONTAINER, so `let v = probe()? > 0` binding a `Result<bool, str>` is exactly right; `!` is the assert-or-return operator and lowers correctly in every position, B224's slot included. The real released miscompile was one level down: a generic enum variant's payload was never checked against the instantiation its position expected — `fun check(): Result<i32, str> { Ok(true) }` compiled, `let x: Result<i32, str> = Ok(true)` compiled and put `[0, true]` behind an `i32`. `infer_enum_constructor_arguments` (~25692) seeded the enum's parameters from the expected type (bidirectional inference, the reason `Ok(decode())` types against `Option<User>`) and DROPPED the failed reconcile when the argument contradicted the seed; the seed is now a hint — a contradicting payload re-binds from its own type. Every expression position now refuses (`let`, argument, field, `ret`/tail, `async`, under `&&`); the condition and non-generic-variant forms were already refused. 10 pins, 5 red-first; goldens unmoved (a check, not a lowering); spec §5.10 gains the bare-`?` slot inventory and §5.11 the payload check. Find: the grammar EBNF lists no bare `?` postfix (D6).)

- **B239. FILE MODE regression from B226: an open non-entry file is analyzed as the entry, so a sibling that imports it (`channel.vl` → `pkg::views`) hits the new "entry cannot be imported" error and everything it carried cascades (kolt's views.vl in the editor: 7 errors, `vilan check .` clean) — CLOSED 2026-09-04** (Order 27, lane b239, vilan a2f1e2d9: the owner's report the evening the toolchain was installed: seven errors in kolt's views.vl in the editor, `vilan check .` clean — FILE MODE (an open non-entry file analyzed as its own entry) hit B226's new cycle error on a sibling's `pkg::views` import, and everything the sibling carried cascaded; before B226 the same situation silently loaded the entry as a module and dropped its derives (the owner's original SidebarTab report), so both behaviors were wrong there. `Workspace::entry_mode: EntryMode { Declared | OpenFile }` set by both front ends from ONE rule moved into core (`platform_color::is_package_module`: under the source root, not the `[package] entry`, not an `[entry.<name>]` path); an open-file entry that a sibling aliases takes the monolithic order a std file opened as the entry always took — the pre-entry `resolve_world` skipped, imports and preludes resolved once in the post-entry `build()` with the entry's declarations already in the aliased scope; the file still walks as the program (derives expand, no `main` demanded); the world is NOT stored (a world whose `pkg::views` means "the entry" is not one another entry may be served), so the base-cache key did not move. B226's refusal and warning fire for a DECLARED entry only; its five pins untouched. 7 pins red-first (5 core, the CLI `check src/views.vl` + `check .` fixture, one LSP open); vilan-core whole 4846/4846. kolt itself had already been worked around in the owner's tree (a new styles.vl) by the time the lane measured. Finds: B240 (three file-mode residues), N56 (`check <abs path>` from a foreign cwd resolves a foreign std).)
