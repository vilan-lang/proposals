# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A72](items/A72.md) | NEW — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate` | feature | dom-30's finds 1 and 2 (2026-09-08) |
| [A73](items/A73.md) | NEW — std-30's two unshipped halves: `Storage` key ITERATION (today `len` + `key_at` by hand; a `keys(): List<str>` or an iterator) and `Debounce::flush()` (fire the pending callback now — a form that unloads before the window closes needs it) | feature | std-30's open questions (a) and (b) (2026-09-08) |
| [A76](items/A76.md) | NEW — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there | feature | reverse-31's find 2 (2026-09-09) |
| [A77](items/A77.md) | NEW — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel | feature | reverse-31's find 3 (2026-09-09) |
| [A81](items/A81.md) | NEW — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere | feature | reverse-31's finds 4 and open Q 2 (2026-09-09); transport-rpc §9.3's async-coloring paragraph is this item's |
| [A87](items/A87.md) | NEW — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it | design | reactive-32's open Q1 (2026-09-11); rec: add `release()` (run the cleanups, stay live) only when a caller appears — the pattern is a smell (a boundary that outlives its content is what fresh owners are for) |
| [A94](items/A94.md) | NEW — a FRAGMENT does not FLATTEN: `<><i/><>..</></>` is a `List<View>` inside a `List<View>` and is refused — A46's lowering is a literal, so nesting one fragment in another is a type error, not a run; pinned and documented as the limit; if flattening is wanted it is a lowering change (concatenate the inner list into the outer) with no type change | feature | slots-33's find F2 (2026-09-11); no exhibit asks for it yet — filed so the limit has an id |
| [A97](items/A97.md) | NEW — HOLD-COUNTING vs remote-sources §3: the server counts a client's leases per channel and revokes at zero (A92), so a RAW client that sends `Subscribe` twice and `Unsubscribe` once holds its channel open until disconnect; the alternative — client-side demand coordination that never double-subscribes — is plumbing the generated client already does and a raw client would have to repeat. Rule whether the wire contract is 'one Unsubscribe per Subscribe' (documented, as built) or the server forgives | design | rpc-33's find F3 and open Q (4) (2026-09-11), unfiled at the sweep; filed at Order 34's GO (R9); remote-sources §3 was AMENDED at Order 33's sweep to say what the server SENDS holds, not what it relies on — this item is the client-side half |
| [A101](items/A101.md) | NEW — the `css` block's declarations become CALLS: `property: value;` → `property(value);`, the strategy element syntax took for attributes (`type("checkbox")`, `stroke-width("2")`), so `css { outline("none"); width(pct(100)); .script_label(); }` — the property keeps its hyphenated CSS name, its ARGUMENTS are ordinary vilan expressions (`pct(100)`, `Color::current().alpha(0.5)`, `size.get(4)` — no `{expr}` holes, no CSS token soup), and a dotted item without a block `.method(args);` is the method call on the chain (today a nested-rule head REQUIRES `{ … }`). The lowering does not move: every declaration is still exactly one `.raw(property, value)` (css-block.md §5.2's one rule, name-blind, total), typed values through `raw`'s §6 groundwork. What it buys: one hole spelling fewer (the `{ }` hole exists only because a value was a token span), the §4.1 lexer cost gone (no CSS value tokens — `100%`, `1rem`, `#333` never reach the lexer; the css block's `#333` refusal row retires), value-position completion becomes ordinary expression completion (the paper's Q4 half-dissolves: `pct(`, `Color::` complete as expressions), the formatter's value pass (§8) becomes expression formatting. BREAKING: 138 blocks — docs 14, corpus 4, vilan-core tests 80, vilan-cli tests 12, kolt 28 (at the owner's word); the codemod rides E167's converter machinery (it already types declarations) | design | the owner's ask (2026-09-14): 'change css block `property: value` into `property(value)` similar to the strategy taken for element syntax attributes'. OWNER QUESTIONS: (Q1) several arguments — `margin(px(4), px(8))` → `raw("margin", ..)` joined by single spaces (rec: yes, the typed `padding(a, b)` methods' own rule), or one argument only; (Q2) the codemod's value typing — bare CSS tokens become STRINGS (`overflow("hidden")`), `{expr}` holes become the expression, bare numbers stay numbers, and unit literals (`Npx`, `Nrem`, `N%`, `Nem`, `Nvh`, `Nvw`) become the prelude constructors (rec: yes — E167's converter already maps them; anything it cannot type stays a string); (Q3) custom properties `--name(value)` keep the leading dashes (rec: yes, as the hyphenated name rule) |
| [A102](items/A102.md) | NEW — `lazy let` bindings and HMR: excluded from transfer in v1 (`TransferForm::Excluded` — a swap carries a VALUE and a lazy binding may not have one; adopting the cell would hand the new bundle the old bundle's thunk), so a lazy binding holding state that should survive a hot swap has no answer today | design | lang-c-36's FIND 5 (2026-09-14); design, on demand |
| [A103](items/A103.md) | NEW — `lazy` S3, the std RETROFIT (lazy.md §6b): `Option::expect(self, lazy message: str)` and `unwrap_or(self, lazy fallback: T)` / `Result`'s twins become lazy, GATED on the corpus/std/docs differential (any observable difference is stop-and-decide); the `Option`/`Result` doc page says today that the retrofit has NOT happened | feature | A100's third slice, ruled Order 37's at Order 36's GO; READY |

## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B298](items/B298.md) | NEW — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's | design | smalls-32's find F2 and open Q1 (2026-09-11); rec: yes for a bounded `_`, behind the Q3 ruling |
| [B342](items/B342.md) | NEW — A67's rule cannot see a collision NOBODY walks into: `import pkg::a;` alone, with `a.vl` declaring `b` beside `a/b.vl`, reports nothing — a child's scope exists only for a child something loaded (the doc comment says so), while the section heading above the pins claims the collision is reported 'whether or not anything imports the path' (true only of paths that walk THROUGH the child). Closing it means listing each loaded module's directory (a `read_dir` per module per analysis) — a perf decision | bug | hygiene-36's F4 (2026-09-14, found while fixing B337's namespace half); low |
| [B343](items/B343.md) | NEW — where the `context` clause sits on a `fun` DECLARATION: after the return type (`fun f(): i32 context settings`, contexts.md §3's deliberate choice — the type grammar's own `context` suffix parses greedily and the declaration peels it back off) or after the parameters and BEFORE the `:` of the return type (`fun f() context settings: i32`). The greedy-peel reading has one real hole: a closure-typed return with its OWN clause — `fun f(): (\|\| void) context c` — cannot say whether `c` is the function's requirement or the returned closure's clause (the peel takes it for the function); the before-the-colon form spells both (`fun f() context c: (\|\| void) context d`). And whichever position stands, the TextMate grammar highlights `context` only after a `)` (`(?<=\))\s+(context)\b` — the closure-type rule), so on `fun f(): i32 context settings` the keyword paints as an identifier; the estate writes the declaration form at 0 std / 1 kolt / 2 docs sites against 30 closure-type clauses in std, so a move costs little today | design | the owner's ask (2026-09-14): 'File an item for whether or not the `context` clause on a `fun` declaration should be after the return type or after the parameters but before the `:` marker of the return type. If the current setup is best (after the return type), please note that the syntax highlighting doesn't recognize the keyword correctly.' REC: keep the position (it reads as the closure type's own clause, one spelling for one idea, and contexts.md §3 chose it knowingly), REFUSE the ambiguous closure-typed-return-with-clause form with a steer to parenthesize the return type (`fun f(): ((\|\| void) context d) context c`), and FIX the highlighter — the lookbehind admits any type-ending token (`)`, an identifier, `>`, `]`) so the declaration form paints, with a scope pin for both forms |
| [B344](items/B344.md) | NEW — a GENERIC lazy parameter instantiated at a resource can slip the type check: the declaration check reads the DECLARED type (`lazy fallback: T` is not a resource) and the argument check reads `expr_id_to_type_id_map`, which a bare `Expr::Local` argument does not populate (transformer.rs's B328 comment records the same gap); a resource LOCAL is still caught by the R9 capture scan, a resource reached another way is not | bug | lang-c-36's FIND 2 (2026-09-14) |
| [B345](items/B345.md) | NEW — `scan_closure_view_captures`'s `Expr::Call` arm never walks the call SUBJECT (only the arguments) — pre-existing, and now also on the lazy path, so a view named inside a nested call subject in a closure or a lazy argument is missed by the view-capture scan; one line, but it changes an existing check's firing | bug | lang-c-36's FIND 4 (2026-09-14) |
| [B346](items/B346.md) | NEW — HIGH: `VILAN_STD` pointing at a byte-identical COPY of `vilan/std` outside `<repo>/vilan/` silently MIS-ANALYZES std: `Each<..> does not implement trait 'Slot'` (std's own impl invisible) or a `[derive(PartialEq)] expanded before std's compare.vl declared its macro` load-ordering refusal; works from `<repo>/vilan/std` and `<repo>/vilan/std2`, fails from `<repo>/target/std-probe`, a scratch copy, a `cp -a` copy — `diff -r` empty; reproduces on the sealed 9b22ec36 toolchain. Smells like two std worlds mixed (the discovered one and the `VILAN_STD` one). A packaged std, a sibling checkout or a CI cache can get a wrong answer with no diagnostic | bug | lang-a-36's FIND 1 (2026-09-14) |
| [B347](items/B347.md) | NEW — `each_by`'s `T` freezes abstract when its render closure's parameter is UNANNOTATED: `view("nav").child(each_by(handles, \|h\| h.id, \|h\| … h.map(\|c\| c.title)))` reports `cannot access field 'title' on type T` plus a spurious `owner_scope … can be reached without an enclosing run` cascade on every sibling chain link; pre-existing, but A99 makes it reachable at every site — the retired `bind_each_by` METHOD bound `T` through the receiver path and the free call does not (the one place A99's rewrite is not type-preserving); `each`/`each_values` unaffected; kolt has no `bind_each_by` | bug | lang-a-36's FIND 2 (2026-09-14); worked around by annotating 3 sites in ui_rows.rs and 1 in guide/ui.md |
| [B348](items/B348.md) | NEW — a GENERIC `Callable` does not coerce into a closure-typed position (by design: `impl Box<type T> with Callable` writes `call` in the impl's own binders — fn-coercion rule 2's reason); `held()` calls, `let f: \|…\| T = held;` is the ordinary mismatch; needs the expected-type-driven instantiation rule 2 is waiting on | design | lang-b-36's FIND 3 (2026-09-14); design, on demand |
| [B349](items/B349.md) | NEW — a MODULE-LEVEL (top-level statement) body resolves its members under NO file scope: the transformer's `current_admitting_file` is set only by `enter_instance` (a function-body seam), so module-level initializers and top-level statements resolve unrestricted — harmless today (they sit in the file that declares them, which admits its own blocks) but a hole in §3.5's rule | bug | visibility-36's FIND 1 (2026-09-14) |
| [B350](items/B350.md) | NEW — the `#`-on-an-already-EXPORTED-impl redundancy warning (the impl twin of S2's "the reach marker is redundant — delete the `#`") is not built: `import a::{ #(impl T) }` where `a` exports the block earns no warning; a warning, not a refusal, and the symmetry B318 §14/§15 leave open | feature | visibility-36's unfinished note (2026-09-14); low |
| [B351](items/B351.md) | NEW — REGRESSION (Order 36): a block hole whose tail is `context.run(v, \|\| { <element .child(swap(s, \|x\| match x { .. => <e/>, .. => <div .child(swap(t, \|y\| match y { .. })) /> })) /> })` leaves `run<U>`'s `U` unresolved, and the emitter reaches `Slot::place` with no body — the B55 internal refusal, on a shape kolt compiled at 9b22ec36 (its `app_shell`). Twenty-line reproduction at the integrator's scratch `kolt-migrate/run-repro/src/client.vl` (case h): the same program with `run` replaced by a user generic `ident<U>(body: \|\| U): U` PASSES, with an expression-bodied closure PASSES, with a single (un-nested) `swap` PASSES, outside a `{{ }}` block hole PASSES, with both closure parameters annotated PASSES — the trigger needs the EXTERNAL generic `run`, a block-bodied closure, and a nested `swap` whose `C` is decided by `match` arms. Workaround in kolt (views.vl:89, FIXME): `let shell: View = user_context.run(..); shell` | bug | the integrator's find on kolt at d783fbf4 (2026-09-15); culprit lane: bisect in progress (49611256 before lang-b vs 365dd9ff after — B333's closure landings and B340's coercion note both touch `infer_type_inner`; B347 is the sibling) — stamped when known |

## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C1](items/C1.md) | `Weak<T>` | design | |
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |
| [C14](items/C14.md) | NEW — `SignalCell` (and `Owner`) off `Shared` boxes and onto `Rc` or OWNER-ATTACHED arena `Handle`s — the necessary step for native apps with native code and NO garbage collector (F1). Today `SignalCell<T> { value: Shared<T>, subscribers: Shared<List<Subscriber>>, identity }` and `Owner { cleanups: Shared<..>, disposed: Shared<bool> }`; `Shared` is a compiler primitive (`external struct`, `primitive_struct_ids` 'Shared', 8 analyzer sites) whose doc says 'reference-counted' but whose JS backing is a plain object box the HOST GC reclaims — nothing counts, cycles (cell ↔ subscriber closures capturing cells) are the GC's problem, and a disposed owner's cells stay readable forever. Neither property survives a native backend. Two candidate representations, to be DECIDED in a paper: (a) a real `Rc<T>` — counted box with deterministic drop (`destruction.md`, `drop.vl`), cycles broken by the owner's disposal or by `Weak` (C1, OPEN); (b) owner-attached `Arena` + `Handle` (std::arena already exists; the owner holds the arena, a cell is `Handle<Slot<T>>` + its owner, disposal frees the arena wholesale — no counting, no cycles, and a stale handle reads `None` instead of a ghost value, which is a BEHAVIOUR change from today for a cell that escapes its owner: module-level cells, `std::memo` caches, rpc mirrors, `Task` results). Blast radius: reactive.vl 47 `Shared` mentions, 13 std files use `Shared`, kolt 4 (store, lib/input_system, lib/overlay, lib/rotary) | design | the owner's ask (2026-09-14): 'change SignalCells from `Shared` boxes to `Rc` or arena `Handle`s (handles attached to owner). This is a necessary step to support native apps with native code (no garbage collector)'. Companion of F1; C1 (`Weak<T>`) is the other brick if (a) wins |
| [C15](items/C15.md) | NEW — spec §6.9's closure rule ('captures BINDINGS, not values') makes every mutably-captured binding a SHARED CELL on a native backend: the F1 probe (R-2) had to box `let mut count` as `Rc<RefCell<_>>` because two closures assign it — on JS the host boxes the place for free; natively the compiler must decide per binding whether it is captured by value (copied), by counted reference, or refused. Larger than `Shared` (C14) — no item existed | design | paper-native-36's FIND-4 (2026-09-14; native-apps.md §2.4 R-2); rule with C14's S4 and F1's S1 |
| [C16](items/C16.md) | NEW — C12 PROMOTED to a native-backend precondition: a captured VIEW of a dead local (C12's closed-as-limit shape) is a use-after-free the moment the host stops boxing places — on JS the escaped view reads a boxed ghost; on the emit-Rust backend it is a dangling borrow rustc will refuse or a freed slot the runtime will read. The limit C12 accepted as 'spec §6.9's honesty limit' must become a refusal before F1's S1 | bug | paper-native-36's NEW-2 (2026-09-14; native-apps.md §8); C13 (the storing-callee escape, OPEN) is the same family |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |
| [D8](items/D8.md) | NEW — `shared.vl`'s headnote says 'prefer an `Arena` + `Handle` whenever an owner exists', and 0 of the 167 `Shared` sites in std and kolt follow it — no cell has an owner today (only `Subscription`, `Owner`, `ReactiveServer`, `ReactiveClient` are `Disposable`); the sentence describes a relation the estate does not have. Rewrite it to what the C14 paper found: `Shared` is the counted box for diffuse ownership, `Arena`/`Handle` is the tier for graphs and stable identities | documentation | paper-native-36's FIND-2 (2026-09-14; signal-cell-representation.md §2.4 finding 2) |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E69](items/E69.md) | NEW — attribute-NAME completion in an element head is a semantics decision | feature | RE-OPENED + RULED 2026-09-14: the vocabulary is GENERATED from the WHATWG HTML (+ SVG) attribute index by a pinned code-gen script with a sync test; completion only, the desugar stays name-blind; was deferred since 2026-08-22 |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E182](items/E182.md) | NEW — `infer_platform` (vilan-core lib.rs:93) is BINARY browser/not-browser: a third `std::ui` twin makes the editor report the PROCESS twin's `View` against native source — the E113 shape its own doc comment describes; latent until F1's S2 adds the `native` platform | bug | paper-native-36's NEW-4 (2026-09-14; native-apps.md §3, §8) |
| [E183](items/E183.md) | NEW — completion at `css { .\| }` offers only the 14 std condition combinators (`css_block_completions`, vilan-ide completion.rs:718 — `CssPosition::Condition` → `STYLE_CONDITION_METHODS`): a CUSTOM `impl Style` method (kolt's `script_label` in theme.vl, `flex_row`, every app helper) and std's non-condition methods (`on`, `raw`) are absent, although the dotted head is exactly where an app writes them. Offer every `impl Style` method the file can reach: std's from the tables, the program's from its impl table (E172/E175's `StyleSurface` is the same source — reuse it), filtered by the B318 visibility bit under the uncurated-module exemption; with A101 the bare `.method();` items complete the same way | editor | the owner's ask (2026-09-14): 'include custom impl methods in autocomplete at `css { .\| }`'. The vocabulary comes from the ANALYSIS (the impl table), not from a table — the position still comes from E67's raw re-parse |
| [E184](items/E184.md) | NEW — E178's FOURTH completion consumer: `auto_import_completions` (`AutoImportOrder::build`, vilan-ide) still offers every importable, because it runs outside `owned_modules::collecting()` and calling `module_importables` there parses open buffers into the process-global parse cache once per keystroke (four `overlay_module_reclaim` pins red, measured by editor-36); visibility-36 exposed `Program::exported_entities` + `Program::curated_modules` (= `Analyzer::is_exported_in`) for exactly this — wire the filter on them | editor | editor-36's OPEN Q1 + visibility-36's efa55a2f (2026-09-14); the three other consumers filter since 880f92aa |
| [E185](items/E185.md) | NEW — S4's monomorphization refusal prints a PLACEHOLDER where the module's name belongs: "'begin_struct' is provided by an `impl` in module `json`, and module `that module` does not admit it … Widen `that module`'s own import of `json`" (seen at the sweep merge, sixteen times); the declaring file's module name is in hand (`current_admitting_file`) and the row's `{}` slots want it | bug | the integrator's find at sweep-36's merge (2026-09-15); ledger row 491-family (visibility-36's NEW rows) — a row EDIT |
| [E186](items/E186.md) | NEW — Organize Imports' whole-run deletion leaves the paragraph gap behind: pruning a run followed by a blank line deletes the run plus one line ending, not the separator, so kolt's organized lucide shows two blank lines before `export *;` (pre-existing, E168-era; cosmetic; `vilan fmt` repairs a non-generated file) | editor | editor-36's FIND 1 (2026-09-14) |
| [E187](items/E187.md) | NEW — a cross-file quick fix (E177's Export fixes) and E175's sibling reads take the declaring file from DISK, so an edited-but-unsaved sibling is edited at its SAVED offsets — the same bargain go-to-definition makes; a server-side document-map lookup (the open buffer's text when the sibling is open) closes it | editor | editor-36's FIND 3 (2026-09-14) |
| [E188](items/E188.md) | NEW — E177's exposure fix ("Export `S`") DECLINES when a file names two different types with one spelling: it resolves the exposed name through this file's `type_references` and refuses on disagreement; an analyzer-recorded (warning site → exposed entity) pair would make it exact — the same `Program` surface E184 reads | editor | editor-36's FIND 4 (2026-09-14) |
| [E189](items/E189.md) | NEW — CONTEXT-REACHABILITY diagnostics CASCADE behind a resolution error: on kolt at d783fbf4 with its seven retired `View` methods still written, `vilan check` reported 180 errors — 7 real (A99's steer) and 173 `context \`owner_scope\` is read here, but this code can be reached without an enclosing \`run\`` (146), `required by …'s \`context\` clause` (10), `reads context …, so it can't be used as a value` (9) and one injected-closure error, 74 of them in the generated lucide module that writes no closure at all; migrating the seven sites took the count to 1. Mechanism: a closure handed to a call that failed to resolve never attaches to the clause-carrying parameter it was written for, so every function it reaches (the pages, the icons, everything under them) is reachable 'without an enclosing run' along that path — one primary error fans out into every context read downstream of the closure | diagnostics | the owner (2026-09-15): 'investigate the ridiculous amount of context requirement diagnostics. Perhaps the context diagnostics shouldn't appear at all if there are other diagnostic kinds present?' REC: the narrow rule first — a reachability path that passes through an UNRESOLVED call (or a closure argument of one) contributes nothing to the walk (the path's premise failed), which kills this cascade at its root and keeps context errors on a program whose only defect is the missing `run`; the owner's broad rule — suppress the context family outright while any resolution/type error is present in the program — as the backstop the narrow rule does not reach (a closure typed by a failed call elsewhere), the way unused-import fades already go silent under any diagnostic |
| [E190](items/E190.md) | NEW — the never-silent body-less-emission refusal (B55, `internal: a call resolved to \`Slot\`'s requirement \`place\`, which has no body …`) carries NO usable span: its span is the trait requirement's name in std, which the CLI never renders, so kolt showed one bare `Error:` line with no file, no line and no hint which of 19 files to look in; the emitter at that point does not know the call site either (`function_with_name` records the bodyless id when the requirement is EMITTED, long after the call that asked for it). The integrator had to instrument `ensure_function_emitted` to learn the emitting stack (`app_shell` → `place`) and bisect kolt by hand | diagnostics | the integrator's find on kolt at d783fbf4 (2026-09-15), while chasing B351 |

## F. Native targets & rendering — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [F1](items/F1.md) | NEW — DISCUSSION: building GPU-RENDERED NATIVE APPS in vilan. Today the build model is `Target` = `Backend` + `Platform` (platform-model.md, implemented 2026-06-23) with ONE backend (the JS emitter) and the browser/node/deno/bun platforms; `std::ui` has two layers behind one public surface (browser = live DOM, process = SSR string tree — the precedent for a third). A native app needs FOUR things the project has none of: (1) a native BACKEND — emit Rust (rustc does codegen; vilan's affine moves, borrows and `own` map onto ownership) vs C vs wasm-in-a-host vs an interpreter; (2) memory without a GC — C14 (cells on Rc/owner arenas), real counting for `Shared`, closure captures (`capture-clones.md`), strings/lists with owners, `destruction.md`'s drops actually running; (3) a RENDER LAYER — a third `std::ui` twin building a retained scene tree rendered by a GPU renderer (wgpu + vello-class 2D, text shaping, a flexbox-class layout engine such as taffy) with `Style` mapped onto it (A95 conditions: `hover()`/`active()` become hit-test state, `media`/`sm..xl` become window queries, the `css` block a native style resolver) and events/input/windowing (winit-class); (4) std's platform layers (`[library.layer]`, `PlatformPattern`) admitting a `native` platform and the coloring rules deciding what a native entry may reach (no `std::dom`, no `fetch` shaped as the browser's). Prior art to measure against: Dioxus/Blitz (HTML+CSS semantics, vello-rendered), Makepad, Slint, GPUI (Zed), Flutter (Impeller) | design | the owner's ask (2026-09-14): 'File an item to discuss support for building GPU-rendered native apps'. OWNER QUESTIONS before a paper: (1) which backend — emitting Rust is the shortest path to a real binary and reuses the ecosystem (wgpu, winit, taffy, cosmic-text) but makes rustc a toolchain dependency; (2) scope — desktop first (Windows/Linux/macOS), mobile later?; (3) is the UI surface the DOM-shaped `View` (so kolt's views compile unchanged onto the native layer, the SSR precedent) or a new retained-tree API?; (4) does 'native app' mean client-only, or client + server in one process (the rpc runtime in-process)?; (5) sequencing — C14 first (verifiable on JS), then a backend probe (hand-translate one small vilan program to Rust and list what the emitter must know), then the render layer |
| [F16](items/F16.md) | NEW — a closure PARAMETER that is both STORED and CALLED must lower to `Rc<dyn Fn>` on any non-JS backend: the F1 probe's hand translation hit `E0382` (use after move) the moment a callback was pushed into a subscriber list and also invoked — JS hides it because a function value is a reference; the emit-Rust backend needs a rule for which closure values are counted (probe R-1) | design | paper-native-36's FIND-3 (2026-09-14; native-apps.md §2.4 R-1); an S1 (backend) precondition |
| [F15](items/F15.md) | NEW — the THREE-LAYER `std::ui` rule and an N-way twin-parity gate: today two `ui.vl` twins (browser DOM, process SSR) share one public surface and the parity gate is structurally TWO-valued; a native twin makes it three, and nothing today states the rule that every twin exposes the same surface (`ssr.md`'s 'same public surface' is a sentence, not a gate). F1's S2 cannot be sized without the rule | design | paper-native-36's NEW-3 (2026-09-14; native-apps.md §6.2, §8 — 'should be ruled with this paper'); OWNER RULING wanted |

## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |

## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |
| [J6](items/J6.md) | NEW — ASYNC on a native backend: `Task`/`Nursery`/`await` need an executor and a cancellation story where JS supplied the event loop; the F1 paper's largest item F1 did not list — `Task` is an `external struct` over the host promise (task.vl:18), so the native runtime must provide the promise, the microtask queue and the turn model (reactive-turns.md) before any `async fun` compiles natively | design | paper-native-36's NEW-1 (2026-09-14; native-apps.md §2.3, §8: single-threaded executor recommended, `Task` stays `external`) |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | OPEN — an unchanged package module is re-analyzed every LSP analysis / HMR round; T1a–T1d landed (Order 30), next M55/M56/M57 | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M54](items/M54.md) | NEW — `LastUse`/`source_of` residuals from perf-30's re-pricing: the restored tables are CLONED into `LastUse::compute`'s walk state (8.9M Ir per analysis — needs `&mut Analyzer` or a read-through layer); `collect_unfollowable_loans` is 68% of the warm `LastUse` (whole-program by nature, a loan crosses modules — a record DESIGN); `Program::source_of` is still a linear scan of ~60 ranges and now the residual in `arrival_by` (`source_lookup` exists but is not the default); and `compute_shared_read_bindings` (B267) adds a second whole-program `collect_written_roots()` walk beside the capture pass's | performance | perf-30's and rule1-30's finds (2026-09-08) |
| [M56](items/M56.md) | NEW — R10's residue after T1d is the ENTRY's share of the sweep: 24,459 of 153,302 sites still walked, because the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole and ids minted after the world was stored fall in no range — removing it needs a source→entity index, §3.5's missing module boundary | performance | m19-t1d's find 2 (2026-09-08); the next tranche's shape |
| [M57](items/M57.md) | NEW — the corpus replay differential is BLIND to the R10 seam (and to T1b's): the golden corpus as modules declares almost no resource and none in a container, so a dropped-keys plant leaves `replay_differential` green — an R10 probe (a `List<Guard>` in every third module) would make the standing gate see it; it means editing a shared test function, which needs an owner's word or a lane that owns the file | performance | m19-t1d's find 1 and open question 1 (2026-09-08); OWNER Q |
| [M59](items/M59.md) | NEW — build-30's two follow-ups: persist the leg RECORD into `dist/` so a plain `build` gets the schedule's edges (and the artifact-edge guard's redo) from the second build on, not only under `--watch`; and give the HMR round's own compile loop (`run --watch`, ~main.rs:1340) the same tiering — it writes `dist/` after the whole loop, so the guard's shape differs | performance | build-30's open questions 1 and 2 (2026-09-08) |
| [M60](items/M60.md) | NEW — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update` | performance | keyed-31's find 2 (2026-09-09); rule 1's copy elision is the machinery |
| [M62](items/M62.md) | NEW — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it | perf | wire-32's find F5 and open Q1 (2026-09-11); the owner may prefer the six methods now — say so and it moves to A82's residue |
| [M71](items/M71.md) | NEW — 19 FRAME-SCOPED `Shared` cells in std want a `mut` local: cells constructed, mutated and dropped inside ONE function frame (json/binary/fs/rpc_server/rpc — 12.3 % of std's 154 `Shared` declaration sites) pay a box allocation and an indirection for state that never escapes the frame; each is a `mut` local (or a `&mut` parameter) in the language as it stands. The C14 paper's S1 ('the frame-scoped subtraction', size S) | perf | paper-native-36's FIND-1 (2026-09-14; signal-cell-representation.md §2.4 finding 1, §10 S1) |
| [M72](items/M72.md) | NEW — `[service]` ENTRIES never reach the base cache (`base_cacheable` requires `!contains_service`), and on kolt that is the single most expensive file per keystroke after M70's six: `store.vl` pays 290–605 ms of CPU per keystroke with 0 hits and 0 misses (it never consults the cache); the bypass was written when a service entry was exotic — kolt has one and edits it | perf | perf-36's FIND 1 (2026-09-14); HIGH-ish, user-facing latency |
| [M73](items/M73.md) | NEW — an open module's world is stored UNRESOLVED, so ~150 ms of `resolve_world` is paid on every keystroke in M70's six kolt files (theme.vl build 161 ms vs views.vl 11 ms with the same checks phase); the naive import-fixpoint split moved 0.4 ms of it; a fix has to defer only the ALIAS-reaching work rather than the whole pre-entry pass — scope-chain territory, size after B318 S4 (landed) | perf | perf-36's FIND 2 (2026-09-14) |
| [M74](items/M74.md) | NEW — M50's 24× weight factor is measured on RESOLVED worlds and OVER-CHARGES an open-module world (its ~229 derived tables are exactly the ones an entry-shaped world has not filled), so M70's six new worlds are billed at a rate they do not cost — which matters to M67's 192 MiB budget; re-weigh with `world_cache_spike.rs`'s harness | perf | perf-36's FIND 4 (2026-09-14); small; makes M67's bound honest |
| [M75](items/M75.md) | NEW — `refuse_imported_member_collisions` is O(collisions × files × statements) through `statement_for`'s linear scan — zero on the estate (gated on a banked collision), but the first real two-package collision in a large program pays it; an index `(importer) → [(span, sources)]` is the fix | perf | visibility-36's FIND 3 (2026-09-14) |

## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N86](items/N86.md) | NEW — N82's shape survives outside the `inference` binary: `std::env::temp_dir()` is still the scratch root in `module_resolution.rs` (21 sites), `base_cache.rs` (9), `embedded_std.rs` (6), `vilan-cli/tests/corpus.rs` (4 — including `every_corpus_golden_is_byte_identical`'s whole work tree), `interpreter.rs` (2), `intrinsic_binding.rs` (2), `check_scope_differential.rs` (2), `corpus_harness/mod.rs` (2), `watch_lifecycle.rs` (2), `chunks.rs`, `markdown_golden.rs`, `diagnostic_determinism.rs` — each on the 12 GiB tmpfs every lane shares. Each binary needs its own copy of N82's three helpers (`scratch_root`/`scratch_dir`/`ScratchFile`; `CARGO_TARGET_TMPDIR` is per-target) | hygiene | hygiene-36's F2 (2026-09-14); mechanical, one item |
| [N88](items/N88.md) | NEW — std doc comments outside lang-a-36's ownership still say `bind_each` after A99 — `reactive.vl` (7 lines), `rpc.vl` (7), `math.vl` (1) — plus `browser/ui.vl:537`'s `child_relation` mention after A95 S2, `ui.vl:1187`'s now-redundant `context owner_scope` annotation (B333 adopts it), and formatter.rs's two element-head layout pins that spell `.bind_each(` (syntactic, pass, name a method that no longer exists) | hygiene | lang-a-36's FIND 3–4, style-36's and lang-b-36's notes (2026-09-14) |
| [N89](items/N89.md) | NEW — `Node::Const` peels in only 2 of ~24 item-shape sites (`item_visibility`, `unwrap_item`); the other `Node::Export(..)` match sites (formatter, macros.rs, lib.rs, css.rs, elements.rs, lift.rs, parsing.rs) do not peel `Const`, so `export const fun f()` parses and resolves but is unaudited across the export-marker consumers | hygiene | lang-b-36's FIND 2 (2026-09-14) |
| [N94](items/N94.md) | NEW — two diagnostic/assertion strings carry SWALLOWED line continuations (a `\`+newline Rust does not strip, leaving a run of ~18 spaces mid-sentence): `const_eval.rs:723` is user-facing (`asset::staged` reads the registry AFTER evaluation has finished…), `analyzer.rs:58192` is a test assertion; both predate Order 36; a one-line gate — no diagnostic literal may contain three consecutive spaces — catches the class (visibility-36 hit it four times itself) | hygiene | visibility-36's FIND 2 (2026-09-14) |
| [N95](items/N95.md) | NEW — `Program::hidden_impls` is DRAINED by `build_impl_admission`, so a consumer reading it after the post-passes sees an empty set; documented at the field, but a `debug_assert` or a rename (`hidden_impls_pending`) would be safer | hygiene | visibility-36's FIND 4 (2026-09-14) |
| [N87](items/N87.md) | NEW — the spec's appendix A.2 reserved-word list is UNGATED (only the tour's list is held to the lexer's keywords by `docs.rs`) — it had been missing `css` since the css block shipped; lang-c-36 added `css` and `lazy` by hand | hygiene | lang-c-36's FIND 6 (2026-09-14) |
| [N90](items/N90.md) | NEW — HIGH: `vilan fmt --check` cannot distinguish "clean" from "BAILED": a file the printer cannot render is reported as already-formatted, so a printer gap is invisible to N55's gate — `export let x = 1;` bailed, `fmt --check vilan/std` was green over it, and only `formatter::idempotency::reactive_vl` caught it; `[deprecated(..)]` bailed the same way until style-36 caught it through `formatter_never_silently_bails` | hygiene | sweep-36's FIND 1 + style-36's FIND 1 (2026-09-14) |
| [N91](items/N91.md) | NEW — `macro` declarations are UNFORMATTABLE: `macro M { fun m() { } }` bails the printer, so std's `hash.vl`, `json.vl` (6) and `rpc.vl` (8) are silently unformatted files — and after the curation 15 of them are `export macro`, so the printer fix must handle the wrapper too | hygiene | sweep-36's FIND 2 (2026-09-14); pre-existing; N90 is what hid it |
| [N92](items/N92.md) | NEW — `vilan check` writes `dist/.cache` into the checked package's directory: a read-only-sounding command mutates the tree (and `split.rs:63` `read_to_string`s every entry of its fixture directory, so a `dist/` left there fails 10 tests with `IsADirectory` instead of a readable message) | hygiene | sweep-36's FINDS 3–4 (2026-09-14) |
| [N96](items/N96.md) | NEW — the REMAINING unrowed `Failure::unsupported` family after N85: the staging registry (`asset::stage`/`staged`), the project files (`asset::read`/`digest`/`{spelling}`), the macro-engine limits ("the array method `{other}`", "await (macro bodies are synchronous)", "stringifying a function", `__shared_new`, M66's "`Shared::identity` over {type}") — all user-visible; decide whether the ledger's check (3) widens its walk to `Failure::new`/`unsupported` (rec) rather than rowing piecemeal | hygiene | hygiene-36's F3 + perf-36's note (2026-09-14) |
| [N97](items/N97.md) | NEW — the analyzer's expression walk overflowed libtest's 2 MiB test thread ON WINDOWS at NINE levels of nesting (`b250_a_declared_leg_with_no_manifest_is_unchanged` ABORTED the Windows shard with 0xc00000fd at Order 36's seal; Linux passes at 2 MiB and fails at 1 MiB) — `walk_expr_node_inner`'s frame GREW this order (the lazy, const, callable and visibility arms all landed in it) while the recursion did not; the binaries are safe (the compiler runs on a 128 MiB thread), so the seal fix raised the suite's test threads to 8 MiB (`.cargo/config.toml` `[env] RUST_MIN_STACK`). Left: MEASURE the frame (`walk_expr_node` + `_inner` per level, both codegens), bound it, and plant a CANARY pin — a 30-level nested fixture analyzed on a thread of a fixed size — so the next bloat is caught on Linux instead of by the Windows shard | hygiene | the integrator's find at the seal (2026-09-15); the local seal's Windows leg only COMPILES (L20's gap) — this is exactly the class it cannot see |
