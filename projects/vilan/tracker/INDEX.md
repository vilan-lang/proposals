# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A72](items/A72.md) | NEW — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate` | feature | dom-30's finds 1 and 2 (2026-09-08) |
| [A76](items/A76.md) | NEW — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there | feature | reverse-31's find 2 (2026-09-09) |
| [A77](items/A77.md) | NEW — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel | feature | reverse-31's find 3 (2026-09-09) |
| [A81](items/A81.md) | NEW — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere | feature | reverse-31's finds 4 and open Q 2 (2026-09-09); transport-rpc §9.3's async-coloring paragraph is this item's |
| [A87](items/A87.md) | NEW — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it | design | reactive-32's open Q1 (2026-09-11); rec: add `release()` (run the cleanups, stay live) only when a caller appears — the pattern is a smell (a boundary that outlives its content is what fresh owners are for) |
| [A94](items/A94.md) | NEW — a FRAGMENT does not FLATTEN: `<><i/><>..</></>` is a `List<View>` inside a `List<View>` and is refused — A46's lowering is a literal, so nesting one fragment in another is a type error, not a run; pinned and documented as the limit; if flattening is wanted it is a lowering change (concatenate the inner list into the outer) with no type change | feature | slots-33's find F2 (2026-09-11); no exhibit asks for it yet — filed so the limit has an id |
| [A97](items/A97.md) | NEW — HOLD-COUNTING vs remote-sources §3: the server counts a client's leases per channel and revokes at zero (A92), so a RAW client that sends `Subscribe` twice and `Unsubscribe` once holds its channel open until disconnect; the alternative — client-side demand coordination that never double-subscribes — is plumbing the generated client already does and a raw client would have to repeat. Rule whether the wire contract is 'one Unsubscribe per Subscribe' (documented, as built) or the server forgives | design | rpc-33's find F3 and open Q (4) (2026-09-11), unfiled at the sweep; filed at Order 34's GO (R9); remote-sources §3 was AMENDED at Order 33's sweep to say what the server SENDS holds, not what it relies on — this item is the client-side half |
| [A106](items/A106.md) | NEW — the style vocabulary is ambient only inside a css HOLE (`std::style::prelude`, names.md §4.7's one-position rule): a `fun` body building a chain imports nine names per function (kolt styles.vl:54/103 — `style as s`, `Length`, `Cursor`, `TextAlign`, `AlignItems`, `active`, `attribute`, `hover`, `pseudo` — under a `// FIXME: Add a prelude to std.`). Doors: (1) document `import std::style::prelude::*;` at file level as the idiom (the module is ordinary outside a hole — but the TYPES `Length`/`Cursor`/`TextAlign`/`AlignItems` are not in it); (2) the `std::web` prelude `export import`s a widened style prelude — ambient in every web file (E180's R9 rule fades it cleanly), gated on a name-collision census over the estate; (3) nothing. Rec: widen the prelude with the types (S), document (1) now, take (2) after the census | design | kolt sweep at Order 37 GO (2026-09-17); OWNER Q (door 2) |
| [A111](items/A111.md) | NEW — KEEP-ALIVE for `swap` (`swap_cached`): park a swapped-out subtree (row AND owner) per key and re-insert on return — `cut_row`/`insert_row` already do the DOM half; missing is `Owner` SUSPEND/RESUME, without which a parked subtree stays subscribed (detached repaints, remote leases never release, a parked nested `swap` keeps rebuilding); `std::memo` cannot do it from app code (the maker runs under the per-instantiation owner `swap` disposes) | design (paper) | the nested-`swap` conversation (2026-09-19); NOT queued — no measured need; design with A87 (`Owner::release`), this would be its caller; cap/eviction is not optional |
| [A122](items/A122.md) | NEW — the REVERSE of `reactive::combine` — `divorce<T: (2..)>(source: SignalCell<T>): (U in T: SignalCell<U>)`, one derived cell per tuple position (kolt channel.vl:19 writes the signature with `// TODO: Implement`) — cannot be written in userland OR std today: the tuple comprehension `(x in xs => e)` is the only way to iterate a tuple and it takes a MAPPED-TUPLE PARAMETER, not a value of type `T` (`a tuple comprehension's source must be a mapped tuple, got T`); and there is no INDEXED read of a generic tuple (`whole[pair.0]` / `whole.i` at a generic position). The 2-arity hand-written form works (`(source.map(\|w\| w.0), source.map(\|w\| w.1))` — probed live) | feature (+ a comprehension form) | the owner (2026-09-21); B183 (tuple comprehension + the zip form — the comprehension family this extends); `combine`'s own doc comment (variadic over a mapped-tuple parameter; widening blocked on per-element bounds); A112's `when_some` body receives a `SignalCell<(Message, User)>` and kolt splits it by hand with two `.map`s — the customer |
| [A124](items/A124.md) | PAPER — `Source` as a PUSH-PULL PIPELINE: every combinator is a cold, stateless NODE (`Map<S, U>`, `Switch<S, I>`, `Combine<..>`), `get()` PULLS through the chain, a notification carries NO value, and state lives only at the ends — the root `Signal` and an explicit `memo()`/`to_signal()` node where sharing or a total value is wanted; a pending value (a task, a fetch) is NOT a `Source` until a fallback makes it total; struct fields hold a pipeline as `dyn Source<T>` (an object over the object-safe core `get`/`on_change`, reopening B4 in that scope only) so a field does not mint a hidden generic. The owner's `reactive2.vl` sketch, made precise; it REPLACES this item's first filing (a read-tracked `computed`), withdrawn below with reasons | paper + ruling | the owner (2026-09-21 question; 2026-09-22 `kolt/src/lib/reactive2.vl` sketch and rulings R1 + fields-as-`dyn`); `sweeps/order39/probes/a124_*.vl` (eight probes); `reactive-batching.md` (explicit tracking — KEPT by this design); `reactive-turns.md` §7 + A110 door 2 (the contract the push-pull protocol must keep); `trait-objects.md` (B4 DECLINED 2026-08-07; §3.5 criteria; P13 object-safety; Q4/Q5) — reopened in one scope; `trait-typed-fields.md` + B184/B186/B161 (the hidden-generic positions that work today); B253 (return-position existential, RULED keep); A123 (ships regardless, becomes a node); A112 (delta derivatives are not this); A28 (owner-tied `memo`); C14 (arena applies to `Memo`); F1/vilan-rt (a `dyn` on the native backend) |
| [A128](items/A128.md) | A121 S3 (LOW) — the `focusout` path: `Event::related_target` is bound and pinned (ui-40), so a `Contain` scope can learn 'focus left me, and to there' and a `Wrap` scope can dismiss on Tab-out if an app wants it; deferred by focus-scope.md §S3 and by ui-40, `Contain` is correct without it | feature (std::ui) | ui-40 (S1+S2 landed; S3 deferred, rec close A121 and file S3 LOW); proposal/focus-scope.md §S3 |
| [A129](items/A129.md) | NEW — RULING — a `Splice` is applied BY POSITION on the delta path, so ROW IDENTITY is lost across it: a splice that removes rows and re-inserts the SAME keys rebuilds them where the keyed pass keeps them (`set_all` over the same three keys: `cut=3 built=3` vs `set`'s `cut=0 built=0`; `reconcile_to` with one edit + an append rebuilds the tail; in the 300-turn walk `each_by` builds 1,078 rows on the op path against 557 on the pass) — and `set_all`'s doc claim ('cheaper than `set`') is FALSE for keyed consumers. Rule: does a splice's span get a KEYED match (the pass's identity rule inside the span — rec yes, bounded to the span, so identity survives a re-insert and the op path never builds more than the pass), or is `set_all`'s doc corrected to say so | design (ruling) | collections-41's find 2 (2026-09-24); repros in `sweeps/order41/collections-41/finds/`; incremental-collections.md §13 did not price it |
| [A130](items/A130.md) | a MODULE-LEVEL `.cell()` leaks by design (it is owner-tied and a module has no owner to release it — the strong root → record → node → root loop lives for the program), and the mistake is easy to make: should a separate `.cell_global()` carry that intention explicitly, with `.cell()` REFUSED in a module binding's initializer and steered ('build it under an owner, or say `.cell_global()`')? | design | the owner (2026-09-25) on A124's no-cycle question; the no-cycle gate's exemplar |
| [A131](items/A131.md) | the delta-driven `each` builds rows that are CUT in the SAME DRAIN: `insert_at(0)` then `remove_at(0)` in one `edit`, or a push then a `pop`, builds a row the settled list never shows — A129's keyed span match brought the 300-turn walk's `each_by` from 1,078 rows to 575 against the pass's 557, and the remaining 18 (one per such turn) are exactly this class; only a DEFERRED build (row construction at the end of the drain, over the ops' net effect) recovers them — a real redesign of the op path, not a patch | design | collections-42's find 3 (2026-09-25); the per-turn diagnostic at `sweeps/order42/collections-42/`a129/walk-diagnostic.vl; incremental-collections.md §13 |
| [A132](items/A132.md) | `std::delta`'s `map_each` LEAKS through a reference cycle: its `source.on_change(\|_\| … source.since(cursor) …)` handler captures the SOURCE it subscribes to, so the subscription holds the source and the source holds the subscription — the native leak census (C14's gate) reads 14 cells live at exit in `delta-law` and 92 in `list-cell`; on JS the same cycle is garbage-collected only once nothing else reaches it | bug (leak) | native-42 (2026-09-25); `native-leak-census.tsv` under `sweeps/order42/native-42/`; C14 §15; collections' file |
| [A133](items/A133.md) | over an IN-PROCESS transport, `sub`/`effect` on a node over a MIRROR sees the inline SEED twice (over a socket it is correct): the mirror's seed arrives inline under the subscribing turn and the node's pull re-reads it — a std design question (the in-process transport's inline delivery vs the push-pull protocol's one-notify-per-settle) | design | reactive-42 (2026-09-25); repro `sweeps/order42/reactive-42/finds/`node_over_a_mirror_sub_sees_the_inline_seed_twice.vl; remote-sources.md §2b; reactive-turns.md §7 |
## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B298](items/B298.md) | NEW — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's | design | smalls-32's find F2 and open Q1 (2026-09-11); rec: yes for a bounded `_`, behind the Q3 ruling |
| [B348](items/B348.md) | NEW — a GENERIC `Callable` does not coerce into a closure-typed position (by design: `impl Box<type T> with Callable` writes `call` in the impl's own binders — fn-coercion rule 2's reason); `held()` calls, `let f: \|…\| T = held;` is the ordinary mismatch; needs the expected-type-driven instantiation rule 2 is waiting on | design | lang-b-36's FIND 3 (2026-09-14); design, on demand |
| [B364](items/B364.md) | NEW — a CLOSURE RECORD cannot be built over a `&mut self` receiver (`a view cannot escape its scope`): 'erase a stateful thing behind a bag of closures' has one spelling — own a `mut` local in the frame that mints the record (spec §6.9's binding capture) — which is what deleted `JsonWriter::serializer()` and its three siblings under A108 | design | wire-38's FIND-5; relevant to C14 S4 and the native backend |
| [B367](items/B367.md) | NEW — B318 S4's ADMISSION MISS may be unreachable: a four-module reproducer (trait in A, `impl` in B, a generic body in C importing only the trait, the entry importing `(impl _)`) compiled CLEAN, so the generic body resolved under a file that does not admit the impl — either the refusal is dead code or visibility.md §3.5's rule is looser than written; and neither source-carrying transformer note (the admission miss, B55) has any pin | bug | editor-38's FIND-3 (found hunting an end-to-end pin for E196); B354's new pins are the nearest neighbours |
| [B385](items/B385.md) | CRASH — any GENERIC recursive type in a world that holds a resource type overflows the analyzer's stack: `import std::reactive::SignalCell; enum M<type T> { Base(T), Map(List<M<T>>) }` (nine lines) aborts `vilan check` AND the language server (`thread has overflowed its stack` → SIGABRT, the server restarts and dies again on the same buffer). The recursion is `container_resource_in` ↔ `container_resource_in_anonymous` (R10's resource-in-container walk, analyzer.rs ~10820/~10957) with `substitute_type` above each frame — its `visiting: HashSet<TypeId>` guard never repeats because each member substitution mints a fresh `TypeId` for `M<T>`. The non-generic `enum M { Map(List<M>) }` is fine; a bare file with no resource type in the world is fine (the container list is empty) | bug (CRASH — stack overflow in the analyzer; the language server dies with it) | the owner (2026-09-21, a `SignalMapper<T, U, O>` with `Shared<SignalMapper<any, T, O>>` — neither `any` nor `Shared` is needed; kept as `probes/b385_owner_signal_mapper.vl`); R10 / A19 / B103 (the walk's own history — `container_resource_in`'s doc names all three); B300 family (fresh ids from substitution defeating identity-keyed guards is the same class as `TypeCycleGuard` exists for); E-family (a server crash on a legal program is the worst editor failure — the server must also survive an analyzer panic/abort, which is a separate hardening question: N-item if wanted) |
| [B398](items/B398.md) | NEW — MISCOMPILE — `(U in T: dyn Source<U>)` (a mapped tuple over a `dyn` element) TYPE-CHECKS and then fails at runtime with `s[1].get is not a function` — the `dyn` coercion at a mapped-tuple element position emits no object (dyn-40's explicit-coercion rule has no arm for the comprehension's element type); probe `sweeps/order41/papers-41/probes/a122_09_mapped_dyn_source.vl` (`a122_09b` the controls) | bug (miscompile) | papers-41's F-b (2026-09-24) |
| [B399](items/B399.md) | NEW — the ELEMENT bound in a tuple-family bound `T: (2..: PartialEq)` does not reach `U` inside a comprehension body: `a == b` on two elements is refused as unbounded although the pack declares the bound; probe `sweeps/order41/papers-41/probes/a122_12_element_bound.vl` (`a122_12b` the control that a pack WITHOUT the bound is refused — that half is right) | bug (inference) | papers-41's F-c (2026-09-24); A122's `TupleKey<T, U>` typing needs it |
| [B400](items/B400.md) | NEW — MISCOMPILE — COPY ELISION on JS is wrong when a `Shared::read()` TEMPORARY is live while the cell is written: `for x in cell.read() { cell.write().push(..) }` iterates the GROWING array (JS b=4, native b=3); and a CLOSURE whose parameter is `&List<T>` accepts `cell.read()` where a `fun`'s `&` parameter refuses the same argument, and sees the live list (JS seen=3, native seen=2) — the checker gap is the closure parameter's `&` admitting a temporary read. `ListCell::peek` (M86) is written with that closure spelling and documents that its closure must not write the cell; when the gap closes, `peek` re-spells through `Weak::get`, which the Rust backend cannot emit yet (`WeakGet`) | bug (miscompile) | collections-41's find 1 (2026-09-24); repros in `sweeps/order41/collections-41/finds/`; solver + native |
| [B401](items/B401.md) | NEW — the analyzer's INHERITED-DEFAULT lookup ignores impl ADMISSION: `b.vl` exports `Box`; `t.vl` has traits `One`/`Two` each with a default `describe`; `p1.vl`/`p2.vl` each `export impl Box with One/Two {}`; `main.vl` with `import pkg::p1 only;` still compiles `box.describe()` and emits "one" — and with p2 loaded in any form (`only`, or a non-exported impl) the call is 'ambiguous on Box' whatever the file admits. B318's `only`/`(impl …)` selectors decide which impls a FILE admits for a method call; the inherited-default path does not consult them (N124's pin covers the shared `mono` lookup, which is right — the analyzer's own resolution is what ignores it) | bug (inference) | hygiene-41's find 1 (2026-09-24); solver lane |
| [B403](items/B403.md) | NEW — MISCOMPILE (native) — B393's real shape: the analyzer ACCEPTS a static call on a bounded impl generic that nothing binds (a bare `Holder::tag()` inside `impl Holder<type T: Label>`); JS then stops with an internal error (loud), NATIVE silently prints `A A` for a `Holder<B>` — one unkeyed instance reads whatever substitution was active when it was first emitted; at top level both backends fail at emission. The fix is in the analyzer's 'cannot infer `T`' check, NOT in the emitters (sharing the four per-emitter functions would copy the wrong answer): solver-41's first attempt was too broad — `Self::` and `Holder<T>::` bind through a channel that check does not read, and std's `FromJson` statics tripped it (reverted). N125's four functions stay per-emitter until this lands | bug (miscompile) | solver-41's B393 probe verdict (2026-09-24); solver lane |
| [B404](items/B404.md) | NEW — an UNSUFFIXED literal typed `BigInt` is emitted as a JS NUMBER and crashes at runtime: `fun tb(v: BigInt): BigInt { v + 1n }` then `tb(3)` throws 'Cannot mix BigInt and other types' — the literal law (B370/B389) types `3` as `BigInt` from the parameter but the emitter writes `3`, not `3n`; pre-existing before solver-41 (B389's stall step EXCLUDES BigInt for this reason) | bug (miscompile) | solver-41's find 1 (2026-09-24) |
| [B405](items/B405.md) | NEW — NUMERIC MIXING is silent: `let y: i32 = 3; print(y / 2.0)` prints `1` — an `i32` operand meets an `f64` literal and the expression is integer division with no diagnostic (the literal takes the LEFT operand's type under B389's rule, which is right for `i32 / 2` and wrong for `2.0`: a fractional literal cannot be an `i32`); the deferred numeric-mixing family — rule whether a fractional literal at an integer-typed position is REFUSED (rec: refuse with an `as_f64()` steer; B402 covers the literal-only case) | bug (inference) + ruling | solver-41's find 2 (2026-09-24) |
| [B406](items/B406.md) | NEW — B389's remaining gap — a literal passed to a GENERIC CONSTRUCTOR inside a STRUCT LITERAL FIELD is not typed from the field: `struct S { count: Shared<u53> }` then `S { count = Shared::new(0) }` refuses with `Expected Shared<u53>, but got Shared<i32>`; std's `rpc.vl` hits it (index-41's `b389-gap-literals.patch` carries two suffixes until fixed) | bug (inference) | index-41's find 1 (2026-09-24); solver lane, before I5 S2 |
| [B407](items/B407.md) | NEW — NEGATIVE literals are ACCEPTED at UNSIGNED types: `let n: usize = -1;` (and `u53`, `u8`) compiles and prints `-1` — with B389 in place every `-1` sentinel compiles SILENTLY as a `usize` after I5 S2, which is exactly the class the migration must catch. Refuse a negative literal at an unsigned type (a diagnostic naming the type's range); the seven sentinels of index-type.md §3.4 are the pin | bug (inference) | index-41's find 2 (2026-09-24); solver lane, before I5 S2 |
| [B408](items/B408.md) | NEW — a BLANKET METHOD is UNREACHABLE through an abstract BOUND: `fun f<S: Src<i32>>(s: S) { s.twice() }` fails 'S has no method twice' when `twice` is a blanket over `S: Src<T>` — blocks `.cell()`/`.distinct()` in generic code TODAY and blocks A124 S2c (every generic `s.map(..)` once `map` is a blanket). Repro in `sweeps/order41/reactive-41/finds/`; reproduces on next @9ffa397e | bug (inference) | reactive-41's defect 1 (2026-09-24); SOLVER, FIRST in Order 42 — the flip cannot land without it |
| [B409](items/B409.md) | NEW — the B395 CLASS again — a SAME-TRAIT bound is checked at the WRONG arguments: a default inherited through `impl M<type S: Src<type X>, X, type U> with Src<U>` is checked at `Src<U>` instead of the upstream's `Src<X>`; worked around in S2b with a private `Upstream` blanket. Repro in `sweeps/order41/reactive-41/finds/`; reproduces on 9ffa397e (after B395's fix) | bug (inference) | reactive-41's defect 2 (2026-09-24); solver, Order 42 with B408 |
| [B410](items/B410.md) | NEW — MISCOMPILE — an OVERRIDE is not selected through a bound when the trait's ARGUMENT is a TUPLE: JS prints 'default' where the impl's override must run; worked around in S2b by each node calling its own `on_settle`. Repro in `sweeps/order41/reactive-41/finds/`; reproduces on 9ffa397e | bug (miscompile) | reactive-41's defect 3 (2026-09-24); solver, Order 42 |
| [B411](items/B411.md) | NEW — a default's CLOSURE PARAMETER is left UNTYPED when the impl reads its value type from a NESTED binder (`impl Map<type S: Src<type T>, T, type U>`); worked around in S2b with phantom type parameters (which is why the nodes are `Map<S, T, U>` / `Switch<S, T, I, U>`, not the paper's `Map<S, U>`). Repro in `sweeps/order41/reactive-41/finds/` | bug (inference) | reactive-41's defect 4 (2026-09-24); solver, Order 42 |
| [B412](items/B412.md) | NEW — a generic `S: Source<X>` VALUE cannot be ERASED to `dyn Source<X>` — the explicit coercion at a `dyn`-typed position (dyn-40's rule) does not admit an abstract `S` that satisfies the bound, only a concrete type; needed by `Combine`'s `(U in T: dyn Source<U>)` in generic callers and by A124 §3's 'store as dyn'. Repro in `sweeps/order41/reactive-41/finds/` | bug (inference) | reactive-41's defect 6 (2026-09-24); with B398 (the mapped-tuple element coercion) |
| [B413](items/B413.md) | DISSOLVE the `resource` KEYWORD into a struct attribute (`[resource] struct Handle { … }`), and settle the word: 'resource' vs 'droppable' vs 'temp' — the keyword bit A124's `Resource<T>` (the pending family's obvious public name), and a kind that changes destruction rules already has precedent as an attribute (`[platform(..)]` changes analysis) | design (breaking) | the owner (2026-09-25); destruction.md §10; trait-objects.md Q5 ('may an object hold a resource'); A124 Q-name |
| [B414](items/B414.md) | CONTEXTUAL keywords: not every keyword needs banning as an IDENTIFIER — `with` (bit M86's borrowing read, now `peek`), `as` (only in imports), `in`, `layer`, `sync`?… should be usable as names where the grammar cannot confuse them; DECLARATION keywords (`let`, `mut`, `fun`, `struct`, `enum`, `trait`, `impl`, `import`, `export`, `type`…) stay reserved | design | the owner (2026-09-25); the reserved-word list in the lexer; B382/E221 (labels), F27 R1 (`[platform]`) |
| [B415](items/B415.md) | a HOST for file-level attributes: `[platform("browser")] mod self;` instead of the bare `[platform("browser")];` statement F27 R1 shipped — `self` a reserved module name; and possibly `export *;` → `export self::*` | design | the owner (2026-09-25) on F27 R1's spelling; editor-41's `;` choice ('an attribute with nothing under it', chosen for lack of a host); E181/B318 (`export *;`) |
| [B416](items/B416.md) | `[derive(Wire)]` binds a LOCAL per field, so a field named `deserializer` (or any name the expansion itself uses) fails with 5 errors (`json.vl:995`, `rpc.vl:5692`) — the derive's hygiene: its own locals must not collide with field names | bug (macro) | papers-42's find 3 (2026-09-25) |
| [B417](items/B417.md) | a METHOD named `Self` is accepted at its declaration but cannot be called (`x.Self()` parses `Self` as the type); refuse the name at the declaration with a steer | bug | papers-42's find 4 (2026-09-25); B414 Q2 |
| [B418](items/B418.md) | MISCOMPILE — copy elision on JS is wrong when a `read()` reaches a binding through a BRANCH or an ARGUMENT (B400's sibling): `let b = if flag { s.read() } else { [] }` and its `match` twin alias the LIVE list, so a later `s.write().push` shows through `b` (JS 4, native 3); a `.read()` passed BY VALUE to a `fun` or a closure that writes the cell sees the write (JS 6/5, native 5/4). It bit collections-42's first A129 draft silently (the `if` form dropped M86's copy pin from 11 to 6); std was re-spelled to not depend on it | bug (miscompile) | collections-42's find 1 (2026-09-25); repros in `sweeps/order42/collections-42/`finds/elision_read_through_if_and_args/; solver-42 told — fix with B400 if it is the same rule |
| [B419](items/B419.md) | a BLANKET over a supertrait is not found on a type that implements the supertrait only through a ONE-BLOCK subtrait impl (`impl Cell with Sig<T>` — B243's form): under the flip `map` is a blanket over `Src`, and `traits::b243_a_one_block_signal_impl_reaches_source_map_and_effect_on_change` stays red ('Cell<i32> has no method map') even with B408 applied — pre-existing on d65d4e75 | bug (inference) | reactive-42 (2026-09-25); repro `sweeps/order42/reactive-42/finds/`blanket_over_a_supertrait_not_found_through_a_one_block_subtrait_impl.vl; B243; on the flip's path |
| [B420](items/B420.md) | an abstract `S: Src<i32>` does not SATISFY a bound `Up<i32>` when `Up` is provided by a blanket IMPL over `Src` — the trait-satisfaction twin of B408 (B408 fixed method LOOKUP through the bound; satisfaction of a blanket-provided trait at an abstract parameter still fails): keeps the four b371 pins red after B408 ("'Map<S, T, I>' does not implement trait 'Source<I>'") | bug (inference) | reactive-42 (2026-09-25); repro `sweeps/order42/reactive-42/finds/`blanket_impl_bound_not_entailed_by_an_abstract_parameter.vl; cured for std by B409 (retiring reactive.vl's `Upstream` blanket), so B409 is on the flip's critical path — solver-42 told |
| [B421](items/B421.md) | MISCOMPILE — the `dyn` coercion accepts a VOID closure body as its value: `show(root.mapped(\|n\| { n * 2; }))` with a `dyn Src<i32>` parameter checks clean and prints `NaN` (the block's trailing `;` makes it void; the coercion at the `dyn` position does not check the closure's result type against `U`) — on d65d4e75 | bug (miscompile) | reactive-42 (2026-09-25); repro `sweeps/order42/reactive-42/finds/`dyn_coercion_accepts_a_void_closure_body_as_its_value.vl; dyn-40's explicit coercion |
| [B422](items/B422.md) | the BASE-CACHE KEY carries no std ROOT: two byte-identical std roots (the tree's `std_root()` and the materialized `~/.vilan/std-cache` std) file under ONE key, so M19's hard assertion (the `references::` fingerprint collision — two fingerprints equal exactly `sources[1..]` of `import std::io::print` under the two roots) can fire in ONE LSP session with documents inside and outside a checkout — a product bug, seen in-suite as the five `references::` panics under plain `cargo test` (N131); the cache pins also read other tests' worlds because `BASE_CACHE_LOCK` serializes only the ~33 pins that take it | bug (LSP/cache) | std-42 (2026-09-25); documented at `BASE_CACHE_LOCK` in vilan-lsp/src/document.rs; the analyzer's cache key (solver's file) |
| [B423](items/B423.md) | a literal in an `if` ARM is not typed from its SIBLING arm in an unannotated `let`, either order: `let n: u53 = 5; let m = if n > 2 { n } else { 0 };` (and the reverse) → '`if` arms have mismatched types: expected u53, but got i32'; with a declared return type (`fun pick(flag: bool, n: u53): u53 { if flag { 0 } else { n } }`) it passes — not among index-type.md §4's positions; std needs it nowhere (I5 S2's residue is zero) but kolt hits it | bug (inference) | index-42 (2026-09-25); B389's literal law; the `match` twin presumably alike |
| [B424](items/B424.md) | RULING — an UNCONSTRAINED generic at a call: `err.or_else(\|e\| Ok(7))` leaves `or_else<F>`'s `F` with nothing to bind it (the closure only ever builds `Ok`), the analyzer records `any`, JS runs, and the native backend must pick a Rust type — it refuses by name today (`result-combinators.vl` stops there). Rule: (a) the analyzer refuses an unbound generic at the call with a steer to a turbofish/annotation (both backends agree; breaks JS programs that ran); (b) default `F` to the `Err` side's declared type when the closure never produces one (a defaulting rule); (c) native keeps refusing by name | ruling (inference) | native-42 (2026-09-25); F38's remainder; B403's family (a generic nothing binds) |
| [B425](items/B425.md) | the CONTEXT PASS under-threads an argument: `blanket-impl.vl`'s `badge("static")` passes FEWER context arguments than its callee declares (rustc E0061 natively — 'broken' — now refused by name by native-42); the analyzer's context-threading plan (B146's class) misses a call through a blanket | bug (context pass) | native-42 (2026-09-25); F36 (a) got the program past the interpolation only to hit this |
| [B426](items/B426.md) | B407 does NOT catch a CONSTANT `0 - 1` at an unsigned type: `let end: usize = 0 - 1;` compiles and prints -1 on JS — B407 refuses a negative LITERAL, not a literal-only expression that folds negative; the sentinel census (index-type.md §3.4) missed this spelling too (ui.vl's focus wrap was one). Fold literal-only arithmetic at an unsigned position and refuse the negative result with B407's message | bug (inference) | index-42 (2026-09-25); B407; the two markdown pins now pin ruling 2's bounds-check refusal instead |
| [B427](items/B427.md) | `combine((a, b)).map(\|pair\| { let (x, y) = pair; x + y })` leaves `map`'s `U` OPEN: a destructuring `let` inside the closure does not carry the tuple's element types into the body's tail; annotating `pair` fixes it — under the flip `map` is a blanket, so this is the everyday spelling | bug (inference) | reactive-42 (2026-09-25); repro in `sweeps/order42/reactive-42/finds/`; B392's family (a closure typed from its body) |
## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |
| [C14](items/C14.md) | NEW — `SignalCell` (and `Owner`) off `Shared` boxes and onto `Rc` or OWNER-ATTACHED arena `Handle`s — the necessary step for native apps with native code and NO garbage collector (F1). Today `SignalCell<T> { value: Shared<T>, subscribers: Shared<List<Subscriber>>, identity }` and `Owner { cleanups: Shared<..>, disposed: Shared<bool> }`; `Shared` is a compiler primitive (`external struct`, `primitive_struct_ids` 'Shared', 8 analyzer sites) whose doc says 'reference-counted' but whose JS backing is a plain object box the HOST GC reclaims — nothing counts, cycles (cell ↔ subscriber closures capturing cells) are the GC's problem, and a disposed owner's cells stay readable forever. Neither property survives a native backend. Two candidate representations, to be DECIDED in a paper: (a) a real `Rc<T>` — counted box with deterministic drop (`destruction.md`, `drop.vl`), cycles broken by the owner's disposal or by `Weak` (C1, OPEN); (b) owner-attached `Arena` + `Handle` (std::arena already exists; the owner holds the arena, a cell is `Handle<Slot<T>>` + its owner, disposal frees the arena wholesale — no counting, no cycles, and a stale handle reads `None` instead of a ghost value, which is a BEHAVIOUR change from today for a cell that escapes its owner: module-level cells, `std::memo` caches, rpc mirrors, `Task` results). Blast radius: reactive.vl 47 `Shared` mentions, 13 std files use `Shared`, kolt 4 (store, lib/input_system, lib/overlay, lib/rotary) | design | the owner's ask (2026-09-14): 'change SignalCells from `Shared` boxes to `Rc` or arena `Handle`s (handles attached to owner). This is a necessary step to support native apps with native code (no garbage collector)'. Companion of F1; C1 (`Weak<T>`) is the other brick if (a) wins |
| [C15](items/C15.md) | NEW — spec §6.9's closure rule ('captures BINDINGS, not values') makes every mutably-captured binding a SHARED CELL on a native backend: the F1 probe (R-2) had to box `let mut count` as `Rc<RefCell<_>>` because two closures assign it — on JS the host boxes the place for free; natively the compiler must decide per binding whether it is captured by value (copied), by counted reference, or refused. Larger than `Shared` (C14) — no item existed | design | paper-native-36's FIND-4 (2026-09-14; native-apps.md §2.4 R-2); rule with C14's S4 and F1's S1 |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |
| [D14](items/D14.md) | the lexical spec §2.2's contextual-keyword list OMITS `as` and `only`, which the compiler already treats as contextual (accepted as names at every position probed); `layer` is listed nowhere and is not a word the compiler knows | docs | papers-42's find 5 (2026-09-25); contextual-keywords.md §1 |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E182](items/E182.md) | NEW — `infer_platform` (vilan-core lib.rs:93) is BINARY browser/not-browser: a third `std::ui` twin makes the editor report the PROCESS twin's `View` against native source — the E113 shape its own doc comment describes; latent until F1's S2 adds the `native` platform | bug | paper-native-36's NEW-4 (2026-09-14; native-apps.md §3, §8) |
| [E199](items/E199.md) | NEW — a refused generic-bound message can name the WRONG `T`: in solver-37's B352 probe a wrong render body was refused as `Swap<str, StorageSignalCell<bool>, View>` does not implement `Slot` — `T` taken from the closure BODY rather than from the SOURCE argument, so the message names a type the author never wrote; the refusal is right, its sentence is not | diagnostics | solver-37's FIND 5 (2026-09-17); S |
| [E214](items/E214.md) | NEW — E202 as built places the `>` but leaves the CURSOR AFTER it, and typing `>` then doubles it: the server answers `<` through `onTypeFormatting`, and an LSP text edit cannot position the caret or register the placed character as an auto-closed pair, so VS Code's native overtype (typing the closing character steps over the one it auto-placed) never engages. The owner: 'natively in VS Code the closing pair is placed after the cursor and typing that pair steps the cursor over the auto-placed one — can you hook into that?' | editor | the owner (2026-09-21); editor-39's OPEN Q1 said exactly this in its report and recommended a client-side fix; E202's ruling R9 kept `<` OUT of `autoClosingPairs` because a static pair fires on `a < b` |
| [E223](items/E223.md) | NEW — the mapped-tuple refusal calls a tuple-BOUNDED `T` "unbounded": a comprehension over a value of type `T: (2..)` answers 'a tuple comprehension's source must be a mapped tuple, got T' and its note says `T` is unbounded, when `T` carries the `(2..)` bound — the sentence should name the bound and say a VALUE of mapped-tuple type is not a comprehension source today (A122's ask); probes `sweeps/order41/papers-41/probes/a122_02_generic_divorce_value_source.vl`, `a122_03_generic_positional_read.vl` | diagnostics | papers-41's F-d (2026-09-24) |
| [E224](items/E224.md) | NEW — deprecation and internal-use WARN at import leaves for TYPES (E221's lint, B382's type warnings) but NOT for FUNCTIONS: a `[deprecated]` function's import leaf is silent (pre-existing); one rule for all labelled items at the import leaf | diagnostics | editor-41's find 5 (2026-09-24) |
| [E225](items/E225.md) | bindgen's `RESERVED` list has DRIFTED from the lexer: it lacks `css`, `dyn` and `lazy`, so a TypeScript member named one of those produces bindings that do not parse — the list should be derived from the lexer's `KEYWORDS` (one source), and after B413 it must not name `resource` | bug (bindgen) | papers-42's find 6 (2026-09-25) |
| [E226](items/E226.md) | the regime-3 `;` steer (a missing return value) is not reached through `.map(..).cell()`: the expectation stops at the receiver, so a `;`-ended closure body inside `map` followed by `.cell()` gets the plain mismatch instead of the steer; B421 (the `dyn` coercion accepting a void body — a MISCOMPILE) is the same blind spot at a coercion | diagnostics | reactive-42 (2026-09-25); B421; the regime-3 pin re-anchored on a `Map<SignalCell<i32>, i32, i32>` annotation meanwhile |
## F. Native targets & rendering — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [F1](items/F1.md) | NEW — DISCUSSION: building GPU-RENDERED NATIVE APPS in vilan. Today the build model is `Target` = `Backend` + `Platform` (platform-model.md, implemented 2026-06-23) with ONE backend (the JS emitter) and the browser/node/deno/bun platforms; `std::ui` has two layers behind one public surface (browser = live DOM, process = SSR string tree — the precedent for a third). A native app needs FOUR things the project has none of: (1) a native BACKEND — emit Rust (rustc does codegen; vilan's affine moves, borrows and `own` map onto ownership) vs C vs wasm-in-a-host vs an interpreter; (2) memory without a GC — C14 (cells on Rc/owner arenas), real counting for `Shared`, closure captures (`capture-clones.md`), strings/lists with owners, `destruction.md`'s drops actually running; (3) a RENDER LAYER — a third `std::ui` twin building a retained scene tree rendered by a GPU renderer (wgpu + vello-class 2D, text shaping, a flexbox-class layout engine such as taffy) with `Style` mapped onto it (A95 conditions: `hover()`/`active()` become hit-test state, `media`/`sm..xl` become window queries, the `css` block a native style resolver) and events/input/windowing (winit-class); (4) std's platform layers (`[library.layer]`, `PlatformPattern`) admitting a `native` platform and the coloring rules deciding what a native entry may reach (no `std::dom`, no `fetch` shaped as the browser's). Prior art to measure against: Dioxus/Blitz (HTML+CSS semantics, vello-rendered), Makepad, Slint, GPUI (Zed), Flutter (Impeller) | design | the owner's ask (2026-09-14): 'File an item to discuss support for building GPU-rendered native apps'. OWNER QUESTIONS before a paper: (1) which backend — emitting Rust is the shortest path to a real binary and reuses the ecosystem (wgpu, winit, taffy, cosmic-text) but makes rustc a toolchain dependency; (2) scope — desktop first (Windows/Linux/macOS), mobile later?; (3) is the UI surface the DOM-shaped `View` (so kolt's views compile unchanged onto the native layer, the SSR precedent) or a new retained-tree API?; (4) does 'native app' mean client-only, or client + server in one process (the rpc runtime in-process)?; (5) sequencing — C14 first (verifiable on JS), then a backend probe (hand-translate one small vilan program to Rust and list what the emitter must know), then the render layer |
| [F15](items/F15.md) | NEW — the THREE-LAYER `std::ui` rule and an N-way twin-parity gate: today two `ui.vl` twins (browser DOM, process SSR) share one public surface and the parity gate is structurally TWO-valued; a native twin makes it three, and nothing today states the rule that every twin exposes the same surface (`ssr.md`'s 'same public surface' is a sentence, not a gate). F1's S2 cannot be sized without the rule | design | paper-native-36's NEW-3 (2026-09-14; native-apps.md §6.2, §8 — 'should be ruled with this paper'); OWNER RULING wanted |
| [F17](items/F17.md) | NEW — DISCUSSION (the owner's, 2026-09-17): how to write UI code ONCE so it works for native and web simultaneously — with named exceptions (native window chrome: close/minimize/maximize buttons, and whatever else the platforms genuinely do not share). The F1 paper's answer (the DOM-shaped `View` as a third twin, Order 37's R2) is the STARTING POSITION, not the conclusion; the conversation decides the shape of the shared surface, the exception mechanism (platform-colored members? a `native`-only module? separate chrome components?), and what the three-layer rule (F15, R4 recorded) has to gate. Nothing in F1 S3–S5 starts before it | design | the owner's conversation, 2026-09-17; NOT queued — a conversation, then a paper section |
| [F26](items/F26.md) | NEW — the monomorphisation RESOLUTION half now exists twice: `resolve_type_id`, `type_key`, `collect_type_generics`, `inherited_substitution`, `call_substitution`, `trait_parameter_substitution`, `resolve_inherited_default`, `trait_default_member` are `&self`-over-`Program` in transformer.rs and re-written in vilan-rust, held together only by the differential; and `analyzer::RENDER_MEMBER` + `transformer::unescape_string` are `pub(crate)`, so the backend crate carries copies | design | native-a-38's FINDS 1–2 (the JS emitter's instance SET cannot be shared — it is minted during the JS walk with `js::Node` values) |
| [F27](items/F27.md) | NEW — how USERLAND writes platform-specific code: kolt's `lib/conditional_value.vl` is a user `Slot` impl over the BROWSER twin's `Region` (`region.anchor`, `cut_row`), so (1) opened before anything imported it, the editor analyzed it as `process` and errored `struct 'Region' has no field 'anchor'` — every name it imports (`Region`, `Row`, `Slot`) exists in BOTH `std::ui` twins, so import-name inference (B36) has no evidence; and (2) it has no server-side meaning at all, though 'technically a server-side implementation should exist too'. The owner's candidates: `import std::ui::Region layer browser;`, a `[layer(L)]` attribute, a layer entry in vilan.toml. Measured the same day: today's `[platform("browser")]` FUNCTION fence does not change how a body resolves — `region.anchor` still errors under it | design (paper) | the owner's question (2026-09-21); platform-coloring.md §3.1 ('color is on code, not on types') and §3.4 (layers are a LIBRARY's boundary contract); E113/B36/E182 (how the editor picks a platform); F17 (write UI once for native and web, with NAMED EXCEPTIONS — the same mechanism); A119 (`when_some` — std shipping this form in both twins removes kolt's exhibit, not the question) |
| [F28](items/F28.md) | NEW — once `[platform(..)]` fences items (F27), should std's LAYERS collapse? Measured on c3f7d1a3: std has exactly ONE twin pair — `std::ui` (browser/ui.vl 1,756 lines, process/ui.vl 757) — and thirteen single-platform layered modules with no twin at all (`dom`, `storage`, `router`, `dev`; `fs`, `http`, `db`, `document`, `rpc_server`, `build`, `watch`, `process`); std uses NO `[platform]` fence anywhere today. The ui twins declare 60 names in common: 19 byte-identical (11 % of the browser twin's code, 21 % of the process twin's — the forms `each`/`each_by`/`each_values`/`when`, `Conditional`, `Each*`, the traits `Slot`/`AttrValue`/`AttrBinding`, `View::attr`/`bind_attr`/`child`), 41 with the same name and a different body, 4 nominals that differ (`View`, `Region`, `Row`, `Swap`), 18 browser-only and 8 process-only | design | the owner's question (2026-09-21); F27 (RULED: module-level `[platform]`, fenced twin items); F15 (the N-way twin-parity gate a native `std::ui` twin would need); `std_twin_parity` (today's two-file gate — found field-blind by F27 R5); platform-coloring.md §3.4 (layers are a library's boundary contract) |
| [F35](items/F35.md) | NEW — native — `ListCell` and `map_each` do NOT BUILD on the Rust backend: 'generic type instantiated at `any`' (pre-existing before Order 41's collections work; the delta path is JS-only today) — the A112 family joins F34's list of reactive shapes the native emitter must reach before A124 S2c makes them common | bug (native) | collections-41's find 4 (2026-09-24); native lane, with F34 |
| [F36](items/F36.md) | NEW — native — two pre-existing gaps independent of `usize`: (a) INTERPOLATING a generic method's result is refused; (b) `encode_json` on a `List<i32>` produces Rust that rustc refuses (E0596, a `&mut` through an immutable binding) | bug (native) | index-41's find 4 (2026-09-24); native lane |
| [F37](items/F37.md) | NEW — native — a PARTIAL MOVE is emitted where rustc refuses it: `let h = r.live; push(copy_of(r))` moves a field out of `r` and then uses `r`; the attach path of the reactive nodes pays one extra copy to dodge it (native copy census: dyn-objects 1/4 → 5/12, board probe 0/4 → 1/7). Repro in `sweeps/order41/reactive-41/finds/` | bug (native) | reactive-41's defect 7 (2026-09-24); native lane, with F34/F35 |
| [F38](items/F38.md) | NEW — native — `result-combinators.vl`: `Result::and_then`'s `U` stays UNBOUND at the native call — the analyzer records no value for it (F34's `map` half was the same class on `Source::map`, fixed by reading the recorded `U`; here nothing is recorded); the program stays refused by name | bug (native) | native-41's find 2 (2026-09-24) |
| [F39](items/F39.md) | NEW — native — a REENTRANT cell read through an ALIAS of the cell being `update`d still PANICS at run time (a `RefCell` double borrow): native-41 refuses `todos.update(|&mut l| todos.get())` at compile time by NAME only when the read names the same place; `let t2 = todos; todos.update(|..| t2.get())` escapes the check. JS answers the in-progress value there. Wanted: the check follows aliases (B267's walk) or the runtime answers JS's value | bug (native) | native-41's find 4 (2026-09-24) |
| [F40](items/F40.md) | NEW — RULING — OS RANDOMNESS natively: kolt's real `server.vl` is 4 host gaps from native (PBKDF2 + `toString`, `random_bytes`, one generic `map`); `random_bytes` and PBKDF2's salt need OS randomness and `vilan-rt` is dependency-free with `forbid(unsafe_code)` — no `getrandom` without either a dependency or `unsafe`. Options: (a) a second optional crate `vilan-rt-crypto` (like `vilan-rt-sqlite`: `getrandom` + PBKDF2/SHA-512, linked only when reached — rec); (b) read `/dev/urandom` through std::fs (Linux/macOS only; Windows needs an API); (c) refuse by name for good | ruling (native) | native-41's find 5 (2026-09-24); the last wall before kolt's server leg builds natively AS WRITTEN |
| [F41](items/F41.md) | native — a FIELD named `self` or `super` is emitted as `r#self`/`r#super`, which rustc REFUSES (raw identifiers cannot spell those two) — live today; the emitter must mangle instead of raw-quoting | bug (native) | papers-42's find 2 (2026-09-25); contextual-keywords.md §12 |
| [F42](items/F42.md) | native — `KeyedCell` does not build natively at all, before or after M87: 'unbound generic type parameter (parameter 2 of struct `Map`)' at `positions = Shared::new(Map::new())` — probably B406's struct-literal-field shape (a generic constructor inside a field not typed from the field); the keyed rpc pins cannot run natively until it does | bug (native) | collections-42's find 2 (2026-09-25); repro `sweeps/order42/collections-42/`m87-native.vl; with B406 (solver-42) and F35 |
| [F43](items/F43.md) | native — a blanket's trait ARGUMENT is unbound through a SECOND trait (reactive.vl's private `Upstream` shape): the rust backend refuses it, so under the flip three native_differential pins go red (the map/flatten probe, the native copy census, the default suite's `reactive-on-change.vl`/`reactive-flatten.vl`) — cured for std when B409 lets `Upstream` retire; the emitter gap itself stays | bug (native) | reactive-42 (2026-09-25); repro `sweeps/order42/reactive-42/finds/`native_blanket_trait_argument_unbound_through_a_second_trait.vl; with B409/B420 |
| [F44](items/F44.md) | native — a CLOSURE pushed into the list it READS is refused by rustc as a RECURSIVE type: `let cell: Shared<List<\|\| i32>> = Shared::new([]); let same = cell; cell.write().push(\|\| same.read().len());` (the closure's type contains the list that contains the closure); the emitter needs a boxed closure type at that position | bug (native) | native-42 (2026-09-25) |
| [F45](items/F45.md) | the native HTTP server has NO GRACEFUL STOP: `vilan_rt::http` runs until a signal, so a server program never reaches process end and the leak census (C14's gate) cannot cover kolt's server exit program; wanted: `Server::stop()` (or a signal handler that drains and returns from `main`) so the census reads a server's cells at exit | feature (native) | native-42 (2026-09-25); C14 §15 'not covered'; A96's HMR socket teardown is the JS twin |
| [F46](items/F46.md) | native — `source.attach(watching(source))`: the RECEIVER is borrowed while the ARGUMENT moves it (rustc E0505); reactive-42 wrote the node subscription as two statements to dodge it; the emitter should evaluate such an argument first (native-42's rule for a `&mut` argument whose place a later argument reads, applied to a by-value argument that moves the receiver's place) | bug (native) | reactive-42 (2026-09-25); repro in `sweeps/order42/reactive-42/finds/`; native lane |
## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |
| [I5](items/I5.md) | NEW — ONE spelling for an index: `usize`. std spells positions, lengths and counts as `i32` almost everywhere (~100 signature sites — `List::len(): i32`, `insert(index: i32, ..)`, `remove(index: i32)`, `get(index: i32)`, `str::len`/`repeat(count: i32)`, `Bytes::slice(from: i32, to: i32)`, `Iterator::take(count: i32)`, `index_of(): Option<i32>`, `Map::len`, `Set::len`) and as `i53` in a dozen more, so a position can be NEGATIVE by type, two halves of std disagree, and ids (`u53`) and indexes meet with casts. The owner's ruling-in-waiting (2026-09-21): switch std to `usize` for every index — one spelling everywhere — with `usize` = `u53` on JS targets unless revisited | design (breaking) | the owner's ask, mid-Order 39; `usize` does not exist in the language today (no hit in std or spec/types.md); census on c3f7d1a3: ~100 index-ish `i32` sites, ~13 `i53`, 0 `u32`/`u53` |
| [I6](items/I6.md) | NEW — RULING — NEGATIVE-to-UNSIGNED conversion differs by backend: `(-5i8).as_usize()` answers `-5` on JS and `0` natively (`u53` has always done this; `usize` mirrors `u53` on purpose). Rule which answer is the language's: (a) saturate to 0 on both (native's answer; JS emits a `max(0, ..)`), (b) wrap (neither today), (c) unspecified like underflow (I5 ruling 2's class — then the native differential excludes it by name). Rec (a): a conversion is an explicit call and should have one defined answer | design (ruling) | index-41's find 3 (2026-09-24); I5's family |
## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |
| [K15](items/K15.md) | NEW — the playground does not say when the formatter DECLINED: vilan-wasm now exports `format_checked` → `{ text, declined }` (E197), but the deployed glue calls the String-shaped `format` | feature | editor-38's hand-back; the WEBSITE repo's |
| [K24](items/K24.md) | the website's playground editor carries its OWN keyword list (`vilan-website/playground/editor-src/editor.mjs:35`, mirrored in the built `src/playground/editor.js`): it still names `resource` (dissolved into `[resource]` by B413) and lacks `css`, `dyn` and `lazy` — the same drift as bindgen's `RESERVED` (E225); the list should be generated from the lexer's table (one source), or at least re-synced at every keyword change | hygiene (site) | syntax-42 (2026-09-25); E225; the website is the owner's (read-only to lanes) |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |
| [L22](items/L22.md) | NEW — `publish-brew` mints its tap token through `app-id`, an input `actions/create-github-app-token` deprecated at v3.1.0 in favour of `client-id`: the v2 → v3 bump must switch inputs in the same change, and the App's client ID is a different value from the one `TAP_APP_ID` holds | process | the review of vilan-lang/vilan#3 (2026-09-18); needs one secret from the owner |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | OPEN — an unchanged package module is re-analyzed every LSP analysis / HMR round; T1a–T1d landed (Order 30), next M55/M56/M57 | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M54](items/M54.md) | NEW — `LastUse`/`source_of` residuals from perf-30's re-pricing: the restored tables are CLONED into `LastUse::compute`'s walk state (8.9M Ir per analysis — needs `&mut Analyzer` or a read-through layer); `collect_unfollowable_loans` is 68% of the warm `LastUse` (whole-program by nature, a loan crosses modules — a record DESIGN); `Program::source_of` is still a linear scan of ~60 ranges and now the residual in `arrival_by` (`source_lookup` exists but is not the default); and `compute_shared_read_bindings` (B267) adds a second whole-program `collect_written_roots()` walk beside the capture pass's | performance | perf-30's and rule1-30's finds (2026-09-08) |
| [M56](items/M56.md) | NEW — R10's residue after T1d is the ENTRY's share of the sweep: 24,459 of 153,302 sites still walked, because the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole and ids minted after the world was stored fall in no range — removing it needs a source→entity index, §3.5's missing module boundary | performance | m19-t1d's find 2 (2026-09-08); the next tranche's shape |
| [M57](items/M57.md) | NEW — the corpus replay differential is BLIND to the R10 seam (and to T1b's): the golden corpus as modules declares almost no resource and none in a container, so a dropped-keys plant leaves `replay_differential` green — an R10 probe (a `List<Guard>` in every third module) would make the standing gate see it; it means editing a shared test function, which needs an owner's word or a lane that owns the file | performance | m19-t1d's find 1 and open question 1 (2026-09-08); OWNER Q |
| [M59](items/M59.md) | NEW — build-30's two follow-ups: persist the leg RECORD into `dist/` so a plain `build` gets the schedule's edges (and the artifact-edge guard's redo) from the second build on, not only under `--watch`; and give the HMR round's own compile loop (`run --watch`, ~main.rs:1340) the same tiering — it writes `dist/` after the whole loop, so the guard's shape differs | performance | build-30's open questions 1 and 2 (2026-09-08) |
| [M60](items/M60.md) | NEW — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update` | performance | keyed-31's find 2 (2026-09-09); rule 1's copy elision is the machinery |
| [M62](items/M62.md) | NEW — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it | perf | wire-32's find F5 and open Q1 (2026-09-11); the owner may prefer the six methods now — say so and it moves to A82's residue |
| [M73](items/M73.md) | NEW — an open module's world is stored UNRESOLVED, so ~150 ms of `resolve_world` is paid on every keystroke in M70's six kolt files (theme.vl build 161 ms vs views.vl 11 ms with the same checks phase); the naive import-fixpoint split moved 0.4 ms of it; a fix has to defer only the ALIAS-reaching work rather than the whole pre-entry pass — scope-chain territory, size after B318 S4 (landed) | perf | perf-36's FIND 2 (2026-09-14) |
| [M79](items/M79.md) | NEW — M73's BUILD, paper first: an entry-open module's world is stored UNRESOLVED and `resolve_world` is paid on every keystroke (theme.vl 133–145 ms; the constraint FIXPOINT is 79–96 % of the pass on all nine measured files — preludes/use-drain read 0.0 ms, imports 0.4 ms, correcting M70's attribution); a RESOLVED world already runs `resolve_world` TWICE (pre-entry and inside the post-entry `build()`) and the second run costs 1.2–8.9 % of the first, so running it twice is proven safe — the whole prize is moving the FIRST run in front of the store. Design (perf-37's Q1): resolve pre-entry and store the partially-resolved world, letting the post-entry second call finish it; the deferral set is per-FILE, seeded by files carrying `import pkg::<entry-alias>` and closed transitively under 'imports from a deferred file' (kolt: 9 of 26 package files for every one of the six, leaving ~60 of ~69 sources resolvable before the store), with a fallback to today's wholesale deferral if the package PRELUDE module lands in the set; the cost is that eleven queues drained inside `resolve_world` must hold their deferred sources back — B222/B236/B237 live in exactly that machinery | perf | perf-37's OPEN Q1 (2026-09-17): out of proportion to an M slot; its own item, paper first; M73 re-pointed here |
| [M84](items/M84.md) | NEW — `Ord::clamp` over an integer costs two `compare` calls since B359 (R1: a default body reaches the TRAIT's `min`/`max`, not the inherent `Math.min`/`Math.max` externs) — semantics identical, JS larger, every integer type | performance | solver-38's OPEN Q1; rec leave it |
| [M87](items/M87.md) | NEW — `KeyedCell::locate` (rpc.vl) COPIES the whole list to read one element — the same shape M86 fixed on `ListCell` (`size()` through `SignalCell::get` deep-cloned 5.8 M of 6.3 M Ir per push); a borrowing read (`peek`-shaped) or a `Shared`-held list beside the notification cell | performance | collections-41's find 3 (2026-09-24); std's rpc lane |
## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N106](items/N106.md) | NEW — `f64` PRINTING is a latent differential hazard the JS side never pins: node prints `Infinity`/`0` where Rust prints `inf`/`-0`, and switches to exponential outside `1e-6..1e21`; `vilan_rt::js_number` documents and implements that boundary for the Rust backend, but no corpus program prints `1e21`, `-0.0` or `Infinity`, so the JS emitter's own behaviour at the boundary is unrecorded; and `str::len` is UTF-16 on JS while the native backend's `str_len` matches it only for the BMP (surrogate pairs diverge — `vilan_rt::str_len` says so) | hygiene | native-b-37's FINDS 4 and 6 (2026-09-17); a corpus program each; the surrogate case is F1 S1b's |
| [N127](items/N127.md) | NEW — the playground's exhibits ROT unchecked: `vilan-playground/todo` on 1265ea5d3 had FOUR dead lines (a removed `import std::print`, `.bind_each` retired by A99, a bare-trait field `Signal<List<Note>>` refused by dyn-40 — whose `dyn` spelling cannot serve it because `update` is INHERENT on `SignalCell`, not a trait member — and the missing `each_by` import), fixed by the integrator at Order 41 GO; the estate sweep counts the playground's six programs for BREAKING lanes but nothing CHECKS them — wanted: the playground's programs in the estate check (a `vilan check` per exhibit in the sweep script, or a corpus copy of each) | hygiene (estate) | Order 41 GO (2026-09-24); the playground is not a git repository — the owner's |
| [N128](items/N128.md) | NEW — N121's remainder — (a) SYNTACTIC visitor walks are unprobed: `collect_module_paths::walk` over `Node::for_each_child` recurses once per method-chain link (a 450-link chain needs > 2 MiB in debug before any probe is reached) — a probe in `for_each_child` (node.rs; gates `node_size`) is the fifth site; (b) UNDECLARED threads: the CLI's three compiler threads (`COMPILER_STACK_SIZE`) and the vilan-wasm entry declare no stack, so their probes are inert — one `with_declared_stack` line each turns a SIGABRT into a panic naming the walk; (c) the 11.3 KiB optimized walk-frame figure in `COMPILER_STACK_SIZE`'s comment is STALE (release measures ~2.4 KiB per level; 1.05 MiB at 453 levels) | hygiene (compiler) | hygiene-41's finds 2–4 (2026-09-24) |
| [N129](items/N129.md) | NEW — `native_differential` FAILS under plain `cargo test` (23/30): it shares one per-process staging directory across threads; nextest's process-per-test hides it — and CLAUDE.md's sentence that `cargo test --workspace` is equivalent to nextest is FALSE for this binary. Fix: a per-test staging directory (tempdir per test), or the sentence names the exception | hygiene (tests) | hygiene-41's find 5 (2026-09-24) |
| [N130](items/N130.md) | NEW — Windows clippy over TESTS is red and nothing gates it: `cargo clippy --target x86_64-pc-windows-msvc -p vilan-cli --tests` fails on `split.rs` (an unused `Instant` import under the cfg) — pre-existing on 1265ea5d; the seal's Windows cross-check runs without `--tests`. Fix the import (one line, any lane) and decide whether the seal's Windows leg adds `--tests` | hygiene (gates) | hygiene-41's find 6 (2026-09-24); integrator one-liner |
| [N131](items/N131.md) | NEW — the vilan-lsp suite FLAKES under plain `cargo test` at high load: `overlay_module_reclaim::open_dependents…` and `session_growth::…` fail on base 1265ea5d too (2 of 3 runs — pre-existing); five `references::` tests failed on the M19 replay assertion ('sources vector moved') in 2 of 4 runs at loadavg 45–50 and never in three base runs nor targeted re-runs — UNEXPLAINED; everything passes under nextest. The M19 set wants a look (a shared-state race the per-process runner hides, or a load-sensitive assertion in N116's class) | hygiene (tests) | editor-41's find 2 (2026-09-24); with N129 (native_differential's plain-cargo-test race) |
| [N132](items/N132.md) | the entry-shaped REPLAY pin (`replay_differential::an_entry_shaped_world_agrees_between_replayed_and_rederived_module_checks`) required EVERY warm pair to reuse while the base cache's 8 MiB LRU budget (`BASE_CACHE_DEFAULT_BUDGET`, ~28–30 worlds at ~280 KiB) EVICTS a slow pair's world under sixteen workers before its second analysis — flaky on the Order 40 seal already (`element-syntax.vl`), 2 of 3 at idle on 9bc243d9 (`list-cell.vl`; four more corpus programs + ~1,000 std lines took the retained count 30–32 → 28–29), red on CI's ubuntu-1 and windows-1. FIXED test-side (bisect-41 50d38921): the pin lifts the budget to unlimited for its two legs and restores it (+410 MiB peak RSS for ~20 s). The honest alternative: require exact reuse only for pairs that actually HIT the cache plus a ≥ 90 % hit rate (so the pin cannot pass on nothing) — needs a per-thread 'served from cache' signal the analyzer does not have; and the file's header (lines 92–95) still says the binary holds ONE test (it holds two, both flipping the global `set_world_reuse` under plain `cargo test`) | hygiene (tests) | the Order 41 seal (2026-09-25); the bisect lane's report; with N129/N131 (the plain-cargo-test races) |
