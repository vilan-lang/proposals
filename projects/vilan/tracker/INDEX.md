# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A72](items/A72.md) | NEW — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate` | feature | dom-30's finds 1 and 2 (2026-09-08) |
| [A73](items/A73.md) | NEW — std-30's two unshipped halves: `Storage` key ITERATION (today `len` + `key_at` by hand; a `keys(): List<str>` or an iterator) and `Debounce::flush()` (fire the pending callback now — a form that unloads before the window closes needs it) | feature | std-30's open questions (a) and (b) (2026-09-08) |
| [A76](items/A76.md) | NEW — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there | feature | reverse-31's find 2 (2026-09-09) |
| [A77](items/A77.md) | NEW — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel | feature | reverse-31's find 3 (2026-09-09) |
| [A81](items/A81.md) | NEW — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere | feature | reverse-31's finds 4 and open Q 2 (2026-09-09); transport-rpc §9.3's async-coloring paragraph is this item's |
| [A87](items/A87.md) | NEW — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it | design | reactive-32's open Q1 (2026-09-11); rec: add `release()` (run the cleanups, stay live) only when a caller appears — the pattern is a smell (a boundary that outlives its content is what fresh owners are for) |
| [A94](items/A94.md) | NEW — a FRAGMENT does not FLATTEN: `<><i/><>..</></>` is a `List<View>` inside a `List<View>` and is refused — A46's lowering is a literal, so nesting one fragment in another is a type error, not a run; pinned and documented as the limit; if flattening is wanted it is a lowering change (concatenate the inner list into the outer) with no type change | feature | slots-33's find F2 (2026-09-11); no exhibit asks for it yet — filed so the limit has an id |
| [A95](items/A95.md) | NEW — `Style`'s CONDITION MODEL is the source of the styling pain: a condition is a COMBINATOR WRAPPING an inner style (`hover(inner)`, `attribute(name, value, inner)`), one condition per axis slot, nested outside-in in a fixed order — so two pseudo-classes on one rule are unspellable (`hover:not(:active)` becomes a raw token that miscompiles when wrapped, B311), `not` had to be a MARKER read by the enclosing combinator (A89), the author memorises `md(within(attribute(hover(..))))` or meets a refusal, and every wrap mints an intermediate style whose rules land dead on the sheet (B308). REDESIGN: a condition is a VALUE — `hover()`, `active().not()`, `pseudo("focus-visible")`, `attribute("disabled")` (presence), `attribute("data-selected").eq("true")`, `within(..)`, `children()`, `md()` — a rule is `style()…on(conditions..)`, rules compose by `.add(rule)` (an inner rule inherits the outer's conditions), and the compiler CANONICALISES the set into the slot order (media, relation, attributes, pseudos): duplicates merge, only a contradiction (`hover()` with `hover().not()`) refuses; `.hover(inner)` and friends stay as one-condition sugar; the css block's name-blind head takes the arguments it already parses as expressions (`.on(hover(), active().not()) { … }`; `.hover { }` = `.on(hover()) { }`), no bare selectors. B308 dies structurally (a rule set is a value, emitted once at application), B311 and the two-pseudo gap die (no condition is ever a string), A89's marker and A93's fence go, `Style::when`'s runtime merge becomes const `.add` composition. BREAKING for every `hover(inner)` in std, kolt, the website and the docs — a mechanical migration. L; a PAPER before any lane: canonicalisation and the specificity band (ui-styling §0bis) are where a hasty version goes wrong | design | the owner's critique and sketch (2026-09-12): `style().set("outline-offset", Length::zero()).add(style().color(Color::blue(500)).pseudo(pseudo("hover")).pseudo(pseudo("active").not()).attribute(attribute("disabled").not()))`, and in block form `css { outline-offset: 0; .add {} }`; kolt's `new_button_style` (styles.vl:62–86, rewritten 2026-09-12) is the exhibit — four gates forced into raw pseudo tokens; my refinement (accepted for the paper's starting point): one `.on(..)` taking the condition set, typed condition values with `.not()`/`.eq()`, canonicalisation in place of the nesting-order refusals. Paper on the next order, RULED before any build |
| [A97](items/A97.md) | NEW — HOLD-COUNTING vs remote-sources §3: the server counts a client's leases per channel and revokes at zero (A92), so a RAW client that sends `Subscribe` twice and `Unsubscribe` once holds its channel open until disconnect; the alternative — client-side demand coordination that never double-subscribes — is plumbing the generated client already does and a raw client would have to repeat. Rule whether the wire contract is 'one Unsubscribe per Subscribe' (documented, as built) or the server forgives | design | rpc-33's find F3 and open Q (4) (2026-09-11), unfiled at the sweep; filed at Order 34's GO (R9); remote-sources §3 was AMENDED at Order 33's sweep to say what the server SENDS holds, not what it relies on — this item is the client-side half |
| [A98](items/A98.md) | NEW — A91's region reconciler costs +1 node per row and roughly DOUBLE the DOM mutation calls per reconcile pass (1,000 rows: nodes 1,005 → 2,005; insertions ~4,000 → ~8,000), because the order pass cuts every live row by span and re-inserts it whether or not its index moved; the plan knows `Keep(index)` but not whether the index moved — skip the cut/re-insert for an unmoved row | perf | slots-35's F3 (2026-09-13); the Ir figure there is stub-dominated (O(children) `insertBefore`), so the honest claim is the node and call counts |
| [A99](items/A99.md) | NEW — RETIRE the parent-method forms `View::when` / `View::swap` (and `View::swap_split`, the split recognizer's retarget twin) in favour of the free `when` / `swap` of `std::ui` that A85 shipped in Order 35: `<div>{swap(value, \|x\| match x { Some(_) => <span>"Exists"</span>, None => <span>"Empty"</span> })}{when(condition, \|\| <span>"Is true"</span>)}</div>`. The methods are one-line sugar (`self.child(Conditional { .. })` / `self.child(Swap { .. })`) kept in lockstep across the browser and process layers; inside their own `impl` block they SHADOW the free function of the same name (which is why `split_route` exists under a third name and why the methods build the literal instead of calling the value form), and `chunks.rs` recognizes BOTH spellings of `swap` (lines 102/105 and 243/247) — one spelling halves it. BREAKING: the rewrite is mechanical, `parent.swap(s, r)` → `parent.child(swap(s, r))` or `{swap(s, r)}` in a child hole; census on 9b22ec36: kolt 6 `.swap` + 1 `.when` (theme.vl:270, views.vl:86/114/158/162/394, overlay.vl:502), std 0 live sites (router.vl:9 and ui.vl doc lines only), docs 7/6, examples 2/3, vilan-core tests 24/6, vilan-cli tests 11/9 (the `.when(` counts include `Style::when`, which STAYS). The owner's example spells `swap`/`when` BARE, so the `std::web` prelude gains the five slot functions (`when`, `swap`, `each`, `each_values`, `each_by`) beside `View`/`view` | design | the owner's ask (2026-09-14): 'File an item to replace `View.swap` and `View.when` with top-level `swap` and `when` functions'. OWNER QUESTIONS: (1) the `bind_each` / `bind_each_values` / `bind_each_by` trio is the same sugar over `each*` — retire them in the same order (rec: yes, one breaking order for all six, one migration note)?; (2) the path: pre-beta hard removal with an editor quick fix 'Use the value form' that rewrites the call (rec), or a `[deprecated]` window (`deprecation.md`)? |

## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B298](items/B298.md) | NEW — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's | design | smalls-32's find F2 and open Q1 (2026-09-11); rec: yes for a bounded `_`, behind the Q3 ruling |
| [B318](items/B318.md) | NEW — VISIBILITY AND THE IMPORT SURFACE (the owner's design, 2026-09-12; paper on the next order): (1) items are PRIVATE by default; `export` marks an item and keeps it local; `export *;` opts a module into today's everything-public; `export(PATH)` narrows (`export(in mod)` keeps one item hidden under an `export *;`, `export(in pkg)` is package-wide); (2) visibility NEVER blocks access — an explicit reach spelling (`import a::{ #hidden }`, `#` recommended) imports a private item, and reaching a DEPENDENCY's private item is no diagnostic at all (RULED: whether an item should be exported is subjective; the consumer may need it); visibility gates COMPLETION and the "import it first" steers; (3) a WARNING when an exported item exposes an unexported type in a signature position (RULED wording: "`S` is returned here. `my_fun` is exported, but `S` is not. A consumer can call `my_fun`, but are unable to name the return type.") with the quick fix "Export `S`"; (4) `export mod name { … }` + `import path::name::*` — the named prelude set (inline `mod` already parses and works; `::*` is the one new row, legal once exports are explicit); (5) IMPL IMPORTS get a spelling: `import a::b;` keeps today's meaning (names + every impl along the path — probed: an intermediate module's impls arrive too), `import a::b only;` brings names and no impls (RULED `only`), `import a::b::{ (impl List<i32>) }` brings the impls COMPATIBLE with that TYPE (a module's `impl List<type T>` arrives for `List<i32>` only — RULED type-level, blanket impls covered), `impl PATH` resolves in the IMPORTER's scope (`impl S` after `import item::Struct as S`; `impl item::Struct` qualified — RULED), and a specific method is `{ (impl List<i32>)::first }` / `{ (impl List<i32>)::{ first, last } }` (RULED: the selector is PARENTHESIZED — `{ (impl std::display::List<str>)::{ join } }` is unambiguous for the compiler and the reader — and the method path is the import's own path grammar); `_` is the selector's PLACEHOLDER (RULED): `{ (impl List<_>) }` selects every impl compatible with any `List` — against `impl List<type T>` it is any type, against `impl List<type T: MyTrait>` it stands for whatever the block admits — and no binders are written in a selector. Consequence for the paper: the per-type method namespace (names.md §4.6, one namespace, collisions refused at DECLARATION) becomes per-IMPORTER — the change that lets independent extension modules coexist, and the one with solver reach | design | three exchanges 2026-09-12 (owner → assessment → rulings); 'File this. We'll begin actual work tomorrow.' Today: everything is public (probed), `pub` is a curated parse error (parsing.rs:257 'a module's items are importable as they stand'), `export` on a declaration parses and means nothing, std writes `export` 11× (re-exports only) over 364 top-level items, `analyzer.warnings` exists. B317 (associated-function import) is the prelude set's other brick; E168/E169 (Organize Imports over-prune/over-keep) are the symptom of impls having no spelling — build them now on the `import a;` rewrite, re-point at the selector later |
| [B322](items/B322.md) | NEW — B311's latent halves: `Style::add` (`+`) also splits an inner slot key at `:` and runs `without_covered` against the mis-split property (benign today — the mis-split field is never a family shorthand name — and deliberately NOT fenced, since kolt merges such styles at views.vl:265–266); and `Style::media`'s `min_width` and `Style::raw`'s property are unfenced against `:` (unreachable through `sm/md/lg/xl` and the typed property methods) | bug | paper-style-34's finds (2026-09-13); style-conditions.md §5.1 — both dissolve under A95's build, where no condition is a string |
| [B333](items/B333.md) | NEW — an ANNOTATED binding whose initializer is a clause-returning CALL is refused (`let body: (\|\| void) context c = make();` → 'a `context`-typed binding takes a closure literal, or a value with the same `context` clause') while the unannotated `let body = make();` runs (B325): the call's `expr_type_ids` entry does not carry the clause — only the binding's inferred type does — so `clause_carried_by` cannot see it; and the mirror shape from slots-35: `let wire = \|\| { … };` (unannotated literal) does not carry the clause of the FIELD it later lands in, though an annotated `let` does — two faces of one gap: the clause is inferred from a type but never recorded on the expression | bug | solver-35's F1 + slots-35's F2 (2026-09-13); the annotated-call widening was tried and did not fire (reverted) |
| [B334](items/B334.md) | NEW — a METHOD NAME SHADOWS a same-named FREE FUNCTION inside its own `impl` block: `fun when(self, ..) { self.child(when(condition, body)) }` resolves `when` to the method ('`when` expects 3 arguments, but got 2'), and there is no qualified escape from inside std (`pkg::ui::when(..)` → '`pkg` is a namespace, not a value'; a self-import is a cycle) — the value forms' methods had to construct the struct literal inline instead of being the sugar the paper promised | design | slots-35's F1 (2026-09-13); a ruling: a self-qualified path inside a module, or a rule that a free function is reachable from an impl block by its bare name when the method form does not fit the arity |
| [B335](items/B335.md) | NEW — `source_of` on an import's MODULE segment lies when the module owns both a file and a directory: `pkg::x` with `x.vl` beside `x/y.vl` resolves its segment to the directory NAMESPACE, an entity minted in the IMPORTING file's id range, so `program.source_of(segment_id)` answers the importer, not `x.vl`; E168/E169's `import_module_is_used` makes the same lookup, so Organize Imports is probably wrong for such a module | bug | visibility-b-35's F1 (2026-09-13); worked around there by `ancestor_module_sources`/`module_source_by_name` |
| [B336](items/B336.md) | NEW — `export(in <general PATH>)` (`export(in pkg::a)`) parses, stores, formats and reprints, but the visibility test ADMITS it — the subtree test needs source PATHS, which live on `Program`, not the analyzer; only `mod` and `pkg` are enforced; zero estate occurrences | bug | visibility-a-35's F8 (2026-09-13); B318 S1 landed with the general form documented at the site |
| [B337](items/B337.md) | NEW — A67's message says 'its directory holds a `{name}.vl`' where a DIRECTORY-bodied child holds `{name}/` (`a/b/lib.vl`); and A67's rule does not reach a pure-NAMESPACE child: `a.vl` declaring `c` beside `a/c/x.vl` (no `a/c/lib.vl`) resolves `c` to the declaration with no ambiguity — the same silent winner A67 closed for a file child | bug | lang-35's F1 + F3 (2026-09-13); B331's pin exhibits the wording |
| [B338](items/B338.md) | NEW — an impl SELECTOR that admits NO implementation is silent (`import a::{ (impl Nothing) }` — almost certainly a typo); a bad selector only surfaces as the admission refusal at the first call; a second row would say so at the import | bug | visibility-b-35's OPEN Q3 (2026-09-13) |
| [B339](items/B339.md) | NEW — the macro-world derive corner after N79: a macro world registers no macros, so a `[derive(..)]` written into one of the eleven std modules a world force-loads (`boolean/list/null/promise/compare/default/debug/json/hash/number/string`) would be refused with the load-ordering text for a perfectly good std; reach EMPTY today (documented at `IN_MACRO_WORLD`) — a `!in_macro_world()` guard is one condition when wanted | design | hygiene-35's F2 (2026-09-13); no guard added because no branch could exercise it |

## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C1](items/C1.md) | `Weak<T>` | design | |
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |
| [C14](items/C14.md) | NEW — `SignalCell` (and `Owner`) off `Shared` boxes and onto `Rc` or OWNER-ATTACHED arena `Handle`s — the necessary step for native apps with native code and NO garbage collector (F1). Today `SignalCell<T> { value: Shared<T>, subscribers: Shared<List<Subscriber>>, identity }` and `Owner { cleanups: Shared<..>, disposed: Shared<bool> }`; `Shared` is a compiler primitive (`external struct`, `primitive_struct_ids` 'Shared', 8 analyzer sites) whose doc says 'reference-counted' but whose JS backing is a plain object box the HOST GC reclaims — nothing counts, cycles (cell ↔ subscriber closures capturing cells) are the GC's problem, and a disposed owner's cells stay readable forever. Neither property survives a native backend. Two candidate representations, to be DECIDED in a paper: (a) a real `Rc<T>` — counted box with deterministic drop (`destruction.md`, `drop.vl`), cycles broken by the owner's disposal or by `Weak` (C1, OPEN); (b) owner-attached `Arena` + `Handle` (std::arena already exists; the owner holds the arena, a cell is `Handle<Slot<T>>` + its owner, disposal frees the arena wholesale — no counting, no cycles, and a stale handle reads `None` instead of a ghost value, which is a BEHAVIOUR change from today for a cell that escapes its owner: module-level cells, `std::memo` caches, rpc mirrors, `Task` results). Blast radius: reactive.vl 47 `Shared` mentions, 13 std files use `Shared`, kolt 4 (store, lib/input_system, lib/overlay, lib/rotary) | design | the owner's ask (2026-09-14): 'change SignalCells from `Shared` boxes to `Rc` or arena `Handle`s (handles attached to owner). This is a necessary step to support native apps with native code (no garbage collector)'. Companion of F1; C1 (`Weak<T>`) is the other brick if (a) wins |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E69](items/E69.md) | NEW — attribute-NAME completion in an element head is a semantics decision | feature | |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E175](items/E175.md) | NEW — the css-block converter stops at the PACKAGE boundary: kolt's `button_style` converts the prefix its own file's `flex_row` opens and splits at `.script_label()`, declared in `theme.vl`; widening needs the analyzed impl table plus each declaring file's text, not one raw parse | editor | editor-35's F1 (2026-09-13); pinned as the limit (`refactor_does_not_reach_an_impl_style_extension_in_a_sibling_file`) |
| [E176](items/E176.md) | NEW — a PARENLESS head item has no attribute vocabulary in the TextMate grammar: `disabled` in `<input type("checkbox") disabled />` and the `hidden` of `hidden.show(flag)` paint as nothing; one more rule inside the head region, which must decline the tag name and a `{hole}` | editor | editor-35's F2 (2026-09-13) |
| [E177](items/E177.md) | NEW — `Document::QuickFix` is SINGLE-FILE, so the visibility warnings' 'Export `S`' / 'Export `hidden`' fixes ship as message steers only (the two same-file fixes — `Import as #leaf`, `Delete the #` — ship); a cross-file fix needs an optional target path + the other file's line index on `QuickFix` and a multi-file `WorkspaceEdit` | editor | visibility-a-35's F5 (2026-09-13) |
| [E178](items/E178.md) | NEW — vilan-ide's two completion filters (`OriginListing::completions`, `auto_import_completions`) do not consult the visibility bit — the third tooling consumer B318 S1 named, left because the file was another lane's; and `module_impl_blocks` / the selector member arm should filter unexported subjects once `export impl` is enforced | editor | visibility-a-35's F7 + visibility-b-35's S1 seam (2026-09-13) |
| [E179](items/E179.md) | NEW — the session summary line carries `memory:` and `programs=` but nothing about the base cache; `base_cache_worlds/weight/budget` belong beside them now that M67 bounds the cache (session_trace's pin text is E174's payload) | editor | dx-35's OPEN Q1 (2026-09-13) |
| [E180](items/E180.md) | NEW — Organize Imports BREAKS kolt's generated `src/lucide/lib.vl`: under `prelude = "std::web"` its two imports (`std::option::Option`, `std::ui::{ View, view }`) are redundant with the prelude (rule 0 — correct, the file checks clean with both deleted), but E168's module rescue then rewrites them to `import std::option;` + `import std::ui::{ (impl View) };`, and `import std::option;` binds the module name `option` over the file's own `fun option()` icon, so the organized file fails to check ('`option` is a module, not a value' at `Option::Some(option())`, lib.vl:17453). Two defects: (1) rule (2) `module_import_brings_a_use` counts every occurrence DECLARED in the module's file minus the file's own leaves, but not minus the PRELUDE's bindings and not minus what is reached by qualification through a bound head — `Option::Some`'s variants are option.vl declarations reached through the prelude-bound `Option`, so the module 'brings' them, `rescuing_subject` finds no impl member and answers `ModuleRescue::Module`; (2) a rescue INTRODUCES a binding (`import <module>;` binds `<module>`) with no check that the name is free in the file — the organizer must never write a statement whose new name collides with a declaration or another import; keep the statement verbatim (leaves unfaded) rather than break a green build | editor | the owner's report (2026-09-13): '`~/code/kolt/src/lucide/lib.vl` claims that `std::option::Option` and `std::ui::{ View, view }` aren't used even though they are extensively used throughout the file. Organize Imports prunes them, which causes errors in the file'. Reproduced with an LSP driver on 9b22ec36: three fades reading 'unused; the module's impls are in use — Organize Imports rewrites this to …', one edit over L9–L10; the organized copy fails `vilan check`; the copy with BOTH statements deleted checks clean. Kolt-side mitigation at the owner's word: `scripts/lucide.mjs` stops emitting the two imports (the prelude covers them) and lib.vl is regenerated or edited to match |
| [E181](items/E181.md) | NEW — `vilan fmt` gives the module-level `export *;` marker a canonical place: just below the file's imports — module comment, blank line, the import block, blank line, `export *;`, blank line, then the items. Today the printer keeps the marker wherever it was written (before the imports, in the middle of the file, glued to the next item without a blank line — probed on 9b22ec36: all four placements survive `vilan fmt` unchanged) because it prints statements in source order and only sorts imports WITHIN their run; B318 S1 introduced the marker and S6's estate sweep is about to write it into most of std, so the rule should land before the sweep prints hundreds of them | tooling | the owner's ask (2026-09-13): 'File an item to format a module level `export *;` just below the `import`s' with the shape `// Module comment` / blank / `import such_and_such;` / blank / `export *;`. Kolt's 19 migrated modules + lucide already sit in this shape by hand |

## F. Native targets & rendering — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [F1](items/F1.md) | NEW — DISCUSSION: building GPU-RENDERED NATIVE APPS in vilan. Today the build model is `Target` = `Backend` + `Platform` (platform-model.md, implemented 2026-06-23) with ONE backend (the JS emitter) and the browser/node/deno/bun platforms; `std::ui` has two layers behind one public surface (browser = live DOM, process = SSR string tree — the precedent for a third). A native app needs FOUR things the project has none of: (1) a native BACKEND — emit Rust (rustc does codegen; vilan's affine moves, borrows and `own` map onto ownership) vs C vs wasm-in-a-host vs an interpreter; (2) memory without a GC — C14 (cells on Rc/owner arenas), real counting for `Shared`, closure captures (`capture-clones.md`), strings/lists with owners, `destruction.md`'s drops actually running; (3) a RENDER LAYER — a third `std::ui` twin building a retained scene tree rendered by a GPU renderer (wgpu + vello-class 2D, text shaping, a flexbox-class layout engine such as taffy) with `Style` mapped onto it (A95 conditions: `hover()`/`active()` become hit-test state, `media`/`sm..xl` become window queries, the `css` block a native style resolver) and events/input/windowing (winit-class); (4) std's platform layers (`[library.layer]`, `PlatformPattern`) admitting a `native` platform and the coloring rules deciding what a native entry may reach (no `std::dom`, no `fetch` shaped as the browser's). Prior art to measure against: Dioxus/Blitz (HTML+CSS semantics, vello-rendered), Makepad, Slint, GPUI (Zed), Flutter (Impeller) | design | the owner's ask (2026-09-14): 'File an item to discuss support for building GPU-rendered native apps'. OWNER QUESTIONS before a paper: (1) which backend — emitting Rust is the shortest path to a real binary and reuses the ecosystem (wgpu, winit, taffy, cosmic-text) but makes rustc a toolchain dependency; (2) scope — desktop first (Windows/Linux/macOS), mobile later?; (3) is the UI surface the DOM-shaped `View` (so kolt's views compile unchanged onto the native layer, the SSR precedent) or a new retained-tree API?; (4) does 'native app' mean client-only, or client + server in one process (the rpc runtime in-process)?; (5) sequencing — C14 first (verifiable on JS), then a backend probe (hand-translate one small vilan program to Rust and list what the emitter must know), then the render layer |

## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |

## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | OPEN — an unchanged package module is re-analyzed every LSP analysis / HMR round; T1a–T1d landed (Order 30), next M55/M56/M57 | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M54](items/M54.md) | NEW — `LastUse`/`source_of` residuals from perf-30's re-pricing: the restored tables are CLONED into `LastUse::compute`'s walk state (8.9M Ir per analysis — needs `&mut Analyzer` or a read-through layer); `collect_unfollowable_loans` is 68% of the warm `LastUse` (whole-program by nature, a loan crosses modules — a record DESIGN); `Program::source_of` is still a linear scan of ~60 ranges and now the residual in `arrival_by` (`source_lookup` exists but is not the default); and `compute_shared_read_bindings` (B267) adds a second whole-program `collect_written_roots()` walk beside the capture pass's | performance | perf-30's and rule1-30's finds (2026-09-08) |
| [M55](items/M55.md) | NEW — module-level SHARING across the platform axis: M52's union key was refused because kolt's browser leg is 82% of all base time and no seed union crosses the platform; the node legs load 48 and 49 sources from disjoint seeds but the FILES overlap — sharing a module's analyzed world across legs (and platforms where coloring allows) is where kolt's remaining base-cache headroom is | performance | perf-30's M52 conclusion and open question 2 (2026-09-08); OWNER Q — M19's next tranche or its own item |
| [M56](items/M56.md) | NEW — R10's residue after T1d is the ENTRY's share of the sweep: 24,459 of 153,302 sites still walked, because the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole and ids minted after the world was stored fall in no range — removing it needs a source→entity index, §3.5's missing module boundary | performance | m19-t1d's find 2 (2026-09-08); the next tranche's shape |
| [M57](items/M57.md) | NEW — the corpus replay differential is BLIND to the R10 seam (and to T1b's): the golden corpus as modules declares almost no resource and none in a container, so a dropped-keys plant leaves `replay_differential` green — an R10 probe (a `List<Guard>` in every third module) would make the standing gate see it; it means editing a shared test function, which needs an owner's word or a lane that owns the file | performance | m19-t1d's find 1 and open question 1 (2026-09-08); OWNER Q |
| [M59](items/M59.md) | NEW — build-30's two follow-ups: persist the leg RECORD into `dist/` so a plain `build` gets the schedule's edges (and the artifact-edge guard's redo) from the second build on, not only under `--watch`; and give the HMR round's own compile loop (`run --watch`, ~main.rs:1340) the same tiering — it writes `dist/` after the whole loop, so the guard's shape differs | performance | build-30's open questions 1 and 2 (2026-09-08) |
| [M60](items/M60.md) | NEW — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update` | performance | keyed-31's find 2 (2026-09-09); rule 1's copy elision is the machinery |
| [M62](items/M62.md) | NEW — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it | perf | wire-32's find F5 and open Q1 (2026-09-11); the owner may prefer the six methods now — say so and it moves to A82's residue |
| [M66](items/M66.md) | NEW — every `SignalCell` pays `fresh_id()` for `.id` (A92's server dedup key), so twelve corpus goldens moved and every program mints an id per cell whether or not it is ever exported — a LAZILY-STAMPED identity intrinsic (`Shared::identity`: `o.__id ??= next++`) gives reference identity at zero cost until asked and zero golden churn | perf | rpc-33's find F2 (2026-09-11), unfiled at the sweep; filed at Order 34's GO (R9); not built this order — measure the per-cell cost on kolt's client first |
| [M70](items/M70.md) | NEW — SEVEN of kolt's 19 files can NEVER be served by the analyzer's base cache (`app_context, app_overlay, prefs, sidebar, store, styles, theme`): an import cycle reaches the entry's own module, so `entry_alias_module` is set and in `EntryMode::OpenFile` `entry_is_open_module` suppresses the pre-entry resolve AND the store — nothing else stores their key; each pays 0.42–1.43 s of CPU per analysis, per keystroke (paired probe: 0 hits in 7 tries with an unbounded cache). Dwarfs M67's budget question | perf | dx-35's F1 (2026-09-13); M55/M19's territory — the next perf order's top item |

## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N81](items/N81.md) | NEW — the two item-merge tools (`fold_tests_by_name.py`, `merge_items_by_name.py`) mis-split a MULTI-LINE STRING CONST whose first line ends in `"\` (a line continuation): the header lines were dropped inside a `use { }` block and the 32-line body vanished (visibility-a-35's merge, lexer errors at the gate's build); they also resurrect a test HEAD RENAMED under its old name and drop an EDITED existing const's changes (the fold carries new consts only) | hygiene | the integrator's hand repair f48ebd7e (2026-09-13) |
| [N82](items/N82.md) | NEW — the inference harness writes its `.mjs` runs into `std::env::temp_dir()`, and `/tmp` is a 12 GiB tmpfs shared by every lane: three `No space left on device` failures in one full run under nine lanes (also seen by slots-35), green on re-run — a spurious red indistinguishable from a real one | hygiene | solver-35's F2 + slots-35 (2026-09-13) |
| [N83](items/N83.md) | NEW — `vilan build` over a corpus program IN PLACE emits four asset artifacts into `vilan/test/` (`.vilan-bundled`, `icons/close.svg`, `logo.<hash>.svg`, `robots.txt`) that `git add -A` sweeps into a commit; none is gitignored | hygiene | solver-35's housekeeping note (2026-09-13); one commit was amended to drop them |
| [N84](items/N84.md) | NEW — two flakes under lane load: `keystroke::gate::the_entity_table_costs_the_edited_buffer_not_the_program` (32.2 s once, green after) and `rpc_http.rs::a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method` ('note err Waiting' — a lease answer read before the first lease failed); and `cargo test -p vilan-core --test inference -- modules::` fails two prelude-shaped tests that `cargo nextest` passes (threaded-process shared state) | hygiene | editor-35's F4, style-35's find, visibility-b-35's F2 (2026-09-13) |
| [N85](items/N85.md) | NEW — prelude.md §10.1's OTHER figure is stale too ('169 std-internal import statements … 46 (27%) would vanish'; std has 193 `pkg::` import statements at e4d192e3, visibility.md §6 says 208 intra-package) and cannot be re-derived without redoing the prelude census; css-block.md §15.3's three compact-styling exhibits no longer exist in kolt (`button_style_base` is gone; `default_panel_style` is an empty `const style::style()`), so §8's re-measure plan needs re-anchoring; and the interpreter's `Failure` messages (`asset::emit` outside a const, `check_emit_kind`'s two, `stage`/`staged`) are consistently UNROWED — check (3) enumerates only `Error { msg: <literal> }` | hygiene | hygiene-35's F1, style-35's finds (2026-09-13) |
