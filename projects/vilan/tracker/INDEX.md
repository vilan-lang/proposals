# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A72](items/A72.md) | NEW — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate` | feature | dom-30's finds 1 and 2 (2026-09-08) |
| [A73](items/A73.md) | NEW — std-30's two unshipped halves: `Storage` key ITERATION (today `len` + `key_at` by hand; a `keys(): List<str>` or an iterator) and `Debounce::flush()` (fire the pending callback now — a form that unloads before the window closes needs it) | feature | std-30's open questions (a) and (b) (2026-09-08) |
| [A76](items/A76.md) | NEW — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there | feature | reverse-31's find 2 (2026-09-09) |
| [A77](items/A77.md) | NEW — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel | feature | reverse-31's find 3 (2026-09-09) |
| [A81](items/A81.md) | NEW — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere | feature | reverse-31's finds 4 and open Q 2 (2026-09-09); transport-rpc §9.3's async-coloring paragraph is this item's |
| [A87](items/A87.md) | NEW — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it | design | reactive-32's open Q1 (2026-09-11); rec: add `release()` (run the cleanups, stay live) only when a caller appears — the pattern is a smell (a boundary that outlives its content is what fresh owners are for) |
| [A94](items/A94.md) | NEW — a FRAGMENT does not FLATTEN: `<><i/><>..</></>` is a `List<View>` inside a `List<View>` and is refused — A46's lowering is a literal, so nesting one fragment in another is a type error, not a run; pinned and documented as the limit; if flattening is wanted it is a lowering change (concatenate the inner list into the outer) with no type change | feature | slots-33's find F2 (2026-09-11); no exhibit asks for it yet — filed so the limit has an id |
| [A97](items/A97.md) | NEW — HOLD-COUNTING vs remote-sources §3: the server counts a client's leases per channel and revokes at zero (A92), so a RAW client that sends `Subscribe` twice and `Unsubscribe` once holds its channel open until disconnect; the alternative — client-side demand coordination that never double-subscribes — is plumbing the generated client already does and a raw client would have to repeat. Rule whether the wire contract is 'one Unsubscribe per Subscribe' (documented, as built) or the server forgives | design | rpc-33's find F3 and open Q (4) (2026-09-11), unfiled at the sweep; filed at Order 34's GO (R9); remote-sources §3 was AMENDED at Order 33's sweep to say what the server SENDS holds, not what it relies on — this item is the client-side half |
| [A104](items/A104.md) | NEW — `std::json` has no `Json`/`FromJson` for `Result<T, E>`: `Option` has both (json.vl:23 and :264) and kolt hand-writes the pair at shared.vl:65/80 as a `[kind, value]` array (0 = Ok, 1 = Err); the WIRE twin landed with A82 (variant-name tags) — the JSON twin should match `Option`'s spelling and the wire tags, not kolt's integers | feature | kolt sweep at Order 37 GO (2026-09-17); the `// FIXME: Implement with std.` pair at shared.vl:65/80; S |
| [A105](items/A105.md) | NEW — `View::bind_attr` takes `Source<str>` only (browser/ui.vl:259); a `Source<Option<str>>` whose `None` REMOVES the attribute has no std form, so kolt writes `bind_attr_proper` (styles.vl:35: `Some` → `set_attribute`, `None` → `remove_attribute`) and calls std's 'poorly written'; the element syntax admits an `Option` attribute since A89 — the method should match | feature | kolt sweep at Order 37 GO (2026-09-17); S |
| [A106](items/A106.md) | NEW — the style vocabulary is ambient only inside a css HOLE (`std::style::prelude`, names.md §4.7's one-position rule): a `fun` body building a chain imports nine names per function (kolt styles.vl:54/103 — `style as s`, `Length`, `Cursor`, `TextAlign`, `AlignItems`, `active`, `attribute`, `hover`, `pseudo` — under a `// FIXME: Add a prelude to std.`). Doors: (1) document `import std::style::prelude::*;` at file level as the idiom (the module is ordinary outside a hole — but the TYPES `Length`/`Cursor`/`TextAlign`/`AlignItems` are not in it); (2) the `std::web` prelude `export import`s a widened style prelude — ambient in every web file (E180's R9 rule fades it cleanly), gated on a name-collision census over the estate; (3) nothing. Rec: widen the prelude with the types (S), document (1) now, take (2) after the census | design | kolt sweep at Order 37 GO (2026-09-17); OWNER Q (door 2) |
| [A107](items/A107.md) | NEW — `[rpc]` RETURN types kolt could not write: `void` ('not supported as an rpc return type', store.vl:182 — `remove_message` returns `bool` instead) and `Result<i53, str>` (store.vl:166's FIXME predates A82, which BUILT `Result` as an rpc return — VERIFY on d783fbf4 and retire the FIXME if it compiles); a `void` rpc is a call whose reply is an empty ack (distinct from `notify`, which has no reply) | feature | kolt sweep at Order 37 GO (2026-09-17); Order 38 candidate; S–M |
| [A108](items/A108.md) | NEW — BREAKING candidate: the Wire visitor's BY-VALUE receivers force eleven interior-mutability cells — `Serialize`/`Deserialize` declare every method on a bare `self` and `Wire::describe<S: Serialize>(self, serializer: S)` takes `S` by value, so `JsonWriter`/`JsonReader` (json.vl, 6 cells) and `BinaryWriter`/`BinaryReader` (binary.vl, 4) keep their state in `Shared` boxes a `mut` local cannot replace; C14 S1 retired 8 of the census's 19 frame-scoped cells and stopped here. The replacement shape COMPILES today (native-a-37's probe: `&mut self` trait methods, a generic `tell<S: Sink>(self, sink: &mut S)` forwarding the view down a recursive `List<T>` impl, a closure-record erasure over a `mut` local — zero `__shared_new`); blast radius 17 std `impl … with Wire` + the derive emitters in json.vl + 3 fixtures in `inference/generics.rs` + docs prose + kolt's derives | design | native-a-37's FIND-1 (2026-09-17); Order 38 breaking candidate, `wire.vl` in the lane's ownership; retires the whole json/binary frame-scoped residue |
| [A109](items/A109.md) | NEW — `Result::expect_err` is NOT retrofitted to `lazy` while `Result::expect`, `Result::unwrap_or`, `Option::expect` (new) and `Option::unwrap_or` are (A103, Order 37): the same position with the same argument shape, now asymmetric — a trap; the differential shows zero estate call sites, so it is free | feature | lazy-37's FIND 1 (2026-09-17); one line of std + a pin; Order 38 |
| [A110](items/A110.md) | NEW — NESTED FORMS ON ONE SOURCE settle child-before-parent: `drain` runs a wave in SUBSCRIPTION order, so an inner `swap`/`when`/`each` fires with a value its enclosing form is about to exclude; and OUTSIDE a turn the inline notify walks a SNAPSHOT, so an observer whose owner was disposed earlier in the same notify STILL FIRES — a `swap` then builds under a fresh owner nothing disposes, into a closed region (MEASURED on 0fa109eb7, probe in the item) | bug + design | the nested-`swap` conversation (2026-09-19); door 1 (liveness flag, S) is a leak fix — Order 38 candidate; door 2 (derivations to fixpoint, then effects in ascending id = parent-first) wants a `reactive-turns.md` section and a ruling on whether effect order within a wave is contract; kolt `page_content` carries the latch workaround meanwhile |
| [A111](items/A111.md) | NEW — KEEP-ALIVE for `swap` (`swap_cached`): park a swapped-out subtree (row AND owner) per key and re-insert on return — `cut_row`/`insert_row` already do the DOM half; missing is `Owner` SUSPEND/RESUME, without which a parked subtree stays subscribed (detached repaints, remote leases never release, a parked nested `swap` keeps rebuilding); `std::memo` cannot do it from app code (the maker runs under the per-instantiation owner `swap` disposes) | design (paper) | the nested-`swap` conversation (2026-09-19); NOT queued — no measured need; design with A87 (`Owner::release`), this would be its caller; cap/eviction is not optional |
| [A112](items/A112.md) | NEW — INCREMENTAL COLLECTIONS: a collection-valued signal reruns its derivation for EVERY element when one changes; the formal shape is a change structure — an op vocabulary per collection SHAPE (`Splice`/`SetAt`/`Reset`), a delta-source trait (op log + cursors — `KeyedCell`'s A54 machinery lifted out of rpc), and operators written ONCE as derivatives (`map_each` = 1 call of `g` per insert; group folds O(1), monoid folds O(log N)); `each` becomes a delta consumer. The receiver question is PROBED: `self`-receiver defaults over one `splice` primitive give `my_list.push(x)` on a `let` cell (3 pushes → 3 runs of `g`, a remove → 0), and a `&mut self` twin serves `cell.edit(..)` (4 mutations → 1 notification) | design (paper) + feature | the owner's `map_collection` question (2026-09-21); paper → S1 lift log/cursor → S2 `ListCell` + `map_each` + the LAW pin → S3 delta-driven `each`; probe B found B359 (does not block) |

## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B298](items/B298.md) | NEW — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's | design | smalls-32's find F2 and open Q1 (2026-09-11); rec: yes for a bounded `_`, behind the Q3 ruling |
| [B348](items/B348.md) | NEW — a GENERIC `Callable` does not coerce into a closure-typed position (by design: `impl Box<type T> with Callable` writes `call` in the impl's own binders — fn-coercion rule 2's reason); `held()` calls, `let f: \|…\| T = held;` is the ordinary mismatch; needs the expected-type-driven instantiation rule 2 is waiting on | design | lang-b-36's FIND 3 (2026-09-14); design, on demand |
| [B354](items/B354.md) | NEW — HIGH: a `[derive(..)]`-SYNTHESIZED generic body resolves its members under the SENTINEL source (`current_admitting_file` → `source_of` → `DERIVED_SOURCE`), not the file the derive was written in — so B318 S4's export gate hides every non-exported impl from every derived visitor. This is WHY E185's placeholder message fired sixteen times at Order 36's sweep merge and why std had to export its codec blocks (`JsonWriter`/`JsonReader`/`BinaryReader`) that it had kept private. E185 fixed the NAME in the message (diagnostics-37); the resolution rule is this item. Reproducible: an exported `Counter` + a non-exported `impl Counter with Serialize` in `w.vl`, `[derive(Wire)]` in `thing.vl`, `.describe(counter)` in a third file | bug | diagnostics-37's FIND 1 (2026-09-17); HIGH — a hidden rule the std curation paid for |
| [B355](items/B355.md) | NEW — E189's BROAD GATE (drop the `context`-family diagnostics when the list already carries a resolution/type error, with a note) is buildable only after `b279_an_unresolvable_dispatch_site_still_fences_its_candidates` gets a NON-DIAGNOSTIC observation of the fence: that pin observes a SOUNDNESS property (the refined-dispatch fallback widening to the whole candidate list — without it the program compiles and prints `undefined`) only THROUGH the coverage fence, in a program that carries another diagnostic by construction. diagnostics-37 built the gate, measured it (kolt's `href(href())` shape 139 → 1), and removed it because it blinds that pin (AGENTS.md's stop condition). The narrow rule landed; E191's remaining 138 wait on this | feature | diagnostics-37's OPEN Q1 (2026-09-17), the recommended third option; OWNER RULING (gate or no gate) recorded here |
| [B356](items/B356.md) | NEW — explicit TYPE ARGUMENTS on an associated-function path were INERT for a TRAIT subject: `Signal<i32>::new("x")` compiled at d783fbf4 because only `Struct`/`Enum` subjects seeded `static_subject_bindings` (fixed for the static path by solver-37's `seed_trait_static_subject_bindings`, B352); whether EVERY path shape checks its written arguments deserves a sweep — B323's family ('an annotation-only generic argument is silently inert') | bug | solver-37's FIND 1 (2026-09-17); a census, then pins per shape |
| [B357](items/B357.md) | NEW — a PAYLOAD-LESS variant types as the BARE enum with an EMPTY argument list (`None` is `Option` with no arguments — no hole to test for), so it unifies with every instantiation and carries nothing: `Box<Option<str>>::new(None)` became `Box<Option>` until B352's `binding_is_weaker` special-cased the shape. The root fix — infer `None`'s (any nullary variant's) type against the constraint it lands in, with a real hole — is larger than the special case and is this item | bug | solver-37's FIND 2 (2026-09-17); the special case stays until this lands |
| [B359](items/B359.md) | NEW — MISCOMPILE + ruling: a trait DEFAULT body's `self.member(..)` is HIJACKED by the implementing type's INHERENT member of the same name (`push_twice`'s `self.push` calls `Bag`'s own `push`, not the trait's); when that inherent is `external` (`impl List<type T> with Pusher<T> {}`) the emit calls a function never emitted — `ReferenceError: $b is not defined`, clean at check; the generic-bound route reaches the TRAIT's member, so the two routes disagree | bug (miscompile) | A112's receiver probe B (2026-09-21), 0fa109eb7; RULING: (a) trait member always inside a default body (rec — Rust's rule, what the bound route does) or (b) inherent wins + fix the external emit; Order 38 candidate |

## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |
| [C14](items/C14.md) | NEW — `SignalCell` (and `Owner`) off `Shared` boxes and onto `Rc` or OWNER-ATTACHED arena `Handle`s — the necessary step for native apps with native code and NO garbage collector (F1). Today `SignalCell<T> { value: Shared<T>, subscribers: Shared<List<Subscriber>>, identity }` and `Owner { cleanups: Shared<..>, disposed: Shared<bool> }`; `Shared` is a compiler primitive (`external struct`, `primitive_struct_ids` 'Shared', 8 analyzer sites) whose doc says 'reference-counted' but whose JS backing is a plain object box the HOST GC reclaims — nothing counts, cycles (cell ↔ subscriber closures capturing cells) are the GC's problem, and a disposed owner's cells stay readable forever. Neither property survives a native backend. Two candidate representations, to be DECIDED in a paper: (a) a real `Rc<T>` — counted box with deterministic drop (`destruction.md`, `drop.vl`), cycles broken by the owner's disposal or by `Weak` (C1, OPEN); (b) owner-attached `Arena` + `Handle` (std::arena already exists; the owner holds the arena, a cell is `Handle<Slot<T>>` + its owner, disposal frees the arena wholesale — no counting, no cycles, and a stale handle reads `None` instead of a ghost value, which is a BEHAVIOUR change from today for a cell that escapes its owner: module-level cells, `std::memo` caches, rpc mirrors, `Task` results). Blast radius: reactive.vl 47 `Shared` mentions, 13 std files use `Shared`, kolt 4 (store, lib/input_system, lib/overlay, lib/rotary) | design | the owner's ask (2026-09-14): 'change SignalCells from `Shared` boxes to `Rc` or arena `Handle`s (handles attached to owner). This is a necessary step to support native apps with native code (no garbage collector)'. Companion of F1; C1 (`Weak<T>`) is the other brick if (a) wins |
| [C15](items/C15.md) | NEW — spec §6.9's closure rule ('captures BINDINGS, not values') makes every mutably-captured binding a SHARED CELL on a native backend: the F1 probe (R-2) had to box `let mut count` as `Rc<RefCell<_>>` because two closures assign it — on JS the host boxes the place for free; natively the compiler must decide per binding whether it is captured by value (copied), by counted reference, or refused. Larger than `Shared` (C14) — no item existed | design | paper-native-36's FIND-4 (2026-09-14; native-apps.md §2.4 R-2); rule with C14's S4 and F1's S1 |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |
| [D9](items/D9.md) | NEW — editing-dx.md owes one sentence Order 37 proved twice: because the unused-import FADE and Organize Imports' PRUNE are one predicate (E114), every false positive in the fade is a code action that BREAKS a green file — E192 (a type import used only by a selector subject) would have deleted a line kolt's views.vl needs, and E168 before it; plus the §18 as-built paragraphs for E69 (the generated attribute table, its two shaping rules) and E193 (E160 already shipped the initializer; the head now resolves through the scope chain) | documentation | editor-37's FIND 3 + hand-backs (2026-09-17); the integrator's, at the sweep |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E182](items/E182.md) | NEW — `infer_platform` (vilan-core lib.rs:93) is BINARY browser/not-browser: a third `std::ui` twin makes the editor report the PROCESS twin's `View` against native source — the E113 shape its own doc comment describes; latent until F1's S2 adds the `native` platform | bug | paper-native-36's NEW-4 (2026-09-14; native-apps.md §3, §8) |
| [E191](items/E191.md) | NEW — a diagnostics CASCADE from one arity error in an element head: kolt login.vl:181/286 — `href(href())` (the attribute call given `href()` with its `Route` argument missing) 'caused a ton of error noise'; one wrong call inside `<a href(..) on:click(..) .styled(..)>` should report ONE error at the call, not fan out through the head's desugar (the head lowers to a chain; a failed argument may leave the chain's type unresolved and every later link reports) | diagnostics | kolt sweep at Order 37 GO (2026-09-17); lane diagnostics-37 beside E189 |
| [E194](items/E194.md) | NEW — a HYPHENATED completion label is filtered out by the client's default word pattern: `editors/vscode/language-configuration.json` sets no `wordPattern`, so VS Code's default excludes `-` — at `<svg stroke-w\|` the client's word is `w` and `stroke-width` does not match; `Completion` carries no `filter_text` and the server sends no `textEdit` range. Pre-existing since E153's css property vocabulary (`flex-` → `flex-direction`, whose pin asserts only the SERVER's list); E69's attribute table makes it visible at every element head | editor | editor-37's FIND 1 (2026-09-17); one fix serves both vocabularies; S–M |
| [E195](items/E195.md) | NEW — `Analysis::nominal_id_by_name` (vilan-ide completion.rs) still scans the program for the FIRST name match — the shape E193 fixed for the struct-initializer head (which now resolves through the scope chain and, qualified, through the namespace); its callers (`element_view_nominal_id`'s neighbours, receiver typing) answer the wrong struct in the same two-module fixture (a sibling's twin `Dot` beside this file's own) | editor | editor-37's FIND 4 (2026-09-17); S |
| [E196](items/E196.md) | NEW — the HMR overlay still places a TRANSFORMER refusal at the entry: E190 made the CLI read the `SourceId` an `Error`'s note carries (`compile_to_js`'s transform-`Err` arm), but `hmr::OverlayDiagnostic::located(&filename, source_ref, …)` one line above still uses the entry's filename — the overlay shows the B55 refusal against the wrong file | editor | diagnostics-37's FIND 3 (2026-09-17); one line |
| [E197](items/E197.md) | NEW — format-on-save cannot say it DECLINED: `vilan-lsp/src/main.rs` (`formatting`) and `vilan-wasm/src/lib.rs` (`format_program`) both call `formatter::format`, which answers the original bytes on every way out, so a file the printer cannot render returns 'no edit' — indistinguishable from an already-canonical file. N90 closed exactly this silence for the CLI (`reprint` returns a `Decline` with reason, construct and line; `vilan fmt` exits 2 and names it) | editor | dx-37's FIND 3 (2026-09-17); S |
| [E198](items/E198.md) | NEW — the wasm PLAYGROUND's `Program::std_sources` is EMPTY, which is why its A99 steer suffix never appears (`View has no method 'swap'` bare, the N88 check): the playground registers the whole embedded toolchain through the DOCUMENT OVERLAY (vilan-wasm lib.rs ~321–329) and the analyzer records a std source only when `matches!(origin, Origin::Std) && !document_overlay_contains(&module_path)` — `std_sources` conflates 'this source IS std' (what the steer keys on) with 'its definition-site diagnostics are FROZEN' (why the overlay is excluded). Not a one-line fix: vilan-wasm `tests/compile.rs:168` records that filling it naively makes a split-wired playground chunk std | bug | hygiene-37's FIND 4 (2026-09-17); K14's neighbour; M |
| [E199](items/E199.md) | NEW — a refused generic-bound message can name the WRONG `T`: in solver-37's B352 probe a wrong render body was refused as `Swap<str, StorageSignalCell<bool>, View>` does not implement `Slot` — `T` taken from the closure BODY rather than from the SOURCE argument, so the message names a type the author never wrote; the refusal is right, its sentence is not | diagnostics | solver-37's FIND 5 (2026-09-17); S |
| [E200](items/E200.md) | NEW — a DOUBLE report for one mistake, pre-existing and not widened by B344: a lazy argument reaching a resource through a FIELD of a resource binding (`hold(flag, holder.conn)`, and the same through a `&Holder` loan) gets both R9's capture message (naming the binding) and the lazy-argument message (naming the field's type, ledger row 475) — two diagnostics for one shape; closing it means deciding which owns it | diagnostics | lazy-37's FIND 3 (2026-09-17); S |
| [E201](items/E201.md) | NEW — A101's breaking diagnostic (`A_CSS_DECLARATION_IS_A_CALL`, the flagship row at the `:`) carries NO quick fix, although the single-argument and plain-text cases are mechanical — exactly the codemod's own rules (`property: value;` → `property(value);`, typed where E167's converter types, string otherwise); it is the message every migrating program hits | editor | css-37's FIND 4 (2026-09-17); S–M; reuse the codemod's rules server-side |

## F. Native targets & rendering — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [F1](items/F1.md) | NEW — DISCUSSION: building GPU-RENDERED NATIVE APPS in vilan. Today the build model is `Target` = `Backend` + `Platform` (platform-model.md, implemented 2026-06-23) with ONE backend (the JS emitter) and the browser/node/deno/bun platforms; `std::ui` has two layers behind one public surface (browser = live DOM, process = SSR string tree — the precedent for a third). A native app needs FOUR things the project has none of: (1) a native BACKEND — emit Rust (rustc does codegen; vilan's affine moves, borrows and `own` map onto ownership) vs C vs wasm-in-a-host vs an interpreter; (2) memory without a GC — C14 (cells on Rc/owner arenas), real counting for `Shared`, closure captures (`capture-clones.md`), strings/lists with owners, `destruction.md`'s drops actually running; (3) a RENDER LAYER — a third `std::ui` twin building a retained scene tree rendered by a GPU renderer (wgpu + vello-class 2D, text shaping, a flexbox-class layout engine such as taffy) with `Style` mapped onto it (A95 conditions: `hover()`/`active()` become hit-test state, `media`/`sm..xl` become window queries, the `css` block a native style resolver) and events/input/windowing (winit-class); (4) std's platform layers (`[library.layer]`, `PlatformPattern`) admitting a `native` platform and the coloring rules deciding what a native entry may reach (no `std::dom`, no `fetch` shaped as the browser's). Prior art to measure against: Dioxus/Blitz (HTML+CSS semantics, vello-rendered), Makepad, Slint, GPUI (Zed), Flutter (Impeller) | design | the owner's ask (2026-09-14): 'File an item to discuss support for building GPU-rendered native apps'. OWNER QUESTIONS before a paper: (1) which backend — emitting Rust is the shortest path to a real binary and reuses the ecosystem (wgpu, winit, taffy, cosmic-text) but makes rustc a toolchain dependency; (2) scope — desktop first (Windows/Linux/macOS), mobile later?; (3) is the UI surface the DOM-shaped `View` (so kolt's views compile unchanged onto the native layer, the SSR precedent) or a new retained-tree API?; (4) does 'native app' mean client-only, or client + server in one process (the rpc runtime in-process)?; (5) sequencing — C14 first (verifiable on JS), then a backend probe (hand-translate one small vilan program to Rust and list what the emitter must know), then the render layer |
| [F15](items/F15.md) | NEW — the THREE-LAYER `std::ui` rule and an N-way twin-parity gate: today two `ui.vl` twins (browser DOM, process SSR) share one public surface and the parity gate is structurally TWO-valued; a native twin makes it three, and nothing today states the rule that every twin exposes the same surface (`ssr.md`'s 'same public surface' is a sentence, not a gate). F1's S2 cannot be sized without the rule | design | paper-native-36's NEW-3 (2026-09-14; native-apps.md §6.2, §8 — 'should be ruled with this paper'); OWNER RULING wanted |
| [F17](items/F17.md) | NEW — DISCUSSION (the owner's, 2026-09-17): how to write UI code ONCE so it works for native and web simultaneously — with named exceptions (native window chrome: close/minimize/maximize buttons, and whatever else the platforms genuinely do not share). The F1 paper's answer (the DOM-shaped `View` as a third twin, Order 37's R2) is the STARTING POSITION, not the conclusion; the conversation decides the shape of the shared surface, the exception mechanism (platform-colored members? a `native`-only module? separate chrome components?), and what the three-layer rule (F15, R4 recorded) has to gate. Nothing in F1 S3–S5 starts before it | design | the owner's conversation, 2026-09-17; NOT queued — a conversation, then a paper section |
| [F18](items/F18.md) | NEW — native WEB SERVERS as the first native product after CLI programs (the owner, 2026-09-17: 'more easily achievable in the short term' and 'a huge win in and of itself'): the `@process` family on the Rust backend — `std::fs`, `std::http`, `std::db`, `process/rpc_server.vl`, the service layer — which REVISITS native-apps.md §8 Q2 (`native` does not join `@process` initially): for a server it must, and `check_library_contract`'s base-layer obligation is the cost to size; J6's executor is the precondition (a server is async through and through); the exit test is kolt's server leg built natively and serving its client | feature | the owner's priority, 2026-09-17; Order 38's first native slice; sized by native-b-37's report |
| [F19](items/F19.md) | NEW — `vilan-rt` is NOT REACHABLE from a RELEASED binary: `native.rs::runtime_crate()` resolves `$VILAN_RT`, else this crate's SOURCE sibling — a released toolchain has neither, so `--backend rust` works only from a source checkout. Embed the runtime crate the way `vilan-embedded-std` embeds std (materialize it beside the std cache; the emitted `Cargo.toml` points at the materialized path) | feature | native-b-37's FIND 1 (2026-09-17); S1b-sized; BLOCKS any non-from-source use of the backend |

## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |
| [I4](items/I4.md) | NEW — `Default` is implemented for the numeric family only (number.vl): `Map`, `List`, `Set` (and `str`, `bool`, `Option<T>` — verify each) have no `Default`, so a `T: Default` bound cannot be met by a container and kolt writes `impl Map<type K, type V> with Default` itself (prefs.vl:100 under `// FIXME: Implement with std.`) | feature | kolt sweep at Order 37 GO (2026-09-17); S |

## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |
| [J6](items/J6.md) | NEW — ASYNC on a native backend: `Task`/`Nursery`/`await` need an executor and a cancellation story where JS supplied the event loop; the F1 paper's largest item F1 did not list — `Task` is an `external struct` over the host promise (task.vl:18), so the native runtime must provide the promise, the microtask queue and the turn model (reactive-turns.md) before any `async fun` compiles natively | design | paper-native-36's NEW-1 (2026-09-14; native-apps.md §2.3, §8: single-threaded executor recommended, `Task` stays `external`) |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |
| [L22](items/L22.md) | NEW — `publish-brew` mints its tap token through `app-id`, an input `actions/create-github-app-token` deprecated at v3.1.0 in favour of `client-id`: the v2 → v3 bump must switch inputs in the same change, and the App's client ID is a different value from the one `TAP_APP_ID` holds | process | the review of vilan-lang/vilan#3 (2026-09-18); needs one secret from the owner |
| [L23](items/L23.md) | NEW — `release.yml` is exercised by nothing but a real `v*` tag, so a bump to the actions it alone uses is first RUN mid-release: it needs a dry-run lane — build → upload → download → checksum, no publishing step — before upload-artifact v7 / download-artifact v8 land | process | the review of vilan-lang/vilan#3 (2026-09-18) |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | OPEN — an unchanged package module is re-analyzed every LSP analysis / HMR round; T1a–T1d landed (Order 30), next M55/M56/M57 | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M54](items/M54.md) | NEW — `LastUse`/`source_of` residuals from perf-30's re-pricing: the restored tables are CLONED into `LastUse::compute`'s walk state (8.9M Ir per analysis — needs `&mut Analyzer` or a read-through layer); `collect_unfollowable_loans` is 68% of the warm `LastUse` (whole-program by nature, a loan crosses modules — a record DESIGN); `Program::source_of` is still a linear scan of ~60 ranges and now the residual in `arrival_by` (`source_lookup` exists but is not the default); and `compute_shared_read_bindings` (B267) adds a second whole-program `collect_written_roots()` walk beside the capture pass's | performance | perf-30's and rule1-30's finds (2026-09-08) |
| [M56](items/M56.md) | NEW — R10's residue after T1d is the ENTRY's share of the sweep: 24,459 of 153,302 sites still walked, because the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole and ids minted after the world was stored fall in no range — removing it needs a source→entity index, §3.5's missing module boundary | performance | m19-t1d's find 2 (2026-09-08); the next tranche's shape |
| [M57](items/M57.md) | NEW — the corpus replay differential is BLIND to the R10 seam (and to T1b's): the golden corpus as modules declares almost no resource and none in a container, so a dropped-keys plant leaves `replay_differential` green — an R10 probe (a `List<Guard>` in every third module) would make the standing gate see it; it means editing a shared test function, which needs an owner's word or a lane that owns the file | performance | m19-t1d's find 1 and open question 1 (2026-09-08); OWNER Q |
| [M59](items/M59.md) | NEW — build-30's two follow-ups: persist the leg RECORD into `dist/` so a plain `build` gets the schedule's edges (and the artifact-edge guard's redo) from the second build on, not only under `--watch`; and give the HMR round's own compile loop (`run --watch`, ~main.rs:1340) the same tiering — it writes `dist/` after the whole loop, so the guard's shape differs | performance | build-30's open questions 1 and 2 (2026-09-08) |
| [M60](items/M60.md) | NEW — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update` | performance | keyed-31's find 2 (2026-09-09); rule 1's copy elision is the machinery |
| [M62](items/M62.md) | NEW — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it | perf | wire-32's find F5 and open Q1 (2026-09-11); the owner may prefer the six methods now — say so and it moves to A82's residue |
| [M73](items/M73.md) | NEW — an open module's world is stored UNRESOLVED, so ~150 ms of `resolve_world` is paid on every keystroke in M70's six kolt files (theme.vl build 161 ms vs views.vl 11 ms with the same checks phase); the naive import-fixpoint split moved 0.4 ms of it; a fix has to defer only the ALIAS-reaching work rather than the whole pre-entry pass — scope-chain territory, size after B318 S4 (landed) | perf | perf-36's FIND 2 (2026-09-14) |
| [M76](items/M76.md) | NEW — the CHECKS phase, not the resolve, is the largest remaining per-keystroke cost on M70's six kolt files: theme.vl at loadavg 40 reads `load+walk 460.7 / base 0.0 / build 632.4 / checks 1,103.2` ms — M19's per-module checks record is deliberately WITHHELD from an entry-shaped world (`analyze_inner`'s store site says why: module checks through the alias may depend on the entry), and `[vilan phase] reused 0/69` on every one of the six confirms nothing is reused; worth MORE than M73 (checks are 1.7× the resolve) | perf | perf-37's F-A (2026-09-17); M70's own recorded follow-up; E121's lane |
| [M77](items/M77.md) | NEW — `statement_sources` runs for EVERY import statement program-wide the moment ONE collision is banked (`build_impl_admission`, the `if !collisions.is_empty()` arm): one linear pass, not a product, so M75's indexes leave it — but it is what remains of the large leg's 60 ms and it scales with the whole program's statements; an index keyed on the statement's path spans removes it | perf | perf-37's F-B (2026-09-17); small; only reachable by a program that already has a collision |
| [M78](items/M78.md) | NEW — the `[vilan phase]` marks are WALL time (`PhaseClock` is `Instant`), so every perf item that reads the phase line is reading wall — under load the numbers are shares, never absolutes (M73's stage split had to be reported as percentages). `keystroke::gate::process_cpu_now` already has the `CLOCK_PROCESS_CPUTIME_ID` reader for the LSP; a CPU-clocked phase line would let E121's ledger take absolute figures on a loaded box | perf | perf-37's F-C (2026-09-17); cheap; blocks nothing |
| [M79](items/M79.md) | NEW — M73's BUILD, paper first: an entry-open module's world is stored UNRESOLVED and `resolve_world` is paid on every keystroke (theme.vl 133–145 ms; the constraint FIXPOINT is 79–96 % of the pass on all nine measured files — preludes/use-drain read 0.0 ms, imports 0.4 ms, correcting M70's attribution); a RESOLVED world already runs `resolve_world` TWICE (pre-entry and inside the post-entry `build()`) and the second run costs 1.2–8.9 % of the first, so running it twice is proven safe — the whole prize is moving the FIRST run in front of the store. Design (perf-37's Q1): resolve pre-entry and store the partially-resolved world, letting the post-entry second call finish it; the deferral set is per-FILE, seeded by files carrying `import pkg::<entry-alias>` and closed transitively under 'imports from a deferred file' (kolt: 9 of 26 package files for every one of the six, leaving ~60 of ~69 sources resolvable before the store), with a fallback to today's wholesale deferral if the package PRELUDE module lands in the set; the cost is that eleven queues drained inside `resolve_world` must hold their deferred sources back — B222/B236/B237 live in exactly that machinery | perf | perf-37's OPEN Q1 (2026-09-17): out of proportion to an M slot; its own item, paper first; M73 re-pointed here |
| [M80](items/M80.md) | NEW — `push_or_share`'s body-sharing key does not normalize LOCAL GENSYMS: `canonical_instance_body` normalizes only the instance's own name, so two monomorphizations whose bodies differ only in gensym numbering stop sharing — C14 S3 gave `observe` two compiler-minted temporaries and `observe` is now emitted TWICE in `reactive-flatten.mjs` and `reactive-on-change.mjs` where it was once (`$D`/`$H` identical modulo `$E$F` vs `$I$J`); +2,857 bytes over the corpus goldens, ~+640 each in those two | perf | native-a-37's FIND-4 (2026-09-17); S |
| [M81](items/M81.md) | NEW — every `unwrap_or(<literal>)` now allocates a memo cell: 111 of A103's 114 `__lazy` thunk emissions carry an INERT argument (`0`, `-1`, `"?"`, `false`, `[]`, an enum constant); an 'inert argument stays eager' elision in `record_lazy_arguments` would keep most of the 16 moved corpus goldens byte-identical, remove the allocation and the `__force` on the hot path, and is unobservable by construction (a literal has no effects) | perf | lazy-37's FIND 2 (2026-09-17); S–M; the 36-program JS delta is the evidence |

## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N98](items/N98.md) | NEW — N94's class SURVIVES outside the rowed surface: swallowed `\`-continuations (a run of ~15–18 spaces mid-sentence) at `vilan-core/src/macros.rs:1535` (a clippy `#[allow(.., reason = "…")]`), `vilan-lsp/src/document.rs:18661` (an assertion), `vilan-core/src/formatter.rs:13388` (a test program const) and ~15 `.vl` program consts in test files (`module_resolution.rs` 1344/5541/5626/5768/5791, `check_scope_differential.rs` 826/833, `hmr.rs` 558, `document.rs` 9312–9434, `inference/generics.rs` 1603, `build_hooks.rs` 1262, `grammar_ebnf.rs` 137/141/145/168); N94's gate covers only what the ledger enumerates | hygiene | hygiene-37's FIND 1 (2026-09-17) |
| [N99](items/N99.md) | NEW — two user-visible messages the ledger's widened walk still cannot reach: (1) `asset::staged`'s registry refusal — `const_eval.rs`'s helpers return `Err(String)` and are re-wrapped at `Err(message) => Err(Failure::new(.., message))`, a variable not a literal; (2) the derive LOAD-ORDERING message (`[derive({name})]` expanded before std's `{module}` declared its `{name}` macro: a compiler load-ordering bug (B21's class); please report how this module is reached`) lives in a `let msg = if … else …` ladder handed over by field shorthand — its sibling arm (row 434) and both `[service]` arms (rows 161, 418) are rowed, this one is not, and it is not in `ROWS_THE_ENUMERATION_CANNOT_REACH` either, so nothing reds | hygiene | hygiene-37's FIND 2 + dx-37's FIND 1 (2026-09-17); hand-row both |
| [N100](items/N100.md) | NEW — the ledger's check (2) searches only the LONGEST fragment of a key: 283 of 558 rows carry more than one searchable fragment, so a reword of a non-longest run does not red its row. Measured: widening the search to EVERY fragment ≥ `MIN_FRAGMENT` would red exactly two rows today (494, 499), both for one cosmetic cause — their keys spell `{{`/`}}` where `normalized()` collapses them | hygiene | hygiene-37's FIND 3 (2026-09-17); cheap, strengthens 283 rows |
| [N101](items/N101.md) | NEW — two recorded FRAME figures do not reproduce and one doc claim miscounts: `deep_nesting.rs:108`'s `parse` frame says ~71.8 KiB/level unoptimized but `VILAN_DEPTH_STATS` over 30 → 130 parenthesis levels measures 32.8 KiB/level (0.98 MiB at depth 34, 4.18 MiB at 134) — either the frame shrank or the figure predates the counter's move to `parse_nested_as`; `reactive_lifetimes.rs`'s headnote says the mounted exemplar holds 'V4 plus two live-session loops' (three) where the walk measures 2; N97 re-keyed the walk's figure (42,464 B/level, was '~36 KiB') the same way | hygiene | hygiene-37's FIND 5 + native-a-37's FIND-5 (2026-09-17); re-measure and re-key |
| [N102](items/N102.md) | NEW — 38 vilan-cli test suites remain on the shared tmpfs after N86 (named by FILE in `BINARIES_STILL_ON_THE_SHARED_TMPFS`, so the gate holds the line without blocking a lane that adds a path to a listed file); most do not declare `mod support;`, so porting is a structural edit to ~40 files best done when few lanes are adding tests to them — and the merge lesson of Order 37: a lane that adds a NEW scratch-writing test file after the gate lands reds at the merge (perf-37's M74/M75 pins did; one line per site) | hygiene | hygiene-37's FIND 6 + the Order 37 merge (2026-09-17) |
| [N103](items/N103.md) | NEW — `vilan run` creates a `dist/` that holds nothing but the compiler's memory: N92 moved `vilan check`'s macro-expansion table out of the package, but `run_single` compiles with `CompileGoal::Emit` and writes its script beside the entry, never into `dist/` — yet after `vilan run <package>` the tree holds exactly `dist/.cache/macro-expansions` and nothing else; milder than the check case (`rm -rf dist` still means what it says) but a `dist/` holding no build is misleading | hygiene | dx-37's FIND 2 (2026-09-17); N92's sibling; S |
| [N104](items/N104.md) | NEW — three AST doc-comment traps that cost a lane a wrong emission each: `Expr::Number`'s three fields are `(whole, fraction, suffix)` and reading the second as the suffix turned `3.5f` into `3i32` (caught only by rustc); `Expr::StructInitializer`'s FIRST field is not the struct — the JS emitter ignores it (a commented-out `structs.get(struct_id)` still sits at transformer.rs ~6591) and takes the struct from the expression's resolved type; `diagnostics_ledger.rs`'s walk is a HARDCODED four-crate list, so a new crate carrying user-facing diagnostics (`vilan-rust`) is invisible to the ledger until someone remembers | hygiene | native-b-37's FINDS 2, 3, 5 (2026-09-17); one line each, the last a small derivation |
| [N105](items/N105.md) | NEW — `manifest::toolchain_root` (B346's one-root rule) is latent-wrong for a std whose `[library]` declares `root = "."`: it walks up two levels from `base_root`, assuming `<package>/src`, so a source-root-less std would resolve `macro_std` one directory too high — the formula is unchanged from `resolve_macro_std`, no std in the estate does this, and the refusal would name the wrong path rather than a right one | hygiene | dx-37's FIND 4 (2026-09-17); a line, if a packaged std ever ships that shape |
| [N106](items/N106.md) | NEW — `f64` PRINTING is a latent differential hazard the JS side never pins: node prints `Infinity`/`0` where Rust prints `inf`/`-0`, and switches to exponential outside `1e-6..1e21`; `vilan_rt::js_number` documents and implements that boundary for the Rust backend, but no corpus program prints `1e21`, `-0.0` or `Infinity`, so the JS emitter's own behaviour at the boundary is unrecorded; and `str::len` is UTF-16 on JS while the native backend's `str_len` matches it only for the BMP (surrogate pairs diverge — `vilan_rt::str_len` says so) | hygiene | native-b-37's FINDS 4 and 6 (2026-09-17); a corpus program each; the surrogate case is F1 S1b's |
| [N107](items/N107.md) | NEW — E69 carries TWO implementations of one arrow: the python generator's default mode (TSV → Rust table) and the Rust sync gate `html_attributes_sync.rs`, which re-renders and diffs byte for byte — held equal by the gate, so drift reds on the first `cargo test`, but duplication; and `Error.note` is ONE location by contract, with no channel for a program-level footnote (what made 'a note on the first primary error' unbuildable for E189's broad gate when that error already had a note) | process | editor-37's FIND 2 + diagnostics-37's FIND 4 (2026-09-17); two small decisions |
| [N108](items/N108.md) | NEW — `vilan fmt` DECLINES a braced import list at kolt's client.vl:7 — `import std::asset::{ bundle, bundle_as }` — with `reprinting it would have changed the code, so the reprint was thrown away (the formatter's own safety net)`: the round-trip guard fires on a construct the printer should render verbatim, so the file can never be `--check` green on 31c13567; pre-existing (HEAD's and the owner's copies decline identically) and only VISIBLE since N90 gave a decline its own exit code. A formatter bug, not a kolt problem: find which token the reprint moves in a two-leaf braced import (the spaces inside the braces? the trailing comma rule?), pin the round trip, fix the printer | bug | the kolt migration's verifier (2026-09-17, post-seal); found by N90's new exit 2; S |
