# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A34](items/A34.md) | NEW — a typed style token has no mid-value spelling in a css block | design | b148's census find; real gap revealed |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A46](items/A46.md) | NEW — fragment syntax `<>..</>` lowering to a `List<View>` literal (reverses element-syntax.md §7's refusal); NOT a multi-root View | feature | owner's ask (kolt); S–M for the list lowering, L for a marker-node fragment; owner to say which |
| [A53](items/A53.md) | NEW — a GENERIC source field for `[expose]`/`[service]` (A52's split-off): `ServiceItem` (meta.vl) carries no generics so the expansion cannot spell `impl Store<type T>`; the contract hash is built from WRITTEN types so every instantiation hashes identically (a `Store<Task>` client would connect to a `Store<Note>` server); `Client::connect(url, codec)` has nowhere for the element type to come from — three answers wanted before a build | design | rpc-29's investigation (2026-09-06), both spellings pinned red (f162e63c); B184's trait-typed-field carve-out is the other spelling's refusal |
| [A54](items/A54.md) | NEW — a `Delta`-producing CELL for the keyed exposure (A51's split-off): `keyed_diff` re-keys two snapshots and `KeyedSource::apply` reaches `index_of` per op, both O(N) — 1,000 rows 1.08 ms per change per connection, 10,000 rows 8.83, linear in the collection and in connections (`getrusage`, loadavg 128–169) — irreducible while the source is `List`-valued: only the MUTATION knows what changed | design | rpc-29's measurement and decision (2026-09-06); a new std type with mutators, a `Source` impl, `expose_keyed_cell`, its own attribute spelling, an op-log trimming policy and a `Reset` fallback |
| [A55](items/A55.md) | NEW — the mirror seams A52 left: `KeyedSource` is still not a `Source` (A52 covered `RemoteSource` only), and a `RemoteSource<List<T>>` is a `Source<Option<List<T>>>`, so `bind_each` still takes `or([])` — a `Source<Option<List<T>>>` sibling for `bind_each`, or the boundary stays deliberate | design | rpc-29's finds and open Q (2026-09-06); the boundary is pinned and documented today |
| [A56](items/A56.md) | NEW — two rpc rulings after A51/A52: `[expose(keyed = K)]` over a `Map<K2, V>` whose argument disagrees with `K2` — today the argument silently wins and the mismatch surfaces as a plain type error at the mirror (refuse at the attribute?); and A48's `authorize_timeout` still answers 429 now that `Reject::Unavailable` is 503 (std refusing on the app's behalf is not the app's judgement — re-rule or leave) | design | rpc-29's open questions (2026-09-06) |
| [A57](items/A57.md) | NEW — a typed `Storage` handle in `std::storage`: kolt hand-writes the whole surface (`external struct Storage`, `length`, `key`, `window().local_storage()`) because std ships six free functions and a missing key that reads as `""` — five FIXMEs in one 40-line file, and a real customer that enumerates and presence-tests the store | feature | kolt/src/storage.vl:6-41, the sweep's densest FIXME cluster; carries the answer to prefs.vl:27's codec question (already covered by `std::json`'s per-value string codecs) |
| [A58](items/A58.md) | NEW — `std::dom::Event` has `key` and `target_value` but no `code`, no `target` and no `current_target`: three apps-facing getters hand-written in three different kolt files, two of them under an explicit "FIXME: Implement with std" | feature | kolt/src/input_system.vl:5-9, views.vl:31-34, overlay.vl:91-94; `target_value` already reaches through the target, so the binding is there and only the accessor is missing |
| [A59](items/A59.md) | NEW — `std::dom` cannot MEASURE, OBSERVE, or un-set: kolt's overlay.vl carries twenty externs under a hand-written "DOM bindings std::dom lacks yet" banner (`getBoundingClientRect`, `contains`, `offsetWidth/Height`, `isConnected`, capture-phase listeners, `ResizeObserver`) and views.vl adds `removeAttribute` — the other half of `set_attribute`, without which `toggle_attr` cannot be written | feature | kolt/src/overlay.vl:53-125 and views.vl:378-382; every binding has a named customer in the anchored-overlay path in the same file |
| [A60](items/A60.md) | NEW — style smalls from kolt's styles.vl: `flex_grow` missing beside `flex_shrink`, no `Color::current()` for `currentColor` — and the `[hidden]` HACK, which is not a small: the preflight rule loses to ANY app `display` because it sits a CASCADE LAYER below, so `View::show()` silently does nothing on every flex container | design | kolt/src/styles.vl:11-37 (three FIXMEs, one of which — `Length::full` — is already covered by `Length::pct(100f)`) and views.vl:132-139's HACK; item 4 wants a ruling, the first two want five lines each |
| [A61](items/A61.md) | NEW — `str::parse_bool` is missing beside `parse_i32`/`parse_f64` (four call sites in kolt's preference decoders) — while the OTHER two conversions kolt hand-wrote, `bool.to_string` and `f64.to_string`, already ship as `Display` impls and can be deleted: the gap is the parse direction plus a discoverability trap on a trait method | feature | kolt/src/prefs.vl:69-93, three FIXMEs; two are already covered (display.vl:54-64), the third is real |
| [A62](items/A62.md) | NEW — `std::router` is write-only: `Routable`/`to_path` renders a route to a URL and `segments()` is the ONLY read helper, so every app hand-writes index arithmetic to parse one back (kolt's `parts.len() > 3 && parts[2] == "task"`) — and no query string or fragment is reachable at all | feature | kolt/src/routes.vl:22-52, the owner's FIXME asking for path helpers or "a canonical url parsing format that's easier to read and write than the below" |
| [A63](items/A63.md) | NEW — a `Debounce` in `std::time`: kolt built one over `Timer` (trailing edge, deadline pushed out per call, callback replaced) and std has only the primitives — plus the answer to its own TODO, that the `Shared` cell IS required, because the `async` block outlives `run`'s `self` loan (rule 1) and writes the cell from inside it (rule 3) | feature | kolt/src/debounce.vl, 37 lines of non-app-specific code; the tracker's only other `debounce` hits are E121 (the LSP's own number) and A14 ("no default debounce", about std not debouncing FOR the app — a different question) |
| [A64](items/A64.md) | NEW — a style-paper ruling A36 did not cover: should conditional component state COMPUTE a class list per state (`.when` today), or ride CSS variables, or data attributes via the `within`/`child_relation` machinery std already has — the owner's own first use of `when` hit the compound condition and asked, and views.vl runs `when` chains inside a signal `.map` where nobody has priced the per-notify rebuild | design | kolt/src/styles.vl:62-68; A36 (`Style::when`) CLOSED 2026-09-04, Order 27, lane std-27, vilan bb80ec4f — shipped exactly the ruling's five lines, and this is the question after it |
| [A66](items/A66.md) | NEW — a boolean-attribute binding on `View`: `toggle_attr(name, source: Source<bool>)` (presence, not value — `inert`, `disabled`, `hidden`, `open`) beside `bind_attr(name, Source<str>)`; kolt hand-writes it over its own `Element::remove_attribute` extern (views.vl:371–384) to make the app shell `inert` while a modal is up | feature | the owner's question (2026-09-07); `Element::remove_attribute` itself is in A59's list — this is the `View`-level half, a two-method change plus the SSR twin |
| [A67](items/A67.md) | NEW — A65's ruling left open: `import pkg::a::b` where `a.vl` DECLARES an item `b` and `a/b.vl` also exists takes the FILE (longest module prefix wins, documented in `longest_module_prefix`), not an ambiguity error — unlike `a.vl` + `a/lib.vl`, which is one | design | lane a65's find (2026-09-08); OWNER Q — my recommendation: the ambiguity error, one rule for both collisions |
| [A68](items/A68.md) | NEW — a `css { … }` block is `const` BY CONSTRUCTION: today `let b = css { padding: 1rem; };` is refused ("`raw` … is compile-time-only; evaluate this call inside a `const` expression") and every block needs `const` written in front; a block is a compile-time asset by definition, so it is one, and const-eval refuses a hole that reads a runtime value | feature | the owner's sketch (2026-09-07: `css { // auto const`); probed on a5473227 |
| [A69](items/A69.md) | NEW — a CHAIN LINK inside a `css` block: a dotted item ending in `;` (`.ghost();`, `.flex_row();`, `.custom_method(a, b);`) is a verbatim `Style` method call in written position, while a dotted item with a block stays a condition rule — the element syntax's own rule (dotted = chain link), and the way an app's helpers and std's combinators reach the block | feature | the owner's sketch (`.custom_method()` inside the block, 2026-09-07); css-block.md §10 refused "composing styles inside a block" and left the grammar space free — this is the ask that wants it |
| [A70](items/A70.md) | NEW — `std::style::prelude`: free-function aliases of the typed tokens (`rem`, `px`, `em`, `pct`, `vh`, `vw`, `auto`, `space`; `black`, `white`, `transparent`, `hex`, `gray`, `blue`, `red`, `green`, `rgba`, `oklch`; `s()` for `style()`), AMBIENT inside css-block holes (names.md's ambient-module precedent) and importable in any block — so `{rem(4)}` and `{red(500)}` are the hole spellings and `const { import std::style::prelude; s().p(…) }` is a legal local scope | feature | the owner's two sketches (2026-09-07); block-scoped imports have shipped since July, `const { … }` works as a block expression — the prelude module is the only missing piece for both |

## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B256](items/B256.md) | NEW — `Shared::read()` hands out the cell's STORAGE: `SharedValue` lowers to the bare slot `self.v` though the intrinsic table says "a copy of the cell's value" and spec §6.1 says a value-returning signature copies; the clone pass exempts every call ("calls own their result"), so `let b = h.read()`, `mut c = h.read(); c.push(x)` and `SignalCell::get()` all alias the cell (`c.push` grows the cell) | bug | found under B255 (2026-09-06); the seam that lets `bind_each`'s alias out and makes `update`'s in-place mutation observable through every earlier `get()`; the owner's call on WHERE the copy lives |
| [B258](items/B258.md) | NEW — MISCOMPILE: overriding a trait DEFAULT (`map`) in a sub-trait impl (`impl RemoteSource<type T> with Source<Option<T>>`) reddens A25's owner pins — a call on a `SignalCell` FIELD that INHERITS the default reaches `register_with_owner` with an undefined owner (`self[0].v.push` on undefined); "overriding a default the receiver's own field type inherits" is the breaking shape | bug | rpc-29's find (2026-09-06); `map`/`or` kept INHERENT on the mirror as the workaround, so a generic `S: Source<…>` calling `.map` on a mirror gets the owner-optional default instead of A25's strict law |
| [B259](items/B259.md) | NEW — B248's siblings: a block-like head of a binary expression OUTSIDE statement position (`let y = match x {..} + 1;`) reports "expected `;`" plus B248's refusal (same steer, different path); a POSTFIX chain on a block-like head (`match x {..}.to_str()`) is still the bare `found '.' expected an expression` | bug | smalls-29's finds 1 and 2 (2026-09-06); the postfix case was left out of B248 because skipping the `.` turns the member into a bare name and cascades |
| [B260](items/B260.md) | NEW — the missing-member diagnostic's HEAD names the trait UNPARAMETERIZED: `does not implement trait 'Counted'`, never `Counted<i32>`, even where the arguments are what the refusal is about (B249 fixed only the suggested declaration) | bug | smalls-29's find 3 (2026-09-06) |
| [B261](items/B261.md) | NEW — `Type::Generic`'s pretty-print for an IMPLICIT binder (B186's sugar) is the trait's name, so row 346's head can still read "`Add`'s `add` accepts `Add`" after B218; only the steer could be worked around (B246's refusal names the written-out rewrite instead) | bug | solver-29's find 1 (2026-09-06); B186's mint needs a display of its own — B184's `C<A>` face is the precedent |
| [B262](items/B262.md) | NEW — B251's written-argument bound check is STRUCT-only: enums and traits reach the same `prepped_type_locals` drain with the same unchecked written arguments (`Held<i32, SignalCell<List<str>>>`'s enum twin is still accepted) | bug | solver-29's find 2 (2026-09-06); a one-arm extension with its own census |
| [B263](items/B263.md) | NEW — `derive_generics_from_bounds` inside an IMPL body: `impl Held<type T> { fun first(self): T { self.list.get()[0] } }` reports "cannot index this List: its element type is never determined" — the impl binder's `T` is not recovered through the sibling parameter's bound the way B251 now recovers it at the literal | bug | solver-29's find 3 (2026-09-06); separate from B251's two halves and unaffected by them |
| [B264](items/B264.md) | NEW — TYPE-position import aliases are not fully split after E145: `import Foo as Bar; let x: Bar` records `type_references` against the target (the alias span catches it by spelling, so references narrow correctly) while go-to-definition and hover at such a use resolve THROUGH the target — a ruling on what go-to-definition at an alias should answer | design | editor-29's find 1 (2026-09-06); correct today, not deliberate; also editor-29's open Q: should find-references from the DEFINITION side list alias-reached uses informationally? (the lane took the narrow reading — one index for rename and references) |
| [B265](items/B265.md) | NEW — the intrinsic table binds `remove`/`insert` (and `pop`, `push`) by NAME on any `List` impl declaration, external or not: a vilan-bodied `List::remove` is silently replaced by the intrinsic (found when an old-std A/B emitted the NEW lowering); std-only today, but a std author cannot give an intrinsic's name a vilan body | bug | perf-29's find 1 (2026-09-06) |
| [B266](items/B266.md) | NEW — a GENERIC `[service]` subject has no curated refusal: it cascades through generated code (first line `'contract_hash' is already defined for 'StoreClient<T>'` plus `` `Store` takes 1 type argument, 0 given ``) | bug | rpc-29's find (2026-09-06); the diagnostic A53 owes whichever way A53 is ruled |
| [B267](items/B267.md) | NEW — B256's third option: rule 2 (last-use elision) is LOCAL-only and cannot see a `Shared` slot, yet both of std's hot per-notify reads (`bind_each`'s `row_views.read()`/`row_owners.read()`, `drain`'s `turn[0].v`) are reads whose source dies or is only walked — a CELL-AWARE last-use would make the B256 ruling nearly free (+12–25% CPU on the reactive pins today, +344% on a raw `get()`) | design | rule1-29's find (2026-09-06); the recommended path to landing B256: build this, re-measure, then land the place treatment |
| [B268](items/B268.md) | NEW — a `Signal<View>` (or `SignalCell<View>`) in CHILD position stringifies instead of rendering and re-rendering (kolt views.vl:543's HACK: `.swap(signal, \|x\| view)` stands in for `{signal}`); with A46's fragment the question is the same — WHAT MAY BE A CHILD (str, View, List<View>, a Signal of each), and a `View` struct cannot say it while a trait could | bug | the owner's kolt find (2026-09-07), tied by the owner to A46: both may push `View` from a struct to a TRAIT; decide the child contract once, then the fragment and the signal child follow |
| [B269](items/B269.md) | NEW — the dead-code paint (E124) marks a function used ONLY under `const` as unused: kolt's `fun page_defaults()` is called by `const page_defaults();` (build-time side effects — `declare`, `preflight`, `bundle`) and reads as dead because the paint follows the pruner's RUNTIME reachability, under which a const-evaluated call never runs; a `const` call is a use | bug | the owner's find (2026-09-07); the binding spelling `let _page_defaults = const page_defaults();` is not needed — a bare `const page_defaults();` statement at module level compiles and emits the same CSS (probed on a5473227) |
| [B270](items/B270.md) | NEW — the `css { … }` block is UNUSABLE under the web prelude: its lowering seeds the chain with a bare `style` accessor (css.rs:93, `Node::Accessor("style")`), and `std::web` binds `style` to the ambient MODULE, so every block in a `prelude = "std::web"` package fails with "`style` is a module, not a value; qualify through it (`style::item`)" (kolt channel.vl:71); the element lowering's bare `view` survives only because the prelude's `view` is the function — both desugarings are scope-DEPENDENT where they should be hygienic | bug | the owner's find (2026-09-07), reproduced in a scratch web-prelude package; kolt has zero css blocks for this reason |
| [B271](items/B271.md) | NEW — `View::autofocus()` (A45) focuses in a MICROTASK, before the frame's rendering step, so an input inside a subtree that becomes focusable only during rendering (kolt's overlay panel is `visibility: hidden` until a ResizeObserver callback places it and flips it visible, overlay.vl:443/:554) is not focusable when `focus()` runs and the call is a no-op — the browser rule, not a race: `focus()` needs a connected, rendered, visible, non-inert target AT THE CALL; "just created" is never the problem, "still hidden" is | bug | the owner's find (2026-09-07): `element.focus()` fires and the browser "doesn't take it"; the old `set_timeout(1)` only happened to land after the ResizeObserver flip; std's doc claims the microtask is enough, which is true only for a subtree that is visible at build time |

## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C1](items/C1.md) | `Weak<T>` | design | |
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E69](items/E69.md) | NEW — attribute-NAME completion in an element head is a semantics decision | feature | |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E106](items/E106.md) | NEW — the language server slows down over a session | bug | owner report; measure first |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E150](items/E150.md) | NEW — fmt-29's residuals: E147's break threshold is a COUNT (two operators) — `std/src/markdown.vl:537` stays at 118 columns because its last operand is a two-operand `\|\|`; a MEASURED threshold (break when the widest resulting line is narrower) reaches it but flips `a_single_link_right_operand_leaves_the_line_long` (116 → 105, still over budget); a `match` leg body written `=> { expr }` still expands where an `if` arm now stays inline; `for`/`while` conditions and a `match` subject do not take the split permission an `if` condition now has (no over-budget instance in the tree) | editor | fmt-29's finds and open questions (2026-09-06); OWNER Q on the threshold — my recommendation: keep the count |
| [E151](items/E151.md) | NEW — an element-head ATTRIBUTE sorter in the formatter, the import and style-chain sorters' sibling: undotted attributes in a canonical order (`id`, `name`, `type`, `for`, `href`, `src`, then alphabetical), `on:` handlers after them alphabetically, dotted chain links LAST and in written order (a chain's order is semantics — `.child` order, `.styled` then `.when`) | editor | the owner's ask (2026-09-07); the style-chain sorter (formatter.rs `sort_style_chains`, `style_chain_order.rs`'s corpus proof) is the precedent: sort only what commutes, and prove it |
| [E152](items/E152.md) | NEW — a pure-namespace module entity (a directory with no body, A65) is attributed to `SourceId(0)` at `EMPTY_SPAN` because it names no file, so go-to-definition on such a segment lands at the ENTRY's top; hover and references on the segment have the same nowhere to point | editor | lane a65's find (2026-09-08); harmless, not ideal |
| [E153](items/E153.md) | NEW — css-block editor smalls: `:hover {` inside a block is refused with the bare `found ':' expected a declaration …` where a STEER to `.hover { … }` is owed (the dotted rule is deliberate — one name-blind rule covers pseudo-classes, breakpoints, `within`, `divide`); and PROPERTY-NAME completion inside a block from the closed CSS property list (values stay token runs — css-block.md §10's non-goal is value completion, not names) | editor | the owner tried `:hover` (2026-09-07); property names are the "write" half of compact styling |

## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |

## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | NEW — an unchanged package module is re-analyzed every LSP analysis / HMR round | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M48](items/M48.md) | NEW — M19 tranche 1d: R10 `check_container_resource_arguments` is now the largest single item on the phase line (477–1,113 ms warm on kolt's client, comparable to the enrolment gate T1c removed); its whole-program state `reported_container_structures` is already a `HashSet<String>` structural key, id-free and recordable — the blocker is that its diagnostics sit OUTSIDE T1's Class A recording window, so restoring it means moving that window | performance | m19-t1c's find 1 (2026-09-06); a tranche of its own |
| [M49](items/M49.md) | NEW — m19-t1c's residuals: the resource-reaching nominal FINGERPRINT includes entry-declared nominals, so two entries in one package that differ in whether they declare a resource invalidate each other's enrolment record (split world-declared from entry-declared); and `LastUse::compute` still costs 258–558 ms warm after T1b — the restore may not reach as much as assumed (re-price) | performance | m19-t1c's finds 2 and 3 (2026-09-06) |
| [M50](items/M50.md) | NEW — M24's byte budget is denominated ~24× LOW: `base_cache_retained_bytes` records 195,087 B for a retained world that weighs 4,722,688 B resident (eight distinct keys minted via `macro_limits`, minus a same-key control, byte-identical across three runs) — 512 MiB of recorded bytes is on the order of 12 GB resident; the cache is bounded, but not where M24 thinks | performance | m36's find 1 (2026-09-06); M46's `ModuleTables::bytes` is a floor by design — this is the base world's own accounting |
| [M51](items/M51.md) | NEW — `build` in parallel: the AUDIT (perf-29, no code) — the obstacle is one `dist/` and two order-dependent accumulators: `leg_schedule` is a real producer→consumer DAG (parallelism caps at the widest tier); `bundled_names` is mutated per leg and read by every later one (collision blame becomes scheduler-dependent — lock + member-order blame); `explain::leg_facts` is process-global and loop-ordered (M35's capture/replay); `write_assets`/`write_bundled`/`write_chunks` share one directory and `write_chunks` sweeps a namespace (per-leg staging + atomic move, or a `dist/` lock); `watch_state: &mut` must become per-leg results merged after join; `?` aborts at the first failing leg. Smallest honest step: parallelize `compile_unit` within a tier, keep every writer serial in schedule order. `check_single`'s per-platform loop's blocker is the process-global B182 `RoundReports` ledger | performance | perf-29's audit (2026-09-06), M44's `build` half |
| [M52](items/M52.md) | NEW — M44 is INERT wherever members' std seed sets differ: kolt's three legs have three distinct seed sets and so three base-cache keys, so its residual multi-entry cost is not duplicated worlds — a coarser key (the union of the package's seeds) or M19's module-level sharing is the follow-up that reaches kolt | performance | perf-29's find 3 and open Q (2026-09-06) |
| [M53](items/M53.md) | NEW — compile-perf-29's smalls on a cold kolt check: `infer_type_path` clones `constraint` on entry 180,670 + 148,029 times (a `Cow` at twenty use sites inside a 1,300-line function, ~0.23%); path handling ~3% (`Components::next` 2.15% + `Path::_starts_with` 0.70%); `const_eval::State::new` 0.69% + 0.68% (a per-evaluation allocation); `Node`'s next ceilings 136 (`Trait`, `StructInitializer`) then 120 (`Element`, `Struct`, `Enum`), boxing all five lands at 96, below which `Node::Call` at 88 is an expression where boxing trades the memcpy for an allocation | performance | compile-perf-29's finds (2026-09-06); each needs a profile DIFF, not a total — Ir drifts ~1% on this tree from unrelated codegen |

## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N35](items/N35.md) | NEW — `hmr_css_matrix` reads the bundle while the watcher writes it | bug | load-dependent race, seen once |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N59](items/N59.md) | NEW — corpus programs inherit the test process's cwd (`crates/vilan-core`), which is how `file-corpus-<n>/` trees strand there on a crash; a per-run `current_dir` in `run_node_within` would end the pollution at the source but changes relative-path resolution for all three gates on the shared harness | hygiene | hygiene-29's open question (2026-09-06); the `.gitignore` line landed as the interim |
| [N60](items/N60.md) | NEW — smalls from Order 29: `wait_for_port` is duplicated three ways (`document`, `split`, `init`) with three signatures — N58's `free_port` consolidation, not done; `release_emission.rs:377` carries `Option::{ None, Some, self }` as an inline fixture, non-canonical under E146 (unreached by `vilan fmt`); `diagnostics_ledger.rs`'s `ROWS_WITHOUT_A_KEY` comment still says row 229 is "the nine `ParseErrorReason::Rule` statements" — the tree has ~22 | hygiene | hygiene-29's, fmt-29's and smalls-29's finds (2026-09-06) |
| [N61](items/N61.md) | NEW — `service_layer.rs:1608` (`a_refused_client_reports_unauthorized_without_burning_the_retry_budget`) bounds a whole `vilan run .` (build included) at 45 s WALL: 52.56 s at loadavg 184, 5.88 s at loadavg 59 — a wall-clock gate on a shared box, `diagnostics_budget`'s class | hygiene | editor-29's find 2 (2026-09-06); nothing in that lane touched rpc |
| [N62](items/N62.md) | NEW — two accuracy gains left unobserved by any pin: B254's subject narrowing removes bogus `call_graph`/`init_order` edges (strictly more accurate, nothing asserts an edge that should disappear); B244's `current_substitution` now ACCUMULATES outer bindings across nested instance emissions (constraint ids are per-declaration so the sets are disjoint; the cost of a deep monomorphization chain is unmeasured) | hygiene | checker-29's open questions (2026-09-06) |
| [N63](items/N63.md) | NEW — `vilan check` now CREATES `dist/` (M33's `dist/.macro-expansions` lives there), where it previously wrote nothing; compile-perf-29 gitignored `vilan/benchmarks/dist/` and `vilan/test/dist/` after a cache file was staged by accident — where the on-disk caches live (`dist/.cache/`, a per-package `.vilan/`, or the build dir only for `build`) is the owner's call | process | compile-perf-29's open question (2026-09-06); M36's design (§6.15) would put a second cache beside it |
| [N64](items/N64.md) | NEW — three notes from a65: `own` is a vilan keyword documented nowhere in the tour (cost a lane a debug cycle on a fixture); loading `pkg::lib::util` also loads `lib`'s body when it has one (Rust's model — a child import can pull a heavy parent), intended but unstated in the docs; build-hooks.md:1035's "a subdirectory cannot be imported today" is now false, so the generated root (kolt's `src/lucide`) may move under `root` as an ordinary module path — the build-hooks paper's follow-up | hygiene | lane a65's finds (2026-09-08) |
