# Vilan tracker — open items

`backlog <ID>` resolves to `items/<ID>.md` if open, or a tombstone in `archive.md` (or the frozen chain it points at) if closed — see `proposal/tracker-migration.md` §2.

## A. Reactive core & UI (`std::reactive`, `std::ui`)

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [A7](items/A7.md) | SSR tail | design | |
| [A8](items/A8.md) | UI styling — the tail | design | |
| [A14](items/A14.md) | Reactive residuals | design | |
| [A37](items/A37.md) | NEW — `[gone]` trait-member attribute: reachable only through the trait (method surface, for blanket impls) | design discussion | NOT queued; the owner unsure of its value |
| [A67](items/A67.md) | NEW — A65's ruling left open: `import pkg::a::b` where `a.vl` DECLARES an item `b` and `a/b.vl` also exists takes the FILE (longest module prefix wins, documented in `longest_module_prefix`), not an ambiguity error — unlike `a.vl` + `a/lib.vl`, which is one | design | lane a65's find (2026-09-08); OWNER Q — my recommendation: the ambiguity error, one rule for both collisions |
| [A72](items/A72.md) | NEW — `std::router` after A62: `segments` (raw) and `parse_path().segments` (decoded) disagree on `%20` — two forms of one question, making `segments` decode is the tidier end state (a behaviour change not taken); and `current_path()` is pathname-only so `parse_path(current_path().get())` never sees a query — there is no SIGNAL over the full URL (`location_url()` is a read), so an app routing on `?q=` still wires its own `popstate` | feature | dom-30's finds 1 and 2 (2026-09-08) |
| [A73](items/A73.md) | NEW — std-30's two unshipped halves: `Storage` key ITERATION (today `len` + `key_at` by hand; a `keys(): List<str>` or an iterator) and `Debounce::flush()` (fire the pending callback now — a form that unloads before the window closes needs it) | feature | std-30's open questions (a) and (b) (2026-09-08) |
| [A76](items/A76.md) | NEW — the SSE/`SplitDuplex` leg has no reverse lane: `s:` is socket-only, `events_response` registers no client channel, so `self.client.ping()` on an SSE connection drops silently; documented at the site, but a service mounted for SSE gives no signal that the direction does not exist there | feature | reverse-31's find 2 (2026-09-09) |
| [A77](items/A77.md) | NEW — a PEER struct's browser-side instance has no honest proxy value: the pin writes `PeerProxy::for_connection(0 - 1)`, a generated struct literal reaching for a sentinel | feature | reverse-31's find 3 (2026-09-09) |
| [A79](items/A79.md) | NEW — the keyed RETURN mapping: `[rpc] fun tasks_in(workspace): KeyedCell<i32, Task>` → a stub returning `Result<KeyedSource<i32, Task>, RpcError>` — §9.2's row, UNBLOCKED by A54 (the cell exists) and A74 (the mapping machinery, `handle_element` + `reply_source`), unbuilt | feature | handles-31 and keyed-31 both left it as instructed (2026-09-09); kolt's per-workspace tasks are the exhibit (store.vl:96–99) |
| [A80](items/A80.md) | NEW — vilan has no MUTABLE PATTERN BINDING: `Some(let mut x)` and `Some(mut let x)` are both parse errors, so `cell.update(\|&mut held\| match held { Some(let list) => list.push(..) })` is refused with "cannot mutate immutable 'list'"; `Option::take`/`replace` is the working idiom and it is not obvious | feature | keyed-31's find 3 (2026-09-09) |
| [A81](items/A81.md) | NEW — v2 of the reverse direction: AWAITED server→client calls — the `s:` id bytes are reserved on the wire, the server end needs a pending table and `fresh_id`, the client dispatches `s:` under a held turn and replies on the same lane; and `notify` on a closed connection is silent by design — an app cannot learn a notification went nowhere | feature | reverse-31's finds 4 and open Q 2 (2026-09-09); transport-rpc §9.3's async-coloring paragraph is this item's |
| [A85](items/A85.md) | NEW — POSITIONAL SLOTS: `when`, `swap` and `bind_each` as `Slot` VALUES placed at an ANCHOR, instead of parent methods that APPEND — today each is `parent.when(..)` / `parent.swap(..)` / `parent.bind_each(..)` and its content lands at the END of the parent, so a conditional or a keyed run between static siblings costs a wrapper element per region (and the `Source<View>` / `Source<List<View>>` child arms re-order themselves behind their siblings on the first change, A71). `Slot` already IS the child contract (B268: `View`, `str`, `List<View>`, and a `Source` of each — a signal is admitted by a blanket impl over the bound, not by a type-system special case), so `each(items, key, row)`, `when(more, \|\| footer)` and `swap(route, render)` RETURNING values that `impl .. with Slot` and place themselves at a marker node (`Element::insert_before`, which `std::dom` lacks) put fragments (A46), conditionals and keyed runs in position as ordinary children — `<ul>{header}{each(..)}{when(..)}</ul>` — with no container | design | the owner's ask (2026-09-11): a child trait that admits an array of views as one child, positional conditionals, positional `bind_each`, and signals without special handling; the trait exists (B268 made `Slot` the contract) — what is missing is POSITION and the VALUE form. A71's anchor is the mechanism's first brick and A46's fragment rides the same anchor; B253 (a bare trait in RETURN position stays refused, Order 29) shapes the values' spelling — a helper returning one names the concrete type. Paper in Order 32 (lane ui-32), the surface ruled before the build |
| [A87](items/A87.md) | NEW — `Owner::release()`: after B291 an `Owner` is SINGLE-USE (a `take`/`defer` after `dispose` releases on the spot), so dispose-then-REFILL on one owner is gone; nothing in std/examples/docs/kolt did it (every boundary mints a fresh owner) and the one shipped pin that did was rewritten — but an app that wants a releasable-and-refillable owner has no verb for it | design | reactive-32's open Q1 (2026-09-11); rec: add `release()` (run the cleanups, stay live) only when a caller appears — the pattern is a smell (a boundary that outlives its content is what fresh owners are for) |
| [A91](items/A91.md) | NEW — a RENDER CLOSURE may yield a `Slot`: today `swap`'s and `bind_each`'s render closures (and `when`'s body) must return a `View`, so the two kolt wrappers A85's value forms do not remove (views.vl:127 a swap render's result, views.vl:204 a bind_each row) exist only because a `List<View>`, a `str` or a positional value cannot be what a row or a page IS — the closure type becomes `\|T\| C` with `C: Slot` (a fragment, a value form, a text node), the region owning whatever the child placed | feature | A85's paper §7/§9 Q3, RULED 2026-09-11 (owner): filed as its own item, queued behind A85's build (Order 33, slots-33); kolt rows 4 and 6 of the paper's census are the exhibits |
| [A92](items/A92.md) | NEW — the SYNC UNLEASED HANDLE STUB (Order 32's P1–P3, RULED 2026-09-11 as recommended): an `[rpc]` method returning `SignalCell<T>` / `Option<SignalCell<T>>` / `KeyedCell<K, T>` (A79) becomes a SYNC stub returning `RemoteSource<T>` / `KeyedSource<K, T>` minted UNLEASED from its `Origin` (the shipped re-mint path is the first mint, so an unleased handle costs nothing — R3 kept); `Status` gains `Absent` (the `Option` form's `None` reply) and `Failed(RpcError)` (the next 0→1 lease retries); `.or(seed)` at the call site, no `remote_signal` bridge; the SERVER dedups dynamic channels by SOURCE IDENTITY with a per-channel client-lease count (revoke at zero, never under another mirror); a std `Memo<K: Hashable, V>` (`get_or`/`forget`, the maker at the call site) is the app's composition tool — the stub does not auto-memo. BREAKING for every handle stub's type | feature | Order 32's rulings P1–P3, deferred at that GO and RULED at Order 33's (owner: 'go with your recommendations on all rulings', 2026-09-11); kolt's `Task::remote_signal` hack, model.vl's three memos and `lib/memo.vl` are the exhibits; the brief is briefs32.md's rpc-32 section re-anchored (Order 33, rpc-33); transport-rpc §9.6's standing questions annotated |
| [A93](items/A93.md) | NEW — `child_relation` is a PUBLIC RAW-SELECTOR HATCH and kolt uses it as one: `views.vl:165` writes `child_relation("not([hidden])", "display", ..)` — the token is neither `^…` nor `>…`, so it lands in the pseudo slot, skips the `@layer` wrap and the `> *` suffix, and renders `.sX:not([hidden])`, a rule about the element itself from a child-relation method; A89 gives it a real spelling (`attribute("hidden", None, not(..))` emits the identical selector, verified). Fence the token to `>*` / `>*+*` (the two shipped relations) once kolt's site migrates | feature | styles-33's find F5 (2026-09-11); the fence is BREAKING for kolt views.vl:165 — kolt migrates first, at the owner's word |
| [A94](items/A94.md) | NEW — a FRAGMENT does not FLATTEN: `<><i/><>..</></>` is a `List<View>` inside a `List<View>` and is refused — A46's lowering is a literal, so nesting one fragment in another is a type error, not a run; pinned and documented as the limit; if flattening is wanted it is a lowering change (concatenate the inner list into the outer) with no type change | feature | slots-33's find F2 (2026-09-11); no exhibit asks for it yet — filed so the limit has an id |

## B. Type system & the type solver

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [B3](items/B3.md) | Variadic-generics tail | feature | |
| [B11](items/B11.md) | `!` / `?.` tail | design | |
| [B146](items/B146.md) | NEW — the context coverage check's refinement consumes node-owned dispatch sites only | feature | |
| [B147](items/B147.md) | NEW — a module/file-level default for `[platform(...)]` | design | |
| [B149](items/B149.md) | NEW — an async function returning a `Task` mistypes as the task | bug | the pin names it since Order 21; the gap itself stays open |
| [B183](items/B183.md) | NEW — tuple comprehension `(item in tuple => EXP)` + the zip form | design | owner-proposed |
| [B274](items/B274.md) | NEW — `shared.read().push(x)` GROWS the cell: a temporary read stays free under the B256 ruling and a `&mut self` receiver is not a considered position, so the write lands in the cell; four doc sites say the write is lost (tour/memory-model.md:172, :240; std/cells.md:36; appendix/gotchas.md:44) and spec/memory.md:377 says `read()` copies — a ruling: copy at a `&mut self` receiver on a temporary read (the docs' promise, the narrow remnant of clone-at-every-read), or rewrite the five sites | design | rule1-30's find (2026-09-08); OWNER Q — my recommendation: the copy, it is what every reader of those pages already believes |
| [B278](items/B278.md) | NEW — an i-string hole cannot contain an ESCAPED QUOTE: `i"{x.get(\"k\")}"` is a parse error, so a call with a string argument inside a hole must be bound first; cost a lane an iteration | bug | std-30's find 3 (2026-09-08) |
| [B279](items/B279.md) | NEW — the silence behind B258 has no STRUCTURAL guard: nothing forbids a future over-approximation promoting a node to strict that coverage's narrower edge set never fences — the result would again be a silent `undefined`; and B254 + B258 are one class (a NAME-keyed `candidates_of` list consumed as receiver-specific) with a third instance named in `impl_members_for_bound`'s doc — a sweep of `candidates_of` consumers is owed (the const-only capability check reads edges as refusals and is safe by direction) | bug | b258's finds 1 and 3 (2026-09-08) |
| [B298](items/B298.md) | NEW — an ANONYMOUS binder has no name for a diagnostic to use: `impl Pair<_: PartialEq, _: PartialEq>` with `self.first == self.second` reports "`_`'s `eq` accepts `_`, but the right operand is `_`" — three distinct parameters all rendered `_`. B186's nameless generics already get an `impl Bound` face (`operand_label`/`implicit_binder_bound`); routing anonymous binders through it would read `impl PartialEq` — but that function's own doc records that two `impl X` still collide, which is trait-typed-fields.md rev 2's Q3, the owner's | design | smalls-32's find F2 and open Q1 (2026-09-11); rec: yes for a bounded `_`, behind the Q3 ruling |
| [B302](items/B302.md) | NEW — `render_type` renders a PATH annotation as `_`: `[rpc] fun note(self, id: i32): models::Note` reported "return type of `[rpc]` method `note` is `_`, which is not Wire" on 65af4be0 (B289 makes that program compile — the path type is now read for what it is — but the `_` rendering presumably still reaches every other message that renders a written path type) | bug | wire-32's find F2 (2026-09-11); verified on the installed binary |
| [B308](items/B308.md) | NEW — DEAD CSS from every intermediate `Style` in a condition nest: `Style::rule` (style.vl:935, the one chokepoint) calls `emit("css", ..)` at CONSTRUCTION, so `attribute(.., hover(inner))` puts the inner `hover(..)` style's own `.sY:hover{..}` on the sheet beside the composed `.sX[..]:hover{..}` although `sY` never reaches an element — one dead rule set per nesting level per leaf declaration (`md(within(attribute(hover(x))))` emits four rule sets for one applied class); measured on a two-level probe: two of five emitted rules were for classes the bundle never applies | bug | the owner's ask (2026-09-11); found while probing A89's forms (`kolt-styles-probe/forms/app.css`: `*.s1rdmbny:hover` and `*.st5yvuy:focus` emitted, neither class in app.js); pre-existing since the combinators shipped — every `hover(style().background(..))` in kolt pays it |
| [B309](items/B309.md) | NEW — a `context` clause lives on a PARAMETER and nowhere else: `body: (sync \|\| View) context owner_scope` as a STRUCT FIELD is refused three ways ("a `context` clause is only supported on a parameter's closure type"; "a `sync` closure contract is only supported on parameters"; "an injected closure can only be called, forwarded to a parameter with the same `context` clause, or passed to `run`") — the clause is recorded side-band by parameter id (`analyzer.rs:3589` `parameter_contexts`), `Type::Closure` (`type_.rs:41`) carries none, and `context.rs` keys `value_contexts` on locals, so a clause flows through no field, generic argument or return type. This is the wall A85 (positional slots as `Slot` VALUES) hit: a value form must hold its body, and a plain-closure field CAPTURES AT CREATION, so every body/row/page leaks into the enclosing boundary (measured by slots-33). A91 inherits it | design | slots-33's find F1 (2026-09-11): the four pins in `inference/bounds.rs` (`a85_a_context_clause_is_refused_off_a_parameter` is the CONTROL that goes red when this lands; two `#[ignore = "A85: …"]`; `a85_a_plain_closure_field_answers_to_the_owner_it_was_built_under`); ambient-owner.md §5's v1 restriction; A85's re-ruling: (a) build this first is the recommendation |
| [B310](items/B310.md) | NEW — MISCOMPILE: `Map::entries()` hands out the GENERIC tuple layout for a `Map<K, (A, B)>`: `for entry in m.entries() { let (key, slot) = entry; let (class, declaration) = slot; }` reads `class = "class,decl"`, `declaration = undefined` (and `entry.1.0` / `entry.1.1` the same way) — a tuple built inside a GENERIC function from type-parameter-typed values (`self.table.insert(key.hash(), (key, value))`, map.vl:73 `self.table.values()`) is not flattened, while the caller, which knows `V = (A, B)`, reads the result flat (`$m.slice(1, 3)`); `keys()`, `values()` and `get()` are right; silent wrong values, no diagnostic | bug | styles-33's find F3 (2026-09-11), hit while writing `class_list` and routed around; `entries()` has no other caller in std — which is why it survived |
| [B311](items/B311.md) | NEW — MISCOMPILE: `Style::pseudo` accepts a name containing `:` (the slot key's own delimiter — `rule` builds `media:condition:property` and every consumer `split(":")`s it), so an ENCLOSING combinator emits a WEAKER selector: `attribute("data-open", Some("true"), pseudo("hover:not(:active)", S))` → `.sX[data-open="true"]:hover` with the `:not(:active)` GONE (and the property field reads `not(`, so `without_covered`'s family resolution is computed against garbage); pre-existing, and LIVE IN KOLT: `styles.vl:48–49` write `pseudo("hover:not(:active)", ..)` and `pseudo("focus-visible:not(:active)", ..)` — not biting only because nothing wraps them. The fence (`:` refused in `pseudo`'s name, as `check_attribute_name` refuses it) is BREAKING for kolt and needs the spelling that replaces it: A89's `not` negates the ENCLOSING condition, so "hovered AND not active" (two pseudo-classes in one slot) is unspellable today — `hover(not(active(S)))` means `:not(:hover):active` | bug | styles-33's finds F1 + F2 (2026-09-11); the raw-`pseudo` escape is what kolt reached for; one item because the fence cannot land before the spelling does |
| [B312](items/B312.md) | NEW — generated METHOD-name collisions in a `[service]` expansion are unhandled, and B295's qualification cannot help because the clash is a DUPLICATE DEFINITION: `[rpc] fun verify` → "'verify' is already defined for 'EchoClient<T>'" (1 error), `contract_hash` / `dispatcher` → 3 errors each, `connect` / `connect_with` / `with_handlers` → 1 each (`dispatcher_for` is clean — it lives on the trait impl), all anchored inside generated code on the struct; wants a refusal in the method's own vocabulary naming the reserved set | bug | rpc-smalls-33's find F1 (2026-09-11), B295's sibling |
| [B313](items/B313.md) | NEW — a SUSPENDING `async fun f(&mut self)` `[rpc]` method now takes TWO refusals: B287's expansion-time refusal and E3's `async_view_parameter_message` ("an async function cannot take '&mut' parameters"), both true, different vocabularies; deduping needs a channel from expansion-time diagnostics into the analyzer's stand-down set, which does not exist today | bug | rpc-smalls-33's find F2 (2026-09-11); B189/B236's one-mistake-one-report house rule |
| [B314](items/B314.md) | NEW — `pkg` and `Self` are INDEXED AS REFERENCES to entities they are not: `pkg` (the origin segment inside a package's own sources) is recorded against the package ROOT module, whose name is the package's; `Self` against the trait it stands for — both survive `narrow`'s exact check by LENGTH COINCIDENCE (`pkg`/`std`, `Self`/`Wire`), so a rename of a 3-letter package or a 4-letter trait REWRITES them and breaks the build, and at any other length the row is dropped and rename refuses (the safe half of the same bug); ~40 rows across `vilan/std/src` | bug | editor-33's find F3 (2026-09-11), found by E158's widened invariant pin (`KEYWORD_SPELLED_ROWS` enumerates the two breaches so a third reds); the fix belongs at the analyzer's RECORDING site — the index has no source text at build time |
| [B315](items/B315.md) | NEW — the duplicate-inherent-member rule now reads a bound's ARGUMENTS only for two BARE binders (A86's `subjects_collide`), so a blanket pair whose bounds OVERLAP but are not identical (`impl type S: Source<type I>` and `impl type S: Source<Option<type I>>` on a hypothetical `Option: Source`) would both be admitted, and tier 1 of method resolution takes the first inherent candidate UNRANKED — 'no silent pick' is weaker than it was; not reachable today (no `Option: Source` in std) | bug | solver-33's find F1 (2026-09-11); the pins `a86_two_blankets_bounded_*` and `subjects_collide`'s doc comment are the evidence |
| [B316](items/B316.md) | NEW — `reconcile_type(Trait, Concrete)` REFUSES what every value-first position accepts (B4 / method-resolution.md §10): measured at 18 correct programs when a pass reported on the parameter-first drop (B306's rejected backstop — `Doubler`'s supertrait defaults read "Expected Add, but got Doubler"), so any future 'report where the binder gave up' instrument is unbuildable until the arm is settled; and B304's real discriminator was receiver-resolution TIMING (a chain receiver defers, a parameter receiver freezes the one-shot slot) — the other `is_unknown_closure_parameter` doors may be schedule-dependent the same way | design | solver-33's finds F2 + F3 (2026-09-11); B4 was declined (trait-objects.md) — this is the reconcile arm's residue, not trait objects |

## C. Memory model

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [C1](items/C1.md) | `Weak<T>` | design | |
| [C2](items/C2.md) | Dynamic rule-4 remainder | design | |
| [C13](items/C13.md) | a closure over a view parameter escapes through a storing callee | bug | pinned ignored; spec §6.9's honesty limit |

## D. Documentation

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [D5](items/D5.md) | Public traction plan | design | |

## E. LSP & tooling

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [E37](items/E37.md) | bindgen v2 — the remainder | feature | |
| [E62](items/E62.md) | NEW — Zed language extension | feature | |
| [E69](items/E69.md) | NEW — attribute-NAME completion in an element head is a semantics decision | feature | |
| [E99](items/E99.md) | NEW — the `-d` dump's `.parse.out` is the post-desugar tree, and no dump shows the raw parse | process | |
| [E121](items/E121.md) | NEW — the editor-latency mandate: <10 ms keystroke path, <500 ms errors | design | owner-set target; ARC, paper first |
| [E155](items/E155.md) | NEW — PRE-EXISTING, confirmed on f0f4e301: on kolt's generated `src/lucide/lib.vl` `vilan fmt` rejoins a hand-broken four-link `.child` ladder into one over-budget line and then breaks INSIDE an element argument (`.child(<path⏎ d(…)⏎/>)`), which reads worse than the source; visible only because kolt excludes `src/lucide` from its fmt gate | editor | fmt-30's find 2 (2026-09-08) |
| [E163](items/E163.md) | NEW — the TextMate scope tests assert REGEXES, not scopes: E161's five pins beside `grammar_sync` check each rule's regex and the two rule ORDERS that decide the outcome, but nothing runs VS Code's tokeniser over the exhibit — that needs `vscode-textmate` + `vscode-oniguruma` as extension devDependencies (neither is in `editors/vscode/package-lock.json`; 41 packages, none textmate), a NEW dependency the lane stopped on | editor | smalls-32's open Q3 (2026-09-11); rec: add both as devDependencies — it is the only thing that can assert a scope rather than a regex; E161's unverified half closes with it |
| [E164](items/E164.md) | NEW — the TextMate `generics` rule swallows a CLOSING TAG straight after text: `<span>hello</span>` paints `</span>` as `meta.generic.vilan` with generic delimiters (its begin is `(?<=[A-Za-z0-9_])<` and `hello` ends in an identifier character) — the commonest markup shape in the language, mis-coloured, invisible to E161's regex pins; and the `>` closing an OPENING tag WITH attributes (`<div class="row">`) is painted `keyword.operator.vilan` because the terminator rule is line-start only | editor | editor-33's finds F1 + F2 (2026-09-11), both found by the E163 tokeniser prototype in its first run — the argument that E163 is worth its CI price |
| [E165](items/E165.md) | NEW — scope completion on a BLANK LINE inside a function offers only globals: `fun main() { let start = 1; ▮ }` does not offer `start` while `st▮` does — `scope_at` falls back to the nearest entity BEFORE the offset and lands on the global scope when the line is empty; pre-existing, and it is the moment a user actually asks for completion | editor | editor-33's find F4 (2026-09-11) |
| [E166](items/E166.md) | NEW — `Vilan: Show Language Server Status` reports cardinalities and deliberately no memory; `mallinfo2`'s in-use/free split IS attributable (leak-soak §7.7) and is exactly what located E106's memory (738 → 320 MB in use, 421 MB retained free after closing 18 documents) — put RSS, heap in-use and retained-free on the status page so the next report carries the numbers | tooling | dx-33's find F4 (2026-09-11) |

## G. Macros & const

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [G2](items/G2.md) | Const-eval tail | feature | |
| [G9](items/G9.md) | NEW — a workspace member's own `[build] run` never runs, and nothing says so | design | |
| [G12](items/G12.md) | NEW — `read_dir_all` fuel charged on the result, not the walk | design | audit 4; the basis is the question |

## I. Collections

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [I2](items/I2.md) | Fixed-arrays tail | design | |
| [I3](items/I3.md) | Iterator adapters — the remainder | feature | |

## J. Concurrency

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [J4](items/J4.md) | Free-spawn lint | design | |
| [J5](items/J5.md) | Async recorded opens — the deferred pair | feature | |

## K. Web presence (site, playground, docs delivery) — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [K5](items/K5.md) | The design language — adopt | feature | |
| [K8](items/K8.md) | Website features & small visual upgrades | feature | |
| [K13](items/K13.md) | NEW — the docs on the vilan framework, the port proper — behind its markdown prerequisite | feature | |
| [K14](items/K14.md) | NEW — the playground's buffers carry no prelude; examples teach the old spelling | design | v0.40.0 deploy's find; OWNER QUESTION |

## L. Release engineering & beta — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [L3](items/L3.md) | std tier sweep | design | |
| [L8](items/L8.md) | Contribution scaffolding | design | |
| [L15](items/L15.md) | NEW — release artifacts are checksummed but unsigned | feature | |
| [L16](items/L16.md) | NEW — `std::markdown`'s ~20 strict-parse refusals enter the diagnostics ledger | process | |
| [L18](items/L18.md) | NEW — the pages repo is the one repo in the fleet with unpinned workflow actions | process | |
| [L20](items/L20.md) | NEW — L19's step 2, the owner's own: the Windows test leg run natively on the WSL2 host (`powershell.exe -c "cargo nextest run --workspace"` with a Windows toolchain), the one leg that is compile-only locally — three seals running, Windows had the last word | process | the owner's 'whenever' (2026-09-04); a `scripts/ci-windows-host.sh` wrapper once it works |

## M. Performance & footprint — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [M10](items/M10.md) | NEW — mechanize the BASE_CACHE transmute's completeness claim | perf | |
| [M12](items/M12.md) | NEW — the corpus leak-soak passes in 0.005 s asserting nothing when its corpora are absent | perf | |
| [M17](items/M17.md) | NEW — cross-subject body sharing, M16's residual | perf | separate decision |
| [M18](items/M18.md) | NEW — a function attribute marking a bundle boundary | design | owner-proposed; lucide the exhibit |
| [M19](items/M19.md) | OPEN — an unchanged package module is re-analyzed every LSP analysis / HMR round; T1a–T1d landed (Order 30), next M55/M56/M57 | perf | E106's prime hypothesis; lucide's 636 KB |
| [M27](items/M27.md) | NEW — `lsp-index` editor tables 110–584 ms per keystroke, outside analyze and every tranche — MEASURED (e126): on the phase line after `capture_landed`, with `lsp-landed`; the per-module fix half stays | performance | m19-paper's find; measurement half landed Order 27 |
| [M36](items/M36.md) | NEW — the base cache is process-global and in-memory, so one process per corpus program re-analyzes std: a 3.5–5 s floor per test (infer differential 12 → 47 s, release 56 s) | performance | hygiene-27's find; N49 paid this bill silently |
| [M54](items/M54.md) | NEW — `LastUse`/`source_of` residuals from perf-30's re-pricing: the restored tables are CLONED into `LastUse::compute`'s walk state (8.9M Ir per analysis — needs `&mut Analyzer` or a read-through layer); `collect_unfollowable_loans` is 68% of the warm `LastUse` (whole-program by nature, a loan crosses modules — a record DESIGN); `Program::source_of` is still a linear scan of ~60 ranges and now the residual in `arrival_by` (`source_lookup` exists but is not the default); and `compute_shared_read_bindings` (B267) adds a second whole-program `collect_written_roots()` walk beside the capture pass's | performance | perf-30's and rule1-30's finds (2026-09-08) |
| [M55](items/M55.md) | NEW — module-level SHARING across the platform axis: M52's union key was refused because kolt's browser leg is 82% of all base time and no seed union crosses the platform; the node legs load 48 and 49 sources from disjoint seeds but the FILES overlap — sharing a module's analyzed world across legs (and platforms where coloring allows) is where kolt's remaining base-cache headroom is | performance | perf-30's M52 conclusion and open question 2 (2026-09-08); OWNER Q — M19's next tranche or its own item |
| [M56](items/M56.md) | NEW — R10's residue after T1d is the ENTRY's share of the sweep: 24,459 of 153,302 sites still walked, because the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole and ids minted after the world was stored fall in no range — removing it needs a source→entity index, §3.5's missing module boundary | performance | m19-t1d's find 2 (2026-09-08); the next tranche's shape |
| [M57](items/M57.md) | NEW — the corpus replay differential is BLIND to the R10 seam (and to T1b's): the golden corpus as modules declares almost no resource and none in a container, so a dropped-keys plant leaves `replay_differential` green — an R10 probe (a `List<Guard>` in every third module) would make the standing gate see it; it means editing a shared test function, which needs an owner's word or a lane that owns the file | performance | m19-t1d's find 1 and open question 1 (2026-09-08); OWNER Q |
| [M59](items/M59.md) | NEW — build-30's two follow-ups: persist the leg RECORD into `dist/` so a plain `build` gets the schedule's edges (and the artifact-edge guard's redo) from the second build on, not only under `--watch`; and give the HMR round's own compile loop (`run --watch`, ~main.rs:1340) the same tiering — it writes `dist/` after the whole loop, so the guard's shape differs | performance | build-30's open questions 1 and 2 (2026-09-08) |
| [M60](items/M60.md) | NEW — `SignalCell::get()` and `set()` DEEP-COPY the value (`__clone` over the whole container) and nothing at the source says so: `let x = shared.read()` clones, `shared.read().len()` does not, and the rule is invisible — it is what made `KeyedSource::apply` O(N) per patch on the receiving end of an O(change) protocol until keyed-31 moved it to `update` | performance | keyed-31's find 2 (2026-09-09); rule 1's copy elision is the machinery |
| [M62](items/M62.md) | NEW — small integers cost 4 bytes in BINARY frames: A82's `i8`/`u8`/`i16`/`u16` ride the `i32`/`u32` visitor lanes (deliberate, documented at wire.vl — six new methods on both traits, both closure records and both codecs was the alternative); a width-accurate framing is the candidate optimization if the binary codec ever wants it | perf | wire-32's find F5 and open Q1 (2026-09-11); the owner may prefer the six methods now — say so and it moves to A82's residue |
| [M63](items/M63.md) | NEW — the language server RETAINS ONE FULL ANALYSIS PER OPEN DOCUMENT: ~48 MB per kolt file, 126 MB for `views.vl`/`client.vl`/`channel.vl` each, 864 MB with kolt's 18 `src/*.vl` open — the growth E106 measured and the owner's field reading (VmRSS 4.13 GB after 8 h under the VS Code host, VmPeak 6.64 GB); bounding it (only the FOCUSED document keeps its `Program`; the others keep their editor tables and re-analyze on focus) is a design question | perf | dx-33's find F2 (2026-09-11), E106's successor; OWNER Q: the retention policy (focused-only, an LRU of N, or a byte budget) |
| [M64](items/M64.md) | NEW — glibc never TRIMS what a closed document gives back: closing kolt's 18 documents returned 418 MB to the allocator's free list and 62 MB to the OS; `malloc_trim(0)` on document close would return it, but needs `libc` as a REAL dependency of vilan-lsp (dev-only today) — a dependency ruling | perf | dx-33's find F3 (2026-09-11); OWNER Q (the dependency) |
| [M65](items/M65.md) | NEW — `vilan_ide::completion::CompletionIndex::build` asks `Program::source_of` 14,580× / 7.5 M Ir per warm analysis — the identical M27/M58 hoist (`source_lookup`, the binary search that is answer-identical by construction) not yet taken there | perf | dx-33's find F1 (2026-09-11); editor-33's file, so filed rather than taken |

## N. Hygiene & rot — NEW SECTION

| ID | Title | Kind | Discussion |
|----|-------|------|------------|
| [N16](items/N16.md) | NEW — the recurring codebase audit | process | |
| [N20](items/N20.md) | NEW — `.claude/` is live configuration that no gate can see, and it had three dead pointers in it | process | |
| [N23](items/N23.md) | NEW — 37 `pub` items in `vilan-core` are never referenced outside their own file | process | |
| [N35](items/N35.md) | NEW — `hmr_css_matrix` reads the bundle while the watcher writes it | bug | load-dependent race, seen once |
| [N47](items/N47.md) | NEW — an output-asserting docs form (`vilan,run` + transcript) | process | docs-law's proposal |
| [N66](items/N66.md) | NEW — harness notes from Order 30: the replay harness drives kolt's client clean only with `pkg_root = <repo>/src` (the manifest's `root`) — the repo root gives 22 spurious "cannot find … in the imported path" errors and 39 sources instead of 69, a trap for anyone measuring a real package; `reused_source` and `container_site_visited` are two spellings of one predicate at two granularities | hygiene | m19-t1d's finds 4 and 5 (2026-09-08) |
| [N67](items/N67.md) | NEW — two rulings the hygiene lane left: does N63's "every on-disk cache under `dist/.cache/`" include `dist/.build-hooks.json` (a stamp under build-hooks §3.3 Q2, deliberately not moved), and build-hooks.md:1035's Q5 (`generated` as a second module root "since a subdirectory cannot be imported today") is overtaken by A65 — the generated root can be an ordinary subdirectory of `root` (`src/lucide/*.vl` resolves as `pkg::lucide::*`; the manifest key `generated_root_problem` still exists) | process | hygiene-30's open question and N64's reported note (2026-09-08); OWNER Qs on the build-hooks paper |
| [N74](items/N74.md) | NEW — `css_style_import_note` is N69's identical twin: same `.or_else` chain in analyzer.rs, same B270 seed change, same false steer ("add `import std::style::style;`" can only fire for a std with no `style` item, where that import misses too); `styling.rs`'s own comment already says it is unreachable from a well-formed std | hygiene | hygiene-33's find F1 (2026-09-11); the same deletion as N69, one pin |
| [N75](items/N75.md) | NEW — two DOM stubs remain outside N73's consolidation: `mount_missing_id.rs` (a `format!` stub, braces doubled) and `hmr_swap.rs` — both `StubElement` near-copies; folding them in means their binaries' gates (one is a watch test); and the Rust DERIVE fallback (`derive_impl_source`'s Json/Wire split) is N70's twin, UNPINNED — it fires only for a std lacking `json.vl`'s macros, so no shipped path reaches it: tombstone it the same way or give it a fixture-std pin | hygiene | hygiene-33's find F2 and rpc-smalls-33's find F3 (2026-09-11) |
| [N76](items/N76.md) | NEW — `ROWS_THE_ENUMERATION_CANNOT_REACH` (diagnostics_ledger.rs) is keyed by row NUMBER, so a lane's `NEW` row cannot be listed there safely (it has no number until integration, and a second lane's `NEW` row would make the exemption name the wrong row) — key the list on the row's KEY; and `LegArtifact.bundled`'s doc claims a skipped watch round "still re-copies" its bundled files, which both skip paths deliberately do not (unreachable before B276, so nobody could notice) | hygiene | hygiene-33's find F3 and dx-33's find F5 (2026-09-11) |
