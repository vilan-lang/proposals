{"date": "2026-09-07", "order": "Order 29", "closes": [
{"id": "N58", "lane": "hygiene-29", "commit": "8b80a64e", "text": "all four: E128's pin imports `PartialEq` from `std::compare` (its control had been an impl of an unresolved trait); `.gitignore file-corpus-*/` with no Drop guard (the harness never learns the suffix — N54's hazard); docs fences a STATED EXCLUSION in `leg_vilan_fmt` (152 of 208 fences would reflow, 1,062 lines, and the extractor lives in a test target); `transport_robustness` picks its port at SPAWN through one `free_port` in `tests/support/port.rs`, four private copies dropped. Find: corpus programs inherit the test process cwd (N59)"},
{"id": "B244", "lane": "checker-29", "commit": "986a03ac", "text": "two generic parameters were never the condition — a conditional impl whose parameter binds a CONSTRUCTOR-HEADED type containing a generic (`List<Option<T>>` fails with one). `emit_instance` REPLACED the enclosing substitution and stranded the inner parameter; it COMPOSES now. The worse half was a silent MISCOMPILE: `write_type_key` spelled an unresolved nested generic as its binder id, so every instantiation of the outer function shared one emission (`Delta<str, str>` narrated through the `Delta<str, i32>` body wrote `z` for `\"z\"`). `send_patch` drops its workaround; five pins, four red first; changelog family miscompile"},
{"id": "B252", "lane": "checker-29", "commit": "c278542e", "text": "`annotation_slots_behind` gained an `Expr::Call` arm offering the callee's declared return type id (a named function reaches its call as an `Expr::Local`), so both of B182's consumers stand a refused return annotation's uses down; four pins, three red first"},
{"id": "B243", "lane": "checker-29", "commit": "0ce66681", "text": "`inherited_default_bindings` bound only the `with` clause's trait; it walks `trait_with_supertraits_at` (B164's argument-carrying walk) now, so a one-block `impl X<type T> with Signal<T>` types `map`/`effect_on_change`'s closure parameters as `T`. A49's split recipe (kolt's StorageSignalCell) is no longer required; five pins, the split form pinned green beside the one-block form"},
{"id": "B254", "lane": "checker-29", "commit": "d7312cd6", "text": "`dispatch_candidates`' known-receiver `OnType(Some(_), member)` arm uses the subject narrowing a49-async gave the default-body arm (`default_body_candidates` → `trait_subject_candidates`); `call_graph`/`init_order` lose bogus edges (unobserved by any pin — N62). Not observable through `assert_compiles_without_async`; the field-escape refusal is the witness"},
{"id": "M42", "lane": "m19-t1c", "commit": "f49008e9", "text": "PREMISE CORRECTED by measurement: `plan_resource_drops`'s cost is `resource_reaching_roots` (M28's per-body enrolment gate, 950–1,230 of 1,095–1,385 ms), and every `TypeId`-carrying row is in the cheap half — the record is `drop_roots: Vec<Id>` per module, no type crosses; restore condition a u64 fingerprint of the resource-reaching nominal set. Gate walks 154 of 3,398 bodies on kolt's client (load-independent); `plan_resource_drops` 4,729 → 627 ms cold/warm, leg warm CPU 5,875 → 5,615 ms (debug, loadavg 97–190). Foreign-touch decisions documented in place: `check_hmr_transfer_bounds` re-runs, `compute_capture_clone_sites` is residue, `build_drop_glue` stays live, `compute_resource_types` is not restorable in principle (a Type-keyed memo measured, unseparable from noise, left unshipped). Paper section in analysis-reuse/editor-latency on proposals main. Successors: M48 (R10 = T1d), M49"},
{"id": "B247", "lane": "smalls-29", "commit": "d1675611", "text": "PREMISE WRONG: `print(\"{1 + 2}\")` is a PLAIN string with no holes (string-interpolation.vl:6 pins it); the i-string leg already interpolates every operator shape (twenty probed) and lexical.md §3.4 already reads `hole = '{' expression '}'`. The adjacent real gap shipped instead: a hole that is not an expression ended at the offending byte and the body scan resumed INSIDE the hole (six diagnostics for `i\"{if c { 1 } else { 2 }}\"`) — `refuse_malformed_hole` states it once at the hole's `{` and resyncs on the matching `}`. Six pins; no ledger row (a `ParseErrorReason::Rule` statement, row 229)"},
{"id": "B249", "lane": "smalls-29", "commit": "1c5d6676", "text": "`declaration_type_label_for` falls through to `trait_argument_substitution` (the trait's constraint ids zipped onto the impl's `with` arguments, rendered through `pretty_print_type` so nested positions substitute): `impl Counted with Source<i32>` is told `|i32| void`. Five pins, three red first; the diagnostic HEAD still names the trait unparameterized (B260)"},
{"id": "B248", "lane": "smalls-29", "commit": "e04911e9", "text": "RULED refuse, Rust's rule: a block-like statement is complete, so `match x { .. } + 1` at a statement head is refused with `BLOCK_LIKE_STATEMENT_IS_COMPLETE` and a steer to parenthesize (one diagnostic, replacing the bare parse error); scoped to `Token::Op` minus `! - & *`, `<`/`>` excluded for element syntax, postfix `.` excluded. Six pins. The same head OUTSIDE statement position and the postfix chain are B259"},
{"id": "B250", "lane": "smalls-29", "commit": "604ecb65", "text": "RULED agreement per leg: a dependency file opened as the entry keeps its `pkg::` remap and SAYS so (`entry_as_dependency_module_note`, ledger row 400); `EntryMode::Declared { declared_entries }` refuses a sibling's import of another declared program with B226's own message. Six pins, four red first"},
{"id": "B257", "lane": "rule1-29", "commit": "c8d63a4f", "text": "MISCOMPILE, root found: `type_id_of_expr` answers None for a bare `Expr::Local` (a read interns no type; only its declaration does), so `compute_clone_sites`' Assignment arm dropped every plain local on the right of `=` while `b = a[0]` copied one line away. `consider` now takes the position's DECLARED type with `place_value_type_id` as the fallback (not `place_value_type_id` alone: 84 `let` sites would have lost their clone). Six pins in borrows.rs (the live list, the struct, the dead source still elided, the binding control, `h.write() = c`, `List::insert`); 17 goldens regenerated, each new `__clone` at an assignment from a live place; census 283 → 311. Also fixed by the same root: `List::insert`'s `self[index] = value` wrote the CALLER's aggregate into the receiver's slot, the same class in `Arena`'s three slot writes and `SignalCell::set`, and iterator folds aliasing a list element into the accumulator. Residue: an `own` parameter also stored by assignment inside the callee copies twice (no std site)"},
{"id": "B255", "lane": "rule1-29", "commit": "070b1dcc", "text": "covered by B257 with NO std change — `row_items.write() = list` copies because a closure parameter is a live place a parameter never elides — so `reconcile`'s `old_items` is a snapshot: both symptoms pinned red-first in ui_rows.rs (the `remove` through `update` threw `index out of bounds: the length is 5 but the index is 5` verbatim; the in-place element write missed its render line), `bind_each_by` pinned too. Kolt's `set(filter)` workaround is no longer needed"},
{"id": "E145", "lane": "editor-29", "commit": "e0d67f10", "text": "RULED preserve, collapse, extend: an import alias is an entity of its own (`Program::import_aliases` + `import_alias_spans` read from the SOURCE after resolution; the scope still binds the target) — rename at the definition moves the path segment and keeps the alias, rename at the alias renames the alias and its uses; references.rs INVARIANT 1 un-excepted. Red-first found the worse bug: an alias of a different LENGTH than its target had NO references at all (`greet as hi` → `[]`, rename `NotAnIdentifier`); E142's five-letter fixture hid it. `as` in both grammars as a contextual word; the `::` line rule extended to type paths and struct-literal heads (row 390 reused); the formatter's `import a::b as b;` collapse in fmt-29 (ffa69aea; census zero). Residue: type-position aliases (B264)"},
{"id": "E148", "lane": "editor-29", "commit": "afe7dc7f", "text": "`Func::signature_end` (after the return type and any `borrows`, before `{`) → `Program::function_signature_end_spans`; the quickfix fires on B242's BOUNDARY refusal, finds the innermost enclosing `fun`, refuses one that already carries a clause, and inserts ` context s` / ` context (a, b)`; four pins across a bare `fun`, a return type, `borrows`, and files. One shared QuickFix construction — `book_sync` counts title literals"},
{"id": "E149", "lane": "editor-29", "commit": "0065ac30", "text": "all four: operator hover through `binary_op_dispatch` (a native operator answers nothing); `Document::references` filters DERIVED_SOURCE rows as `references_across` does; a shorthand caret renames the LOCAL in both declaration orders; `diagnostics_budget`'s ignore reason records its load dependence. Six pins, five red first"},
{"id": "E146", "lane": "fmt-29", "commit": "bb5c88df", "text": "all four printer rules, each its own commit and pins: a bare `self` heads its import group (`BranchKey::SelfLeaf`; an aliased `self` keys by its text); an `if` arm reached mid-line stays an expression (a `match` leg is a measured line now — 133f0bba); the `context` clause closes the signature after `borrows` (`canonicalize_declaration_clauses` in `normalize`); a field's comment flushes to the field's VALUE and forces the split form. The reformat regenerated at integration over the merged tree: 81 files, lines over 100 columns 150 → 107 (of the rest, most hold a string literal)"},
{"id": "E147", "lane": "fmt-29", "commit": "c2593131", "text": "a binary chain over the budget breaks at the LOWEST-precedence operator, operator-leading, one level in; one precedence tier is one chain, parentheses survive; TWO operators is the threshold (one link is not a chain — E137); an `if` condition takes the split permission. Six pins incl. both meta.vl lines and a fixed-point sweep. Residue: count versus a MEASURED threshold (markdown.vl:537 at 118), `for`/`while`/`match` subjects — E150"},
{"id": "B251", "lane": "solver-29", "commit": "664f8732", "text": "UNSOUND accept refused, two halves: B186's `derive_generics_from_bounds` runs at the struct literal over `literal_param_ids` (`Held<T, S: Signal<List<T>>>` grounds `T`), and a new `check_written_nominal_bounds` (sites recorded in the `prepped_type_locals` drain, asked after `build()`, the bound grounded by sibling arguments) refuses `Held<i32, SignalCell<List<str>>>` (ledger row 401). Estate census ZERO over the four bounded structs. Nine pins in the new bounds.rs. Residue: enums and traits reach the same drain unchecked (B262); the impl-body form (B263)"},
{"id": "B245", "lane": "solver-29", "commit": "b6f8ca5a", "text": "a defaulted parameter's written mention shared a MEANING with the parameter, not an id (`Add<B = Self>`'s `b: B` is the type `Self` spells); `defaulted_parameter_mentions` records the identity and B216's `written_parameter_argument` workaround is DELETED; conformance reads `trait_with_supertraits_at`; `trait Ord with Eq + PartialOrd` regressed mid-lane and is pinned. The Mixer program compiles end to end; seven pins, four red first"},
{"id": "B246", "lane": "solver-29", "commit": "56586c69", "text": "RULED census first, close on a clean estate — the census is ZERO (std, 128 corpus programs, examples, benchmarks, macro_std, templates; kolt 3 entries, website 4): `refuse_operator_right_operand_on_bound` refuses a concrete right operand against a bounded parameter (ledger row 402), steering to `<P: Add<i32>>`; an implicit binder is offered the written-out rewrite, not a name it lacks. B233's deliberate boundary pin superseded; nine pins, five red first"},
{"id": "A52", "lane": "rpc-29", "commit": "9e72b3bb", "text": "five of six: `RpcError` derives `PartialEq` (0bddc3f4); `impl RemoteSource<type T> with Source<Option<T>>` — `Option` because `get` cannot invent a `T` before the first frame; `sub`/`effect` overridden onto the counted lease; `on_change` attaches BEFORE taking the lease or the in-process seed is lost (3ddcb319); `Reject::Unavailable` → 503 from the app's own `authorize`, A47's frame eligibility restated as `tells_the_client` = did the APP decide (e4976c1f); bare `connect_socket(url)` offers `vilan-rpc` (1b32e385). The generic source field STOPPED with both spellings pinned red for a structural reason — `ServiceItem` carries no generics and the contract hash is built from WRITTEN types, so a `Store<Task>` client would connect to a `Store<Note>` server — and is its own item (A53). A mirror is still not a `Source<List<T>>` (`bind_each` takes `or([])`, pinned as the boundary — A55)"},
{"id": "A51", "lane": "rpc-29", "commit": "9e72b3bb", "text": "RULED the attribute argument: `[expose(keyed = K)]` names the key type (the one attribute argument that is a type); `Map<K, V>` keeps the bare form with its hash unmoved; the generated and the hand-wired `expose_keyed` channels pinned frame-for-frame; ledger row 399 edited in place, rows 403–404 new; grammar `field` production updated. CPU MEASURED (`getrusage`, loadavg 128–169): 1,000 rows 1.08 ms/change/connection, 10,000 rows 8.83 — linear in the collection and in connections. The incremental diff NOT built, rightly: the O(N) scan is irreducible while the source is `List`-valued; the fix is a `Delta`-producing cell (A54)"},
{"id": "M47", "lane": "perf-29", "commit": "676a7aa8", "text": "`List::remove`/`insert` are intrinsics over `splice` (`Intrinsic::ListRemove/ListInsert`, `__remove_at`/`__insert_at` keeping `__at`'s guard and wording because `splice` clamps past the end and reads negatives from the end; interpreter twins). Emitted-program CPU at 10,000 elements: `remove(0)` 13.92 → 2.52 µs, `remove(mid)` 6.84 → 1.44, `insert(0)` 11.73 → 1.81; six-element lists ~0.02 µs SLOWER (`splice` allocates its return; `copyWithin` measured at 483 µs and rejected). Nine pins; clone census 283 → 278. Residue: the intrinsic table binds by NAME (B265)"},
{"id": "M43", "lane": "perf-29", "commit": "2e73e4c9", "text": "the interpreter's environment maps on `fx`: `sip.rs` 80.4M Ir (1.469%) → 9.2M (0.171%), whole check −1.66% on a cold kolt client. The item's \"under 0.1%\" is NOT met and cannot be: the residual is `content_hash`/`load_package_module`, SipHash by design. Order pins under `VILAN_HASH_SHUFFLE`"},
{"id": "M44", "lane": "perf-29", "commit": "5867db52", "text": "`base_cache_admit` claims on a miss, waits on a claimed key, releases by `Drop` (120 s bound, cycle-free, macro worlds re-entrant by thread id): a four-leg generated workspace goes parallel Ir −13.5%, the parallel CPU penalty +15.6% → 0.0%, two legs' `base` phase 31/35 → 0 ms. INERT for kolt, whose three legs have three distinct std seed sets and so three keys (M52). `build` stays serial — its parallelism audit is M51"},
{"id": "M46", "lane": "perf-29", "commit": "18bcd757", "text": "the 8M-row bound is a BYTE budget: `ModuleTables::bytes` + `CheckedCacheState` in M24's shape (LRU, 64 MiB = `BASE_CACHE_DEFAULT_BUDGET/8`), eviction instead of clearing; a std-only world measures 85,712 B, `std::router` 140,536 B, so the 256-key bound binds first at these sizes. m19-t1c's `drop_roots` rows counted in the flat sum (resolved at integration). Residue: M24's own denomination (M50)"},
{"id": "M32", "lane": "compile-perf-29", "commit": "b89e091c", "text": "both slices: `size_of::<Node>()` 320 (`Func` alone 312) → `Box<Func>` on `Node::Func`/`MacroFun` → 144, exhibit Ir −6.09%, kolt client −3.18% (75c86c5c; a `node_size.rs` ceiling pin); the five hot `get_type_by_type_id` callers to the borrowing twin, −0.81%/−0.80% (only 112,732 of the 1.1M clones reached the allocator). Next ceilings 136/120/96, then `Node::Call` at 88 where boxing trades a memcpy for an allocation (M53)"},
{"id": "M33", "lane": "compile-perf-29", "commit": "93564019", "text": "the four macro worlds are a phase-line row (`[vilan phase] macro-worlds 4 …`, 7a850800; `vilan_core::macro_worlds_compiled()` counted at the compile site) and a cross-process table `dist/.macro-expansions` keyed on `MacroDef::world_key` with a header of toolchain version + `macro_std` hash: a warm kolt client check −10.7% Ir / −15.8% CPU, the 0.568e9 removed matching the item's estimate. Nine pins. `macro_world_cache_clear()` had silently stopped forcing a compile once the key became world-free — fixed, and the rule stands: a short-circuit added ahead of a cache invalidates the clear. Precompiling std's worlds at install time not taken. `check` now creates `dist/` (N63)"},
{"id": "A50", "lane": "ruling", "commit": "ruling", "text": "RULED as designed: no read-only wrapper for `bind_each_by`'s row cell until trait objects exist, and `Selector` stays a handle with `.of(key)` (a closure spelling needs a context-carrying closure type). Nothing to build"},
{"id": "B253", "lane": "ruling", "commit": "ruling", "text": "RULED keep the refusal: a bare trait in return position is the existential where every other bare-trait position is universal, vilan has no opaque-type kind, and B252's fix removed the cascade that was its strongest practical argument. `C<A>` stays unwritable. No build"}
]}
