# Order 29 — integration notes (running; the sweep reads this)

## hygiene-29 — landed first (4 commits @ 8b80a64e), merged with --allow-edit on document.rs's E128 pin import
- N58 all four: E128 pin import fixed (the pin's control was an impl of an unresolved trait — corrected claim); `.gitignore file-corpus-*/` (no Drop guard: the harness never learns the suffix, a glob would race siblings — N54's hazard); docs fences = STATED EXCLUSION in `leg_vilan_fmt` (152/208 fences would reflow, 1,062 lines, 91 of them an aligned trailing comment lifted to its own line; extractor lives in a test target); transport_robustness port picked at SPAWN via one `free_port` in tests/support/port.rs (document/split/init/asset_bundle dropped their copies).
- FINDS (to file or note at the sweep): `wait_for_port` duplicated three ways with three signatures; corpus programs inherit the test process cwd (crates/vilan-core) — a per-run `current_dir` in `run_node_within` would end the pollution but changes relative-path resolution for three gates (integrator's call → item).
- Brief error of mine: `-p vilan-cli --test corpus_harness` does not exist; the binary is `--test corpus` (harness source lives in vilan-core/tests/corpus_harness/). Several briefs carry the wrong name; lanes verify names by rule.

## m36 — spike only, nothing built (vilan m36 @ c40716b1: world_cache_spike.rs 3 ignored measurements + tooling changelog; proposals m36 @ 9b5c363 merged to main: analysis-reuse.md §6.15)
- Serializing the analyzed world NOT feasible: four address-keyed structures (macro_item_invocations/macro_expression_expansions/macro_failed_sites keyed by AST node address; MacroRegistry::blocks_by_module; MacroDef::world Arc<World>; GeneratedItems::nodes &'static). Inputs+reconstruction buys only the 90 ms parse share (resolve is 210 ms = 2× parse).
- Floor re-measured: cold 400 / warm 210 / floor 190 ms (loadavg 13.8); true per-process first 490 / hit 160. infer_differential 93.8 s CPU/131 procs, release 100.7 s/130. Whole vilan-core 5,089 tests 3,949 s CPU (loadavg 86).
- FINDS: (1) M24's budget denominated ~24× LOW — `base_cache_retained_bytes` records 195,087 B for a world that weighs 4,722,688 B resident; 512 MiB recorded ≈ 12 GB resident → ITEM. (2) M36 is SUITE-WIDE: every test process pays 240–330 ms, 5,089 × 240 ms ≈ 1,221 s ≈ 31% of suite CPU (vs 86 s for the two differentials) → M36 status update, reframed. (3) fork()-based warm-base harness needs no relocation (COW) but nextest owns the spawn → note on M36 as the shape that fits.
- CI saving if a cache shipped: a few percent of wall (legs are compile-dominated) — not the reason to build.
- M36 stays OPEN with §6.15 as its design; the flag was never built.

## checker-29 — all four landed (986a03ac B244, c278542e B252, 0ce66681 B243, d7312cd6 B254), merged third
- B244: the transformer's `emit_instance` REPLACED the enclosing substitution with a constructor-headed bound type (`Delta<K,T>`, `Option<T>`, `Map<K,V>`), stranding the inner parameter → now COMPOSES; and `write_type_key` spelled an unresolved nested generic as its binder id so every instantiation of the outer fn shared ONE emission — a SILENT MISCOMPILE (`Delta<str,str>` narrated through the `Delta<str,i32>` body → invalid JSON). rpc.vl `send_patch` workaround removed. Golden reactive-flatten.mjs moved by name numbering only. → RELABEL the changelog entry's family fix → miscompile at integration; the tombstone says "two generic parameters" was the WRONG condition — one parameter suffices (`List<Option<T>>`): a conditional impl whose parameter binds a constructor-headed type containing a generic.
- B252: `annotation_slots_behind` gained an `Expr::Call` arm (callee's declared return type id, reached via `Expr::Local`).
- B243: `inherited_default_bindings` now walks `trait_with_supertraits_at` (B164's walk) — the split recipe is no longer required (kolt untouched; A49's tombstone recipe obsolete → note at the sweep).
- B254: `dispatch_candidates` known-receiver arm uses the subject narrowing (`default_body_candidates` → `trait_subject_candidates`).
- FINDS/open: (1) `current_substitution` now accumulates outer bindings across nested instance emissions — cost unmeasured, guarded by RecursionGuard (note on M-list? probably nothing). (2) B254's edge removal in call_graph/init_order is unobserved — no pin asserts a removed edge (small N item). (3) `assert_compiles_without_async` cannot observe the known-receiver miscoloring — the field-escape refusal shape is the only witness (note in the tombstone).

## m19-t1c — M42 landed (vilan m19-t1c, 5 commits: f49008e9 plan_resource_drops restored; d117d924/eed38bd0 foreign-touch docs; proposals m19-t1c paper section merged to main), merged fourth
- PREMISE CORRECTED: `plan_resource_drops`'s cost is `resource_reaching_roots` (M28's per-body enrolment gate, 950–1,230 of 1,095–1,385 ms); every TypeId-carrying row is in the cheap half → the record is `drop_roots: Vec<Id>` per module, NO type crosses. Restore condition = u64 fingerprint of the resource-reaching nominal set. New counter `drop_plan_stats::asked_roots()`.
- Decisions: `check_hmr_transfer_bounds` RE-RUNS (would carry a minted TypeId; 100–165 ms, inert without std::dev); `compute_capture_clone_sites` = RESIDUE (foreign-touch: reads whole-program `collect_written_roots()`); `build_drop_glue` stays live (TypeId-keyed output); `compute_resource_types` NOT restorable in principle (interned-table key space) — a Type-keyed memo written, measured at loadavg 110+, unseparable from noise, LEFT UNSHIPPED (comment records the lever).
- Numbers (debug, loadavg 97–190): plan_resource_drops 4,729 → 627 ms cold/warm; gate walks 154/3,398 bodies (load-independent claim); leg warm CPU 5,875 → 5,615 ms median. World-reuse 58/58. Release re-measure on a quiet box wanted before quoting the leg figure.
- FINDS → items: (1) R10 `check_container_resource_arguments` now the largest single line item (477–1,113 ms warm); `reported_container_structures` is a HashSet<String> structural key, recordable, but its diagnostics sit OUTSIDE T1's Class A window → moving the window is a tranche (M42's successor, "T1d"). (2) the nominal fingerprint includes ENTRY-declared nominals → two entries differing in a resource declaration invalidate each other's record (missed optimization; split world- vs entry-declared). (3) `LastUse::compute` 258–558 ms warm after T1b — re-price. (4) `drop_roots` adds rows to CHECKED_CACHE's counter → M46's byte budget must count them (tell perf-29 at its merge / note on M46).

## smalls-29 — all four landed (d1675611 B247, 1c5d6676 B249, e04911e9 B248, 604ecb65 B250), merged fifth
- B247 PREMISE WRONG: `print("{1 + 2}")` is a PLAIN string (no holes; string-interpolation.vl:6 pins it); the i-string leg interpolates every operator shape (20 probed) and lexical.md §3.4 already says `hole = '{' expression '}'`. Shipped instead: `refuse_malformed_hole` (lexing.rs) — a hole that is not an expression used to end at the offending byte and the body scan resumed INSIDE the hole (6 diagnostics for `i"{if c { 1 } else { 2 }}"`) → one statement at the hole's `{`, resync on the matching `}`. No ledger row (ParseErrorReason::Rule, row 229 keyless). Close B247 as "premise wrong; the adjacent gap fixed" with the pins.
- B249: `declaration_type_label_for` falls through to `trait_argument_substitution` (nested positions substitute); message head unmoved.
- B248: REFUSED with `BLOCK_LIKE_STATEMENT_IS_COMPLETE` (steer: parenthesize); scoped to `Token::Op` minus `! - & *`, `<`/`>` excluded (element syntax), postfix `.` excluded. No ledger row (row 229).
- B250: leg 1 note `entry_as_dependency_module_note` (ONE NEW ledger row, tail of the TSV); leg 2 `EntryMode::Declared { declared_entries }` refuses a sibling's import of another declared program with B226's message; manual Default impl.
- FINDS → items/notes: (1) a block-like head of a binary expression outside statement position (`let y = match x {..} + 1;`) gives "expected `;`" + B248's refusal — same steer, different path (small item). (2) `match x {..}.to_str()` still the bare parse error — postfix chain on a block-like head, out of scope (same item). (3) B249's diagnostic HEAD names the trait unparameterized (`'Counted'` not `'Counted<i32>'`) (small E/B item). (4) `ROWS_WITHOUT_A_KEY`'s comment says "nine Rule statements", tree has ~22 → prose-ledger correction at the sweep (row 229's note).

## PROCESS — the fold helper's false positive (smalls-29's merge, first attempt)
- `fold_tests_by_name.py` counted braces per line without regard to string literals; tuples.rs's `a_match_subject_does_not_take_a_struct_literal` holds a raw-string vilan program with an unbalanced brace, so its "function" ran to EOF and any lane appending to the file read as having EDITED it (REFUSED). The lane had not. Fixed at proposals 5693424: the scanner skips `"…"` and `r#"…"#` bodies and `//` comments before counting; verified on base/HEAD/lane (fn 181–198 in both, six new pins listed). Rule: a helper that reads Rust test files is string-aware or it is wrong.

## rule1-29 — B257 + B255 landed, B256 HELD (c8d63a4f B257 miscompile; 47d7a599 B256 pins only, ignored, no source; 070b1dcc B255 pins), merged sixth
- B257 ROOT: `type_id_of_expr` answers None for a bare `Expr::Local` (a read interns no type; only the declaration does) → the Assignment arm's `let Some(type_id)` dropped every plain local on the right of `=`; `b = a[0]` (Index interns) copied one line away. Fix: `consider` takes the position's DECLARED type (a binding's `variable.type_id`) with `place_value_type_id` as the fallback; every store position asks the same way. (Not unified on `place_value_type_id` alone: 84 `let` sites would have LOST clone sites.) 17 goldens regenerated, each new `__clone` at an assignment from a live place; census 283 → 311.
- Also fixed by B257 (FINDS, in the tombstone): `List::insert`'s `self[index] = value` wrote the CALLER's aggregate into the receiver's slot (a miscompile of its own); the same class in `Arena` (three `__at(self[0], index)[1] = value` slot writes) and `SignalCell::set`; iterator folds aliased a list element into the accumulator (`total = item` in sum/product) — all copy now.
- B255: covered by B257 with NO std change (`row_items.write() = list` copies; a closure parameter is a live place a parameter never elides); three ui_rows pins red-first (both removes threw the verbatim message; the edit pin missed its render line); `bind_each_by` pinned.
- B256 HELD — the ruling (read result = place) implemented and measured, then NOT landed: std copies deep aggregates PER NOTIFY (`bind_each`'s `__clone(row_views.v)` + `__clone(row_owners.v)` per notification; `drain`'s `__clone(turn[0].v)` per wave of a list cleared the next line). CPU (node, loadavg 101–104, medians of 9): bind_each 500 rows/200 updates +20%; A44 selector +12%; batch drain +25%; raw `get()` 1000×2000 +344%. kolt client 136,205 → 136,331 B (158 → 172 clones). Three ignored pins carry the numbers (`ci_ignored_pins` green). THIRD OPTION found: both hot std sites are reads whose source dies or is only walked — rule 2 (last-use elision) is local-only and cannot see a cell; a CELL-AWARE last-use would make the ruling nearly free → my recommendation to Reed: build that first (new item), then re-measure and land B256. B256 stays OPEN with the numbers and the three options.
- Open: an `own` parameter also stored by assignment inside the callee copies twice (no std site today) — note on B257's tombstone.
- perf-29 interaction: M47 rewrites `insert` as an intrinsic with `own value` — the `own` store copies at the call site, so the semantics B257 fixed must hold; check at perf-29's merge (list.vl conflict expected).
- PROCESS (rule1-29's merge): `vilan/test/reactive-flatten.mjs` was moved by checker-29 (name renumbering) and rule1-29 (three `__clone(value)` writes in `SignalCell::set`); regenerated from the merged compiler (`target/debug/vilan build vilan/test/reactive-flatten.vl`) — equals the lane's modulo names, differs from HEAD by exactly the three clones. borrows.rs folded by name (10 new pins) with the string-aware helper.

## fmt-29 — DONE, HELD for the LAST merge (8 commits: bb5c88df self-first, c4f29232+133f0bba inline if-arms + match-leg measured, 0bc36b2c context after borrows, aa6459fc field comment, c2593131 E147 chain break, ffa69aea E145 collapse, 942c5ee1 THE REFORMAT — drop and regenerate over the merged tree)
- Merge procedure: merge origin/fmt-29 WITHOUT 942c5ee1 (cherry-pick the seven rule commits, or merge then `git revert`/drop the reformat), then build the CLI from the merged tree and run `target/debug/vilan fmt` over the tree as ONE final commit; the fmt gate (`scripts/ci-local.sh vilan-fmt`) must be green.
- Reformat figures on the lane: 81 files, +403/−284; over-100 lines 198 → 145 (132 of the 145 hold a string literal, 5 are comments; +7 new over-100, 6 string literals + markdown.vl:537 at 118). Kolt exhibit (read-only scratch): 10 of 25 files reformat, ONE new over-100 line (a SQL literal) outside lucide.
- Rules: E146 all four (self heads its group — `BranchKey::SelfLeaf`; an `if` arm reached mid-line stays an expression, match legs measured; `context` after `borrows` via `canonicalize_declaration_clauses`; a field's comment flushes to the field's VALUE and forces the split form); E147 lowest-precedence operator-leading break, TWO operators the threshold, an `if` condition takes the split permission; E145's `as b` collapse (census zero in the tree).
- FINDS/open → items or rulings: (1) E147 threshold count vs MEASURED (markdown.vl:537 stays at 118 under count; measured flips a pin to 105, still over budget) — owner Q, my rec: keep count. (2) `match` leg `=> { expr }` still expands (same shape one construct over) — small item. (3) `for`/`while` conditions and `match` subjects lack the split permission — no instance in tree; note. (4) release_emission.rs:377 carries `Option::{ None, Some, self }` inline (non-canonical, unreached) — hygiene one-liner.

## editor-29 — all three landed (e0d67f10 E145, afe7dc7f E148, 0065ac30 E149), merge after rule1-29
- E145: `Program::import_aliases` (entity id, name, span, target) + `import_alias_spans` read from the SOURCE after resolution; scope still binds the target (typing/dispatch/codegen untouched). WORSE BUG FOUND: an alias of a different LENGTH than its target was invisible — `greet as hi` gave `hi()` NO rows (references `[]`, rename `Err(NotAnIdentifier)`); E142's `greet as hello` (5→5) hid it. INVARIANT 1 un-excepted. E142's two collapse pins REPLACED (assert overturned behaviour) → expect the fold to see deleted/edited fns in the lsp test file; take the lane's version. `as` in both grammars (TextMate + docs/theme/vilan.js) + `CONTEXTUAL_WORDS`; `::` rule extended to type paths and struct-literal heads (ledger row 390 REUSED, no NEW rows). `DefinitionKind::Alias` new (user-visible "cannot rename the import alias `x`…").
- E148: `Func::signature_end` → `Program::function_signature_end_spans`; quickfix on B242's BOUNDARY refusal inserts ` context s` / ` context (a, b)`; one shared QuickFix construction (book_sync counts title literals — a duplicate literal reds it).
- E149: operator hover via `binary_op_dispatch` (native answers nothing); `Document::references` filters DERIVED_SOURCE; shorthand caret renames the LOCAL both orders; diagnostics_budget reason records load.
- FINDS → items/rulings: (1) type-position aliases not fully split (`let x: Bar` records `type_references` against the target; go-to-def at an alias use resolves through the target) — ruling wanted: what go-to-def at an alias should do. (2) `service_layer.rs:1608` `a_refused_client_reports_unauthorized_without_burning_the_retry_budget` is a 45 s WALL-CLOCK bound on a whole `vilan run .` (52.56 s at loadavg 184; 5.88 s at 59) — N item: the wall bound goes, or declines under load (M27's rule). (3) `declaration_source` needed an alias arm (alias ids minted after the file walks) — in the tombstone. (4) open: should find-references from the DEFINITION side also list alias-reached uses (informational)? Lane took the narrow reading (one index for rename and references) — my rec: keep narrow, note on E145's tombstone.
