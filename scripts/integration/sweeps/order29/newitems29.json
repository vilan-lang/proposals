{"items": [
{"id": "B258", "section": "B", "kind": "bug", "title": "NEW — MISCOMPILE: overriding a trait DEFAULT (`map`) in a sub-trait impl (`impl RemoteSource<type T> with Source<Option<T>>`) reddens A25's owner pins — a call on a `SignalCell` FIELD that INHERITS the default reaches `register_with_owner` with an undefined owner (`self[0].v.push` on undefined); \"overriding a default the receiver's own field type inherits\" is the breaking shape", "discussion": "rpc-29's find (2026-09-06); `map`/`or` kept INHERENT on the mirror as the workaround, so a generic `S: Source<…>` calling `.map` on a mirror gets the owner-optional default instead of A25's strict law", "status": "OPEN — lane rpc-29's find (2026-09-06, Order 29)", "area": "Type system & the type solver", "body": "Reproduce with the mirror's override restored (the doc comment in rpc.vl names the shape), find the dispatch that routes the field's inherited default through the OVERRIDE's body (or the override through the field's owner), fix, and pin both: the field call with the override present, and a user sub-trait impl overriding a default its own field type inherits."},
{"id": "B259", "section": "B", "kind": "bug", "title": "NEW — B248's siblings: a block-like head of a binary expression OUTSIDE statement position (`let y = match x {..} + 1;`) reports \"expected `;`\" plus B248's refusal (same steer, different path); a POSTFIX chain on a block-like head (`match x {..}.to_str()`) is still the bare `found '.' expected an expression`", "discussion": "smalls-29's finds 1 and 2 (2026-09-06); the postfix case was left out of B248 because skipping the `.` turns the member into a bare name and cascades", "status": "OPEN — lane smalls-29's find (2026-09-06, Order 29)", "area": "Type system & the type solver", "body": "One refusal for both spellings (the steer is the same: parenthesize), one diagnostic each, no cascade; pin the `let` form, the argument form, and the postfix form beside B248's."},
{"id": "B260", "section": "B", "kind": "bug", "title": "NEW — the missing-member diagnostic's HEAD names the trait UNPARAMETERIZED: `does not implement trait 'Counted'`, never `Counted<i32>`, even where the arguments are what the refusal is about (B249 fixed only the suggested declaration)", "discussion": "smalls-29's find 3 (2026-09-06)", "status": "OPEN — lane smalls-29's find (2026-09-06, Order 29)", "area": "Diagnostics & the ledger", "body": "Render the `with` clause's arguments in the head through the same `trait_argument_substitution`; the ledger row's text moves (an edit in place, not a NEW row); pin `Source<i32>` and a two-parameter trait."},
{"id": "B261", "section": "B", "kind": "bug", "title": "NEW — `Type::Generic`'s pretty-print for an IMPLICIT binder (B186's sugar) is the trait's name, so row 346's head can still read \"`Add`'s `add` accepts `Add`\" after B218; only the steer could be worked around (B246's refusal names the written-out rewrite instead)", "discussion": "solver-29's find 1 (2026-09-06); B186's mint needs a display of its own — B184's `C<A>` face is the precedent", "status": "OPEN — lane solver-29's find (2026-09-06, Order 29)", "area": "Diagnostics & the ledger", "body": "Give the implicit binder a display (the bound's trait plus a marker, or the parameter's position) and route every operator/dispatch head through it; pin row 346's head on the implicit form and B246's steer."},
{"id": "B262", "section": "B", "kind": "bug", "title": "NEW — B251's written-argument bound check is STRUCT-only: enums and traits reach the same `prepped_type_locals` drain with the same unchecked written arguments (`Held<i32, SignalCell<List<str>>>`'s enum twin is still accepted)", "discussion": "solver-29's find 2 (2026-09-06); a one-arm extension with its own census", "status": "OPEN — lane solver-29's find (2026-09-06, Order 29)", "area": "Type system & the type solver", "body": "Extend `check_written_nominal_bounds` over `enums` and `traits`; census the estate (expect zero as for structs); pins per kind plus a satisfied control."},
{"id": "B263", "section": "B", "kind": "bug", "title": "NEW — `derive_generics_from_bounds` inside an IMPL body: `impl Held<type T> { fun first(self): T { self.list.get()[0] } }` reports \"cannot index this List: its element type is never determined\" — the impl binder's `T` is not recovered through the sibling parameter's bound the way B251 now recovers it at the literal", "discussion": "solver-29's find 3 (2026-09-06); separate from B251's two halves and unaffected by them", "status": "OPEN — lane solver-29's find (2026-09-06, Order 29)", "area": "Type system & the type solver", "body": "Run the bound-driven recovery when an impl body reads a field typed by a bounded sibling parameter; pin the exhibit and a generic method control."},
{"id": "B264", "section": "B", "kind": "design", "title": "NEW — TYPE-position import aliases are not fully split after E145: `import Foo as Bar; let x: Bar` records `type_references` against the target (the alias span catches it by spelling, so references narrow correctly) while go-to-definition and hover at such a use resolve THROUGH the target — a ruling on what go-to-definition at an alias should answer", "discussion": "editor-29's find 1 (2026-09-06); correct today, not deliberate; also editor-29's open Q: should find-references from the DEFINITION side list alias-reached uses informationally? (the lane took the narrow reading — one index for rename and references)", "status": "OPEN — lane editor-29's find (2026-09-06, Order 29); OWNER Q", "area": "Language server & tooling", "body": "Decide: go-to-definition at an alias use lands on the alias declaration (one hop, then the target) or on the target; and whether definition-side references list alias-reached uses. Then record type-position aliases as alias entities like value-position ones and pin hover, go-to-definition and references at a type-position alias."},
{"id": "B265", "section": "B", "kind": "bug", "title": "NEW — the intrinsic table binds `remove`/`insert` (and `pop`, `push`) by NAME on any `List` impl declaration, external or not: a vilan-bodied `List::remove` is silently replaced by the intrinsic (found when an old-std A/B emitted the NEW lowering); std-only today, but a std author cannot give an intrinsic's name a vilan body", "discussion": "perf-29's find 1 (2026-09-06)", "status": "OPEN — lane perf-29's find (2026-09-06, Order 29)", "area": "Codegen & emission", "body": "Bind intrinsics to `external fun` declarations only (a bodied declaration of the same name keeps its body), or refuse a bodied declaration of an intrinsic name with a message; pin both."},
{"id": "B266", "section": "B", "kind": "bug", "title": "NEW — a GENERIC `[service]` subject has no curated refusal: it cascades through generated code (first line `'contract_hash' is already defined for 'StoreClient<T>'` plus `` `Store` takes 1 type argument, 0 given ``)", "discussion": "rpc-29's find (2026-09-06); the diagnostic A53 owes whichever way A53 is ruled", "status": "OPEN — lane rpc-29's find (2026-09-06, Order 29)", "area": "Diagnostics & the ledger", "body": "One refusal at the attribute naming the subject's parameters and the reason (A53's), ledger `NEW` row; pin the cascade gone."},
{"id": "B267", "section": "B", "kind": "design", "title": "NEW — B256's third option: rule 2 (last-use elision) is LOCAL-only and cannot see a `Shared` slot, yet both of std's hot per-notify reads (`bind_each`'s `row_views.read()`/`row_owners.read()`, `drain`'s `turn[0].v`) are reads whose source dies or is only walked — a CELL-AWARE last-use would make the B256 ruling nearly free (+12–25% CPU on the reactive pins today, +344% on a raw `get()`)", "discussion": "rule1-29's find (2026-09-06); the recommended path to landing B256: build this, re-measure, then land the place treatment", "status": "OPEN — lane rule1-29's find (2026-09-06, Order 29); the B256 unblocker", "area": "Memory model (rule 1 / rule 2)", "body": "Extend liveness to a `Shared` slot read whose cell is not written again before the binding's last use (the cell's own writes are the aliasing hazard; a `write()` or `set`/`update` in between refuses the elision), then re-run rule1-29's three ignored B256 pins and its CPU table; land B256 if the reactive pins come back under ~5%."},
{"id": "A53", "section": "A", "kind": "design", "title": "NEW — a GENERIC source field for `[expose]`/`[service]` (A52's split-off): `ServiceItem` (meta.vl) carries no generics so the expansion cannot spell `impl Store<type T>`; the contract hash is built from WRITTEN types so every instantiation hashes identically (a `Store<Task>` client would connect to a `Store<Note>` server); `Client::connect(url, codec)` has nowhere for the element type to come from — three answers wanted before a build", "discussion": "rpc-29's investigation (2026-09-06), both spellings pinned red (f162e63c); B184's trait-typed-field carve-out is the other spelling's refusal", "status": "OPEN — lane rpc-29's find (2026-09-06, Order 29); OWNER design", "area": "Reactive core & UI", "body": "Design: generics on `ServiceItem`; the element type INTO the contract hash (per instantiation) and into `connect` (a typed client constructor); or refuse generic services outright and say so (B266). Then build or close."},
{"id": "A54", "section": "A", "kind": "design", "title": "NEW — a `Delta`-producing CELL for the keyed exposure (A51's split-off): `keyed_diff` re-keys two snapshots and `KeyedSource::apply` reaches `index_of` per op, both O(N) — 1,000 rows 1.08 ms per change per connection, 10,000 rows 8.83, linear in the collection and in connections (`getrusage`, loadavg 128–169) — irreducible while the source is `List`-valued: only the MUTATION knows what changed", "discussion": "rpc-29's measurement and decision (2026-09-06); a new std type with mutators, a `Source` impl, `expose_keyed_cell`, its own attribute spelling, an op-log trimming policy and a `Reset` fallback", "status": "OPEN — lane rpc-29's find (2026-09-06, Order 29)", "area": "Reactive core & UI", "body": "Build the cell (`KeyedCell<K, T>` with `insert`/`remove`/`update(key)` producing `Delta<K, T>` ops), expose it through the keyed channel without re-keying, trim the op log by lease, `Reset` when a subscriber lags; pin bytes AND CPU per change flat in N at 1,000 and 10,000 rows."},
{"id": "A55", "section": "A", "kind": "design", "title": "NEW — the mirror seams A52 left: `KeyedSource` is still not a `Source` (A52 covered `RemoteSource` only), and a `RemoteSource<List<T>>` is a `Source<Option<List<T>>>`, so `bind_each` still takes `or([])` — a `Source<Option<List<T>>>` sibling for `bind_each`, or the boundary stays deliberate", "discussion": "rpc-29's finds and open Q (2026-09-06); the boundary is pinned and documented today", "status": "OPEN — lane rpc-29's find (2026-09-06, Order 29); OWNER Q on the sibling", "area": "Reactive core & UI", "body": "`impl KeyedSource<type K, type T> with Source<…>` on the same lease pattern as the mirror; decide the `bind_each` sibling (recommend: no sibling — `or([])` says what an unseeded list renders as, which a binding should have to say)."},
{"id": "A56", "section": "A", "kind": "design", "title": "NEW — two rpc rulings after A51/A52: `[expose(keyed = K)]` over a `Map<K2, V>` whose argument disagrees with `K2` — today the argument silently wins and the mismatch surfaces as a plain type error at the mirror (refuse at the attribute?); and A48's `authorize_timeout` still answers 429 now that `Reject::Unavailable` is 503 (std refusing on the app's behalf is not the app's judgement — re-rule or leave)", "discussion": "rpc-29's open questions (2026-09-06)", "status": "OPEN — lane rpc-29's questions (2026-09-06, Order 29); OWNER Qs", "area": "Reactive core & UI", "body": "Recommend: refuse the disagreeing key at the attribute (one ledger row); keep 429 for the timeout (it is not the app's decision) and say so on A48's tombstone."},
{"id": "M48", "section": "M", "kind": "performance", "title": "NEW — M19 tranche 1d: R10 `check_container_resource_arguments` is now the largest single item on the phase line (477–1,113 ms warm on kolt's client, comparable to the enrolment gate T1c removed); its whole-program state `reported_container_structures` is already a `HashSet<String>` structural key, id-free and recordable — the blocker is that its diagnostics sit OUTSIDE T1's Class A recording window, so restoring it means moving that window", "discussion": "m19-t1c's find 1 (2026-09-06); a tranche of its own", "status": "OPEN — lane m19-t1c's find (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Widen the Class A window to cover R10's diagnostics (or record R10's under a second window keyed the same way), record `reported_container_structures` per module, restore per reused module; the replay differential and the 58/58 pin are the gate; measure the leg before/after with loadavg."},
{"id": "M49", "section": "M", "kind": "performance", "title": "NEW — m19-t1c's residuals: the resource-reaching nominal FINGERPRINT includes entry-declared nominals, so two entries in one package that differ in whether they declare a resource invalidate each other's enrolment record (split world-declared from entry-declared); and `LastUse::compute` still costs 258–558 ms warm after T1b — the restore may not reach as much as assumed (re-price)", "discussion": "m19-t1c's finds 2 and 3 (2026-09-06)", "status": "OPEN — lane m19-t1c's find (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Two small slices under M19: split the fingerprint; profile `LastUse::compute` warm on kolt's client and say what the restore misses."},
{"id": "M50", "section": "M", "kind": "performance", "title": "NEW — M24's byte budget is denominated ~24× LOW: `base_cache_retained_bytes` records 195,087 B for a retained world that weighs 4,722,688 B resident (eight distinct keys minted via `macro_limits`, minus a same-key control, byte-identical across three runs) — 512 MiB of recorded bytes is on the order of 12 GB resident; the cache is bounded, but not where M24 thinks", "discussion": "m36's find 1 (2026-09-06); M46's `ModuleTables::bytes` is a floor by design — this is the base world's own accounting", "status": "OPEN — lane m36's find (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Measure what the retained-bytes counter misses (the leaked ASTs, the interned tables, the macro worlds hanging off the registry), correct the denomination or the budget, pin the world's weight against the counter within a factor."},
{"id": "M51", "section": "M", "kind": "performance", "title": "NEW — `build` in parallel: the AUDIT (perf-29, no code) — the obstacle is one `dist/` and two order-dependent accumulators: `leg_schedule` is a real producer→consumer DAG (parallelism caps at the widest tier); `bundled_names` is mutated per leg and read by every later one (collision blame becomes scheduler-dependent — lock + member-order blame); `explain::leg_facts` is process-global and loop-ordered (M35's capture/replay); `write_assets`/`write_bundled`/`write_chunks` share one directory and `write_chunks` sweeps a namespace (per-leg staging + atomic move, or a `dist/` lock); `watch_state: &mut` must become per-leg results merged after join; `?` aborts at the first failing leg. Smallest honest step: parallelize `compile_unit` within a tier, keep every writer serial in schedule order. `check_single`'s per-platform loop's blocker is the process-global B182 `RoundReports` ledger", "discussion": "perf-29's audit (2026-09-06), M44's `build` half", "status": "OPEN — lane perf-29's audit (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Build the smallest step with the audit as the checklist; measure a three-leg workspace (kolt read-only) CPU and wall with loadavg; pin that collision blame is deterministic under parallel compile."},
{"id": "M52", "section": "M", "kind": "performance", "title": "NEW — M44 is INERT wherever members' std seed sets differ: kolt's three legs have three distinct seed sets and so three base-cache keys, so its residual multi-entry cost is not duplicated worlds — a coarser key (the union of the package's seeds) or M19's module-level sharing is the follow-up that reaches kolt", "discussion": "perf-29's find 3 and open Q (2026-09-06)", "status": "OPEN — lane perf-29's find (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Measure kolt's three legs' seed-set overlap; decide between a package-union key (one world, superset) and module-level sharing; pin the second leg's `base` phase at ~0 on kolt's shape."},
{"id": "M53", "section": "M", "kind": "performance", "title": "NEW — compile-perf-29's smalls on a cold kolt check: `infer_type_path` clones `constraint` on entry 180,670 + 148,029 times (a `Cow` at twenty use sites inside a 1,300-line function, ~0.23%); path handling ~3% (`Components::next` 2.15% + `Path::_starts_with` 0.70%); `const_eval::State::new` 0.69% + 0.68% (a per-evaluation allocation); `Node`'s next ceilings 136 (`Trait`, `StructInitializer`) then 120 (`Element`, `Struct`, `Enum`), boxing all five lands at 96, below which `Node::Call` at 88 is an expression where boxing trades the memcpy for an allocation", "discussion": "compile-perf-29's finds (2026-09-06); each needs a profile DIFF, not a total — Ir drifts ~1% on this tree from unrelated codegen", "status": "OPEN — lane compile-perf-29's finds (2026-09-06, Order 29)", "area": "Performance & footprint", "body": "Four slices, each with a callgrind profile diff on the E121 exhibit and kolt's client; stop at `Node::Call`."},
{"id": "N59", "section": "N", "kind": "hygiene", "title": "NEW — corpus programs inherit the test process's cwd (`crates/vilan-core`), which is how `file-corpus-<n>/` trees strand there on a crash; a per-run `current_dir` in `run_node_within` would end the pollution at the source but changes relative-path resolution for all three gates on the shared harness", "discussion": "hygiene-29's open question (2026-09-06); the `.gitignore` line landed as the interim", "status": "OPEN — lane hygiene-29's find (2026-09-06, Order 29)", "area": "Hygiene & rot", "body": "Give `run_node_within` a per-run scratch cwd and pass the program's own directory explicitly where a gate resolves a relative path; run the three gates; drop the ignore line."},
{"id": "N60", "section": "N", "kind": "hygiene", "title": "NEW — smalls from Order 29: `wait_for_port` is duplicated three ways (`document`, `split`, `init`) with three signatures — N58's `free_port` consolidation, not done; `release_emission.rs:377` carries `Option::{ None, Some, self }` as an inline fixture, non-canonical under E146 (unreached by `vilan fmt`); `diagnostics_ledger.rs`'s `ROWS_WITHOUT_A_KEY` comment still says row 229 is \"the nine `ParseErrorReason::Rule` statements\" — the tree has ~22", "discussion": "hygiene-29's, fmt-29's and smalls-29's finds (2026-09-06)", "status": "OPEN — Order 29's finds (2026-09-06)", "area": "Hygiene & rot", "body": "Three one-line edits; the prose ledger's row 229 note corrected at the same time."},
{"id": "N61", "section": "N", "kind": "hygiene", "title": "NEW — `service_layer.rs:1608` (`a_refused_client_reports_unauthorized_without_burning_the_retry_budget`) bounds a whole `vilan run .` (build included) at 45 s WALL: 52.56 s at loadavg 184, 5.88 s at loadavg 59 — a wall-clock gate on a shared box, `diagnostics_budget`'s class", "discussion": "editor-29's find 2 (2026-09-06); nothing in that lane touched rpc", "status": "OPEN — lane editor-29's find (2026-09-06, Order 29)", "area": "Hygiene & rot", "body": "Bound the retry budget's claim in CPU or in retry COUNT, not wall; or decline under load (M27's rule). Sweep the tree for other wall bounds over a build."},
{"id": "N62", "section": "N", "kind": "hygiene", "title": "NEW — two accuracy gains left unobserved by any pin: B254's subject narrowing removes bogus `call_graph`/`init_order` edges (strictly more accurate, nothing asserts an edge that should disappear); B244's `current_substitution` now ACCUMULATES outer bindings across nested instance emissions (constraint ids are per-declaration so the sets are disjoint; the cost of a deep monomorphization chain is unmeasured)", "discussion": "checker-29's open questions (2026-09-06)", "status": "OPEN — lane checker-29's find (2026-09-06, Order 29)", "area": "Hygiene & rot", "body": "One pin asserting a removed edge on the a49 shape; one measurement of the substitution depth on the deepest corpus instantiation, with a bound if it matters."},
{"id": "N63", "section": "N", "kind": "process", "title": "NEW — `vilan check` now CREATES `dist/` (M33's `dist/.macro-expansions` lives there), where it previously wrote nothing; compile-perf-29 gitignored `vilan/benchmarks/dist/` and `vilan/test/dist/` after a cache file was staged by accident — where the on-disk caches live (`dist/.cache/`, a per-package `.vilan/`, or the build dir only for `build`) is the owner's call", "discussion": "compile-perf-29's open question (2026-09-06); M36's design (§6.15) would put a second cache beside it", "status": "OPEN — lane compile-perf-29's question (2026-09-06, Order 29); OWNER Q", "area": "CLI & workflow", "body": "Recommend `dist/.cache/` for every on-disk cache (one ignore line, one place to delete), `check` allowed to create it; pin that `check` writes nothing else."},
{"id": "E150", "section": "E", "kind": "editor", "title": "NEW — fmt-29's residuals: E147's break threshold is a COUNT (two operators) — `std/src/markdown.vl:537` stays at 118 columns because its last operand is a two-operand `\\|\\|`; a MEASURED threshold (break when the widest resulting line is narrower) reaches it but flips `a_single_link_right_operand_leaves_the_line_long` (116 → 105, still over budget); a `match` leg body written `=> { expr }` still expands where an `if` arm now stays inline; `for`/`while` conditions and a `match` subject do not take the split permission an `if` condition now has (no over-budget instance in the tree)", "discussion": "fmt-29's finds and open questions (2026-09-06); OWNER Q on the threshold — my recommendation: keep the count", "status": "OPEN — lane fmt-29's finds (2026-09-06, Order 29); OWNER Q", "area": "Language server & tooling", "body": "Rule on count versus measured; extend the inline-arm rule to a `match` leg and the split permission to the other conditions with pins; one reformat commit if the tree moves."}
]}
