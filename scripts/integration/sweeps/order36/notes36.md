# Order 36 — running record (integrator)

Opened 2026-09-14 off 9b22ec36 (Order 35's sealed tip; nothing landed since). Ten lanes: visibility-36 (B318 S4 + B335/B336/B338), sweep-36 (B318 S6 + S5 docs; merged LAST), style-36 (A95 S2/S3/S5 + B322), lang-a-36 (A99, A98, B334), lang-b-36 (B340 → G24 → B333), lang-c-36 (A100 S1 → S2), editor-36 (E180, E181, E175–E179), perf-36 (M70, M55, M66), hygiene-36 (N81–N85, B337, B339; first), paper-native-36 (C14 + F1 papers; no build). Rulings R1–R10 as recommended (briefs36.md). Ledger next id 464. Tracker 94 open.
- PROCESS (GO): no items to file or close at GO (every item exists); the chronicle OPENING entry appended; worktrees created off origin/next = 9b22ec36; ten Opus agents launched; kolt READ-ONLY for every lane (already curated for S6; its nine A99 sites are the integrator's at the sweep, R7).
- Watch-outs from Order 35: run long gates DETACHED (`setsid nohup … &` + a waiter); lanes must commit as the worktree's identity; the merge tools mis-split `"\` string consts (N81 is in this order — until it lands, hand-check every fold); two lanes add rule constants (lang-b, lang-c) — union one line per constant; three lanes move goldens (lang-a, perf, style) and sweep-36 must not.

## paper-native-36 — both papers landed (signal-cell-representation.md 727 lines, native-apps.md 613 lines; probe crate beside them at proposal/native-apps-probe/), FIRST to report (23 min, 98 tool uses); no vilan commits, nothing to merge
- C14: census 154 std sites (rpc.vl 72, reactive.vl 27, browser/ui.vl 22 …) + 13 kolt; classes root 9 / owner-scoped 97 / escaping 29 / frame-scoped 19 (the lane's fourth class); `Shared` today = `__shared_new(value) { return { v: value } }`, clone = identity (transformer.rs:1273/7256/7264). RECOMMENDS (a) counted `Shared` per destruction.md §10 + `Weak` at two std sites; (b) rejected because no owner owns a cell today. Slices S1–S5. The SCC gate (`a_disposed_exemplar_holds_no_reactive_cycle` + heap_cycles.js) is the exit test, NOT leak-soak (brief correction).
- F1 (R10): emit Rust / desktop / third `View` twin / client-first, with reasons; the probe BUILT AND RAN both translations; runtime enumerated (52 intrinsics, ~35 helpers, closures `Rc<dyn Fn>`, async the big unlisted item); slices S0–S6 (S3 the twin is XL). `Target` no longer exists (brief correction).
- FINDS filed as newitems36a: M71 (19 frame-scoped cells → `mut` locals), D8 (shared.vl headnote wrong), F16 (stored+called closure param → `Rc<dyn Fn>`), C15 (§6.9 captures-bindings → shared cells natively), J6 (async on native), C16 (C12 promoted to a native precondition), F15 (three-layer ui rule + N-way parity gate — OWNER RULING), E182 (`infer_platform` binary).
- OPEN Qs for Reed (Order 37 gates): C14's recommendation (a); rule 4 under rustc (`RefCell` + measure); the three-layer rule; `native` not in `@process`; single-threaded executor; `i53` keeps the JS range; C16 blocks S1; `vilan-rt` separate crate. M66 lands as written (perf-36 need not wait).

