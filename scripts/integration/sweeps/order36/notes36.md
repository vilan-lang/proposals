# Order 36 — running record (integrator)

Opened 2026-09-14 off 9b22ec36 (Order 35's sealed tip; nothing landed since). Ten lanes: visibility-36 (B318 S4 + B335/B336/B338), sweep-36 (B318 S6 + S5 docs; merged LAST), style-36 (A95 S2/S3/S5 + B322), lang-a-36 (A99, A98, B334), lang-b-36 (B340 → G24 → B333), lang-c-36 (A100 S1 → S2), editor-36 (E180, E181, E175–E179), perf-36 (M70, M55, M66), hygiene-36 (N81–N85, B337, B339; first), paper-native-36 (C14 + F1 papers; no build). Rulings R1–R10 as recommended (briefs36.md). Ledger next id 464. Tracker 94 open.
- PROCESS (GO): no items to file or close at GO (every item exists); the chronicle OPENING entry appended; worktrees created off origin/next = 9b22ec36; ten Opus agents launched; kolt READ-ONLY for every lane (already curated for S6; its nine A99 sites are the integrator's at the sweep, R7).
- Watch-outs from Order 35: run long gates DETACHED (`setsid nohup … &` + a waiter); lanes must commit as the worktree's identity; the merge tools mis-split `"\` string consts (N81 is in this order — until it lands, hand-check every fold); two lanes add rule constants (lang-b, lang-c) — union one line per constant; three lanes move goldens (lang-a, perf, style) and sweep-36 must not.

## paper-native-36 — both papers landed (signal-cell-representation.md 727 lines, native-apps.md 613 lines; probe crate beside them at proposal/native-apps-probe/), FIRST to report (23 min, 98 tool uses); no vilan commits, nothing to merge
- C14: census 154 std sites (rpc.vl 72, reactive.vl 27, browser/ui.vl 22 …) + 13 kolt; classes root 9 / owner-scoped 97 / escaping 29 / frame-scoped 19 (the lane's fourth class); `Shared` today = `__shared_new(value) { return { v: value } }`, clone = identity (transformer.rs:1273/7256/7264). RECOMMENDS (a) counted `Shared` per destruction.md §10 + `Weak` at two std sites; (b) rejected because no owner owns a cell today. Slices S1–S5. The SCC gate (`a_disposed_exemplar_holds_no_reactive_cycle` + heap_cycles.js) is the exit test, NOT leak-soak (brief correction).
- F1 (R10): emit Rust / desktop / third `View` twin / client-first, with reasons; the probe BUILT AND RAN both translations; runtime enumerated (52 intrinsics, ~35 helpers, closures `Rc<dyn Fn>`, async the big unlisted item); slices S0–S6 (S3 the twin is XL). `Target` no longer exists (brief correction).
- FINDS filed as newitems36a: M71 (19 frame-scoped cells → `mut` locals), D8 (shared.vl headnote wrong), F16 (stored+called closure param → `Rc<dyn Fn>`), C15 (§6.9 captures-bindings → shared cells natively), J6 (async on native), C16 (C12 promoted to a native precondition), F15 (three-layer ui rule + N-way parity gate — OWNER RULING), E182 (`infer_platform` binary).
- OPEN Qs for Reed (Order 37 gates): C14's recommendation (a); rule 4 under rustc (`RefCell` + measure); the three-layer rule; `native` not in `@process`; single-threaded executor; `i53` keeps the JS range; C16 blocks S1; `vilan-rt` separate crate. M66 lands as written (perf-36 need not wait).

## hygiene-36 — all seven landed (N81 tools handed back; N82 164085c5, N83 c24b5263, N84 b339596e, N85 4e8b301d, B337 29e83e8e, B339 346fb70a), second to report (75 min, 212 tool uses); pushed; merge_fold running detached (scratchpad/order36/merge-hygiene-36.log) — first merge
- N81: LANDED in proposals from the lane's scratch — `fold_tests_by_name.py`, `merge_items_by_name.py`, `apply_row_mapping.py` (the union step lives THERE, brief correction) + `merge_tools_harness.py` (11/11 green on the fixed tools, 7/11 red on the originals). Root causes: (a) the item scanners read a Rust string's body lines as column-0 items (fixed by a string/comment-aware `code_line_starts`); (b) the carried-const insertion point was the LAST `use ` line — inside a multi-line `use { }` (the reported symptom); renamed-on-HEAD never resurrected; an edited const now REFUSED like an edited fn (taken under `--allow-edit` — behaviour change: `merge_fold.sh`'s allow list must name edited consts too); a same-named single-line integer const on both sides is SUMMED through the merge base (23+5+6=34 — Order 35's hand answer), anything else keeps HEAD and exits non-zero. Cross-check over 139 real test files: 0 fns lost/gained.
- N82: 13 `temp_dir()` sites in the inference harness → `scratch_root()` under CARGO_TARGET_TMPDIR + `ScratchFile` (Drop-cleaned) + `storage_failure()` naming free space on ENOSPC; 3 pins. N83: four .gitignore lines + `every_asset_a_corpus_build_emits_beside_the_sources_is_gitignored` (asks `git check-ignore`; non-vacuous). N84: `cpu_ratio_allowance_ms()` load-aware bound (fetch 0.008 ms @load 3 → 0.013 @load 90; scan 5.6–11.6 ms); rpc pin bounded settle loop. N85: NINE ledger rows NEW (asset emit/emit_keyed/stage/staged/read/digest/schedule_at_end outside a const + `check_emit_kind`'s THREE — item said two); the three paper edits APPLIED (prelude.md §10.1 re-derived: 194 `pkg::` statements, 49 (25%) prelude-only, all Option/Result sets; css-block.md §15.3 re-anchored on the SHEET 11,986 → 11,020 B — and N85's item text was wrong: `default_panel_style` is a live five-link chain at app_overlay.vl:6; style-conditions.md §8 item 1 follows). B337: wording row 431 re-keyed (`{child}` as spelled on disk) + the NAMESPACE half fixed in the LOADER's request resolution (`deepest_module_or_namespace`, `seed_module`; `longest_namespace_prefix` gained a `floor`) — NOT in `refuse_shadowed_submodules` (brief correction). B339: guard in `macros.rs`'s `expand_item_position` derive arm (`derive_impl_source` no longer exists — N79 deleted it; brief/ownership-map correction) + new binary `derive_world_guard.rs` (plants a derive into a staged boolean.vl).
- Gates by the lane: inference 4079, module_resolution+derive_world_guard+macro_world_census 181, cli batch 90 (release_scripts 22, split 13, corpus 12, examples 10, diagnostics_ledger 19, rpc_http 8, module_paths 14), std full-scan, lsp 757, clippy, fmt; no goldens; no .vl touched.
- FINDS → filed newitems36b: B341 (HIGH — the base-cache key omits desugar-seeded std modules; two-entry package fails to build; handed to perf-36 by message, to land before M70's measurements), N86 (temp_dir sites outside the inference binary), B342 (A67 cannot see an unwalked collision). F3 (the remaining unrowed `Failure::unsupported` family — staging registry, project files, macro-engine limits) is the open decision on N85's closure: widen check (3)'s walk to `Failure::new/unsupported` or row piecemeal — rec at the sweep: widen, one hygiene item.
- OPEN Qs (lane's recs, adopted): Q1 B341 in perf-36 this order (done by message); Q2 keep the edited-const REFUSAL (integrator names consts in allow lists); Q3 do NOT mask the two `modules::` tests with `base_cache_clear()` — they are B341's cheapest reproducer.
- EXISTING fns edited: vilan-lsp `keystroke::gate::the_entity_table_costs_the_edited_buffer_not_the_program`; vilan-cli rpc_http.rs `a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method` (its inline program string); inference support/modules/markdown/bounds/generics/macros harness fns. No program CONST.
- MERGED 262ee97d (hygiene-36, first merge; pushed): clean, no fold, no conflicts; ledger rows 464–472 assigned to the nine NEW (next id 473), row 431 edited in place; changelog parity 420/420; gates green (inference 4079, module_resolution, derive_world_guard, corpus, rpc_http, diagnostics_ledger, lsp, release_scripts, split, fmt).

## editor-36 — all seven landed (E180 a20f36e6, E181 56fcbe74+ee8a457f, E175 ad163380, E176 19253040, E177 3908938f, E178 880f92aa, E179 6ed903e8, + e4e88827 N81-hygiene concat! consts), third to report (125 min, 297 tool uses); pushed; merge_fold running detached (scratchpad/order36/merge-editor-36.log) — merged ahead of lang-a/perf/lang-b/lang-c (no overlapping files)
- E180 (R9 taken): rule (2) subtracts `prelude_bindings`, keeps only RECEIVER-syntax member resolutions (read from `Program::member_name_spans`, written only at `Node::MemberAccessor` — no text fallback needed), and treats a module the prelude module `export import`s as ambient (one-line test: `prelude_bindings` carries the module id). A struct FIELD read through a receiver also drops out (an E169 leak). `ModuleRescue::Keep` + `RescuedImport{Dropped,Narrowed,Verbatim}`; collision guard via a lazily-built `ImportUseContext::names_bound_in` (top-level decls + import leaves/aliases; the prelude deliberately NOT in it). Fade text `REDUNDANT_PRELUDE_BINDS`. No ledger row (LSP paint is unrowed — E173's ruling). KOLT VERIFIED over a copy: 9b22ec36's server rewrites to `import std::option;` and `vilan check` fails at lucide/lib.vl:17453; this lane's server deletes both statements and `vilan check` exits 0. Six pins.
- E181: `Printer::export_all_marker_slot`/`print_export_all_marker`/`module_comment_end`/`take_marker_comments` + `hoist_export_all_markers` (the token net's SIXTH order-insensitivity — a relocation, so a lost/doubled marker still fails). SECOND COMMIT = a real defect found on kolt: the slot is the leading import BLOCK, not `import_run_end`'s sort run (views.vl's `// FIXME:` above an import broke the run → the marker moved up into the imports). The six `vilan/benchmarks/src/*.vl` markers (line 1, above their module comment) reformatted. `insert_import` needed no change (offset 0 already). Ten pins + one insert pin. Brief correction sent to sweep-36.
- E175: `Document::style_impl_texts` walks `program.implementations` for `Style` subjects, reads each `Implementation.source`; fold order std → file → siblings. E172's boundary pin RENAMED and INVERTED (`refactor_inlines_an_impl_style_extension_from_a_sibling_file`).
- E176: one TextMate rule (lookahead declines `(`, `:`, `-`; `.` allowed for `hidden.show(flag)`); blast radius over 257 .vl + 464 doc fences: 10 chars / 1 token (`disabled` in element-syntax.vl); kolt 0. TextMate only (the book theme has no head region).
- E177: `QuickFix.target: Option<FixTarget{path, range}>` converted through the TARGET's line index; three warnings gain "Export `S`"/"Export `hidden`" fixes; four refusals (no file, outside the package root, indented, already exported). Five pins incl. the handler-level one.
- E178: `offered_importables` at THREE consumers (`OriginListing::completions`, `module_member_completions` — a fourth consumer the item did not name, `impl_selector_completions`); `auto_import_completions` NOT reachable without a `Program` surface (OPEN Q1 → asked visibility-36 to expose `exported_entities` + `curated_modules` on `Program`; the vilan-ide wiring is a sweep/Order 37 follow-up).
- E179: `session_trace::BaseCacheSizes{worlds, weight_bytes, budget_bytes}`; E166/E174 pins extended (EXISTING fns edited: `the_summary_orders_by_total_time_and_reports_retained_state`, `the_summary_reports_the_process_memory_and_the_retained_programs`, `the_summary_reports_what_the_editor_tables_cost`, `the_declared_command_answers_the_session_summary`).
- Gates by the lane: lsp 773, core lib 774, module_resolution 175, grammar_sync 25 (CI=true), vscode_extension 9, book_mirrors 2, diagnostics_ledger 17, release_scripts 22, cli batch 168, clippy, fmt, `vilan fmt --check vilan`, kolt copy check 0 + fmt clean. No goldens; no ledger rows.
- FINDS (file at the sweep, newitems36c): (1) the organizer's whole-run deletion leaves the paragraph gap (two blank lines before `export *;` in organized lucide; cosmetic; fmt fixes non-generated files); (2) `module_member_completions` was E178's unnamed fourth consumer (fixed); (3) a cross-file quickfix reads the declaring file from DISK (unsaved sibling → saved offsets; E175's sibling reads too) — a server-side document-map lookup closes it; (4) E177's §4 fix declines when a file names two types with one spelling — an analyzer-recorded (warning site → exposed entity) pair would make it exact (the same Program surface as Q1). Plus the Q1 follow-up: wire `auto_import_completions` on the two Program fields.
- OPEN Q2 (E181): a module comment glued to the marker travels with it — nothing in the estate meets it; rule as the item wrote it.
- Corrections: E181's slot = the leading import BLOCK; E178 has four consumers; E176's hole/tag declines are structural.

## lang-c-36 — A100 S1 177be8de + S2 4454df49 landed, fourth to report (139 min, 369 tool uses); pushed; merge after editor-36 (expect the one-number `RULE_STATEMENT_SITES` conflict with lang-b later — the fixed `apply_row_mapping.py` sums integer consts through the base)
- S1: `Token::Lazy` + `KEYWORD_ROLES` row (Modifier) → TextMate + book theme GENERATED; modifier in the three grammar homes, refused on closures/external fun and beside own/view/mut/spread; `record_lazy_arguments` partitions THUNK vs FORWARD; transformer `__lazy("name", () => …)` / `__force` (interpreter arms same commit; cell `{name, state, value, thunk}`); v1 rejections via existing scans (R9 for a resource local, rule 3 for a view) + `check_lazy_argument_effects` in `post_analysis_passes` (await both spellings; context-reading); laziness agreement position-wise in `check_one_conformance` (B29 landed — brief correction). Hover renders `lazy`. 22 pins in new `inference/lazy.rs`.
- S2: `Node::Let` fifth field; `record_lazy_bindings`; `const database = __lazy(..)`; unused binding emits nothing; `init_order` gives a lazy binding no out-edges (trap at runtime by design); cycle + poison messages verified; HMR `TransferForm::Excluded`; sync half already covered by the module-initializer async refusal (B5). 15 pins + 2 in vilan-cli database.rs. Docs: spec §6.10, §3.4, appendix A.2 (+ `css` which was missing), tour sections, Option/Result page note.
- Gates by the lane: core 5789 (1018 s), lsp+ide 766, cli+wasm+embedded 898, grammar_sync 23, book_mirrors 2, book_sync 14, grammar_ebnf 12, docs 9, markdown_golden 2, release_scripts 22, diagnostics_ledger 17, corpus byte-identical, clippy workspace, fmt. Seven ledger rows NEW; `RULE_STATEMENT_SITES` 34 → 42 (no named constant, CURATED untouched).
- Goldens moved: `vilan/test/reactive-on-change.mjs` (the `lazy` → `quiet` rename only), `markdown_anchors.golden` (+3 headings). EXISTING fns/consts edited: markdown.rs `a52_an_rpc_mirror_is_a_source_and_drives_a_generic_consumer` (rename), reactive_selection.rs const `EAGER_AND_LAZY` (raw string; rename), parse_expr_regression.rs (Node::Let arity), diagnostics_ledger.rs (the count), grammar_sync.rs (KEYWORD_ROLES row), vilan-ide completion.rs (`keyword_lexeme` + `KEYWORD_DOCS`).
- FINDS (file at the sweep, newitems36c): (2) a generic lazy parameter instantiated at a resource slips the type check (declared type read at the declaration; a bare `Expr::Local` argument has no `expr_id_to_type_id_map` entry — B328's comment records the same gap) → item; (3) the effects walk descends into closures the argument merely creates — an `await` inside a created closure is refused (conservative; documented at `ThunkEffects`) → item only on demand; (4) `scan_closure_view_captures`'s `Expr::Call` arm never walks the call SUBJECT (pre-existing; one line; changes a check's firing) → item; (5) lazy bindings vs HMR state (excluded in v1) → design item on demand; (6) spec appendix A.2's reserved words are ungated (was missing `css`) → hygiene item. (1) the paper's grep claim was stale — recorded on lazy.md §8, no item.
- OPEN Qs built as recommended (keep): poison wording; `lazy mut` refused; an unused lazy binding emits nothing; `lazy` first in the prefix order.
- MERGED 97623395 (editor-36, second merge; pushed): clean, no fold, no conflicts with hygiene; no ledger rows (head_max 472); changelog parity 427/427; gates green (lsp, core lib, module_resolution, grammar_sync CI=true, vscode_extension, book_mirrors, diagnostics_ledger, corpus, style_chain_order, release_scripts, split, fmt).
- lang-c-36 merge: two false starts on GATE SPECS (`book_sync` is a vilan-lsp src module under `-p vilan-lsp`, not a binary; `docs` and `markdown_golden` are vilan-CORE binaries) — merge_lane's spec check stopped the chain BEFORE merging each time (exit 8, worktree untouched — the check works); third run in flight with every spec verified against tests/ (allow list: `a52_an_rpc_mirror_is_a_source_and_drives_a_generic_consumer,EAGER_AND_LAZY`). PROCESS: verify `--test` names against `crates/<crate>/tests/*.rs` before launching a merge.

