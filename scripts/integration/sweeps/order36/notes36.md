# Order 36 — running record (integrator)

Opened 2026-09-14 off 9b22ec36 (Order 35's sealed tip; nothing landed since). Ten lanes: visibility-36 (B318 S4 + B335/B336/B338), sweep-36 (B318 S6 + S5 docs; merged LAST), style-36 (A95 S2/S3/S5 + B322), lang-a-36 (A99, A98, B334), lang-b-36 (B340 → G24 → B333), lang-c-36 (A100 S1 → S2), editor-36 (E180, E181, E175–E179), perf-36 (M70, M55, M66), hygiene-36 (N81–N85, B337, B339; first), paper-native-36 (C14 + F1 papers; no build). Rulings R1–R10 as recommended (briefs36.md). Ledger next id 464. Tracker 94 open.
- PROCESS (GO): no items to file or close at GO (every item exists); the chronicle OPENING entry appended; worktrees created off origin/next = 9b22ec36; ten Opus agents launched; kolt READ-ONLY for every lane (already curated for S6; its nine A99 sites are the integrator's at the sweep, R7).
- Watch-outs from Order 35: run long gates DETACHED (`setsid nohup … &` + a waiter); lanes must commit as the worktree's identity; the merge tools mis-split `"\` string consts (N81 is in this order — until it lands, hand-check every fold); two lanes add rule constants (lang-b, lang-c) — union one line per constant; three lanes move goldens (lang-a, perf, style) and sweep-36 must not.

## paper-native-36 — both papers landed (signal-cell-representation.md 727 lines, native-apps.md 613 lines; probe crate beside them at proposal/native-apps-probe/), FIRST to report (23 min, 98 tool uses); no vilan commits, nothing to merge
- C14: census 154 std sites (rpc.vl 72, reactive.vl 27, browser/ui.vl 22 …) + 13 kolt; classes root 9 / owner-scoped 97 / escaping 29 / frame-scoped 19 (the lane's fourth class); `Shared` today = `__shared_new(value) { return { v: value } }`, clone = identity (transformer.rs:1273/7256/7264). RECOMMENDS (a) counted `Shared` per destruction.md §10 + `Weak` at two std sites; (b) rejected because no owner owns a cell today. Slices S1–S5. The SCC gate (`a_disposed_exemplar_holds_no_reactive_cycle` + heap_cycles.js) is the exit test, NOT leak-soak (brief correction).
- F1 (R10): emit Rust / desktop / third `View` twin / client-first, with reasons; the probe BUILT AND RAN both translations; runtime enumerated (52 intrinsics, ~35 helpers, closures `Rc<dyn Fn>`, async the big unlisted item); slices S0–S6 (S3 the twin is XL). `Target` no longer exists (brief correction).
- FINDS filed as newitems36a: M71 (19 frame-scoped cells → `mut` locals), D8 (shared.vl headnote wrong), F16 (stored+called closure param → `Rc<dyn Fn>`), C15 (§6.9 captures-bindings → shared cells natively), J6 (async on native), C16 (C12 promoted to a native precondition), F15 (three-layer ui rule + N-way parity gate — OWNER RULING), E182 (`infer_platform` binary).
- OPEN Qs for Reed (Order 37 gates): C14's recommendation (a); rule 4 under rustc (`RefCell` + measure); the three-layer rule; `native` not in `@process`; single-threaded executor; `i53` keeps the JS range; C16 blocks S1; `vilan-rt` separate crate. M66 lands as written (perf-36 need not wait).

## hygiene-36 — all seven landed (N81 tools handed back; N82 164085c5, N83 c24b5263, N84 b339596e, N85 4e8b301d, B337 29e83e8e, B339 346fb70a), second to report (75 min, 212 tool uses); pushed; merge_fold running detached (scratchpad/order36/merge-hygiene-36.log) — first merge
- N81: LANDED in proposals from the lane's scratch — `fold_tests_by_name.py`, `merge_items_by_name.py`, `apply_row_mapping.py` (the union step lives THERE, brief correction) + `merge_tools_harness.py` (11/11 green on the fixed tools, 7/11 red on the originals). Root causes: (a) the item scanners read a Rust string's body lines as column-0 items (fixed by a string/comment-aware `code_line_starts`); (b) the carried-const insertion point was the LAST `use ` line — inside a multi-line `use { }` (the reported symptom); renamed-on-HEAD never resurrected; an edited const now REFUSED like an edited fn (taken under `--allow-edit` — behaviour change: `merge_fold.sh`'s allow list must name edited consts too); a same-named single-line integer const on both sides is SUMMED through the merge base (23+5+6=34 — Order 35's hand answer), anything else keeps HEAD and exits non-zero. Cross-check over 139 real test files: 0 fns lost/gained.
- N82: 13 `temp_dir()` sites in the inference harness → `scratch_root()` under CARGO_TARGET_TMPDIR + `ScratchFile` (Drop-cleaned) + `storage_failure()` naming free space on ENOSPC; 3 pins. N83: four .gitignore lines + `every_asset_a_corpus_build_emits_beside_the_sources_is_gitignored` (asks `git check-ignore`; non-vacuous). N84: `cpu_ratio_allowance_ms()` load-aware bound (fetch 0.008 ms @load 3 → 0.013 @load 90; scan 5.6–11.6 ms); rpc pin bounded settle loop. N85: NINE ledger rows NEW (asset emit/emit_keyed/stage/staged/read/digest/schedule_at_end outside a const + `check_emit_kind`'s THREE — item said two); the three paper edits APPLIED (prelude.md §10.1 re-derived: 194 `pkg::` statements, 49 (25%) prelude-only, all Option/Result sets; css-block.md §15.3 re-anchored on the SHEET 11,986 → 11,020 B — and N85's item text was wrong: `default_panel_style` is a live five-link chain at app_overlay.vl:6; style-conditions.md §8 item 1 follows). B337: wording row 431 re-keyed (`{child}` as spelled on disk) + the NAMESPACE half fixed in the LOADER's request resolution (`deepest_module_or_namespace`, `seed_module`; `longest_namespace_prefix` gained a `floor`) — NOT in `refuse_shadowed_submodules` (brief correction). B339: guard in `macros.rs`'s `expand_item_position` derive arm (`derive_impl_source` no longer exists — N79 deleted it; brief/ownership-map correction) + new binary `derive_world_guard.rs` (plants a derive into a staged boolean.vl).
- Gates by the lane: inference 4079, module_resolution+derive_world_guard+macro_world_census 181, cli batch 90 (release_scripts 22, split 13, corpus 12, examples 10, diagnostics_ledger 19, rpc_http 8, module_paths 14), std full-scan, lsp 757, clippy, fmt; no goldens; no .vl touched.
- FINDS → filed newitems36b: B341 (HIGH — the base-cache key omits desugar-seeded std modules; two-entry package fails to build; handed to perf-36 by message, to land before M70's measurements), N86 (temp_dir sites outside the inference binary), B342 (A67 cannot see an unwalked collision). F3 (the remaining unrowed `Failure::unsupported` family — staging registry, project files, macro-engine limits) is the open decision on N85's closure: widen check (3)'s walk to `Failure::new/unsupported` or row piecemeal — rec at the sweep: widen, one hygiene item.
- OPEN Qs (lane's recs, adopted): Q1 B341 in perf-36 this order (done by message); Q2 keep the edited-const REFUSAL (integrator names consts in allow lists); Q3 do NOT mask the two `modules::` tests with `base_cache_clear()` — they are B341's cheapest reproducer.
- EXISTING fns edited: vilan-lsp `keystroke::gate::the_entity_table_costs_the_edited_buffer_not_the_program`; vilan-cli rpc_http.rs `a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method` (its inline program string); inference support/modules/markdown/bounds/generics/macros harness fns. No program CONST.
- MERGED 262ee97d (hygiene-36, first merge; pushed): clean, no fold, no conflicts; ledger rows 464–472 assigned to the nine NEW (next id 473), row 431 edited in place; changelog parity 420/420; gates green (inference 4079, module_resolution, derive_world_guard, corpus, rpc_http, diagnostics_ledger, lsp, release_scripts, split, fmt).

## editor-36 — all seven landed (E180 a20f36e6, E181 56fcbe74+ee8a457f, E175 ad163380, E176 19253040, E177 3908938f, E178 880f92aa, E179 6ed903e8, + e4e88827 N81-hygiene concat! consts), third to report (125 min, 297 tool uses); pushed; merge_fold running detached (scratchpad/order36/merge-editor-36.log) — merged ahead of lang-a/perf/lang-b/lang-c (no overlapping files)
- E180 (R9 taken): rule (2) subtracts `prelude_bindings`, keeps only RECEIVER-syntax member resolutions (read from `Program::member_name_spans`, written only at `Node::MemberAccessor` — no text fallback needed), and treats a module the prelude module `export import`s as ambient (one-line test: `prelude_bindings` carries the module id). A struct FIELD read through a receiver also drops out (an E169 leak). `ModuleRescue::Keep` + `RescuedImport{Dropped,Narrowed,Verbatim}`; collision guard via a lazily-built `ImportUseContext::names_bound_in` (top-level decls + import leaves/aliases; the prelude deliberately NOT in it). Fade text `REDUNDANT_PRELUDE_BINDS`. No ledger row (LSP paint is unrowed — E173's ruling). KOLT VERIFIED over a copy: 9b22ec36's server rewrites to `import std::option;` and `vilan check` fails at lucide/lib.vl:17453; this lane's server deletes both statements and `vilan check` exits 0. Six pins.
- E181: `Printer::export_all_marker_slot`/`print_export_all_marker`/`module_comment_end`/`take_marker_comments` + `hoist_export_all_markers` (the token net's SIXTH order-insensitivity — a relocation, so a lost/doubled marker still fails). SECOND COMMIT = a real defect found on kolt: the slot is the leading import BLOCK, not `import_run_end`'s sort run (views.vl's `// FIXME:` above an import broke the run → the marker moved up into the imports). The six `vilan/benchmarks/src/*.vl` markers (line 1, above their module comment) reformatted. `insert_import` needed no change (offset 0 already). Ten pins + one insert pin. Brief correction sent to sweep-36.
- E175: `Document::style_impl_texts` walks `program.implementations` for `Style` subjects, reads each `Implementation.source`; fold order std → file → siblings. E172's boundary pin RENAMED and INVERTED (`refactor_inlines_an_impl_style_extension_from_a_sibling_file`).
- E176: one TextMate rule (lookahead declines `(`, `:`, `-`; `.` allowed for `hidden.show(flag)`); blast radius over 257 .vl + 464 doc fences: 10 chars / 1 token (`disabled` in element-syntax.vl); kolt 0. TextMate only (the book theme has no head region).
- E177: `QuickFix.target: Option<FixTarget{path, range}>` converted through the TARGET's line index; three warnings gain "Export `S`"/"Export `hidden`" fixes; four refusals (no file, outside the package root, indented, already exported). Five pins incl. the handler-level one.
- E178: `offered_importables` at THREE consumers (`OriginListing::completions`, `module_member_completions` — a fourth consumer the item did not name, `impl_selector_completions`); `auto_import_completions` NOT reachable without a `Program` surface (OPEN Q1 → asked visibility-36 to expose `exported_entities` + `curated_modules` on `Program`; the vilan-ide wiring is a sweep/Order 37 follow-up).
- E179: `session_trace::BaseCacheSizes{worlds, weight_bytes, budget_bytes}`; E166/E174 pins extended (EXISTING fns edited: `the_summary_orders_by_total_time_and_reports_retained_state`, `the_summary_reports_the_process_memory_and_the_retained_programs`, `the_summary_reports_what_the_editor_tables_cost`, `the_declared_command_answers_the_session_summary`).
- Gates by the lane: lsp 773, core lib 774, module_resolution 175, grammar_sync 25 (CI=true), vscode_extension 9, book_mirrors 2, diagnostics_ledger 17, release_scripts 22, cli batch 168, clippy, fmt, `vilan fmt --check vilan`, kolt copy check 0 + fmt clean. No goldens; no ledger rows.
- FINDS (file at the sweep, newitems36c): (1) the organizer's whole-run deletion leaves the paragraph gap (two blank lines before `export *;` in organized lucide; cosmetic; fmt fixes non-generated files); (2) `module_member_completions` was E178's unnamed fourth consumer (fixed); (3) a cross-file quickfix reads the declaring file from DISK (unsaved sibling → saved offsets; E175's sibling reads too) — a server-side document-map lookup closes it; (4) E177's §4 fix declines when a file names two types with one spelling — an analyzer-recorded (warning site → exposed entity) pair would make it exact (the same Program surface as Q1). Plus the Q1 follow-up: wire `auto_import_completions` on the two Program fields.
- OPEN Q2 (E181): a module comment glued to the marker travels with it — nothing in the estate meets it; rule as the item wrote it.
- Corrections: E181's slot = the leading import BLOCK; E178 has four consumers; E176's hole/tag declines are structural.

## lang-c-36 — A100 S1 177be8de + S2 4454df49 landed, fourth to report (139 min, 369 tool uses); pushed; merge after editor-36 (expect the one-number `RULE_STATEMENT_SITES` conflict with lang-b later — the fixed `apply_row_mapping.py` sums integer consts through the base)
- S1: `Token::Lazy` + `KEYWORD_ROLES` row (Modifier) → TextMate + book theme GENERATED; modifier in the three grammar homes, refused on closures/external fun and beside own/view/mut/spread; `record_lazy_arguments` partitions THUNK vs FORWARD; transformer `__lazy("name", () => …)` / `__force` (interpreter arms same commit; cell `{name, state, value, thunk}`); v1 rejections via existing scans (R9 for a resource local, rule 3 for a view) + `check_lazy_argument_effects` in `post_analysis_passes` (await both spellings; context-reading); laziness agreement position-wise in `check_one_conformance` (B29 landed — brief correction). Hover renders `lazy`. 22 pins in new `inference/lazy.rs`.
- S2: `Node::Let` fifth field; `record_lazy_bindings`; `const database = __lazy(..)`; unused binding emits nothing; `init_order` gives a lazy binding no out-edges (trap at runtime by design); cycle + poison messages verified; HMR `TransferForm::Excluded`; sync half already covered by the module-initializer async refusal (B5). 15 pins + 2 in vilan-cli database.rs. Docs: spec §6.10, §3.4, appendix A.2 (+ `css` which was missing), tour sections, Option/Result page note.
- Gates by the lane: core 5789 (1018 s), lsp+ide 766, cli+wasm+embedded 898, grammar_sync 23, book_mirrors 2, book_sync 14, grammar_ebnf 12, docs 9, markdown_golden 2, release_scripts 22, diagnostics_ledger 17, corpus byte-identical, clippy workspace, fmt. Seven ledger rows NEW; `RULE_STATEMENT_SITES` 34 → 42 (no named constant, CURATED untouched).
- Goldens moved: `vilan/test/reactive-on-change.mjs` (the `lazy` → `quiet` rename only), `markdown_anchors.golden` (+3 headings). EXISTING fns/consts edited: markdown.rs `a52_an_rpc_mirror_is_a_source_and_drives_a_generic_consumer` (rename), reactive_selection.rs const `EAGER_AND_LAZY` (raw string; rename), parse_expr_regression.rs (Node::Let arity), diagnostics_ledger.rs (the count), grammar_sync.rs (KEYWORD_ROLES row), vilan-ide completion.rs (`keyword_lexeme` + `KEYWORD_DOCS`).
- FINDS (file at the sweep, newitems36c): (2) a generic lazy parameter instantiated at a resource slips the type check (declared type read at the declaration; a bare `Expr::Local` argument has no `expr_id_to_type_id_map` entry — B328's comment records the same gap) → item; (3) the effects walk descends into closures the argument merely creates — an `await` inside a created closure is refused (conservative; documented at `ThunkEffects`) → item only on demand; (4) `scan_closure_view_captures`'s `Expr::Call` arm never walks the call SUBJECT (pre-existing; one line; changes a check's firing) → item; (5) lazy bindings vs HMR state (excluded in v1) → design item on demand; (6) spec appendix A.2's reserved words are ungated (was missing `css`) → hygiene item. (1) the paper's grep claim was stale — recorded on lazy.md §8, no item.
- OPEN Qs built as recommended (keep): poison wording; `lazy mut` refused; an unused lazy binding emits nothing; `lazy` first in the prefix order.
- MERGED 97623395 (editor-36, second merge; pushed): clean, no fold, no conflicts with hygiene; no ledger rows (head_max 472); changelog parity 427/427; gates green (lsp, core lib, module_resolution, grammar_sync CI=true, vscode_extension, book_mirrors, diagnostics_ledger, corpus, style_chain_order, release_scripts, split, fmt).
- lang-c-36 merge: two false starts on GATE SPECS (`book_sync` is a vilan-lsp src module under `-p vilan-lsp`, not a binary; `docs` and `markdown_golden` are vilan-CORE binaries) — merge_lane's spec check stopped the chain BEFORE merging each time (exit 8, worktree untouched — the check works); third run in flight with every spec verified against tests/ (allow list: `a52_an_rpc_mirror_is_a_source_and_drives_a_generic_consumer,EAGER_AND_LAZY`). PROCESS: verify `--test` names against `crates/<crate>/tests/*.rs` before launching a merge.

## perf-36 — M70 d963a119, B341 828de17c, M66 091762e5 landed; M55 measured and STOPPED (R6); fifth to report (175 min, 244 tool uses); pushed; merge after lang-c-36 (expect src conflicts with lang-c in interpreter.rs/transformer.rs arms and the `reactive-on-change.mjs` golden both lanes moved — hand-merge + regenerate)
- M70: key change (`entry_open_module`), not cycle resolution — the deferral is `resolve_world` whole (2,046 lines); SIX files not seven (`store.vl` is a `[service]` entry → M72); measured 8–14 % per keystroke saved on the six (paired harness, 5 edits/file); the item's premise corrected — the unresolved world saves the load walk only, ~150 ms of `resolve_world` stays per keystroke (M73; the naive fixpoint split = 0.4 ms, reverted); memory 12 → 18 worlds (+9.5 MiB budget currency; M74 says the factor over-charges). OPEN Qs built as recommended: KEEP M70 (removes a class of never-served files; prerequisite for M73/M19-record work; revert is one field); `entry_open_module` stays the conservative superset (12 → 18 keys, no sharing lost on kolt).
- B341: one hunk in `analyze_inner`; verified on both repro packages; 285 `modules::` tests under plain `cargo test`; pin asserted on diagnostics (macro worlds consult the cache — first draft's hit delta read (2,3)).
- M66: −14.4 % Ir per cell; eleven goldens back (not twelve); kolt client emits no helper; docs std/cells.md.
- M55: two legs; 96 % overlap; ceiling 3.4 % — STOPPED; close as measured-and-declined at the sweep.
- Gates by the lane: core 5754, lsp 757, cli corpus+service_layer+reactive_lifetimes+reactive_channels 76, release_scripts 22, docs+module_resolution 184, clippy, fmt, vilan fmt on three .vl. No ledger rows. GOLDENS moved: eleven corpus .mjs (blanket-impl, element-syntax, reactive, reactive-flatten, reactive-on-change, reactive-owner, reactive-selector, reactive-turns, signal-update, spread-parameters, ssr-render). NEW fns only (no existing fn/const edited).
- FINDS (newitems36c): M72 `[service]` entries never reach the base cache — kolt's store.vl 290–605 ms/keystroke, 0 hits 0 misses (HIGH-ish); M73 an open module's world is stored unresolved → ~150 ms `resolve_world` per keystroke on the six (size after B318 S4 — scope-chain territory); M74 M50's 24× weight factor over-charges open-module worlds (re-weigh with `world_cache_spike.rs`; makes M67's budget honest); note on M19: the per-module checks record cannot extend to open-module worlds as keyed (entry-dependent through the alias; needs a red-proving pin first); N83 confirmed (artifacts hit by hand); one unrowed `Failure::unsupported` ('`Shared::identity` over {type}') → N85's F3 list. Kolt nit for Reed: vilan.toml's header comment says 'three entries' — there are two.

## style-36 — A95 S2 f70f7941+7abec3ee (BREAKING), S3 806dbfdf, S5 e6dd697b, + 66cf15b8/1e8416b8 (two finds fixed); B322 verified closed; sixth to report (174 min, 330 tool uses); pushed; merge after perf-36
- R1/R2 built as ruled; R3 built for its PURPOSE: the triple-beside-each-slot storage was measured at +26,201 B (+9.4 %) on kolt's bundle and REJECTED — the guard carries its inner TOKEN (`^hover`) instead, so `within(hover())` lands and every class name stays byte-identical. `slot_of` is the one total slot-key reader. GATE: 145 artifacts, every stylesheet byte-identical. Ledger 8 NEW / 6 RETIRED (419 420 421 426 427 445 453) / 422 re-keyed. Goldens: css-block/preflight/style/style-when/theme .mjs + markdown_anchors; NO .css golden moved.
- Finds FIXED in lane: `vilan fmt` bailed silently on `[deprecated(..)]` (first real catch of `formatter_never_silently_bails`); S1's guard was a latent MISCOMPILE (`![x] .sX{}`); the paper's `let interactive = ..` needs `const`. Find for the sweep: ui.vl:537 doc mention of `child_relation` (one word; lang-a's file — fix at the sweep).
- KOLT: one S2-breaking site `src/styles.vl:138` `.pseudo(":placeholder")` → `.on(style::element("placeholder"))` — at Reed's word.
- EXISTING fns renamed (5) and edited in place (9) in styling tests — allow list for the merge: a_negated_attribute_rule_outranks_the_plain_pseudo_rule,a_negated_condition_is_its_own_slot,a_pseudo_class_name_cannot_forge_the_negation_marker,a_child_relation_cannot_wrap_a_breakpoint,within_cannot_wrap_a_breakpoint,an_attribute_cannot_wrap_a_media_conditioned_style,a_delegating_condition_is_recorded_on_the_axis_it_delegates_to,every_table_row_names_a_method_that_exists,attributes_survive_the_reprint. formatter.rs: `print_func` gained the `[deprecated]` arm (not a named fn of either lane; disjoint from editor-36's).

## lang-a-36 — A99 e535baa8, A98 af9e859a, B334 ad48117f; seventh to report (181 min, 447 tool uses); pushed; merge after style-36
- A99: six methods (browser) / five (process — never had `swap_split`); `split_route` folded; chunks.rs one retarget entry, `view_method` deleted; prelude gains the five; steer suffix on row 212 (re-keyed, no NEW); ONE editor arm `Document::retired_slot_method_fix`; estate: docs 9 pages, examples 9 sites, corpus ssr-render.vl, core tests 7 files, cli tests 6 files; six completion tests re-keyed `bind_each` → `bind_text` (outside the one arm — brief correction); appendix/editor.md gains the quick-fix row (book_sync requires it — MERGE POINT with sweep-36's S5 and lang-b's G24 row, same page, disjoint rows). Goldens: ssr-render.mjs (OVERLAPS perf-36's M66 regen — expect a conflict, regenerate), split golden app.js (twice), markdown_anchors.
- A98: `settled_steps` two greedy scans + `row_references`; 1,000 rows: append/prepend 1000 → 0 cuts, single edits 1000 → 1; Ir −61.2 % (stub-dominated); pin with cost AND tree per edit.
- B334 (R4): `receiverless_call_subject` before the scope walk; order-independent; four pins.
- FINDS (newitems36c): `VILAN_STD` pointing at a byte-identical COPY of std outside `<repo>/vilan/` silently mis-analyzes std (pre-existing on 9b22ec36; two std worlds mixed?) — HIGH-ish for packaged/relocated std; `each_by`'s `T` freezes abstract with an unannotated render closure parameter (pre-existing; A99 makes it reachable; kolt clear); std doc comments in reactive.vl (7)/rpc.vl (7)/math.vl (1) still say `bind_each` (sweep fix); formatter.rs's two element-head layout pins spell `.bind_each(` (cosmetic). Flaky rpc_http pin under load (N84's family).
- Renamed test fns (delete+add): ui_rows.rs b255_*/a71_* `bind_each` → `each`; inference/modules.rs ssr_bind_each_*/bind_each_rows_*; DELETED split.rs `stage_value_form` + `a85_a_value_form_route_swap_splits_exactly_as_the_method_form_does`; EDITED in place: chunks.rs `a_route_match_plans_its_arms…`, `a85_the_ssr_twins_of_the_value_forms_render_what_the_methods_do`, `a91_the_ssr_twin_renders_what_each_row_shape_renders`, `a91_a_render_closure_may_yield_any_slot`, vilan-lsp `element_head_dot_offers_the_view_methods` (+4) and main.rs `completion_after_a_dot_in_an_element_head_offers_the_view_methods`. CONSTS rewritten (N81 shape): THREE_FORMS, CHILD_CONTRACT, A85_VALUE_FORMS_SSR, A91_ROWS_SSR, A91_ROW_SHAPES, INDEX_FORM, IN_PLACE_REMOVE_BY + other ui_rows program consts, router.rs APP, ssr_differential.rs COMPONENT, chunks.rs four `plan_for` fixtures, parse_expr_regression/fixtures.rs; std_twin_parity ALLOWED_DIVERGENCES edited. ALLOW LIST for the merge = those edited fns + consts.
- KOLT nine A99 sites re-censused (views.vl:77,118,162,166,399; theme.vl:270,216; overlay.vl:502; views.vl:736) + overlay.vl:32 comment — at Reed's word (R7).

## lang-b-36 — B340 5cc1e079, G24 e67cbf7b, B333 6a71bb09; eighth to report (185 min, 352 tool uses); pushed; merge after lang-a-36
- B340: analyzer-only (`resolve_call_subject` → method path; transformer + interpreter unchanged — brief correction); `check_callable_impls` post-build; Q1 coercion on GROUND `call` signatures (`callable_closure_type`, `note_callable_coercion`, `wrap_callable_coercion` `($a) => call(s, $a)`); Q2 steer text. 19 pins (`inference/callable.rs`); 2 rows NEW; std/traits.md.
- G24: `parse_const_declaration`; `CONST_HAS_NO_MUTATION` (RULE_STATEMENT_SITES 34 → 35 + CURATED line — UNION with lang-c's 42 → 43 at the merge, the fixed tool sums); `ConstValue::Callable` snapshot lowered by SUBSTITUTION (`const space = (n) => { return 0.25 * n; };`); `check_const_functions` over the call graph; R2 steers + ONE editor arm `Document::const_let_fix`; row 291 re-keyed (no `ConstVariable` row existed — brief correction); 17 pins (`inference/const_eval.rs`); spec/const.md §9.6; appendix/editor.md Ten → Eleven (MERGE POINT). const-eval.md §1 + §11 and fn-coercion.md §1 + rule 7 LANDED in proposals.
- B333: `call_return_clause` at all four landings + `clause_carried_by`; `adoptable_closure`; 4 pins; no row.
- FINDS (newitems36c): `Node::Const` peels in only 2 of ~24 item-shape sites (`export const fun` unaudited); a generic `Callable` does not coerce (design; needs rule 2's instantiation); `literal_size()` sentinel; aggregate capture inline per call; ui.vl:1187 annotation redundant (sweep). Flaky rpc_http (N84 family).
- OPEN Qs built as recommended: captures keyed by the const world's emitted names (keep); substitution not struct literal/IIFE (keep; consequence recorded); `call`'s clause not carried onto a coerced type (keep; probe at the sweep).

## sweep-36 — bb95dc82 (24-file codemod), 7a6fc959 (std curation + suppression dropped), f723a238 (S5 docs); ninth to report (177 min, 360 tool uses); pushed; MERGED LAST
- Curation 548/856 exported; 33 `export *;` modules; 27 curated per item; 319 `export impl`; criterion (a) = 152 items / 45 modules; exposure warning 44× → fixed by exporting; `check_plain_reaches` suppression deleted; warnings 0 everywhere incl. kolt (21 marker files). TWO S1 DEFECTS fixed (`transparent_declarations` for `export [derive(..)] struct`; `needs_semicolon` for `export let` — the formatter BAILED silently) + `StyleSurface::extend` unwrapped `export impl` (E175 supersedes → CONFLICT expected with editor-36's E175 — take editor's). Shared test readers edited (named): `docs_enum_parity::enum_declarations`, `style_table_sync::top_level_block` (+ `strip_visibility_marker`). std/vilan.toml census refreshed. Goldens: markdown_anchors +2 only.
- EXTERNAL: website 7 files + playground 2 — exact edits in the report; apply at the sweep (website warnings 130 → 0).
- OPEN Qs: `///`-led no-import files take the marker at line 1 above the doc comment (E181 rule must treat `///`/`//!` as not the module comment — `vilan fmt --check` over the merged tree arbitrates; take fmt's answer); `export impl` marks assume S4 enforces; the curation MUST be re-run over the merged tree (style.vl/ui.vl rewritten by other lanes; new declarations private by default) — the std full-scan gate at the sweep merge catches it.
- FINDS (newitems36c): `vilan fmt --check` cannot tell 'clean' from 'BAILED' (N55's gate is blind to printer gaps) — HIGH for hygiene; `macro` declarations are unformattable (hash.vl, json.vl ×6, rpc.vl ×8 silently unformatted; 15 now `export macro`); `vilan check` writes `dist/.cache` into the checked package; `split.rs:63` read_to_string over a fixture dir fails on a `dist/` with IsADirectory; visibility.md §6 census stale (664 → 741 / 118 → 152 / 73 → 24+9); a path-dependency package is never named by the warning (examples/fullstack/common uncurated) — paper sentence.
- PROCESS: the lane ran `git stash` ONCE by accident (popped and dropped at once; the four pre-existing entries untouched; tree restored) — reported by the lane; the permission classifier refused `cargo build --release -p vilan-cli` twice ('Modify Shared Resources') — `cargo run --release -- …` was allowed; add an allowlist entry for lanes next order.
- MERGED 06eba0e5 (lang-c-36, third merge; pushed): clean; ledger rows 473–479 (next 480); parity 429/429.
- MERGED cfc5136e + 6c034487 (perf-36, fourth; pushed): the merge itself clean (no src conflicts), but the SPLIT GOLDEN gate failed — M66 changed std's emitted reactive code (`SignalCell::new` lost `fresh_id()`) and the lane regenerated the corpus goldens but not tests/split; regenerated over the merged tree (the only diff: the removed `fresh_id()`), merge fix 6c034487, pushed. PROCESS FAULT (mine): I launched style-36's merge before reading perf-36's outcome — merge_lane's exit 6 — and had to kill it and `git merge --abort`; no damage (the worktree returned to cfc5136e). The standing rule stands: never chain a merge behind another's outcome check.
- MERGED 10e5031a (style-36, fifth; pushed): ONE hand fold — formatter.rs `normalize`'s pass chain (editor-36's `hoist_export_all_markers` + style-36's `sort_on_heads`, unioned into one chain); ledger rows 480–487 (next 488), row 422 edited, six retired; gates green via scratchpad/order36/gates_push.sh (build → specs → release_scripts → split → fmt → push).
- MERGED 49611256 (lang-a-36, sixth; pushed): ONE hand fold — vilan-lsp document.rs adjacent insertions after `quickfixes` (E177's `reached_definition`/`export_declaration_fix`/`exposed_type_definition` + A99's `retired_slot_method_fix`; the lane's `QuickFix { .. }` literal gained E177's `target: None`); row 212 edited; gates green.
- MERGED f5088771 + 58f5aa81 + 365dd9ff (lang-b-36, seventh; pushed): `RULE_STATEMENT_SITES` SUMMED THROUGH THE BASE by the fixed `apply_row_mapping.py` (34 + 8 + 1 = 43 — N81 paid for itself) and the CURATED list unioned; two hand folds in document.rs (the G24 `const_let_fix` else-if arm after A99's, `target: None` added; the lane's `impl Document` block moved ahead of `QuickFix`'s doc comment so E177's doc lines stay attached to the struct); then two merge fixes: lang-c's two `__lazy` thunk `js::Closure` literals gained lang-b's `origin: None` (58f5aa81), and appendix/editor.md's quick-fix count word Eleven → TWELVE (365dd9ff — both lanes bumped Ten by one; `book_sync::editor_page_code_action_titles_are_the_servers` caught it). Ledger rows 488–490 (next 491), row 291 edited.
- QUEUE: visibility-36 (running) → sweep-36 LAST (rebase over style.vl/ui.vl changes; re-run the curation over the merged tree; E175 vs `StyleSurface::extend` conflict expected — take editor-36's).
- EXTERNAL codemod applied (sweep-36's handback, integrator, 2026-09-14): `~/code/vilan-lang/vilan-website` 7 files marked at the leading-import-block slot (art, code, masthead, page, playground_page, theme, topbar) — warnings 130 → 0, errors 0, verified with the sealed 9b22ec36 toolchain; UNCOMMITTED in the website repo (Reed's to commit, as kolt). `vilan-playground/todo` 2 files (routes, store) — warnings 10 → 0; its two ERRORS are pre-existing stale example code (`Signal<List<Note>>` as a type; `import std::print;`) — K14's territory, not the markers.
- Dry-run `git merge-tree origin/next origin/sweep-36`: 5 conflicts — CHANGELOG.md (mechanical), examples/reactive-ui/todos.vl, std browser/ui.vl, process/ui.vl, style.vl (sweep's markers on declarations A99/A95 S2 rewrote or deleted) — hand folds: keep HEAD's code, keep `export` where the declaration survives; document.rs auto-merges.

## visibility-36 — B318 S4 (eee69a19 a08d7ee3 a196f94d 32bc1147 5c91ae33 f432ede1), B335/B336/B338 (fd4dafa2 c58eb372), E178 add-on (efa55a2f); ninth to report (234 min, 351 tool uses); MERGED 414d5476 (clean after the tools; rows 491–499; changelog restored=1; gates green)
- S4 as the paper wrote it with three corrections (the admission map keyed by the PAIR; `build_impl_admission` at the TOP of post_analysis_passes ahead of `thread_contexts`; the in-walk lookup cannot be scoped — the call is CORRECTED post-build; the call check is KEPT — it is what enforces `only`/selectors/`export impl`). §3.5 pin proven non-vacuous with a new `transform_package` helper. B330 refused narrowly (`bounds_differ_only_at_binder_bounds`, `declaring_maxima` — std's `flatten` pair stays legal). `#(impl T)` needed `collect_reach_marked_spans`'s `Selector` arm filled (brief correction). BREAKING: macro_std meta.vl/build.vl → `export impl` (508 inference tests red without it).
- OPEN Qs built as recommended: const_eval's three site scans NARROWED (revert = `None` at three calls); B330 narrowed to the `(Generic, Generic)` arm (keep); macro_std markers vs sweep — no conflict arose.
- FINDS (newitems36c): B349 a module-level statement body resolves under NO file scope (`current_admitting_file` only set by `enter_instance`); N94 two diagnostic/assertion strings with swallowed `\`+newline continuations (const_eval.rs:723 user-facing) + a three-consecutive-spaces gate; M75 `refuse_imported_member_collisions` O(collisions × files × statements); N95 `Program::hidden_impls` drained by `build_impl_admission` (rename/debug_assert); E185 the monomorphization refusal prints the literal `that module` where the module's name belongs (seen at the sweep merge: "module `that module` does not admit it"; also the steer "Widen `that module`'s own import"); B350 the `#`-on-an-exported-impl redundancy warning (S2's twin) not built.

## sweep-36 — MERGED da8cb6e0 (last; hand folds + three merge fixes; gates green; pushed)
- Four .vl conflicts folded by `scratchpad/order36/fold_markers.py` (take HEAD's code, re-apply the lane's `export` where the declaration survives: 11 markers + 1 `export *;`; `swap_split` — A99's folded free fn — exported by hand). Then the CURATION RE-RUN over the merged tree, as the lane predicted: (1) `every_std_module_is_clean_under_full_scan` red → fetch's `impl BodyStream/BodyReader/ChunkResult` → `export impl`; then `cell_identity` (M66) → `export fun`; (2) the sweep's gates red on SIXTEEN inference pins (Wire/json/binary/jwt/handle round trips): S4's monomorphization rule — a derived Wire visitor is a generic body declared in wire.vl and resolves under wire.vl's set — met json's `impl JsonWriter{,with Serialize}`/`impl JsonReader{,with Deserialize}` and binary's `impl BinaryReader{,with Deserialize}` left PRIVATE by the curation → `export impl` (fa74352e); (3) style-36's gate 6 reader (`free_condition_constructors`) did not strip the marker the sweep put on the prelude's constructors → `strip_visibility_marker` applied (da8cb6e0). LESSON for the paper (§15 says it): a curation decided on the base tree must be re-decided on the tree it merges into; the export gate makes every private impl a potential monomorphization hole.
- ALL TEN LANES MERGED. next tip da8cb6e0. Ledger next id 500. Seal running detached (scratchpad/order36/seal36.log); CI run 34912073720 on da8cb6e0 watched (ci36.log). NOTE: CI on 365dd9ff (lang-b's tip, before visibility/sweep) FAILED tests+check — the da8cb6e0 run is the one that counts.

## SEAL (2026-09-15, tip da8cb6e0 → 9da581a6 → d783fbf4)
- seal.sh over da8cb6e0: union 7583 run / 1 FAILED (`vilan-wasm::compile a_splittable_route_match_still_compiles_to_one_playground_bundle` — the playground fixture wrote `View::swap`; lang-a's estate sweep covered core/cli tests only) → seal fix 9da581a6 (the free `swap`); clippy 0, windows cross-check 0, audit 0, fmt 0, parity 445/445. Union RE-RUN over the fixed tree: 7583/7583.
- CI on da8cb6e0 (34912073720, cancelled by the push after its shards ran): `test (windows-latest, 2)` FAILED — `b250_a_declared_leg_with_no_manifest_is_unchanged` ABORTED with 0xc00000fd (stack overflow) — the ci-local wrapper's console shows no FAIL line for an ABORT (found via the nextest ABORT marker in the job log); reproduced on Linux at RUST_MIN_STACK=1 MiB (2 MiB passes) — `walk_expr_node_inner`'s frame grew this order; the binaries run on 128 MiB threads → seal fix d783fbf4: `.cargo/config.toml` `[env] RUST_MIN_STACK = "8388608"` (proven honoured by nextest: a 1 MiB probe value overflowed on Linux). Filed N97 (measure the frame + a canary pin). CI on d783fbf4 = run 34913736715 (watched, ci36c.log).
- Also noted: the A99 steer suffix did not appear in the wasm playground's diagnostic (`View has no method 'swap'` bare) — K14's no-prelude buffer or the steer's keying on std's `View` — folded into N88's doc sweep as a check.

## SWEEP applied (2026-09-15, proposals @5eaa67b): 31 closed, 29 filed, tracker 105 open. Ten worktrees + branches reaped (paper-native-36 was never pushed). Release binaries built at d783fbf4 in the integration worktree (refresh after CI).
## ORDER 37 QUEUE (written at the sweep)
- RULINGS OWED (Reed): C14's recommendation (a) the counted `Shared` + `Weak` (C1) — then its slices S1–S5; F1's assumptions (emit Rust, desktop first, third `View` twin, client-only) + the three-layer `std::ui` rule (F15) + rule 4 under rustc (`RefCell` + measure) + `native` out of `@process` + single-threaded executor + `i53` keeps the JS range + C16 blocks S1; B343 the `context` clause's position (rec: keep after the return type, refuse the ambiguous closure-typed-return form, fix the highlighter); A101's three Qs (multi-arg join, codemod typing, `--name(..)`); B348 (generic Callable coercion) on demand; A102 (lazy vs HMR) on demand.
- BUILD candidates: A103 (lazy S3 std retrofit, READY); A101 (css declarations as calls — after A95 S2, now landed); E183 (custom impl methods at `css { .| }`); E69 (attribute completion from the HTML spec, code-gen); E184 (the fourth completion consumer on the Program fields); E185 (the `that module` placeholder); E186–E188 (editor smalls); B341's family done; B346 (HIGH: VILAN_STD relocated copy mis-analyzes std); B347 (`each_by` T with an unannotated closure); B344/B345 (lazy corners); B349 (module-level statement body under no file scope); B350 (# on an exported impl); M72 (HIGH-ish: service entries never cached — kolt's store.vl); M73 (open-module world unresolved, ~150 ms); M74 (M50's factor); M75; N86 (temp_dir sites), N87 (A.2 gated), N88 (doc sweep: bind_each/child_relation mentions, ui.vl:1187, the wasm steer), N89 (`export const` audit), N90 (HIGH: fmt --check vs bailed), N91 (macro printer), N92 (check writes dist/.cache; split loader), N94–N97 (line continuations; hidden_impls drain; unrowed Failure family; the walk's frame size + canary), N93→N92; C15/C16/J6/F16 (native preconditions — after the papers are ruled); M56/M57/M54; E121's arc; K14 (the playground's prelude — and its stale examples); B298 (Q3); A87/A94; L20 (owner's Windows leg — the seal's blind spot bit this order: N97 was found by CI, not by the seal).
- KOLT at Reed's word: the nine A99 sites (views.vl:77, 118, 162, 166, 399; theme.vl:270, 216; overlay.vl:502; views.vl:736) + the overlay.vl:32 comment; styles.vl:138 `.pseudo(":placeholder")` → `.on(style::element("placeholder"))`; vilan.toml's header comment 'three entries' → two. Website: 7 files uncommitted (Reed commits).
- PROCESS lessons this order: verify every gate `--test` name against crates/<crate>/tests/*.rs (two false starts); never launch a merge before reading the previous one's outcome (one abort); a curation decided on the base tree must be re-decided on the merged tree (the export gate + monomorphization); a lane's estate sweep must name EVERY crate with .vl fixtures (vilan-wasm was missed); the ci-local console shows no FAIL line for an ABORT — grep the nextest ABORT marker; the local seal cannot see a Windows-only stack overflow (L20); allowlist `cargo build --release` for lanes; hand-folded merges use scratchpad/order36/gates_push.sh; marker conflicts fold with fold_markers.py.
## SEALED 2026-09-15 at d783fbf4 — CI 34913736715 green on all eleven jobs; toolchain refreshed BOTH locations (vilan 0.40.0 (d783fbf4c)); chronicle SEALED paragraph written.

