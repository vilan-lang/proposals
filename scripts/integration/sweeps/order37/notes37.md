# Order 37 — running record (integrator)

Opened 2026-09-17 off d783fbf4 (Order 36's sealed tip; nothing landed since). Ten lanes: hygiene-37 (N94 N95 N96 N87 N88 N89 N86 N97; first), dx-37 (B346, N90, N91, N92, B342), lazy-37 (A103, B344, B345, A102), solver-37 (B351, B347, B349, B350 + B352, B353 at GO), diagnostics-37 (E189, E190, E185, B343 + E191 at GO), editor-37 (E69, E184, E186–E188 + E192, E193 at GO), perf-37 (M72–M75), native-a-37 (C14 S1–S3 + D8), native-b-37 (C16 then F1 S1a; F18 sized), css-37 (A101 + E183; merged LAST).
- PROCESS (GO): the owner's "go" carried two steers — the native path is CLI programs now, native SERVERS next (F18 filed), the UI layer behind a design conversation (F17 filed); and a fresh kolt TODO/FIXME/HACK sweep (46 comment lines at kolt 0decf84): 12 items filed (newitems37a.json), 3 stamps (A72 replace-navigation + routes.vl:25; I3 `filter_map`; A8 `scrollbar_color`/declare rules), 14 comments STALE against shipped std (A59, A60, A62, A82, A83, B268, B340, fetch's `Request`) — kolt's migrations at the owner's word (go-items37.json `kolt_owed_at_the_owners_word`); the app-only TODOs (show error to user, input-system design, focus the panel, effects at scale, the two "Implement this", todo.md's JWT rotation, prefs' StorageKey boilerplate) stay in kolt. R2 amended, R4 recorded; the rest as recommended. Chronicle cycle 55 entry appended; worktrees off origin/next = d783fbf4; ten Opus agents launched; kolt READ-ONLY for every lane.
- Watch-outs from Order 36: verify every `--test` name against crates/<crate>/tests/*.rs before a merge; never launch a merge before reading the previous one's outcome; an estate sweep names every crate with .vl fixtures (css-37); the ci-local console shows no FAIL line for an ABORT (grep nextest's marker); the merge tools refuse an unnamed const edit and SUM same-named integer consts (RULE_STATEMENT_SITES: css-37 and diagnostics-37 report deltas); five lanes share analyzer.rs (hygiene N95/N94, solver, lazy, perf, native-a/-b) — hand-fold adjacent edits; hygiene-37's N88 comment lines in reactive.vl/rpc.vl merge BEFORE native-a-37's edits to the same files.

## perf-37 — FIRST to report (85 min, 355 tool uses): M72 BUILT d3903646, M75 BUILT c160badb, M73 MEASURED + instrumented ffb48c89 (not built), M74 MEASURED f10988d2 (no second factor); all gates green (core 5,899, lsp 778, cli 850); no goldens; no ledger rows; NOT pushed (agent down)
- M72: `base_cacheable` drops `!contains_service` (the bypass predated the derive/macro hoist — a service expands through `expand_entry_over_world` AFTER the store, identically on hit and miss); `std::rpc` joins `BaseCacheKey::std_seeds` through the same `seed_module` path. kolt copy, paired LSP harness (release, 5 edits × 3 reps, CPU/keystroke, loadavg 3.2–7.6): store.vl 482 → 196 ms (−59.3 %), views.vl −7.2 %, client.vl −1.5 %; `vilan build` dist byte-identical vs d783fbf4; `vilan check` identical on 17 files. Pins (vilan-core --test base_cache): `a_service_entry_hits_the_cache_on_second_analysis`-shaped pair (planted red). Doc-comment-only edit on `world_entangling_entries_and_overlays_bypass`.
- M75: three indexes hoisted out of the collision × importer product (`carried_at`, `implementation_of_member`, the importer Vec); pin (vilan-core --test module_resolution) `m75_a_collisions_refusal_does_not_scale_with_the_importers_statement_count` — a SHAPE bound at 1,500 statements / 250 members (the item's 500 sits inside one 10 ms /proc tick): scan 80 → 650 ms vs index 10 → 60 ms (debug, loadavg 42–50).
- M73: `resolve_world` split into eleven `[vilan phase]` stages (N43's treatment). FINDINGS: M70's attribution was wrong — preludes/use-drain read 0.0 ms, imports 0.1–1.2 ms, the constraint FIXPOINT is 79–96 % of the pass on all nine files (theme.vl 133/145 ms); a RESOLVED world runs `resolve_world` twice and the second run is 1.2–8.9 % of the first → the prize is moving the first run before the store. Rec (OPEN Q1): build as its own item, paper first — pre-entry resolve + store the partially-resolved world; a per-FILE deferral set seeded by `import pkg::<entry-alias>` closed transitively (kolt: 9 of 26 files per entry-open module; ~60 of ~69 sources resolvable before the store); eleven drained queues must hold deferred sources back (B222/B236/B237 territory) — out of proportion to an M slot. Family marker `tooling` (Q2).
- M74: two-shape weighing in `world_cache_spike.rs` (two `#[ignore]`d measurement tests): resolved 24.9×, entry-shaped 21.7× → the over-charge is 13 %, not an order of magnitude (2.6 MiB of the 192 MiB budget on kolt, conservative direction); `BASE_CACHE_WEIGHT_FACTOR` stays 24; M67's budget pin unchanged. METHOD WARNING: the two shapes cannot be weighed in one process (allocator page reuse read 0.1×) — one shape per test process.
- Corrections: M67/M50's weights live in vilan-core analyzer.rs (`BASE_CACHE_WEIGHT_FACTOR`/`_RESIDENT_BUDGET`/`_DEFAULT_BUDGET`), not vilan-lsp memory.rs; store.vl is NOT an entry-open-module file.
- FINDS for newitems37b: F-A the CHECKS phase is the largest remaining per-keystroke cost on M70's six (theme.vl checks 1,103 ms vs build 632 — `reused 0/69`; M19's per-module checks record withheld from entry-shaped worlds); F-B `statement_sources` runs for every import statement program-wide once a collision is banked (linear, index-able); F-C `[vilan phase]` marks are WALL (`PhaseClock` = `Instant`) — a CPU-clocked variant would let E121's ledger take absolute figures under load.
- Sweep: close M72 M74 M75; M73 re-pointed (instrument + measurement landed; the build is Q1's item); E121 ledger rows P10 (M72 pairing), P11 (the stage split), P12 (per-shape weighing); the M74 factor pair + one-process-per-shape rule beside `BASE_CACHE_WEIGHT_FACTOR`'s derivation.
- MERGED perf-37 07eb16b7 (first merge; pushed to next): clean merge, no rows (ledger head 499, lane rows 0), changelog parity 449/449. The merge gates were KILLED twice by SIGTERM mid-run (once at the first cargo compile, once at inference test 2271/4173) — traced through the lane transcripts to `pkill -f 'nextest run -p vilan'` / `pkill -f 'gates.sh'` in diagnostics-37 (and `pkill -f` in native-b-37 and solver-37): lanes killing by PATTERN, the m19-t1b incident again. Pushed on the strength of TREE IDENTITY: `07eb16b7^{tree}` == `f10988d2^{tree}` (next had not moved), and the lane ran the full suite on that exact tree (core 5,899 / lsp 778 / cli 850 / clippy / fmt; the merge's `cargo fmt --check` clean; base_cache 34, module_resolution 199, world_cache_spike 1 re-run green at the merge). Every LATER merge's tree differs and its gates must run — the three lanes are told to stop pattern kills. PROCESS RULE for the brief template: "kill only PIDs you started — `kill <pid>`; never `pkill`/`killall` by name or pattern; a runaway of yours is found by `pgrep -f <your worktree path>`".

## native-b-37 — SECOND to report (137 min, 341 tool uses): C16 BUILT e56b1227 (fix; 1 row NEW), F1 S1a BUILT 7e245e39 (feature; 5 rows NEW); clippy/fmt/docs/release_scripts/diagnostics_ledger/third_party_notices/ci_ignored_pins/native_differential green; `-p vilan-core` full run NOT completed by the lane (3,701/5,902, zero failures, stopped for load) and the whole-corpus differential NOT completed (22/117) — both re-run at the merge/seal; no .vl touched; no goldens
- C16: an interprocedural summary of RETAINING parameter positions (stored/collected/enum-payload/assigned/returned/passed on to a retaining callee), a monotone fixpoint in `infer_bumps`'s shape, asked only of view-capturing closure arguments; a bodiless callee keeps everything. New refusal text names the callee ("`keep` keeps what it is handed past the call"). Pins (vilan-core --test inference, borrows.rs): C13's pin UN-IGNORED + 3 refusals + 2 controls, planted red. Estate: nothing fires (131 corpus, 11 examples, docs, kolt copy 0) → `fix`. BOTH direct shapes were already refused on d783fbf4 (brief correction); the storing-callee remainder was ≤ M and landed whole. analyzer.rs: `check_view_escape` edited; new `stored_view_capturing_arguments`, `callee_name`, `callee_retaining_positions`, `compute_retaining_positions`, `note_retained_parameter`.
- F1 S1a: `Backend::Rust` (`--backend rust` on build AND run; `effective_backend` — `validate_backend` does not exist, brief correction); NEW crates `vilan-rt` (~700 lines, ZERO deps: Rc<str>, Vec, ordered Map/Set, `Shared`/`Captured` = Rc<RefCell>, `guarded` catch_unwind, `with_finally`, the `Js` console.log renderer — where the differential actually lives; 7 unit tests) and `vilan-rust` (~1,950 lines: structs/enums → Rust's, impl methods need NO impl-selection machinery — the analyzer already resolves `p.bump(2)` into a call whose arg 0 is the receiver, closures, match, loops, views, rule-1 copies off `clone_sites`); `vilan-cli/src/native.rs` writes `dist/native/<entry>/{Cargo.toml,src/main.rs}`, `cargo build` DEBUG, runs; `--stdout` prints the Rust; `--watch`/workspaces refused by name; `guide/native.md` + SUMMARY + `appendix/cli.md` row. Pins (vilan-cli --test native_differential, 4/4): default suite byte-identical; every platform-free program identical-or-named; `board.vl` names its own gap (SignalCell<i32> = a GENERIC — monomorphisation, not module bindings: brief correction); the enumeration (`platform_free_programs()` + `PLATFORM_MODULES`, a support fn Order 38 widens by deleting rows). NUMBERS: corpus 131 / platform-free 117 / emitted 33 / refused by name 98 / byte-identical 17 / differing 0 / rustc-refused 0. Refusals: generic type parameter 29 + named generic fn 17 + generic dispatch 5 = 51 of 98 are MONOMORPHISATION (S1b); module-level binding 6; overloaded operator 6; intrinsics 3; resource 2; JSON.stringify 2; `?` 2; async main 2; … Two DIFFERING outputs found and closed in-lane (`List::remove` answered Option; resource teardown → refused by name). R3: boxed bindings = 0 over the accepted corpus (mechanism proved on a probe; `VILAN_NATIVE_REPORT_BOXED=1`) → C15 has nothing to optimise until S1b's corpus compiles — do NOT build C15 yet. F16 as built: every closure TYPE is `Rc<dyn Fn>`, a closure value read from a binding is `.clone()`d per use; the `impl Fn` half NOT built (OPEN Q2: keep uniform until measured; C16's `compute_retaining_positions` is the discriminator if wanted).
- J6 DESIGN (for native-apps.md): seven primitives read off `transformer.rs::helper_source` — Task external over an executor slab (state/continuations Vec<Rc<dyn Fn()>>/observed/origin; eager spawn; failure latches; unowned-unobserved failure reports once), a single-threaded loop with a microtask queue drained to exhaustion before a deadline-ordered timer heap (observable — reactive-turns is the pin), Nursery = Rc<NurseryBody> with cancelled/fail_latch/wake list/parent Weak (CancelSignal = the Weak; cancellation-on-drop IS destruction.md's mechanism), the join drain reproduced exactly (child list grows mid-drain; each child raced against the fail-wake; on failure cancel → absorb → propagate with the " (in task spawned in …)" suffix), detached nurseries override `__fail`, `sleep` an abortable timer, `Timer` a memoized verdict + waiters. Emitter rec (a): Rust `async fn` over a hand-written single-threaded executor (RawWakerVTable over Rc<Cell<bool>>; no Pin/Send surfaces); CPS rejected. Size M; precondition for F18.
- F18 SIZING: S1b (monomorphisation + module-level bindings via thread_local! + remaining intrinsics; L) → J6 (M) → http (`node:http` createServer + stream consumers: 3 imports + 22 accessors ≈ 25 bindings + `node:crypto` createHash for the RFC 6455 key; M) → db (`node:sqlite` DatabaseSync + 11 `__db_*`; M) → rpc (S — NOTHING new: `process/rpc_server.vl` 1,303 of 1,306 lines carry no extern; `ws.vl` 229 and `wire.vl` 708 zero externs → ~2,240 server lines compile natively the day generics work, on 28 host bindings) → fs (`node:fs/promises` 16 imports + 21 accessors; M; NOT on the exit path). Of nine `process/` files four carry zero externs (build, watch, document, ui — 2,142 lines, no twin needed); five need bindings (http 25, fs 44, db 14, rpc_server 3, process 2). `native` MUST join `@process` AND IT IS FREE: `check_library_contract` (analyzer.rs ~52616) is structural, never inspects `[extern]`; joining `@process` alone = 0 violations; joining `all_hosts()` alone = exactly one (`web.vl:41/:57` re-export `pkg::ui`); joining BOTH = 0 → native-apps.md §8 Q2's reason does not hold — REVERSED on measurement (the real gate is emission: 15 base modules with JS-only bindings, 12,500 lines, `number.vl`'s 81 Math.* dominate, + 8 intrinsic-only modules). EXIT TEST is small: kolt's server leg = 3 files / 564 lines (server, store, shared), reaches 5 of 9 process modules and NO fs. Total to kolt's server serving its client ≈ L+M+M+M+S = two to three lane-orders; S1b must not be under-scoped.
- FINDS for newitems37b: (1) `vilan-rt` unreachable from a RELEASED binary (`VILAN_RT` env or the source sibling) → embed it as vilan-embedded-std does; BLOCKS non-from-source `--backend rust`; (2) `Expr::Number`'s three fields `(whole, fraction, suffix)` are a trap (3.5f → 3i32 read wrong; one doc comment); (3) `Expr::StructInitializer`'s first field is not the struct (transformer.rs:6591's dead `structs.get`); (4) f64 printing is a latent JS-side hazard (Infinity/-0/1e21) with no JS pin — a corpus program printing 1e21 and -0.0; (5) `diagnostics_ledger.rs`'s crate list is hardcoded (four crates) — derive from workspace members; (6) `str::len` UTF-16 parity holds only for the BMP.
- OPEN Qs (recs adopted): Q1 native as a Platform too — leave for S2/F17; Q2 F16's impl-Fn half — keep uniform Rc<dyn Fn> until measured; Q3 `print` of aggregates need not match node's inspector forever — deep-inspection output an excluded class of the differential.
- FOREIGN-FILE EDITS to hand-fold at later merges: `crates/vilan-cli/tests/diagnostics_ledger.rs` (`"vilan-rust"` appended in `message_sources()` and `enumerated_sites()` — hygiene-37 edits the same file); `third_party_notices.rs` `WORKSPACE_CRATES` += vilan-rust, vilan-rt. Existing test fn edited: `a_view_capturing_closure_may_not_leave_through_a_storing_callee` (un-ignored). Process note: the lane admitted one `pkill -f "nextest run -p vilan-core"` (before the reminder) and switched to recorded PIDs after.
- Paper corrections for the sweep: native-apps.md §8 Q2 reversed (joining is free — the gate is emission); §2.3 intrinsics 53 not 52, helper registry 47 entries (25 server-path), `RESERVED_NAMES` at transformer.rs:11291; §5-S1 "no impl selection needed" recorded as a finding; §10 as-built with the numbers.
- Sweep: close C16, F16 (as decided/built — half), C15 stamped "wait for S1b's corpus" (not closed), J6 design landed (stays OPEN until built with F18), F1 stamped S1a; F18 stamped with the sizing; merge running (merge-native-b-37.log): 6 rows NEW → 500–505; gates inference/docs/native_differential/diagnostics_ledger/third_party_notices/ci_ignored_pins + release_scripts/split.

## editor-37 — THIRD to report (147 min, 230 tool uses): all seven landed at 03e58d8e — E69 57598fad, E184 e406978a, E186 1e117a6d, E187 0e4092ea, E188 16ebbd44, E192 3f5946ad (FIXED — a false positive), E193 03e58d8e (premise stale; the type-path clause fixed); gates green: core 5,901 (two earlier runs died with 16 SIGTERM aborts at loadavg 170–186 — zero FAIL lines), lsp 795, ide 13, wasm 40, release_scripts, clippy, fmt; no rows, no goldens, no .vl, no grammar; merge QUEUED behind native-b-37
- E69: `scripts/regen-html-attributes.py` (`--fetch` / default / `--check`), vendored `crates/vilan-ide/src/html-attributes.tsv` (2,204 lines; header = fetch date + both URLs + sha256 per page: indices.html 6153c9ad…, SVG2 attindex d008a1b6…), generated `html_attributes.rs` (1,926 lines, fmt-stable), the gate `html_attributes_sync.rs` = a SECOND Rust implementation of TSV→table diffed byte-for-byte offline (`VILAN_REGENERATE_HTML_ATTRIBUTES=1` rewrites); tables: 104 tags with own attributes (46 HTML-only, 58 SVG-shaped), 31 globals, 1,645 per-tag pairs, 59 SVG presentation, 71 events (`on:` gains them as snippets). Shaping rules stated + pinned: an unspellable name (`xml:space`, `xlink:href`) is dropped; SVG `on…` rows go to the event table; SVG-shaped = SVG-index-only PLUS the SVG root read off the HTML index's own "SVG root" cell (else `svg` loses `viewBox`). Pins: 7 in vilan-lsp document.rs, 5 in vilan-ide (the sync gate proven red on a stale table), 1 in vilan-wasm.
- E184: one filter in `AutoImportOrder::build` on `exported_entities`/`curated_modules`; `module_importables` not called; the four `overlay_module_reclaim` pins green (131 s).
- E186: the rule is "was this a paragraph" (blank above or BOF) — in vilan-core formatter.rs `Printer::organize_run` + `line_above_is_blank`/`blank_line_end` (BRIEF CORRECTION: formatter.rs, not document.rs; disjoint from css-37/dx-37/hygiene-37's fns); 5 pins in `mod organize` + 1 lucide-shape pin; FOUR existing vilan-lsp document.rs test fns edited (they recorded the leading blank): `organize_deletes_an_emptied_import_whose_module_brings_nothing`, `organize_prunes_a_prelude_redundant_import_a_qualified_variant_reaches_through`, `organize_never_binds_a_module_name_the_file_has_taken`, `organize_strips_a_ui_import_the_prelude_module_itself_reexports`.
- E187: `Document::sibling_text` (overlay first, disk second) for both readers; `document_overlay_get` → pub (analyzer.rs ~50544, outside the ownership map — named); two pins red against the disk reader.
- E188: `Program::exposed_private_types: Vec<(SourceId, Span, Id)>` written in `check_exposed_unexported_types`; `analyze_over_world` edited (analyzer.rs ~58705); pin `quickfix_exports_an_exposed_type_this_file_never_names`. Q3: the item's "two `S`es in one file" is not expressible in vilan — built the complement (a warning whose type the file never names).
- E192 VERDICT: FALSE POSITIVE, and a build-breaker — deleting `import std::map::Map;` on a kolt copy took a clean check to FIVE errors (the selector admits nothing; `insert`/`get` at views.vl:555/680/681 lose their impl); the fade and the prune are one predicate (E114) so Organize Imports would have deleted the line. Fix: rule (1)'s import-statement exclusion no longer covers a selector SUBJECT, counted exactly when the selector survives. Pins planted red. Kolt's views.vl:2 FIXME can go (the fade is fixed; the import stays).
- E193: E160 (Order 36) already ships struct-initializer completion (`struct_initializer_completion_over_the_kolt_shape` IS store.vl:238); the live residue was the head resolved by a program-wide first-name scan — now the scope chain, qualified via `path_struct_id`; two pins + a wasm pin. Close E193 with the note; kolt's store.vl:238 FIXME stale.
- Fns edited: completion.rs `innermost_open_tag_end`, `error_tag_name_range` (renamed), `element_head_tag` (renamed), `CursorContext` (+'text; `ElementHead{chain,tag}`), `member_context`, `cursor_context`, `completion`, `element_head_completions`, `AutoImportOrder::build`, `struct_initializer_head`, `struct_initializer_context`; new `attribute_completions`, `event_completions`, `path_struct_id`. document.rs: `quickfixes` (one call), `export_declaration_fix`, `exposed_type_definition` (signature `(program, span)`), `std_style_source`, `style_impl_texts` (the two read_to_string call sites only — E167's converter untouched), `import_leaf_is_used`; new `sibling_text`, helper `completion_items_at_marker`.
- FINDS for newitems37b: (1) hyphenated completion labels are filtered by the client's default wordPattern (no `wordPattern` in language-configuration.json; no `filter_text`/edit range on `Completion`) — pre-existing since E153's css vocabulary (`flex-` → `flex-direction`), one fix serves both (S–M); (2) E69 has two implementations of one arrow (python default mode + the Rust gate) — demote the python default to `--check` (sweep decision); (3) editing-dx.md line: every fade false positive is a code action that breaks a green file (E114's identity); (4) `Analysis::nominal_id_by_name` still scans program-wide for the first name match (E193's shape; S).
- OPEN Qs (built): Q1 events as snippets; Q2 namespaced names dropped; Q3 E188 complement; Q4 close E193 + drop kolt's store.vl:238 and views.vl:2 FIXMEs at the sweep.
- Handed back: editing-dx.md §18 as-built paragraphs for E69/E193 (in the report).

## diagnostics-37 — FOURTH to report (160 min, 1,011 tool uses): E190 c6948b45, E185 2ab89967, E189 6879cbb5 (narrow rule; BROAD GATE NOT BUILT — deviation), E191 b9ce70e2 (does NOT close), B343 f3f86783; gates green (core 5,906, lsp 779, grammar_sync CI=true, diagnostics_ledger, release_scripts, diagnostics 37, clippy, fmt); ledger: row 330 EDITED + one NEW (B343); RULE_STATEMENT_SITES/CURATED +1/+1 (43 → 44, `MISBOUND_RETURN_CLAUSE`); no .vl, no goldens; merge QUEUED behind native-b-37 and editor-37
- E189: the ROOT — the call-graph collector read a call's operands from `function_calls`, and an unwired METHOD call has no entity_map entry, so the walk stopped at a chain head: calls lost their owner ("entered from outside the graph"), closures lost their lexical parent (uncovered). `Program::unwired_calls` (call, subject, arguments; banked at walk time like `call_subjects`) feeds three rules: the collector descends into an unwired call's operands; the subject joins `call_subject_entities`; a closure handed to an unwired call is a covered boundary. kolt pre-migration tree (kolt 6fcca40 — NINE retired sites, six swap + one when + two bind_each_values; brief corrections): 184 → 9 (the nine = the A99/A98 steers); migrated 0decf84: 0 → 0. Four pins (inference/macros.rs). BROAD GATE: built, measured, REMOVED — it turns off four shipped pins and `b279_an_unresolvable_dispatch_site_still_fences_its_candidates` observes a SOUNDNESS property (the refined-dispatch fallback widening to the whole candidate list; without it the program prints `undefined`) ONLY through the coverage fence in a program that carries another diagnostic by construction; AGENTS.md's stop condition decided it. OWNER Q1: build the gate and lose B279's only observable face (+ restate b229/b241 as deferrals), leave it out (E191's 138 stand), or the THIRD option (rec): give B279 a non-diagnostic observation of the fence as its own item, then land the gate behind it.
- E191: reproduced on a 0decf84 copy with `href(href())` restored — 194 on base (1 arity + 130 coverage + 63 injected-closure); 139 with E189's rule; the remaining 138 enter at `app_shell`'s body (views.vl:81/85) because the failed attribute poisons the RETURN TYPE of the component the route arm calls — the broad gate closes it (same copy → 1); the head's recovery would not reach it. Pin `e191_an_arity_error_in_an_element_head_reports_alone` (2 → 1 on the head shape); new helper `assert_fails_browser_without`. Item stays OPEN, re-pointed at the poisoned-return route (or the gate).
- E190: `emitting_stack: Vec<Id>` beside the `emitting` set; `bodyless_emissions: Vec<(Id, Option<Id>)>`; the message gains ". It was first reached while emitting `app_shell` (src/views.vl)" with that frame's name span; `Error`'s note carries its `SourceId` and the CLI reads it (no more entry assumption). Pin = a UNIT TEST of the construction (`transformer::tests::the_bodyless_refusal_names_the_frame_that_asked`) — B351's reproduction does NOT reproduce at kolt 0decf84 even with the views.vl:94 workaround dropped (FIND 2: the login-route split changed the shape — solver-37 must rebuild from the item, not from kolt's current text). Row 330 edited.
- E185: ROOT CAUSE = a `[derive(..)]`-synthesized body's `current_admitting_file` is `DERIVED_SOURCE` (outside `sources`), so the stem lookup fell to its `unwrap_or`; `AdmissionMiss` carries `importing_body`, resolved via `note_source_of`, placed through `Program::anchored` on the derive attribute. Site is transformer.rs (brief correction: not analyzer.rs). Pin `b318_the_admission_refusal_names_the_module_that_did_not_admit`. FIND 1 (HIGH): a derive-synthesized generic body resolves its members under the SENTINEL source → B318 S4's export gate hides every non-exported impl from every derived visitor — this is WHY E185 fired sixteen times at Order 36's sweep merge and why std had to export its codec blocks; the resolution rule is the item.
- B343 (R9): refused at `parse_function`'s head via `take_misbound_return_clause` — the clause is TAKEN OFF as it is reported (one mistake, one diagnostic); both readings spelled; nested legal `|| (|| void) context c` untouched; quick fix "Parenthesize the closure type"; BOTH grammars guard `context` on both sides (tighter: `context.run(..)` no longer paints after any `)`); book theme `CONTEXT_CLAUSE`; grammar_sync pin planted red; `appendix/editor.md` row + count (Twelve → Thirteen; `book_sync::number_words` extended to Sixteen — brief correction: a quick-fix title owes those two edits); contexts.md §3 paragraph handed back (in the report); spec/grammar.md sentence.
- FINDS for newitems37b: (1) derive-synthesized bodies resolve under DERIVED_SOURCE (HIGH — the S4 gate's blind spot); (2) B351 not reproducible on kolt 0decf84; (3) the HMR overlay (`hmr::OverlayDiagnostic::located`) still places a transformer refusal at the entry — one line; (4) `Error.note` is one location by contract — no channel for a program-level footnote (a `footnotes: Vec<Note>` if more arrive).
- Named edits: analyzer.rs `walk_expr_node_inner` (both call-walk arms), `analyze_over_world`, `Analyzer::new`, fields `written_call_arguments`/`Program::unwired_calls`; context.rs `analyze` (solver-37's fns untouched); call_graph.rs `CallGraph::build`/`add_node`, `Collector::walk` + `unwired`; transformer.rs `function_with_name`, `assemble` (B55 block + admission-miss block), `select_member_here`, `bodyless_refusal_frame`, `AdmissionMiss`, `mod tests`; parsing.rs `parse_function` (one check) + `take_misbound_return_clause` + `MISBOUND_RETURN_CLAUSE`; main.rs `compile_to_js` (the transform-Err arm); document.rs `quickfixes` (one arm) + `parenthesized_return_clause`; book_sync.rs `number_words`. No existing test fn edited (b229/b241 restored byte-for-byte). Docs: spec/grammar.md, spec/contexts.md, appendix/editor.md, theme/vilan.js.

## hygiene-37 — FIFTH to report (165 min, 656 tool uses): all eight landed at a7a4a1d7 — N94 d9735a79, N95 148c3229, N96 0d2da795 (34 rows NEW), N87 06522c1f, N88 5889e7c8, N89 f02e6d4a, N86 96304ea8, N97 a7a4a1d7; gates: fmt, clippy, core 5,825/5,901 zero failures (SIGTERM'd at 5,841 by the lane's own background-task lifetime; `release_differential` 133/133 re-run alone), embedded-std 13, harness_scratch 1, diagnostics_ledger 19, corpus 11 byte-identical, grammar_sync 25, grammar_ebnf 12, watch_lifecycle 2, split 12 + the 24 swept suites; no goldens; rule-const delta +0/−0; merge QUEUED (after native-b → editor → diagnostics)
- N94: both continuations fixed (const_eval.rs's `asset::staged` refusal; analyzer.rs's m9-memo assertion); gate `no_rowed_diagnostic_literal_swallows_a_line_continuation` (vilan-cli --test diagnostics_ledger) over check (3)'s enumeration + every TSV key (322 messages, threshold three spaces because one message legitimately carries `\n` + two).
- N95: `hidden_impls` → `hidden_impls_pending` (six sites) + `debug_assert!` after the drain; pin `b318_an_unexported_impl_is_hidden_from_a_consumer_that_did_not_reach_it` (module_resolution) proven by turning the take into a clone.
- N96: check (3) widened to `Failure::new(FailureKind::_, <literal>)` and `Failure::unsupported(<literal>)`: 52 sites → 34 rows NEW (33 subjects + one envelope row), 11 already rowed by N85, 3 FENCED as the host's own text (V8's `Invalid count value`/`Division by zero`/BigInt serialize — `MESSAGES_THAT_ARE_THE_HOSTS` with an expiry check); the `unsupported` family rowed on its SUBJECT; `Failure::internal`'s 46 stay out; `KEYS_WITHOUT_A_FRAGMENT` re-keyed from row number to full key (N76's ruling; exact match). Five existing diagnostics_ledger.rs test fns EDITED (named in the report) — `--allow-edit` them at the merge, plus native-b-37's `message_sources`/`enumerated_sites` one-word edits in the same file.
- N87: BOTH spec lists gated to `lexing::KEYWORDS` — §2.2 (the normative one) was missing `lazy` (33 of 34); pin `the_specs_reserved_words_are_the_lexers_keywords` (docs) shares the tour's extractor; `spec/lexical.md` §2.2's fence rewritten (docs beyond the brief).
- N88: 17 lines (reactive 8, rpc 8, math 1 — item said 7/7/1), `browser/ui.vl`'s `child_relation` sentence, `place_swap`'s redundant `context owner_scope` annotation removed (full std scan clean; corpus byte-identical); the two formatter layout pins now spell `.child(each(items, key, row))` with remeasured layouts (existing fns edited); gate `no_std_comment_names_a_retired_method` (docs) + `RETIREMENT_NOTES` for the 5 lines whose subject is the retirement.
- N89: PREMISE WRONG — `export const fun`/`export const let` were REFUSED outright (`export_takes` did not list `Node::Const`); six peels (parsing.rs `export_takes`; formatter.rs `needs_semicolon` + a `print_item` arm; lib.rs `node_declares` + the macro-world loop; analyzer.rs `hoist_generated_declarations`; vilan-lsp document.rs `names_bound_in` — MAY overlap editor-37's readers: hand-fold, take both hunks); four sites audited and deliberately not peeled; pins in module_resolution, formatter (`const_declaration_printing`), and a compiling fence in spec/const.md §9.6. NOTE for dx-37: `const fun` no longer bails the printer — N90's red plant must use `macro`.
- N86: 88 scratch paths in 39 test files → `CARGO_TARGET_TMPDIR` via a shared `tests/scratch/mod.rs` (vilan-core 48 + vilan-embedded-std 6 entirely off /tmp; vilan-cli `support/mod.rs` + corpus.rs + watch_lifecycle + 24 suites); gate `no_test_file_writes_to_the_shared_tmpfs` (NEW target vilan-cli --test harness_scratch) naming the 38 unported vilan-cli suites BY FILE; two paths the BINARY computes (`vilan-watch-<pid>.mjs`, main.rs:1758) fenced in `PATHS_THE_BINARY_OWNS` after a self-inflicted 300 s timeout was found and reverted.
- N97: 42,464 B per level DEBUG (gdb deltas and `VILAN_DEPTH_STATS` agree to the byte: `walk_expr_node_inner` subtracts 41,384 B of coalesced locals in ONE `sub rsp`, the outer 0x408), ~4,650 B RELEASE (strip = true → the instrument); `deep_nesting.rs` said ~36 KiB (+15 % unnoticed); canary `a_thirty_level_chain_still_fits_libtests_own_two_mib_thread` (deep_nesting; a method chain — flat to the parser, deep to the walk — 75 % of 2 MiB; planted red at 1 MiB); AGENTS.md gates point 7. The walk is NOT split — the cost is one function's coalesced locals, not an arm.
- Brief corrections/ownership deviations: formatter.rs (`needs_semicolon`, `print_item` arm, new `mod const_declaration_printing`), parsing.rs (`export_takes`), document.rs (`names_bound_in`), lib.rs (`node_declares`), analyzer.rs (`hoist_generated_declarations`); docs spec/lexical.md + spec/const.md; N96's `__shared_new` mention stale (M71/M66 replaced it); the lane BELIEVED its commits unsigned (`op-ssh-sign-wsl.exe` failed at times; `--no-gpg-sign` per command) — the integrator checked: only HEAD a7a4a1d7 was signed, the other SEVEN unsigned; the push hook does not check signatures, but next's history is signed throughout, so the integrator RE-SIGNED all eight (`git rebase d783fbf4 --exec 'git commit --amend --no-edit --no-verify -S'`; new head 096cba99) and force-pushed the lane branch before its merge. Rule for the brief template: a lane that cannot sign says so at its FIRST commit, not in the report.
- FINDS for newitems37b: (1) N94's class outside the rowed surface (macros.rs:1535 allow-reason, document.rs:18661 assertion, formatter.rs:13388 const, ~15 .vl program consts in test files) — a second gate over prose literals with an aligned-table exemption; (2) `asset::staged`'s registry refusal is unreachable by the walk (re-wrapped from `Err(String)`) — hand-row via `ROWS_THE_ENUMERATION_CANNOT_REACH`; (3) check (2) searches only the LONGEST fragment (283 of 558 rows have more) — widening reds exactly two rows (494, 499: `{{`/`}}` keys) — fix those two then widen; (4) the wasm playground's missing A99 steer: `Program::std_sources` is EMPTY there because the playground registers the embedded toolchain through the document overlay and analyzer.rs:56247 excludes overlaid std — `std_sources` conflates "is std" with "definition-site diagnostics frozen"; split the two sets (vilan-wasm compile.rs:168 records why a naive fill breaks split-wired chunks); (5) `deep_nesting.rs:108`'s parse-frame figure (~71.8 KiB) does not reproduce (32.8 KiB/level measured) — re-key; (6) 38 vilan-cli suites remain on the tmpfs (structural, 40 files).
