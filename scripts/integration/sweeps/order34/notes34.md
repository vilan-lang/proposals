# Order 34 — running record (integrator)

Opened 2026-09-13 off e4d192e3 (Order 33's sealed tip; nothing landed since). Ten lanes: paper-visibility-34, paper-style-34, solver-34 (TOP: B310), lang-34, editor-34, tm-34, dx-34, rpc-34 (TOP: A96), hygiene-34 (droppable, lands first), slots-34 (B309, R3 ruled). Briefs: briefs34.md. Ledger next id 426. Rulings: ALL as recommended (owner) with one change — A80 spelled `Some(mut x)`; stamped on A85/B309/A91/B316/A67/B274/A80/E163/E164/M63/M64/E166/N67/B311/B308/A93/A95/B318/M62 and the built items. A92/A79 CLOSED at GO (R8); B319/M66/A97 filed at GO (R9).
- PROCESS (GO): items filed and closes applied (`--check` first, own calls) BEFORE the lanes launched (rpc-34 reads B319, lang-34 reads A80's spelling); the chronicle OPENING entry appended and grep-verified before the commit; worktrees created off origin/next = e4d192e3 (verified before `worktree add`); lanes never push.
- OWNER ACTION pending: kolt views.vl:165 (`child_relation("not([hidden])", ..)` → `attribute("hidden", None, not(..))`) before A93's fence ships in a toolchain.

## paper-visibility-34 — PAPER LANDED (`proposal/visibility.md`, 989 lines), first to report (25 min, 159 tool uses); no vilan edits
- Probes (installed 0.40.0 e4d192e3): `export` on a declaration is a NO-OP today (`unwrap_item` strips it); `export *;` needs a rule, no collision; `export(in pkg)` needs a rule; `export (helper);` COLLIDES (compiles clean today as an export of an expression statement — F4); `#` is a LEXER refusal (lexing.rs:281, ledger row 335) — the brief's "lexically free" was WRONG; `only` needs a rule, no collision; `{ (impl T) }` lexes and the parser refuses (all at column 1 — F3); `export mod m {}` + `import pkg::a::m::x` WORKS today; `::*` needs a rule; an intermediate module's impls arrive (confirmed); B318 (i) reproduced.
- Census: 294 items need `export` estate-wide (std 118 of 664 declarations across 63 files — the brief's "364 unexported" descends from std's stale manifest comment; kolt 72; website 81; examples 15; templates 3; benchmarks 5; single-file corpus 0); codemod = 44 `export *;` lines; std curates.
- (a)–(i) answered: (a) refused at the IMPORT, `candidates_of`/`impl_members_for_bound` gain a file parameter via a `file_impls` map; (b) receiver's type at the call via `impl_select::subject_applies`; (c) general `export(in PATH)`, `mod`/`pkg` reserved heads; (d) `#` reaches a private mod yes, a private impl yes (`{ #(impl T) }` required), a `::*` member no; (e) prune per selector, selectors sort AFTER names by type text (`BranchKey` variant shared by fmt/organizer); (f) completion from the parse cache; (g) warning one release then error, same-package only — a dependency never diagnoses; (h) std curates from release N, never `export *;`; (i) declaration-site rule STAYS within one module (`subjects_collide`), cross-module pairs move to the import.
- Slices: S1 bit + warnings (M, shippable alone) → S2 reach marker (S) → S3 `only` + selectors (L) → S4 per-importer namespace (L; depends on S3 + solver-34's B279) → S5 docs (M) → S6 estate sweep (M).
- FINDS → candidate items: F1 `[doc(hidden)]` parsed/stored/formatted/documented and NO tool reads it; F2 a library's unexported top-level item becomes fadeable (`unused_local_spans`'s doc + editor.md:83 rest on "no visibility marker"); F3 a malformed import reports `found 'import' expected an expression` at column 1 (6 probes identical); F4 `export (helper);` / `export * helper;` compile clean and mean nothing — one curated refusal, shippable today; F5 std's vilan.toml quotes a stale census (264/59 vs 664/63).
- OPEN Qs (owner): `#` vs a lexically-free marker (rec: pay for `#`; fallback `^`); the qualified reach's second fix (rec: ship with the Export fix alone — 19 sites); what `export` means on an `impl` (§10 j — the one observable change without new syntax); `[doc(hidden)]`'s fate after the flip (rec: refuse at top level, keep on members).
- Brief corrections: (1) `#` is NOT lexically free; (2) std's 364 not reproducible — 664/118; (3) the exposure warning's home is the post-`build()` family (analyzer.rs ~53950, after `check_trait_conformance`), not "after hoisting, before bodies"; (4) same-module collisions stay at the declaration; (5) EMISSION: `impl_select::select_member` runs at monomorphization (transformer.rs:8038, :9576) and takes the first maximum — per-importer selection must reach it or B57's silent-pick returns: the feature's largest single cost, absent from the brief's sizing. Also: both warnings need no new machinery (`Program::warnings`/`warning_sources` exist; CLI non-fatal; LSP WARNING severity).
- Integrator: paper committed (visibility.md only — build-hooks.md is hygiene-34's N67 edit in flight, committed at its merge).

## tm-34 — E163 BUILT (2328b162), E164 BUILT (07ec37ba), second to report (29 min, 104 tool uses); MERGED d4ad9d30 (pushed) — before hygiene-34, whose regions it does not share
- E163: `vscode-textmate@9.3.2` + `vscode-oniguruma@2.0.1` devDependencies (they were NOT added by editor-33 — brief correction); `crates/vilan-cli/tests/support/tokenize.js`; a `Painting`/`Scoped` harness in grammar_sync.rs; `npm ci --prefix editors/vscode` in ci.yml's `test` job AND release.yml's `gate` job (it runs the suite inline); ignored-pins.yml needs nothing (`--run-ignored only`); notices gate free (Cargo.lock only; the vsix bundles no devDependency). Skip discipline: no node_modules → eprintln skip naming the command; `CI` set → panic. Cost: warm `npm ci` 1.96 s wall (0.68 u + 0.43 s), lockfile 41 → 43, the two packages 632 KB, audit 0. Integrator ran `npm ci` in the integration tree and `CI=true … --test grammar_sync` → 20/20 with the scope pins REALLY running.
- E164: generics begin `(?<=[A-Za-z0-9_])<(?!/)` AND the closing tag split into its own guardless rule (the `(?!/)` alone does not fix it — the element rule's own guard also refused `</span>` after text: brief correction); the opening head is a begin/end region (`<tag` … `/?>` or a `{`/`}`/`;` bail-out), E115's line-start terminator deleted; `#element-head-value` keeps closure-valued items from ending the head. Blast radius measured over 257 `.vl` + 456 doc fences: exactly 14 characters change scope, all a `>`/`/>` of a head moving operator → tag delimiter. Five scope pins, four red on the pre-fix grammar.
- Fold: grammar_sync.rs DELETED five e161/e162 regex pins + two helpers, EDITED the header/imports and `generic_depth` (delegates to new `region_depth`) — merged clean (no other lane touches the file). Gates: grammar_sync 20, vscode_extension 9, third_party_notices 1, ci_local_script 5, release_gate 3, release_scripts 22, split 12; parity 345/345; no ledger rows.
- FINDS → candidate items: F1 a plain attribute name in a head paints as a function call (`<div class("row")>` → `entity.name.function`); F2 a keyword-named attribute paints as the declaration keyword (`type`/`for`); one rule inside the head region closes both; F3 the book's highlight.js tag rule carries the same closing-tag refusal unreachably (`vilan/docs/theme/vilan.js:123`).
- OPEN Qs (owner): (1) `scripts/ci-local.sh test` does not `npm ci` → the scope pins SKIP locally (built as the no: the local gate stays network-free; one line at `leg_test` is the yes); (2) `setup-node` has no `cache: npm` (cold cost 1.83 s — rec leave); (3) the Windows `test` leg now runs `npm ci` too (`shell: bash`) — pure JS + prebuilt wasm, NOT exercised from here: CI on the seal is the check.
- Brief corrections: the devDependencies were absent; no notices change; ignored-pins needs no step; both regex halves needed; `<div class="row">` is HTML spelling (vilan is `class("row")`); there is no grammar-pin file beside grammar_sync (the pins live in it); "+1 MB" was 632 KB.

## paper-style-34 — PAPER LANDED (`proposal/style-conditions.md`, 674 lines) + B311 fence BUILT (8ceca2b7) + A93 fence BUILT (c5228523), third to report (33 min, 106 tool uses); merge started (merge_fold, ten gates)
- Paper's two load-bearing results (probed at e4d192e3): (1) the sketched VARIADIC `.on(hover(), active().not())` cannot be spelled — a spread parameter is refused on an impl member, must be last, and the css block's dotted-head lowering appends the inner style LAST (contradiction even on a free fn; no overloading) → rec `fun on<C: IntoConditions>(self, conditions: C, inner: Style)` with `Condition + Condition` making a set (`Style::raw<V: CssValue>`'s own shape; block heads take composed expressions and list literals — probed); (2) A95 does NOT close B308 structurally — `raw` emits at construction, `emit` is const-only, application is runtime, so the inner style has already emitted when `.on` sees it; A95 REDUCES it (one dead set per rule, not per nesting level) and cheapens B308's design (2) (five `retract_inner` sites → one) → B308 = A95's slice 4, not a separate item. The B318/A95 item text "closes B308 structurally" is too strong — correct at the sweep.
- B311 fence: `check_wrappable_slot(key, outer)` in style.vl called from the six combinators that re-read an inner style's slot keys (`not`, `within`, `child_relation`, `attribute`, `pseudo`, `media`) — a key that does not split into three fields refused at const time; the fence is on the WRAP, the unwrapped raw token still emits and `+` still merges. Four new pins in `inference/styling.rs`; one ledger row NEW; family `miscompile`; NO breaking note owed (nothing in std/corpus/examples/templates/benchmarks/docs writes a `:` in a pseudo name; kolt's are all unwrapped). Miscompile reproduced first (`.sX[data-open="true"]:hover{…}` with the `:not(:active)` gone).
- A93 fence: token checked against `>*`/`>*+*`; two pins; one row NEW; family `breaking` — but kolt's site HAS MOVED (views.vl:184 commented out, `.attribute("hidden", None, style::style().not(style::style().flex_col()))` live at :185): NON-BREAKING across the estate; NO owner action needed; the toolchain need not wait.
- Census refined: 318 combinator sites in 19 files (`when` excluded); per method hover 71, attribute 50, within 47, md 27, not 27, divide 24, children 22, pseudo 18, lg/sm/media 14, focus 6, active/disabled/first/last 9, child_relation 3. With the sugar kept only 40 sites must change (27 `not`, 10 kolt pseudo compounds, 3 child_relation). The brief's headline over-counted (same-named non-style methods + prose): std 20 in 1 file, kolt 39 in 4, docs 47 code + 21 prose, corpus 47 in 3, cli 5 in 2, core tests 159.
- FINDS → candidate items: kolt's raw-pseudo estate is now 10 (nine compounds in styles.vl + views.vl:77 `pseudo(":selection", ..)` — a pseudo-ELEMENT through the leading-colon hatch; paper §2.4 proposes `element(name)`); `Style::add` (`+`) has B311's latent half (splits the key, runs `without_covered` on the mis-split property — benign today, deliberately not fenced, kolt merges such styles at views.vl:265–266); `Style::media`'s min_width and `Style::raw`'s property unfenced against `:` (unreachable through the typed doors; one small fence); B308 live in a probe (3 rules, 1 dead for a one-level nest); doc error css-block.md §7.1 places `style_table_sync.rs` in vilan-cli (it is vilan-core only) and its `formatter.rs:574` ref drifted (~818).
- OPEN Qs (paper §10, rec built): (i) two values of one attribute in a set — admit; (ii) two ancestor guards — refuse in v1; (iii) `on`'s argument shape — the generic bound; (iv) the sugar keeps `Option<str>` one release then `[deprecated]`; (v) `+` as the intersection spelling — yes; (vi) B308 stays open as A95 slice 4; (vii) `element(name)` over the leading-colon hatch.
- Brief corrections: `new_button_style` is now `button_style(color)` at styles.vl:40–66 (raw tokens at :62–65, nine not four); the hatch is at views.vl:184 (commented out), not :165; the census over-count; B311's `pseudo_not` spelling recommended AGAINST (A95's values subsume it).
- Gates run by the lane: fmt, `vilan fmt --check style.vl`, core styling 229, std full-scan, style_table_sync, cli style_when/style_chain_order/hmr_css_matrix/ssr_differential/corpus/examples/release_scripts/diagnostics_ledger, clippy — green; no golden moved.
- MERGED 131cc9ec (paper-style-34, second merge): clean; ledger rows 426 (B311 wrap fence) and 427 (A93 relation fence) assigned; parity 347/347; gates green (core inference 3955, style_table_sync, std full-scan, cli style_when/style_chain_order/hmr_css_matrix/ssr_differential/corpus/examples/diagnostics_ledger, release_scripts, split); pushed. Next ledger id 428.
