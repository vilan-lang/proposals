Order 28 is sealed at <TIP> on next. Fourteen lanes merged: the thirteen launched plus the same-day a49-async fix. Union <UNION>, clippy, the Windows cross-check, audit and fmt green locally; CI <CI>. The train is <PARITY> entries; the cut stays held until you have tested kolt.

**CI in five minutes.** `scripts/ci-local.sh` holds every leg and ci.yml calls it, pinned four ways so they cannot drift; the test legs carry a Rust build cache and two nextest partitions per OS; the fmt gate is live after one reviewed reformat of 101 files; the modules differential is its own binary. The first push to next after this merge is the first run with the cache, so the job times are still to be read. Step 2, the Windows leg on your host, is filed as L20 for you.

**The context boundary.** B241's cascade is rooted (an arity-invalid call is a call, not a value use), B232 gives a stalled method call its own residual, and B242 is built as you proposed: `fun f(): T context settings`, the subset rule over strict reads, a wider clause a warning, callers checked against the declaration only, trait methods deferred with a message saying why. Hover shows the clause; a quickfix declares the inferred set on a narrow clause.

**Unsound accepts and a miscompile.** Two rigid parameters meeting on an operator and a rigid parameter as a `bool` condition are refused, both with zero-site censuses; a `= Self` default is no longer read as a bound. B237 turned out to be a miscompile: an assignment beside a guard capture targeted a top-level function and compiled clean. Trait-typed fields landed as sugar over a hidden parameter grounded per literal, with the `C<A>` display you ruled; emission byte-identical.

**Std and rpc.** `Source` is inverted as you asked (`on_change` required, `sub` derived), with the nine implementor migrations and the kolt recipe on the item; a refused client is now told so on the only channel the host WebSocket leaves; the verifier is bounded. A39 landed keyed deltas and per-key subscription: an edit costs 95 bytes instead of the channel, a leased key's later events cost nothing. The `List<T>` keyed form has no macro spelling yet (A51, your call on the spelling).

**Performance.** Tranche 1b restores six analysis passes per reused module: kolt's client leg 411 → 367 → 341 ms across the arc. The other session's compile-time finds took 26% of the instructions off a cold check and `check` now compiles entries in parallel. M38 was measured and withdrawn; M36 is designed but needs world serialization.

**Two premises overturned.** E138 was a phantom (the probe imported the comparison traits from the wrong module), and `disabled` was never a reserved word (B238 became a standing gate over the keyword tables).

**Closed 41, filed 26** (B243–B254, E145–E149, M42–M44, M46, N58, A50–A52, L20). M27 narrowed, M36 designed, E121 annotated. The prose ledger gained rows 386–399, three of them my omission from Order 27.

**Questions for you**, each on its item:
1. B253: a bare trait in return position. Existential where the rest is universal; the case each way is written.
2. B246: close the `fun bump<P: Add>(a: P) { a + 1 }` hole, at what census cost?
3. A51: the spelling for a `List<T>` keyed exposure (`[expose(keyed = str)]`, a wrapper type, or wait for associated types).
4. E145: should rename preserve an import alias, and should `import a::b as b;` collapse?
5. A52 / A50: a 503-class `Reject` arm; bare `connect_socket` offering `vilan-rpc`; a read-only row projection; `Selector` as a closure.
6. B250: should the declared-entry leg also refuse a sibling's import of a different program?
7. Still held: the cut (after kolt), A46's form, B183's six.

**Process.** Six incidents with their rules in the chronicle: a lane ran a broad `pkill` and killed sibling test runs, then said so; four semantic conflicts git resolved textually, three caught by the build and one only by a pin on the merged tree (std-28's inversion meeting rpc-28's closures, fixed the same hour); the fold helper learned to carry consts and drop deleted functions; my own target-name miss stopped one chain; 1Password's agent went down mid-order and three lanes stopped exactly as briefed; two perf items had been filed on wall-clock readings under load; and Windows had the last word a fourth time, on a short-spelled manifest directory and a cost pin the Windows clock's granularity cannot measure, two commits.

**Your editor build** (next only):

```bash
cargo install --path ~/code/vilan-lang/vilan/.claude/worktrees/integration/crates/vilan-cli --force && cargo install --path ~/code/vilan-lang/vilan/.claude/worktrees/integration/crates/vilan-lsp --force
```
