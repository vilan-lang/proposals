# Order 32 — running record (integrator)

Opened 2026-09-11 off 65af4be0 (Order 31's sealed tip). Lanes: solver-32 (TOP), reactive-32 (+A86), wire-32, rpc-smalls-32, ui-32 (+A85 paper), smalls-32. Briefs: briefs32.md. Ledger next id 412. rpc-32 NOT in this order (P1–P3 unruled); B287 not built (P4 unruled); A71 built (P5 taken from the positional-slots ask).
- PROCESS (GO): items filed in two specs (newitems32.json A85 at the draft; newitems32b.json B294/A86/E161 at GO), each `--check`ed then filed in its own call; the chronicle OPENING entry appended and grep-verified BEFORE the commit (Order 31's third-strike rule).

## rpc-smalls-32 — all four landed (fb9e8c4e B282 `__request` gensym; 0e2fc458 B284 refusal, NEW row; 097bf774 B285 refusal, NEW row sibling of 407; 13051db2 A78 built DIFFERENTLY: refused at `local_rpc` WIRING when the protocol is unstamped and carries handle methods, the POST leg's runtime failure names the method), first to report (44 min, 182 tool uses), merge first
- Gates green in the lane: core 3794 (inference+docs), cli 93, std full-scan, lsp 678, clippy/fmt 0; book_mirrors + markdown_golden 4 passed, no golden moved.
- Brief corrections: (1) A78's premise was FALSE — `ServerBuilder::build` (process/http.vl:745) installs the WebSocket leg beside every mounted service's POST leg, so a refusal at `Service::new`/`at` would refuse every working handle service; the only connectionless mount is `local_rpc` → refused there at wiring (`Dispatcher.handle_methods` → `RpcProtocol.handle_methods`, emitted only for a handle service, hash unmoved); (2) attribute pins live in `tests/inference/traits.rs` (main.rs is `mod` declarations only) — mechanics fix for the next order; (3) A78 touched rpc.vl regions beyond the lane's ownership (`Dispatcher` ~1396–1440, `RpcProtocol` ~1184–1205, `local_rpc` ~1226, `reply_source*` ~1355–1400, the generated chain ~4780) — no live conflict (rpc-32 not in the order); `RpcProtocol {` and `Dispatcher {` each have ONE construction site, both updated — re-grep post-merge; (4) commit trailer is `Claude Opus 5 (1M context)` (truthful; left as is).
- FINDS → candidate items (file at the sweep): (F1) B: an `[rpc]` method named `call` (or a `[client_service]` method named `notify`) fails the build — the generated stub calls the imported free `call`/`notify` from inside an impl that now declares the method ("`call` expects 2 arguments, but got 4", spanned on the struct); B282's family, not closed — qualify the free calls or refuse the name; (F2) B: INTERNAL ERROR "a call resolved to `Keyed`'s requirement `key`, which has no body" when a `[service]` has TWO exposed `KeyedCell<str, Task>` fields both built `KeyedCell::new([])` (one compiles; a seeded list compiles) — an unsolved `T` from `[]` unifying across two sites under `Keyed<K>`; solver territory; repro in the lane's scratch; (F3) paper line: a hand-written `Dispatcher` calling `reply_source` records no handle methods, so `local_rpc` admits it and it fails at the first call with the curated message (deliberate: `handles` is a record); (F4) `__request` is now the reserved binding — an `[rpc]` parameter literally named `__request` reproduces B282; candidate: refuse `__`-prefixed parameter names.
- OPEN Qs (owner): (1) the `local_rpc` wiring panic newly refuses a program that mounts a handle service in-process and only calls its plain methods (nothing in std/examples/corpus/kolt does; no `breaking` marker) — keep, or revert to the per-call message alone? (2) should the POST leg refuse handle methods at the ROUTE level (501 at mount) rather than per call? (3) doc placement: the transports line went to std/rpc.md, the prose to guide/services.md's handles section (the guide has no status table for this).

## smalls-32 — both landed (b5365f0a B294 `_` as the anonymous type binder; ccbc995d E161 the generic head), second to report (60 min, 226 tool uses), merge second
- B294's CHECK answered: two `type _` binders in one head ALIASED at 65af4be0 (registered under the name `_`, the second overwrote the first — `impl Pair<type _: PartialEq, type _: PartialEq>` with `self.first == self.second` COMPILED). Fixed: `register_subject_binders` withdraws the anonymous name from the scope and files the parameter by `(source, name span)`; `anonymous_binder_scopes` beside B186's `implicit_generic_scopes`; scoped to SUBJECT binders (`fun identity<_>(value: _): _` still resolves by name). `_` and `type _` are one `Node::TypeBinder` (`ANONYMOUS_TYPE_BINDER` in node.rs); `vilan fmt` prints `_` (a sixth normalize fold, `drop_anonymous_binder_keywords`). One NEW ledger row (the placeholder refusal: "`_` is the anonymous type binder: it introduces a generic parameter, and only an `impl` subject introduces one"). `RULE_STATEMENT_SITES` unchanged (21). Spec: grammar.md §3.9, types.md §5.4. Eleven `inference/bounds.rs` pins (two freshness pins proven non-vacuous), two parser pins, one formatter pin.
- E161's three observations, ATTRIBUTED: (1) `type` coloured as a type = the SEMANTIC layer — the binder's entity was spanned by its whole NODE (`type _: Source<type U>`), one `TypeParameter` token whose fallback scope is `entity.name.type`; the TextMate half was NOT a mis-colour in VS Code's own themes (`storage.type` = keyword blue); (2) `<type` as a tag delimiter = TEXTMATE (the element rule); (3) the other brackets and a line-leading `>` = TEXTMATE (operator fallback; E115's terminator). Landed: `Node::TypeBinder` carries the NAME's span (also fixes go-to-definition on a binder — named binders were one token over the whole head too); a `generics` begin/end rule (`meta.generic.vilan`, `punctuation.definition.generic.begin/end.vilan`, self-including so `>>>` closes three), the binder rule → `keyword.other.type-binder.vilan`, `_` → `variable.language.wildcard.vilan`; the element rule gains `(?<![A-Za-z0-9_])`; the book's `vilan.js` `ELEMENT_TAG` had the identical bug and takes the identical guard; E115's terminator untouched (a begin/end spans lines). No generated fragment moved. Pins: one in vilan-lsp `document.rs`, five beside `grammar_sync` (regex + order assertions).
- STOP honoured: `vscode-textmate`/`vscode-oniguruma` are NOT in the extension's `package-lock.json` (a new dependency) → the scope test asserts regexes and the two rule orders, and NOTHING ran VS Code's tokeniser over the exhibit — that half is UNVERIFIED.
- Gates: core inference + lsp + cli (corpus, grammar_sync, grammar_ebnf, vscode_extension, diagnostics_ledger, release_scripts) 3865 passed; parser/docs/markdown_golden/infer_differential 216; core lib 736; clippy/fmt 0; `vilan fmt --check vilan/test/` 0. Branch pushed by the integrator (lanes never push).
- Brief corrections: the B294 fix REQUIRED analyzer edits in the binder-registration neighbourhood (~:24545–24720, :28040) — outside solver-32's listed ranges, but the ownership map did not anticipate any smalls-32 analyzer edit (watch the solver-32 merge); E161's premise about `storage.type` did not hold for VS Code's defaults; the two items meet in the TextMate file (the wildcard scope).
- FINDS → candidate items: (F1) B: an unresolved name in an impl SUBJECT is reported TWICE (`impl Source<Option<Nope>>` → 2 errors; `let x: Option<Nope>` → 1) — the subject is walked twice in `Node::Impl` (`walk_trait_position_type_node` and per-argument for `impl_subject_args`), each prepping its own `prepped_type_locals` entry; pre-existing (B189/B236's house rule); (F2) diagnostics: an anonymous binder has no NAME to report — `impl Pair<_: PartialEq, _: PartialEq>` reads "`_`'s `eq` accepts `_`, but the right operand is `_`" — B186's `impl Bound` face (`operand_label`/`implicit_binder_bound`) would read `impl PartialEq`, but two `impl X` still collide there (trait-typed-fields.md rev 2's Q3, the owner's); (F3) editor: a fixed-array generic argument (`SignalCell<[i32; 4]>`) ends the TextMate generic list early at the `;` bail-out — zero occurrences in std/corpus/examples/book; a nested `[..]` sub-rule would close it.
- OPEN Qs (owner): (1) the `impl Bound` diagnostic face for a bounded `_` (behind Q3); (2) a DECLARED generic named `_` (`fun identity<_>`) stays legal — or is `_` never a name anywhere (a small refusal)? (3) add `vscode-textmate` + `vscode-oniguruma` as extension devDependencies so the scope test tokenises for real — rec yes; (4) `_` as an inference placeholder in annotations stays a separate feature (refused with the curated row).
- MERGED 64e4765b (rpc-smalls-32, first): no conflicts; ledger rows 412 (B284) and 413 (B285); CHANGELOG parity 287/287; gates green (core inference+docs 3794, cli corpus/service_layer/rpc_http/examples/diagnostics_ledger, std full-scan, lsp, release_scripts, split, fmt); pushed. Post-merge grep owed after the next rpc.vl-touching merge: `RpcProtocol {` and `Dispatcher {` (one construction site each).

## reactive-32 — four landed, A86 DROPPED with the gap pinned (9577026f B291; f90a0855 B292; 5d1760e1 B283; ac8fd66c B277; 5d0e1592 A86's two `#[ignore]`d pins, no CHANGELOG entry), third to report (71 min, 223 tool uses), merge third
- B291: `Owner.disposed` + `is_disposed()`; `take`/`defer` on a disposed owner release ON THE SPOT; `dispose` idempotent, flag raised BEFORE the loop. Mirror half: measured A (the general `take` fix alone: `up=2 down=1`, live=0, observer fires once with the seed) vs B (`effect`/`map` consult `is_disposed` before leasing: `up=0 down=0`, never fires) — built A (the backstop every path goes through; B's numbers in the pin's comment). Three pins, all planted-bug non-vacuous. BREAKING (unavoidable): an `Owner` is now SINGLE-USE — dispose-then-refill on the same owner releases each new item at once; nothing in std/examples/docs/kolt does it; the ONE shipped pin that did (`inference/borrows.rs::owner_disposes_subscriptions_across_re_renders`) was rewritten to mint a fresh owner per render (same claim, same `2`) inside B291's commit with a `breaking` note — a stop-condition case the lane took and disclosed. OPEN Q: a releasable-and-refillable owner wants a second verb (`Owner::release()`), not added.
- B292: no catch seam existed → two runtime helpers in the `__db_exec_guarded` shape, `__with_finally` (try/finally) and `__guarded` (try/catch → `Option<str>`), registered in `EXTERN_HELPERS` with matching interpreter arms (`FailureKind::Thrown`). `drain` runs the wave under the FINALLY and rethrows nothing extra (the original error keeps its type/stack; the rest of its wave is abandoned); `Owner::dispose` takes the opposite guard (`guarded` per cleanup, first failure panicked after the group is released — without it B291's idempotence would leak everything behind a throwing cleanup). Ir (node --jitless, deterministic): 20k drains × 16 notifies +0.436%; 1,250 × 256 +0.031% → ~1.3 kIr per DRAIN, 0 per notify. OPEN Q: the item said "rethrow AFTER the wave" (catch-and-continue) — built the finally (abandon the wave); a one-line swap to `guarded` finishes the wave at ~1.3 kIr + one Option per notify.
- B283: reproduced `same-turn:up=2 down=1` (control born inside: 0/0). Narrow fix: `releasing_turns` (stack of `Option<Turn>`) published by `Subscription::dispose` for the hook's duration (under `with_finally`), read by a new `at_release_settle`; `at_settle` refactored onto `defer_to_turn(turn, id, action)`, its own rule untouched; THREE rpc.vl release paths switched (`RemoteSource::release` ~3081, `KeyedSource::release` ~3721, `KeyedSource::release_key` ~3777 — the brief named one). After: 0/0; the four A25 pins + the macrotask hop pin green throughout.
- B277: a third helper `__with_finally_async` (separate from the sync one — the sync one is on the drain hot path), declared in time.vl; the driving loop clears `running` on EVERY unwind; `pending`/`timer` untouched (the next `run` opens a fresh window). Pin planted non-vacuous.
- A86 DROPPED, correction to the item AND the brief: (1) `impl Source<type I: Source<type U>> { .. }` PARSES but `self` is then a bare trait type — "cannot call 'sub' on a value of bare trait type 'Source'" — the spelling is unusable for a body, not merely unselectable; (2) `impl type S: Source<type I: Source<type U>>` (MaybeSignal's shape): at one level the trait's argument is NOT bound from the receiver (`outer.flatten()` "never fully determined"; annotated it runs), and with the NESTED bound the receiver resolves to no concrete impl ("internal: a call resolved to `Source`'s requirement `get`, which has no body") even for a concrete `SignalCell<SignalCell<i32>>`. The `or` blanket (`impl type S: Source<Option<type T>>`) RUNS over a RemoteSource but `T` is not inferred at the call site (`client.notes.or([])` would need an annotation) → both mirrors' `or`s stay. Two `#[ignore = "A86: …"]` pins in `inference/traits.rs` record the gaps (verified failing under `--run-ignored all`).
- Shipped artifacts edited (disclosed): 10 corpus goldens regenerated across the three std commits (every diff read: the new Owner field, the guards, the two helpers, mangled-name shifts); one corpus WITNESS retyped (`vilan/test/reactive-on-change.vl`, `$F/$G` → `$M/$N`, mangler shift, claim unchanged); `transformer.rs` (3 helpers + rows) and `interpreter.rs` (3 arms).
- Gates: cli 130 (reactive_lifetimes/channels/debounce/cancellation/owned_nursery/corpus/release_scripts/ci_ignored_pins/examples/ui_rows/router/service_layer), core inference+docs 3783, std full-scan, clippy/fmt 0, `vilan fmt` idempotent on four .vl.
- FINDS → candidate items: (F1) a `Debounce` whose CALLBACK throws killed the loop the same way (fixed by the same finally) but is NOT pinned — a free-task failure reports to the console and `debounce.rs` asserts stderr empty; wants a harness that can observe a free-task failure; (F2) `__with_finally`/`__guarded` are now emitted into every program touching `std::reactive` (Owner::dispose always reachable) — two small functions of bundle weight, unmeasured; (F3, from A86) B: an inherent blanket over a bare-trait SUBJECT (`impl Trait<..> { .. }`) parses and then refuses every use of `self` — refuse the head with a steer to `impl type S: Trait<..>`, or make it mean that; (F4, from A86) B: a blanket `impl type S: Trait<type I: Trait<type U>>` — the trait's argument is not bound from the receiver at one level, and the nested bound resolves the receiver to no impl (internal error); the two ignored pins are the exhibits — solver territory, A86 blocked behind it; (F5) design: `Owner::release()` (refillable owner) if any app wants dispose-then-refill.
- Not built: B291's dev-build assertion (a late registration is now a legitimate async shape; std has no dev-build seam).
