# a65 — single-lane order (module directories), 2026-09-07

- Lane a65 off a5473227: cd2ff4c5 core (`resolve_module_file` takes a `::` path; `collect_module_paths` whole path; loader resolves the LONGEST module prefix, then a bodiless-directory namespace prefix; normalization before the canonical drain so flat programs' load order/ids/bytes are unmoved — M11's keystroke pin went red on raw paths first), 8e08af4e editor (completion through a bodiless directory; `pkg::` offers directories; go-to-def on a module segment; BUG FIXED: the completion listing dropped anything spelled `lib`, hiding a directory named lib), 7e5db561 spec/docs/changelog (names.md §4.1/§4.2; tour "Directories"; markdown golden regenerated).
- Scope decision PINNED NO: a module's submodules live in `module_children_scopes`, consulted only by the import-path walk; `import pkg::lib` binds lib's own items only. A child loading before its parent creates the parent node and the parent adopts it (`module_nodes`). `declared_entry_module_names` names an entry at any depth. One NEW ledger row (the pure-namespace refusal). Corpus `module-dirs.vl` + `nested/…` (manifest row only).
- Kolt recipe (proven on a scratch copy): `mkdir -p src/lib && git mv src/input_system.vl src/overlay.vl src/lib/`, rewrite `pkg::input_system`/`pkg::overlay` → `pkg::lib::…` (6 import lines), then `vilan fmt src` (REQUIRED: the new spelling re-sorts the import block).
- FINDS → items/rulings: (1) longest-prefix beats an ITEM of the same name (`a.vl` declares `b` AND `a/b.vl` exists → the file wins, documented, not an ambiguity error) — OWNER RULING wanted (rec: ambiguity error, consistent with `a.vl`+`a/lib.vl`). (2) a pure-namespace module entity is attributed to SourceId(0) at EMPTY_SPAN → go-to-def on such a segment lands at the entry's top (small E). (3) `own` is a keyword undocumented in the tour (docs small). (4) build-hooks.md:1035's generated-root note may now move under `root` (a follow-up on the build-hooks paper). (5) loading `pkg::lib::util` also loads lib's body if it has one (Rust's model) — a child import can pull a heavy parent; intended cost, state it in the docs.
