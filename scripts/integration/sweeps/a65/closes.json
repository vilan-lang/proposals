{"date": "2026-09-08", "order": "a65 (single lane between Orders 29 and 30)", "closes": [
{"id": "A65", "lane": "a65", "commit": "cd2ff4c5", "text": "module DIRECTORIES, Rust's rule: `a.vl` or `a/lib.vl` is module `a`'s body (both stays the ambiguity error), `a/b.vl` is `a::b` whether or not `a` has a body, nesting unbounded, the byte-exact case rule per component. `resolve_module_file` takes a `::` path; `collect_module_paths` records the whole import path; the loader resolves the LONGEST module prefix, then a bodiless-directory namespace prefix, with normalization ahead of the canonical drain so every flat program's load order, ids and bytes are unmoved (M11's keystroke pin went red on raw paths first). Scope decision PINNED NO: submodules live in `module_children_scopes`, consulted only by the import walk — `import pkg::lib` binds `lib`'s own items only. A child loading before its parent creates the parent node and the parent adopts it. Editor (8e08af4e): completion through a bodiless directory, `pkg::` offers directories, go-to-definition on a module segment, and a bug fixed on the way — the completion listing dropped anything spelled `lib`, hiding a directory of that name. Spec names.md §4.1/§4.2, the tour's \"Directories\" (7e5db561); ledger row 405 (a directory with no body is a namespace, the refusal names its modules); corpus `module-dirs` + `nested/`. Sealed at f0f4e301 (CI green on all eleven jobs; the census row for the new golden was the one repair). Residue: A67, E152, N64. Kolt's recipe: `git mv src/input_system.vl src/overlay.vl src/lib/`, rewrite the six imports, then `vilan fmt src`"}
]}
