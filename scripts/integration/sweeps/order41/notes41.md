# Order 41 — running notes (cycle 59; GO 2026-09-24 off vilan next @1265ea5d)

## GO
- The owner: "Go with all recs. `isize` can exist too if useful." R-a–R-e as recommended; I5 ruling 4 amended (isize permitted if free) — stamped on I5, A124, B382, C14, M86, A112, F18, E222, F33, F27, A122.
- Ledger next id 566; shared_census literal 140; 35 unreleased breaking entries; kolt RED 4/4 (A125, owner-owed).
- Playground store.vl:14 → `dyn Signal<List<Note>>` (integrator, at GO).
- Worktrees: vilan/.claude/worktrees/{hygiene,solver,index,reactive,collections,editor,native}-41 off origin/next @1265ea5d; proposals/.claude/worktrees/papers-41 (branch papers-41).
- Playground store.vl:14: `dyn Signal<List<Note>>` refuses line 27's `self.notes.update(..)` (`update` is INHERENT on SignalCell, not a trait member — the pipeline paper §5 (2)'s class); the field takes the concrete `SignalCell<List<Note>>` instead (plus its import); `vilan check` re-run after the import fix — see the next line.
- Playground todo on 1265ea5d3: `vilan check` CLEAN after four rot lines (store.vl: `SignalCell<List<Note>>` + its import; client.vl: `import std::print` removed, `.bind_each` (A99) → `{each_by(..)}` in a child hole with `note.map(|n| n.text)` — each_by hands the row a SignalCell). The exhibit had rotted BEFORE dyn-40 (print + A99); a sweep item: the playground's six programs want a check in the estate sweep (file at the sweep).

## papers-41 — REPORTED 2026-09-24 (branch papers-41 merged → proposals 03c7a5e): three documents
- A122 `proposal/tuple-module.md` (3da7c23): `std::tuple` — `Tuple` blanket, `TupleKey<T, U>`, `map` as the comprehension's spelling (one closure cannot type per position), `divorce` in three lines; PREMISE corrections: `impl type T: (2..) with Tuple` does not PARSE (an impl binder takes trait bounds only — the grammar needs one alternative); a `for` over a mapped tuple wants a new rule (body checked once, element abstract); kolt channel.vl's hand form is at :26/:47 (the round trip), not :19. Q1–Q7 with recs (owner). Build after I5 S2 + A124 S2c (Order 42+), and after B397.
- F27 R3 `platform-coloring.md` §8 (892a6af): fenced twin ITEMS — coherent iff platform sets are disjoint; leg-scoped collection reusing E113's per-platform analysis; the new invariant stated; Q1–Q6 with recs; M–L after R1. §8.8 assumes R1 analyses a fenced item under its own platform — reconcile at editor-41's report.
- CUT PLAN `sweeps/order41/papers-41/cut-plan.md` (4240d45) + the dry run: THE CUT REFUSES on 1265ea5d — F0: the E122 entry (CHANGELOG.md:1893) was reworded inside merge f261a90d and `git log -S` skips merge diffs → `<!-- commit: f261a90d -->` above its family marker (INTEGRATOR one-liner at integration; lasting fix `-m` in cut-release.sh, a release_scripts pin). F1: `RELEASE_FILES` (cut-release.sh:621–624) omits vilan-rt/vilan-rt-sqlite/vilan-rust → derive from crates/*/Cargo.toml (native-41 with N125 — told). F2: THIRD-PARTY-NOTICES.txt says 0.40.0 (regenerate at the cut). F3: 12,656 chars of headroom before a breaking entry drops from the release body. F4: A95's deprecation (:700) has no `<!-- deprecates: -->` markers (INTEGRATOR one-liner: two lines, `std::style::Style::attribute` + `::within`). Trains: I5 S2 first (moves no JS golden), A124 S2c second; Q1–Q5 with recs.
- FILED from the report + GO: B397 (HIGH MISCOMPILE, shipped `combine` over a tuple-valued source — REPRODUCED by the integrator: `x=1,2 y=c l=undefined`; solver-41 told, tail), B398 (mapped tuple over `dyn` crashes at runtime), B399 (pack element bound does not reach `U`), E223 (the "unbounded" wording), N127 (the playground exhibits rot unchecked). 118 open.
- INTEGRATOR on next (after the GO cut of the worktrees — lanes are based on 1265ea5d; the merge helper folds this): the cut plan's F0 + F4 markers landed in CHANGELOG.md (`<!-- commit: f261a90d -->` above the E122 entry; two `<!-- deprecates: std::style::Style::attribute/within -->` above A95's) — release_scripts + release_gate 25/25; next @f90d24d6, pushed. F1 (RELEASE_FILES) is native-41's with N125 (told); B397 is solver-41's tail (told).
- CROSS-LANE CONTAMINATION (2026-09-24): hygiene-41 and native-41 both wrote a `changelog.py` helper at the SHARED scratchpad root; hygiene's copy (hard-coded to its own CHANGELOG path) overwrote native's, so native-41's three entries (F33, the seams, the builder) landed in hygiene-41's CHANGELOG.md — two inside its N121 commit 79edbcdd, one uncommitted. native-41 was (correctly) denied editing another lane's worktree and reported; hygiene-41 told to remove all three itself; native-41 re-adds them in its own follow-up commit. RULE for briefs42: a lane's scratch files live under `scratchpad/<lane>/`, never the root; a CHANGELOG helper takes its path from `git rev-parse --show-toplevel`, never a literal. Verify at merge: hygiene-41's CHANGELOG diff carries only its own eight heads.

## collections-41 — REPORTED 2026-09-24 (tip 8994d395 on 1265ea5d; merges AFTER solver-41 — ef172d1e is B395 d476202a cherry-picked, drop at the merge): S3b 896ff39f, M86 b979c0af (premise corrected: the copies were `size()`/`set_at`/`reconcile_to` through `SignalCell::get`, not the notify copy; `ListCell::peek` = the borrowing read, `with` is a keyword — OWNER Q), the switch 8994d395 (ruled shape; B396 unlanded so `each()` carries no bound). Gates: full union 8,315 with ONE flake (`replay_differential::an_entry_shaped_world…` at loadavg ~40, green twice alone — NOTE for the seal), cli 159, natdiff 29/29 both, docs 18, book_sync 14, clippy/fmt/doc 0. Shared census 140 (−1 each_by rows, +1 the list's own cell). Copy-elision census list-cell 42 → 43 static sites. FILED: B400 (MISCOMPILE: copy elision with a live `read()` temporary; a closure's `&List<T>` param admits `cell.read()`), A129 (RULING: Splice by position loses row identity; `set_all`'s doc claim false for keyed consumers), M87 (`KeyedCell::locate` copies), F35 (ListCell/map_each not native). Paper edit owed (§1, §9): the 22,347 Ir figure was a DOM-less template — end-to-end numbers above. Merge overlaps with reactive-41: shared_census literal + ui_rows.rs.

## hygiene-41 — REPORTED 2026-09-24 (tip 1b0a9391; pushed origin/hygiene-41; MERGING via merge_lane.sh → merge-hygiene-41.log): N123 0fe4e487, N121 79edbcdd (NEW `vilan-core/src/stack_guard.rs`: `with_declared_stack` + `ensure_sufficient_stack` at FOUR sites — three in analyzer.rs + `util::RecursionGuard::enter`, the funnel B385/B387 went through; the LSP's 128 MiB thread declares; fails closed; +0.26 % Ir; SECOND BUG fixed: `analyze_source`'s inner fence swallowed a caught panic with NO diagnostic), N124 d704e290 (premise partly wrong — the two-trait scenario is refused as ambiguous regardless; the pin calls `mono::resolve_inherited_default` directly), N126 e940427b (option (a); the finalizer window MEASURED 154–216 ms, GC-driven; CI's flake reproduced on demand by a 20 ms delayed-close preload), N120 0703fd83 (the CLI reads rustc's `arithmetic_overflow` and names the PROGRAM's overflow + the emitted expression — no source map back to vilan; index-41 reads the sentence), N122 18a025d2, A127 a049edd6 (`HeaderedHttpTransport`; fetch.vl touched — outside its map), A128 STOPPED (brief vs paper Q2 — OWNER Q, proposal `FocusScope::on_leave`). Contamination cleaned in 1b0a9391 (its CHANGELOG diff = its six heads, verified). Gates: union 8,321/8,321, natdiff 30/30 both, goldens unmoved, clippy/fmt/doc 0, release 25/25. Outside its map: util.rs (one probe line), vilan-core lib.rs (module + fence fix), vilan-lsp document.rs, fetch.vl, four docs pages. FILED: B401 (inherited-default lookup ignores impl admission), N128 (N121's remainder: for_each_child probe, undeclared CLI/wasm threads, the stale 11.3 KiB figure), N129 (native_differential under plain cargo test; CLAUDE.md's equivalence claim false), N130 (Windows clippy --tests red on split.rs's unused Instant — pre-existing; integrator one-liner).
- MERGED hygiene-41 → next 7f32f846 (clean; gates: natdiff 30/30 default + 30/30 env; cli set 201; core set 241; lsp 860; release_scripts 22; split 13), pushed. Next in order: editor-41, solver-41, then collections-41 (waiting on solver's B395).

## solver-41 — REPORTED 2026-09-24 (tip 48333165, 10 commits; pushed origin/solver-41; MERGING → merge-solver-41.log): B395 d476202a (cause was NOT subject_applies — `inherited_default_candidates` kept the blanket; + `impl_bounds_hold` binder substitution), B389 94a648d1 (the five positions + a stall step; native reads literal widths from `expr_type_ids`), B396 89e1faa7, B390 0a05aa58 (WORSE than filed: a refused impl made every type satisfy its trait), B391 940bf43a (the export gate asked `source_of` — the generated-code sentinel), B392 3a41b038 + efed0385 (two shipped pins that asserted the deadlock now assert the fix), E220 ce280830, e0dd9c51 MISCOMPILE `1 / 4.0` → 0 (filed B402 for the record), B397 48333165 FIXED (unrolled per element). B393 PROBED: native is the WRONG side → B403 (analyzer cannot-infer); N125's four stay per-emitter (native-41 told). Gates: union 8,353/8,353 under VILAN_NATIVE_DIFFERENTIAL=1, natdiff 31/31, doc 0, clippy/fmt 0; no golden moved; no ledger rows. FILED: B402, B403, B404 (BigInt literal emitted as a number), B405 (numeric mixing silent — ruling). Incident: the lane's scratch target filled /tmp to 100 % for minutes (3.4 GB freed) — other lanes may have seen ENOSPC; RULE for briefs42: scratch build targets under the worktree's own target dir, not /tmp. index-41 told (B389 landed; the subscript note; expr_type_ids).

## index-41 — REPORTED 2026-09-24 (tip 7c746d47; pushed origin/index-41; merges AFTER solver-41 + collections-41 — d6eb60c4 is B389 94a648d1 cherry-picked, drop at the merge): A126 c936f3d7 (i64/u64 do not exist; f32 was the miss), E218 97acd1b3 (touches vilan-lsp document.rs — editor-41's file), I5 S1 12750bb0 + 7c746d47 (the type, the family, wire on i32's lane, the subscript's two-type admission; three corpus programs; whole set 123: 85/38/0). `isize` NOT built (not free — own wire/range rulings; nothing asks). S2 PREPARED (sweeps/order41/index-41/run_s2.sh): 105 signatures, 214 codemod edits, residue 0, goldens 121 identical / 17 MOVED (§5.4 was wrong), kolt FIVE files. Gates: union 8,378/8,378, natdiff 31/31 env, interpreter 151, release_differential 140, infer_differential 141, goldens/grammar_sync/markdown_golden/book_sync green; clippy/fmt/doc 0. Six hours wall at loadavg 30–58. FILED: B406 (literal to a generic ctor in a struct-literal field), B407 (negative literals accepted at unsigned types — must land before S2), I6 (RULING: negative-to-unsigned differs by backend), F36 (two native gaps).
- MERGED solver-41 → next 9ffa397e (clean; natdiff 32/32 default + 32/32 env; cli set 141; vilan-core 6,367; lsp 860; release_scripts 22; split 13), pushed. Next: collections-41 (B395 duplicate to drop), then index-41 (B389 duplicate).
- MERGED collections-41 → next 30dfd05b (TWO hand folds: the cherry-picked B395 head duplicated in CHANGELOG — one block dropped, parity 668/668; `inference/bounds.rs` an add-at-EOF conflict with the lane side EMPTY — kept HEAD's; the tail replayed by scratchpad/resume_merge.sh = merge_lane.sh from renumber_ledger on). Gates: natdiff 32/32 default + 32/32 env; cli set 148; core set 4,532; lsp 860; release_scripts 22; split 13; pushed. LESSON for briefs42: a lane that cherry-picks another lane's commit merges with a duplicate CHANGELOG head and an empty-side EOF conflict in any test file both touched — merge the source lane first and REBASE the picker (drop the pick), or budget the hand fold. Next: index-41 (same shape, B389).
- index-41 merge ABORTED (helper exit 2 + five analyzer.rs hunks, bounds.rs ×2, native_differential.rs ×2, the copy-elision census — the cherry-picked B389 d6eb60c4 against solver-41's later edits of the same lines); integration restored to 30dfd05b; index-41's agent RESUMED to rebase onto origin/next @30dfd05b dropping the pick and re-gate. RULE (briefs42): a lane that needs another lane's fix cherry-picks for its OWN gates only and REBASES before reporting once the source lane is on next — the integrator never hand-folds a cherry-pick.

## editor-41 — REPORTED 2026-09-24 (tip 125b8210; pushed origin/editor-41; MERGING → merge-editor-41.log — the lane asked to merge BEFORE solver-41, which is already on next; its analyzer touch points (registration arms, ModulePlatform, check_deprecated_reexports, the E119 note, Program/Workspace fields) look disjoint from solver's; NODE SHAPE changes — Struct/Enum/Trait/Let/Impl + `ItemLabels`, Export a third field, EnumVariant a fourth, new `Node::ModulePlatform` — break other lanes' patterns at BUILD, the helper's tripwire): E222 cbc31fe4, E221 ae541ba1 (no docs generator exists — the 'Internal' heading not built; NEW `[lints]` section), F27 R1 caaf6620 (`[platform("browser")];` first statement; `platform_color.rs`; status-bar 'analyzed as'), B382 b79db3bc (`[deprecated]` on types + `export [deprecated] import … as`; the steer on the Export node — Import would have busted node_size), E219 125b8210 (second bug: short `[extern]` lines hid over-budget signatures — four estate spots reformatted incl. rpc.vl/fs.vl, which hygiene-41 also touched). Gates: union 8,370/8,370; natdiff 29/29 both; goldens, grammar_ebnf/sync, node_size, fmt suites, tsc, clippy, fmt, doc all green. Ledger 3 NEW rows; RULE_STATEMENT_SITES 48 → 50; TOKEN_MODIFIERS 4. OWNER confirmations: the `;` spelling; nominal labels affect resolution only; item fences decide only when the package colour admits none; `internal_use` exempts the declaring module; B382's declaring module silent. FILED: N131 (lsp suite flakes under plain cargo test; the M19 `references::` set unexplained), E224 (function deprecation silent at import leaves). Find 1 (R3's invariant: a server leg still type-checks a browser-declared module under node) → F27 R3's paper owns it. 7 h wall at loadavg 35–50.

## reactive-41 — REPORTED 2026-09-24 (tip b3fa0a47, three commits; pushed origin/reactive-41; merges AFTER editor-41): S2a bb798cbc (`on_settle` takes the leaf's `Subscriber` record, DEFAULTED; `observe` = mint + attach; a diamond fires once IN A TURN, twice inline), S2b 29c0f2bb (`Map<S, T, U>` etc. — the paper's arities do not compile, B411; constructors `[internal]`; `.cell()` adds no Shared site — census 143, +3; `dispose` → `release_under`, 8 goldens move runtime-identical), guide b3fa0a47. S2c PREPARED (`s2c-flip.patch` applies + builds on b3fa0a47; `s2c-combine.patch` throws until B398): 19 red — B408 (5), A25 (8, owner), 4 to re-derive, 1 diagnostic, the NO-CYCLE gate; census: kolt 17 + 6, examples 9, Rust consts 30 (11 fixed), rpc 4. SEVEN defects, all reproduce on 9ffa397e: FILED B408 (blanket method unreachable through a bound — Order 42 solver FIRST), B409 (B395 class, same-trait bound), B410 (MISCOMPILE: tuple trait argument override not selected), B411 (nested-binder closure param untyped), B398 confirmed (mapped-tuple dyn coercion), B412 (generic erasure to dyn), F37 (native partial move). Gates: union 8,321; natdiff 29/29 env; clippy/fmt/std fmt/doc 0; copy census 405. OWNER DECISIONS (stamped on A124): B408 first; A25's law moves to the leaf; the no-cycle gate vs a module-level `.cell()`; `Resource`'s public name (`resource` is a keyword). Contamination: the lane overwrote `probe/vilan.toml` (restored) and possibly `build0.log` at the scratchpad root — another lane's (native-41's?) — the per-lane scratch rule again. For native-41 (F34): nodes plain; `Switch`/`Combine` HANDLES read a context → a `dyn` holding one is refused natively by name.
- MERGED editor-41 → next fee137a9 (clean — the node-shape changes broke nothing on next; natdiff 32/32; cli set 274; core set 4,545; lsp 879; release_scripts 22; split 13), pushed. Next: reactive-41 (merging), then index-41 (rebased tip awaited), native-41 (rebases after reactive).

## native-41 — REPORTED 2026-09-24 (tip 52c7d4b2, 8 commits; NOT pushed — rebases once on reactive-41's merge by its resumed agent, then merges LAST): F33 cb5b2e5d, seams 6ad60c2c (SHA-1/base64/NodeHash/now_millis; std's private `Hash` → `NodeHash`), builder 39045eca + 52c7d4b2 (14 rustc errors in five classes fixed in general; the first version's private `AsyncGate` tripped the visibility check — replaced), rpc server ac5fc1e7 (keyed chat halves pulled apart; hash 43077e29 on both; a reentrant `update` read REFUSED natively by name), THE EXIT 629c69a5 (`kolt_shape_server.vl`; host census 0; kolt's real server.vl 69 → 4 gaps), F34 83f4db67 (on today's reactive.vl; the `.cell()` re-run after the rebase), N125 3d59e59e (+ `release_files()` derived — the cut plan's F1 DONE), C14 S4 NOT built (by construction natively; needs a native leak gate). Whole set 122 / 89 / 33 / 0 / 0 / 0; async 7/4/3/0. Gates: union 8,335; natdiff 41/41 whole set; rpc/golden 146; clippy/fmt/doc green; Windows + audit legs NOT run (no dependency change) — the seal runs them. The lane REWROTE its first three commits so each carries its CHANGELOG entry (the contamination's fix on its side). Commit trailers say Opus 5.5 (the model that wrote them) — fine. Touched outside its map: analyzer.rs (an additive view record), AGENTS.md (one word), rpc_server.vl, a corpus manifest row + interpreter exclusion, one anchor line. FILED: F38 (and_then's U unbound natively), F39 (reentrant read through an alias panics), F40 (RULING: OS randomness natively — rec a `vilan-rt-crypto` crate). F36(a) confirmed. native-apps.md §12 column added.
