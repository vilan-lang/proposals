cut v0.41.0 — 2026-09-24
against 1265ea5d  Two lowering sketches in the native emitter's doc comments are text fences, not Rust: rustdoc compiled the indented blocks and CI's doc-test leg went red (the seal runs nextest, which skips doc-tests)

ci (§7.2 step 4, backlog L17) — ci.yml on origin, at the commit that becomes the tag

  ok    ci.yml is green on origin at 1265ea5d

sweep (a) — each entry's commit, against 1265ea5d

  ok    c11a6edc  A reordered list is reconciled in one pass: `each`, `each_values`, `each_by` and `reconcile` now ask their KEY for a hash as well as an equality (`K: PartialEq + Hashable`), and a 1,000-row reversal costs 1,000 key comparisons instead of 500,500.
  ok    4c5fb6cc  A bare trait at a struct FIELD is refused again, and the message names `dyn Trait`.
  ok    bf2e4ad7  `std::style::prelude` publishes the 46 names it was designed with, and not the 21 internal alias spellings that were riding out on `export *;`.
  ok    60794a42  `std::random::range` is the range its doc always said it was: `[low, high)`, so `random::range(1, 7)` is a d6 and never answers 7.
  ok    c69fb566  `Self<i32>` is refused, with the steer: `Self` already names the impl's subject WITH its arguments.
  ok    b850d369  `std::reactive` has a change structure: `DeltaLog<O>`, `DeltaCursor`, the `SeqOp`/`MapOp`/`SetOp` vocabularies and a `DeltaSource<C, O>` bound, with `KeyedCell` re-based on them and not one wire byte moved.
  ok    63161d83  An awaited `void` rpc stub answers `Result<void, RpcError>`, not `Option<RpcError>`.
  ok    141bea80  BREAKING: the Wire visitor's receivers are `&mut`, and the ten `Shared` cells they were forcing are gone — `std::json` and `std::binary` now construct no cell at all.
  ok    58e852e5  An explicit type ARGUMENT survives an argument that knows less, and a trait's own arguments stop being inert: `Signal<Option<str>>::new(None)` means what it says.
  ok    e493ffb1  `Option::expect`, `Option::unwrap_or`, `Result::expect` and `Result::unwrap_or` take their argument `lazy`, so the fallback and the message are built only on the path that needs them.
  ok    928ccedb  A `css` block's declarations are CALLS: `property(value);`, not `property: value;`.
  ok    177be8de  `lazy` is a keyword, and a `lazy` parameter defers its argument to the callee's first read of it — evaluated at most once, late, and not at all if the callee never reads.
  ok    e6dd697b  `Style::attribute`'s and `Style::within`'s `Option<str>` enters its one-release deprecation window, with the condition value as the steer, and the estate moves ahead of it.
  ok    7abec3ee  A styling condition is a value you write `.not()` on, `child_relation` is deleted, and an ancestor guard takes any condition an ancestor can match — the condition surface A95 designed, now the documented one.
  ok    e535baa8  The six `View` parent methods are RETIRED: `when`, `swap`, `swap_split`, `bind_each`, `bind_each_values` and `bind_each_by` are gone, and the free slot values of `std::ui` are the whole surface.
  ok    5c91ae33  A type's method namespace is now per IMPORTING FILE, and two packages that each extend one type can both be installed.
  ok    7a6fc959  std curates its export surface — 548 of its 856 declarations are `export`ed, 308 are the modules' own machinery — and the suppression that hid std from its own visibility warning is gone.
  ok    2bbea34e  A type segment REPLACES an import path's namespace — enum and struct alike — so `Length::rem` is the static in the one file that also declares a module-level `rem`.
  ok    fe542196  A plain import of an item its module does not export WARNS, with the reach marker as the fix — the warning that lets the default flip without breaking a program.
  ok    fe542196  `[doc(hidden)]` is retired, and the `pub` rule stops telling a Rust writer to delete the word.
  ok    068ae50b  `export (helper);` and `export * helper;` are refused instead of compiling clean and meaning nothing.
  ok    c5228523  `Style::child_relation` checks its token against the two relations it exists for, so the public raw-selector hatch underneath `children` and `divide` is closed.
  ok    9b45a253  `[derive(Wire)]` emits the wire codec and nothing else: the JSON pair it used to emit beside the §6.1 visitor is now `[derive(Json)]`'s alone, and a type that wants both asks for both.
  ok    c64ac090  `Style::attribute` and `Style::within` take `value: Option<str>`, so a PRESENCE condition has a spelling — `attribute("data-selected", None, ..)` is `.sX[data-selected]` — and the new `not(inner)` marker negates the condition immediately enclosing it, which is what finally spells "hovered AND not disabled".
  ok    d868feb8  BREAKING: the stub of an `[rpc]` method that returns a source is now SYNC and answers the mirror itself — `fun get_message(self, id: str): RemoteSource<MessageBody>`, where it answered `Result<RemoteSource<MessageBody>, RpcError>` before — and it makes no call at all until something leases it.
  ok    164a4dcd  `fun sum<P: Add, Q>(a: P, b: Q): P { a + b }` compiled, and `sum(1, "two")` printed `1two` — a `str` handed back through a signature the call bound to `i32`.
  ok    875484d3  `fun f<T>(x: T) { if x { } }` compiled, and every instantiation reached the host's truthiness test: `f(1)` took the branch, `f(0)` did not, and `for x { }` over a truthy value looped forever.
  ok    220d0637  A `::` path may no longer cross a line break, and `import a::b::c as d;` is the way a long path gets shorter.
  ok    1c36c9e1  `Source::on_change` is now the trait's REQUIREMENT and `sub` is derived from it — an implementation writes `get` and the LAZY attach, and the whole eager family arrives free.
  ok    f17b682c  `for !(x is Some(let n)) { print(n); }` compiled and read a payload that is not there — the shape an `if` has refused since B195. Both of the other two conditions now install the same polarity frame, and the captures they used to bind under a negation are refused.
  ok    4f93d174  An operator trait's method is now required at impl time: `impl P with Add { }` was accepted, `check` reported no errors, and `P { n = 1 } + P { n = 2 }` threw an uncaught `not implemented yet` naming no type, no method and no span.
  ok    6990ba4d  `Optimistic<T>` gains a second type parameter — `Optimistic<T, S: Signal<T>>` — so the cell wraps any signal, not only the canonical one.
  ok    57c450d0  An `is` capture is in scope exactly where its test is known to have PASSED — the then-branch and the rest of the condition after an `&&` — and the spec sentence that said it was "scoped to nothing" is gone. The one program this stops is a read in the other arm of a `||`, which used to compile and answer garbage.
  ok    ef8e5634  `fun main` takes no parameters, and writing any is now a compile error that names the argument door instead: `std::process::args()`.
  ok    6f5b5e62  A native operator whose LEFT operand is a generic parameter now needs a bound that PROVIDES the operator's method — `+` needs `T: Add`, `-` needs `T: Sub`, `==` needs `T: PartialEq`, `<` needs `T: PartialOrd` — and the estate's whole migration was one `<T>` → `<T: Add>`.
  ok    55464670  UNSOUND, fixed: a supertrait blanket no longer makes EVERY type satisfy its bound.
  ok    1dfc86a4  A numeric literal assigned to a narrow binding takes that binding's width natively — `mut i: u53 = 5; i -= 1;` no longer hands rustc a program it refuses.
  ok    8258b73c  The subscript operator checks its INDEX: `xs[1.5]`, `xs["a"]` and `xs[true]` are refused where they used to compile and answer `undefined`.
  ok    c86db7e3  A `lazy` parameter reached through a generic BOUND is thunked at the call site, so a program that mixes a dispatched call with a direct one no longer dies with a `TypeError` it checked clean.
  ok    f7a08dab  An unused `let` whose initializer is a block, an `if`, a `match` or a loop is no longer DROPPED with everything inside it.
  ok    54d42be9  Five latent native defects, each of which a slice-2 feature was the first program to REACH — one of them a wrong answer.
  ok    76dd23a2  A module-level `mut` aggregate mutated in place is mutated in the CELL natively, not in a copy of it — and so is a mutably-captured one reached through a `&mut self` method.
  ok    49de3915  A generic recursive type no longer overflows the compiler — and the language server no longer dies with it.
  ok    2321790e  The JSON codec's READER enforces the type it is asked to read: a decode that should fail now fails, instead of succeeding with garbage and leaving the reader unpoisoned.
  ok    3ccbf2da  `[derive(Json)]` on an enum no longer CRASHES on a document that is not an object: `Shape::from_json("null")` threw a host `TypeError` out of the emitted tag helper, and `FromJson` promises it cannot.
  ok    d0911b74  An arithmetic expression of unsuffixed literals takes its type — and its OPERATOR — from the context it lands in: `rem(4 / 16)` for `fun rem(value: f64)` is float division and `0.25`, where it printed `0`.
  ok    ee9edd4e  An aggregate that holds a CALLBACK compiles natively — four classes a `std::http` server program reaches and no corpus program does.
  ok    f35e178b  Inside a trait DEFAULT body `self.member(..)` is the TRAIT's member, always — an implementor's same-named INHERENT member no longer hijacks it, and where that member was `external` the specialized default no longer emits a name nothing defines.
  ok    835c29d2  Five wrong answers the native backend was standing on, four of them found by its own whole-corpus differential and none of them visible to the default suite.
  ok    7d9427dc  A `Shared` handle bound by a MATCH CAPTURE aliased the cell it read from.
  ok    dbea74f3  A method call written inside an UNANNOTATED closure body is type-checked: an `Option` stored where a `List` is read no longer ships.
  ok    f70f7941  No styling condition can carry the slot key's own `:` any more, and no reader splits a key by hand — B311's class closes at the name, and B322's three latent halves close with it.
  ok    8ceca2b7  A pseudo-class name carrying the slot key's own `:` is refused when another condition WRAPS it, instead of compiling to a strictly weaker selector.
  ok    25ca2972  `Map::entries()` on a `Map<K, (A, B)>` handed back a pair the caller could not read — `let (key, slot) = entry; let (class, declaration) = slot` gave `class = "class,decl"` and `declaration = undefined`, silently — because a tuple built inside a GENERIC body was laid out by the layout the analyzer saw and read at the caller by the instantiated one.
  ok    1df74952  Overriding a trait DEFAULT in one impl silently changed how a DIFFERENT type's inherited copy of that default read its ambient context — `self.cache.map(..)` on a `SignalCell` field ran `register_with_owner` against an `undefined` owner.
  ok    986a03ac  `ops.describe(serializer)` on a `List<Delta<K, T>>` compiled and then failed at EMISSION with the never-silent `internal: a call resolved to `Wire`'s requirement `describe`, which has no body` — and the same shape one instantiation later was a silent JSON MISCOMPILE.
  ok    c8d63a4f  `b = a` did not copy: an assignment whose right-hand side was a plain local aliased its source, so `mut b = [0]; b = a; a.push(4)` grew `b` too, and `r = q; q.x = 10` left `r.x` at 10.
  ok    53882371  `n = 5` beside a guard clause whose capture is also called `n` compiled CLEAN and emitted an assignment to a top-level function.
  ok    5b825900  `fun check(): Result<bool, str> { let v = probe()? > 0; Ok(v) }` compiled clean and handed back `Ok(Ok(true))`, and an `Err` from `probe` came back as `Ok(Err("boom"))` — never propagated.
  ok    c7bce95a  `if a is Some(let x) && x.on_drag_end is Some(let y) { … }` compiled clean and then threw on a `None`: `TypeError: cannot read properties of undefined`.
  ok    b368e14e  `let present = x is Some(let n); print(n);` compiled, and read the payload slot of a value that may have no payload — `undefined` for a `None`, `NaN` where the program's own type said `i32`.
  ok    bb7e1591  `fun main() { if flag { ret; } … }` compiled clean and then died at load: `SyntaxError: Illegal return statement`, the whole module refused before a line of it ran.
  ok    8c500409  An `is` capture reached through a call argument is unbound in both branches.
  ok    de685d46  A generic parameter is rigid inside its own body.
  ok    8e0fc1ff  `for x in tuple` compiled, ran, and bound `x` to `any` — so `for x in (1, "two", true) { print(x + 1) }` printed `2`, then `two1`, then `2`, and a `&mut` walk over a tuple silently discarded every write. The loop is now refused.
  ok    600f8ef3  A PARTIAL generic-argument list is now what it reads as — a PREFIX. `tag<Dog>(a, b)` on a two-parameter `tag` binds `T` and lets the arguments infer `U`, where it used to leave `U` abstract all the way into emission and stop the compiler with an internal error asking the author to report their own program; and an OVER-long list, which used to be accepted with the extra argument silently dropped, is refused.
  ok    9b021361  A trait default's `self <op> self` now dispatches to the type the default is specialized for: `trait Doubler with Add { fun twice(self): Self { self + self } }` made `Money { cents = 21 }.twice()` print `2`.
  ok    de0a9035  `-` and `!` now hold their operand to an admitted set: `let flipped: bool = -true` compiled and held `-1` — truthy, and equal to neither `true` nor `false` — while `-Point { x = 1, y = 2 }` was the host's `NaN` and reading a field of it printed `undefined`.
  ok    d09028f6  A negated `is` test now scopes its capture to the branch that actually matched: `if !(x is Some(let n)) { … }` no longer binds `n` in its then-branch — the branch reached only when the pattern FAILED — and does bind it in the `else`, which is reached only when it matched. The program this stops compiled, ran, and printed `undefined`.
  ok    6b10a69d  Every native operator now checks its LEFT operand, not just `+`: `true - 3` typed `bool`, `"12" - "3"` typed `str` and `Level::High ^ Level::Low` typed `Level` were each a number wearing a type it was not, and all three compiled clean in v0.40.0.
  ok    0ab1066b  A generic type named with no type argument — `fun read(h: Holder)` for `struct Holder<S>` — is refused instead of having its parameter silently erased. The erasure was a live miscompile: it let a `Holder<str>` satisfy a `Holder` parameter and print `seven1` through a declared `i32` return, on a clean compile.
  ok    82331356  An operator that dispatches to your impl now checks the right operand against the `B` that impl declares — until this landed it checked nothing at all, and passed a foreign struct straight into a body typed for another one.
  ok    3c9ffcd7  `T::default()` under a bound now types as `T`, not as the bound — which makes `List::sum` over a struct element actually add: it printed `4` for a wallet holding 40 and 2.
  ok    7850d253  `&&` and `||` refuse a generic RIGHT operand: `fun both<T>(flag: bool, value: T): bool { flag && value }` compiled, and `both(true, Point { x = 1, y = 2 })` printed `[ 1, 2 ]` — the struct itself, typed `bool`.
  ok    9b84d93c  `export impl` no longer dodges a `[service]`'s method refusals: `mut self`, `async` beside `&mut self`, a `__` parameter and a generated member's name are refused through it exactly as through a plain `impl`.
  ok    bd1d4fe7  The binary codec's reader gets the JSON reader's audit: an `Option` marker that is neither `0` nor `1`, a `bool` byte that is neither, and a frame with bytes left over are decode failures.
  ok    5d8fcd98  An rpc call carrying MORE arguments than its method takes is a decode failure, not a silent success — and a no-argument method finally has a decode gate at all.
  ok    1becc367  `i32::from_json("1.5")` is an error, not `Ok(1.5)`: the scalar decoders take the integer lanes' value check the codec reader already had.
  ok    e2358ed9  A `[service]` whose methods live in an `export impl` generates its routes — and a `[service]` that declares nothing at all is refused instead of shipping an empty API.
  ok    c0eb0d07  An unreachable host no longer takes the process down: `HttpTransport` answers `Err(RpcError::Transport(..))`, which is what `Transport`'s own contract always said.
  ok    a6bf7c43  A trait whose supertrait is the bound of its own blanket impl no longer crashes the compiler, and is accepted.
  ok    bf0c806e  A blanket over a nested bound, called from inside a generic body, dispatches: `self.map(f).flatten()` in `fun switch<U, I: Source<U>>` no longer stops the compiler.
  ok    ecdcf9b3  `==` on a bounded parameter inside a nested `match` arm within a closure is pinned — the gap filed as B381 was B379.
  ok    62b52ff8  A generic call whose argument is built from a closure's parameter binds its type parameter: `n.map(|m| Signal::new(m * 2))` infers.
  ok    3078a259  Indexing the value a `Shared<List<K>>` hands back resolves inside a generic body.
  ok    5c9a554c  A generic parameter reachable only through a BLANKET impl's trait argument now binds.
  ok    42323dda  A blanket impl bounded on a SUBTRAIT now outranks one bounded on its supertrait — which is how an optional capability is dispatched.
  ok    548eef91  A native binary that FAILS answers node's shape: the message alone on stderr and exit code 1, not Rust's panic banner and 101 — and printing a host handle is now refused where it is written.
  ok    9eaf5320  The drop planner's enrolment gate stops comparing a struct literal's own expression id against the set of struct DECLARATIONS.
  ok    ab4ee1af  An `external fun` with neither an `[extern(..)]` binding nor a compiler lowering is refused where it is declared, instead of emitting a call to a name nothing defines.
  ok    08cd75f2  A function with a `lazy` parameter is not a closure VALUE: handed to a closure slot, it reached a body that forced a plain number.
  ok    b2ae74c3  A variant constructor's WRITTEN type arguments settle the expression's type, so one wrong payload is one refusal — and a bound on the result can be checked at all.
  ok    21b24d00  A `for` header binds its element with `let`'s binder, so a tuple destructures right there: `for (index, item) in list.iter().enumerate() { .. }`.
  ok    99d83953  A call to a generic function with NO declared return type is typed under the call's own generic arguments: `hold("x")` for `fun hold<C: Show>(body: C) { Holder { body } }` is `Holder<str>`, not the callee's abstract `Holder<C>`.
  ok    71df73c0  `vilan run` of a lone package or a bare file no longer creates a `dist/` holding nothing but the compiler's memory.
  ok    6bdbf94a  A std whose `[library]` declares `root = "."` resolves its own toolchain root, instead of the directory above it.
  ok    9d748536  Completion resolves a type NAME the way the analyzer does — through the scope chain at the cursor — where it used to take the first item in the program that happened to spell it.
  ok    55394ca0  The playground's diagnostics know what the standard library is: "is this source std" and "are this source's definition-site diagnostics frozen" are two sets now, and everything keyed on residence answers correctly where std arrives through the document overlay.
  ok    82e486b2  A written type argument on a VARIANT constructor path is checked: `Option<i32>::Some("y")` is refused instead of quietly becoming an `Option<str>`.
  ok    60b65151  A payload-less variant of a generic enum carries a real HOLE: `None` is `Option<unknown>`, not the bare `Option`, and B352's erased-arguments special case retires with it.
  ok    1ec627ef  A `[derive(..)]`-SYNTHESIZED body resolves its members under the file the ATTRIBUTE was written in, not under the sentinel source — so B318 S4's export gate stops hiding every non-exported `impl` from every derived visitor.
  ok    2b06202c  A dispose reached from inside a settle now scrubs the draining turn's queue — the scrub had been dead code for every teardown a form performs.
  ok    2f93d7a2  A disposed observer no longer fires — and nested `swap`s on one source stop building a subtree into a region that is already closed.
  ok    e0421335  `--backend rust` works from an INSTALLED toolchain: the runtime crate the emitted program links is embedded in the binary and materialized under `~/.vilan/rt-cache/`, beside the standard library's own cache.
  ok    e56b1227  A closure that captures a view and is handed to a callee that KEEPS it is now refused, which closes the escape rule's last hole before a non-GC backend.
  ok    03e58d8e  A struct initializer's head resolves through the scope chain and, when it is qualified, through its namespace — so `Dot { ▎` offers THIS file's `Dot` and `shapes::Dot { ▎` offers `shapes`'s.
  ok    3f5946ad  A type import whose only use is an `(impl S)` selector's SUBJECT is no longer faded — and no longer deleted by Organize Imports, which was breaking a green build.
  ok    16ebbd44  B318 §4's "Export `S`" reads the analyzer's own record of which type the warning is about, so it answers on the warnings that named a type the file never writes.
  ok    0e4092ea  The two readers that take a sibling file's text consult the open buffer before the filesystem, so an edited-but-unsaved sibling is edited where it actually is.
  ok    1e117a6d  Organize Imports takes the paragraph separator with a run it deletes whole, instead of leaving two blank lines behind.
  ok    e406978a  The auto-import popup consults the visibility bit too: a curated module's private machinery is no longer an add-import target.
  ok    2ab89967  S4's monomorphization refusal names the module whose import decides it, instead of the placeholder `that module`, and lands on the attribute that generated the body.
  ok    c816b2f9  `export const fun` and `export const let` are the items they read as, and `vilan fmt` no longer returns a file containing one unchanged.
  ok    7d9427dc  std's two back edges are weak: `observe`'s captured value cell and `Subscription`'s alias of the subscriber list.
  ok    42175eb7  A module-level binding's initializer is emitted under the file that DECLARES it, closing §3.5's last unscoped seam.
  ok    b20498ed  A call's own generic is no longer bound to ANOTHER call's abstract type parameter, so a block hole whose tail is `context.run(v, || { … })` compiles.
  ok    054c889e  `each_by`'s `T` reaches an UNANNOTATED render closure: the bidirectional fill resolves the WHOLE expected parameter type through the call's substitution, not only a bare generic.
  ok    6615f4f3  A `std` copied away from its toolchain no longer mis-analyzes itself: one root answers for both `std` and `macro_std`, and a root carrying only half a toolchain is refused by a message naming both paths.
  ok    6191ccfa  Rule 3's view-capture scan walks a call's SUBJECT, not only its arguments.
  ok    bc7aaf7a  A `lazy` parameter instantiated at a resource is refused, at the argument when the caller can see it and at the instantiation when only the solver can.
  ok    880f92aa  Import-path completion consults the visibility bit: a curated module offers its exports and keeps its machinery to itself.
  ok    a20f36e6  Organize Imports stops rewriting a prelude-redundant import into a statement that breaks the build: rule (2) counts only what a module import ALONE carries, and a rescue never binds a name the file has taken.
  ok    828de17c  A two-entry package where one entry uses element or `css` syntax builds again: the base cache's key carries the modules an entry's own SYNTAX seeds, not only the ones a `std::` path names.
  ok    ad48117f  A call written with NO RECEIVER means the FREE function, even inside the `impl` block that declares a method of the same name.
  ok    6a71bb09  A `context` clause is recorded on a CALL's own type and on a closure literal's, from the callee's declared return and from the landing — the two faces of one gap.
  ok    9e818c1f  The stylesheet holds the rules the program KEPT: `Style::rule` stages its rule against its class and G23's scheduled finaliser writes the ones whose class the build still names, so every intermediate style a condition combinator wrapped and dropped stops shipping.
  ok    95a5eafd  `transformer::expr_type_id` resolves a PARAMETER spelled `Expr::Local`, which is how every reference to a parameter is spelled.
  ok    347c0540  The `[derive(Hashable)]` field boundary sees a HAND-WRITTEN `impl .. with Hashable`, so `[derive(Hashable)] struct Key { inner: Custom }` compiles when `Custom` has one.
  ok    edd4075d  `let held = injected;` — an UNANNOTATED binding of an injected closure — is a forward the threading follows, not an escape the value-flow restriction refuses.
  ok    b979cbfa  A `context` clause written as a generic ARGUMENT on an annotated `let` now reaches the struct literal it annotates, so a body stored there defers its context instead of capturing one at construction.
  ok    ec06569c  Scope completion on a BLANK LINE offered only globals — the one position where a user actually asks for it.
  ok    72e7304b  Organize Imports broke a green build in one direction and could never clean up in the other, and both were the same question asked of the wrong set: what does a MODULE import bring, on its own?
  ok    b5e59047  `pkg` and `Self` were indexed as references to entities they are not, so a rename of a three-letter package or a four-letter trait rewrote the keyword and broke the build.
  ok    fd77ec7d  An HMR swap no longer throws one websocket error per remote source, and no longer leaves the dead bundle's connection redialing beside the new one's: the duplex's teardown closes it for good instead of only closing its socket.
  ok    9169bcd8  `reconcile_type` gives the same answer whichever way round it is asked: a trait-typed position met by a value that implements the trait AGREES, at a call that reconciles parameter-first exactly as everywhere else that reconciles value-first.
  ok    e527dce5  A type's STATICS bind to a bare name now — `import std::style::Length::rem;`, `use Length::rem;`, `export import std::style::Length::rem as rem;` — which is what the spec has promised since it was written and what an enum's variants have always done.
  ok    39f67e99  `shared.read().push(x)` grew the CELL: the receiver of a `&mut self` method is a copy position now when a temporary read stands in it, which is what five doc sites across four pages have always promised.
  ok    237d8f60  A string argument inside an interpolation hole can be written with its quotes escaped now — `i"{get(\"k\")}"` — and lexes to the same token the plain spelling does.
  ok    511a33ae  A disposed boundary now REMOVES what it placed, so a portal container is empty when the component that filled it goes: `when`'s body, `swap`'s subtree, `bind_each`'s rows and every reactive `{signal}` child leave the document with their owner, marker node included.
  ok    cba0826e  An `[rpc]` method named for one of the generator's own free functions did not build: `[rpc] fun call(self, what: str): str` stopped with "`call` expects 2 arguments, but got 4", spanned on the STRUCT.
  ok    52842527  A `--watch` leg that bundles anything can be `Fresh` again: the hash a tracked build input is RECORDED with and the hash the watch loop RE-VERIFIES it with are now one rule.
  ok    3c25fcc4  A `Source`-generic runtime function is callable from generic code again: a parameter reachable only through ANOTHER parameter's parameterized bound is now solved from that bound's application, so `expose_dynamic<T: Wire, S: Source<T>>(source: S)` can be called with an `S` that is itself a parameter.
  ok    8fae0eca  A NON-EMPTY list literal whose element type has not landed is an open `List<?>` now, not an ERASED `List` with no arguments at all — so `[x][0]` over a closure parameter still waiting to be filled stops being "cannot index this List: its element type is never determined".
  ok    3d7a19e7  Two sibling arguments of one call — or two sibling fields of one struct literal — no longer share an instantiation: a call whose own generics bind nothing stopped leaking the NEXT argument's bindings, which is what turned two `KeyedCell::new([])` fields into `internal: a call resolved to Keyed's requirement key, which has no body … please report this program`.
  ok    d5cd510e  An unannotated closure parameter is no longer frozen at the CALLEE's own unbound generic, so `View::link_to`'s `|event|` needs no annotation — and whether a call needed one no longer depends on what its receiver happened to be.
  ok    c8a639aa  A method reached through a blanket whose subject is BOUNDED now binds the bound's own arguments from the receiver, so `impl type S: Source<type I>`'s members type at the call site without an annotation and a NESTED bound resolves to a real body instead of an internal error.
  ok    fb9e8c4e  An `[rpc]` method with a parameter named `request` did not build: the generated route's closure took `request` too, so the author's parameter shadowed it and every `arg`/`decode_failed` after the first was handed a decoded value where the request handle belonged.
  ok    ac8fd66c  A `Debounce` whose nursery was cancelled is still a `Debounce`: the driving loop's bookkeeping is now cleared on every unwind, not only on the loop's own end.
  ok    5d1760e1  A remote lease taken outside every turn now rebuilds inside one for free, like a lease born inside it: the mirror's `Unsubscribe` is deferred against the turn ambient at the RELEASE rather than the one its release hook captured at creation.
  ok    f90a0855  A throw out of an observer no longer turns the reactive graph off for the rest of the session: the drain restores its scheduling state under a `finally` and lets the error keep unwinding, and a disposal group now finishes past a throwing cleanup.
  ok    9577026f  An `Owner` now has a DISPOSED state, so a `take` or a `defer` that lands after its owner is gone releases on the spot instead of parking a cleanup on a list nothing runs again.
  ok    3f310c37  `Wire` is a trait again: a hand-written `impl … with Wire` now makes its type sendable everywhere the compiler asks, instead of being invisible to a syntactic allowlist of six scalar spellings and the `[derive(Wire)]` names.
  ok    202aadee  A reactive child now keeps its POSITION: `when`'s body, `swap`'s subtree, `bind_each`'s rows and a `{signal}` element or run all land where they are written instead of at the end of the parent, and stay there across every change (A71).
  ok    e049b099  `std::router::link` no longer builds a drag-armed anchor: in Chrome a link drag that starts while a quick click's in-app navigation is still settling WEDGES THE TAB, and `draggable="false"` removes the trigger at no cost.
  ok    20a2b7d6  `self.items.sort_by(|a, b| a.compare(b))[0]` inside `impl Holder<type T: Ord>` reported "cannot index this List: its element type is never determined" over complete code: `freshen_list_element_slots`' EXTERNAL-function path was unguarded.
  ok    5d7e9615  A bound's trait ARGUMENTS were dropped whenever an impl reached the required trait through a SUPERTRAIT, so `Source<anything>` satisfied `: Source<str>` — `SignalCell<bool>` was an element-attribute value, and `set_attribute(name, true)` shipped, correct only by JS coercion.
  ok    1405ca7e  `impl CatBox with Holder<Cat>` compiled with `Cat` implementing no `Label`, and `impl CatBox with Holder<Cat, i32>` compiled too: an impl's `with` clause was arity-checked and bound-checked by NOTHING.
  ok    3aafe281  `take(|x| Remote { seed = [x] })` satisfied a `|i32| Remote<List<str>>` parameter, and `handle.remote_signal([], |x| x.ok().flatten())` over a `List<i53>` handle accepted a `SignalCell<List<Message>>` annotation — a closure's return through a USER generic struct was neither bound nor checked.
  ok    1028cdb2  `if inner is Some(let payload)` over an UNANNOTATED closure parameter typed the payload as `Option`'s own declared `T` — a free, unbounded generic — so a legitimate read of it was refused and an illegitimate one compiled.
  ok    4a9cb0ec  `[rpc] fun login(&mut self, …)` did not build: the generated dispatcher declared `fun dispatcher(self)`, an IMMUTABLE binding, so no route could call a method that wanted a mutable receiver — and the build stopped inside generated code, spanned on the STRUCT, recommending `mut self`, the receiver that silently loses the write.
  ok    3f00157d  `Shared::read()` handed out the cell's STORAGE, so `let b = h.read(); h.write().push(9)` made `b` read 6 and `mut c = h.read(); c.push(10)` grew the CELL — a read is a place now, and storing one copies.
  ok    5b22616d  `View::show(condition)` was a silent NO-OP on every flex container — on every element an app gave a `display` at all.
  ok    774d68c7  A generic bound's ARGUMENTS were dropped when an impl was matched to a receiver, so every blanket over a parameterized trait applied to every instantiation of it.
  ok    2ae38a3e  The base cache's 512 MiB byte budget was denominated ~24× low: it bounded a number that is a fraction of what a retained world weighs, so "512 MiB retained" was a bound on the order of 12 GB resident.
  ok    9ba73762  A `css { … }` block and an element are HYGIENIC: the block means `std::style::style` and `<tag />` means `std::ui::view`, whatever those names mean at the site — and neither needs an import.
  ok    9b722ab6  `impl Held<type T> { fun first(self): T { self.list.get()[0] } }` over `struct Held<T> { list: Signal<List<T>> }` reported "cannot index this List: its element type is never determined" - and the element WAS determined; the call threw it away.
  ok    da549cde  B251's written-argument bound check was STRUCT-only, so `Held<i32, SignalCell<List<str>>>`'s ENUM twin — the same parameters, the same bound, the same annotation carrying a `str` list out through a declared `i32` one — was still accepted.
  ok    0e0978f0  The intrinsic table bound `remove`/`insert`/`pop`/`push` (and `new`, `get`, `sort_by`, and every other intrinsic name) by NAME on any impl of the subject, external or not — so a vilan-bodied `List::remove` was silently replaced by the intrinsic and the body never ran.
  ok    d7312cd6  a49-async narrowed the trait-default half of `GenericDispatch::OnType`; the KNOWN-receiver half kept the widest answer, so an inherited default called on a concrete value was still colored async by any same-named member anywhere in the program.
  ok    0ce66681  `impl Cell<type T> with Signal<T>` written as ONE block typed `x.map(|v| v * 2)`'s `v` as the trait's abstract `T`, and steered the author to "add it where `T` is declared on `trait Source`" — a std file.
  ok    c278542e  A refused RETURN annotation cascaded — `'Greet' is a trait, not a type` and then `cannot call method 'who' on unknown` — where the same trait refused at a closure PARAMETER stood its uses down.
  ok    c09aacf3  A type-position `as` alias corrupted a rename of the type it aliases: `import pkg::helper::Point as Spot;` made `Spot { x = 1 }` an "occurrence" of `Point` five bytes wide, and renaming `Point` rewrote it to `zzz{ x = 1 }`.
  ok    d85838ce  The dead-code paint grayed a function called only from a module-level `const` region — three of the four spellings, including the `const { bundle(..); style::preflight(); };` block kolt writes at the top of `client.vl`.
  ok    4783d045  `View::autofocus` focused in a microtask — before the frame's rendering step — so an input inside a subtree that becomes focusable only during rendering was never focused at all.
  ok    604ecb65  File mode's last two disagreements, and they were the same mistake twice: a fact about the PACKAGE was being read on one leg and not the other.
  ok    d1675611  A hole whose text is not an expression was abandoned MID-BODY, so `i"{if c { 1 } else { 2 }}"` produced six diagnostics — an unclosed `(` from the hole's own generated paren, two `expected a token`s, `found 'else' expected an expression`, two missing terminators, and the ban on a string spanning lines, about a line break nobody wrote.
  ok    070b1dcc  An in-place `remove` through `SignalCell::update` beside a `bind_each` crashed the reconciler (`index out of bounds: the length is 5 but the index is 5`), and an in-place element write left the row silently STALE.
  ok    e0d67f10  Renaming through an import alias COLLAPSED it — `greet as hello` renamed to `greeting as greeting` — and an alias whose name was a different LENGTH from the thing it renames was invisible to the editor outright: `import pkg::helper::greet as hi;` gave `hi()` no rows at all, so find-references answered nothing and rename answered "there is no symbol to rename here".
  ok    56586c69  `fun bump<P: Add>(a: P): P { a + 1 }` compiled, and it is unsound: a bare `P: Add` means `Add<B = Self>`, so `P`'s `add` accepts a `P` and only a `P`.
  ok    b6f8ca5a  `impl Cup with Mixed` was told `mix`'s first parameter had to be a `Cup` where `trait Mixed with Blender<i32, str>` says `i32`, and `Meters < Feet` was refused as though `PartialOrd<Feet>`'s `lt` accepted a `Meters`.
  ok    664f8732  `struct Held<T, S: Signal<List<T>>> { list: S }` left `T` at `any`, and `Held<i32, SignalCell<List<str>>>` was ACCEPTED anywhere it was written.
  ok    1b32e385  Bare `connect_socket(url)` offered no subprotocol, so the one call a first program writes was the one that could not be told why it was refused.
  ok    0e29d961  Every `[service]` program stopped compiling the moment A49's `Source` inversion met the RPC capability table: three copies of "field `start` of `Capability` receives an async closure, but its type awaits nothing", reported against std's own `rpc.vl` for a program that awaits nothing.
  ok    5acdae14  Three holes B239 left on the open-file path.
  ok    a4487230  One import cycle back into the entry printed four errors: B226's refusal at the import, then "cannot find 'Tab' in the imported path", then "cannot find type 'Tab'" twice — three of them naming a type the reader can see declared in the file the refusal already pointed at.
  ok    31dff857  A `= Self`-defaulted trait parameter was read as a BOUND when its member was reached through a sub-trait's parameterized clause: `trait Mixer<A = Self, B = Self>` under `trait Mixed with Mixer<i32, str>` refused with `'i32' does not implement trait 'Mixer', required by a generic bound of this call`, and the same for `str`.
  ok    65ac6af5  `disabled` is usable as a binding name, and there is now a gate that says so.
  ok    84153d76  Renaming through a struct-init field shorthand refused; it EXPANDS now.
  ok    7958e896  Renaming a derive-generated member refused as generated for ONE of the types that derived it and silently rewrote the call site alone for every other — a rename that leaves the generated declaration behind and breaks the program.
  ok    a269eed6  Hover at the end of a bare use still answered the enclosing function after E133: `let _ = count|` showed `fun main()`.
  ok    411b9fe6  A method call the solver never selected produced no diagnostic of its own — and on a program with nothing else wrong, no diagnostic at all: `for item in empty { item.len(); }` compiled to a call that resolves to nothing, silently.
  ok    4527660d  One argument short on a call turned into nine errors: `channel_component(stub_user)` reported its arity mistake, then three "`channel_component` reads context `app_context`, so it can't be used as a value" at the same call and five "context `…` is read here, but this code can be reached without an enclosing `run`" — four of them inside std's `reactive.vl`, in code the program never touched.
  ok    4f4f6509  A client refused at the handshake burned the full retry budget — ten redials over ~24 s — and then reported `Transport("could not reach …")` about a server that had answered immediately. It now gets `RpcError::Unauthorized` on the first attempt.
  ok    d2dc12a2  A client holding both a whole-collection lease and a per-key lease on one keyed channel could make its own mirror report a protocol fault.
  ok    a2f1e2d9  Opening `views.vl` in the editor turned a legitimate module cycle into seven errors — "`pkg::views` is this program's entry file … it cannot be imported" on the sibling's import, then every name that sibling took from it — over a package `vilan check .` compiled clean.
  ok    ec6a20ac  One mistyped `run` argument deleted the `run` from the program: adding a field to a context struct and not to its initializer reported three "context `app_ctx` is read here, but this code can be reached without an enclosing `run`" — one per read — and the real error, the missing field, LAST.
  ok    26680c46  A struct-init field shorthand `A { x }` is one identifier naming two things — the field and the local it reads — and the index kept only one of them, so `x` faded as unused, find-references missed the use, and renaming the field silently broke the site.
  ok    92b4eb58  The reference index was end-exclusive, so a caret at `name|` — where it sits the moment you finish typing a word — named nothing: rename showed "the element can't be renamed", find-references came back empty, and hover went blank.
  ok    fee45662  `linkedEditingRange` answered in the ANALYZED snapshot's coordinates, and the client mirrors that answer's keystrokes — so typing under element syntax during the debounce typed into whatever unrelated live code had moved into the tag's old offsets.
  ok    8c21e6b6  `impl Pair<type T> { fun make(x: T) { Pair { b = "hello", a = x } } }` compiled clean and returned a `Pair<str>` into a `let p: Pair<i32>` — no diagnostic anywhere.
  ok    8c21e6b6  `compare_type_rigid` is `reconcile_type`'s read-only twin, `trait-objects.md` §1.4 says the two agree exactly, and since B211 they did not.
  ok    2257224a  A type a derive template names but nobody declared reported `cannot find type 'NoSuchType'` with no provenance, and drew that label over whatever the deriving file happened to hold at the template's offsets.
  ok    c4c6d8f4  `trait Bumper with Add<i32> { fun bumped(self): Self { self.add(1) } }` was refused with `Expected Bumper, but got Add instead.`
  ok    1cd12461  `<div .styled() />` looked undiagnosed: `styled` takes an argument, the arity WAS checked, and the refusal rendered against an unrelated line of std's own `browser/ui.vl` with the author's file never named — so the only honest reading of the output was that nothing had been reported.
  ok    3c249aed  One `print(v)` at the top of a closure body typed `v` as `any` and silenced every check on it for the rest of the body: `apply(|v| { print(v); v.no_such_method(); })` was refused "cannot call method 'no_such_method' on any", naming a type the program never wrote, while the same closure without the `print` said "i32 has no method".
  ok    24d4377f  `import pkg::client::SidebarTab;` at the top of the ENTRY `client.vl`, beside `[derive(PartialEq)] enum SidebarTab { … }`, made the derive vanish: "type 'SidebarTab' does not implement the `PartialEq` operator" over a type that plainly derives it.
  ok    1f95343b  `fun g(): i32 { ret 1; print("dead"); }` was refused — "this body ends without producing a value" — and so was every body whose exit is not its LAST statement.
  ok    880ad531  `if !(maybe is Some(let n)) { panic("missing"); } print(n)` refused `n`, while the same guard ending in `ret` bound it — one rule with two answers, decided by which ending the WALK could recognise.
  ok    c46df96e  `vilan fmt --check src src/pkg` printed the files under `src/pkg` twice.
  ok    0c8424bf  A member request answered inside the debounce resolved its receiver in ANALYZED coordinates, so a receiver typed since the last landing answered the OLD expression's type: `<div .styled(const style::style().` offered `element, text, class, styled, style_var, attr, on, child, bind_text, …` — `View`'s method set — where the settled request offers Style's 89.
  ok    8b99fda6  Member completion went silent on a call whose declared return is a bare type parameter: `c.get().` on a `SignalCell<List<str>>` offered nothing, while `c.`, `let v = c.get(); v.` and even `c.get().len().` all answered.
  ok    b79f49ba  A `::` path in code completed only its LAST segment, so `style::FlexDirection::` and `geo::Shape::` offered nothing while the import spelling of the same path descended fine.
  ok    98c7e11d  A reactive channel minted at runtime leaked its source for the life of the connection, and went silently dead — reporting `Ready` — after a reconnect.
  ok    289e2a2b  Two spellings of one unsaved file agree again in the editor's containment tests.
  ok    186b5fdb  `semanticTokens/range` answered in the ANALYZED snapshot's coordinates while `semanticTokens/full` answered the live buffer, so a viewport request after an unlanded edit above the window painted every token in it one line off until the next analysis landed — on the request an editor sends most.
  ok    0847c531  A `--watch` round decides each leg's freshness at that leg's own turn, and compiles a leg that writes another leg's input first.
  ok    0847c531  The language server's containment test resolves both sides the same way, so an unsaved buffer under a symlinked project root keeps its project.
  ok    0847c531  One file reached under two names is collected once.
  ok    ffbcf454  A self-call bound by a `let` and read in an unannotated recursive body's return position resolves.
  ok    a9c07d77  A trait-conformance refusal now advises a signature the author can actually write. `impl P with PartialEq { }` said ``declare `fun eq(self, b: PartialEq): bool` `` — and `PartialEq` is a trait, so that line names a type no value can have; the advice was unfollowable at the one moment the reader most needs to follow it.
  ok    22c239da  `self.add(self)` written out in a trait default over a supertrait resolves. `trait Doubler with Add { fun twice(self): Self { self.add(self) } }` was two errors on a program with no mistake in it — `Expected Doubler, but got Add instead.` at the call and `Expected Add, but got Doubler instead.` at the argument — while the OPERATOR spelling of the same thing, `self + self`, has dispatched since B193.
  ok    4851466b  A refused `[expose]` now generates NOTHING — no contract entry, no client mirror, no `session.expose` call — so `[expose] tasks: i32` is ONE diagnostic instead of five. B189 took the pile from six to four; the residue was two `cannot infer 'T' … ': Wire'` and two `cannot find type '_'`, and `_` is not a type: it is the `service` expansion's own placeholder, handed to an author who never wrote it.
  ok    ee84e7ab  A `[derive(..)]` on a type declared inside an inline `mod` now generates its impl INTO that `mod`, where the type it names is in scope. `mod shapes { [derive(PartialEq)] enum Kind { .. } }` was four diagnostics on a program with no mistake in it — `cannot find type 'Kind'; import it first` twice, plus a follow-on per variant raised inside a body the author never wrote.
  ok    baa10423  One module declares a name once, and a bound names a trait.
  ok    413f8c27  A tuple receiver resolved NO method at any arity — not an inherent impl, not a trait impl, not `impl (i32, i32) with Add`'s own `add` — so `(1, 2) + (3, 4)` ran and answered `4` / `6` while `(1, 2).add((3, 4))` was "cannot call method 'add' on (i32, i32)".
  ok    53c3b8bf  A build hook's stamp records the inputs it consumed, not the inputs on disk after it ran.
  ok    83af3f52  Two rpc frames that TCP coalesced into one read were serialized by the receive loop, so a handler awaiting anything the SECOND frame would have delivered waited for a message the loop was holding.
  ok    6c4a8065  `each` over a `ListCell` builds only the rows a write names: one push into 1,000 rows builds ONE row and reads ONE key, where a plain `SignalCell<List<T>>` re-reads every key and diffs the run.
  ok    404c066b  The native backend monomorphises on ASYNCNESS as well as on types: a callee handed an async closure at one call site and a synchronous one at another comes out twice, and `adapt.vl` is byte-identical on both backends.
  ok    b8c11a7e  A view inside an enum payload compiles natively: `fun get_mut(&mut self): Option<&mut i32>` emits `Option<&mut i32>`, and `option-view.vl` is byte-identical on both backends.
  ok    e162ddfa  The two dynamic combinators land on `Source`: `switch` follows whichever source its selector answers, and `and_then` does it for a source of options.
  ok    e4570ffb  The SHOW takes the focus: `FocusScope::focus_initial`, the chained `View::focus_scope`, and `View::autofocus` now writing the attribute.
  ok    9a0bed40  A focus SCOPE in std: `Element::tabbable` walks the tab order, and `focus_scope(root, Contain | Wrap)` traps Tab inside an overlay without `inert`.
  ok    a4f780dc  `std::dom` can read what a focus scope has to know: `tab_index`, `parent`, `has_attribute`, `computed_style`, `active_element`, `Event::related_target` — and the test DOM answers all six.
  ok    c7127a23  `[service(AuthClient, http)]` — an opt-in marker that a service is an HTTP API, refusing at the declaring member what the connectionless leg cannot carry.
  ok    269231ac  A service can gate its connectionless `POST {mount}rpc` leg per request: `Service::authorize_request(|request| ..)`, a second hook beside `authorize`, whose `Session` rides on the request.
  ok    39f993bb  The `POST {mount}rpc` leg answers the STATUS its envelope earns — 400 for a decode failure, 404 for an unknown method, 405 for a non-POST, 501 as an envelope — and requires a content type, which is the whole CSRF posture.
  ok    633d5567  A `[service]` can be reached as a plain HTTP API: `FooClient::over_http("/auth/", json_codec())` speaks the connectionless `POST {mount}rpc` leg with no socket, no handshake and no mirrors.
  ok    f917da4e  A list whose writes are its CHANGES: `ListCell<T>` and `map_each`, so a derivation over a list runs its function once for the element that arrived instead of once for every element.
  ok    54d42be9  A `vilan` server with the shape of kolt's runs as a native binary and answers a login over a real socket — SQLite store, hashed password, JSON route and all.
  ok    0e816010  `BigInt` compiles natively as an `i128` — a documented LIMIT, enforced at both ends — and the corpus's two `BigInt` programs print the same bytes on both backends.
  ok    a0ac30dc  Five more of std's host modules answer natively — `Math`, the number labels, `charCodeAt`, `process.env`, the plain `node:fs/promises` set and SHA-256 — and a closure that awaits in a `void` position no longer stops a build.
  ok    735a0813  `std::json` works on the native backend: a `[derive(Json)]` struct serializes and decodes in a compiled binary, and the two JSON corpus programs are now byte-identical on both backends.
  ok    699c51d9  `dyn Trait` — a value whose concrete type is erased, so one struct field, one list and one parameter can hold several types that share a trait.
  ok    4bd52f56  A call through a `dyn` threads the hidden context arguments its implementations read.
  ok    257d7e4d  `dyn Trait` compiles natively, as a fat pointer, and prints byte-for-byte what the JS build prints.
  ok    6752664a  `dyn Trait` reaches what a bound reaches, copies like a value, and keeps its declaration's asyncness — and its table is shaped for the call site that sees several types.
  ok    42985e3d  A native `vilan` server answers a request: a `std::http` program compiled with `--backend rust` binds a port, serves `GET /` over a real socket, and the JS twin answers the same status line, the same header and the same body.
  ok    dad719f9  The native backend reaches `std::http`: twenty-three of the twenty-five `node:http` bindings are answered by `vilan_rt::http`, and a destructuring `let` compiles — which flipped `destructuring.vl` to byte-identical.
  ok    1fcdcf64  `vilan-rt` has an HTTP/1.1 server, written by hand over `std::net`: no dependencies, no `unsafe`, and it runs on the executor J6 built.
  ok    a66fed94  `std::router::navigate_replace` — navigation that rewrites the current history entry instead of pushing one.
  ok    80ea4159  `when_some` — the conditional form that BINDS the value, and keeps its row when the value changes.
  ok    5a17769e  Element syntax admits an `Option` attribute: `<div data-dragging(maybe)>` takes an `Option<str>` or a `Source<Option<str>>`, and a `None` means the attribute is not there.
  ok    51efbeb0  `scoped_effect` — an effect whose every run gets its own `Owner` — and the free `on_cleanup` beside it.
  ok    1d8dfbb2  A mirror's `map` is a DERIVATION too, so an effect on the same mirror reads the mapped cell settled, not one wave stale.
  ok    e59e1413  Effect order within one settle is now a CONTRACT: derivations run to a fixpoint, then effects run in ascending subscriber id — so a parent form's effect runs before anything its render created.
  ok    de237ee1  `reconcile` keeps a position index, so `each` stops paying N(N+1)/2 key comparisons per change: 355.1 M Ir → 29.1 M at 1,000 rows, and quadratic → linear.
  ok    8f9b8038  A BACKED enum compiles natively — it IS its backing value, on both backends — and with `List::sort_by` and B109's read-through that is five more corpus programs byte-identical.
  ok    cb97f323  An OVERLOADED operator compiles natively: `a == b` on a type with its own `PartialEq` is a call to that impl's member, and five more corpus programs print byte-identically.
  ok    fcf62f2e  `lazy` parameters compile natively, and with them the whole `Option`/`Result` combinator surface: thirteen more corpus programs print byte-identically.
  ok    ec1811bd  The native path reaches its exit: `board.vl` — `native-apps.md`'s own probe, the program the whole paper was written around — prints byte-identically as a native binary.
  ok    34d12d06  Structured concurrency compiles natively: `nursery.vl` — a helper's spawn, a grandchild spawned by a running child, and a join that waits for a child list which grew while it was draining — prints byte-identically as a native binary.
        note: 34d12d06 touched records only (Structured concurrency compiles natively: `nursery.vl` — a helper's spawn, a grandchild spawned by a running child, and a join that waits for a child list which grew while it was draining — prints byte-identically as a native binary.) - confirm its code landed
  ok    3bcaa083  `--backend rust` compiles `async` programs: an `async fun` is a Rust `async fn`, an `await` is `.await`, an `async` spawn is a task on the new executor, and `await-postfix.vl` — a real corpus program — now prints byte-identically as a native binary.
  ok    5b810d06  The native backend has an event loop: `vilan-rt` gains a hand-written single-threaded executor, and it is the JS turn model rather than a second one.
  ok    9f6a174c  The style vocabulary reaches a chain written outside a css hole: the four keyword-property types join `std::style::prelude`, and one file-level import is the idiom.
  ok    594d2e6a  `filter_map` — `map` and `filter` in one pass — joins the adapters, with an eager `List` twin under the same name.
  ok    454a8c94  `Default` reaches the containers: `List`, `Map` and `Set` have an empty one, so a `T: Default` bound can finally be met by a container.
  ok    28bd50ba  `Result::expect_err`'s message joins the four that were already `lazy`, and the asymmetry it leaves behind is gone.
  ok    b74a51cf  `Result<T, E>` crosses JSON directly, in the one spelling the codecs already use.
  ok    96726f96  `Storage::keys()` and `Debounce::flush()` — std-30's two unshipped halves.
  ok    c511ffe5  `bind_attr` takes a `Source<Option<str>>`, whose `None` REMOVES the attribute.
  ok    835c29d2  The native backend compiles GENERICS: a generic function, a generic struct, a generic enum and a trait default each emit one Rust item per instantiation, and 51 of S1a's 98 refusals are gone.
  ok    2adb83d9  An `[rpc]` method may return NOTHING and still be awaited: `void` is admitted as an rpc return, the reply is the ack, and the stub waits for it.
  ok    7e245e39  Vilan has a second backend: `--backend rust` emits your program as Rust, builds it with the `cargo` already on your machine, and the binary prints byte-identical output to the JavaScript one.
  ok    1379ccdd  `Weak<T>` lands: `Shared::downgrade`, `Weak::upgrade`, `Weak::get` — the handle that names a cell without keeping it alive.
  ok    95058794  A `lazy let` carries its value across a hot swap once something has forced it; one still pending is re-minted.
  ok    4454df49  `lazy let database: Database = Database::open("kolt.db");` — a module binding whose initializer runs at its FIRST USE and then memoizes.
  ok    e67cbf7b  `const let` and `const fun`: a compile-time binding may hold a CLOSURE, and a const function's promise is checked where it is made.
  ok    5cc1e079  A struct VALUE is callable: `impl Scale with Callable { fun call(self, n: f64): f64 { .. } }` makes `space(2f)` resolve as the method `call` on it.
  ok    42908f11  A styling condition is a VALUE now, and a set of them is the same value: `Style::on(conditions, inner)` is the one condition combinator, every named one is a line of sugar over it, and no program in the estate moves a byte.
  ok    6c77482d  Const evaluation gains an END-OF-EVALUATION HOOK: `asset::schedule_at_end(f)` runs `f` once, after every const evaluation of the compile has finished, in a const context of its own.
  ok    fe542196  `#` is a token — the import REACH marker — and the `css` block's hex-colour rule moves to the one parser that knows a `#` is a colour.
  ok    c0a41264  The `export` marker is a BIT on an importable row instead of a wrapper the compiler throws away, and the three tooling surfaces read it.
  ok    d5178a6e  `export *;` and `export(in PATH)` are grammar: the module-wide marker and the scope narrowing B318's visibility model is written in.
  ok    42598432  `only` and the `(impl …)` selectors decide which implementations a FILE admits, and a method call reaching one it declined is refused naming the selector.
  ok    b71d6fdb  `import a::b only;` and `import a::{ (impl List<i32>)::first };` parse, print and sort — B318's impl-selection surface, grammar first.
  ok    d5c129bf  A render closure may yield any `Slot`, so a row, a `when` body or a `swap` branch can be a fragment, a text node or another value form — and the last wrapper elements in kolt are gone.
  ok    6c75ab53  `when`, `swap` and the three `each` runs are VALUES that fill a child position now, so a conditional or a keyed run sits BETWEEN its siblings instead of after them.
  ok    a75c0f49  A `context` clause lives in the closure's TYPE now, so a struct field, a generic argument and a return can carry one, and the coverage check follows the value wherever it flows.
  ok    7f911239  `<>…</>` is the fragment literal: several children under one hole, with no wrapper element and no new runtime type — it lowers to a `List<View>` LITERAL, which the child contract already places.
  ok    6e4e10b1  `Vec2::length_squared` — `x² + y²` under the name the comparison wants, with `length` now stated in terms of it.
  ok    74018c52  An `[rpc]` method may now return a `KeyedCell<K, T>`, and its stub hands the client a `KeyedSource<K, T>` — the keyed row of the handle mapping, in the same sync unleased shape as the plain one.
  ok    ee62236c  `flatten` is a BLANKET over `Source` now, not a member of the cell, so a `map` result, a derived cell or a mirror joins exactly as a `SignalCell<SignalCell<U>>` does — and a second blanket joins an outer of `Option<inner source>`.
  ok    b5365f0a  `_` is the anonymous type binder: `impl Source<Option<_: Source<type U>>>` is the head that needed `type _: Source<type U>`, and two of them in one head are two parameters where they used to be one.
  ok    b919c708  `std::wire` carries `Result<T, E>` and the whole sized numeric family, so a fallible reply and an unsigned id are ordinary payloads instead of forty hand-written lines and a workaround.
  ok    a1a43084  `std::math` gains `Vec2 { x: f64, y: f64 }` — the 2-D vector value a pointer drag needs, with `scale`/`length`/`distance`/`dot` and component-wise `Add`/`Sub`/`PartialEq`.
  ok    234fe33f  An `[rpc]` method may RETURN a signal handle: the server writes `SignalCell<T>`, a plain `ChannelId` crosses, and the client's stub answers `Result<RemoteSource<T>, RpcError>` — the half `[expose]` structurally cannot do.
  ok    25d34f25  `KeyedCell<K, T>`: a keyed collection whose WRITES are the deltas, so a keyed channel stops paying for the size of the collection on every change.
  ok    9994d969  A keyed mirror IS a `Source`: `impl KeyedSource<K, T> with Source<Option<List<T>>>`, on the lease that was already there.
  ok    0b429daf  `std::router` can READ: `parse_path` takes a URL apart, `FromPath` is `Routable`'s inverse, and the query and fragment are reachable at all for the first time.
  ok    f19313a6  `std::dom` can MEASURE, OBSERVE and UN-SET.
  ok    3bf4a8c6  `std::storage` grows a typed `Storage` handle — `window().local_storage()` / `window().session_storage()`, with `len`, `key_at`, `get`, `has`, `set`, `remove` and `clear` — and its `get` is the one that can tell an ABSENT key from a key stored as `""`.
  ok    cfd98359  `std::dom::Event` grows `code`, `target` and `current_target` — three apps-facing getters kolt hand-wrote in three different files, two of them under an explicit `// FIXME: Implement with std.`
  ok    e6d091b9  `str::parse_bool(): Option<bool>` joins `parse_i32` and `parse_f64` in `std::option` — the string→value family had a hole exactly where four of kolt's six preference decoders reach.
  ok    5b22616d  `flex_grow(value)` beside `flex_shrink`, and `Color::current()` for CSS's `currentColor`.
  ok    1cc7e632  `View::toggle_attr(name, flag)` — a BOOLEAN attribute, bound: present when the source is true, removed when it is false.
  ok    774d68c7  The CHILD CONTRACT: `str`, `View`, `List<View>` — and a `Source` of each.
  ok    5169b537  A typed style token has a MID-VALUE spelling: `border: 1px solid {Color::gray(500)};` emits `var(--gray-500)` AND the `:root` line that declares it.
  ok    e24d1bac  `std::style::prelude` — `rem`, `px`, `space`, `gray`, `hex`, `rgba`, `s()` and the rest of the typed tokens as free functions, AMBIENT inside a `css` block.
  ok    fb074b8b  A `css { … }` block takes CHAIN LINKS: `.flex_row();`, `.ghost();`, `.custom(a, b);` — a dotted item ending in `;` is a verbatim `Style` method call at its written position.
  ok    8497af44  A `css { … }` block is `const` BY CONSTRUCTION: `let card = css { padding: 1rem; };` compiles, and so does `<div .styled(css { … })>`.
  ok    7e5db561  A package's modules can live in DIRECTORIES: `lib/util.vl` is the module `pkg::lib::util`, `lib/ui/widget.vl` is `pkg::lib::ui::widget`, and nesting is unbounded.
  ok    7e5db561  The editor knows the tree: `pkg::` offers module directories, `pkg::lib::` offers a directory's children beside the module's own items, and a pure namespace offers what it holds.
  ok    9e72b3bb  A `List<T>` had no keyed exposure the macro could write: `[expose(keyed)]` read TWO written types off the annotation, a `Map<K, V>` element names both and a `List<T>` names only the element, so A39 refused the list form and steered to `Map<K, V>` or the hand-wired `expose_keyed`.
  ok    e4976c1f  A service that is up, reads the token fine and still cannot serve — the database is down, the migration is mid-flight, the node is draining for a deploy — had no way to say so: `Reject` had three arms, and 429 was the closest.
  ok    3ddcb319  A mirror was not a `Source`: `RemoteSource<T>` carried `get`/`map`/`or` as inherent members and implemented nothing, so A49's `on_change` was not on it, `effect` was not on it, and no generic `S: Source<…>` function — `selector`, or an application's own — could take one.
  ok    0bddc3f4  `RpcError` had no `PartialEq`, so the commonest question an application asks a typed error — which one is it? — could not be written as `error == RpcError::Unauthorized`.
  ok    22f92423  Arrays join the nominal receiver set for USER impls: `impl [i32; 2] with PartialOrd { partial_compare }` and `a < b` runs.
  ok    220d0637  `import` and `use` paths take an `as` alias
  ok    1f8a7c7a  `flag && match probe() { … }` was a parse error — `found 'match' expected an expression` — and now it is an expression.
  ok    c919edbe  A struct field may name a TRAIT — `struct Store { count: Signal<i32> }` — and it means a type parameter the author never writes.
  ok    2ccf97e7  A `fun` can now DECLARE what it reads: `fun render(row: Row): str context (app_ctx, turn_scope)`.
  ok    b4f61219  `Service::authorize_timeout(millis)` bounds the verifier, and `Service::trust_forwarded_for(bool)` decides which address a limit is keyed on.
  ok    d2dc12a2  `[expose]` resynced the whole value on every change, so editing one message in a channel of five thousand cost the channel — `[expose(keyed)]` sends the change instead, and a client can subscribe to ONE key.
  ok    302d0b97  An rpc service can refuse a connection at the handshake: `Service::authorize`, a token subprotocol, and three limits that need no auth at all.
  ok    9b96c740  `Service::factory` mounts one service INSTANCE per connected client, so a method can finally learn who called.
  ok    b3793c24  The editor fades top-level code no entry of your package reaches, and every top-level item of a module no entry loads at all.
  ok    bb80ec4f  `Style::when(condition, delta)` — the conditional merge, as a chain instead of a pile of `if`s.
  ok    161e9bc7  `view("input").autofocus()` — an element mount hook, replacing the uuid-plus-timer dance a custom modal needs otherwise.
  ok    07218676  `bind_each` asked for a key AND a `PartialEq`, and most lists only have one of the two to give — so there are three forms now, each dropping a bound.
  ok    3f617699  `selector(source)` answers "is this row the selected one?" for a thousand rows in two writes, not a thousand.
  ok    f92fcc4b  `source.on_change(observer)` subscribes WITHOUT the immediate first call — the lazy half of the pair `sub`/`effect` had no spelling for.
  ok    26b82414  The guard clause: `if !(slot is Some(let n)) { ret; }` binds `n` for the rest of the block.
  ok    6b38a705  `panic(…)` evaluates to `never`, and `never` ERASES: it yields to whatever it meets in every join — `if`/`else` arms, `match` arms, a block's tail against the type expected of it, an operator's operands (`never + i32` is `i32`), a call's argument against its parameter — so a body that ends in a `panic`, or in a `for { … }` nothing breaks out of, owes no return value, and programs the compiler used to refuse now compile.
  ok    24fa5888  `shapes::Dot { x = 1 }` parses. The struct LITERAL was the one spelling in the language whose head was still a bare identifier, so a module-qualified type could be named everywhere except where a value of it is constructed — and the refusal was "expected `;` to end this statement", which says nothing about the path it choked on.
  ok    0a20b28a  `[derive(..)]` on a GENERIC type now generates a real generic impl — `impl Handle<type T> with Wire` over `Handle<T>` — instead of an erased one that named the subject bare in every role, and the arity rule generated code was exempted from now applies to it.
  ok    6990ba4d  A trait on a PARAMETER is now an implicit generic: `fun f(x: Trait)` reads as `fun f<T: Trait>(x: T)`, monomorphized per call like any generic you write out.
  ok    3e4e57df  The playground's buffers get a real prelude, chosen by mode: the WEB set in the browser mode that runs your program, the base set in the server check mode, and a toggle that turns the ambient scope off.
  ok    399d6669  A type can be named through the module that declares it — `style::Style`, `ui::View`, `std::reactive::SignalCell<i32>` — in every position a type is written.
  ok    3267426c  A server can call functions the CLIENT declared.
  ok    6a8cbd46  The native backend has a liveness pass: a value read at its binding's LAST use is moved rather than copied, and the paper's board probe loses all four of the copies it was taking.
  ok    1901e91c  A hover on a struct field no longer costs the workspace.
  ok    c9e9ddeb  The native executor reuses a settled task's slab slot, so a program that spawns per unit of work no longer grows the slab for its lifetime.
  ok    b71a16fa  A `lazy` position every call site fills with a literal builds no memo cell and forces nothing, so `unwrap_or(0)` stops allocating — 65 of the corpus's 84 thunk emissions gone and 9,310 bytes back.
  ok    b6c09abe  The body-sharing key normalizes local gensyms, so two monomorphizations that differ only in the NUMBER the generator gave a temporary share one body again — 3,387 bytes back across the corpus and 2,567 out of the split fixture's `app.js`.
  ok    7451c2ca  What is left of the import-collision refusal stops following the whole program's import-statement count: the ancestor walk is an index and `carried` holds only the rows the refusal reads.
  ok    770521b4  An entry-shaped world files its checks record too, so a keystroke in one of kolt's six open modules stops re-checking 59 of the 69 sources it already checked.
  ok    c160badb  The import-collision refusal is an index, not a product of linear scans, so the first real two-package collision in a large program stops paying for the whole program's import statements.
  ok    d3903646  A `[service]` entry reaches the base cache, so kolt's `store.vl` stops paying its package's whole pre-entry world on every keystroke.
  ok    c5b98491  Eight frame-scoped `Shared` cells in std become `mut` locals, and the census that keeps the class at zero is a gate.
  ok    091762e5  Reference identity is an intrinsic that stamps a cell on the first ASK, so a program that exposes nothing stops minting an id per signal — and the eleven goldens A92 moved come back.
  ok    d963a119  A module opened as the entry, whose own package imports it back, stores its world under a key that says which module the world excludes — so the files a cycle reaches stop being uncacheable.
  ok    af9e859a  A keyed run's order pass leaves an unmoved row alone: an append costs no cut at all where it used to cut and re-insert the whole list.
  ok    1b3437cf  B317's `impl_namespaces` is shared by reference, so a base-cache hit stops deep-copying every impl block std's closure registered.
  ok    c4611c08  The `malloc_trim` at the analysis-landing seam is conditional on how much the release handed back, and what it costs there is measured instead of estimated.
  ok    b17c8984  The analyzer's base cache is bounded at 192 MiB of resident worlds, measured rather than assumed, and its budget never evicts a world an open focused document is analyzed from.
  ok    552c1623  `CompletionIndex::build` asked `Program::source_of` once per declaration in the whole program — the identical M27/M58 hoist, taken where M58's own profile said it had not been.
  ok    ca810fd5  Closing a document gives its memory back to the OPERATING SYSTEM rather than to glibc's free list: `libc` is a normal dependency of `vilan-lsp` on Linux now, and `malloc_trim(0)` is called after a document closes and after an analysis is dropped.
  ok    09cb7e94  The language server keeps a whole analysis only for the file you are in and the one before it: every other open document drops its `Program` to its editor tables and re-analyses when you come back, which takes kolt's nineteen open `src/*.vl` from 1,090 MiB resident to 510 MiB.
  ok    01684b36  `__with_finally` and `__guarded` cost a browser bundle 241 raw bytes, 94 minified and 10 after gzip — measured, and left alone.
        note: 01684b36 touched records only (`__with_finally` and `__guarded` cost a browser bundle 241 raw bytes, 94 minified and 10 after gzip — measured, and left alone.) - confirm its code landed
  ok    c9528180  A warm re-analysis is 6.4% cheaper, because the editor's own token table stopped asking a linear-scan accessor 426,198 questions it could ask once.
  ok    25d34f25  A keyed change costs the change, on both ends: 40x cheaper at 1,000 rows and 372x at 10,000, and flat in the collection.
  ok    740ea1a8  Rule 2's last-use elision can see a `Shared` CELL: a binding fed by `cell.read()` may keep naming the cell's storage when nothing can mutate that storage while the binding holds it.
  ok    aec708ac  M19's fourth tranche: R10 — the largest single line left on the reuse phase line — joins the Class A window, and what made that legal is that its whole-program report is keyed on a RENDERING rather than on an id.
        note: aec708ac touched records only (docs(changelog): M19 T1d — the measurement) - confirm its code landed
  ok    7fe26f99  Four small slices off compile-perf-29's list, each with its own profile diff, take a cold `vilan check` of a real browser application's client entry down 8.20% and the WARM analysis after it down 12.86%.
  ok    c993961c  M44's "under construction" claim is inert on every package whose entries do not import the same `std`, and the package-union key that would fix that is measured here and REFUSED.
  ok    fd51eb16  Two entries of one package that differ in a `resource` declaration threw away each other's drop-scan enrolment record and re-walked every module body in the program.
  ok    33f818c1  `vilan build` compiles a TIER of its legs at once, and every writer stays serial and in schedule order — kolt's three legs go 23.8 s wall to 15.7 s at no CPU cost (2.70 → 2.63 s, −2.6%), on a box at loadavg 114–128.
  ok    eed38bd0  M19's third tranche: the drop planner's per-body enrolment gate — the drop planner's whole price, and half of what M42 called the largest item left on the reuse phase line — is SERVED from a reused module's record instead of re-walked, and it needed no `Type` in the record to do it.
  ok    18bcd757  M19 T1b bounded `CHECKED_CACHE`'s new class D table slices at eight million ROWS, and its own comment could only guess what that was worth — "on the order of a hundred megabytes".
  ok    5867db52  M35 put a workspace's members on their own threads and measured the price it did not pay for: a member whose base-cache key nobody has built yet starts COLD, and two such members race to build the same pre-entry world twice.
  ok    2e73e4c9  M31 moved the analyzer's 35 `IndexMap`s off SipHash and named what it was told not to touch: 83M of the 85M Ir left in `sip.rs` was the const-eval interpreter's environment maps — left on `std`'s hasher by a comment in `fx.rs` claiming they are "not in the cold-analysis hot path", which the same profile falsified.
  ok    676a7aa8  `List::remove` and `List::insert` were hand-written shift loops over the CHECKED subscript helpers, so moving a list's tail cost a bounds check, a `__clone` and a store per element: `remove(0)` on a ten-thousand-element list was 13.9 µs, and `xs.insert(2, 3)` emitted a `while` loop where the host has a `memmove`.
  ok    93564019  Every `vilan check` and every `vilan build` compiled kolt's four macro worlds again from scratch — 0.57e9 instructions, 10.7% of a cold client check — for std files and a toolchain that had not changed since the last build.
  ok    b89e091c  A cold kolt client check clones `Type` 1.1 million times through `get_type_by_type_id`, whose borrowing twin `borrow_type_by_type_id` has existed the whole time — and the top of that census is the resource classifier, which clones its subject and then never looks at the copy again.
  ok    75c86c5c  `Node` was 320 bytes wide because `Func` — seventeen declaration fields — was inlined into two of its variants, and the parser moves `Spanned<Node>` BY VALUE up every precedence level, so every expression in every file paid for a function declaration's shape.
  ok    0ce9b060  The base cache is process-global and in memory, so every process that analyzes anything pays the whole cold `std` analysis again — and both differentials now run one process per corpus program. Measured: the base world is 190 ms of a 370 ms cold analysis, 51%.
  ok    799c897f  `lsp-index`'s entity table is the open buffer's, and it was built by walking the whole program to find it: 8.3–11.2 ms of thread CPU per keystroke that follows the CODEBASE, against 0.014 ms for the same rows fetched by the entry's own id range.
  ok    8896ed55  A completion request read the declaring module's whole text to render a candidate's one-line documentation — the last table-shaped cost left in the keystroke path after M25 and M29, and the only one that followed the CODEBASE.
  ok    76a66e87  `collect_unfollowable_loans` walked `expr_id_to_expr_map` three times, and the first walk built a `Vec<Convention>` for every call in the program only to ask whether it was `Some`.
  ok    92a7b697  `collect_resource_bindings` swept every variable and every parameter in the loaded world, twice per analysis, on programs that own no resource at all — and the pre-filter the item asked for was built, measured, and WITHDRAWN, because it made things slower.
  ok    6b88c9ca  The base cache's byte tally counted the module texts a world was built from and nothing else, so it under-reported every retained world by the size of T0's per-`TypeId` census — 25,900 B of a plain std world's 177,775 B, 17% of the figure M24's LRU budget is compared against.
  ok    d3fe3276  A keystroke stopped rebuilding the tables the EMITTER reads for every module it did not touch.
  ok    b4379cec  A package's entries were checked one after another on one of sixteen threads: a workspace's `check` wall was the SUM of its members where it could be closer to the largest.
  ok    cc0e41d5  `callee_bindable_generics` answered "which impl declares this member" by scanning every impl and, inside each, its whole declaration map — once per call, 856,908 times in one cold check, 10.4% of its instructions.
  ok    f4f53779  `vilan check .` cost 99.94% of what `vilan build .` cost, because a check of an ENTRY ran the whole transformer and threw the JavaScript away.
  ok    9a9fbe04  The analyzer's `IndexMap` tables were still hashing every `u32` key with SipHash: 7.4 million SipHash calls, `sip.rs` 6.7% of a cold `vilan check` of a real browser application's client entry.
  ok    eba51e4f  Drop planning asked one WHOLE-PROGRAM question — "is anything in this program a `resource`?" — and `std::db` and `std::fs` each answer yes, so a browser entry that owns no resource at all paid the per-body ownership walk for every body it had. It is a per-body question now: on kolt's client leg the whole analysis goes 1.775 s -> 1.642 s of process CPU, and on a thousand-function exhibit the planner walks 40 scan roots instead of 1,575.
  ok    5e29ed4a  Completion's third tranche: a member list stopped being a walk over every impl in the program, an auto-import edit stopped costing a whole-buffer parse per request, and the raw parse behind the element/`css` classifier is now paid only where either can contain the cursor. On E121's generated 1,791-function exhibit a completion request goes 0.813 ms → 0.415 ms of thread CPU and the three-provider burst 0.814 → 0.530 ms.
  ok    31fa19dd  A keystroke no longer re-checks every module of a program it did not touch.
  ok    186b5fdb  Completion was the keystroke path's entire remaining budget, and it was spending it on the CODEBASE rather than on the file: every request swept every `std`/`pkg` child module's `name_to_id_map` to find auto-import candidates, and an import path's origin arm did a `read_dir` per source root. Both are functions of the analysis alone, so both moved onto the analysis that produced them. On E121's generated 1,791-function exhibit a completion request goes 0.628 ms → 0.126 ms of thread CPU, and the three-provider burst 0.568 → 0.103 ms.
  ok    3213e0e8  A superseded analysis now STOPS. A keystroke burst used to run one whole whole-program analysis per debounce window and throw all but the last away at `land`; ten keystrokes on kolt's `views.vl` cost 989 ms of CPU each and now cost 127 ms — one complete analysis and nine cancelled, measured through the real notification handlers.
  ok    765353bd  The language server's base-world cache has a byte budget and evicts least-recently-used, instead of growing for the life of the session.
  ok    b4503ecf  Editing a file that imports an OPEN sibling no longer rebuilds the whole pre-entry world on every keystroke.
  ok    5fe3b82b  An editor keystroke no longer rebuilds the standard library's world just because the file imports a sibling module. Any `pkg::` import used to bypass `BASE_CACHE` outright, so kolt's `views.vl` and `client.vl` paid a full pre-entry world build on EVERY analysis — the `base` phase read 156–586 ms per keystroke (loadavg 10–30) — while their sibling `theme.vl`, which imports no sibling, hit the cache at 0.0 ms.
  ok    5fe3b82b  A generic bound is now evaluated once per (type, trait, arguments) instead of once per call site, which takes the whole-program bound check off the per-keystroke curve it was on.
  ok    5fe3b82b  `vilan build --watch` recompiles the legs an edit reached, not every leg of the workspace.
  ok    78011907  Semantic highlighting, inlay hints and completion now describe the buffer on your screen instead of the last finished analysis, and their cost stopped tracking the size of the codebase. On a generated 1,791-function exhibit the whole-program walk every request used to pay costs 1.104 ms of thread CPU; the keystroke path that replaced it answers semantic tokens in 0.004 ms — 276× less — and inlay hints in 0.001 ms, with the whole three-provider burst at 0.828 ms against the 10 ms budget (release, 50 requests per window, loadavg 41.9 on 16 cores; wall 2.926 / 0.004 / 0.001 / 1.632 ms beside it and asserted on nowhere). On a read-only copy of a real 1,791-function application the same request went from 12.2 ms of wall and 10 ms of CPU to 0.30 ms and 0 ms, and from 12.3 ms measured from the keystroke to 1.8 ms — at loadavg 45 against the earlier reading's loadavg 11, so the four-times-busier machine makes those a floor rather than a ceiling.
  RED   no commit in this repository introduced this entry - is it committed?
        The viewport semantic-token request now costs the viewport: `semanticTokens/range` computed the WHOLE file's token stream and then filtered it by line, so twenty visible lines cost 9.0 ms of wall and 10 ms of process CPU on kolt's `views.vl` against the whole file's 8.9 ms — a ratio of 1.01, which is the defect stated as a number (measured before E121's keystroke path landed, at loadavg 8.2–11.9).
  ok    dff2c268  Opening a file no longer parks a tokio worker for the length of its first analysis: a `semanticTokens/full` issued the instant after `didOpen` answered in 1270.7 ms before this change and 7.9 ms after, on kolt's `views.vl` at loadavg 8.2–11.9.
  ok    734be77f  A bounded generic's dispatch is refined once per TYPE reaching it, not once per CALL SITE — which halves every surface that analyzes a program with a large generated module in it: the language server's keystroke, a `vilan watch` round, and a plain `vilan build`.
  ok    9f2e317f  The questions both emitters ask of a finished program are answered in ONE place: a new `vilan_core::mono`, and the backend stops carrying its own copy of `RENDER_MEMBER` and of what `\n` means in a string literal.
  ok    c0683da5  `std/src/reactive_pipeline.vl` — the A124 paper's evidence, in std and out of the surface.
  ok    30c20147  A global host PROPERTY bound in the function form is refused where it is written, instead of emitting a call to it.
  ok    e4070eef  A counter that could only ever read zero is gone, and what the arm it watched actually does is written down.
  ok    8f420b47  `[derive(PartialOrd)]` is refused at the attribute instead of expanding to nothing.
  ok    0c37536b  The std twin-parity gate says what it means by "surface", and it is the EXPORTED one — which retired fourteen of its twenty-six recorded divergences.
  ok    03d83f9b  Two pins that could red for reasons that had nothing to do with the code they guard no longer can — and the rule that would have caught both is written at each suite's head: no assertion reads a clock.
  ok    8b401ddd  The completion engine has pins in the crate that implements it.
  ok    2292a7f7  A hover test helper that silently rewrote its own fixtures now refuses them.
  ok    fc16c3aa  `()` and `void`: the two spellings the grammar took and the language has no meaning for are refused where they are written.
  ok    13e44b7c  The language server's analysis fence is pinned, and its internal-error diagnostic now NAMES the file it could not analyze.
  ok    b4021651  `[internal("reason")]` marks an item that is part of the surface and dangerous to reach for — the editor hides it, dims it and leads its hover with the reason.
  ok    850c9114  Format-on-save honours your package's `[fmt]` section, and one comment run can be re-filled on demand.
  ok    f40ab8ba  A member missing from a std twin says which twin HAS it — and a file with no name evidence is coloured by what it does with the twins it imported.
  ok    4c3cc710  The native host census walks PAST a refusal, into the refused call's arguments and the bodies of the closures among them.
  ok    c682b2dc  The pipeline's own nodes stand behind one `dyn Source<i32>` field.
  ok    11fd28ec  std opts into `[fmt] wrap_comments` at 84 columns, and its prose is re-filled once.
  ok    ce55652b  A generic `impl` header past the width is now PRINTED, one bound per line — where a hand-wrapped one made `vilan fmt` decline the whole file.
  ok    e68ee261  `[fmt] comment_width` — prose gets its own width, and it defaults to the code width.
  ok    066dfabb  `vilan fmt` can re-fill your comments to the line width, behind `[fmt] wrap_comments` — opt-in, default off for one release.
  ok    80b948e9  A `vilan fmt` that declines names the line it and your file stop agreeing on, found by bisecting the two token streams — where it used to name the file's first declaration.
  ok    9035aac9  `vilan fmt` reads its own output back: the formatter parses the text it is about to write, and declines rather than write a file that does not read back.
  ok    f6b232a8  A context-threaded hidden parameter's TYPE is recorded where the pass mints it, so the native backend looks it up instead of inferring it back from the call sites.
  ok    839f50e4  A diagnostic still carries ONE note, and the first real candidate for a second one is why.
  ok    b6c62f2e  A nominal declaration's generic parameter has ONE spelling in its own body, and it is now written down and gated instead of defended against.
  ok    027fe10c  A generated name inside a `// witness:` is a hole, so the claim gate can no longer be reddened by the counter it says nothing about.
  ok    a8998ce8  `List::new` and `List::push` are `Intrinsic` rows like every other built-in lowering, and the class of defect B359 was one instance of is closed at the table instead of patched at each reader.
  ok    b0cd9dce  Four smalls Order 38's lanes left behind: the stack-frame record says what was measured, the depth line survives a refused parse, the temp scripts cannot be written through a symlink, and the JSON census says it is a census.
  ok    788400d4  A `$VILAN_STD` pointing at half a toolchain is refused ONCE, first, and against the file the reader can act on.
  ok    629171e7  Every completion candidate now states the prefix it replaces and the text to filter it by, so a hyphenated name survives in a client that is not VS Code.
  ok    e3c63ef9  A generic `<` closes itself, and a backtick pairs in prose — the two the editor made you type by hand.
  ok    cad84bdb  A `///` above a struct FIELD now reaches every position that field appears in — and a field position answers about the field rather than restating its struct.
  ok    ea9a7ee1  Hovering a generic call shows two signatures: the declaration as written, and the exact function that call reached.
  ok    f99330c2  A parameter's `context` clause renders once in a hovered signature again, and the pins that let it double now count instead of containing.
  ok    381c5567  "Who cleans up what" is a table in the guide and five pins in the suite, where it was five doc comments.
  ok    ee9edd4e  B366: `bind_parameters`' bare-constraint-id branch is deleted — it was unreachable, and the measurement says so.
  ok    53fc9913  `release.yml` has a dry run: `workflow_dispatch` walks the same graph up to the asset list and stops.
  ok    7a9f2e70  E69's TSV-to-table arrow has one implementation again: the Rust gate, and the python generator keeps only the fetch.
  ok    c931d39d  N86's tail of suites writing to the shared tmpfs goes from thirty-eight files to three.
  ok    05032e16  Three AST traps that each cost a lane a wrong emission are written down at their declarations, and the ledger's crate list is derived from the workspace instead of copied.
  ok    244fda88  The parser's stack frame is 34,181 bytes per level of source nesting, not the ~71.8 KiB the record claimed, and it has a canary now.
  ok    5b0faf43  Swallowed `\`-continuations are gated over every string literal in the workspace, and the thirty-eight sites the tree still carried, across nine files, are restored.
        note: 5b0faf43 touched records only (N98's changelog entry says thirty-eight sites across nine files, which is what the gate named and what was fixed.) - confirm its code landed
  ok    ce0bbd24  Two user-visible messages the ledger's walk cannot read are rowed by hand, at the files that build them.
  ok    ef8cce46  The diagnostics ledger's liveness check searches EVERY literal fragment of a row's key, not only the longest one.
  ok    e9dd3732  A lazy argument that reaches a resource through a FIELD is one diagnostic, not two: the argument refusal owns the shape and R9's thunk capture stands down.
  ok    d8a7d262  The HMR overlay places a transformer refusal in the file the refusal names, which is what the terminal has been doing since E190.
  ok    a632218b  A hyphenated attribute or css property completes under its own prefix: the extension declares a `wordPattern` that reads `stroke-w` as one word, where the client's default read it as `w`.
  ok    e47143ce  A101's refusal now carries the codemod: the editor rewrites `property: value;` into `property(value);` one declaration at a time, or every declaration in the file at once.
  ok    a1147c08  Format-on-save says when it DECLINED: the language server names the construct and the line once per file per cause, and the playground's formatter hands back a verdict instead of silence.
  ok    7329fc47  `vilan fmt` no longer throws away a whole file over a `const { … };` block: the `const` EXPRESSION prefix keeps the terminator its statement form needs, where only `const let` and `const fun` may ask the declaration under them.
  ok    9111c29e  Every `[vilan phase]` figure carries the CPU it burned beside the wall it took, so the perf arc's numbers are absolutes on a loaded box instead of shares.
  ok    488d404a  E189's BROAD GATE lands: on a program that does not type, the whole `context` family stands down and says so once — and kolt's `href(href())` shape goes from 35 errors to 1.
  ok    c118d23b  `VILAN_NATIVE_HOST_CENSUS=1` answers "what does this program still need from its host" in ONE emit, and kolt's server leg answers 85.
  ok    f10988d2  An entry-shaped world is weighed, and it weighs 80% of a resolved one — so the base cache's single 24x factor stands and M74's over-charge is 13%, not an order of magnitude.
  ok    ffb48c89  `resolve_world`'s eleven stages are on the `[vilan phase]` line, and the first reading through it says an open module's per-keystroke resolve is the constraint fixpoint and nothing else.
  ok    57598fad  An element head offers the attribute NAMES, generated from the WHATWG HTML and SVG 2 attribute indices and gated against a vendored extract of them.
  ok    f3f86783  The `context` clause stays after the return type, the one shape that position cannot spell is refused with both ways out, and both grammars paint the keyword where it actually sits.
  ok    b9ce70e2  One arity error in an element head reports alone at the head, and kolt's `href(href())` goes from 194 errors to 139 — the item does not close.
  ok    6879cbb5  A call the solver could not wire no longer deletes everything written inside it from the call graph — kolt's 184 errors become its 9.
  ok    c6948b45  The never-silent body-less refusal (B55) names the function that was being emitted when the requirement was first reached, and renders in that function's file.
  ok    096cba99  The analyzer's walk costs 42,464 bytes per level of nesting, measured two ways, and a canary now reds on Linux when that grows.
  ok    1fd46341  Eighty-eight scratch paths across thirty-nine test files moved off the shared tmpfs, and the tail is a list rather than a memory.
  ok    f7289fc4  Seventeen std doc comments stopped teaching a method A99 retired, and a gate holds the rest of them.
  ok    91aab5e1  The spec's two reserved-word lists are held to the lexer's table, and §2.2 — the normative one — was missing `lazy`.
  ok    9860e92b  The ledger's enumeration reaches the const channel and the macro engine: fifty-two refusals that were rowed by nobody now have thirty-four rows.
  ok    9fa452ce  `Program::hidden_impls` is `hidden_impls_pending`, because by the time most of the compiler can see it, it is empty.
  ok    219c660e  Two messages that shipped with eighteen spaces in the middle of a sentence are repaired, and the ledger refuses a third.
  ok    baefa933  B267's cell walk says what a counted `Shared` does to it, and its worked example names a relation that still exists.
  ok    e975dece  A `#` on an `(impl …)` selector whose blocks the module EXPORTS is redundant, and says so.
  ok    48e129e1  A67's bound is written down and pinned instead of overclaimed: the module-shadowing refusal names a collision something WALKS INTO, not one nothing has ever reached.
  ok    ab7a08c3  `vilan check` writes nothing into the package it checks: its macro expansion table moved out of `dist/` and into `~/.vilan/check-cache/`.
  ok    b773f4a4  std's fifteen `export macro fun`s are held to the formatter by name, and the fixed-point harness can no longer pass by bailing.
  ok    8e9e56db  `vilan fmt` says out loud when it could not format a file, with its own exit code: a bail is no longer reported as "clean".
  ok    600d393b  Completion at a `css` block's dotted head offers every `impl Style` method the file can reach, not only the fourteen condition combinators.
  ok    4e8b301d  The const channel's nine interpreter refusals are rowed, and the ledger says out loud that its walk cannot reach them.
  ok    b339596e  Two pins that went red under lane load and green on a re-run are bounded by what their instruments can actually measure.
  ok    164085c5  The `inference` harness writes its scratch under `target/`, cleans it by `Drop`, and says so when the disk is full.
  ok    c24b5263  The four asset artifacts a corpus build writes beside the corpus sources are gitignored, and a pin asks git rather than a list.
  ok    346fb70a  A `[derive(..)]` reached inside a macro WORLD is skipped instead of being refused as a compiler bug.
  ok    29e83e8e  A67's ambiguity names the child as it is spelled on disk, and it reaches the pure-NAMESPACE child it could not see before.
  ok    6ed903e8  The session summary's retained-state line names the analyzer's base cache: `base_cache_worlds`, `base_cache_weight` and `base_cache_budget`.
  ok    3908938f  A quickfix can edit another file, and B318's "Export `S`" / "Export `hidden`" become actions instead of sentences.
  ok    19253040  A head item with no parentheses of its own paints as an attribute name too: `disabled` and the `hidden` of `hidden.show(flag)`.
  ok    ad163380  The css-spelling refactor reaches an `impl Style` extension in ANY file of the program, not just the one the cursor is in.
  ok    56fcbe74  `vilan fmt` gives the module-level `export *;` marker a canonical place: the slot just below the file's imports, with a paragraph gap on both sides.
  ok    806dbfdf  `vilan fmt` sorts the condition values inside an `on` head, the completion table answers inside one, and the axis column is held to the byte each constructor actually builds.
  ok    fd4dafa2  Three visibility finds from Order 35 close: a selector that admits nothing says so at the import, `export(in <general PATH>)` is enforced, and `source_of` stops lying about a module that owns both a file and a directory.
  ok    6938bea6  The std cache prunes itself at materialization, and `vilan cache prune` is the gesture for the times that is not enough.
  ok    6a291bd0  R10's two reuse predicates become one, keyed on the site rather than on which handle the caller happened to have.
  ok    010a444b  The Rust DERIVE generators are deleted, and a std that does not declare a derive's macro is told so instead of being served a second answer.
  ok    11004d8e  std's manifest states a census a reader can re-run, with the command beside it: 664 top-level declarations across 63 files, 118 of them reached by name from another std file.
  ok    c4ed1169  `SignalCell::update` and A80's pattern-binder steer both name the write-back the cell-growing idiom needs, instead of stopping at the spelling that binds a copy.
  ok    b7ea1e41  `lib` is a module's BODY FILE and not a segment below its own directory, so `import pkg::a::lib` is refused with a steer to `pkg::a` instead of resolving `a/lib.vl` a second time under a second name.
  ok    4f786f8f  A malformed import reports at the token its path grammar stopped on, with a rule naming the import grammar, instead of `found 'import' expected an expression` at column 1 of the statement.
  ok    0464161d  Two blanket impls whose argument binders are bounded DIFFERENTLY stay admitted, and what that costs is now written down at the rule and held by two pins.
  ok    297a8929  The `[expose]` ELEMENT refusal quotes the annotation as the author wrote it, so a field annotated `SignalCell<models::Note>` is told about `models::Note` and not about `Note`.
  ok    c15ad25e  An `[rpc]` method returning `Option<KeyedCell<K, T>>` is refused in the method's own vocabulary instead of inside the code the attribute generates.
  ok    4d09b402  A `context` clause written where the threading cannot follow it — inside a `List`, a `Map`, an `Option`, a tuple or an array — is refused instead of admitted and ignored.
  ok    7f58266d  Organize Imports prunes an unused impl selector, rewrites an emptied statement to the selector its file actually needs, and completion answers inside one.
  ok    675ec9fd  `Vilan: Show Language Server Status` asks the SERVER for its session summary now, so E166's memory numbers arrive when you run the command instead of at the next 500-request tick.
  ok    b091f593  An unused import leaf whose statement Organize Imports will KEEP says so, instead of fading with the same "unused import" text as one the action deletes.
  ok    474971b5  "Convert to a `css` block" inlines the CURRENT file's own `impl Style` methods, so a chain that opens with the app's own shorthand converts instead of offering nothing.
  ok    d4e82a8a  The book's element-tag rule guards the OPENING tag's `<` and stops guarding the closing one's, so `</span>` after text is a tag in the third grammar too.
  ok    bb12b4bc  A head item's NAME paints as an attribute name, so `class("row")` is no longer a call and `type("checkbox")` no longer the declaration keyword.
  ok    07ec37ba  A closing tag straight after text is a tag again, and the `>` of an attributed opening tag is a tag delimiter wherever it sits.
  ok    2328b162  The TextMate grammar is gated on the scopes it produces, not on the text of its rules.
  ok    433d6737  `hmr_css_matrix` waits for the round to say it finished and then reads until the bundle PARSES, instead of reading a file the watcher is still writing and hoping two substrings mean it is whole.
  ok    c955b2bf  The replay harness refuses a package's MANIFEST directory where its source root belongs, instead of quietly measuring a smaller program.
  ok    9ea794d6  The last two DOM stubs outside N73's consolidation are folded in: `mount_missing_id.rs` and `hmr_swap.rs` run against `support/dom/stub.js` now, and the tree holds ONE hand-rolled DOM.
  ok    7fc6eba7  The ledger's hand-rowed exemptions name their rows by KEY now, so the one kind of row that most needs an exemption — a lane's unnumbered `NEW` one — can finally be written into the list.
  ok    98676cfa  The `css` block's import steer is deleted as the element's twin was: the second of two notes that could only fire where their own advice was false.
  ok    82c038d2  An element argument is atomic to the chain-break rule: `vilan fmt` was rejoining a hand-broken `.child` ladder into one over-budget line and then tearing an element open inside it.
  ok    32816069  The "Convert to a `css` block" refactor never fired on real code — the seed it demanded is the one spelling the estate does not write, and any typed link refused the whole chain.
  ok    bd7d3931  `Vilan: Show Language Server Status` reports memory now — resident size, heap in use and heap retained-free — beside the cardinalities it always had, and says how many of the open documents still hold a program.
  ok    f80c8420  A keyed handle's KEY is now Wire-checked (and Hashable-checked) in the `[rpc]` method's own vocabulary, on the annotation the author wrote, instead of failing four times inside generated code.
  ok    f56882b8  A suspending `async fun f(&mut self)` `[rpc]` method takes ONE report — the `[service]` attribute's — where it used to take that one and E3's view-parameter refusal beside it.
  ok    a29efc51  An `[rpc]` method whose name the `[service]` expansion already generates is refused at the attribute and spanned on the method, instead of colliding as a duplicate definition inside code the author never wrote.
  ok    a16f583a  The name-keyed candidate list behind B254 and B258 is swept: every consumer of `candidates_of` now says what the list is keyed by and which DIRECTION it reads an edge in, because that direction is the whole argument for whether widening is safe — and the invariant B258's silence rested on is held by a pin rather than left implicit.
  ok    da4ef2f3  A written `::` path quoted in a diagnostic is the path the author wrote: `[rpc] fun note(self, id: i32): models::Note` is refused as "return type … is `models::Note`, which is not Wire" instead of "… is `_`".
  ok    7f7a4654  Two blanket impls declaring one name are refused when their bounds OVERLAP, not only when they are identical — "no silent pick" is the rule tier 1 of method resolution cannot keep on its own, and sameness was a narrower test than the rule needed.
  ok    99c022db  An `a.vl` that declares `b` beside an `a/b.vl` is an ambiguity error now — the same answer `a.vl` beside `a/lib.vl` has had since A65 — and the collision was worse than the item recorded: the DECLARATION was winning, and the file could not be reached at all.
  ok    42883a86  `Some(mut list)` was already the spelling; what was missing was what happens when you write `Some(let mut list)` — the two binding forms in a pattern name the one that was meant now, instead of reporting the payload's own paren.
  ok    aab095bf  The eighteen curated rule statements and the resource-derive refusal were unrowed messages rather than red ones, and the ledger gate holds both now.
  ok    ba0d3554  The last wall bound N61's sweep left standing is gone: "the verifier's refusal landed on the BOUND, not when the hook finally answered" is asserted as order now, not as 2,500 milliseconds.
  ok    b75a29c2  The element-syntax import steer is deleted rather than narrowed: a note that could only fire where its own advice was false.
  ok    3e56f67f  `nested_nurseries_join_inside_out` asserts the nesting it is named for instead of an interleaving nothing promises: the set of lines, plus the four orderings a nursery actually guarantees.
  ok    15a4a067  A free task's failure is assertable now, and B277's other half is pinned with it: a `Debounce` whose OWN callback throws survives it, and says so on the way out.
  ok    4d8af1ef  The eight browser-bundle suites now share ONE DOM stub — `crates/vilan-cli/tests/support/dom/stub.js`, with each suite's extras layered on top by `concat!(include_str!(..), include_str!(..))` — instead of nine near-copies of it spread across their test files.
  ok    78c1a8af  The reference index's first invariant is now checked over every module a program loads instead of over the entry file alone, and it immediately found two rows whose text is not their definition's name — the exact class B264 closed one instance of.
  ok    bee7fa0c  The element-head sorter's bargain is written down where the people it affects will read it: sorting an element head moves WHEN its attribute values are evaluated, and that is accepted rather than checked for.
  ok    c64e5286  A `for x in <iterable>` head over the line budget now breaks at its iterable, the way a `for` condition and a `match` subject already do.
  ok    18395ced  A doc listing of a std enum is now held to the enum: the book cannot go on counting five variants after std declares a sixth.
  ok    f1deb784  `match x { .. }::foo` is refused with what is actually wrong — a path's left side is a NAME, and a block-like form is a value — instead of being steered to parentheses that do not parse either.
  ok    97ee2943  A fixed-array generic argument no longer ends the TextMate list at its own length separator: `SignalCell<[i32; 4]>` is highlighted as one head instead of stopping at the `;` and falling back to the top-level rules.
  ok    3c3c931d  Completion inside a struct initializer offers the struct's FIELDS: `KoltStore { us▮` used to fall through to the bare expression position and list every binding in scope, which is the one list that cannot be right where the author is writing a field name.
  ok    55bcc01b  A `context` clause lives on a PARAMETER and nowhere else, and the suite now says so by name: a closure stored in a struct field cannot carry one, and an injected closure cannot be stored at all — which is what blocks A85's positional-slot VALUES, not shipped here.
  ok    240720ad  An `impl` whose `with` clause under-supplies a trait's arguments now reports once, where it used to report the arity error and then a conformance mismatch read off the padding that hid it.
  ok    97aa7bb1  The Rust fallback `[service]` generator is gone: a std with no `rpc.vl` is now told so, where it used to be handed 270 lines of a stale twin of the macro that expands the attribute for everyone else.
  ok    27d0b3e8  An `[rpc]` method declared `async` may no longer take `&mut self`: the receiver is a promise that the handler does not await, and until now nothing enforced it.
  ok    a90a5a27  A non-Wire `[rpc]` payload reported two and three times: the signature refusal the author can act on, plus the generated client's own `call<T: Wire>` bound failures, anchored over the whole `[service]` struct and reading first.
  ok    5d48c85b  The language server's session is measured rather than suspected: 2,000 keystrokes on one document do not get slower and do not accumulate, and what a session really grows along is the files it OPENS.
  ok    41b4122d  `Color::var` and `Length::var` refuse a custom-property name without its two leading dashes at const time, instead of rendering a reference no browser resolves.
  ok    526964cb  One unresolved name in an `impl` SUBJECT is one error again: `impl Source<Option<Nope>>` said `cannot find type 'Nope'` twice for one mistake, where `let x: Option<Nope>` has always said it once.
  ok    6cb86d9c  A BARE TRAIT in impl-subject position now MEANS "for every type that implements it": `impl Source<type I> { fun flatten(self) .. }` is `impl type S: Source<type I> { .. }`, so `self` in the body is the implementing type instead of a value of a trait, which vilan has none of.
  ok    9d9c8922  `bind_callee_own_generics`' dropped reconcile is settled rather than papered over: a later check always fires, the drop is not evidence of a defect, and both facts are now written at the site and pinned.
  ok    13051db2  A handle-returning service wired to a connectionless mount now says so where it can be acted on: `local_rpc` over an unstamped protocol is refused at WIRING time naming the method, and the `{mount}rpc` POST leg's runtime failure names the method and the transport that works.
  ok    097bf774  `[expose(keyed = K)]` over a `KeyedCell<K2, T>` whose written key disagrees with the cell's own is refused at the attribute, instead of the argument being silently ignored.
  ok    0e2fc458  `[expose]` on a struct carrying only `[client_service]` is refused at the attribute instead of compiling into a channel nothing ever mints.
  ok    ccbc995d  A nested generic head had four rules painting it and no two agreed: in `impl Source<Option<type _: Source<type U>>>` the semantic layer took `type _: Source<type U>` as ONE type-parameter token, `<type` opened a tag, the other brackets were comparison operators, and a wrapped head's closing `>` was an element terminator.
  ok    eeca2e8d  The rpc guide's hand-wired-channel trap now names what actually makes a hand-wired mirror different — it has no ORIGIN — instead of a mechanism claim about `Unsubscribe` that the demand ruling supersedes, and `RpcError` is documented with all six of its arms.
  ok    f03ee14d  The four handshake refusals are one table in the services guide, on the line that decides between them: who made the call.
  ok    a092d246  `[expose(keyed = K)]` over a `SignalCell<Map<K2, V>>` whose written `K` disagrees with the map's own key is refused at the attribute, instead of the argument silently winning and the disagreement surfacing four times over as a `Keyed<K>` bound failure in generated code.
  ok    71570b77  `[rpc] fun login(mut self, …)` compiled and lost the write in silence: the handler mutated its own copy, the reply carried the new value, and the very next call on that connection read the old field.
  ok    234fe33f  A handle return's WIRE rule lands on the element, not on the source.
  ok    4afb7a3e  `:hover { … }` inside a `css` block is steered to `.hover { … }` instead of reporting a missing declaration.
  ok    4afb7a3e  Property-name completion inside a `css` block now offers the CSS property index, not just the properties a `Style` method happens to write.
  ok    877bebf3  `fun bump(a: Add) { a + 1 }` refused as "`Add`'s `add` accepts `Add`, but the right operand is `i32`" - three `Add`s in one line, two of them the parameter and one of them the trait it is bound by.
  ok    7e5db561  `import pkg::lib;` where `lib/` holds modules but no `lib.vl` is refused, and the refusal names the modules it does hold.
  ok    7e5649f5  B248's refusal was at the STATEMENT fork; the rule is about the FORM, so `let y = match x { .. } + 1;`, an argument position, and a `.` chain on a block-like head all took the bare parse error instead.
  ok    a011c61e  The missing-member refusal's HEAD named the trait UNPARAMETERIZED — `'Counted' does not implement trait 'Source'` — even where the arguments were what the refusal was about.
  ok    e7c435d4  A GENERIC `[service]` subject had no refusal at all: it expanded anyway and handed the author the generated client's own errors — `'contract_hash' is already defined for 'StoreClient<T>'`, then `` `Store` takes 1 type argument, 0 given `` twice, then two "cannot call method … on unknown".
  ok    2b5592a3  Two accuracy gains Order 29 shipped are now observed by something: B254's narrowing is asserted as a missing EDGE, and B244's composing substitution is measured over the corpus with a bound.
  ok    69492cf6  Every on-disk cache lives under `dist/.cache/` — the owner's ruling — and `vilan check` may create that directory and writes nothing else, pinned.
  ok    3745cd9a  `own` is a keyword the tour never mentioned, and a lane spent a debug cycle discovering it on a fixture.
  ok    5c7893ac  A 45-second WALL bound on a whole `vilan run .` — build included — for a claim about how many times a client redialed. Three of those, replaced by the count itself.
  ok    ee901d6b  Three smalls Order 29 left behind: a fourth `wait_for_port`, a fixture the formatter would rewrite, and a comment that counted to nine while the tree counted to twenty.
  ok    72520330  The ignore line N58 added for `file-corpus-<random>/` is gone, because the strand it covered cannot happen: a corpus program runs in a scratch working directory of its own.
  ok    86c9e559  A module DIRECTORY with no body had nowhere to point: go-to-definition on `ui` in `import pkg::lib::ui::widget::label;` landed on line 1 of the file the caret was already in, and hover said `module ui`.
  ok    8b80a64e  `transport_robustness` flaked once on `EADDRINUSE` under lane load, and the reason was a bind-release-rebind window a whole `vilan build` wide.
  ok    f7efa0f1  N55's `vilan fmt --check` gate covers `.vl` FILES only, so the 208 compilable ```vilan fences of `vilan/docs` and `README.md` are outside it — and they stay outside, stated rather than silent.
  ok    161e8834  A corpus differential that dies mid-run strands `file-corpus-<random>/` in `crates/vilan-core`, untracked and unignored.
  ok    87084164  E128's own impl pin imported `PartialEq` from `std::operators`, the line that seeded the E138 phantom — and it still carried it after E138 was closed as a phantom.
  ok    c40716b1  M36 asked for a cross-process on-disk base cache and the spike answers that the on-disk unit cannot be the world: `World` is keyed in three places by AST node ADDRESSES, and one of them hangs a lazily COMPILED macro world off an `Arc`.
  ok    2c9a9301  A `match` leg body written `=> { expr }` expanded to three lines whatever it held — E146 rule 2's inline-arm rule, one construct over.
  ok    69a8e0b0  A `for` condition — vilan's `while` — and a `match` SUBJECT had no split permission, so an over-budget one had nowhere to break at all.
  ok    fb28428c  `vilan fmt` canonicalizes an element HEAD — the import and style-chain sorters' third sibling: undotted attributes lead in `id`, `name`, `type`, `for`, `href`, `src` order and then alphabetically, `on:` handlers follow them alphabetically, and a dotted chain link is a BARRIER that never moves.
  ok    e04911e9  `match x { .. } + 1` in leading statement position reported `found '+' expected an expression` — true, and about a statement the author did not know they had written.
  ok    1c5d6676  `impl Counted with Source<i32>` was told to `declare fun on_change(self, observer: |T| void)` — a `T` the impl does not have, cannot introduce, and cannot copy the line without.
  ok    0065ac30  Four small editor defects from E145's neighbourhood, each with a pin.
  ok    afe7dc7f  The "Declare the inferred contexts" quickfix fired on B242's SUBSET refusal and on nothing else — which is the refusal a caller almost never meets.
  ok    e0d67f10  E142's `::` line rule now reaches the two productions it left out: a TYPE PATH and a STRUCT-LITERAL HEAD.
  ok    9e72b3bb  What a keyed channel costs in CPU, measured before anything was built for it: `keyed_diff` re-keys two whole snapshots on every change, so it is O(N) per change PER SUBSCRIBED CONNECTION — and the client's `KeyedSource::apply` reaches `index_of` per op, which is a linear scan of the mirror, so both halves are O(N).
  ok    7a850800  A cold `vilan check` of kolt's client compiles FOUR macro worlds — 18.2% of that entry's instructions, comparable to its own whole-program checks — and the phase line could not see any of them.
  ok    bb5c88df  `vilan fmt` sorted a bare `self` LAST in an import group, because the sort was plain ASCII and `self` is a lowercase four-letter word: `Option::{ self, Some, None }` reprinted as `Option::{ None, Some, self }`, and N55's reformat wrote that shape into 37 groups — `vilan/std/src/prelude.vl` and `web.vl` among them.
  ok    c4f29232  Every `if` arm expanded to its own lines, wherever the `if` sat, so a one-line closure body inside a builder ladder became five: `.bind_text(pending().map(|busy| if busy { "..." } else { "" }))` came out of N55's reformat as a `{`⏎`"..."`⏎`} else {`⏎`""`⏎`}` in the middle of a `.child(…)` chain — and the enclosing chain, measuring a first line that now FIT, never broke.
  ok    0bc36b2c  A declaration's `context` clause printed where it was WRITTEN, so `fun slot(x: i32): i32 context turn borrows x` and `fun slot(x: i32) borrows x context turn` were two spellings the formatter kept apart instead of one it answered.
  ok    aa6459fc  A comment written between a field's name and its value walked out from under the field it explains and landed below the whole statement: `Task { id = 1, name = // the row's label`⏎`"x" }` reprinted as one inline literal with the comment on a line of its own after the `;`.
  ok    c2593131  A binary-operator chain had NO break rule, so the formatter JOINED hand-wrapped conditions without a width bound: `vilan/macro_std/src/meta.vl`'s three-line `text.contains(" ") || …` became one line of 182 characters under N55's reformat, and four lines that reformat ADDED exceed 100 in a tree whose own reflow target is 100.
  ok    ffa69aea  `import a::b as b;` renames `b` to `b` — it binds exactly what `import a::b;` binds — and the formatter kept it, so the redundant spelling was one a rename could produce and nothing would tidy away.
  ok    0a7b0f13  M19 T1's corpus replay differential was a 60-170 s sweep living inside `check_scope_differential`, so every targeted `-p vilan-core` run and every lane's whole-crate gate paid a corpus analysis to ask a question about the S1 seam.
  ok    d3dbbe48  `vilan fmt`'s output was ADVISORY: 101 `.vl` files in this repository — seven of them under `vilan/std`, including both of std's preludes — differed from what the compiler in the same commit would have written for them, and nothing said so.
  ok    111e98da  CI's verdict took 30 minutes for a suite that runs in about 4.5 warm, and none of the difference was test time: `test(ubuntu)` 25 min and `test(windows)` 30 against every other job at or under 3, on runners that compiled the workspace from scratch every single run.
  ok    8d734966  CI's gate commands were a list you could only run by reading a YAML file and retyping six commands, and `scripts/ci-local.sh` is that list with a runner around it — called by `ci.yml` itself, so the two cannot come apart.
  ok    951adae0  `vilan check ~/code/app/src/sidebar.vl` compiled that file against whichever checkout the SHELL happened to be standing in — 37 `macro PartialEq's definition did not compile` typed from the vilan tree, 0 typed from the application's own directory, on one unchanged file.
  ok    50589b1f  Five of eight concurrent runs of the corpus's filesystem program die on `ENOENT: no such file or directory, open 'file-corpus.txt'` — and the two corpus differentials run it concurrently by construction.
  ok    416e2329  The macro engine's conformance gate was the last whole-corpus loop in the tree: ONE test over 124 programs, and a private node deadline of 30 s the two differentials had already moved off.
  ok    3c047de2  `vilan fmt` gives a struct literal's field one spelling: `A { x = x }` is printed as `A { x }`.
  ok    88569090  The normative grammar listed `!` and `?.` as postfixes and had no production for the bare `?`, which has been in the language since 2026-07-16.
  ok    3dcaf1c2  E124's dead-code paint shipped with five of the paper's pins unbuilt, a withdrawal that cleared a whole package on any keystroke, and a union that paid its entries one after another. All three are closed, and the one open question in it is decided.
  ok    78058109  The keystroke gate's per-request budgets are RELEASE figures and the gate asserted them in whatever profile it was run in — so two Order 27 lanes measured completion at 0.705-0.813 ms against its 0.2 ms budget and reported a 4x regression in a tranche that had not moved.
  ok    3dbd8b86  Hover on `PartialEq::eq` and `PartialOrd::lt` was reported to answer nothing where `Add::add` answers a signature; it answers one too, and the silence was an import from the wrong module.
  ok    e27dee56  The diagnostics gate was asserted on a program that never had the problem; it now runs on one that does, and it is RED at 1,053 ms of CPU per keystroke against its 500 ms mandate.
  ok    c15c48ba  The corpus had a program nothing could ever run, and now it has a rule about that.
  ok    27f0b402  The inference differential still had the shape N49 took off its sibling, and the corpus was written down twice.
  ok    14a3e7e6  The language server now handles `workspace/didChangeWatchedFiles`, so a package file written by something that is not the editor is offered by the next completion instead of waiting for an unrelated edit.
  ok    6e9974b6  Hover on a trait's own method showed `fun add(self, b: Add): Add` — a signature nobody can write.
  ok    97b0d679  `vilan fmt` now reformats one-link chains over 100 columns.
  ok    7dfe84da  A two-value element attribute reports on its comma in both element spellings, instead of asking for a `;` on the closing tag.
  ok    813823a6  An unfinished `a::` path now names the name it is missing, instead of asking for a `;` on the `::`.
  ok    d029c356  The changelog's family set gains `fix` and `performance`, and the cut orders six ranks.
  ok    a972bc5e  The release differential is one test per corpus program now, and the 600 seconds it was really spending turn out to be one program that never exits.
  ok    e6cb4667  Seven more exemption tables say out loud that they are still exempting something.
  ok    3a027232  Two rules from Order 25's seal, which was green locally and red on Windows CI.
  ok    0847c531  `VILAN_WATCH_LOG` traces what a `--watch` session SAW and what it DECIDED.
  ok    46ae17b9  std's `rpc.vl` drops the `0 - 1 // unreachable — the panic throws` that `parse_announced` carried after its `panic`, and stops destructuring a `channel` that `dispose` never reads.
  ok    7d5d5bc7  A chunk that reached another chunk's function had no sound way to name it — the emitted preamble snapshots its dependencies once, at evaluation, so a chunk that lands first binds `undefined` and keeps it — and cross-chunk references are now emitted as registry reads AT THE USE, with the by-value snapshot kept for the names the partition proves EAGER. Latent today, and deliberately so: the route partition cannot produce such a reference at all, so every artifact of the split fixture is byte-identical and the change is inert on every plan v1 can make.
  ok    8548b11f  Three piles of follow-on diagnostics B182's stand-down did not reach are gone, each of them a second report about a mistake the compiler had already named once: an unresolved field annotation, a `[derive(Wire)]` built out of a refused annotation's spelling, and an `[expose]` whose refused exposure was restated by the code the `[service]` expansion writes. The three exhibits go 2 → 1, 4 → 1 and 3 → 1, and the kolt-shaped fixture — 21 diagnostics before B182, 4 after it — is now 2, one per mistake actually written into it.
  ok    5fe3b82b  Two buckets of the `VILAN_PHASE_TIMING` post-pass line are now named after what they time, and the constant that was hiding inside both of them has a bucket of its own.
  ok    fecad4a1  E114's remaining two classes ship, and the third one in the item is RULED OUT rather than deferred: a local binding nothing reads is now faded, every statement after one control cannot fall through is faded, and an unused top-level `fun`/`let`/`struct` is not — because Vilan has no private declaration for the item's "unused private declarations" to be about.
  ok    e42238b9  Two corpus goldens had been witnessing the OPPOSITE of the sentence above them since disposal moved to last use, and a corpus program can now NAME the bytes its claim rests on, so a comment cannot go stale under a regenerated golden again.
  ok    48f3a766  The docs twin of that finding: the resources tour opened by stating the law c8609287 REPLACED, two of its programs printed the exact opposite of the output claimed beside them, and the `OwnedNursery` example's background work never ran at all.
  ok    1400bb74  Every std import steer was silently dead in the web playground — the compiler with no filesystem behind it — so the one surface whose visitors most need "import it first (`import std::reactive::Signal;`)" was the one surface that never said it.
  ok    de3b0424  The wall-clock waits run one at a time now, the two bounds that were too small to be liveness bounds at all are liveness bounds, the union's stragglers start first — and the CPU-time cure the item proposed was measured against each family and applies to none of them.
  ok    1ccbf630  Three exemption lists that could only ever go green now red when the thing they exempt stops needing exempting, and the mdBook pin is held in every place the repository spells it instead of one.
  ok    70336ad7  The ignored-pins backstop was suspected blind to a `cfg(windows)` pin and is not — it enumerates textually — so the fix is to say so where it can be checked, plus the one dead helper that was blocking a Windows clippy leg.
  ok    749c7c4c  B198 ruled: a containment test resolves BOTH its sides or neither, never a case-folded root against a lexical child — and the defect turns out to be reachable on unix too, through a symlink, which is how it was red-proved.
  ok    e42238b9  Three record corrections where the prose and the tree had come apart: ledger rows cited two off after integration, an import described as "still" present that had been deleted, and a character cap counted in bytes.
  ok    ac453b38  Windows' own link spellings — junctions, case folding, the `\\?\` verbatim prefix — are now measured by CI instead of inferred from the unix run, and `vilan fmt`'s cycle guard can finally say "I do not know what this directory is".
  ok    f2e14409  Three compiler steers named a spelling that does not compile, and audit run 7 walked each one into the refusal it leads to; all three now name a spelling that does, pinned by BUILDING the program the message draws and applying the message's own words to it.
  ok    6400727c  An import nothing uses is now FADED in the editor rather than left looking live — the first of the three gray-out classes, and it is paint: a hint carrying `DiagnosticTag::Unnecessary`, out of the Problems count and out of every gate.
  ok    279b397a  The language server's line-index cache no longer answers for text that is gone: an entry is served only while the file still matches the length and modification time it was built from.
  ok    eb8a4a5e  The formatter breaks after `|| ` when a closure's element body has to split, so the opening tag, the children and the closing tag are all measured from one column.
  ok    9f7caeab  An element head's closing `>` keeps its highlight when it lands on a line of its own: the parse now records the element's angle brackets and the language server paints them, so the shape a head is written in cannot change what it looks like.
  ok    deb190d3  Writing the `import pkg::…` that reaches a file now re-colors that file in the editor, no restart: a `pkg::` graph edit sweeps every open document in the package, and a saved `vilan.toml` sweeps them again — which it had silently stopped doing.
  ok    7ddcf025  A ghost diagnostic no longer flashes back after a comment/uncomment round trip: every analysis now carries the world revision it READ, and a result older than the one already landed is dropped instead of published.
  ok    f30897ee  A release whose notes exceed GitHub's 125,000-character body cap publishes anyway: the notes keep whole entries to a safe margin and point at the changelog for the rest.
        note: f30897ee touched records only (changelog: the release-notes cap fix's entry (missed by the chained edit)) - confirm its code landed
  ok    cfb1e684  Two `[expose]` fields annotated with the reactive TRAIT rather than the cell produced 53 diagnostics for two mistakes, and printed the cascade FIRST — so the owner read a message about code they had never written and never reached the root two pages down. The same package now reports its two roots, once each, at the top of the list.
  ok    d60e7949  All ten example projects adopt the prelude: every manifest states which ambient scope its sources resolve under, and 33 import statements the prelude carries are gone.
  ok    8f43058e  The two e2e server suites stopped guessing a port: the server asks for port 0 and reports back the one it bound, closing a bind-release-rebind window a whole `vilan build` wide.
  ok    857040a4  A blanket impl never satisfies a bound for an ABSTRACT parameter, and that refusal is now the promise rather than an open question: it is spelled in the spec, pinned as a negative, and the appendix entry says so.
  ok    f19ad8a4  Spec §2 now writes a production for `ISTRING`, the token class §3 had been using normatively without one.
  ok    2e3ac7b4  A `fun view` of your own captures the element desugar's callee, and the diagnostic now says so instead of reporting an argument count nobody wrote.
  ok    e60736da  The ledger gate's coverage check had a catch-all row that was holding five diagnostics it has nothing to do with; those five are now rowed, and the ten new rows include the `+` ladder the enumeration can never see.
  ok    a41461dd  The two reserved-package-name refusals disagreed about the reserved set for two releases; they now read one constant, and the set itself is enumerable and checked.
  ok    ad33ec2c  `book_sync`'s mdBook refusal builds its install line off the pin instead of spelling the version again.
  ok    8335e116  An `#[ignore]`d pin whose tracker item has been CLOSED is now findable: `scripts/ignored-pins.py --tracker <path>` cross-checks every pin's named owner against the tracker index, and found one on its first run.
  ok    1ff39997  The release gate now carries fmt, clippy and audit, and a gate holds it to CI's legs so it cannot fall behind again.
  ok    ed929179  An array can carry a trait impl at last: `impl [i32; 2] with Add<i32>` was refused with "`type`'s `add` returns `[i32; 2]`, but `Add` declares `[i32; 2]`" — the same spelling on both sides of a message that contradicted itself.
  ok    03b606ad  A field or method miss on an OVERLAID std type now names which twin you got and WHY this file is analyzed under that platform — one of four reasons, printed.
  ok    83d8cf53  Two refusals for the reverse direction, both at the attribute and both in its own vocabulary.

order (§7.2) — breaking, then miscompiles, then features, then diagnostics and tooling

   1  breaking    A reordered list is reconciled in one pass: `each`, `each_values`, `each_by` and `reconcile` now ask their KEY for a hash as well as an equality (`K: PartialEq + Hashable`), and a 1,000-row reversal costs 1,000 key comparisons instead of 500,500.
   2  breaking    A bare trait at a struct FIELD is refused again, and the message names `dyn Trait`.
   3  breaking    `std::style::prelude` publishes the 46 names it was designed with, and not the 21 internal alias spellings that were riding out on `export *;`.
   4  breaking    `std::random::range` is the range its doc always said it was: `[low, high)`, so `random::range(1, 7)` is a d6 and never answers 7.
   5  breaking    `Self<i32>` is refused, with the steer: `Self` already names the impl's subject WITH its arguments.
   6  breaking    `std::reactive` has a change structure: `DeltaLog<O>`, `DeltaCursor`, the `SeqOp`/`MapOp`/`SetOp` vocabularies and a `DeltaSource<C, O>` bound, with `KeyedCell` re-based on them and not one wire byte moved.
   7  breaking    An awaited `void` rpc stub answers `Result<void, RpcError>`, not `Option<RpcError>`.
   8  breaking    BREAKING: the Wire visitor's receivers are `&mut`, and the ten `Shared` cells they were forcing are gone — `std::json` and `std::binary` now construct no cell at all.
   9  breaking    An explicit type ARGUMENT survives an argument that knows less, and a trait's own arguments stop being inert: `Signal<Option<str>>::new(None)` means what it says.
  10  breaking    `Option::expect`, `Option::unwrap_or`, `Result::expect` and `Result::unwrap_or` take their argument `lazy`, so the fallback and the message are built only on the path that needs them.
  11  breaking    A `css` block's declarations are CALLS: `property(value);`, not `property: value;`.
  12  breaking    `lazy` is a keyword, and a `lazy` parameter defers its argument to the callee's first read of it — evaluated at most once, late, and not at all if the callee never reads.
  13  breaking    `Style::attribute`'s and `Style::within`'s `Option<str>` enters its one-release deprecation window, with the condition value as the steer, and the estate moves ahead of it.
  14  breaking    A styling condition is a value you write `.not()` on, `child_relation` is deleted, and an ancestor guard takes any condition an ancestor can match — the condition surface A95 designed, now the documented one.
  15  breaking    The six `View` parent methods are RETIRED: `when`, `swap`, `swap_split`, `bind_each`, `bind_each_values` and `bind_each_by` are gone, and the free slot values of `std::ui` are the whole surface.
  16  breaking    A type's method namespace is now per IMPORTING FILE, and two packages that each extend one type can both be installed.
  17  breaking    std curates its export surface — 548 of its 856 declarations are `export`ed, 308 are the modules' own machinery — and the suppression that hid std from its own visibility warning is gone.
  18  breaking    A type segment REPLACES an import path's namespace — enum and struct alike — so `Length::rem` is the static in the one file that also declares a module-level `rem`.
  19  breaking    A plain import of an item its module does not export WARNS, with the reach marker as the fix — the warning that lets the default flip without breaking a program.
  20  breaking    `[doc(hidden)]` is retired, and the `pub` rule stops telling a Rust writer to delete the word.
  21  breaking    `export (helper);` and `export * helper;` are refused instead of compiling clean and meaning nothing.
  22  breaking    `Style::child_relation` checks its token against the two relations it exists for, so the public raw-selector hatch underneath `children` and `divide` is closed.
  23  breaking    `[derive(Wire)]` emits the wire codec and nothing else: the JSON pair it used to emit beside the §6.1 visitor is now `[derive(Json)]`'s alone, and a type that wants both asks for both.
  24  breaking    `Style::attribute` and `Style::within` take `value: Option<str>`, so a PRESENCE condition has a spelling — `attribute("data-selected", None, ..)` is `.sX[data-selected]` — and the new `not(inner)` marker negates the condition immediately enclosing it, which is what finally spells "hovered AND not disabled".
  25  breaking    BREAKING: the stub of an `[rpc]` method that returns a source is now SYNC and answers the mirror itself — `fun get_message(self, id: str): RemoteSource<MessageBody>`, where it answered `Result<RemoteSource<MessageBody>, RpcError>` before — and it makes no call at all until something leases it.
  26  breaking    `fun sum<P: Add, Q>(a: P, b: Q): P { a + b }` compiled, and `sum(1, "two")` printed `1two` — a `str` handed back through a signature the call bound to `i32`.
  27  breaking    `fun f<T>(x: T) { if x { } }` compiled, and every instantiation reached the host's truthiness test: `f(1)` took the branch, `f(0)` did not, and `for x { }` over a truthy value looped forever.
  28  breaking    A `::` path may no longer cross a line break, and `import a::b::c as d;` is the way a long path gets shorter.
  29  breaking    `Source::on_change` is now the trait's REQUIREMENT and `sub` is derived from it — an implementation writes `get` and the LAZY attach, and the whole eager family arrives free.
  30  breaking    `for !(x is Some(let n)) { print(n); }` compiled and read a payload that is not there — the shape an `if` has refused since B195. Both of the other two conditions now install the same polarity frame, and the captures they used to bind under a negation are refused.
  31  breaking    An operator trait's method is now required at impl time: `impl P with Add { }` was accepted, `check` reported no errors, and `P { n = 1 } + P { n = 2 }` threw an uncaught `not implemented yet` naming no type, no method and no span.
  32  breaking    `Optimistic<T>` gains a second type parameter — `Optimistic<T, S: Signal<T>>` — so the cell wraps any signal, not only the canonical one.
  33  breaking    An `is` capture is in scope exactly where its test is known to have PASSED — the then-branch and the rest of the condition after an `&&` — and the spec sentence that said it was "scoped to nothing" is gone. The one program this stops is a read in the other arm of a `||`, which used to compile and answer garbage.
  34  breaking    `fun main` takes no parameters, and writing any is now a compile error that names the argument door instead: `std::process::args()`.
  35  breaking    A native operator whose LEFT operand is a generic parameter now needs a bound that PROVIDES the operator's method — `+` needs `T: Add`, `-` needs `T: Sub`, `==` needs `T: PartialEq`, `<` needs `T: PartialOrd` — and the estate's whole migration was one `<T>` → `<T: Add>`.
  36  miscompile  UNSOUND, fixed: a supertrait blanket no longer makes EVERY type satisfy its bound.
  37  miscompile  A numeric literal assigned to a narrow binding takes that binding's width natively — `mut i: u53 = 5; i -= 1;` no longer hands rustc a program it refuses.
  38  miscompile  The subscript operator checks its INDEX: `xs[1.5]`, `xs["a"]` and `xs[true]` are refused where they used to compile and answer `undefined`.
  39  miscompile  A `lazy` parameter reached through a generic BOUND is thunked at the call site, so a program that mixes a dispatched call with a direct one no longer dies with a `TypeError` it checked clean.
  40  miscompile  An unused `let` whose initializer is a block, an `if`, a `match` or a loop is no longer DROPPED with everything inside it.
  41  miscompile  Five latent native defects, each of which a slice-2 feature was the first program to REACH — one of them a wrong answer.
  42  miscompile  A module-level `mut` aggregate mutated in place is mutated in the CELL natively, not in a copy of it — and so is a mutably-captured one reached through a `&mut self` method.
  43  miscompile  A generic recursive type no longer overflows the compiler — and the language server no longer dies with it.
  44  miscompile  The JSON codec's READER enforces the type it is asked to read: a decode that should fail now fails, instead of succeeding with garbage and leaving the reader unpoisoned.
  45  miscompile  `[derive(Json)]` on an enum no longer CRASHES on a document that is not an object: `Shape::from_json("null")` threw a host `TypeError` out of the emitted tag helper, and `FromJson` promises it cannot.
  46  miscompile  An arithmetic expression of unsuffixed literals takes its type — and its OPERATOR — from the context it lands in: `rem(4 / 16)` for `fun rem(value: f64)` is float division and `0.25`, where it printed `0`.
  47  miscompile  An aggregate that holds a CALLBACK compiles natively — four classes a `std::http` server program reaches and no corpus program does.
  48  miscompile  Inside a trait DEFAULT body `self.member(..)` is the TRAIT's member, always — an implementor's same-named INHERENT member no longer hijacks it, and where that member was `external` the specialized default no longer emits a name nothing defines.
  49  miscompile  Five wrong answers the native backend was standing on, four of them found by its own whole-corpus differential and none of them visible to the default suite.
  50  miscompile  A `Shared` handle bound by a MATCH CAPTURE aliased the cell it read from.
  51  miscompile  A method call written inside an UNANNOTATED closure body is type-checked: an `Option` stored where a `List` is read no longer ships.
  52  miscompile  No styling condition can carry the slot key's own `:` any more, and no reader splits a key by hand — B311's class closes at the name, and B322's three latent halves close with it.
  53  miscompile  A pseudo-class name carrying the slot key's own `:` is refused when another condition WRAPS it, instead of compiling to a strictly weaker selector.
  54  miscompile  `Map::entries()` on a `Map<K, (A, B)>` handed back a pair the caller could not read — `let (key, slot) = entry; let (class, declaration) = slot` gave `class = "class,decl"` and `declaration = undefined`, silently — because a tuple built inside a GENERIC body was laid out by the layout the analyzer saw and read at the caller by the instantiated one.
  55  miscompile  Overriding a trait DEFAULT in one impl silently changed how a DIFFERENT type's inherited copy of that default read its ambient context — `self.cache.map(..)` on a `SignalCell` field ran `register_with_owner` against an `undefined` owner.
  56  miscompile  `ops.describe(serializer)` on a `List<Delta<K, T>>` compiled and then failed at EMISSION with the never-silent `internal: a call resolved to `Wire`'s requirement `describe`, which has no body` — and the same shape one instantiation later was a silent JSON MISCOMPILE.
  57  miscompile  `b = a` did not copy: an assignment whose right-hand side was a plain local aliased its source, so `mut b = [0]; b = a; a.push(4)` grew `b` too, and `r = q; q.x = 10` left `r.x` at 10.
  58  miscompile  `n = 5` beside a guard clause whose capture is also called `n` compiled CLEAN and emitted an assignment to a top-level function.
  59  miscompile  `fun check(): Result<bool, str> { let v = probe()? > 0; Ok(v) }` compiled clean and handed back `Ok(Ok(true))`, and an `Err` from `probe` came back as `Ok(Err("boom"))` — never propagated.
  60  miscompile  `if a is Some(let x) && x.on_drag_end is Some(let y) { … }` compiled clean and then threw on a `None`: `TypeError: cannot read properties of undefined`.
  61  miscompile  `let present = x is Some(let n); print(n);` compiled, and read the payload slot of a value that may have no payload — `undefined` for a `None`, `NaN` where the program's own type said `i32`.
  62  miscompile  `fun main() { if flag { ret; } … }` compiled clean and then died at load: `SyntaxError: Illegal return statement`, the whole module refused before a line of it ran.
  63  miscompile  An `is` capture reached through a call argument is unbound in both branches.
  64  miscompile  A generic parameter is rigid inside its own body.
  65  miscompile  `for x in tuple` compiled, ran, and bound `x` to `any` — so `for x in (1, "two", true) { print(x + 1) }` printed `2`, then `two1`, then `2`, and a `&mut` walk over a tuple silently discarded every write. The loop is now refused.
  66  miscompile  A PARTIAL generic-argument list is now what it reads as — a PREFIX. `tag<Dog>(a, b)` on a two-parameter `tag` binds `T` and lets the arguments infer `U`, where it used to leave `U` abstract all the way into emission and stop the compiler with an internal error asking the author to report their own program; and an OVER-long list, which used to be accepted with the extra argument silently dropped, is refused.
  67  miscompile  A trait default's `self <op> self` now dispatches to the type the default is specialized for: `trait Doubler with Add { fun twice(self): Self { self + self } }` made `Money { cents = 21 }.twice()` print `2`.
  68  miscompile  `-` and `!` now hold their operand to an admitted set: `let flipped: bool = -true` compiled and held `-1` — truthy, and equal to neither `true` nor `false` — while `-Point { x = 1, y = 2 }` was the host's `NaN` and reading a field of it printed `undefined`.
  69  miscompile  A negated `is` test now scopes its capture to the branch that actually matched: `if !(x is Some(let n)) { … }` no longer binds `n` in its then-branch — the branch reached only when the pattern FAILED — and does bind it in the `else`, which is reached only when it matched. The program this stops compiled, ran, and printed `undefined`.
  70  miscompile  Every native operator now checks its LEFT operand, not just `+`: `true - 3` typed `bool`, `"12" - "3"` typed `str` and `Level::High ^ Level::Low` typed `Level` were each a number wearing a type it was not, and all three compiled clean in v0.40.0.
  71  miscompile  A generic type named with no type argument — `fun read(h: Holder)` for `struct Holder<S>` — is refused instead of having its parameter silently erased. The erasure was a live miscompile: it let a `Holder<str>` satisfy a `Holder` parameter and print `seven1` through a declared `i32` return, on a clean compile.
  72  miscompile  An operator that dispatches to your impl now checks the right operand against the `B` that impl declares — until this landed it checked nothing at all, and passed a foreign struct straight into a body typed for another one.
  73  miscompile  `T::default()` under a bound now types as `T`, not as the bound — which makes `List::sum` over a struct element actually add: it printed `4` for a wallet holding 40 and 2.
  74  miscompile  `&&` and `||` refuse a generic RIGHT operand: `fun both<T>(flag: bool, value: T): bool { flag && value }` compiled, and `both(true, Point { x = 1, y = 2 })` printed `[ 1, 2 ]` — the struct itself, typed `bool`.
  75  fix         `export impl` no longer dodges a `[service]`'s method refusals: `mut self`, `async` beside `&mut self`, a `__` parameter and a generated member's name are refused through it exactly as through a plain `impl`.
  76  fix         The binary codec's reader gets the JSON reader's audit: an `Option` marker that is neither `0` nor `1`, a `bool` byte that is neither, and a frame with bytes left over are decode failures.
  77  fix         An rpc call carrying MORE arguments than its method takes is a decode failure, not a silent success — and a no-argument method finally has a decode gate at all.
  78  fix         `i32::from_json("1.5")` is an error, not `Ok(1.5)`: the scalar decoders take the integer lanes' value check the codec reader already had.
  79  fix         A `[service]` whose methods live in an `export impl` generates its routes — and a `[service]` that declares nothing at all is refused instead of shipping an empty API.
  80  fix         An unreachable host no longer takes the process down: `HttpTransport` answers `Err(RpcError::Transport(..))`, which is what `Transport`'s own contract always said.
  81  fix         A trait whose supertrait is the bound of its own blanket impl no longer crashes the compiler, and is accepted.
  82  fix         A blanket over a nested bound, called from inside a generic body, dispatches: `self.map(f).flatten()` in `fun switch<U, I: Source<U>>` no longer stops the compiler.
  83  fix         `==` on a bounded parameter inside a nested `match` arm within a closure is pinned — the gap filed as B381 was B379.
  84  fix         A generic call whose argument is built from a closure's parameter binds its type parameter: `n.map(|m| Signal::new(m * 2))` infers.
  85  fix         Indexing the value a `Shared<List<K>>` hands back resolves inside a generic body.
  86  fix         A generic parameter reachable only through a BLANKET impl's trait argument now binds.
  87  fix         A blanket impl bounded on a SUBTRAIT now outranks one bounded on its supertrait — which is how an optional capability is dispatched.
  88  fix         A native binary that FAILS answers node's shape: the message alone on stderr and exit code 1, not Rust's panic banner and 101 — and printing a host handle is now refused where it is written.
  89  fix         The drop planner's enrolment gate stops comparing a struct literal's own expression id against the set of struct DECLARATIONS.
  90  fix         An `external fun` with neither an `[extern(..)]` binding nor a compiler lowering is refused where it is declared, instead of emitting a call to a name nothing defines.
  91  fix         A function with a `lazy` parameter is not a closure VALUE: handed to a closure slot, it reached a body that forced a plain number.
  92  fix         A variant constructor's WRITTEN type arguments settle the expression's type, so one wrong payload is one refusal — and a bound on the result can be checked at all.
  93  fix         A `for` header binds its element with `let`'s binder, so a tuple destructures right there: `for (index, item) in list.iter().enumerate() { .. }`.
  94  fix         A call to a generic function with NO declared return type is typed under the call's own generic arguments: `hold("x")` for `fun hold<C: Show>(body: C) { Holder { body } }` is `Holder<str>`, not the callee's abstract `Holder<C>`.
  95  fix         `vilan run` of a lone package or a bare file no longer creates a `dist/` holding nothing but the compiler's memory.
  96  fix         A std whose `[library]` declares `root = "."` resolves its own toolchain root, instead of the directory above it.
  97  fix         Completion resolves a type NAME the way the analyzer does — through the scope chain at the cursor — where it used to take the first item in the program that happened to spell it.
  98  fix         The playground's diagnostics know what the standard library is: "is this source std" and "are this source's definition-site diagnostics frozen" are two sets now, and everything keyed on residence answers correctly where std arrives through the document overlay.
  99  fix         A written type argument on a VARIANT constructor path is checked: `Option<i32>::Some("y")` is refused instead of quietly becoming an `Option<str>`.
  100  fix         A payload-less variant of a generic enum carries a real HOLE: `None` is `Option<unknown>`, not the bare `Option`, and B352's erased-arguments special case retires with it.
  101  fix         A `[derive(..)]`-SYNTHESIZED body resolves its members under the file the ATTRIBUTE was written in, not under the sentinel source — so B318 S4's export gate stops hiding every non-exported `impl` from every derived visitor.
  102  fix         A dispose reached from inside a settle now scrubs the draining turn's queue — the scrub had been dead code for every teardown a form performs.
  103  fix         A disposed observer no longer fires — and nested `swap`s on one source stop building a subtree into a region that is already closed.
  104  fix         `--backend rust` works from an INSTALLED toolchain: the runtime crate the emitted program links is embedded in the binary and materialized under `~/.vilan/rt-cache/`, beside the standard library's own cache.
  105  fix         A closure that captures a view and is handed to a callee that KEEPS it is now refused, which closes the escape rule's last hole before a non-GC backend.
  106  fix         A struct initializer's head resolves through the scope chain and, when it is qualified, through its namespace — so `Dot { ▎` offers THIS file's `Dot` and `shapes::Dot { ▎` offers `shapes`'s.
  107  fix         A type import whose only use is an `(impl S)` selector's SUBJECT is no longer faded — and no longer deleted by Organize Imports, which was breaking a green build.
  108  fix         B318 §4's "Export `S`" reads the analyzer's own record of which type the warning is about, so it answers on the warnings that named a type the file never writes.
  109  fix         The two readers that take a sibling file's text consult the open buffer before the filesystem, so an edited-but-unsaved sibling is edited where it actually is.
  110  fix         Organize Imports takes the paragraph separator with a run it deletes whole, instead of leaving two blank lines behind.
  111  fix         The auto-import popup consults the visibility bit too: a curated module's private machinery is no longer an add-import target.
  112  fix         S4's monomorphization refusal names the module whose import decides it, instead of the placeholder `that module`, and lands on the attribute that generated the body.
  113  fix         `export const fun` and `export const let` are the items they read as, and `vilan fmt` no longer returns a file containing one unchanged.
  114  fix         std's two back edges are weak: `observe`'s captured value cell and `Subscription`'s alias of the subscriber list.
  115  fix         A module-level binding's initializer is emitted under the file that DECLARES it, closing §3.5's last unscoped seam.
  116  fix         A call's own generic is no longer bound to ANOTHER call's abstract type parameter, so a block hole whose tail is `context.run(v, || { … })` compiles.
  117  fix         `each_by`'s `T` reaches an UNANNOTATED render closure: the bidirectional fill resolves the WHOLE expected parameter type through the call's substitution, not only a bare generic.
  118  fix         A `std` copied away from its toolchain no longer mis-analyzes itself: one root answers for both `std` and `macro_std`, and a root carrying only half a toolchain is refused by a message naming both paths.
  119  fix         Rule 3's view-capture scan walks a call's SUBJECT, not only its arguments.
  120  fix         A `lazy` parameter instantiated at a resource is refused, at the argument when the caller can see it and at the instantiation when only the solver can.
  121  fix         Import-path completion consults the visibility bit: a curated module offers its exports and keeps its machinery to itself.
  122  fix         Organize Imports stops rewriting a prelude-redundant import into a statement that breaks the build: rule (2) counts only what a module import ALONE carries, and a rescue never binds a name the file has taken.
  123  fix         A two-entry package where one entry uses element or `css` syntax builds again: the base cache's key carries the modules an entry's own SYNTAX seeds, not only the ones a `std::` path names.
  124  fix         A call written with NO RECEIVER means the FREE function, even inside the `impl` block that declares a method of the same name.
  125  fix         A `context` clause is recorded on a CALL's own type and on a closure literal's, from the callee's declared return and from the landing — the two faces of one gap.
  126  fix         The stylesheet holds the rules the program KEPT: `Style::rule` stages its rule against its class and G23's scheduled finaliser writes the ones whose class the build still names, so every intermediate style a condition combinator wrapped and dropped stops shipping.
  127  fix         `transformer::expr_type_id` resolves a PARAMETER spelled `Expr::Local`, which is how every reference to a parameter is spelled.
  128  fix         The `[derive(Hashable)]` field boundary sees a HAND-WRITTEN `impl .. with Hashable`, so `[derive(Hashable)] struct Key { inner: Custom }` compiles when `Custom` has one.
  129  fix         `let held = injected;` — an UNANNOTATED binding of an injected closure — is a forward the threading follows, not an escape the value-flow restriction refuses.
  130  fix         A `context` clause written as a generic ARGUMENT on an annotated `let` now reaches the struct literal it annotates, so a body stored there defers its context instead of capturing one at construction.
  131  fix         Scope completion on a BLANK LINE offered only globals — the one position where a user actually asks for it.
  132  fix         Organize Imports broke a green build in one direction and could never clean up in the other, and both were the same question asked of the wrong set: what does a MODULE import bring, on its own?
  133  fix         `pkg` and `Self` were indexed as references to entities they are not, so a rename of a three-letter package or a four-letter trait rewrote the keyword and broke the build.
  134  fix         An HMR swap no longer throws one websocket error per remote source, and no longer leaves the dead bundle's connection redialing beside the new one's: the duplex's teardown closes it for good instead of only closing its socket.
  135  fix         `reconcile_type` gives the same answer whichever way round it is asked: a trait-typed position met by a value that implements the trait AGREES, at a call that reconciles parameter-first exactly as everywhere else that reconciles value-first.
  136  fix         A type's STATICS bind to a bare name now — `import std::style::Length::rem;`, `use Length::rem;`, `export import std::style::Length::rem as rem;` — which is what the spec has promised since it was written and what an enum's variants have always done.
  137  fix         `shared.read().push(x)` grew the CELL: the receiver of a `&mut self` method is a copy position now when a temporary read stands in it, which is what five doc sites across four pages have always promised.
  138  fix         A string argument inside an interpolation hole can be written with its quotes escaped now — `i"{get(\"k\")}"` — and lexes to the same token the plain spelling does.
  139  fix         A disposed boundary now REMOVES what it placed, so a portal container is empty when the component that filled it goes: `when`'s body, `swap`'s subtree, `bind_each`'s rows and every reactive `{signal}` child leave the document with their owner, marker node included.
  140  fix         An `[rpc]` method named for one of the generator's own free functions did not build: `[rpc] fun call(self, what: str): str` stopped with "`call` expects 2 arguments, but got 4", spanned on the STRUCT.
  141  fix         A `--watch` leg that bundles anything can be `Fresh` again: the hash a tracked build input is RECORDED with and the hash the watch loop RE-VERIFIES it with are now one rule.
  142  fix         A `Source`-generic runtime function is callable from generic code again: a parameter reachable only through ANOTHER parameter's parameterized bound is now solved from that bound's application, so `expose_dynamic<T: Wire, S: Source<T>>(source: S)` can be called with an `S` that is itself a parameter.
  143  fix         A NON-EMPTY list literal whose element type has not landed is an open `List<?>` now, not an ERASED `List` with no arguments at all — so `[x][0]` over a closure parameter still waiting to be filled stops being "cannot index this List: its element type is never determined".
  144  fix         Two sibling arguments of one call — or two sibling fields of one struct literal — no longer share an instantiation: a call whose own generics bind nothing stopped leaking the NEXT argument's bindings, which is what turned two `KeyedCell::new([])` fields into `internal: a call resolved to Keyed's requirement key, which has no body … please report this program`.
  145  fix         An unannotated closure parameter is no longer frozen at the CALLEE's own unbound generic, so `View::link_to`'s `|event|` needs no annotation — and whether a call needed one no longer depends on what its receiver happened to be.
  146  fix         A method reached through a blanket whose subject is BOUNDED now binds the bound's own arguments from the receiver, so `impl type S: Source<type I>`'s members type at the call site without an annotation and a NESTED bound resolves to a real body instead of an internal error.
  147  fix         An `[rpc]` method with a parameter named `request` did not build: the generated route's closure took `request` too, so the author's parameter shadowed it and every `arg`/`decode_failed` after the first was handed a decoded value where the request handle belonged.
  148  fix         A `Debounce` whose nursery was cancelled is still a `Debounce`: the driving loop's bookkeeping is now cleared on every unwind, not only on the loop's own end.
  149  fix         A remote lease taken outside every turn now rebuilds inside one for free, like a lease born inside it: the mirror's `Unsubscribe` is deferred against the turn ambient at the RELEASE rather than the one its release hook captured at creation.
  150  fix         A throw out of an observer no longer turns the reactive graph off for the rest of the session: the drain restores its scheduling state under a `finally` and lets the error keep unwinding, and a disposal group now finishes past a throwing cleanup.
  151  fix         An `Owner` now has a DISPOSED state, so a `take` or a `defer` that lands after its owner is gone releases on the spot instead of parking a cleanup on a list nothing runs again.
  152  fix         `Wire` is a trait again: a hand-written `impl … with Wire` now makes its type sendable everywhere the compiler asks, instead of being invisible to a syntactic allowlist of six scalar spellings and the `[derive(Wire)]` names.
  153  fix         A reactive child now keeps its POSITION: `when`'s body, `swap`'s subtree, `bind_each`'s rows and a `{signal}` element or run all land where they are written instead of at the end of the parent, and stay there across every change (A71).
  154  fix         `std::router::link` no longer builds a drag-armed anchor: in Chrome a link drag that starts while a quick click's in-app navigation is still settling WEDGES THE TAB, and `draggable="false"` removes the trigger at no cost.
  155  fix         `self.items.sort_by(|a, b| a.compare(b))[0]` inside `impl Holder<type T: Ord>` reported "cannot index this List: its element type is never determined" over complete code: `freshen_list_element_slots`' EXTERNAL-function path was unguarded.
  156  fix         A bound's trait ARGUMENTS were dropped whenever an impl reached the required trait through a SUPERTRAIT, so `Source<anything>` satisfied `: Source<str>` — `SignalCell<bool>` was an element-attribute value, and `set_attribute(name, true)` shipped, correct only by JS coercion.
  157  fix         `impl CatBox with Holder<Cat>` compiled with `Cat` implementing no `Label`, and `impl CatBox with Holder<Cat, i32>` compiled too: an impl's `with` clause was arity-checked and bound-checked by NOTHING.
  158  fix         `take(|x| Remote { seed = [x] })` satisfied a `|i32| Remote<List<str>>` parameter, and `handle.remote_signal([], |x| x.ok().flatten())` over a `List<i53>` handle accepted a `SignalCell<List<Message>>` annotation — a closure's return through a USER generic struct was neither bound nor checked.
  159  fix         `if inner is Some(let payload)` over an UNANNOTATED closure parameter typed the payload as `Option`'s own declared `T` — a free, unbounded generic — so a legitimate read of it was refused and an illegitimate one compiled.
  160  fix         `[rpc] fun login(&mut self, …)` did not build: the generated dispatcher declared `fun dispatcher(self)`, an IMMUTABLE binding, so no route could call a method that wanted a mutable receiver — and the build stopped inside generated code, spanned on the STRUCT, recommending `mut self`, the receiver that silently loses the write.
  161  fix         `Shared::read()` handed out the cell's STORAGE, so `let b = h.read(); h.write().push(9)` made `b` read 6 and `mut c = h.read(); c.push(10)` grew the CELL — a read is a place now, and storing one copies.
  162  fix         `View::show(condition)` was a silent NO-OP on every flex container — on every element an app gave a `display` at all.
  163  fix         A generic bound's ARGUMENTS were dropped when an impl was matched to a receiver, so every blanket over a parameterized trait applied to every instantiation of it.
  164  fix         The base cache's 512 MiB byte budget was denominated ~24× low: it bounded a number that is a fraction of what a retained world weighs, so "512 MiB retained" was a bound on the order of 12 GB resident.
  165  fix         A `css { … }` block and an element are HYGIENIC: the block means `std::style::style` and `<tag />` means `std::ui::view`, whatever those names mean at the site — and neither needs an import.
  166  fix         `impl Held<type T> { fun first(self): T { self.list.get()[0] } }` over `struct Held<T> { list: Signal<List<T>> }` reported "cannot index this List: its element type is never determined" - and the element WAS determined; the call threw it away.
  167  fix         B251's written-argument bound check was STRUCT-only, so `Held<i32, SignalCell<List<str>>>`'s ENUM twin — the same parameters, the same bound, the same annotation carrying a `str` list out through a declared `i32` one — was still accepted.
  168  fix         The intrinsic table bound `remove`/`insert`/`pop`/`push` (and `new`, `get`, `sort_by`, and every other intrinsic name) by NAME on any impl of the subject, external or not — so a vilan-bodied `List::remove` was silently replaced by the intrinsic and the body never ran.
  169  fix         a49-async narrowed the trait-default half of `GenericDispatch::OnType`; the KNOWN-receiver half kept the widest answer, so an inherited default called on a concrete value was still colored async by any same-named member anywhere in the program.
  170  fix         `impl Cell<type T> with Signal<T>` written as ONE block typed `x.map(|v| v * 2)`'s `v` as the trait's abstract `T`, and steered the author to "add it where `T` is declared on `trait Source`" — a std file.
  171  fix         A refused RETURN annotation cascaded — `'Greet' is a trait, not a type` and then `cannot call method 'who' on unknown` — where the same trait refused at a closure PARAMETER stood its uses down.
  172  fix         A type-position `as` alias corrupted a rename of the type it aliases: `import pkg::helper::Point as Spot;` made `Spot { x = 1 }` an "occurrence" of `Point` five bytes wide, and renaming `Point` rewrote it to `zzz{ x = 1 }`.
  173  fix         The dead-code paint grayed a function called only from a module-level `const` region — three of the four spellings, including the `const { bundle(..); style::preflight(); };` block kolt writes at the top of `client.vl`.
  174  fix         `View::autofocus` focused in a microtask — before the frame's rendering step — so an input inside a subtree that becomes focusable only during rendering was never focused at all.
  175  fix         File mode's last two disagreements, and they were the same mistake twice: a fact about the PACKAGE was being read on one leg and not the other.
  176  fix         A hole whose text is not an expression was abandoned MID-BODY, so `i"{if c { 1 } else { 2 }}"` produced six diagnostics — an unclosed `(` from the hole's own generated paren, two `expected a token`s, `found 'else' expected an expression`, two missing terminators, and the ban on a string spanning lines, about a line break nobody wrote.
  177  fix         An in-place `remove` through `SignalCell::update` beside a `bind_each` crashed the reconciler (`index out of bounds: the length is 5 but the index is 5`), and an in-place element write left the row silently STALE.
  178  fix         Renaming through an import alias COLLAPSED it — `greet as hello` renamed to `greeting as greeting` — and an alias whose name was a different LENGTH from the thing it renames was invisible to the editor outright: `import pkg::helper::greet as hi;` gave `hi()` no rows at all, so find-references answered nothing and rename answered "there is no symbol to rename here".
  179  fix         `fun bump<P: Add>(a: P): P { a + 1 }` compiled, and it is unsound: a bare `P: Add` means `Add<B = Self>`, so `P`'s `add` accepts a `P` and only a `P`.
  180  fix         `impl Cup with Mixed` was told `mix`'s first parameter had to be a `Cup` where `trait Mixed with Blender<i32, str>` says `i32`, and `Meters < Feet` was refused as though `PartialOrd<Feet>`'s `lt` accepted a `Meters`.
  181  fix         `struct Held<T, S: Signal<List<T>>> { list: S }` left `T` at `any`, and `Held<i32, SignalCell<List<str>>>` was ACCEPTED anywhere it was written.
  182  fix         Bare `connect_socket(url)` offered no subprotocol, so the one call a first program writes was the one that could not be told why it was refused.
  183  fix         Every `[service]` program stopped compiling the moment A49's `Source` inversion met the RPC capability table: three copies of "field `start` of `Capability` receives an async closure, but its type awaits nothing", reported against std's own `rpc.vl` for a program that awaits nothing.
  184  fix         Three holes B239 left on the open-file path.
  185  fix         One import cycle back into the entry printed four errors: B226's refusal at the import, then "cannot find 'Tab' in the imported path", then "cannot find type 'Tab'" twice — three of them naming a type the reader can see declared in the file the refusal already pointed at.
  186  fix         A `= Self`-defaulted trait parameter was read as a BOUND when its member was reached through a sub-trait's parameterized clause: `trait Mixer<A = Self, B = Self>` under `trait Mixed with Mixer<i32, str>` refused with `'i32' does not implement trait 'Mixer', required by a generic bound of this call`, and the same for `str`.
  187  fix         `disabled` is usable as a binding name, and there is now a gate that says so.
  188  fix         Renaming through a struct-init field shorthand refused; it EXPANDS now.
  189  fix         Renaming a derive-generated member refused as generated for ONE of the types that derived it and silently rewrote the call site alone for every other — a rename that leaves the generated declaration behind and breaks the program.
  190  fix         Hover at the end of a bare use still answered the enclosing function after E133: `let _ = count|` showed `fun main()`.
  191  fix         A method call the solver never selected produced no diagnostic of its own — and on a program with nothing else wrong, no diagnostic at all: `for item in empty { item.len(); }` compiled to a call that resolves to nothing, silently.
  192  fix         One argument short on a call turned into nine errors: `channel_component(stub_user)` reported its arity mistake, then three "`channel_component` reads context `app_context`, so it can't be used as a value" at the same call and five "context `…` is read here, but this code can be reached without an enclosing `run`" — four of them inside std's `reactive.vl`, in code the program never touched.
  193  fix         A client refused at the handshake burned the full retry budget — ten redials over ~24 s — and then reported `Transport("could not reach …")` about a server that had answered immediately. It now gets `RpcError::Unauthorized` on the first attempt.
  194  fix         A client holding both a whole-collection lease and a per-key lease on one keyed channel could make its own mirror report a protocol fault.
  195  fix         Opening `views.vl` in the editor turned a legitimate module cycle into seven errors — "`pkg::views` is this program's entry file … it cannot be imported" on the sibling's import, then every name that sibling took from it — over a package `vilan check .` compiled clean.
  196  fix         One mistyped `run` argument deleted the `run` from the program: adding a field to a context struct and not to its initializer reported three "context `app_ctx` is read here, but this code can be reached without an enclosing `run`" — one per read — and the real error, the missing field, LAST.
  197  fix         A struct-init field shorthand `A { x }` is one identifier naming two things — the field and the local it reads — and the index kept only one of them, so `x` faded as unused, find-references missed the use, and renaming the field silently broke the site.
  198  fix         The reference index was end-exclusive, so a caret at `name|` — where it sits the moment you finish typing a word — named nothing: rename showed "the element can't be renamed", find-references came back empty, and hover went blank.
  199  fix         `linkedEditingRange` answered in the ANALYZED snapshot's coordinates, and the client mirrors that answer's keystrokes — so typing under element syntax during the debounce typed into whatever unrelated live code had moved into the tag's old offsets.
  200  fix         `impl Pair<type T> { fun make(x: T) { Pair { b = "hello", a = x } } }` compiled clean and returned a `Pair<str>` into a `let p: Pair<i32>` — no diagnostic anywhere.
  201  fix         `compare_type_rigid` is `reconcile_type`'s read-only twin, `trait-objects.md` §1.4 says the two agree exactly, and since B211 they did not.
  202  fix         A type a derive template names but nobody declared reported `cannot find type 'NoSuchType'` with no provenance, and drew that label over whatever the deriving file happened to hold at the template's offsets.
  203  fix         `trait Bumper with Add<i32> { fun bumped(self): Self { self.add(1) } }` was refused with `Expected Bumper, but got Add instead.`
  204  fix         `<div .styled() />` looked undiagnosed: `styled` takes an argument, the arity WAS checked, and the refusal rendered against an unrelated line of std's own `browser/ui.vl` with the author's file never named — so the only honest reading of the output was that nothing had been reported.
  205  fix         One `print(v)` at the top of a closure body typed `v` as `any` and silenced every check on it for the rest of the body: `apply(|v| { print(v); v.no_such_method(); })` was refused "cannot call method 'no_such_method' on any", naming a type the program never wrote, while the same closure without the `print` said "i32 has no method".
  206  fix         `import pkg::client::SidebarTab;` at the top of the ENTRY `client.vl`, beside `[derive(PartialEq)] enum SidebarTab { … }`, made the derive vanish: "type 'SidebarTab' does not implement the `PartialEq` operator" over a type that plainly derives it.
  207  fix         `fun g(): i32 { ret 1; print("dead"); }` was refused — "this body ends without producing a value" — and so was every body whose exit is not its LAST statement.
  208  fix         `if !(maybe is Some(let n)) { panic("missing"); } print(n)` refused `n`, while the same guard ending in `ret` bound it — one rule with two answers, decided by which ending the WALK could recognise.
  209  fix         `vilan fmt --check src src/pkg` printed the files under `src/pkg` twice.
  210  fix         A member request answered inside the debounce resolved its receiver in ANALYZED coordinates, so a receiver typed since the last landing answered the OLD expression's type: `<div .styled(const style::style().` offered `element, text, class, styled, style_var, attr, on, child, bind_text, …` — `View`'s method set — where the settled request offers Style's 89.
  211  fix         Member completion went silent on a call whose declared return is a bare type parameter: `c.get().` on a `SignalCell<List<str>>` offered nothing, while `c.`, `let v = c.get(); v.` and even `c.get().len().` all answered.
  212  fix         A `::` path in code completed only its LAST segment, so `style::FlexDirection::` and `geo::Shape::` offered nothing while the import spelling of the same path descended fine.
  213  fix         A reactive channel minted at runtime leaked its source for the life of the connection, and went silently dead — reporting `Ready` — after a reconnect.
  214  fix         Two spellings of one unsaved file agree again in the editor's containment tests.
  215  fix         `semanticTokens/range` answered in the ANALYZED snapshot's coordinates while `semanticTokens/full` answered the live buffer, so a viewport request after an unlanded edit above the window painted every token in it one line off until the next analysis landed — on the request an editor sends most.
  216  fix         A `--watch` round decides each leg's freshness at that leg's own turn, and compiles a leg that writes another leg's input first.
  217  fix         The language server's containment test resolves both sides the same way, so an unsaved buffer under a symlinked project root keeps its project.
  218  fix         One file reached under two names is collected once.
  219  fix         A self-call bound by a `let` and read in an unannotated recursive body's return position resolves.
  220  fix         A trait-conformance refusal now advises a signature the author can actually write. `impl P with PartialEq { }` said ``declare `fun eq(self, b: PartialEq): bool` `` — and `PartialEq` is a trait, so that line names a type no value can have; the advice was unfollowable at the one moment the reader most needs to follow it.
  221  fix         `self.add(self)` written out in a trait default over a supertrait resolves. `trait Doubler with Add { fun twice(self): Self { self.add(self) } }` was two errors on a program with no mistake in it — `Expected Doubler, but got Add instead.` at the call and `Expected Add, but got Doubler instead.` at the argument — while the OPERATOR spelling of the same thing, `self + self`, has dispatched since B193.
  222  fix         A refused `[expose]` now generates NOTHING — no contract entry, no client mirror, no `session.expose` call — so `[expose] tasks: i32` is ONE diagnostic instead of five. B189 took the pile from six to four; the residue was two `cannot infer 'T' … ': Wire'` and two `cannot find type '_'`, and `_` is not a type: it is the `service` expansion's own placeholder, handed to an author who never wrote it.
  223  fix         A `[derive(..)]` on a type declared inside an inline `mod` now generates its impl INTO that `mod`, where the type it names is in scope. `mod shapes { [derive(PartialEq)] enum Kind { .. } }` was four diagnostics on a program with no mistake in it — `cannot find type 'Kind'; import it first` twice, plus a follow-on per variant raised inside a body the author never wrote.
  224  fix         One module declares a name once, and a bound names a trait.
  225  fix         A tuple receiver resolved NO method at any arity — not an inherent impl, not a trait impl, not `impl (i32, i32) with Add`'s own `add` — so `(1, 2) + (3, 4)` ran and answered `4` / `6` while `(1, 2).add((3, 4))` was "cannot call method 'add' on (i32, i32)".
  226  fix         A build hook's stamp records the inputs it consumed, not the inputs on disk after it ran.
  227  fix         Two rpc frames that TCP coalesced into one read were serialized by the receive loop, so a handler awaiting anything the SECOND frame would have delivered waited for a message the loop was holding.
  228  feature     `each` over a `ListCell` builds only the rows a write names: one push into 1,000 rows builds ONE row and reads ONE key, where a plain `SignalCell<List<T>>` re-reads every key and diffs the run.
  229  feature     The native backend monomorphises on ASYNCNESS as well as on types: a callee handed an async closure at one call site and a synchronous one at another comes out twice, and `adapt.vl` is byte-identical on both backends.
  230  feature     A view inside an enum payload compiles natively: `fun get_mut(&mut self): Option<&mut i32>` emits `Option<&mut i32>`, and `option-view.vl` is byte-identical on both backends.
  231  feature     The two dynamic combinators land on `Source`: `switch` follows whichever source its selector answers, and `and_then` does it for a source of options.
  232  feature     The SHOW takes the focus: `FocusScope::focus_initial`, the chained `View::focus_scope`, and `View::autofocus` now writing the attribute.
  233  feature     A focus SCOPE in std: `Element::tabbable` walks the tab order, and `focus_scope(root, Contain | Wrap)` traps Tab inside an overlay without `inert`.
  234  feature     `std::dom` can read what a focus scope has to know: `tab_index`, `parent`, `has_attribute`, `computed_style`, `active_element`, `Event::related_target` — and the test DOM answers all six.
  235  feature     `[service(AuthClient, http)]` — an opt-in marker that a service is an HTTP API, refusing at the declaring member what the connectionless leg cannot carry.
  236  feature     A service can gate its connectionless `POST {mount}rpc` leg per request: `Service::authorize_request(|request| ..)`, a second hook beside `authorize`, whose `Session` rides on the request.
  237  feature     The `POST {mount}rpc` leg answers the STATUS its envelope earns — 400 for a decode failure, 404 for an unknown method, 405 for a non-POST, 501 as an envelope — and requires a content type, which is the whole CSRF posture.
  238  feature     A `[service]` can be reached as a plain HTTP API: `FooClient::over_http("/auth/", json_codec())` speaks the connectionless `POST {mount}rpc` leg with no socket, no handshake and no mirrors.
  239  feature     A list whose writes are its CHANGES: `ListCell<T>` and `map_each`, so a derivation over a list runs its function once for the element that arrived instead of once for every element.
  240  feature     A `vilan` server with the shape of kolt's runs as a native binary and answers a login over a real socket — SQLite store, hashed password, JSON route and all.
  241  feature     `BigInt` compiles natively as an `i128` — a documented LIMIT, enforced at both ends — and the corpus's two `BigInt` programs print the same bytes on both backends.
  242  feature     Five more of std's host modules answer natively — `Math`, the number labels, `charCodeAt`, `process.env`, the plain `node:fs/promises` set and SHA-256 — and a closure that awaits in a `void` position no longer stops a build.
  243  feature     `std::json` works on the native backend: a `[derive(Json)]` struct serializes and decodes in a compiled binary, and the two JSON corpus programs are now byte-identical on both backends.
  244  feature     `dyn Trait` — a value whose concrete type is erased, so one struct field, one list and one parameter can hold several types that share a trait.
  245  feature     A call through a `dyn` threads the hidden context arguments its implementations read.
  246  feature     `dyn Trait` compiles natively, as a fat pointer, and prints byte-for-byte what the JS build prints.
  247  feature     `dyn Trait` reaches what a bound reaches, copies like a value, and keeps its declaration's asyncness — and its table is shaped for the call site that sees several types.
  248  feature     A native `vilan` server answers a request: a `std::http` program compiled with `--backend rust` binds a port, serves `GET /` over a real socket, and the JS twin answers the same status line, the same header and the same body.
  249  feature     The native backend reaches `std::http`: twenty-three of the twenty-five `node:http` bindings are answered by `vilan_rt::http`, and a destructuring `let` compiles — which flipped `destructuring.vl` to byte-identical.
  250  feature     `vilan-rt` has an HTTP/1.1 server, written by hand over `std::net`: no dependencies, no `unsafe`, and it runs on the executor J6 built.
  251  feature     `std::router::navigate_replace` — navigation that rewrites the current history entry instead of pushing one.
  252  feature     `when_some` — the conditional form that BINDS the value, and keeps its row when the value changes.
  253  feature     Element syntax admits an `Option` attribute: `<div data-dragging(maybe)>` takes an `Option<str>` or a `Source<Option<str>>`, and a `None` means the attribute is not there.
  254  feature     `scoped_effect` — an effect whose every run gets its own `Owner` — and the free `on_cleanup` beside it.
  255  feature     A mirror's `map` is a DERIVATION too, so an effect on the same mirror reads the mapped cell settled, not one wave stale.
  256  feature     Effect order within one settle is now a CONTRACT: derivations run to a fixpoint, then effects run in ascending subscriber id — so a parent form's effect runs before anything its render created.
  257  feature     `reconcile` keeps a position index, so `each` stops paying N(N+1)/2 key comparisons per change: 355.1 M Ir → 29.1 M at 1,000 rows, and quadratic → linear.
  258  feature     A BACKED enum compiles natively — it IS its backing value, on both backends — and with `List::sort_by` and B109's read-through that is five more corpus programs byte-identical.
  259  feature     An OVERLOADED operator compiles natively: `a == b` on a type with its own `PartialEq` is a call to that impl's member, and five more corpus programs print byte-identically.
  260  feature     `lazy` parameters compile natively, and with them the whole `Option`/`Result` combinator surface: thirteen more corpus programs print byte-identically.
  261  feature     The native path reaches its exit: `board.vl` — `native-apps.md`'s own probe, the program the whole paper was written around — prints byte-identically as a native binary.
  262  feature     Structured concurrency compiles natively: `nursery.vl` — a helper's spawn, a grandchild spawned by a running child, and a join that waits for a child list which grew while it was draining — prints byte-identically as a native binary.
  263  feature     `--backend rust` compiles `async` programs: an `async fun` is a Rust `async fn`, an `await` is `.await`, an `async` spawn is a task on the new executor, and `await-postfix.vl` — a real corpus program — now prints byte-identically as a native binary.
  264  feature     The native backend has an event loop: `vilan-rt` gains a hand-written single-threaded executor, and it is the JS turn model rather than a second one.
  265  feature     The style vocabulary reaches a chain written outside a css hole: the four keyword-property types join `std::style::prelude`, and one file-level import is the idiom.
  266  feature     `filter_map` — `map` and `filter` in one pass — joins the adapters, with an eager `List` twin under the same name.
  267  feature     `Default` reaches the containers: `List`, `Map` and `Set` have an empty one, so a `T: Default` bound can finally be met by a container.
  268  feature     `Result::expect_err`'s message joins the four that were already `lazy`, and the asymmetry it leaves behind is gone.
  269  feature     `Result<T, E>` crosses JSON directly, in the one spelling the codecs already use.
  270  feature     `Storage::keys()` and `Debounce::flush()` — std-30's two unshipped halves.
  271  feature     `bind_attr` takes a `Source<Option<str>>`, whose `None` REMOVES the attribute.
  272  feature     The native backend compiles GENERICS: a generic function, a generic struct, a generic enum and a trait default each emit one Rust item per instantiation, and 51 of S1a's 98 refusals are gone.
  273  feature     An `[rpc]` method may return NOTHING and still be awaited: `void` is admitted as an rpc return, the reply is the ack, and the stub waits for it.
  274  feature     Vilan has a second backend: `--backend rust` emits your program as Rust, builds it with the `cargo` already on your machine, and the binary prints byte-identical output to the JavaScript one.
  275  feature     `Weak<T>` lands: `Shared::downgrade`, `Weak::upgrade`, `Weak::get` — the handle that names a cell without keeping it alive.
  276  feature     A `lazy let` carries its value across a hot swap once something has forced it; one still pending is re-minted.
  277  feature     `lazy let database: Database = Database::open("kolt.db");` — a module binding whose initializer runs at its FIRST USE and then memoizes.
  278  feature     `const let` and `const fun`: a compile-time binding may hold a CLOSURE, and a const function's promise is checked where it is made.
  279  feature     A struct VALUE is callable: `impl Scale with Callable { fun call(self, n: f64): f64 { .. } }` makes `space(2f)` resolve as the method `call` on it.
  280  feature     A styling condition is a VALUE now, and a set of them is the same value: `Style::on(conditions, inner)` is the one condition combinator, every named one is a line of sugar over it, and no program in the estate moves a byte.
  281  feature     Const evaluation gains an END-OF-EVALUATION HOOK: `asset::schedule_at_end(f)` runs `f` once, after every const evaluation of the compile has finished, in a const context of its own.
  282  feature     `#` is a token — the import REACH marker — and the `css` block's hex-colour rule moves to the one parser that knows a `#` is a colour.
  283  feature     The `export` marker is a BIT on an importable row instead of a wrapper the compiler throws away, and the three tooling surfaces read it.
  284  feature     `export *;` and `export(in PATH)` are grammar: the module-wide marker and the scope narrowing B318's visibility model is written in.
  285  feature     `only` and the `(impl …)` selectors decide which implementations a FILE admits, and a method call reaching one it declined is refused naming the selector.
  286  feature     `import a::b only;` and `import a::{ (impl List<i32>)::first };` parse, print and sort — B318's impl-selection surface, grammar first.
  287  feature     A render closure may yield any `Slot`, so a row, a `when` body or a `swap` branch can be a fragment, a text node or another value form — and the last wrapper elements in kolt are gone.
  288  feature     `when`, `swap` and the three `each` runs are VALUES that fill a child position now, so a conditional or a keyed run sits BETWEEN its siblings instead of after them.
  289  feature     A `context` clause lives in the closure's TYPE now, so a struct field, a generic argument and a return can carry one, and the coverage check follows the value wherever it flows.
  290  feature     `<>…</>` is the fragment literal: several children under one hole, with no wrapper element and no new runtime type — it lowers to a `List<View>` LITERAL, which the child contract already places.
  291  feature     `Vec2::length_squared` — `x² + y²` under the name the comparison wants, with `length` now stated in terms of it.
  292  feature     An `[rpc]` method may now return a `KeyedCell<K, T>`, and its stub hands the client a `KeyedSource<K, T>` — the keyed row of the handle mapping, in the same sync unleased shape as the plain one.
  293  feature     `flatten` is a BLANKET over `Source` now, not a member of the cell, so a `map` result, a derived cell or a mirror joins exactly as a `SignalCell<SignalCell<U>>` does — and a second blanket joins an outer of `Option<inner source>`.
  294  feature     `_` is the anonymous type binder: `impl Source<Option<_: Source<type U>>>` is the head that needed `type _: Source<type U>`, and two of them in one head are two parameters where they used to be one.
  295  feature     `std::wire` carries `Result<T, E>` and the whole sized numeric family, so a fallible reply and an unsigned id are ordinary payloads instead of forty hand-written lines and a workaround.
  296  feature     `std::math` gains `Vec2 { x: f64, y: f64 }` — the 2-D vector value a pointer drag needs, with `scale`/`length`/`distance`/`dot` and component-wise `Add`/`Sub`/`PartialEq`.
  297  feature     An `[rpc]` method may RETURN a signal handle: the server writes `SignalCell<T>`, a plain `ChannelId` crosses, and the client's stub answers `Result<RemoteSource<T>, RpcError>` — the half `[expose]` structurally cannot do.
  298  feature     `KeyedCell<K, T>`: a keyed collection whose WRITES are the deltas, so a keyed channel stops paying for the size of the collection on every change.
  299  feature     A keyed mirror IS a `Source`: `impl KeyedSource<K, T> with Source<Option<List<T>>>`, on the lease that was already there.
  300  feature     `std::router` can READ: `parse_path` takes a URL apart, `FromPath` is `Routable`'s inverse, and the query and fragment are reachable at all for the first time.
  301  feature     `std::dom` can MEASURE, OBSERVE and UN-SET.
  302  feature     `std::storage` grows a typed `Storage` handle — `window().local_storage()` / `window().session_storage()`, with `len`, `key_at`, `get`, `has`, `set`, `remove` and `clear` — and its `get` is the one that can tell an ABSENT key from a key stored as `""`.
  303  feature     `std::dom::Event` grows `code`, `target` and `current_target` — three apps-facing getters kolt hand-wrote in three different files, two of them under an explicit `// FIXME: Implement with std.`
  304  feature     `str::parse_bool(): Option<bool>` joins `parse_i32` and `parse_f64` in `std::option` — the string→value family had a hole exactly where four of kolt's six preference decoders reach.
  305  feature     `flex_grow(value)` beside `flex_shrink`, and `Color::current()` for CSS's `currentColor`.
  306  feature     `View::toggle_attr(name, flag)` — a BOOLEAN attribute, bound: present when the source is true, removed when it is false.
  307  feature     The CHILD CONTRACT: `str`, `View`, `List<View>` — and a `Source` of each.
  308  feature     A typed style token has a MID-VALUE spelling: `border: 1px solid {Color::gray(500)};` emits `var(--gray-500)` AND the `:root` line that declares it.
  309  feature     `std::style::prelude` — `rem`, `px`, `space`, `gray`, `hex`, `rgba`, `s()` and the rest of the typed tokens as free functions, AMBIENT inside a `css` block.
  310  feature     A `css { … }` block takes CHAIN LINKS: `.flex_row();`, `.ghost();`, `.custom(a, b);` — a dotted item ending in `;` is a verbatim `Style` method call at its written position.
  311  feature     A `css { … }` block is `const` BY CONSTRUCTION: `let card = css { padding: 1rem; };` compiles, and so does `<div .styled(css { … })>`.
  312  feature     A package's modules can live in DIRECTORIES: `lib/util.vl` is the module `pkg::lib::util`, `lib/ui/widget.vl` is `pkg::lib::ui::widget`, and nesting is unbounded.
  313  feature     The editor knows the tree: `pkg::` offers module directories, `pkg::lib::` offers a directory's children beside the module's own items, and a pure namespace offers what it holds.
  314  feature     A `List<T>` had no keyed exposure the macro could write: `[expose(keyed)]` read TWO written types off the annotation, a `Map<K, V>` element names both and a `List<T>` names only the element, so A39 refused the list form and steered to `Map<K, V>` or the hand-wired `expose_keyed`.
  315  feature     A service that is up, reads the token fine and still cannot serve — the database is down, the migration is mid-flight, the node is draining for a deploy — had no way to say so: `Reject` had three arms, and 429 was the closest.
  316  feature     A mirror was not a `Source`: `RemoteSource<T>` carried `get`/`map`/`or` as inherent members and implemented nothing, so A49's `on_change` was not on it, `effect` was not on it, and no generic `S: Source<…>` function — `selector`, or an application's own — could take one.
  317  feature     `RpcError` had no `PartialEq`, so the commonest question an application asks a typed error — which one is it? — could not be written as `error == RpcError::Unauthorized`.
  318  feature     Arrays join the nominal receiver set for USER impls: `impl [i32; 2] with PartialOrd { partial_compare }` and `a < b` runs.
  319  feature     `import` and `use` paths take an `as` alias
  320  feature     `flag && match probe() { … }` was a parse error — `found 'match' expected an expression` — and now it is an expression.
  321  feature     A struct field may name a TRAIT — `struct Store { count: Signal<i32> }` — and it means a type parameter the author never writes.
  322  feature     A `fun` can now DECLARE what it reads: `fun render(row: Row): str context (app_ctx, turn_scope)`.
  323  feature     `Service::authorize_timeout(millis)` bounds the verifier, and `Service::trust_forwarded_for(bool)` decides which address a limit is keyed on.
  324  feature     `[expose]` resynced the whole value on every change, so editing one message in a channel of five thousand cost the channel — `[expose(keyed)]` sends the change instead, and a client can subscribe to ONE key.
  325  feature     An rpc service can refuse a connection at the handshake: `Service::authorize`, a token subprotocol, and three limits that need no auth at all.
  326  feature     `Service::factory` mounts one service INSTANCE per connected client, so a method can finally learn who called.
  327  feature     The editor fades top-level code no entry of your package reaches, and every top-level item of a module no entry loads at all.
  328  feature     `Style::when(condition, delta)` — the conditional merge, as a chain instead of a pile of `if`s.
  329  feature     `view("input").autofocus()` — an element mount hook, replacing the uuid-plus-timer dance a custom modal needs otherwise.
  330  feature     `bind_each` asked for a key AND a `PartialEq`, and most lists only have one of the two to give — so there are three forms now, each dropping a bound.
  331  feature     `selector(source)` answers "is this row the selected one?" for a thousand rows in two writes, not a thousand.
  332  feature     `source.on_change(observer)` subscribes WITHOUT the immediate first call — the lazy half of the pair `sub`/`effect` had no spelling for.
  333  feature     The guard clause: `if !(slot is Some(let n)) { ret; }` binds `n` for the rest of the block.
  334  feature     `panic(…)` evaluates to `never`, and `never` ERASES: it yields to whatever it meets in every join — `if`/`else` arms, `match` arms, a block's tail against the type expected of it, an operator's operands (`never + i32` is `i32`), a call's argument against its parameter — so a body that ends in a `panic`, or in a `for { … }` nothing breaks out of, owes no return value, and programs the compiler used to refuse now compile.
  335  feature     `shapes::Dot { x = 1 }` parses. The struct LITERAL was the one spelling in the language whose head was still a bare identifier, so a module-qualified type could be named everywhere except where a value of it is constructed — and the refusal was "expected `;` to end this statement", which says nothing about the path it choked on.
  336  feature     `[derive(..)]` on a GENERIC type now generates a real generic impl — `impl Handle<type T> with Wire` over `Handle<T>` — instead of an erased one that named the subject bare in every role, and the arity rule generated code was exempted from now applies to it.
  337  feature     A trait on a PARAMETER is now an implicit generic: `fun f(x: Trait)` reads as `fun f<T: Trait>(x: T)`, monomorphized per call like any generic you write out.
  338  feature     The playground's buffers get a real prelude, chosen by mode: the WEB set in the browser mode that runs your program, the base set in the server check mode, and a toggle that turns the ambient scope off.
  339  feature     A type can be named through the module that declares it — `style::Style`, `ui::View`, `std::reactive::SignalCell<i32>` — in every position a type is written.
  340  feature     A server can call functions the CLIENT declared.
  341  performance The native backend has a liveness pass: a value read at its binding's LAST use is moved rather than copied, and the paper's board probe loses all four of the copies it was taking.
  342  performance A hover on a struct field no longer costs the workspace.
  343  performance The native executor reuses a settled task's slab slot, so a program that spawns per unit of work no longer grows the slab for its lifetime.
  344  performance A `lazy` position every call site fills with a literal builds no memo cell and forces nothing, so `unwrap_or(0)` stops allocating — 65 of the corpus's 84 thunk emissions gone and 9,310 bytes back.
  345  performance The body-sharing key normalizes local gensyms, so two monomorphizations that differ only in the NUMBER the generator gave a temporary share one body again — 3,387 bytes back across the corpus and 2,567 out of the split fixture's `app.js`.
  346  performance What is left of the import-collision refusal stops following the whole program's import-statement count: the ancestor walk is an index and `carried` holds only the rows the refusal reads.
  347  performance An entry-shaped world files its checks record too, so a keystroke in one of kolt's six open modules stops re-checking 59 of the 69 sources it already checked.
  348  performance The import-collision refusal is an index, not a product of linear scans, so the first real two-package collision in a large program stops paying for the whole program's import statements.
  349  performance A `[service]` entry reaches the base cache, so kolt's `store.vl` stops paying its package's whole pre-entry world on every keystroke.
  350  performance Eight frame-scoped `Shared` cells in std become `mut` locals, and the census that keeps the class at zero is a gate.
  351  performance Reference identity is an intrinsic that stamps a cell on the first ASK, so a program that exposes nothing stops minting an id per signal — and the eleven goldens A92 moved come back.
  352  performance A module opened as the entry, whose own package imports it back, stores its world under a key that says which module the world excludes — so the files a cycle reaches stop being uncacheable.
  353  performance A keyed run's order pass leaves an unmoved row alone: an append costs no cut at all where it used to cut and re-insert the whole list.
  354  performance B317's `impl_namespaces` is shared by reference, so a base-cache hit stops deep-copying every impl block std's closure registered.
  355  performance The `malloc_trim` at the analysis-landing seam is conditional on how much the release handed back, and what it costs there is measured instead of estimated.
  356  performance The analyzer's base cache is bounded at 192 MiB of resident worlds, measured rather than assumed, and its budget never evicts a world an open focused document is analyzed from.
  357  performance `CompletionIndex::build` asked `Program::source_of` once per declaration in the whole program — the identical M27/M58 hoist, taken where M58's own profile said it had not been.
  358  performance Closing a document gives its memory back to the OPERATING SYSTEM rather than to glibc's free list: `libc` is a normal dependency of `vilan-lsp` on Linux now, and `malloc_trim(0)` is called after a document closes and after an analysis is dropped.
  359  performance The language server keeps a whole analysis only for the file you are in and the one before it: every other open document drops its `Program` to its editor tables and re-analyses when you come back, which takes kolt's nineteen open `src/*.vl` from 1,090 MiB resident to 510 MiB.
  360  performance `__with_finally` and `__guarded` cost a browser bundle 241 raw bytes, 94 minified and 10 after gzip — measured, and left alone.
  361  performance A warm re-analysis is 6.4% cheaper, because the editor's own token table stopped asking a linear-scan accessor 426,198 questions it could ask once.
  362  performance A keyed change costs the change, on both ends: 40x cheaper at 1,000 rows and 372x at 10,000, and flat in the collection.
  363  performance Rule 2's last-use elision can see a `Shared` CELL: a binding fed by `cell.read()` may keep naming the cell's storage when nothing can mutate that storage while the binding holds it.
  364  performance M19's fourth tranche: R10 — the largest single line left on the reuse phase line — joins the Class A window, and what made that legal is that its whole-program report is keyed on a RENDERING rather than on an id.
  365  performance Four small slices off compile-perf-29's list, each with its own profile diff, take a cold `vilan check` of a real browser application's client entry down 8.20% and the WARM analysis after it down 12.86%.
  366  performance M44's "under construction" claim is inert on every package whose entries do not import the same `std`, and the package-union key that would fix that is measured here and REFUSED.
  367  performance Two entries of one package that differ in a `resource` declaration threw away each other's drop-scan enrolment record and re-walked every module body in the program.
  368  performance `vilan build` compiles a TIER of its legs at once, and every writer stays serial and in schedule order — kolt's three legs go 23.8 s wall to 15.7 s at no CPU cost (2.70 → 2.63 s, −2.6%), on a box at loadavg 114–128.
  369  performance M19's third tranche: the drop planner's per-body enrolment gate — the drop planner's whole price, and half of what M42 called the largest item left on the reuse phase line — is SERVED from a reused module's record instead of re-walked, and it needed no `Type` in the record to do it.
  370  performance M19 T1b bounded `CHECKED_CACHE`'s new class D table slices at eight million ROWS, and its own comment could only guess what that was worth — "on the order of a hundred megabytes".
  371  performance M35 put a workspace's members on their own threads and measured the price it did not pay for: a member whose base-cache key nobody has built yet starts COLD, and two such members race to build the same pre-entry world twice.
  372  performance M31 moved the analyzer's 35 `IndexMap`s off SipHash and named what it was told not to touch: 83M of the 85M Ir left in `sip.rs` was the const-eval interpreter's environment maps — left on `std`'s hasher by a comment in `fx.rs` claiming they are "not in the cold-analysis hot path", which the same profile falsified.
  373  performance `List::remove` and `List::insert` were hand-written shift loops over the CHECKED subscript helpers, so moving a list's tail cost a bounds check, a `__clone` and a store per element: `remove(0)` on a ten-thousand-element list was 13.9 µs, and `xs.insert(2, 3)` emitted a `while` loop where the host has a `memmove`.
  374  performance Every `vilan check` and every `vilan build` compiled kolt's four macro worlds again from scratch — 0.57e9 instructions, 10.7% of a cold client check — for std files and a toolchain that had not changed since the last build.
  375  performance A cold kolt client check clones `Type` 1.1 million times through `get_type_by_type_id`, whose borrowing twin `borrow_type_by_type_id` has existed the whole time — and the top of that census is the resource classifier, which clones its subject and then never looks at the copy again.
  376  performance `Node` was 320 bytes wide because `Func` — seventeen declaration fields — was inlined into two of its variants, and the parser moves `Spanned<Node>` BY VALUE up every precedence level, so every expression in every file paid for a function declaration's shape.
  377  performance The base cache is process-global and in memory, so every process that analyzes anything pays the whole cold `std` analysis again — and both differentials now run one process per corpus program. Measured: the base world is 190 ms of a 370 ms cold analysis, 51%.
  378  performance `lsp-index`'s entity table is the open buffer's, and it was built by walking the whole program to find it: 8.3–11.2 ms of thread CPU per keystroke that follows the CODEBASE, against 0.014 ms for the same rows fetched by the entry's own id range.
  379  performance A completion request read the declaring module's whole text to render a candidate's one-line documentation — the last table-shaped cost left in the keystroke path after M25 and M29, and the only one that followed the CODEBASE.
  380  performance `collect_unfollowable_loans` walked `expr_id_to_expr_map` three times, and the first walk built a `Vec<Convention>` for every call in the program only to ask whether it was `Some`.
  381  performance `collect_resource_bindings` swept every variable and every parameter in the loaded world, twice per analysis, on programs that own no resource at all — and the pre-filter the item asked for was built, measured, and WITHDRAWN, because it made things slower.
  382  performance The base cache's byte tally counted the module texts a world was built from and nothing else, so it under-reported every retained world by the size of T0's per-`TypeId` census — 25,900 B of a plain std world's 177,775 B, 17% of the figure M24's LRU budget is compared against.
  383  performance A keystroke stopped rebuilding the tables the EMITTER reads for every module it did not touch.
  384  performance A package's entries were checked one after another on one of sixteen threads: a workspace's `check` wall was the SUM of its members where it could be closer to the largest.
  385  performance `callee_bindable_generics` answered "which impl declares this member" by scanning every impl and, inside each, its whole declaration map — once per call, 856,908 times in one cold check, 10.4% of its instructions.
  386  performance `vilan check .` cost 99.94% of what `vilan build .` cost, because a check of an ENTRY ran the whole transformer and threw the JavaScript away.
  387  performance The analyzer's `IndexMap` tables were still hashing every `u32` key with SipHash: 7.4 million SipHash calls, `sip.rs` 6.7% of a cold `vilan check` of a real browser application's client entry.
  388  performance Drop planning asked one WHOLE-PROGRAM question — "is anything in this program a `resource`?" — and `std::db` and `std::fs` each answer yes, so a browser entry that owns no resource at all paid the per-body ownership walk for every body it had. It is a per-body question now: on kolt's client leg the whole analysis goes 1.775 s -> 1.642 s of process CPU, and on a thousand-function exhibit the planner walks 40 scan roots instead of 1,575.
  389  performance Completion's third tranche: a member list stopped being a walk over every impl in the program, an auto-import edit stopped costing a whole-buffer parse per request, and the raw parse behind the element/`css` classifier is now paid only where either can contain the cursor. On E121's generated 1,791-function exhibit a completion request goes 0.813 ms → 0.415 ms of thread CPU and the three-provider burst 0.814 → 0.530 ms.
  390  performance A keystroke no longer re-checks every module of a program it did not touch.
  391  performance Completion was the keystroke path's entire remaining budget, and it was spending it on the CODEBASE rather than on the file: every request swept every `std`/`pkg` child module's `name_to_id_map` to find auto-import candidates, and an import path's origin arm did a `read_dir` per source root. Both are functions of the analysis alone, so both moved onto the analysis that produced them. On E121's generated 1,791-function exhibit a completion request goes 0.628 ms → 0.126 ms of thread CPU, and the three-provider burst 0.568 → 0.103 ms.
  392  performance A superseded analysis now STOPS. A keystroke burst used to run one whole whole-program analysis per debounce window and throw all but the last away at `land`; ten keystrokes on kolt's `views.vl` cost 989 ms of CPU each and now cost 127 ms — one complete analysis and nine cancelled, measured through the real notification handlers.
  393  performance The language server's base-world cache has a byte budget and evicts least-recently-used, instead of growing for the life of the session.
  394  performance Editing a file that imports an OPEN sibling no longer rebuilds the whole pre-entry world on every keystroke.
  395  performance An editor keystroke no longer rebuilds the standard library's world just because the file imports a sibling module. Any `pkg::` import used to bypass `BASE_CACHE` outright, so kolt's `views.vl` and `client.vl` paid a full pre-entry world build on EVERY analysis — the `base` phase read 156–586 ms per keystroke (loadavg 10–30) — while their sibling `theme.vl`, which imports no sibling, hit the cache at 0.0 ms.
  396  performance A generic bound is now evaluated once per (type, trait, arguments) instead of once per call site, which takes the whole-program bound check off the per-keystroke curve it was on.
  397  performance `vilan build --watch` recompiles the legs an edit reached, not every leg of the workspace.
  398  performance Semantic highlighting, inlay hints and completion now describe the buffer on your screen instead of the last finished analysis, and their cost stopped tracking the size of the codebase. On a generated 1,791-function exhibit the whole-program walk every request used to pay costs 1.104 ms of thread CPU; the keystroke path that replaced it answers semantic tokens in 0.004 ms — 276× less — and inlay hints in 0.001 ms, with the whole three-provider burst at 0.828 ms against the 10 ms budget (release, 50 requests per window, loadavg 41.9 on 16 cores; wall 2.926 / 0.004 / 0.001 / 1.632 ms beside it and asserted on nowhere). On a read-only copy of a real 1,791-function application the same request went from 12.2 ms of wall and 10 ms of CPU to 0.30 ms and 0 ms, and from 12.3 ms measured from the keystroke to 1.8 ms — at loadavg 45 against the earlier reading's loadavg 11, so the four-times-busier machine makes those a floor rather than a ceiling.
  399  performance The viewport semantic-token request now costs the viewport: `semanticTokens/range` computed the WHOLE file's token stream and then filtered it by line, so twenty visible lines cost 9.0 ms of wall and 10 ms of process CPU on kolt's `views.vl` against the whole file's 8.9 ms — a ratio of 1.01, which is the defect stated as a number (measured before E121's keystroke path landed, at loadavg 8.2–11.9).
  400  performance Opening a file no longer parks a tokio worker for the length of its first analysis: a `semanticTokens/full` issued the instant after `didOpen` answered in 1270.7 ms before this change and 7.9 ms after, on kolt's `views.vl` at loadavg 8.2–11.9.
  401  performance A bounded generic's dispatch is refined once per TYPE reaching it, not once per CALL SITE — which halves every surface that analyzes a program with a large generated module in it: the language server's keystroke, a `vilan watch` round, and a plain `vilan build`.
  402  tooling     The questions both emitters ask of a finished program are answered in ONE place: a new `vilan_core::mono`, and the backend stops carrying its own copy of `RENDER_MEMBER` and of what `\n` means in a string literal.
  403  tooling     `std/src/reactive_pipeline.vl` — the A124 paper's evidence, in std and out of the surface.
  404  diagnostics A global host PROPERTY bound in the function form is refused where it is written, instead of emitting a call to it.
  405  tooling     A counter that could only ever read zero is gone, and what the arm it watched actually does is written down.
  406  diagnostics `[derive(PartialOrd)]` is refused at the attribute instead of expanding to nothing.
  407  tooling     The std twin-parity gate says what it means by "surface", and it is the EXPORTED one — which retired fourteen of its twenty-six recorded divergences.
  408  tooling     Two pins that could red for reasons that had nothing to do with the code they guard no longer can — and the rule that would have caught both is written at each suite's head: no assertion reads a clock.
  409  tooling     The completion engine has pins in the crate that implements it.
  410  tooling     A hover test helper that silently rewrote its own fixtures now refuses them.
  411  tooling     `()` and `void`: the two spellings the grammar took and the language has no meaning for are refused where they are written.
  412  tooling     The language server's analysis fence is pinned, and its internal-error diagnostic now NAMES the file it could not analyze.
  413  tooling     `[internal("reason")]` marks an item that is part of the surface and dangerous to reach for — the editor hides it, dims it and leads its hover with the reason.
  414  tooling     Format-on-save honours your package's `[fmt]` section, and one comment run can be re-filled on demand.
  415  diagnostics A member missing from a std twin says which twin HAS it — and a file with no name evidence is coloured by what it does with the twins it imported.
  416  tooling     The native host census walks PAST a refusal, into the refused call's arguments and the bodies of the closures among them.
  417  tooling     The pipeline's own nodes stand behind one `dyn Source<i32>` field.
  418  tooling     std opts into `[fmt] wrap_comments` at 84 columns, and its prose is re-filled once.
  419  tooling     A generic `impl` header past the width is now PRINTED, one bound per line — where a hand-wrapped one made `vilan fmt` decline the whole file.
  420  tooling     `[fmt] comment_width` — prose gets its own width, and it defaults to the code width.
  421  tooling     `vilan fmt` can re-fill your comments to the line width, behind `[fmt] wrap_comments` — opt-in, default off for one release.
  422  tooling     A `vilan fmt` that declines names the line it and your file stop agreeing on, found by bisecting the two token streams — where it used to name the file's first declaration.
  423  tooling     `vilan fmt` reads its own output back: the formatter parses the text it is about to write, and declines rather than write a file that does not read back.
  424  tooling     A context-threaded hidden parameter's TYPE is recorded where the pass mints it, so the native backend looks it up instead of inferring it back from the call sites.
  425  diagnostics A diagnostic still carries ONE note, and the first real candidate for a second one is why.
  426  tooling     A nominal declaration's generic parameter has ONE spelling in its own body, and it is now written down and gated instead of defended against.
  427  tooling     A generated name inside a `// witness:` is a hole, so the claim gate can no longer be reddened by the counter it says nothing about.
  428  tooling     `List::new` and `List::push` are `Intrinsic` rows like every other built-in lowering, and the class of defect B359 was one instance of is closed at the table instead of patched at each reader.
  429  tooling     Four smalls Order 38's lanes left behind: the stack-frame record says what was measured, the depth line survives a refused parse, the temp scripts cannot be written through a symlink, and the JSON census says it is a census.
  430  tooling     A `$VILAN_STD` pointing at half a toolchain is refused ONCE, first, and against the file the reader can act on.
  431  tooling     Every completion candidate now states the prefix it replaces and the text to filter it by, so a hyphenated name survives in a client that is not VS Code.
  432  tooling     A generic `<` closes itself, and a backtick pairs in prose — the two the editor made you type by hand.
  433  tooling     A `///` above a struct FIELD now reaches every position that field appears in — and a field position answers about the field rather than restating its struct.
  434  tooling     Hovering a generic call shows two signatures: the declaration as written, and the exact function that call reached.
  435  tooling     A parameter's `context` clause renders once in a hovered signature again, and the pins that let it double now count instead of containing.
  436  tooling     "Who cleans up what" is a table in the guide and five pins in the suite, where it was five doc comments.
  437  tooling     B366: `bind_parameters`' bare-constraint-id branch is deleted — it was unreachable, and the measurement says so.
  438  tooling     `release.yml` has a dry run: `workflow_dispatch` walks the same graph up to the asset list and stops.
  439  tooling     E69's TSV-to-table arrow has one implementation again: the Rust gate, and the python generator keeps only the fetch.
  440  tooling     N86's tail of suites writing to the shared tmpfs goes from thirty-eight files to three.
  441  tooling     Three AST traps that each cost a lane a wrong emission are written down at their declarations, and the ledger's crate list is derived from the workspace instead of copied.
  442  tooling     The parser's stack frame is 34,181 bytes per level of source nesting, not the ~71.8 KiB the record claimed, and it has a canary now.
  443  tooling     Swallowed `\`-continuations are gated over every string literal in the workspace, and the thirty-eight sites the tree still carried, across nine files, are restored.
  444  diagnostics Two user-visible messages the ledger's walk cannot read are rowed by hand, at the files that build them.
  445  tooling     The diagnostics ledger's liveness check searches EVERY literal fragment of a row's key, not only the longest one.
  446  diagnostics A lazy argument that reaches a resource through a FIELD is one diagnostic, not two: the argument refusal owns the shape and R9's thunk capture stands down.
  447  tooling     The HMR overlay places a transformer refusal in the file the refusal names, which is what the terminal has been doing since E190.
  448  tooling     A hyphenated attribute or css property completes under its own prefix: the extension declares a `wordPattern` that reads `stroke-w` as one word, where the client's default read it as `w`.
  449  tooling     A101's refusal now carries the codemod: the editor rewrites `property: value;` into `property(value);` one declaration at a time, or every declaration in the file at once.
  450  tooling     Format-on-save says when it DECLINED: the language server names the construct and the line once per file per cause, and the playground's formatter hands back a verdict instead of silence.
  451  tooling     `vilan fmt` no longer throws away a whole file over a `const { … };` block: the `const` EXPRESSION prefix keeps the terminator its statement form needs, where only `const let` and `const fun` may ask the declaration under them.
  452  tooling     Every `[vilan phase]` figure carries the CPU it burned beside the wall it took, so the perf arc's numbers are absolutes on a loaded box instead of shares.
  453  diagnostics E189's BROAD GATE lands: on a program that does not type, the whole `context` family stands down and says so once — and kolt's `href(href())` shape goes from 35 errors to 1.
  454  tooling     `VILAN_NATIVE_HOST_CENSUS=1` answers "what does this program still need from its host" in ONE emit, and kolt's server leg answers 85.
  455  tooling     An entry-shaped world is weighed, and it weighs 80% of a resolved one — so the base cache's single 24x factor stands and M74's over-charge is 13%, not an order of magnitude.
  456  tooling     `resolve_world`'s eleven stages are on the `[vilan phase]` line, and the first reading through it says an open module's per-keystroke resolve is the constraint fixpoint and nothing else.
  457  tooling     An element head offers the attribute NAMES, generated from the WHATWG HTML and SVG 2 attribute indices and gated against a vendored extract of them.
  458  diagnostics The `context` clause stays after the return type, the one shape that position cannot spell is refused with both ways out, and both grammars paint the keyword where it actually sits.
  459  diagnostics One arity error in an element head reports alone at the head, and kolt's `href(href())` goes from 194 errors to 139 — the item does not close.
  460  diagnostics A call the solver could not wire no longer deletes everything written inside it from the call graph — kolt's 184 errors become its 9.
  461  diagnostics The never-silent body-less refusal (B55) names the function that was being emitted when the requirement was first reached, and renders in that function's file.
  462  tooling     The analyzer's walk costs 42,464 bytes per level of nesting, measured two ways, and a canary now reds on Linux when that grows.
  463  tooling     Eighty-eight scratch paths across thirty-nine test files moved off the shared tmpfs, and the tail is a list rather than a memory.
  464  tooling     Seventeen std doc comments stopped teaching a method A99 retired, and a gate holds the rest of them.
  465  tooling     The spec's two reserved-word lists are held to the lexer's table, and §2.2 — the normative one — was missing `lazy`.
  466  diagnostics The ledger's enumeration reaches the const channel and the macro engine: fifty-two refusals that were rowed by nobody now have thirty-four rows.
  467  tooling     `Program::hidden_impls` is `hidden_impls_pending`, because by the time most of the compiler can see it, it is empty.
  468  tooling     Two messages that shipped with eighteen spaces in the middle of a sentence are repaired, and the ledger refuses a third.
  469  tooling     B267's cell walk says what a counted `Shared` does to it, and its worked example names a relation that still exists.
  470  diagnostics A `#` on an `(impl …)` selector whose blocks the module EXPORTS is redundant, and says so.
  471  diagnostics A67's bound is written down and pinned instead of overclaimed: the module-shadowing refusal names a collision something WALKS INTO, not one nothing has ever reached.
  472  tooling     `vilan check` writes nothing into the package it checks: its macro expansion table moved out of `dist/` and into `~/.vilan/check-cache/`.
  473  tooling     std's fifteen `export macro fun`s are held to the formatter by name, and the fixed-point harness can no longer pass by bailing.
  474  tooling     `vilan fmt` says out loud when it could not format a file, with its own exit code: a bail is no longer reported as "clean".
  475  tooling     Completion at a `css` block's dotted head offers every `impl Style` method the file can reach, not only the fourteen condition combinators.
  476  diagnostics The const channel's nine interpreter refusals are rowed, and the ledger says out loud that its walk cannot reach them.
  477  tooling     Two pins that went red under lane load and green on a re-run are bounded by what their instruments can actually measure.
  478  tooling     The `inference` harness writes its scratch under `target/`, cleans it by `Drop`, and says so when the disk is full.
  479  tooling     The four asset artifacts a corpus build writes beside the corpus sources are gitignored, and a pin asks git rather than a list.
  480  diagnostics A `[derive(..)]` reached inside a macro WORLD is skipped instead of being refused as a compiler bug.
  481  diagnostics A67's ambiguity names the child as it is spelled on disk, and it reaches the pure-NAMESPACE child it could not see before.
  482  tooling     The session summary's retained-state line names the analyzer's base cache: `base_cache_worlds`, `base_cache_weight` and `base_cache_budget`.
  483  tooling     A quickfix can edit another file, and B318's "Export `S`" / "Export `hidden`" become actions instead of sentences.
  484  tooling     A head item with no parentheses of its own paints as an attribute name too: `disabled` and the `hidden` of `hidden.show(flag)`.
  485  tooling     The css-spelling refactor reaches an `impl Style` extension in ANY file of the program, not just the one the cursor is in.
  486  tooling     `vilan fmt` gives the module-level `export *;` marker a canonical place: the slot just below the file's imports, with a paragraph gap on both sides.
  487  tooling     `vilan fmt` sorts the condition values inside an `on` head, the completion table answers inside one, and the axis column is held to the byte each constructor actually builds.
  488  diagnostics Three visibility finds from Order 35 close: a selector that admits nothing says so at the import, `export(in <general PATH>)` is enforced, and `source_of` stops lying about a module that owns both a file and a directory.
  489  tooling     The std cache prunes itself at materialization, and `vilan cache prune` is the gesture for the times that is not enough.
  490  tooling     R10's two reuse predicates become one, keyed on the site rather than on which handle the caller happened to have.
  491  diagnostics The Rust DERIVE generators are deleted, and a std that does not declare a derive's macro is told so instead of being served a second answer.
  492  tooling     std's manifest states a census a reader can re-run, with the command beside it: 664 top-level declarations across 63 files, 118 of them reached by name from another std file.
  493  diagnostics `SignalCell::update` and A80's pattern-binder steer both name the write-back the cell-growing idiom needs, instead of stopping at the spelling that binds a copy.
  494  diagnostics `lib` is a module's BODY FILE and not a segment below its own directory, so `import pkg::a::lib` is refused with a steer to `pkg::a` instead of resolving `a/lib.vl` a second time under a second name.
  495  diagnostics A malformed import reports at the token its path grammar stopped on, with a rule naming the import grammar, instead of `found 'import' expected an expression` at column 1 of the statement.
  496  diagnostics Two blanket impls whose argument binders are bounded DIFFERENTLY stay admitted, and what that costs is now written down at the rule and held by two pins.
  497  diagnostics The `[expose]` ELEMENT refusal quotes the annotation as the author wrote it, so a field annotated `SignalCell<models::Note>` is told about `models::Note` and not about `Note`.
  498  diagnostics An `[rpc]` method returning `Option<KeyedCell<K, T>>` is refused in the method's own vocabulary instead of inside the code the attribute generates.
  499  diagnostics A `context` clause written where the threading cannot follow it — inside a `List`, a `Map`, an `Option`, a tuple or an array — is refused instead of admitted and ignored.
  500  tooling     Organize Imports prunes an unused impl selector, rewrites an emptied statement to the selector its file actually needs, and completion answers inside one.
  501  tooling     `Vilan: Show Language Server Status` asks the SERVER for its session summary now, so E166's memory numbers arrive when you run the command instead of at the next 500-request tick.
  502  tooling     An unused import leaf whose statement Organize Imports will KEEP says so, instead of fading with the same "unused import" text as one the action deletes.
  503  tooling     "Convert to a `css` block" inlines the CURRENT file's own `impl Style` methods, so a chain that opens with the app's own shorthand converts instead of offering nothing.
  504  tooling     The book's element-tag rule guards the OPENING tag's `<` and stops guarding the closing one's, so `</span>` after text is a tag in the third grammar too.
  505  tooling     A head item's NAME paints as an attribute name, so `class("row")` is no longer a call and `type("checkbox")` no longer the declaration keyword.
  506  tooling     A closing tag straight after text is a tag again, and the `>` of an attributed opening tag is a tag delimiter wherever it sits.
  507  tooling     The TextMate grammar is gated on the scopes it produces, not on the text of its rules.
  508  tooling     `hmr_css_matrix` waits for the round to say it finished and then reads until the bundle PARSES, instead of reading a file the watcher is still writing and hoping two substrings mean it is whole.
  509  tooling     The replay harness refuses a package's MANIFEST directory where its source root belongs, instead of quietly measuring a smaller program.
  510  tooling     The last two DOM stubs outside N73's consolidation are folded in: `mount_missing_id.rs` and `hmr_swap.rs` run against `support/dom/stub.js` now, and the tree holds ONE hand-rolled DOM.
  511  tooling     The ledger's hand-rowed exemptions name their rows by KEY now, so the one kind of row that most needs an exemption — a lane's unnumbered `NEW` one — can finally be written into the list.
  512  diagnostics The `css` block's import steer is deleted as the element's twin was: the second of two notes that could only fire where their own advice was false.
  513  tooling     An element argument is atomic to the chain-break rule: `vilan fmt` was rejoining a hand-broken `.child` ladder into one over-budget line and then tearing an element open inside it.
  514  tooling     The "Convert to a `css` block" refactor never fired on real code — the seed it demanded is the one spelling the estate does not write, and any typed link refused the whole chain.
  515  tooling     `Vilan: Show Language Server Status` reports memory now — resident size, heap in use and heap retained-free — beside the cardinalities it always had, and says how many of the open documents still hold a program.
  516  diagnostics A keyed handle's KEY is now Wire-checked (and Hashable-checked) in the `[rpc]` method's own vocabulary, on the annotation the author wrote, instead of failing four times inside generated code.
  517  diagnostics A suspending `async fun f(&mut self)` `[rpc]` method takes ONE report — the `[service]` attribute's — where it used to take that one and E3's view-parameter refusal beside it.
  518  diagnostics An `[rpc]` method whose name the `[service]` expansion already generates is refused at the attribute and spanned on the method, instead of colliding as a duplicate definition inside code the author never wrote.
  519  diagnostics The name-keyed candidate list behind B254 and B258 is swept: every consumer of `candidates_of` now says what the list is keyed by and which DIRECTION it reads an edge in, because that direction is the whole argument for whether widening is safe — and the invariant B258's silence rested on is held by a pin rather than left implicit.
  520  diagnostics A written `::` path quoted in a diagnostic is the path the author wrote: `[rpc] fun note(self, id: i32): models::Note` is refused as "return type … is `models::Note`, which is not Wire" instead of "… is `_`".
  521  diagnostics Two blanket impls declaring one name are refused when their bounds OVERLAP, not only when they are identical — "no silent pick" is the rule tier 1 of method resolution cannot keep on its own, and sameness was a narrower test than the rule needed.
  522  diagnostics An `a.vl` that declares `b` beside an `a/b.vl` is an ambiguity error now — the same answer `a.vl` beside `a/lib.vl` has had since A65 — and the collision was worse than the item recorded: the DECLARATION was winning, and the file could not be reached at all.
  523  diagnostics `Some(mut list)` was already the spelling; what was missing was what happens when you write `Some(let mut list)` — the two binding forms in a pattern name the one that was meant now, instead of reporting the payload's own paren.
  524  tooling     The eighteen curated rule statements and the resource-derive refusal were unrowed messages rather than red ones, and the ledger gate holds both now.
  525  tooling     The last wall bound N61's sweep left standing is gone: "the verifier's refusal landed on the BOUND, not when the hook finally answered" is asserted as order now, not as 2,500 milliseconds.
  526  diagnostics The element-syntax import steer is deleted rather than narrowed: a note that could only fire where its own advice was false.
  527  tooling     `nested_nurseries_join_inside_out` asserts the nesting it is named for instead of an interleaving nothing promises: the set of lines, plus the four orderings a nursery actually guarantees.
  528  tooling     A free task's failure is assertable now, and B277's other half is pinned with it: a `Debounce` whose OWN callback throws survives it, and says so on the way out.
  529  tooling     The eight browser-bundle suites now share ONE DOM stub — `crates/vilan-cli/tests/support/dom/stub.js`, with each suite's extras layered on top by `concat!(include_str!(..), include_str!(..))` — instead of nine near-copies of it spread across their test files.
  530  tooling     The reference index's first invariant is now checked over every module a program loads instead of over the entry file alone, and it immediately found two rows whose text is not their definition's name — the exact class B264 closed one instance of.
  531  tooling     The element-head sorter's bargain is written down where the people it affects will read it: sorting an element head moves WHEN its attribute values are evaluated, and that is accepted rather than checked for.
  532  tooling     A `for x in <iterable>` head over the line budget now breaks at its iterable, the way a `for` condition and a `match` subject already do.
  533  tooling     A doc listing of a std enum is now held to the enum: the book cannot go on counting five variants after std declares a sixth.
  534  diagnostics `match x { .. }::foo` is refused with what is actually wrong — a path's left side is a NAME, and a block-like form is a value — instead of being steered to parentheses that do not parse either.
  535  tooling     A fixed-array generic argument no longer ends the TextMate list at its own length separator: `SignalCell<[i32; 4]>` is highlighted as one head instead of stopping at the `;` and falling back to the top-level rules.
  536  tooling     Completion inside a struct initializer offers the struct's FIELDS: `KoltStore { us▮` used to fall through to the bare expression position and list every binding in scope, which is the one list that cannot be right where the author is writing a field name.
  537  tooling     A `context` clause lives on a PARAMETER and nowhere else, and the suite now says so by name: a closure stored in a struct field cannot carry one, and an injected closure cannot be stored at all — which is what blocks A85's positional-slot VALUES, not shipped here.
  538  diagnostics An `impl` whose `with` clause under-supplies a trait's arguments now reports once, where it used to report the arity error and then a conformance mismatch read off the padding that hid it.
  539  tooling     The Rust fallback `[service]` generator is gone: a std with no `rpc.vl` is now told so, where it used to be handed 270 lines of a stale twin of the macro that expands the attribute for everyone else.
  540  diagnostics An `[rpc]` method declared `async` may no longer take `&mut self`: the receiver is a promise that the handler does not await, and until now nothing enforced it.
  541  diagnostics A non-Wire `[rpc]` payload reported two and three times: the signature refusal the author can act on, plus the generated client's own `call<T: Wire>` bound failures, anchored over the whole `[service]` struct and reading first.
  542  tooling     The language server's session is measured rather than suspected: 2,000 keystrokes on one document do not get slower and do not accumulate, and what a session really grows along is the files it OPENS.
  543  diagnostics `Color::var` and `Length::var` refuse a custom-property name without its two leading dashes at const time, instead of rendering a reference no browser resolves.
  544  diagnostics One unresolved name in an `impl` SUBJECT is one error again: `impl Source<Option<Nope>>` said `cannot find type 'Nope'` twice for one mistake, where `let x: Option<Nope>` has always said it once.
  545  diagnostics A BARE TRAIT in impl-subject position now MEANS "for every type that implements it": `impl Source<type I> { fun flatten(self) .. }` is `impl type S: Source<type I> { .. }`, so `self` in the body is the implementing type instead of a value of a trait, which vilan has none of.
  546  diagnostics `bind_callee_own_generics`' dropped reconcile is settled rather than papered over: a later check always fires, the drop is not evidence of a defect, and both facts are now written at the site and pinned.
  547  diagnostics A handle-returning service wired to a connectionless mount now says so where it can be acted on: `local_rpc` over an unstamped protocol is refused at WIRING time naming the method, and the `{mount}rpc` POST leg's runtime failure names the method and the transport that works.
  548  diagnostics `[expose(keyed = K)]` over a `KeyedCell<K2, T>` whose written key disagrees with the cell's own is refused at the attribute, instead of the argument being silently ignored.
  549  diagnostics `[expose]` on a struct carrying only `[client_service]` is refused at the attribute instead of compiling into a channel nothing ever mints.
  550  tooling     A nested generic head had four rules painting it and no two agreed: in `impl Source<Option<type _: Source<type U>>>` the semantic layer took `type _: Source<type U>` as ONE type-parameter token, `<type` opened a tag, the other brackets were comparison operators, and a wrapped head's closing `>` was an element terminator.
  551  tooling     The rpc guide's hand-wired-channel trap now names what actually makes a hand-wired mirror different — it has no ORIGIN — instead of a mechanism claim about `Unsubscribe` that the demand ruling supersedes, and `RpcError` is documented with all six of its arms.
  552  tooling     The four handshake refusals are one table in the services guide, on the line that decides between them: who made the call.
  553  diagnostics `[expose(keyed = K)]` over a `SignalCell<Map<K2, V>>` whose written `K` disagrees with the map's own key is refused at the attribute, instead of the argument silently winning and the disagreement surfacing four times over as a `Keyed<K>` bound failure in generated code.
  554  diagnostics `[rpc] fun login(mut self, …)` compiled and lost the write in silence: the handler mutated its own copy, the reply carried the new value, and the very next call on that connection read the old field.
  555  diagnostics A handle return's WIRE rule lands on the element, not on the source.
  556  diagnostics `:hover { … }` inside a `css` block is steered to `.hover { … }` instead of reporting a missing declaration.
  557  tooling     Property-name completion inside a `css` block now offers the CSS property index, not just the properties a `Style` method happens to write.
  558  diagnostics `fun bump(a: Add) { a + 1 }` refused as "`Add`'s `add` accepts `Add`, but the right operand is `i32`" - three `Add`s in one line, two of them the parameter and one of them the trait it is bound by.
  559  diagnostics `import pkg::lib;` where `lib/` holds modules but no `lib.vl` is refused, and the refusal names the modules it does hold.
  560  diagnostics B248's refusal was at the STATEMENT fork; the rule is about the FORM, so `let y = match x { .. } + 1;`, an argument position, and a `.` chain on a block-like head all took the bare parse error instead.
  561  diagnostics The missing-member refusal's HEAD named the trait UNPARAMETERIZED — `'Counted' does not implement trait 'Source'` — even where the arguments were what the refusal was about.
  562  diagnostics A GENERIC `[service]` subject had no refusal at all: it expanded anyway and handed the author the generated client's own errors — `'contract_hash' is already defined for 'StoreClient<T>'`, then `` `Store` takes 1 type argument, 0 given `` twice, then two "cannot call method … on unknown".
  563  tooling     Two accuracy gains Order 29 shipped are now observed by something: B254's narrowing is asserted as a missing EDGE, and B244's composing substitution is measured over the corpus with a bound.
  564  tooling     Every on-disk cache lives under `dist/.cache/` — the owner's ruling — and `vilan check` may create that directory and writes nothing else, pinned.
  565  tooling     `own` is a keyword the tour never mentioned, and a lane spent a debug cycle discovering it on a fixture.
  566  tooling     A 45-second WALL bound on a whole `vilan run .` — build included — for a claim about how many times a client redialed. Three of those, replaced by the count itself.
  567  tooling     Three smalls Order 29 left behind: a fourth `wait_for_port`, a fixture the formatter would rewrite, and a comment that counted to nine while the tree counted to twenty.
  568  tooling     The ignore line N58 added for `file-corpus-<random>/` is gone, because the strand it covered cannot happen: a corpus program runs in a scratch working directory of its own.
  569  tooling     A module DIRECTORY with no body had nowhere to point: go-to-definition on `ui` in `import pkg::lib::ui::widget::label;` landed on line 1 of the file the caret was already in, and hover said `module ui`.
  570  tooling     `transport_robustness` flaked once on `EADDRINUSE` under lane load, and the reason was a bind-release-rebind window a whole `vilan build` wide.
  571  tooling     N55's `vilan fmt --check` gate covers `.vl` FILES only, so the 208 compilable ```vilan fences of `vilan/docs` and `README.md` are outside it — and they stay outside, stated rather than silent.
  572  tooling     A corpus differential that dies mid-run strands `file-corpus-<random>/` in `crates/vilan-core`, untracked and unignored.
  573  tooling     E128's own impl pin imported `PartialEq` from `std::operators`, the line that seeded the E138 phantom — and it still carried it after E138 was closed as a phantom.
  574  tooling     M36 asked for a cross-process on-disk base cache and the spike answers that the on-disk unit cannot be the world: `World` is keyed in three places by AST node ADDRESSES, and one of them hangs a lazily COMPILED macro world off an `Arc`.
  575  tooling     A `match` leg body written `=> { expr }` expanded to three lines whatever it held — E146 rule 2's inline-arm rule, one construct over.
  576  tooling     A `for` condition — vilan's `while` — and a `match` SUBJECT had no split permission, so an over-budget one had nowhere to break at all.
  577  tooling     `vilan fmt` canonicalizes an element HEAD — the import and style-chain sorters' third sibling: undotted attributes lead in `id`, `name`, `type`, `for`, `href`, `src` order and then alphabetically, `on:` handlers follow them alphabetically, and a dotted chain link is a BARRIER that never moves.
  578  diagnostics `match x { .. } + 1` in leading statement position reported `found '+' expected an expression` — true, and about a statement the author did not know they had written.
  579  diagnostics `impl Counted with Source<i32>` was told to `declare fun on_change(self, observer: |T| void)` — a `T` the impl does not have, cannot introduce, and cannot copy the line without.
  580  tooling     Four small editor defects from E145's neighbourhood, each with a pin.
  581  tooling     The "Declare the inferred contexts" quickfix fired on B242's SUBSET refusal and on nothing else — which is the refusal a caller almost never meets.
  582  diagnostics E142's `::` line rule now reaches the two productions it left out: a TYPE PATH and a STRUCT-LITERAL HEAD.
  583  tooling     What a keyed channel costs in CPU, measured before anything was built for it: `keyed_diff` re-keys two whole snapshots on every change, so it is O(N) per change PER SUBSCRIBED CONNECTION — and the client's `KeyedSource::apply` reaches `index_of` per op, which is a linear scan of the mirror, so both halves are O(N).
  584  tooling     A cold `vilan check` of kolt's client compiles FOUR macro worlds — 18.2% of that entry's instructions, comparable to its own whole-program checks — and the phase line could not see any of them.
  585  tooling     `vilan fmt` sorted a bare `self` LAST in an import group, because the sort was plain ASCII and `self` is a lowercase four-letter word: `Option::{ self, Some, None }` reprinted as `Option::{ None, Some, self }`, and N55's reformat wrote that shape into 37 groups — `vilan/std/src/prelude.vl` and `web.vl` among them.
  586  tooling     Every `if` arm expanded to its own lines, wherever the `if` sat, so a one-line closure body inside a builder ladder became five: `.bind_text(pending().map(|busy| if busy { "..." } else { "" }))` came out of N55's reformat as a `{`⏎`"..."`⏎`} else {`⏎`""`⏎`}` in the middle of a `.child(…)` chain — and the enclosing chain, measuring a first line that now FIT, never broke.
  587  tooling     A declaration's `context` clause printed where it was WRITTEN, so `fun slot(x: i32): i32 context turn borrows x` and `fun slot(x: i32) borrows x context turn` were two spellings the formatter kept apart instead of one it answered.
  588  tooling     A comment written between a field's name and its value walked out from under the field it explains and landed below the whole statement: `Task { id = 1, name = // the row's label`⏎`"x" }` reprinted as one inline literal with the comment on a line of its own after the `;`.
  589  tooling     A binary-operator chain had NO break rule, so the formatter JOINED hand-wrapped conditions without a width bound: `vilan/macro_std/src/meta.vl`'s three-line `text.contains(" ") || …` became one line of 182 characters under N55's reformat, and four lines that reformat ADDED exceed 100 in a tree whose own reflow target is 100.
  590  tooling     `import a::b as b;` renames `b` to `b` — it binds exactly what `import a::b;` binds — and the formatter kept it, so the redundant spelling was one a rename could produce and nothing would tidy away.
  591  tooling     M19 T1's corpus replay differential was a 60-170 s sweep living inside `check_scope_differential`, so every targeted `-p vilan-core` run and every lane's whole-crate gate paid a corpus analysis to ask a question about the S1 seam.
  592  tooling     `vilan fmt`'s output was ADVISORY: 101 `.vl` files in this repository — seven of them under `vilan/std`, including both of std's preludes — differed from what the compiler in the same commit would have written for them, and nothing said so.
  593  tooling     CI's verdict took 30 minutes for a suite that runs in about 4.5 warm, and none of the difference was test time: `test(ubuntu)` 25 min and `test(windows)` 30 against every other job at or under 3, on runners that compiled the workspace from scratch every single run.
  594  tooling     CI's gate commands were a list you could only run by reading a YAML file and retyping six commands, and `scripts/ci-local.sh` is that list with a runner around it — called by `ci.yml` itself, so the two cannot come apart.
  595  tooling     `vilan check ~/code/app/src/sidebar.vl` compiled that file against whichever checkout the SHELL happened to be standing in — 37 `macro PartialEq's definition did not compile` typed from the vilan tree, 0 typed from the application's own directory, on one unchanged file.
  596  tooling     Five of eight concurrent runs of the corpus's filesystem program die on `ENOENT: no such file or directory, open 'file-corpus.txt'` — and the two corpus differentials run it concurrently by construction.
  597  tooling     The macro engine's conformance gate was the last whole-corpus loop in the tree: ONE test over 124 programs, and a private node deadline of 30 s the two differentials had already moved off.
  598  tooling     `vilan fmt` gives a struct literal's field one spelling: `A { x = x }` is printed as `A { x }`.
  599  tooling     The normative grammar listed `!` and `?.` as postfixes and had no production for the bare `?`, which has been in the language since 2026-07-16.
  600  tooling     E124's dead-code paint shipped with five of the paper's pins unbuilt, a withdrawal that cleared a whole package on any keystroke, and a union that paid its entries one after another. All three are closed, and the one open question in it is decided.
  601  tooling     The keystroke gate's per-request budgets are RELEASE figures and the gate asserted them in whatever profile it was run in — so two Order 27 lanes measured completion at 0.705-0.813 ms against its 0.2 ms budget and reported a 4x regression in a tranche that had not moved.
  602  tooling     Hover on `PartialEq::eq` and `PartialOrd::lt` was reported to answer nothing where `Add::add` answers a signature; it answers one too, and the silence was an import from the wrong module.
  603  tooling     The diagnostics gate was asserted on a program that never had the problem; it now runs on one that does, and it is RED at 1,053 ms of CPU per keystroke against its 500 ms mandate.
  604  tooling     The corpus had a program nothing could ever run, and now it has a rule about that.
  605  tooling     The inference differential still had the shape N49 took off its sibling, and the corpus was written down twice.
  606  tooling     The language server now handles `workspace/didChangeWatchedFiles`, so a package file written by something that is not the editor is offered by the next completion instead of waiting for an unrelated edit.
  607  tooling     Hover on a trait's own method showed `fun add(self, b: Add): Add` — a signature nobody can write.
  608  tooling     `vilan fmt` now reformats one-link chains over 100 columns.
  609  diagnostics A two-value element attribute reports on its comma in both element spellings, instead of asking for a `;` on the closing tag.
  610  diagnostics An unfinished `a::` path now names the name it is missing, instead of asking for a `;` on the `::`.
  611  tooling     The changelog's family set gains `fix` and `performance`, and the cut orders six ranks.
  612  tooling     The release differential is one test per corpus program now, and the 600 seconds it was really spending turn out to be one program that never exits.
  613  tooling     Seven more exemption tables say out loud that they are still exempting something.
  614  tooling     Two rules from Order 25's seal, which was green locally and red on Windows CI.
  615  tooling     `VILAN_WATCH_LOG` traces what a `--watch` session SAW and what it DECIDED.
  616  tooling     std's `rpc.vl` drops the `0 - 1 // unreachable — the panic throws` that `parse_announced` carried after its `panic`, and stops destructuring a `channel` that `dispose` never reads.
  617  tooling     A chunk that reached another chunk's function had no sound way to name it — the emitted preamble snapshots its dependencies once, at evaluation, so a chunk that lands first binds `undefined` and keeps it — and cross-chunk references are now emitted as registry reads AT THE USE, with the by-value snapshot kept for the names the partition proves EAGER. Latent today, and deliberately so: the route partition cannot produce such a reference at all, so every artifact of the split fixture is byte-identical and the change is inert on every plan v1 can make.
  618  tooling     Three piles of follow-on diagnostics B182's stand-down did not reach are gone, each of them a second report about a mistake the compiler had already named once: an unresolved field annotation, a `[derive(Wire)]` built out of a refused annotation's spelling, and an `[expose]` whose refused exposure was restated by the code the `[service]` expansion writes. The three exhibits go 2 → 1, 4 → 1 and 3 → 1, and the kolt-shaped fixture — 21 diagnostics before B182, 4 after it — is now 2, one per mistake actually written into it.
  619  tooling     Two buckets of the `VILAN_PHASE_TIMING` post-pass line are now named after what they time, and the constant that was hiding inside both of them has a bucket of its own.
  620  tooling     E114's remaining two classes ship, and the third one in the item is RULED OUT rather than deferred: a local binding nothing reads is now faded, every statement after one control cannot fall through is faded, and an unused top-level `fun`/`let`/`struct` is not — because Vilan has no private declaration for the item's "unused private declarations" to be about.
  621  tooling     Two corpus goldens had been witnessing the OPPOSITE of the sentence above them since disposal moved to last use, and a corpus program can now NAME the bytes its claim rests on, so a comment cannot go stale under a regenerated golden again.
  622  tooling     The docs twin of that finding: the resources tour opened by stating the law c8609287 REPLACED, two of its programs printed the exact opposite of the output claimed beside them, and the `OwnedNursery` example's background work never ran at all.
  623  tooling     Every std import steer was silently dead in the web playground — the compiler with no filesystem behind it — so the one surface whose visitors most need "import it first (`import std::reactive::Signal;`)" was the one surface that never said it.
  624  tooling     The wall-clock waits run one at a time now, the two bounds that were too small to be liveness bounds at all are liveness bounds, the union's stragglers start first — and the CPU-time cure the item proposed was measured against each family and applies to none of them.
  625  tooling     Three exemption lists that could only ever go green now red when the thing they exempt stops needing exempting, and the mdBook pin is held in every place the repository spells it instead of one.
  626  tooling     The ignored-pins backstop was suspected blind to a `cfg(windows)` pin and is not — it enumerates textually — so the fix is to say so where it can be checked, plus the one dead helper that was blocking a Windows clippy leg.
  627  tooling     B198 ruled: a containment test resolves BOTH its sides or neither, never a case-folded root against a lexical child — and the defect turns out to be reachable on unix too, through a symlink, which is how it was red-proved.
  628  tooling     Three record corrections where the prose and the tree had come apart: ledger rows cited two off after integration, an import described as "still" present that had been deleted, and a character cap counted in bytes.
  629  tooling     Windows' own link spellings — junctions, case folding, the `\\?\` verbatim prefix — are now measured by CI instead of inferred from the unix run, and `vilan fmt`'s cycle guard can finally say "I do not know what this directory is".
  630  tooling     Three compiler steers named a spelling that does not compile, and audit run 7 walked each one into the refusal it leads to; all three now name a spelling that does, pinned by BUILDING the program the message draws and applying the message's own words to it.
  631  tooling     An import nothing uses is now FADED in the editor rather than left looking live — the first of the three gray-out classes, and it is paint: a hint carrying `DiagnosticTag::Unnecessary`, out of the Problems count and out of every gate.
  632  tooling     The language server's line-index cache no longer answers for text that is gone: an entry is served only while the file still matches the length and modification time it was built from.
  633  tooling     The formatter breaks after `|| ` when a closure's element body has to split, so the opening tag, the children and the closing tag are all measured from one column.
  634  tooling     An element head's closing `>` keeps its highlight when it lands on a line of its own: the parse now records the element's angle brackets and the language server paints them, so the shape a head is written in cannot change what it looks like.
  635  tooling     Writing the `import pkg::…` that reaches a file now re-colors that file in the editor, no restart: a `pkg::` graph edit sweeps every open document in the package, and a saved `vilan.toml` sweeps them again — which it had silently stopped doing.
  636  tooling     A ghost diagnostic no longer flashes back after a comment/uncomment round trip: every analysis now carries the world revision it READ, and a result older than the one already landed is dropped instead of published.
  637  tooling     A release whose notes exceed GitHub's 125,000-character body cap publishes anyway: the notes keep whole entries to a safe margin and point at the changelog for the rest.
  638  tooling     Two `[expose]` fields annotated with the reactive TRAIT rather than the cell produced 53 diagnostics for two mistakes, and printed the cascade FIRST — so the owner read a message about code they had never written and never reached the root two pages down. The same package now reports its two roots, once each, at the top of the list.
  639  tooling     All ten example projects adopt the prelude: every manifest states which ambient scope its sources resolve under, and 33 import statements the prelude carries are gone.
  640  tooling     The two e2e server suites stopped guessing a port: the server asks for port 0 and reports back the one it bound, closing a bind-release-rebind window a whole `vilan build` wide.
  641  tooling     A blanket impl never satisfies a bound for an ABSTRACT parameter, and that refusal is now the promise rather than an open question: it is spelled in the spec, pinned as a negative, and the appendix entry says so.
  642  tooling     Spec §2 now writes a production for `ISTRING`, the token class §3 had been using normatively without one.
  643  tooling     A `fun view` of your own captures the element desugar's callee, and the diagnostic now says so instead of reporting an argument count nobody wrote.
  644  tooling     The ledger gate's coverage check had a catch-all row that was holding five diagnostics it has nothing to do with; those five are now rowed, and the ten new rows include the `+` ladder the enumeration can never see.
  645  tooling     The two reserved-package-name refusals disagreed about the reserved set for two releases; they now read one constant, and the set itself is enumerable and checked.
  646  tooling     `book_sync`'s mdBook refusal builds its install line off the pin instead of spelling the version again.
  647  tooling     An `#[ignore]`d pin whose tracker item has been CLOSED is now findable: `scripts/ignored-pins.py --tracker <path>` cross-checks every pin's named owner against the tracker index, and found one on its first run.
  648  tooling     The release gate now carries fmt, clippy and audit, and a gate holds it to CI's legs so it cannot fall behind again.
  649  tooling     An array can carry a trait impl at last: `impl [i32; 2] with Add<i32>` was refused with "`type`'s `add` returns `[i32; 2]`, but `Add` declares `[i32; 2]`" — the same spelling on both sides of a message that contradicted itself.
  650  tooling     A field or method miss on an OVERLAID std type now names which twin you got and WHY this file is analyzed under that platform — one of four reasons, printed.
  651  diagnostics Two refusals for the reverse direction, both at the attribute and both in its own vocabulary.

  breaking: 35
  diagnostics: 62
  feature: 113
  fix: 153
  miscompile: 39
  performance: 61
  tooling: 188

cut-release: refusing to cut - fix the reds above (nothing was changed)
