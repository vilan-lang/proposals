{
 "prefix": "- 2026-09-25 (the owner's rulings after Order 41's seal): ",
 "stamps": {
  "A124": "Q-A25 RULED: under the flip the mirrors lose their owner-strict `map` and the OWNER IS ASKED AT THE SUBSCRIBING LEAF (rec accepted; the 8 A25 pins re-derive in Order 42's reactive lane). Q-no-cycle RULED: the exemplar's `.cell()` goes under an owner and §2.4 states a module-level cell is a leak by design unless released — AND the owner asks whether a separate `.cell_global()` should carry that intention, with `.cell()` REFUSED at module level and steered (filed A130, rec yes). Q-name RULED: keep `Resource<T>` as the TYPE; respell only the constructor — and see B413 (dissolve the `resource` keyword into a struct attribute, which removes the collision entirely).",
  "I6": "RULED as recommended — `as_usize()`/`as_u53()`/… of a negative SATURATES to 0 on every backend (the owner: many numeric operations stay unspecified for performance, but this one is fair and cheap). Order 42, with I5 S2.",
  "A129": "RULED as recommended — a `Splice`'s span gets a KEYED match bounded to the span, so identity survives a re-insert and the op path never builds more than the pass; `set_all`'s doc corrected with it. Order 42, collections.",
  "F40": "RULED (a) as recommended — a second optional crate `vilan-rt-crypto` (OS randomness via `getrandom`, SHA-512 + PBKDF2), linked only when reached, `vilan-rt` itself stays dependency-free and `forbid(unsafe_code)`. The owner asked where the unsafe comes from: OS randomness is a SYSCALL — `getrandom(2)` on Linux, `BCryptGenRandom` on Windows, `SecRandomCopyBytes` on macOS — reached only through FFI (`extern \"C\"` calls are `unsafe` in Rust) or a crate that wraps that FFI; Rust's std exposes no stable safe random source; the one safe route is reading `/dev/urandom` through `std::fs`, which is unix-only; SHA-512 and PBKDF2 themselves are pure computation with no unsafe (SHA-1/SHA-256 are already hand-written in vilan-rt). Order 42, native lane — the last wall before kolt's server.vl builds natively as written.",
  "A128": "RULED as recommended — `FocusScope::on_leave(|to: Element|)` from a capture-phase `focusout` when `relatedTarget` is outside the scope and every scope above it; a null target does nothing; Q2 CONFIRMED (no Tab-out arm under `Wrap`). Order 42, std/ui.",
  "B405": "RULED — REFUSE a fractional literal at an integer-typed position, with an `as_f64()` steer (rec accepted). Order 42, solver.",
  "F27": "R1's five spellings CONFIRMED by the owner (the `;` form; nominal labels affect resolution only; item fences decide only where the package's colour admits none; `internal_use` exempts the declaring module; a deprecated type is silent in its own module) — WITH a counter-proposal for the host of file-level attributes: `[platform(\"browser\")] mod self;`, `self` a reserved module name, and possibly `export *;` → `export self::*` — filed as B415 with the integrator's rec (adopt `mod self;` as the host NOW, before anything uses `[platform(..)];`; defer `export self::*` to a `self::` paths question). R3's six paper questions (platform-coloring.md §8.7) remain UNRULED.",
  "B400": "`peek` KEEPS its name (the owner, 2026-09-25) — and the owner notes that not every keyword needs banning as an identifier (`with` should be usable; declaration keywords `let`/`mut`/`fun`… stay reserved): filed B414 (contextual keywords).",
  "A122": "Q1–Q7 RULED as recommended (2026-09-25): `map` takes only a closure literal; a `for` over a mapped tuple is allowed (concrete tuples stay B183's open Q2); no public `TupleKey` constructor; `divorce` does not gate on `PartialEq` (`.distinct()` at the use site); the comprehension syntax stays beside `map`; `values()` omitted; B397 first (landed in Order 41). The build waits for I5 S2 + A124 S2c and the impl-binder grammar alternative."
 }
}