{"prefix": "- 2026-09-25 (Order 42, syntax-42): ", "stamps": {"B415": "BUILT (756aa82b, BREAKING): `[platform(\"…\")]? mod self;` read at the file's head into the existing `Node::ModulePlatform`; elsewhere refused by `MODULE_SELF_LEADS_THE_FILE` (R1's rule reworded); a nested `mod self { … }` refused by the NEW curated rule `MODULE_SELF_IS_RESERVED` (one ledger row) — premise: `mod self { … }` was accepted before as an ordinary module named self; the bare `[platform(..)];` REMOVED (nothing used it outside R1's own pins/pages — now reads as a list expression, no steer since the form never reached a release); formatter one line; the LSP paints `self` as a namespace declaration; a head-only completion snippet; the quick fix + the book (§3.1 EBNF `module-self`, §4.1, §11.3, the editor appendix row) respelled; `export self::*` NOT built (R-d). ONE CALL without a ruling (OWNER): a bare `mod self;` with NO attribute parses and declares nothing — flip to a refusal if preferred.", "B413": "BUILT (f39ff833, its own commit, BREAKING; R-c decides merge-in-train vs hold): `resource` REMOVED from `KEYWORDS` and `Token::Resource` gone (lexing.rs, token.rs); `[resource]` closes the label prefix ahead of `external`, on a struct AND an enum (the premise correction); the old `resource struct|external struct|enum` refused with the steer (one NEW ledger row, still parses); `[resource]` on fun/impl/trait/local/field/variant gets row 206's message re-keyed; the analyzer unchanged except row 184's steer text; formatter prints `[resource] `; both highlighting grammars regenerated; keyword completion drops `resource`, a NEW `ATTRIBUTE_DOCS` table in vilan-ide carries the hover (`book_sync` checks its links); spec §2.2/§A.2/the tour drop `resource` from the reserved lists and the contextual lists gain `as`, `only`, `internal`, `resource` (D14 done on the way); EBNF `resource-attr`; §6.8, the Resources tour, the errors appendix. CODEMOD `sweeps/order42/syntax-42/b413_codemod.py` (`--check` = the residue gate; declaration positions only) — ESTATE 405 sites: std 5 (task.vl, process/db.vl, process/fs.vl ×3), corpus 8, docs fences 16 (5 pages), Rust `.vl` programs + comments 376 (resources.rs 262, borrows.rs 34, backed_enums.rs 31), examples/benchmarks/templates 0, kolt 0, playground 0, website programs 0 (but its editor keyword list → K24). No golden moved. `destruction-md-b413.patch` (papers) and `b413-held-announcement.md` (the v0.41.0 `deprecates:` entry ONLY if R-c holds it) saved. bindgen `RESERVED` left alone (E225).", "A122": "the impl-binder GRAMMAR ALTERNATIVE BUILT (9cfdb4de): `Node::TypeBinder` gains a boxed `Option<TupleBound>` (node_size green); the binder tries a tuple bound before the trait-bound list; `register_subject_binders` records it in `tuple_bounds` as for a generic parameter; EBNF §3.9 + §3.3 prose; `impl type T: (2..) with Arity` reaches a 2- and a 3-tuple, `5.arity()` refused ('i32 is not a tuple'), a pair under `(3..)` refused ('requires at least 3'). ORDER 43 solver work, not built: a `T: (2..)` value read as `(U in T: U)` (§4.2), the TupleKey family rule (§4.3), the `for` over a mapped tuple checking its body once with the element abstract (§4.4)."}}