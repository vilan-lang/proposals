# Order 42 — running notes (cycle 60; GO 2026-09-25 off vilan next @d65d4e75)

## GO
- The owner: "Go with all recs." R-a–R-e as recommended; stamped A130, B413, B415, I5 (stamps42-go.json); the cut plan's Q1–Q5 recorded in go-items42.
- Ledger next id 570; shared_census 143; 35 unreleased breaking entries; kolt RED 4/4 (A125, owner-owed); website 7 uncommitted (owner's).
- Worktrees: vilan/.claude/worktrees/{std,solver,collections,native,syntax,index,reactive}-42 off origin/next @d65d4e75; proposals/.claude/worktrees/papers-42 (branch papers-42).
- Merge order: std, solver, collections, native, syntax (rebased), index (rebased), reactive (rebased, LAST); then the seal, CI, THE CUT.

## mid-order
- syntax-42 QUESTION: `resource enum` is a shipped form (the brief said struct only — my under-count). ANSWERED (a): `[resource]` on struct AND enum, a pure respelling; stamped on B413.

## papers-42 — REPORTED 2026-09-25 (branch papers-42 MERGED → proposals main): B414 `contextual-keywords.md` (28 hard / 6 contextual; `as`/`sync` already contextual; Q1–Q6 OWNER — Q6 relayed to syntax-42: B413 must DELETE the `resource` KEYWORDS row); the as-built notes in reactive-pipeline §11 (B411 correction), incremental-collections §13 Q5, index-type §5.4/§9 (17 goldens move — cut-plan §3.1 invalidated), signal-cell-representation §15 (C14 re-scoped; S5 rec DROP — owner); `release-notes-summary.md` (5,215 chars; finalize table with six [[FINALIZE]] lines + [[SPELLING]]); HEADROOM re-measured: 12,559 before the summary, 7,595 after — the train's ≥ 3 breaking entries at ~2,771 chars each EXCEED it → a breaking entry would DROP from the release body. OWNER/INTEGRATOR CALL: raise `CAP` in release.yml 110,000 → 120,000 before the tag (GitHub's body cap is 125,000). FILED: F41 (native `r#self`/`r#super` field emission — live), B416 (`[derive(Wire)]` local vs a field named `deserializer`), B417 (a method named `Self`), D14 (lexical §2.2 omits `as`/`only`), E225 (bindgen RESERVED drift). Four ⟦INTEGRATOR⟧ placeholders in the papers for the sweep.

## collections-42 — REPORTED 2026-09-25 (tip 01eb9b00; pushed origin/collections-42; MERGING ahead of std-42/solver-42 — no overlap): A129 c64bf317 (the span pass; walk `each_by` 1,078 → 575 vs the pass's 557 — the last 18 are build-and-cut-in-one-drain → A131), M87 01eb9b00 (`locate` 2.88 M → 21.9 K Ir). `peek` after B400: the re-spelling is SAVED (`collections-42/peek/peek-weak-get.patch`, Weak::get; JS green; native `WeakGet` unemitted but nothing native reaches peek) — solver-42 told to apply it IN the B400 commit (else std goes red at solver-42's merge). Gates: union 8,526/8,526 at loadavg 21–64; natdiff 46/46 both; goldens unmoved; census unchanged. FILED: B418 (MISCOMPILE: elision through if/match/arguments — B400's sibling; solver-42 told), F42 (KeyedCell not native — 'unbound generic parameter 2 of Map', B406's shape?), A131 (deferred build for build-and-cut rows — not queued).
- reactive-42 INTERIM: A130 committed 4db5b03d; the flip applied uncommitted; B408 alone does NOT green its 5 reds — S1 (a blanket over a supertrait through a one-block subtrait impl, B243's form — pre-existing) → B419; S2 (abstract `S: Src<i32>` does not satisfy a blanket-provided `Up<i32>` — B408's satisfaction twin) → B420, cured for std by B409 retiring `Upstream` → B409 MOVED UP in solver-42's queue (right after the four); N1 (native refuses the `Upstream` shape) → F43; M1 (dyn coercion accepts a void closure body — MISCOMPILE, NaN) → B421. If B419 is unlanded by reactive-42's merge: the b243 pin re-derives to the defaults, the blanket case waits for Order 43.

## syntax-42 — REPORTED 2026-09-25 (tip 9cfdb4de on d65d4e75; NOT pushed — 1Password down; merges in the train after native-42, rebased by its agent): B415 756aa82b (`mod self;` the host; the bare form removed; one call: an attribute-less `mod self;` parses and declares nothing — OWNER), B413 f39ff833 (`resource` OUT of KEYWORDS; `[resource]` on struct + enum; codemod 405 sites, no golden moved; the held-announcement file if R-c holds it; D14 done on the way), A122's grammar alternative 9cfdb4de. Gates: union 8,538/8,538, natdiff 46/46 env, doc/clippy/fmt 0, grammar/markdown/book_sync/ledger/node_size green (RULE_STATEMENT_SITES 50 → 52). FILED K24 (the website's playground editor keyword list drifts — the owner's tree). B413's merge decision (R-c): ready NOW — it rides the train.

## std-42 — REPORTED 2026-09-25 (tip d2e499c1 on d65d4e75; NOT pushed — 1Password down; merges FIRST): A128 7420825e (as ruled; two choices flagged — OWNER), N130 0cd28e75 (the Windows leg = clippy -D warnings excluding vilan-rt-sqlite; seal.sh:17 changed to match — INTEGRATOR, uncommitted), N129 73224200 (per-test staging; cargo test 46/46), N131 d2e499c1 (documented; the M19 panics = a real key collision → B422), N128 8dc1a941 (all three parts), N132 37cba598 (option (b): the honest pin; one analyzer.rs thread-local), N127 (script placed at scripts/integration/estate_check_playground.sh; the playground's db.vl + fib.vl dead `import std::print` FIXED by the integrator; wire into the sweep), E224 STOPPED (OWNER Q: function import leaves warn vs type leaves stop — rec the paper stands). Gates: union 8,531/8,531; natdiff 46/46 both; Windows clippy 0; wasm clippy 0; no golden, no ledger rows, no quick fix. Outside its map: analyzer.rs (N132's thread-local), stack_guard.rs, ci-local.sh, CLAUDE.md, AGENTS.md, the DOM stub, the replay harness.
- index-42 INTERIM: B406 also refuses an ANNOTATED LET (`let a: Shared<u53> = Shared::new(0)`) — solver-42 told to pin it under B406; B423 filed (an `if` arm's literal not typed from its sibling arm in an unannotated let). std's S2 residue is ZERO; kolt hits both (index-42 writes its edits around them).
- index-42 INTERIM (full): I6 + the diagnostic/quick fixes/`vilan check --fix` committed; the S2+S3+S4 estate move UNCOMMITTED on d65d4e75 (residue zero; 29 goldens + split move, all runtime-identical — ACCEPTED); kolt-i5.patch (five files); waits on solver-42's merge to rebase; an inference-const helper agent still running. Appendix count word → Eighteen (fold at the merge). reactive.vl carries index edits — reactive-42 told.

## native-42 — REPORTED 2026-09-25 (tip f1511065, 8 commits on d65d4e75; pushed origin/native-42; merges after collections-42, before the train): THE EXIT — kolt's REAL server.vl builds natively with 0 host gaps, login byte-identical (tokens masked), cross-backend over one db file; F40 `vilan-rt-crypto` (getrandom only; SHA-512/HMAC/PBKDF2 hand-written; opt-level 3 in dev); vilan-rt changed once (`Shared::identity` → an i32 counter); THIRD-PARTY-NOTICES regenerated (+4 packages); F35 (+ a MISCOMPILE: assignment value-before-place, subscripts hoisted), F36 (a) built, (b) premise false; F37 = precision in the last-use pass (copy census down across the board); F38 (generics bound from arguments; `or_else<F>` → B424 ruling); F39 decided: static alias-following + a runtime REENTRANT_READ stop; C14 = the native leak gate (first finding: map_each LEAKS → A132; a server cannot be censused → F45). Whole set 124 / 93 / 31 / 0. Gates: union 8,553 + 2 count-word fixes (f1511065; not re-run whole), doc/fmt/clippy/audit/notices/release/markdown/book_sync green; the Windows leg red only on libsqlite3-sys cross-compile (pre-existing; N130's exclude). FILED: A132, B424, B425, F44, F45. For reactive-42: `attach`'s reissued workaround can go; the flip moves the copy census AND the leak census's minted column — regenerate both; a non-zero LIVE count after the flip is a leak to report. Integrator: `main_guard` now requires a `Send + 'static` body.
- index-42 ADDENDUM: the inference `.vl` sweep is green (4,588/4,588; 15 files; five subscript pins say 'an index must be a usize'); `markdown.rs::substring_with_a_computed_negative_end` (`let end = 0 - 1;`) now infers usize and relies on UNSPECIFIED underflow — told: re-derive it to B407's refusal after the rebase.

## MERGES: std-42 → next 165d5446 (clean; natdiff 46/46 both; cli set 228; core set 248; lsp 882; release 23; split 13); collections-42 → next d33d60bb (clean; same gates green). native-42 MERGING (→ merge-native-42.log, + audit for the new dependency). Then: solver-42 (report awaited) → the train: syntax-42, index-42, reactive-42, each rebased by its agent onto the tip before it.
- MERGED native-42 → next eb14929b (clean; natdiff 59/59 both; cli set 229; core 18; lsp 882; release 23; split 13; audit clean with the new crate). syntax-42 told to rebase onto eb14929b and re-gate (merges next, before solver-42 if solver has not reported); then solver-42; then index-42 and reactive-42 rebased onto the tip that carries solver-42.
- syntax-42 REBASED onto eb14929b (tip 1404fdf7: B415 0128a8e8, B413 95ff783b, A122 1404fdf7; CHANGELOG-only conflicts; no old spelling reintroduced; union 8,578/8,578, natdiff 59/59 env, doc/clippy/fmt 0; no golden moved); pushed --force-with-lease; merges right after solver-42's four (merging now).
- MERGED solver-42's FOUR blockers (first solver merge, tip d4cb70a1) → next eafd61ef (clean; natdiff 59/59 both; cli set 149; vilan-core 6,492; lsp 882; release 23; split 13); the lane continues on top (B409 next) for a second merge. index-42 SIGNALLED to rebase onto eafd61ef and run its last step; reactive-42 told to rebase now and wait for B409 (fallback prepared: ship with `Upstream` + exclusions by name). syntax-42 MERGING onto eafd61ef.
- index-42 → solver-42 (relayed): B406 RESIDUAL — an earlier `Shared<i32>` field in the same struct literal poisons the next field's literal (rpc.vl's RemoteSource/KeyedSource shape); index-42 keeps the two `0usize` suffixes; stamped on B406; solver-42 takes it after B409.
- MERGED syntax-42 → next badf2bfb (clean; natdiff 59/59 both; cli set 217; core set 4,632; lsp 884; release 23; split 13). B413 RODE THE TRAIN (R-c's condition met: its codemod + sweep were green by index-42's merge) — the held-announcement file is NOT used; B415 `mod self;` in; A122's grammar alternative in. SIX lanes on next; remaining: index-42 (landing), reactive-42 (waits on B409), solver-42's second merge.

## index-42 — LANDED on its branch 2026-09-25 (tip 98039c83 on eafd61ef; pushed; MERGING onto badf2bfb → merge-index-42.log): ONE breaking commit 98039c83 (S2+S3+S4 + the estate) + I6 + the diagnostic/quick fixes/`vilan check --fix`; rebase conflicts: CHANGELOG, the native copy census (regenerated), the ledger (solver's 570 then two NEW), rpc.vl `locate` (M87's body, usize signature), ui.vl (A129's split kept, markers dropped); after the rebase A129's `live_ops` `-1` (a ninth sentinel) → Option<usize>, two native-42 probes converted. B406 residual → suffixes kept; B407 misses a constant `0 - 1` → B426 filed. Goldens 32 (30 classified + 2), all runtime-identical. Union 8,631; natdiff 60/60. Count word Eighteen (fold if book_sync reds at the merge).
- MERGED index-42 → next 73de48f6 (CLEAN — no fold needed against syntax-42; natdiff 60/60 both; cli set 272 incl. check_fix; core set 4,645; vilan-ide 26; lsp 885; release 23; split 13). THE `usize` CAR IS ON NEXT. Seven lanes merged; remaining: reactive-42 (waits on B409) + solver-42's second merge.
- MERGED solver-42's SECOND batch (B409 49b1c930, B405 1e911fba, B406 follow-up 54acf502, B410 3332632f; tip 3332632f) → next 0ed1864e (clean; natdiff 60/60 both; cli set 208; vilan-core 6,536; lsp 885; release 23; split 13). reactive-42 SIGNALLED for its final rebase onto 0ed1864e (retire `Upstream` + the per-node on_settle; the B420/F43 reds should go) → merges LAST. solver-42 continues (B411 …) for a third merge.

## reactive-42 — LANDED on its branch 2026-09-25 (A130 8ff8dd55 + the flip 490f0fb1 on 0ed1864e; pushed; MERGING LAST → merge-reactive-42.log): the five combinators return nodes; `Upstream` retired on B409 (F43 closed), per-node on_settle → `subscribe_pulling` on B410; A25 at the leaf as built; `Resource<T>` with `source.load(fetch)`; the 19 reds re-derived (b243's blanket case parked under #[ignore = B419]); the no-cycle gate with `.cell_global()` in `designedLoops`; census 143 → 141; 12 corpus + 4 split goldens runtime-identical; leak census LIVE unchanged. Union 8,675; natdiff 60/60; whole set 125 / 94 / 31 / 0. Playground client.vl:22 → `route.cell()` (integrator applies); kolt 14 edits (`kolt-edits.md` + `kolt_edits.py`). FILED: F46, B427, A133 (design), E226. papers-42's ⟦INTEGRATOR⟧ placeholders get the as-built + remote-sources §2b sentences at the sweep.

## solver-42 — REPORTED 2026-09-25 (tip 76d3affe; third batch READY: B411 43b6d46e, B412 3abc8f4f, B404 bdcbde61, E223 e9f2512f, B421 76d3affe — merges after reactive-42, before the seal): twelve items fixed across three batches; B405 narrowed to the right-of-an-integer case; B406's residual closed by the follow-up (index-42's suffixes can drop in Order 43); `Upstream` + per-node on_settle retirements confirmed; the phantom params can retire for Switch-shaped nodes (Order 43). STOPPED/UNBUILT for Order 43: B400 (stop condition — OWNER Q: scope the check vs respell std; the patch incl. peek's re-spelling saved to sweeps/order42/solver-42/), B403 (OWNER Q: bare `Type::f()` inside its own impl = `Self::f()`? rec yes), B420 (ruling vs B173 — rec by-design), B401 (restructure), B418 (a value-level miscompile too: `mut b = if flag { a } else { [] }; b.push(3)` mutates `a` on JS), B419 (analysed), B423 (untouched). FILED: B428 (`i8 = 128` accepted), B429 (`-x` on an unsigned value), B430 (MISCOMPILE: a tuple variable at a `(dyn A, dyn B)` position), B431 (a generic erased to dyn instantiated at a resource escapes Q5). Gates at 76d3affe: union 8,582/8,582; natdiff 46/46 env; no golden moved; clippy/fmt 0; doc-tests exit 0 but 0 ran (the seal's leg is the check).
