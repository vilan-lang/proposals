fun half(n: i32): i32 { n }
fun main() {
	let y: i32 = 3;
	print(y / 2.0);
	print(2.5 * y);
	print(1 / 4.0);
	let z: i32 = 2.5;
	print(half(1.5));
	print(y + 0.5);
	let f: f64 = 3.0;
	print(f / 2.0);
}
