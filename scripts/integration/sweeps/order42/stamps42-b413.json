{"prefix": "- 2026-09-25 (Order 42, syntax-42): ", "stamps": {"B413": "PREMISE CORRECTED — `resource enum` is a SHIPPED form (parse_enum takes `resource? enum`; the analyzer reads the flag at ~5 sites; corpus resource_take.vl declares two; B117's pins declare a payload-less `resource enum Handle { Open = 1, Closed = 2 }` that containment cannot express; no doc mentions it). The brief said struct only — the integrator's under-count. RULED by the integrator within the ruling's meaning: `[resource]` on `struct` AND `enum`, a pure respelling, every shipped meaning kept, the codemod moves both; refused everywhere else."}}