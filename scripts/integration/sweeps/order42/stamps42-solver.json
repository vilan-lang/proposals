{"prefix": "- 2026-09-25 (Order 42, solver-42): ", "stamps": {"B408": "FIXED (ca0dd51f): for a bounded parameter, method lookup falls back to blanket impls whose bounds are ENTAILED by the parameter's declared bounds (arguments, supertraits, nested-binder bounds); an inherent blanket member wires statically (B359's rule), a trait member a blanket provides re-dispatches to the most specific impl at monomorphisation; types.md; 11 pins (8 red-first). The flip's `map` blanket reaches through abstract bounds and `dyn`.", "B398": "FIXED (9da0a677): a tuple LITERAL at a mapped-tuple position takes its element constraints from the mapped type and each element is erased at its own `dyn` position; native still refuses mapped tuples by name; 3 pins. Remainder: a tuple VARIABLE at the position escapes → B430.", "B406": "FIXED (68c05c30 + 54acf502): the literal rule bound only the function's own generics — `Shared::new`'s `T` belongs to the IMPL, so annotated lets and declared returns were refused too (premise widened as index-42 found); impl generics now bind from the expectation and each field's type seeds its expectation when closed; the follow-up seeds EVERY field before any is checked (the order-dependent residual gone); a B225 regression (kolt's shape) caught and fixed; pins over u53/usize/u8/f64, let/return, `Signal::new(0)`, both field orders. index-42's two `0usize` suffixes can now drop (Order 43 hygiene).", "B407": "FIXED (d4cb70a1): a negative literal at an unsigned type refused naming the range (the check reads B389's recorded literal types); 12 pins incl. the seven §3.4 sentinel shapes; one ledger row; errors.md. Remainders: B426 (a constant `0 - 1`), B428 (`i8 = 128` accepted), B429 (`-x` on an unsigned value).", "B409": "FIXED (49b1c930): a `type X` in a trait's HEAD was registered as the trait's own parameter id, so `with Src<U>` rebound it — a trait head now passes on its parameter's BOUND, not its identity; two more found on the way (a trait default's JS instance dropped the enclosing instance's bindings; emission left a blanket provider's bound binders ungrounded) fixed; native: `bind_subject_and_bounds` called at one place in vilan-rust's `dispatch_to_member`. reactive.vl's `Upstream` retired on it.", "B405": "BUILT as ruled, NARROWED (1e911fba): only a fractional literal to the RIGHT of an integer operand is refused (on the left it already makes the expression f64 — vilan/benchmarks' `1000.0 * count` — the deferred mixing family); B402's literal-only case green; one ledger row; lexical.md.", "B410": "FIXED (3332632f): `impl_select::instantiation_agrees` compared tuple and array arguments by type ID — element-wise now, both backends; 3 pins. The nodes' per-node `on_settle` retired on it.", "B411": "FIXED (43b6d46e): inherited-default bindings ground a nested binder's bound binders; `mono::trait_parameter_substitution` likewise (native needed it for the node shape); 3 pins. The phantom parameters can retire for a node that reads its value type from the source it selects (`Switch`-shaped); `Map` keeps `T` (its `transform` field names it) — reactive's call, Order 43.", "B412": "FIXED (3abc8f4f): a rigid parameter whose bounds provide a trait erases to `dyn` in reconcile and compare, binding a mapped template's open arguments from the bound (works with B398); the emitters pass an object through unwrapped (one guard in vilan-rust's `erase_into_object`); 4 pins. Remainder: B431 (a generic instantiated at a resource escapes Q5).", "B404": "FIXED (bdcbde61): a literal whose settled type is BigInt emits `3n`; the literal-let exclusion of BigInt stays, its comment corrected; 1 pin.", "E223": "FIXED (e9f2512f): the refusal names the `(2..)` bound (same ledger row 80); 2 pins.", "B421": "FIXED (76d3affe): dyn erasure checks the object's trait ARGUMENTS as well as the trait, so the `NaN` program is refused; 2 pins.", "B400": "STOPPED (stop condition): the closure `&`/`&mut` view check refuses legitimate std code — rpc.vl `KeyedCell::update`'s `mutate(list[at])` at a `|&mut T|` parameter (~line 4130) — failing `check_scope_differential::every_std_module_is_clean_under_full_scan` and `vilan-wasm a_server_program_checks_clean_for_node`. OWNER Q: respell std as `&mut list[at]`, or scope the check (rec: scope the check — a `|&mut T|` parameter fed an lvalue subscript is not the temporary-read hazard; the hazard is `cell.read()` as the argument). The full patch (check, pins, AND collections-42's peek re-spelling, applied with -p0, M86 green, shared_census 2/2) is saved at scratchpad/solver-42/b400-closure-view-check.patch — copy it into proposals before the scratchpad is cleaned. The `for x in cell.read()` half is a copy-POLICY question (shared.vl promises reads copy nothing). Order 43.", "B403": "NEEDS A DESIGN RULING (not built): the refusal trips std's JSON statics — `Option::`/`List::`/`Result::from_json_value(value)` called by bare path INSIDE their own impls (json.vl ~278/296/1521) work only because the enclosing instance supplies `T`; the item's text was wrong (std uses bare paths, not `Self::`). OWNER Q: does a bare `Type::f()` inside `Type`'s own impl mean `Self::f()` (rec YES — then the refusal applies only outside the impl, and the native miscompile's shape is exactly the outside case)? Repro scratchpad/solver-42/p/b403.vl. Order 43.", "B420": "NEEDS A RULING: it contradicts B173 as written in the spec ('never from an abstract one'); MOOT for std once `Upstream` retired (done). Rec: leave B173 as the law and close B420 as by-design, with the std-free repro kept as a pin of the refusal. Order 43.", "B401": "NOT STARTED (a restructure): the admission map is built after the build while the inherited-default ambiguity is decided during the walk. Order 43.", "B418": "NOT FIXED — wider than reads: the emitters copy only at the initializer node, so a copy inside an if/match branch needs its own emission point in both backends + a copy-census re-measure; `mut b = if flag { a } else { [] }; b.push(3)` MUTATES `a` on JS (a=3 vs native a=2) — a value-level miscompile, not just reads; the by-value-argument half is a copy-policy question. Order 43, solver + native.", "B419": "ANALYSED, not built: needs the subtrait closure in `type_implements_trait`, `trait_args_for`, `type_implements_trait_at` and impl_select's `provided_trait_arguments`. Order 43; reactive-42 parked its b243 pin under #[ignore = B419]."}}