{"prefix": "- 2026-09-25 (Order 42, native-42): ", "stamps": {"F40": "BUILT (28bf86d2 + f1511065), THE EXIT REACHED: kolt's REAL `src/server.vl` builds natively with 0 host gaps (premise corrected: 3 gaps on d65d4e75, not 4 — `pbkdf2_sync`, `random_bytes`, `to_string_encoded`; the generic `map` gap was already gone) and its login exchange matches node byte for byte bar the masked random tokens (nine exchanges on each backend over a fresh kolt.db identical; cross-backend over ONE database file: an account registered on node logs in on native and vice versa; a planted PBKDF2 defect makes both answer 'wrong password'). `crates/vilan-rt-crypto`: `getrandom` 0.3, `forbid(unsafe_code)`; `random_bytes`, `random_uuid`, SHA-384/512, HMAC-SHA-512, PBKDF2 (std's four async WebCrypto twins) + node:crypto's `pbkdf2Sync` and its `NodeBuffer` (`toString` hex/base64/base64url/utf8) — SHA-512/HMAC/PBKDF2 HAND-WRITTEN (the RustCrypto route adds six crates; the ruling asked for one dependency at the syscall), pinned to FIPS 180-4, RFC 4231 and node's own output incl. kolt's 100,000-round call; the crate named in the manifest only when reached (`reaches_sqlite` → `OptionalCrates`); the embedded toolchain materialises both optional crates; the crypto crate at opt-level 3 in dev (2.6 s → 0.14 s per hash); the host-table walk deduplicated (`lowered_host_binding`). vilan-rt CHANGED despite the brief: `Shared::identity` returned an i64 address where std declares i32 (5 `cell_identity` sites refused) — now an i32 counter stamped at first ask, as JS; vilan-rt still dependency-free + forbid(unsafe). THIRD-PARTY-NOTICES regenerated (getrandom, r-efi, wasip2, wit-bindgen; Cargo.lock +4). Records in `sweeps/order42/native-42/`. Whole set 124 / 93 / 31 / 0 (was 91/33).", "F35": "BUILT (c562e514) — premise corrected: the `any` wall had already lifted; behind it three lowering fixes (a named function as a value; a `&mut` argument whose place a later by-value argument reads evaluates first — E0502; a `&mut` over a loaned binding reborrows — E0596) and a MISCOMPILE fixed: Rust evaluates an assignment's value before its place, so `list[next()] = next()*10` paired the draws wrong — subscripts hoisted into `let`s in source order; `list-cell.vl` identical, in the default suite.", "F36": "(a) BUILT (cb4a0c32): `i\"{source.get()}\"` judged at the type the call binds; `reactive-selector.vl` identical; `blanket-impl.vl` then hit rustc E0061 (the context pass under-threads `badge(\"static\")` → B425, refused by name); `resource_take.vl` stops at `resource`. (b) PREMISE FALSE: `encode_json` on a `List<i32>` does not reproduce on this base across 11 shapes — pinned as a regression guard.", "F37": "BUILT (6626e54c) — premise corrected: the repro already built (native-41 copies a consumed field conservatively); what landed is PRECISION in the last-use pass — a field read judged by its PATH (moves when every later read touches a disjoint field; a read inside a closure/spawn/loop counts as the whole binding), the pass forks at if/else chains and guard-less match; reactive-41's attach-order workaround builds and matches node. Copy census (copied/moved): dyn-objects 9/21 → 3/27, board 5/15 → 1/19, delta-law 28/46 → 21/53, reactive-on-change 9/24 → 3/30, reactive-flatten 10/23 → 4/29, list-cell 74/136 → 64/146, reactive-selector 15/20 → 10/25.", "F38": "BUILT (61d94fd9): the emitter binds a call's own unbound generics from its ARGUMENTS (the declared closure type against the literal's parameters and body tail; a variant-constructor tail through its arguments; an arithmetic operand through its left side; a concatenation as str); `result-combinators.vl` now stops at `or_else<F>` where nothing constrains `F` (the analyzer records `any`) → B424 (ruling); refused by name.", "F39": "DECIDED and BUILT (fb6c0ac9): the compile-time check FOLLOWS ALIASES — a `let` whose initializer is a place is followed back to the binding it copies (copying a value holding a `Shared` copies the handle); `let t2 = todos; todos.update(|..| t2.get())`, the reverse and alias chains refused by name; a value read out before the update is not an alias (control identical). Why not the runtime option: safe Rust has no second view while `update` holds the only `&mut`; answering the pre-update value would differ from node. An alias arriving through PARAMETERS is invisible statically — it stops at run time with vilan-rt's `REENTRANT_READ` message and exit 1.", "C14": "S4 REPLACED by the NATIVE LEAK GATE (e633b9a2): vilan-rt counts every cell minted and released; under `VILAN_NATIVE_LEAK_CENSUS=1` `main` runs on its own thread and the census is read after it exits (module bindings and executor queues already destroyed with their thread-locals) — a cell still live is a CYCLE; a committed table `native-leak-census.tsv` (the default suite + the board probe); the reactive core ends at 0 live; pins: an acyclic program at 0, a planted cycle at exactly 1 (red with the release count planted out) + the table gate. FIRST FINDING: `std::delta`'s `map_each` leaks 14 cells in delta-law and 92 in list-cell (a handler capturing its own source) → A132. NOT covered: kolt's server exit program (no graceful stop → F45). S5 untouched (papers-42 recs drop — owner). The representation half CLOSED as built by F1 S1a."}}