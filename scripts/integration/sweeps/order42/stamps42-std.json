{"prefix": "- 2026-09-25 (Order 42, std-42): ", "stamps": {"A128": "BUILT exactly as ruled (7420825e): `FocusScope::on_leave(self, handler: |Element| void)` (a process twin accepts and drops); a capture-phase `focusout` on the window; fires when `relatedTarget` is outside the scope and every scope above it AND the element losing focus is inside the scope or one above; null target silent; the listener under the scope's own owner (`FocusScope` gains `owner`); the DOM stub dispatches `focusout` and models `blur()`. TWO CHOICES flagged (OWNER): the losing-element test (without it any page-to-page move would fire); the unit return type. Pin: one ordered transcript (Wrap Tab silent … Contain click-outside fires then the guard pulls back); three mutations red. Docs ×2.", "N130": "FIXED (0cd28e75) with a correction: the seal's Windows line already had `--tests` — the gap is `cargo check` (no `-D warnings`); the Windows cross-check is now `cargo clippy --target x86_64-pc-windows-msvc --workspace --exclude vilan-rt-sqlite --all-targets -- -D warnings` in `scripts/ci-local.sh windows` (the old leg was red on every Linux box — vilan-rt-sqlite's bundled C build has no msvc compiler); seal.sh line 17 changed by the integrator to the same command.", "N129": "FIXED (73224200): staging is `native-differential-src-<pid>-<counter>`; plain `cargo test` 38/46 red → 46/46; CLAUDE.md's equivalence sentence names its condition (process-wide state private per test under nextest, shared under cargo test).", "N131": "INVESTIGATED, documented (d2e499c1), not fixed (analyzer.rs is the solver's): the cache pins read other tests' worlds (`BASE_CACHE_LOCK` covers ~33 of ~850 analyzing tests); the five `references::` M19 panics are a REAL KEY COLLISION — two byte-identical std roots under one `BaseCacheKey` → filed B422 (a product bug: one LSP session with documents inside and outside a checkout). CLAUDE.md names vilan-lsp as not cargo-test-equivalent.", "N128": "BUILT (8dc1a941), all three parts: `for_each_child` is the fifth probe funnel (a 2 MiB syntactic pin SIGABRTed before it); the CLI's three spawn sites go through `spawn_compiler_thread`/`spawn_scoped_compiler_thread` (pinned: the body reads the declared size; the literal appears only there); wasm declares `WASM_STACK_SIZE` = 16 MiB (a pin holds release.yml and ci-local to it — ci-local's leftover 64 MiB fixed); re-measured: ~47,400 B/level debug, ~2,120 B release — the stale 11.3 KiB was in AGENTS.md; corrected in three places. The 2 MiB canary sits at 83 % of its thread.", "N132": "OPTION (b) BUILT (37cba598) — the signal was cheap: `analyzer::served_from_base_cache()` (a thread-local set at the entry tail; the one analyzer.rs touch); both legs require every pair that HIT to have reused + a ≥ 90 % hit-rate floor; the budget lift REMOVED; a lock serializes the two tests; load-bearing (5 MiB budget: old pin red, new green; 3 MiB: new red on the floor; replay off: red); the file header fixed; plain cargo test 2/2.", "N127": "the script BUILT (`scripts/integration/estate_check_playground.sh` — `vilan check` per package and loose .vl in vilan-playground; honours VILAN/VILAN_STD; non-zero on red) and placed in proposals by the integrator; its first run found the playground RED — `db.vl` and `fib.vl` still `import std::print` (removed) — fixed by the integrator 2026-09-25 (the playground is not a git repo). Wire it into the sweep at the seal.", "E224": "STOPPED (patch saved at scratchpad/std-42/e224-held.patch): the fix (labels.rs reads type-position rows for every labelled declaration) works — three CLI pins red → green — but it turns the SHIPPED pin `generics::a_std_marked_item_warns_at_its_use` red ('exactly the call site warns; the import line is not a use') and CONTRADICTS proposal/deprecation.md ('an import line alone does not warn … the behavior we want'). OWNER Q: should function import leaves warn (reversing the paper), or should TYPE leaves (E221/B382's deviation) stop warning? Rec: the paper stands — type leaves stop warning (one rule: a use warns, an import does not)."}}