diff --git a/vilan/std/src/browser/ui.vl b/vilan/std/src/browser/ui.vl
index c80ba02..3241d03 100644
--- a/vilan/std/src/browser/ui.vl
+++ b/vilan/std/src/browser/ui.vl
@@ -1330,12 +1330,17 @@ fun place_swap<T: PartialEq, S: Source<T>, C: Slot>(
 fun settled_steps(steps: List<RowStep>): List<bool> {
 	mut forward: List<bool> = [];
 	mut forward_count = 0;
-	mut highest = -1;
+	// I5 S2: "nothing kept yet" is `None`, where it was the `-1` below every index.
+	mut highest: Option<usize> = None;
 	for step in steps {
 		match step {
 			RowStep::Keep(let index) => {
-				if index > highest {
-					highest = index;
+				let rises = match highest {
+					Some(let top) => index > top,
+					None => true,
+				};
+				if rises {
+					highest = Some(index);
 					forward_count += 1;
 					forward.push(true);
 				} else {
@@ -1354,8 +1359,9 @@ fun settled_steps(steps: List<RowStep>): List<bool> {
 	mut backward: List<bool> = [];
 	mut backward_count = 0;
 	mut lowest = steps.len();
-	mut at = steps.len() - 1;
-	for at >= 0 {
+	mut at = steps.len();
+	for at > 0 {
+		at -= 1;
 		match steps[at] {
 			RowStep::Keep(let index) => {
 				if index < lowest {
@@ -1370,7 +1376,6 @@ fun settled_steps(steps: List<RowStep>): List<bool> {
 				backward.push(false);
 			},
 		}
-		at -= 1;
 	}
 	if forward_count >= backward_count {
 		forward
@@ -1404,22 +1409,25 @@ fun held_span<E>(held: Shared<List<E>>, at: usize, count: usize): List<E> {
 /// where the pass alone, reading only the list the drain settles on, never builds
 /// them.
 fun live_ops<T>(ops: List<SeqOp<T>>): List<SeqOp<T>> {
-	mut last = -1;
+	// I5 S2: "no `Reset` in the drain" is `None`, where it was the `-1` below
+	// every position (B407 refuses the negative at a `usize`).
+	mut last: Option<usize> = None;
 	mut index = 0;
 	for op in ops {
 		match op {
 			SeqOp::Reset(let _items) => {
-				last = index;
+				last = Some(index);
 			},
 			_ => {},
 		}
 		index += 1;
 	}
-	if last <= 0 {
-		ret ops;
-	}
+	let from = match last {
+		Some(let at) if at > 0 => at,
+		_ => ret ops,
+	};
 	mut live: List<SeqOp<T>> = [];
-	index = last;
+	index = from;
 	for index < ops.len() {
 		live.push(ops[index]);
 		index += 1;
@@ -1442,8 +1450,9 @@ fun row_references(
 ): List<Text> {
 	mut references: List<Text> = [];
 	mut reference = anchor;
-	mut at = steps.len() - 1;
-	for at >= 0 {
+	mut at = steps.len();
+	for at > 0 {
+		at -= 1;
 		references.push(reference);
 		if settled[at] {
 			match steps[at] {
@@ -1453,7 +1462,6 @@ fun row_references(
 				_ => {},
 			}
 		}
-		at -= 1;
 	}
 	references.reverse()
 }
@@ -2549,16 +2557,19 @@ export fun focus_scope(root: Element, containment: FocusContainment): FocusScope
 		if order.len() == 0 {
 			ret;
 		}
-		mut index = 0 - 1;
-		mut position = 0;
+		// I5 S2: "focus is on no tab stop" is `None`, where it was the `0 - 1`
+		// below every position (a sentinel the `-1` census missed by its spelling).
+		// `order` is not empty here, so `last` does not underflow.
+		mut index: Option<usize> = None;
+		mut position: usize = 0;
 		for candidate in order {
 			if candidate.matches(":focus") {
-				index = position;
+				index = Some(position);
 			}
 			position = position + 1;
 		}
 		let last = order.len() - 1;
-		if index == 0 - 1 {
+		if index is None {
 			// Focus is inside the scope (or inside one above it) and is not on a
 			// tab stop: the panel ITSELF, holding focus at `tabindex="-1"` because
 			// nothing inside it was tabbable, or an element that stopped being
@@ -2572,13 +2583,13 @@ export fun focus_scope(root: Element, containment: FocusContainment): FocusScope
 				Some(let target) => target.focus(),
 				None => {},
 			}
-		} else if event.shift_key() && index == 0 {
+		} else if event.shift_key() && index == Some(0) {
 			event.prevent_default();
 			match order.get(last) {
 				Some(let target) => target.focus(),
 				None => {},
 			}
-		} else if !event.shift_key() && index == last {
+		} else if !event.shift_key() && index == Some(last) {
 			event.prevent_default();
 			match order.get(0) {
 				Some(let target) => target.focus(),
diff --git a/vilan/std/src/json.vl b/vilan/std/src/json.vl
index 42720b0..9221ca6 100644
--- a/vilan/std/src/json.vl
+++ b/vilan/std/src/json.vl
@@ -663,12 +663,13 @@ export impl JsonReader with Deserialize {
 			// Where the stack stands once every element has been read: what
 			// `end_list` holds the caller to.
 			self.list_marks.push(self.stack.len());
-			mut index = elements.len() - 1;
-			for index >= 0 {
-				self.stack.push(elements[index]);
+			mut index = elements.len();
+			for index > 0 {
 				index -= 1;
+				self.stack.push(elements[index]);
 			}
-			elements.len()
+			// WIRE: `begin_list` answers the codec's `i32` length (§2.2).
+			elements.len().as_i32()
 		} else {
 			0
 		}
@@ -712,10 +713,10 @@ export impl JsonReader with Deserialize {
 					self.stack.push(payload);
 				} else {
 					let elements = payload.elements();
-					mut index = elements.len() - 1;
-					for index >= 0 {
-						self.stack.push(elements[index]);
+					mut index = elements.len();
+					for index > 0 {
 						index -= 1;
+						self.stack.push(elements[index]);
 					}
 				}
 			} else {
diff --git a/vilan/std/src/list.vl b/vilan/std/src/list.vl
index 2c784f7..24d0d65 100644
--- a/vilan/std/src/list.vl
+++ b/vilan/std/src/list.vl
@@ -77,10 +77,10 @@ impl List<type T> {
 	/// which is a different method and belongs to the iterator paper.)
 	fun reverse(self): List<T> {
 		mut result = List::new();
-		mut index = self.len() - 1;
-		for index >= 0 {
-			result.push(self[index]);
+		mut index = self.len();
+		for index > 0 {
 			index -= 1;
+			result.push(self[index]);
 		}
 		result
 	}
diff --git a/vilan/std/src/markdown.vl b/vilan/std/src/markdown.vl
index 07c130b..6e1b34a 100644
--- a/vilan/std/src/markdown.vl
+++ b/vilan/std/src/markdown.vl
@@ -93,6 +93,11 @@ use Inline::{ Code, Emph, Html, Link, Strong, Text };
 
 /// The index of `needle` in `text` at or after `from`, or `None`.
 fun index_of(text: str, needle: str, from: usize): Option<usize> {
+	// I5 S2: `text.len() - needle.len()` underflows when the needle is the longer
+	// of the two, so that case is answered first.
+	if needle.len() > text.len() {
+		ret None;
+	}
 	let limit = text.len() - needle.len();
 	mut index = from;
 	for index <= limit {
diff --git a/vilan/std/src/option.vl b/vilan/std/src/option.vl
index 1736ab5..bd0e75a 100644
--- a/vilan/std/src/option.vl
+++ b/vilan/std/src/option.vl
@@ -428,10 +428,15 @@ impl List<type T> {
 		self.get(0)
 	}
 
-	/// `get(len - 1)`: for an empty list `len - 1` is `-1`, which `get` reports as
-	/// `None`.
+	/// The last element, `None` for an empty list. (I5 S2: it was `get(len - 1)`,
+	/// relying on `-1` reading back as `None` — an unsigned `len - 1` underflows
+	/// on an empty list instead, so the empty case is answered first.)
 	fun last(self): Option<T> {
-		self.get(self.len() - 1)
+		if self.is_empty() {
+			None
+		} else {
+			self.get(self.len() - 1)
+		}
 	}
 
 	/// The first element for which `predicate` holds — `None` when none does.
diff --git a/vilan/std/src/process/db.vl b/vilan/std/src/process/db.vl
index 7710b90..de26670 100644
--- a/vilan/std/src/process/db.vl
+++ b/vilan/std/src/process/db.vl
@@ -323,21 +323,27 @@ fun refuse_database_ahead(names: Set<str>, recorded: Set<str>, count: i32) {
 /// with history this refusal catches every misordering such a rule would, and on a
 /// fresh database any list order is self-consistent — it IS the history.
 fun refuse_inserted_migration(migrations: List<Migration>, recorded: Set<str>) {
-	mut last_applied = -1;
+	// I5 S2: "nothing applied" is `None`, where it was the `-1` below every index.
+	// (After S2 the `-1` COMPILES as a `usize` — the binding takes its type from
+	// `last_applied = index` and a negative literal is not refused at an unsigned
+	// type — which is why this sentinel is a hand edit and not a diagnostic.)
+	mut last_applied: Option<usize> = None;
 	mut index = 0;
 	for index < migrations.len() {
 		if recorded.contains(migrations[index].name) {
-			last_applied = index;
+			last_applied = Some(index);
 		}
 		index += 1;
 	}
-	mut earlier = 0;
-	for earlier < last_applied {
-		let pending = migrations[earlier].name;
-		if !recorded.contains(pending) {
-			let applied = migrations[last_applied].name;
-			panic(i"std::db migrate: migration '{pending}' is not applied, but '{applied}' after it in the list is — a migration was inserted before the applied history");
+	if last_applied is Some(let last) {
+		mut earlier = 0;
+		for earlier < last {
+			let pending = migrations[earlier].name;
+			if !recorded.contains(pending) {
+				let applied = migrations[last].name;
+				panic(i"std::db migrate: migration '{pending}' is not applied, but '{applied}' after it in the list is — a migration was inserted before the applied history");
+			}
+			earlier += 1;
 		}
-		earlier += 1;
 	}
 }
diff --git a/vilan/std/src/process/document.vl b/vilan/std/src/process/document.vl
index ff801c3..0c6b2d4 100644
--- a/vilan/std/src/process/document.vl
+++ b/vilan/std/src/process/document.vl
@@ -869,10 +869,13 @@ fun skip_raw_text(markup: str, from: usize, name: str): usize {
 	let length = markup.len();
 	let closing = "</" + name;
 	let closing_length = closing.len();
-	mut index = if from < 0 { 0 } else { from };
-	for (index.as_usize() + closing_length).as_usize() <= length {
-		if matches_ascii_insensitive(markup, index.as_usize(), closing) {
-			ret index.as_usize();
+	// I5 S2: the `from < 0` clamp is gone — an unsigned `from` cannot be negative,
+	// and a clamp that can no longer fire only hides one that arrived already
+	// wrapped (index-type.md §6).
+	mut index = from;
+	for index + closing_length <= length {
+		if matches_ascii_insensitive(markup, index, closing) {
+			ret index;
 		}
 		index = index + 1;
 	}
@@ -919,10 +922,16 @@ fun skip_spaces(markup: str, from: usize): usize {
 /// The index of `needle` in `text` at or after `from`, or `None`. (`str` can answer
 /// whether it `contains` something, but not where.)
 fun index_of(text: str, needle: str, from: usize): Option<usize> {
+	// I5 S2: `text.len() - needle.len()` underflows when the needle is the longer
+	// of the two (unspecified: negative on JS, a native panic), so that case is
+	// answered first; and the `from < 0` clamp is gone with the sign.
+	if needle.len() > text.len() {
+		ret None;
+	}
 	let limit = text.len() - needle.len();
-	mut index = if from < 0 { 0 } else { from };
-	for index.as_usize() <= limit {
-		if text.substring(index.as_usize(), (index.as_usize() + needle.len()).as_usize()) == needle {
+	mut index = from;
+	for index <= limit {
+		if text.substring(index, index + needle.len()) == needle {
 			ret Some(index);
 		}
 		index = index + 1;
diff --git a/vilan/std/src/reactive.vl b/vilan/std/src/reactive.vl
index b0bbdaa..0db3264 100644
--- a/vilan/std/src/reactive.vl
+++ b/vilan/std/src/reactive.vl
@@ -2453,20 +2453,21 @@ export fun reconcile<T, K: PartialEq + Hashable>(
 	// steps past them along the chain. What the bound forbids is the opposite,
 	// a `==` coarser than the hash, and that is the case the bound refuses to
 	// compile rather than mis-plan.
-	mut first: NativeMap<i32> = NativeMap::new();
-	mut next_same: List<i32> = [];
+	// I5 S2: the chain is `Option<usize>` — a position, or the end of the chain —
+	// where it was an `i32` terminated by `-1`. The walks below are the same
+	// walks; the sentinel became `None` and the build loop counts down to zero
+	// without ever reaching below it.
+	mut first: NativeMap<usize> = NativeMap::new();
+	mut next_same: List<Option<usize>> = [];
 	for _ in old_keys {
-		next_same.push(-1);
+		next_same.push(None);
 	}
-	mut build = held - 1;
-	for build >= 0 {
-		let canonical = old_keys[build].hash();
-		match first.get(canonical) {
-			Some(let after) => next_same[build] = after,
-			None => {},
-		}
-		first.insert(canonical, build.as_i32());
+	mut build = held;
+	for build > 0 {
 		build -= 1;
+		let canonical = old_keys[build].hash();
+		next_same[build] = first.get(canonical);
+		first.insert(canonical, build);
 	}
 	mut steps: List<RowStep> = [];
 	for item in items {
@@ -2476,45 +2477,55 @@ export fun reconcile<T, K: PartialEq + Hashable>(
 		// claimed are dropped from the chain for good (`first` is advanced past
 		// them), so the total cost of skipping claimed entries across a whole pass
 		// is bounded by the number of claims.
-		mut head = match first.get(canonical) {
-			Some(let at) => at,
-			None => -1,
-		};
-		for head >= 0 {
-			if !claimed[head] {
-				jump break;
+		mut head: Option<usize> = first.get(canonical);
+		mut advancing = true;
+		for advancing {
+			match head {
+				Some(let at) => {
+					if claimed[at] {
+						head = next_same[at];
+					} else {
+						advancing = false;
+					}
+				},
+				None => advancing = false,
 			}
-			head = next_same[head];
 		}
-		first.insert(canonical, head);
-		mut found = -1;
+		match head {
+			Some(let at) => first.insert(canonical, at),
+			None => first.remove(canonical),
+		}
+		mut found: Option<usize> = None;
 		mut walk = head;
-		for walk >= 0 {
-			if !claimed[walk] && old_keys[walk] == item_key {
-				found = walk.as_usize();
-				jump break;
+		mut walking = true;
+		for walking {
+			match walk {
+				Some(let at) => {
+					if !claimed[at] && old_keys[at] == item_key {
+						found = Some(at);
+						walking = false;
+					} else {
+						// Unclaimed, the same hash, and yet not `==`: a COLLISION.
+						// Walk on rather than advance the head, because a later
+						// item may still match here.
+						walk = next_same[at];
+					}
+				},
+				None => walking = false,
 			}
-			// Unclaimed, the same hash, and yet not `==`: a COLLISION. Walk on
-			// rather than advance the head, because a later item may still match
-			// here.
-			walk = next_same[walk];
 		}
 		mut step = RowStep::Fresh;
-		if found >= 0 {
-			claimed[found] = true;
-			step = if same(old_items[found], item) {
-				RowStep::Keep(found)
-			} else {
-				RowStep::Refresh(found)
-			};
+		if found is Some(let at) {
+			claimed[at] = true;
+			step = if same(old_items[at], item) { RowStep::Keep(at) } else { RowStep::Refresh(at) };
 		}
 		steps.push(step);
 	}
-	mut removed: List<i32> = [];
+	mut removed: List<usize> = [];
 	mut index = 0;
 	for index < held {
 		if !claimed[index] {
-			removed.push(index.as_i32());
+			removed.push(index);
 		}
 		index += 1;
 	}
diff --git a/vilan/test/adapt.vl b/vilan/test/adapt.vl
index d8e40ee..90353e2 100644
--- a/vilan/test/adapt.vl
+++ b/vilan/test/adapt.vl
@@ -28,6 +28,6 @@ fun main() {
 	print(run(|| 1));
 	print(helper(urls, |url| {
 		sleep(1);
-		url.len() + 10
+		url.len().as_i32() + 10
 	}));
 }
diff --git a/vilan/test/bytes-aliasing.vl b/vilan/test/bytes-aliasing.vl
index 4c9b3db..bacdbec 100644
--- a/vilan/test/bytes-aliasing.vl
+++ b/vilan/test/bytes-aliasing.vl
@@ -33,10 +33,12 @@ fun main() {
 	filled.set(2, 0);
 	print(i"{a.to_hex()} {filled.to_hex()}");
 
-	// `copy_into` writes in place; reads see it through the alias, and a
-	// negative index counts from the end.
+	// `copy_into` writes in place; reads see it through the alias, the last
+	// byte included. (An index is a `usize` since I5 S2, so the host's
+	// count-from-the-end `get(-1)` has no spelling; the last byte is asked for
+	// by its position.)
 	a.copy_into(c, 2);
-	print(i"{b.to_hex()} {b.get(3)} {b.get(-1)}");
+	print(i"{b.to_hex()} {b.get(3)} {b.get(b.len() - 1)}");
 
 	// `concat` is a new buffer.
 	let joined = Bytes::concat(a, c);
diff --git a/vilan/test/capture-clones.vl b/vilan/test/capture-clones.vl
index 05a3a7d..d211609 100644
--- a/vilan/test/capture-clones.vl
+++ b/vilan/test/capture-clones.vl
@@ -89,7 +89,7 @@ fun viewed_guarded(pair: &mut (List<i32>, i32)): i32 {
 	match pair {
 		(let cells, let weight) if weight > 0 => {
 			pair.1 = 9;
-			cells.len() + weight.as_usize()
+			cells.len().as_i32() + weight
 		}
 		_ => 0,
 	}
@@ -99,7 +99,7 @@ fun place_component(): i32 {
 	mut cell = ([1, 2], 3);
 	if cell is (let cells, let weight) {
 		cell.1 = 9;
-		ret (cells.len() + weight.as_usize()).as_i32();
+		ret cells.len().as_i32() + weight;
 	}
 	0
 }
@@ -108,7 +108,7 @@ fun place_rebound(): i32 {
 	mut cell = ([1, 2], 3);
 	if cell is (let cells, let weight) {
 		cell = ([4], 5);
-		ret (cells.len() + weight.as_usize()).as_i32();
+		ret cells.len().as_i32() + weight;
 	}
 	0
 }
@@ -118,7 +118,7 @@ fun place_guarded(): i32 {
 	match cell {
 		(let cells, let weight) if weight > 0 => {
 			cell.1 = 9;
-			cells.len() + weight.as_usize()
+			cells.len().as_i32() + weight
 		}
 		_ => 0,
 	}
@@ -200,7 +200,7 @@ fun called_component(): i32 {
 	mut cell = Cell { pair = ([1, 2], 3) };
 	if cell.slot() is (let cells, let weight) {
 		cell.pair.1 = 9;
-		ret (cells.len() + weight.as_usize()).as_i32();
+		ret cells.len().as_i32() + weight;
 	}
 	0
 }
@@ -209,7 +209,7 @@ fun called_readonly(): i32 {
 	mut cell = Cell { pair = ([1, 2], 3) };
 	if cell.peek() is (let cells, let weight) {
 		cell.pair.1 = 9;
-		ret (cells.len() + weight.as_usize()).as_i32();
+		ret cells.len().as_i32() + weight;
 	}
 	0
 }
@@ -220,7 +220,7 @@ fun fresh_pair(): (List<i32>, i32) {
 
 fun owned_call(): i32 {
 	if fresh_pair() is (let cells, let weight) {
-		ret (cells.len() + weight.as_usize()).as_i32();
+		ret cells.len().as_i32() + weight;
 	}
 	0
 }
diff --git a/vilan/test/delta-law.vl b/vilan/test/delta-law.vl
index 8bb731e..2acd843 100644
--- a/vilan/test/delta-law.vl
+++ b/vilan/test/delta-law.vl
@@ -60,11 +60,8 @@ impl ListCell<type T> {
 		ListCell { elements = Signal::new(initial), log = DeltaLog<SeqOp<T>>::new() }
 	}
 
-	fun bounded(initial: List<T>, limit: i32): ListCell<T> {
-		ListCell {
-			elements = Signal::new(initial),
-			log = DeltaLog<SeqOp<T>>::with_limit(limit.as_usize()),
-		}
+	fun bounded(initial: List<T>, limit: usize): ListCell<T> {
+		ListCell { elements = Signal::new(initial), log = DeltaLog<SeqOp<T>>::with_limit(limit) }
 	}
 
 	/// The wholesale write: `Reset`, N derivations, and the honest answer to
@@ -77,12 +74,12 @@ impl ListCell<type T> {
 		});
 	}
 
-	fun set_at(self, at: i32, value: T) {
+	fun set_at(self, at: usize, value: T) {
 		self.elements.update(|&mut list| {
 			// Bound before the store: `SetAt` carries BOTH values.
-			let previous = list[at.as_usize()];
-			list[at.as_usize()] = value;
-			self.log.record(SeqOp::SetAt(at.as_usize(), previous, value));
+			let previous = list[at];
+			list[at] = value;
+			self.log.record(SeqOp::SetAt(at, previous, value));
 		});
 	}
 }
@@ -125,19 +122,19 @@ impl ListCell<type T> with DeltaSource<List<T>, SeqOp<T>> {
 /// `splice(0, len, [])`. `ListCell` writes exactly `size` and `splice` and
 /// inherits the rest.
 trait SequenceCell<T> {
-	fun size(self): i32;
+	fun size(self): usize;
 
-	fun splice(self, at: i32, removed: i32, inserted: List<T>);
+	fun splice(self, at: usize, removed: usize, inserted: List<T>);
 
 	fun push(self, value: T) {
 		self.splice(self.size(), 0, [value])
 	}
 
-	fun insert_at(self, at: i32, value: T) {
+	fun insert_at(self, at: usize, value: T) {
 		self.splice(at, 0, [value])
 	}
 
-	fun remove_at(self, at: i32) {
+	fun remove_at(self, at: usize) {
 		self.splice(at, 1, [])
 	}
 
@@ -147,30 +144,30 @@ trait SequenceCell<T> {
 }
 
 impl ListCell<type T> with SequenceCell<T> {
-	fun size(self): i32 {
-		self.elements.get().len().as_i32()
+	fun size(self): usize {
+		self.elements.get().len()
 	}
 
-	fun splice(self, at: i32, removed: i32, inserted: List<T>) {
+	fun splice(self, at: usize, removed: usize, inserted: List<T>) {
 		self.elements.update(|&mut list| {
 			// The removed VALUES are the op's payload, so a fold's derivative
 			// can subtract what left without reading the collection.
 			mut left: List<T> = [];
 			mut taken = 0;
 			for taken < removed {
-				left.push(list.remove(at.as_usize()));
+				left.push(list.remove(at));
 				taken += 1;
 			}
 			mut offset = 0;
 			let count = inserted.len();
 			for offset < count {
 				match inserted.get(offset) {
-					Some(let value) => list.insert((at.as_usize() + offset).as_usize(), value),
+					Some(let value) => list.insert(at + offset, value),
 					None => {},
 				}
 				offset += 1;
 			}
-			self.log.record(SeqOp::Splice(at.as_usize(), left, inserted));
+			self.log.record(SeqOp::Splice(at, left, inserted));
 		});
 	}
 }
@@ -185,6 +182,12 @@ fun next_random(bound: i32): i32 {
 	(state % bound.as_i53()).as_i32()
 }
 
+/// A random POSITION below `bound` — the same draw as `next_random`, at the
+/// index type the sequence surface takes.
+fun pick(bound: usize): usize {
+	next_random(bound.as_i32()).as_usize()
+}
+
 // === The derivation under test ===============================================
 
 let calls: Shared<i32> = Shared::new(0);
@@ -220,11 +223,11 @@ fun map_each<S: DeltaSource<List<i32>, SeqOp<i32>>>(source: S): ListCell<i32> {
 						incremental_calls.write() = incremental_calls.read() + 1;
 						mapped.push(g(value));
 					}
-					out.splice(at.as_i32(), removed.len().as_i32(), mapped);
+					out.splice(at, removed.len(), mapped);
 				},
 				SeqOp::SetAt(let at, let _previous, let value) => {
 					incremental_calls.write() = incremental_calls.read() + 1;
-					out.set_at(at.as_i32(), g(value));
+					out.set_at(at, g(value));
 				},
 				SeqOp::Reset(let items) => {
 					mut mapped: List<i32> = [];
@@ -344,11 +347,11 @@ fun main() {
 				if choice < 9 || size == 0 {
 					source.push(next_random(100));
 				} else if choice < 13 {
-					source.insert_at(next_random(size), next_random(100));
+					source.insert_at(pick(size), next_random(100));
 				} else if choice < 16 {
-					source.remove_at(next_random(size));
+					source.remove_at(pick(size));
 				} else if choice < 18 {
-					source.set_at(next_random(size), next_random(100));
+					source.set_at(pick(size), next_random(100));
 				} else if choice < 19 && size > 15 {
 					// The wholesale write — a `Reset`, N derivations.
 					mut fresh: List<i32> = [];
diff --git a/vilan/test/iterator-adapters.vl b/vilan/test/iterator-adapters.vl
index 8985a54..74108ca 100644
--- a/vilan/test/iterator-adapters.vl
+++ b/vilan/test/iterator-adapters.vl
@@ -55,7 +55,7 @@ fun main() {
 	// `to_set` / `to_map`: bounded, so they live on `List`.
 	print([1, 2, 2, 3].iter().filter(|n| n > 1).to_list().to_set().len());
 	let lengths = ["alpha", "hi"].iter().map(|word| (word, word.len())).to_list().to_map();
-	print(lengths.get("hi").unwrap_or(-1));
+	print(lengths.get("hi").unwrap_or(0));
 
 	// `iter()` snapshots (rule 1): the push is not walked, and the list is
 	// untouched by the chain.
diff --git a/vilan/test/lift-chain.vl b/vilan/test/lift-chain.vl
index d2ef897..fa7bec9 100644
--- a/vilan/test/lift-chain.vl
+++ b/vilan/test/lift-chain.vl
@@ -38,7 +38,7 @@ fun to_number(text: str): Result<i32, str> {
 fun main() {
 	// map: field, then method-with-args in one group
 	print((find("hit")?.title).unwrap_or("?")); // dune
-	print((find("hit")?.sequel.len()).unwrap_or(0 - 1)); // 7
+	print((find("hit")?.sequel.len()).unwrap_or(0)); // 7
 	print((find("miss")?.title).unwrap_or("?")); // ?
 	// flatten: Option-yielding continuation stays single
 	print((find("hit")?.shelf()).unwrap_or("?")); // sci-fi
diff --git a/vilan/test/list-cell.vl b/vilan/test/list-cell.vl
index d7fc4a2..155326e 100644
--- a/vilan/test/list-cell.vl
+++ b/vilan/test/list-cell.vl
@@ -107,6 +107,12 @@ fun next_random(bound: i32): i32 {
 	(state % bound.as_i53()).as_i32()
 }
 
+/// A random POSITION below `bound` — the same draw as `next_random`, at the
+/// index type the sequence surface takes.
+fun pick(bound: usize): usize {
+	next_random(bound.as_i32()).as_usize()
+}
+
 let walk_calls: Shared<i32> = Shared::new(0);
 let chain_calls: Shared<i32> = Shared::new(0);
 
@@ -288,16 +294,18 @@ fun main() {
 
 	// === 8. Out of range: one splice, two surfaces ===========================
 	//
-	// `remove_range(1, 99)` asks for "the rest"; `remove_at(50)` and
-	// `insert_at(-3, ..)` are clamped exactly as the cell clamps them, because
-	// the recorder and the cell run the same splice.
+	// `remove_range(1, 99)` asks for "the rest"; `remove_at(50)` is clamped
+	// exactly as the cell clamps it, because the recorder and the cell run the
+	// same splice. (A position is a `usize` since I5 S2, so there is no negative
+	// one left to clamp: the front is asked for as `0`, which is where
+	// `insert_at(-3, ..)` used to land.)
 	let clamped: ListCell<str> = ListCell<str>::of(["a", "bb", "ccc", "dddd"]);
 	let clamped_lengths = map_each(clamped, counted);
 	let clamp_calls = calls.read();
 	clamped.edit(|&mut list| {
 		list.remove_range(1, 99);
 		list.remove_at(50);
-		list.insert_at(-3, "front");
+		list.insert_at(0, "front");
 	});
 	law("clamped edit", clamped, clamped_lengths);
 	clamped.remove_range(1, 99);
@@ -332,21 +340,21 @@ fun main() {
 				if choice < 8 || size == 0 {
 					walk.push(next_random(100));
 				} else if choice < 11 {
-					walk.insert_at(next_random((size + 1).as_i32()).as_usize(), next_random(100));
+					walk.insert_at(pick(size + 1), next_random(100));
 				} else if choice < 13 {
-					walk.remove_at(next_random(size.as_i32()).as_usize());
+					walk.remove_at(pick(size));
 				} else if choice < 15 {
-					walk.set_at(next_random(size.as_i32()).as_usize(), next_random(100));
+					walk.set_at(pick(size), next_random(100));
 				} else if choice < 17 {
-					let from = next_random(size.as_i32());
-					let count = 1 + next_random((size - from).as_i32());
-					walk.move_range(from.as_usize(), count.as_usize(), next_random((size - count + 1).as_i32()).as_usize());
+					let from = pick(size);
+					let count = 1 + pick(size - from);
+					walk.move_range(from, count, pick(size - count + 1));
 				} else if choice < 18 {
 					walk.pop();
 				} else if choice < 19 && size > 12 {
-					walk.remove_range(next_random(size.as_i32()).as_usize(), 1 + next_random(4));
+					walk.remove_range(pick(size), 1 + pick(4));
 				} else if choice < 20 && size > 20 {
-					walk.truncate(next_random(size.as_i32()).as_usize());
+					walk.truncate(pick(size));
 				} else if choice < 21 && size > 16 {
 					// The wholesale write: a `Reset`, the honest N.
 					mut fresh: List<i32> = [];
@@ -360,14 +368,14 @@ fun main() {
 					// The compat door: the same list with one element edited
 					// and one appended — one `Splice`, not a `Reset`.
 					mut edited = walk.get();
-					edited[next_random(size.as_i32()).as_usize()] = next_random(100);
+					edited[pick(size)] = next_random(100);
 					edited.push(next_random(100));
 					walk.reconcile_to(edited);
 				} else {
 					// The batch door, inside the batch.
-					let at = next_random(size.as_i32());
+					let at = pick(size);
 					walk.edit(|&mut list| {
-						list.insert_at(at.as_usize(), next_random(100));
+						list.insert_at(at, next_random(100));
 						list.remove_at(0);
 						list.push(next_random(100));
 					});
diff --git a/vilan/test/list-search.vl b/vilan/test/list-search.vl
index 2c5fc46..ea0c32c 100644
--- a/vilan/test/list-search.vl
+++ b/vilan/test/list-search.vl
@@ -7,12 +7,12 @@ fun main() {
 	print(xs.find(|n| n > 90).is_none()); // true
 	print(xs.contains(20)); // true
 	print(xs.contains(25)); // false
-	print(xs.index_of(20).unwrap_or(-1)); // 1
+	print(xs.index_of(20).unwrap_or(usize::max_value())); // 1
 	print(xs.index_of(99).is_none()); // true
 
 	let words = ["alpha", "beta"];
 	print(words.contains("beta")); // true
-	print(words.index_of("alpha").unwrap_or(-1)); // 0
+	print(words.index_of("alpha").unwrap_or(usize::max_value())); // 0
 
 	mut empty: List<i32> = [];
 	print(empty.find(|n| n > 0).is_none()); // true
diff --git a/vilan/test/reactive-keyed.vl b/vilan/test/reactive-keyed.vl
index 2e95ed7..410499f 100644
--- a/vilan/test/reactive-keyed.vl
+++ b/vilan/test/reactive-keyed.vl
@@ -42,7 +42,7 @@ fun main() {
 	// `each_by`'s predicate: a surviving key is ALWAYS kept, so the same
 	// input that refreshed above keeps its row instead — the row's own cell
 	// carries the new item.
-	// witness: if (same(__at(old_items, found), item)) {
+	// witness: if (same(__at(old_items,
 	show(reconcile([1, 2], [10, 20], [10, 21, 35], |item| item / 10, |_a, _b| true));
 }
 
diff --git a/vilan/test/reconcile-index.vl b/vilan/test/reconcile-index.vl
index e957291..0f730e1 100644
--- a/vilan/test/reconcile-index.vl
+++ b/vilan/test/reconcile-index.vl
@@ -64,11 +64,11 @@ fun scan_reconcile<T, K: PartialEq>(
 		}
 		steps.push(step);
 	}
-	mut removed: List<i32> = [];
+	mut removed: List<usize> = [];
 	mut index = 0;
 	for index < old_keys.len() {
 		if !claimed[index] {
-			removed.push(index.as_i32());
+			removed.push(index);
 		}
 		index += 1;
 	}
