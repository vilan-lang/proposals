<!-- family: breaking -->
**Every length, count and position in std is a `usize` now, and `xs[i]` takes a `usize` and nothing else: `List::len()`, `get(i)`, `insert(i, ..)`, `remove(i)`, `str::len()`/`substring`/`code_at`/`repeat`, `index_of`'s answer, `take`/`skip`, `enumerate()`'s counter `(usize, T)`, `Map`/`Set`/`Bytes`/`Arena` lengths, `SequenceCell`'s `size`/`splice`/`insert_at`…, `SeqOp::Splice(usize, ..)`, `RowStep::Keep(usize)`, macro_std's `Arguments::len`/`get`. Migration: run `vilan check --fix` (it writes the `.as_usize()`/`.as_i32()` each mismatch names, or declares a literal-bound counter `usize`), then fix by hand what it reports — a `-1` "not found" becomes an `Option<usize>` or a real fallback, a `for i >= 0 { …; i -= 1 }` loop becomes `i = len; for i > 0 { i -= 1; … }`, and a `len() - 1` that can meet an empty list gets its guard.** Tracker I5 S2 (+ S3, S4), `proposal/index-type.md`, the owner's rulings of 2026-09-21/22 and 2026-09-24/25. The 105 index-kind std positions the paper's census found by parse (all `i32`) moved, with the three boundaries it named left alone — the WIRE seam (`begin_list(length: i32)`, the codec integers) converts with one `.as_i32()` where an index meets it, `std::fs`'s STREAM offsets stay `i53`, `DeltaLog`'s SEQUENCE numbers stay `i32` and convert once where they become a position. S4, the positions A112 was born without: `delta.vl`'s `// I5` markers (the census now reads zero), `std::ui`'s row positions, `KeyedCell.positions`, `Delta::Insert`'s index — ON THE WIRE AT `i32`'S WIDTH, as ruled: a `Patch` frame (binary 104 bytes, JSON 96) is byte-identical before and after and a service's contract hash does not move (`be3fd1fe` on the paper's probe). Changing YOUR `[rpc]` parameter from `i32` to `usize` does move your service's hash (§7): rebuild both halves. The subscript's two-type admission from S1 is gone, and its refusal names `usize` and the conversion (ledger 558). By hand, each where the compiler cannot see it: the seven `-1` sentinels (`reconcile`'s chain ×3 → `Option<usize>`, `std::ui`'s `settled_steps`, `std::db`'s `refuse_inserted_migration`, `list-search.vl` ×2) plus two the census could not see (`std::ui`'s focus wrap, spelled `0 - 1`, and A129's `live_ops`, written after it); the eight `>= 0` loops (`settled_steps`, `row_references`, `json`'s `begin_list`/`begin_variant`, `List::reverse`, `reconcile` ×3); the dead `from < 0` / `at < 0` / `removed < 0` clamps (`document.vl` ×2, `delta.vl`'s `splice_start`/`splice_count`); and three `len() - n` underflows on an empty input (`List::last`, `document.vl` and `markdown.vl`'s `index_of`). Natively (S3) a length is Rust's `usize`: the emitter's `len() as i32` casts are gone (35 → 0 over the native copy census's programs) and `vilan-rt`'s list, string and `Bytes` helpers take and answer `usize` (their negative-index readings had no spelling left). Goldens: 30 corpus programs and the split fixture moved, every one printing exactly what it printed (node, before against after) — the conversions the migration wrote (12), std's hand-rewritten loops (7), the `-1` fallbacks at a `usize` (3), and `List::last`'s empty-list guard reaching the reactive runtime (13). Natively, a statement no longer takes the expected type of the block around it: a `usize` loop inside an `if` arm of a function answering `i32` emitted `while (i > (0i32))`, which rustc refused (pinned). Two `Shared::new(0usize)` suffixes stay in `std::rpc` (`RemoteSource`/`KeyedSource`): B406's fix does not yet reach a generic constructor in a struct-literal field when an EARLIER field holds the same constructor at another width (reported with a repro). Kolt, vilan-website and vilan-playground: kolt's five files need the edits the index-42 sweep wrote out (`rotary.vl`'s signed wrap converts `len()` once; the two selection lists guard their empty-list `len() - 1`); the website and playground need none.
