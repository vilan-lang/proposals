{"prefix": "- 2026-09-25 (Order 42, collections-42): ", "stamps": {"A129": "BUILT (c64bf317) as ruled: the whole-list pass is now `reconcile_span(at, count, list)` over the whole run (so the two cannot drift); a Splice that both removes and inserts runs the SPAN pass (bounded on the right by the first row after it or the anchor; a key inside the span never claims a row outside it); a one-sided splice stays positional (push still `built=1 keys=1`); a DRAIN RULE — ops before the last `Reset` in one drain are dropped; `held_span` copies only the span's elements (M86's `move_range wholes=11` unmoved); the server twin has no op path. `set_all` over the same keys `cut=3 built=3` → `cut=0 built=0`; `reconcile_to` `cut=2 built=3` → `cut=0 built=1`; the walk's `each_by` on the op path 1,078 → 575 against the pass's 557 — the ≤-pass target NOT fully met: the remaining 18 are rows built and cut inside one drain (→ A131, a deferred-build redesign). `set_all`'s doc corrected (not cheaper than `set`, no longer costlier); docs ×2. Pin `a129_a_splice_keeps_the_rows_whose_keys_survive_it` (cost + row identity per line; red on the old tree and under four plants); the S3 walk pin now carries two pass-control runs (`renders=2061 by=575 … pass=653 pass_by=557`). No golden moved; census unchanged.", "M87": "BUILT (01eb9b00): `locate` reads `self.elements.value.read().get(at)` (what `SignalCell::get` reads) and copies ONE element — 2,881,649 → 21,889 Ir per locate into 1,000 keys (the empty loop 41–62); the list not moved into its own `Shared` (touches all of `KeyedCell` — a `SignalCell::peek` in reactive.vl would be the cleaner surface, reactive-42's call); pin `m87_keyed_cell_locate_copies_no_whole_list` (red first: `wholes=1` per hit); keyed rpc pins and the wire unchanged. FIND: `KeyedCell` does not build natively at all → F42."}}