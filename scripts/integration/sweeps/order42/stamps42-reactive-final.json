{"prefix": "- 2026-09-25 (Order 42, reactive-42): ", "stamps": {"A124": "S2c LANDED on the branch (490f0fb1, ONE breaking commit on next @0ed1864e; A130 8ff8dd55 its own): the five combinators return NODES — `map` → `Map<S, T, U>` (a blanket, reaches through `dyn` and abstract bounds — B408), `switch` + the total `flatten` → `Switch<S, T, I, U>`, the Option `flatten` → `FlattenOption`, `and_then` → `AndThen`, `combine((..))` → `Combine<T>` over `(U in T: dyn Source<U>)` (B398); the phantom arities stay (B411 unlanded); `Upstream` RETIRED on B409 (which also closed F43); per-node `on_settle` → one `subscribe_pulling` through the bound (B410); `attach`'s `reissued` KEPT (the JS backend deep-copies the record on push — native-42's note assumed native-only) with the order fixed. A25 AT THE LEAF as built: a mirror's `map`/`or` are cold nodes leasing nothing; `effect`/`effect_on_change` on one need an enclosing owner statically (the coverage error anchors at the user's call); `.cell()` leases under the ambient owner; `sub`/`on_change` hand the lease back. `Resource<T>` public, constructor `source.load(fetch)`, `Resource::pending()` public (B413 landed, so `.resource(..)` is available if preferred — OWNER). The 19 reds re-derived one by one (b243's blanket case parked under `#[ignore = B419]`); the no-cycle gate: the exemplar's `.cell()` under the view + a module-level `.cell_global()` named in `heap_cycles.js`'s `designedLoops` (asserts cycles=0, designed=1). Shared census 143 → 141 (the four cell-returning joins' rolling `Shared`s gone). Goldens: 12 corpus (4 by the combinators, 8 by attach's order) + 4 split + the anchor, all runtime-identical; JS copy census 419 → 415; native copy census 7 rows; leak census MINTED down, every LIVE count unchanged (nodes hold 0). Union 8,675/8,675; natdiff 60/60; whole set 125 / 94 / 31 / 0. ESTATE: std rpc 4, examples 9, split 1, Rust consts (11 `.cell()`), docs (guide derivations/cleanup/traps, std reactive incl. a Resource section + `.cell_global()`, rpc, services), playground `client.vl:22` → `app(token, route.cell())`, KOLT 14 edits (down from 17 + 6 — `combine` erases its inputs) in `sweeps/order42/reactive-42/kolt-edits.md` + `kolt_edits.py`; with kolt-i5.patch only A125's 4 remain. Finds: F46 (native E0505 on `attach(watching(source))`), B427 (a destructured closure param leaves `U` open), A133 (a node over an in-process mirror sees the seed twice — design), E226 (the regime-3 steer through `.cell()`), B419/B420/B421 still reproduce. What the paper did not predict: a one-block Signal impl no longer reaches `map` (B419); N readers pay N evaluations — a subscribed `switch` calls `select` twice per value.", "A130": "BUILT (8ff8dd55): `init_order::check_module_level_cells` in `post_analysis_passes` reads a module binding's own initializer calls and refuses std reactive's `cell` anchored at the method name, steering to an owner or `.cell_global()`; one ledger row; static-only as ruled (a closure created in the initializer and a call from module init are the pinned remainder — they leak too); `lazy` bindings skipped (already refused); 12 pins.", "F43": "CLOSED BY THE RETIREMENT: with `Upstream` gone (B409) the repro builds natively and the three native pins are green (reactive-42). Close at the sweep.", "B420": "std no longer hits it (the `Upstream` retirement); the std-free repro still reproduces on 0ed1864e — stays open for the solver."}}