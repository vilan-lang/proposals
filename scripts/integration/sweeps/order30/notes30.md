# Order 30 — integration notes (running; the sweep reads this)

Opened 2026-09-08 off f0f4e301; fifteen lanes (chronicle cycle 48 at proposals 183799c). Ledger next id 406.

## rpc-paper-30 — landed first (proposals branch merged to main: transport-rpc.md §9 "Sessions, handles, and the other direction", 536 lines; former §9–§13 shifted to §10–§14, cross-refs fixed; vilan 0f223ce0: one service_layer pin, no changelog)
- 9.1 VERIFIED, three states on a real socket: `&mut self` on an `[rpc]` method REFUSES the build (the generator emits `fun dispatcher(self)` by value, every route closes over that copy — rpc.vl:3249/:3188/:3193; the diagnostic lands on the STRUCT and recommends the receiver the author wrote — misleading); `mut self` compiles and SILENTLY LOSES the write (next call reads the old value); `Shared<T>` fields persist per connection. Pin `a_mut_self_rpc_mutation_is_lost_where_a_shared_field_survives`. → B item (R-A38b): honour `&mut self` in the generated dispatcher (hold the instance in a cell), and fix the misleading diagnostic.
- 9.2 decisions: a Source in RETURN position maps to its mirror, the contract hash covers the MAPPED type; the reply carries a plain-Wire `ChannelId` exported into the connection's capability table (the arena; dropped whole at `drop_session`); laziness = the shipped lease rule; the mirror gains an `origin` (method + arg describers) so `reattach_mirrors` re-issues it (retires `invalidate_dynamic` for generated mirrors); a NEW `Release(channel)` frame drives `revoke` at dispose (distinct from Unsubscribe — A41's reason); `[expose]` stays sugar with its `expose:` surface entry kept verbatim (no shipped hash moves).
- 9.3 decisions: one new lane `s:<id>:` (binary 0x73) in the server's own id namespace (no direction bit needed); a typed proxy via `connection.client()`; `get_client()` context sugar later. FINDS: (1) a REAL DEADLOCK if built naively — `rpc_server.vl:1117-1176` serializes events TCP-coalesced into one read (`for event in parser.feed(chunk) { turn(…, respond) }`), so an awaited server→client call whose answer lands in the same read never settles → R4's justification (notifications only in v1); (2) `turn` with an awaiting body holds every notification until the body completes (reactive.vl:203-207).
- RULINGS for the owner (numbered in §9.5): R1 naming — keep `[service(C, client = H)]` + `[client_service]` (the owner's `[service_server]`/`[service_client]` reads better but renames the shipped attribute); R2 `[expose]` stays as sugar; R3 explicit `Release`, not lease-zero; R4 notifications only in v1; R5 no `bind_each` sibling — build the `KeyedSource` Source impl; R6 refuse a disagreeing keyed key at the attribute, keep 429 for `authorize_timeout`; R7 replay idempotence (lane-raised); R-A38b the `&mut self` hole.
- Corrections to my brief: kolt's TODOs are at store.vl:93 and :102-109 (after the migration moved lines); `DuplexEnd` has no correlation — `SocketDuplex.pending` is client-only. Residue: two vilan-side `§11` citations (benchmarks README/main.vl) now point at §12 — a `(was §11)` note on the heading covers them; clean-up small.

## dom-30 — both landed (f19313a6 A59, 0b429daf A62), merged second (allow-edit: dom_events.rs `the_event_surfaces_externs_are_marked_by_the_audit_rule`, a binding count 4 → 6)
- A59 surface: `DomRect { left, top, width, height }` value (+ `right()`/`bottom()`); on Element: `bounding_rect`, `offset_width/height`, `is_connected`, `contains`, `query_selector_all`, `remove_attribute`, capture-phase `on/off_event_capture`, `[must_use] listen_capture`, `[must_use] observe_resize(on_resize): Subscription`; `ResizeObserver` raw hatch; `Event::target`. PREMISE CORRECTED: `std::dom`/`std::router` are single-platform browser layers (no process twin: `[library.layer.browser]`, `TWINNED_MODULES = ["ui"]`) — the honest zero lives in the DOM STUB (detached rect 0,0,0,0; `is_connected` a real walk). Two emitter helpers `__dom_bounding_rect` (returns `[left, top, width, height]` = the struct's runtime form — a field reorder is pinned red) and `__dom_query_all` (Array.from). DOM_STUB in ui_rows.rs EXTENDED (additive): removeAttribute, getBoundingClientRect from `global.boxes`, offset*, isConnected, contains, querySelectorAll (tag selectors), capture listener tables, a `window` with its own tables, `dispatchEvent` walking capture→target→bubble, a ResizeObserver firing once on observe, `global.resize`. → INTEGRATION HAZARD: view-30 and editor-30 also extend DOM_STUB (a const, not a fn); the fold keeps HEAD's copy of an EDITED const — those merges need a HAND union of the stub.
- A62 surface: `PathParts { segments, query: Map<str,str>, fragment: Option<str> }`, `parse_path`, `parse_query`, `percent_decode` (total), `location_url()`; rules pinned (fragment cut first, then query, split, decode; empty segments dropped; last repeated key wins; `+`→space in the query only). Trait shape: the language REFUSED a trait default calling `Self::from_segments` ("an associated function without one is a per-impl requirement") → `trait FromPath { fun from_segments(parts): Self }` + free `fun from_path<R: FromPath>(path): R`. Kolt recipe in the report (routes.vl's parse pair → `impl Route with FromPath`; ~16 externs leave overlay.vl; `toggle_attr` has a spelling once A66 lands).
- FINDS → items/notes: (1) `segments` (raw) vs `parse_path().segments` (decoded) disagree on `%20` — two forms of one question; making `segments` decode is the tidier end state (behaviour change, not taken) → small A/ruling. (2) `current_path()` is pathname-only — no SIGNAL over the full URL; an app routing on `?q=` still wires its own popstate → A item (a `current_url()` signal). (3) `__dom_bounding_rect` encodes DomRect's field order in a JS string (precedent `__parse_i32`; pinned) — owner's eye. (4) the whole `-p vilan-core` run was NOT done by the lane (filtered 317 only) — the merge gate/seal cover it.

## fmt-30 — DONE, HELD for the LAST merge (2c9a9301 E150-A match legs inline, 69a8e0b0 E150-B for/match head split permission, fb28428c E151 attribute sorter + `sort_element_heads` in `normalize` + element-syntax corpus golden regenerated, 110248d4 THE REFORMAT — drop and regenerate over the merged tree)
- Merge procedure: merge origin/fmt-30~1 as `fmt-30-rules` (the three rule commits), then `target/debug/vilan fmt` over the merged tree as one final commit; fmt gate green. Reformat on the lane: 5 files +12/−13, over-100 lines 146 → 143. Kolt exhibit: 4 heads reorder (three `<input>`s in views.vl — `autofocus` rises past `placeholder` and past `on:keydown` — and lucide's one `<svg>` frame).
- E151: attributes lead (id name type for href src, then alphabetical), `on:` handlers alphabetical, dotted links are BARRIERS holding their index; a head with a comment is refused; `element_layout::an_over_budget_head_splits_one_item_per_line` expectation updated (an edited existing pin → --allow-edit at the fold). Commutation proof `element_head_order.rs` (4 tests): rendered documents equal modulo attribute-set order (non-vacuous), emitted-JS string-literal + character MULTISETS (not byte-identical — the head is a left-nested application; owner's word wanted on the substitution).
- E151 SEMANTICS NOTE for the owner: reordering attributes moves the EVALUATION ORDER of attribute value expressions (an arbitrary expression) — documented in the formatter's section doc and the test header; the style chain took the same bargain. RULING wanted: accept, or refuse to reorder a head whose values are not literals/pure paths.
- Gap: the whole `-p vilan-core` did not complete on the lane (load 140–168; two attempts, one pin fixed on the first) — the merge gate and the seal cover it.
- FINDS → items: (1) `for x in <iterable>` lacks the split permission rule B gave conditions (117-col iterable untouched) — the obvious third, one line (owner Q: yes?). (2) PRE-EXISTING (confirmed on f0f4e301): on kolt's generated lucide/lib.vl `vilan fmt` rejoins a hand-broken four-link `.child` ladder into one over-budget line, then breaks INSIDE an element argument (`.child(<path⏎ d(…)⏎/>)`) — worse than the source; visible only because kolt excludes src/lucide from its fmt gate → E item (element argument break rule / chain rejoin vs element).

## b258 — landed (1df74952, family miscompile), merged third
- ROOT: not dispatch selection — the CONTEXT pass's flavor propagation. `settle_strict` promotes safe candidates to strict over `dispatch_refine::candidates_of`, which is NAME-keyed (every override of every trait declaring the name), so `RemoteSource`'s strict override of `Source::map` promoted `status`'s `self.cache.map(..)` (a `SignalCell` FIELD inheriting the owner-optional default): the default's `get_safe()` became `Some(<hidden>)`, `status` grew a bare-owner parameter, the top-level call passed nothing → `Owner::take` on undefined. SILENT because coverage runs on `refined_edges` (receiver-narrowed) while needs/strict/threading ran on the union — the disagreement IS the wrong-output window. Fix: `dispatch_refine::known_receiver_candidates` extracted (the `OnType`-with-known-receiver narrowing `refined_edges` had) and the context pass's `dispatch_sites` uses it; `refined_edges` calls the same helper so they cannot drift. rpc.vl: `map` back on `with Source<Option<T>>` (the strict owner law now reaches a generic `S: Source<Option<T>>`); `or` stays inherent (not a trait member). Pins: the user program (default(cell) under undefined → default(cell), no scope; covered; override on its own receiver), the fence control, A25's four green.
- FINDS → items: (1) STRUCTURAL GUARD wanted: nothing forbids a future over-approximation promoting a node to strict that coverage's narrower edges never fence — assert every strict node has a refined coverage caller (or is run-closure/declared) and REFUSE rather than emit (N/B item, cheap). (2) `context.rs`'s top-level threading and `top_level_targets` resolve callees through `local_target` — a second site-enumeration policy differing from the graph's (note). (3) B254 + B258 are one class: a name-keyed candidate list consumed as receiver-specific; `impl_members_for_bound`'s doc names a third (two traits sharing a member name) → a SWEEP of `candidates_of` consumers (the const-only capability check reads edges as refusals — safe by direction) → N item. (4) open: should `or` move onto a trait (A25's law reaches it only on a concrete receiver)? (5) `dispatch_callers` loses union edges — call_graph/init_order do not consume this local set; confirm nothing else wants the union.
- PROCESS (b258's merge): the core gate went red on `markdown_golden` — dom-30's routing-guide section ("Reading the URL back") was added without `scripts/regen-markdown-golden.py`, and dom-30's own gate ran `--test docs` but not `markdown_golden`; regenerated on the merged tree and committed above the b258 merge. Rule: a lane that edits vilan/docs runs `--test markdown_golden` (add it to the docs gate list in briefs beside `docs`).

## hygiene-30 — all six landed (72520330 N59, ee901d6b N60, 5c7893ac N61, 2b5592a3 N62, 69492cf6 N63, 3745cd9a N64); merge after b258 with --allow-edit `a_refused_client_reports_unauthorized_without_burning_the_retry_budget,a_client_refused_by_infrastructure_reports_unavailable_and_compares_equal,a_bare_connect_socket_offers_vilan_rpc_and_an_explicit_list_is_untouched` (service_layer.rs conflicts with rpc-paper-30's added fn); document/asset_bundle/split/init (wait_for_port deleted — the fold drops lane-deleted fns; their import lines only survive a CLEAN merge — check), release_emission.rs:377 fixture, diagnostics_ledger.rs's ROWS_WITHOUT_A_KEY note → `RULE_STATEMENT_SITES` (20, pinned against the tree)
- N59: per-run scratch cwd for node children; the ignore line dropped. N60: FOUR `wait_for_port` copies → one (no deadline; deletes split's 30 s and init's 20 s). N61: three wall bounds in service_layer.rs → handshake COUNTS; sweep list: build_hooks.rs:1252/2073/2497 + watch_leg_reuse.rs:176 are loop deadlines; service_layer.rs:1667 (2500 ms over a raw handshake, 10×) and interpreter.rs:433 (20 s margin on a 500 ms kill) left. N62: `reachability_and_depth.rs` — B254's removed edge pinned absent; B244's `substitution_peak()` corpus max 11 (iterator-adapters), BOUND 24, floor 4; a stale `dispatch_candidates` comment fixed. N63: `dist/.cache/macro-expansions`; pin: one `check` leaves exactly `.cache` + that file; `dist/.build-hooks.json` NOT moved (a stamp under build-hooks §3.3 Q2 — owner Q: did "every cache" mean it too?). N64: `## Reserved words` in the tour (32, `own`/`jump` called out), gated both ways against `lexing::KEYWORDS`; names.md §4.1 parent-load paragraph (verified: the parent is analyzed, emission follows reachability).
- REPORTED for the paper owner: build-hooks.md:1035's Q5 (`generated` as a second root "since a subdirectory cannot be imported today") is false after A65 — `src/lucide/*.vl` resolves as `pkg::lucide::*`; the manifest key still exists (`generated_root_problem` in manifest.rs); subdirectory-of-root is the owner's call → item on the build-hooks paper.
- FINDS: service_layer.rs:1667's 2500 ms is the weakest survivor (wall over a 300 ms claim); "the nine" drift and the missing keyword list were the same failure mode (a record kept only in prose) — both gated now.

## std-30 — all four landed (e6d091b9 A61, cfd98359 A58, 3bf4a8c6 A57, fd492291 A63), merge after hygiene-30
- A61 `str::parse_bool` ("true"/"false" only, trims; "1"/"0" refused — a parser wider than its writer adopts another producer's convention); corpus `parse-bool.vl` (manifest row + census row 3). A58 `Event::code/target/current_target` (no process twin — std::dom is browser-only). A57 `Storage` via `window().local_storage()/session_storage()`: `len`, `key_at → Option`, `get → Option`, `has`, `set`, `remove`, `clear`; free fns stay as the one-key form; process twin DECLINED (the cross-platform error beats a diverging in-memory map); pins in `storage_handle.rs`. A63 `std::time::Debounce { new(delay), run(fn), cancel() }`, trailing edge, three `Shared` cells each justified (kolt's TODO answered: rule 1 §6.1 + rule 3 §6.3); base layer — no owner, `cancel()` is the teardown; `debounce.rs` exact-sequence pin.
- Kolt recipes (in the report): delete prefs.vl:69-93 (all three impls) + `import std::display::Display`; delete input_system.vl:5-9, views.vl:31-34, overlay.vl:91-94 (Event externs); delete lib/storage.vl:6-41 (Storage) — `StorageKey::get`/`delete_unknown_keys` now unwrap an Option; kolt's debounce.vl → `std::time::Debounce`.
- FINDS/open → items/notes: (a) `Storage` key iteration not shipped (len + key_at); (b) `Debounce::flush()` not shipped; (c) `Debounce`'s loop runs under the ambient nursery — a nursery cancellation unwinds it with `running` still true, leaving the value INERT (not pinned) → small B/A item: cleanup or a stated scope rule; (d) `is_null` helpers are module-visible (no visibility modifier — a known language gap); (3) an i-string hole cannot contain an escaped quote (`i"{x.get(\"k\")}"` is a parse error) → small B/E item; (4) `return` is not a statement (`ret` is) — docs steer?

## build-30 — M51 landed (720453c5, two commits), merge after std-30; touches `.config/nextest.toml` (filterset `wall-clock-waits`) + build_hooks' `MIXED_BINARIES` mirror (run `--test build_hooks` at the merge; watch for a nextest.toml conflict with perf-30/m19-t1d)
- `build_workspace_artifacts` walks TIERS (`schedule_tiers` over `LegSchedule::order`; only `compile_unit` overlaps; writers serial in schedule order; `bundled_names` untouched by workers so no lock; `explain::leg_facts` captured per leg, replayed at the leg's turn; `recorded[index]` merged after the join; `compile_tier` returns a map and the serial phase `?`s at the first failing leg). M35's warm-up: the round's first leg compiles alone. `VILAN_SEQUENTIAL_BUILD=1` escape hatch = the pins' reference. HAZARD the audit missed: a plain `build` has no schedule edges (they come from the PREVIOUS round's record), so a server leg `const asset::read`ing `dist/client.js` would read last build's bytes → guard: a leg whose loaded sources include a file a same-tier earlier leg just wrote is compiled AGAIN (`leg_writes`). `check_single` stays serial (blocker `RENDERED_THIS_ROUND`, named in its doc).
- Pins `parallel_build.rs` (5, red-first under two plants): byte-identical dist + stdout on a 3-leg generated workspace; collision blame same leg; abort names the same leg; `--watch` round reuses; the artifact-edge leg embeds this round's bundle. Measurement (release, medians of 5, loadavg ~100–128): kolt 3 legs CPU 2.70 → 2.63 s (−2.6%), wall 23.8 → 15.7 s (−34%); generated 3-leg CPU +7% (thread spawns on a quarter-second build), wall −30%.
- FINDS → items: (1) PRE-EXISTING (M22/E12 class): a leg that BUNDLES anything is never `Fresh` under `build --watch` — `asset::bundle` records `content_hash_bytes` while `current_source_hash` re-hashes with `read_source`+`content_hash`; they never agree, so kolt's `build --watch` reuses nothing today → B item. (2) FIXED here: `flush_expansion_cache`'s temp file was per-PROCESS while legs share a build dir — two flushes interleaved (reachable since M35's parallel check). (3) a failed parallel build may leave macro-cache side effects from legs a serial round never reached (no artifact written; a failed round's dist/ is not a baseline).
- Open Qs (owner/next order): persist the leg record into dist/ so a plain build gets the edges from the second build on? tier the HMR round's compile loop (`run --watch`, ~main.rs:1340)? M52's seed-set union is where kolt's remaining parallel CPU headroom is.

## editor-30 — all four landed (c09aacf3 B264, 86c9e559 E152, d85838ce B269, 4783d045 B271); merge after build-30 — ui_rows.rs's DOM_STUB const edited (dom-30 also did): HAND UNION of the stub (HEAD's additions + editor-30's `visibility`, `activeElement`, `matches(":focus")`, driven `requestAnimationFrame`/`flushFrame()`)
- B264: TypeScript check RAN (typescript@5 LanguageService): go-to-def resolves THROUGH; hover `(alias) bar(): number`; rename at a use renames the alias binding + uses, at the original renames declaration + import segment. Vilan already did (1) and (3)-value; gaps fixed: a TYPE alias as a struct-literal head (`Spot { x = 1 }`) derived from `struct_initializer_to_def` with a whole-initializer span → `narrow` produced a row covering `Spot ` under `Point` (INVARIANT 1's exception back; `rename Point` gave `zzz{ x = 1 }`) — an anchored span now; hover renders `(alias) fun hi(): i32` with the target's doc. 5 pins, 3 red first.
- E152: the first child's file at line 1 (a Location is a text-document range; a directory URI would not open); hover `namespace ui` + `Holds \`widget\``; `Program` publishes `module_children_scopes`.
- B269 MEASURED at f0f4e301: `let p = const f();` read by main → not gray; `let _p = const f();` GRAY; `const f();` GRAY; `const { f(); };` GRAY (the `_` exempts the binding so the walk never follows its const edges). Fix: every const region is a ROOT (`CallGraph::const_regions` + `Traversal::walk_const_region`, edges out without recording the region). Broader than the three shapes: a const callee inside an UNREACHED function body is non-gray too (defensible: const_eval evaluates it) — owner sanity check.
- B271: microtask → next frame → frame after → stop; externs `std::dom::request_animation_frame` (browser layer) + `Element::matches`; docs corrected (ui.md claimed autofocus == on_mount(focus)); half (2) — the on-shown/`<dialog>` shape — documented, not built (kolt's Overlay page is the owner's).
- FINDS → items/notes: (1) `references.rs`'s `narrow` trusts that an Anchor span spells its definition's name — any other DERIVED span has the same exposure (N/E sweep). (2) `is_namespace_module` discriminates on "SourceId(0) + has children" — thin; a third SourceId(0) entity needs a real marker (note on A65/E152). (3) the DOM stub could not say whether focus was TAKEN before this lane (fixed by extension).

## rule1-30 — B267 + B256 LANDED (740ea1a8 B267 performance, 3f00157d B256 fix); merge after editor-30 with --allow-edit `b256_a_binding_fed_by_a_shared_read_copies,b256_a_mutable_binding_fed_by_a_shared_read_does_not_grow_the_cell,b256_a_value_returning_body_copies_the_shared_read_it_hands_back` (ignore headers removed); 14 corpus goldens + the split golden + the census moved — REGENERATE census and split over the merged tree (std-30 added a census row; both will conflict)
- B267 design: a binding fed by `cell.read()` may alias when never mutated in place (`collect_written_roots.in_place`), never at a value seam, not opaque to last-use (new `LastUse::is_opaque` — closure captures refuse) AND the cell's storage is safe: no in-place write to that cell (`h.write() = x` REBINDS and leaves the old value — bind_each's row_views/row_owners qualify), or a rebind orphans the read first on the declaring block's straight line (drain's `let wave = turn.pending.read(); turn.pending.write() = [];` — the clear ENDS the aliasing). Cell identity = union-find over slots (`CellSlot::Binding|Field|Unknown`; a handle moves at four forms). Alone moves no byte (pinned).
- B256: `is_shared_read` beside `is_place_expr`; `consider` skips B267-elided reads; `returned_value_places` reads a read leaf through to its receiver (get() copies at the return). DEVIATION: `shared_read_value_type_id` reads the receiver's `Shared<T>` payload instead of the extern's abstract T (the generic fallback admitted every scalar read — File::position, Codec::failed, json's result collected identity clones); golden diff 19 → 14 files. Red-first: `let before = cell.read(); cell.write().push(9); …` printed 4 → 3.
- CPU (node, medians of nine, loadavg 151–169; two earlier rounds agree): bind_each 500/200 960.4 → 952.2 ms (−0.9%); selector 25.5 → 25.9 (+1.6%); drain 89.6 → 86.4 (byte-identical JS — noise floor); raw get() 5.7 → 36.7 (+544%, the ruled cost). Std adds exactly ONE clone site (`SignalCell::get`'s return); corpus census 305 → 319. The whole `-p vilan-core` was in flight at 1169/5241 (0 failed) at report time — the merge gate/seal cover it.
- FIND → OWNER RULING: `shared.read().push(x)` GROWS the cell before and after (a temporary read stays free and a `&mut self` receiver is not a considered position); four doc sites assert the opposite (tour/memory-model.md:172, :240; std/cells.md:36; appendix/gotchas.md:44 "is lost"; spec/memory.md:377 "read() copies"). Rec: a `&mut self` receiver on a temporary aggregate read COPIES (the docs' promise) — the narrow remnant of clone-at-every-read → B item. Open: `compute_shared_read_bindings` calls `collect_written_roots()` — a second whole-program walk (M19 may want it hoisted).
- PROCESS (std-30's merge): `markdown_golden` red a second time — hygiene-30's tour "Reserved words" section landed after std-30 regenerated its golden. Regenerated over the merged tree above the merge. Helper rule: merge_lane now regenerates the mdBook anchor golden whenever a merge touches vilan/docs and folds it into the merge commit before gating.
- PROCESS (build-30's merge): one source conflict, macros.rs `flush_expansion_cache` — hygiene-30's N63 path move (`dist/.cache/macro-expansions`, the temp file renamed with it) met build-30's per-flush temp counter (the two-threads-one-table race); resolved by hand as build-30's counter under hygiene-30's name. The kind git resolves textually elsewhere; the build sees it here.

## solver-30 — all three landed (877bebf3 B261 diagnostics, da549cde B262 fix, 9b722ab6 B263 fix); merge after rule1-30 with --allow-edit `b233_the_implicit_generic_sugar_is_held_to_the_same_rule` (generics.rs; asserted the old face)
- B261: face `impl Add` (`impl Signal<List<i32>>` with arguments) — stands where a type stands, invents no binder (`<P: Add>` would; P is B246's rewrite); scoped to the OPERAND labels (`operand_label`/`implicit_binder_bound`), NOT planted in `pretty_print_type_inner` (that would pre-empt B211's same-trait collision, trait-typed-fields rev-2 Q3 — the owner's); the implicit-subject steer arm hoisted above B233's (before: `<Add: Add<Display>>`, a bound on a name no parameter has). Ledger: no edit (row 346's key is all slots).
- B262: the drain records Struct | Enum | Trait (`nominal_bound_owner`); refusal reuses row 401. Census: 4 bounded structs, 2 bounded traits (tour's `Holder<T: Label>`, types.md's `Doubler<T: Add>`), 0 bounded enums; every written application satisfies → ZERO new refusals; kolt/website zero.
- B263 DIAGNOSIS DIFFERS from the filing: the recovery existed (`self.list.get()` resolves to `List<T>`); `freshen_list_element_slots` ERASED the element because its test ("the element is an unbound generic") is true of a caller's rigid T as much as of `List::new()`'s own parameter; guard `generic_is_enclosing_binder` on the declared-function return path only; not an impl-body rule (a free generic fn failed identically). Tombstone text accordingly.
- FINDS → items: (1) UNSOUND, predates B262: an impl's `with` clause is bound-checked by NOTHING and arity-checked by nothing — `impl CatBox with Holder<Cat>` compiles with `Cat` implementing no `Label`; `impl CatBox with Holder<Cat, i32>` compiles (B188's arity check misses the position); the tour (data-and-traits.md) asserts the opposite in prose — pinned `#[ignore]`d `b262_an_impl_with_clauses_trait_argument_is_still_unchecked` → B item (TOP-class; docs page ahead of the compiler). (2) `freshen_list_element_slots`' EXTERNAL-function path is unguarded (an external method returning `List<T>` through a bounded receiver would still erase) — no exhibit → note on B263's tombstone. (3) the implicit-binder face is top-level only (a binder nested inside a nominal operand renders bare — B218's territory). Open: plant the face in `pretty_print_type_inner` eventually (moves b211/b186 pins + B211 Q3) — with the B211 ruling.
- PROCESS (editor-30's merge): run through merge_lane alone (the fold would have kept HEAD's copy of the edited DOM_STUB const and silently dropped editor-30's additions). Two hand unions: ui_rows.rs's stub (HEAD's capture-aware listeners, measurement, window/dispatchEvent/ResizeObserver from dom-30 + editor-30's `focus()` recording `activeElement`, `matches(":focus")`, and the frame clock) and browser/dom.vl (dom-30's `DomRect`/`ResizeObserver` + editor-30's `request_animation_frame`, inserted at the same spot). Rule: an EDITED shared const in a test file is a hand step — the fold cannot union it.

## m19-t1d — M48 landed (658745f5 the window, fdec5095 the pin + census, aec708ac the measurement; proposals branch merged to main), merge after solver-30
- R10 gets a second `class_a_checks!` block where it already ran; all three tiers skip a reused module (`reused_source` by file for written applications; `container_site_visited` → `world_entity` by entity for inferred sites and native receivers). `ModuleDiagnostics.container_structures: Vec<String>` (keys whose insertion at one of the module's sites returned true), unioned back into `reported_container_structures` by `replay_world_diagnostics` before any check. TypeId boundary: R10 mints per occurrence but types are NOT interned (`type_id_for_type` fresh per call), so an id names one occurrence in one file and the instantiation set is partitionable by construction — no fingerprint (unlike T1c); what R10 reads from outside a module is excluded by B226 + T0's dirty bit (paper).
- Numbers (kolt client, 69/69 reused — the 58 predates the lib/ move; debug, loadavg 120–161): R10 sites visited 153,302 → 24,459; R10 thread CPU 280–340 → 60–70 ms; leg process CPU per keystroke medians 6,230 → 6,070 ms (after-legs at higher load). Release re-measure DECLINED (no quiet box).
- FINDS → items/notes: (1) the corpus replay differential is BLIND to this seam (the golden corpus as modules declares almost no resource and none in a container — the dropped-keys plant leaves it green; T1b's blind-leg shape) → item: an R10 probe (a `List<Guard>` in every third module) — needs a lane owning that shared test file; owner's call. (2) R10's residue = the entry's share (24,459 sites still walked: the tier enumerates `expr_id_to_expr_map`/`variables`/`parameters` whole; ids minted after the world was stored fall in no range) → needs a source→entity index (§3.5's missing module boundary) — the next tranche's shape. (3) R10's early return is entry-sensitive but safe (two entries over one world can disagree about `declares_a_resource()`; both directions sound — paper). (4) HARNESS TRAP: kolt's client is clean only when driven with `pkg_root = <repo>/src` (the manifest's root); the repo root gives 22 spurious "cannot find … in the imported path" and 39 sources — a note on the replay harness's doc (N). (5) `reused_source`/`container_site_visited` are two spellings of one predicate at two granularities (note).
- PROCESS (editor-30's merge, round 2): my stub union dropped `find()`'s closing brace — the conflict hunk ended mid-method with the `}` past the marker, and I joined editor-30's `focus()` straight after `return null;` — a JS SyntaxError that redded all eleven ui_rows tests. Caught by the gate, fixed above the merge. Rule for a hand union of a JS-in-Rust stub: `node --check` the extracted stub before committing (done now, kept as the step).

## smalls-30 — all four landed (7e5649f5 B259, a011c61e B260, 0e0978f0 B265, e7c435d4 B266); merge after m19-t1d with --allow-edit `a49_a_source_that_provides_only_sub_is_refused_and_names_on_change,a52_a_generic_service_subject_is_refused_by_the_expansion` (bounds.rs, traits.rs — both recorded the old behavior); one NEW ledger row (B266) + row 132 EDITED in place (B260)
- B259: `refuse_block_like_head` → `refuse_block_like_continuation` at the four block-bearing heads of `parse_secondary_inner`; recovery consumes a `.` chain WHOLE via `parse_one_postfix`; all four shapes now one diagnostic. LATE FIND: `=>` is a `Token::Op`, so the widened refusal ate match-arm ARROWS (a block-like guard broke — B59's shape); `=>` joined the exclusion set as a separator, pinned. Row 229 (keyless).
- B260: `trait_label_with_arguments` renders `TraitImplCheck::trait_arguments`; head reads `'Source<i32>'`; B197's operator arm shares the slot and follows (open: revert that one line if the operator family should stay unparameterized — rec: keep).
- B265: bind intrinsics to `external fun` only (every std intrinsic name is external — verified); `external_intrinsic_declaration` gates six tables + `list_new_fn_id`/`list_push_fn_id`; new binary `intrinsic_binding.rs` compiles a scratch std with a bodied `remove`.
- B266: `service_generic_refusal` in the `Node::Service` arm above the backend split (B117's pattern), spanning the attribute; 5 cascade → 1.
- FINDS → notes/items: (1) a block-like head followed by `::` takes the parenthesize steer, which is not a fix there (`(match x {..})::foo` is not valid either) — a `::`-specific arm (small E). (2) the intrinsic tables are reachable for a user's own `impl List<type T: Bound>` only for names std has not declared — the reach is std authors (note on B265's tombstone). (3) `resource_derive_refusal`'s message carries NO ledger row (a helper-built `msg:` the ledger's checks cannot reach or miss) → ledger hygiene item (N).
- PROCESS (m19-t1d's merge): `every_std_module_is_clean_under_full_scan` red — std's browser `Event` declared `target` TWICE (dom-30's A59 and std-30's A58, in different spots of dom.vl, both merged cleanly); dom-30's brief said "check origin/std-30 before you push" but std-30 had not pushed yet. Removed std-30's copy above the merge. Rule: two lanes given overlapping members in one file get a NAMED owner for the shared member in the briefs, not a check-before-push.

## view-30 — all three landed (774d68c7 B268, 1cc7e632 A66, 5b22616d A60); merge after smalls-30 as a HAND PASS: ui_rows.rs DOM_STUB edited a THIRD time (text nodes/createTextNode, removeAttribute, a real inline style block, reflecting `hidden`) → union with HEAD's (dom-30 + editor-30) and `node --check`; three other DOM stubs gained `getPropertyValue`; existing expectations moved (`ssr_differential` structure guard, `source_bindings`, two SSR markup pins in vilan-core) → --allow-edit for whatever the fold REFUSES; `add_style_declaration` in `ALLOWED_DIVERGENCES`
- B268 ROOT in the compiler: a bound's ARGUMENTS were dropped when an impl was matched to a receiver — `analyzer::impl_bounds_hold` (`generic_bound_trait_ids`) and `impl_select::subject_applies` (`bound_trait_ids`) — so `impl type S: Source<str>` applied to a `SignalCell<View>`, and two blankets over one trait at different arguments could not coexist (equal rank → "neither impl subject is more specific"). Both now read the bound at its arguments (`instantiated_home_arguments`/`provided_trait_arguments` + `instantiation_agrees`, lenient where undecidable); the emission filter narrows but never empties (`satisfies_trait_bound` threads no arguments through a SUPERTRAIT match — a program the analyzer admits must still find a body). Trait name KEPT `Slot` (it already was the contract; `Child` is the SSR enum; `Render` collides with `render`). New arms `Source<View>` (replace the element) and `Source<List<View>>` (replace the run) + SSR read-once twins; the static `List<View>` arm is A46's door. Red-first: `[object Object]` text.
- A66: `Element::remove_attribute` + `View::toggle_attr` + SSR twin; pins incl. never `"false"`, dead after dispose.
- A60: `show` writes INLINE `display:none` and restores the element's own inline value (new `Element::style_property`; empty removes); mechanism FORCED: `Style::rule` emits app rules UNLAYERED (only child-relations and `declare` get `@layer vilan`), and unlayered author declarations outrank every layer, so no `vilan.overrides` layer could beat `.sX{display:flex}`; `!important` refused permanently. `hidden` attribute still set for selectors/AT; SSR twin writes both. Smalls: `flex_grow` (+ the formatter's order-table row), `Color::current()`. A60 item 3 confirmed a non-gap (`Length::pct(100f)`).
- FINDS → items/rulings: (1) `satisfies_trait_bound` drops trait arguments on a SUPERTRAIT match (v1, documented) — a program with only `Source<str> with Place` still ADMITS `slot(signal_of_panel)`; closing it turns a fallback into a real bound refusal (B item; owner Q1). (2) a reactive element child APPENDS on replacement (like when/swap): `std::dom` has no `insertBefore`, so position is lost after the first change — marker-node fix, its own item (owner Q2: is "last" the contract?). (3) `Signal<View>` leaks by construction (replaced views' bindings belong to the constructing scope) — documented; `swap` stays the per-value form. (4) three DOM stubs had NO `createTextNode` (text children never exercised end-to-end) — fixed. (5) two stale doc claims fixed (attr/child arms; "show … which SVG ignores").
- PROCESS (view-30's merge): the textual head-plus-lane union of ui_rows.rs broke a delimiter (the test-block hunks were cut inside functions). Rebuilt FUNCTION-LEVEL from the three real versions with the fold helper's own extractors: the pre-merge tip's copy + the lane's 8 new fns + 6 carried consts (no lane-edited or deleted fns), and the lane's stub additions applied by hand into the current stub (`hidden` accessors, `StubStyle` replacing the no-op style, `StubText`, `createTextNode`); `node --check` + `cargo test --no-run` before the amend. Rule for the helper: `merge_fold` should attempt the fold on a conflicted test file FIRST and stop only if the file's non-fn text (a const like DOM_STUB) also conflicts — and that const union is a hand step with `node --check`. Queue as a helper change at the sweep.

## perf-30 — all four landed (fd51eb16 M49, 2ae38a3e M50 fix, c993961c M52 REFUSED with numbers + pin, 7fe26f99 M53), merging after view-30; one existing LSP assertion (7 MiB → converted) changed in vilan-lsp/src (not a fold target)
- M49: fingerprint split by DECLARING SOURCE (SourceId(0) = entry; DERIVED/unattributed → the world half); `ModuleTables::drop_nominals_world` the restore condition; pin: two entries differing in a resource share one record (walks 1/10th). `LastUse::compute` re-priced: cold 106M Ir (1.88%), warm 46.7M (1.70%); the restore works (`walk_block` 3.3% of warm) — what it misses is `collect_unfollowable_loans` 31.7M (68%, whole-program by nature: a loan crosses modules → a record DESIGN), restored tables CLONED into the walk state (8.9M), `compute_view_origins` 4.2M. Slice not taken; reported.
- M50: measured 4,701,037 B resident/world vs 194,843 recorded = 24.1× (release and debug agree); leaked ASTs and interned tables are process-global so a per-world figure must NOT carry them — the gap is the analyzer's derived tables. Shipped `BASE_CACHE_RESIDENT_BUDGET` 512 MiB / `BASE_CACHE_WEIGHT_FACTOR` 24 / default = quotient; `VILAN_BASE_CACHE_BUDGET_MIB` re-denominated; M46's 64 MiB re-derived from the resident constant (would have been divided by 24). Pins: measured factor within 3× of shipped (declines without /proc/self/statm) + LSP denomination.
- M52 REFUSED: kolt's seed sets share only `json`; the CLIENT is browser-colored so no union merges it; phase line base = 681.8 / 73.2 / 74.0 ms — the browser leg is 82% of base and unreachable; a union would win 74 of 829 ms, pay a superset world per leg, and put an unimported module's impls in scope (not observation-neutral). Pin: legs share one world exactly when seed sets agree. Follow-up NAMED: module-level sharing across the platform axis (the node legs load 48/49 sources from disjoint seeds — the FILES overlap) — M19's next tranche or its own item (owner Q).
- M53: cold kolt client 5.632e9 → 5.170e9 Ir (−8.20%), exhibit −4.54%, warm re-analysis −12.86%; control: the three prior commits move the cold check +0.0006%. Path handling → `Program::source_layers` (`_starts_with` 261M → 0; `Components::next` 179M → 0; `arrival_by` 311M → 76M; warm `platform_color::check` 7.95% → 2.83%); `const_eval::State::new` 120.6M → 98K (`LocalIndex` OnceCell + `source_lookup`); `infer_type_path` Cow −0.238% (the estimate confirmed); `Node` 96 bytes (five item declarations boxed by field; `node_size.rs` ceiling 96, stops).
- FINDS → items/notes: (1) on a WARM analysis `post_analysis_passes` is 62.7% (57.1% after M53) — the warm profile is a different shape and lane profiling has only ever used the cold one → rule for M briefs: profile both. (2) `Program::source_of` is still a linear scan (~60 ranges), now the residual in `arrival_by`; `source_lookup` exists but is not the default → small M. (3) M46's byte budget was silently coupled to M24's denomination (fixed; note). Open: stop CLONING restored tables into `LastUse`'s walk state (8.9M Ir/analysis; needs `&mut Analyzer` or a read-through layer) → M item.
