vilan-native: 85 host gaps
  a `lazy` parameter
  a value captured by an `is` test outside an `if` condition (only `if subject is Pattern(let x)` restructures into an `if let`)
  a value of an unbound generic type parameter (parameter 1 of `map`)
  a value of type `an unresolved type`
  a value of type `any`
  an `await`
  the async function `etag_of`
  the expression form `a destructuring binding`
  the expression form `an `async` block`
  the host binding `alloc`
  the host binding `as_f64` (`Number`)
  the host binding `code_at`
  the host binding `coerce_bool` (`Boolean`)
  the host binding `coerce_f64` (`Number`)
  the host binding `coerce_i32` (`Number`)
  the host binding `coerce_i53` (`Number`)
  the host binding `coerce_str` (`String`)
  the host binding `coerce_u32` (`Number`)
  the host binding `column_text` (`__db_column`)
  the host binding `copy_into`
  the host binding `decode`
  the host binding `destroy`
  the host binding `destroyed`
  the host binding `exec`
  the host binding `fill`
  the host binding `get`
  the host binding `has_json_field` (`Object.hasOwn`)
  the host binding `headers`
  the host binding `is_directory`
  the host binding `label_i32` (`Number`)
  the host binding `label_i64` (`Number`)
  the host binding `label_u64` (`Number`)
  the host binding `len`
  the host binding `listen`
  the host binding `method`
  the host binding `mtime_ms`
  the host binding `node_create_server` (`createServer`)
  the host binding `now_millis` (`Date.now`)
  the host binding `on_bytes`
  the host binding `on_event`
  the host binding `on_signal`
  the host binding `on_upgrade`
  the host binding `open`
  the host binding `parse_json_value` (`JSON.parse`)
  the host binding `prepare`
  the host binding `queue_microtask` (`queueMicrotask`)
  the host binding `random_bytes` (`__random_bytes`)
  the host binding `raw_stat` (`__fs_stat`)
  the host binding `read_bytes` (`readFile`)
  the host binding `read_file_encoded` (`readFile`)
  the host binding `read` (`__read_asset`)
  the host binding `remote_address_raw`
  the host binding `set_timeout`
  the host binding `size`
  the host binding `slice`
  the host binding `statement_first` (`__db_get`)
  the host binding `statement_run` (`__db_run`)
  the host binding `to_json`
  the host binding `to_json` (`JSON.stringify`)
  the host binding `to_string_encoded`
  the host binding `url`
  the host binding `with_finally` (`__with_finally`)
  the host binding `write_bytes`
  the host binding `write_text`
  the host binding `write`
  the host type `Bytes`
  the host type `Database`
  the host type `Hash`
  the host type `JsonValue`
  the host type `NodeRequest`
  the host type `NodeResponse`
  the host type `NodeServer`
  the host type `NodeSocket`
  the host type `Row`
  the host type `Statement`
  the intrinsic `CanonicalHash`
  the intrinsic `Env`
  the intrinsic `HashEq`
  the intrinsic `JsonElements`
  the intrinsic `JsonField`
  the intrinsic `JsonIsNull`
  the intrinsic `JsonKind`
  the intrinsic `JsonTag`
  the intrinsic `ListSortBy`
  the intrinsic `TryParseJson`
