# Order 38 — running record (integrator)

Opened 2026-09-21 off 0fa109eb (Order 37's sealed tip 31c13567 + 9e4ab461 Dependabot-against-next + 0fa109eb the 1.98.1 toolchain pin). Ten lanes — see go-items38.json. Rulings R1–R8 all as recommended ("Go with all recs (including the before go points)").

- GO: 0fa109eb pushed to origin/next (9e4ab461..0fa109eb); worktrees are cut only after its CI run reads green (first CI run under 1.98.1 — the rust-cache is cold). A110/A111/A112/B359 + briefs38 + this file committed at GO.
- Watch-outs from Order 37: lanes kill by PID only; a lane that cannot sign says so at its first commit; a rowed message is one `\`-continued literal; a deleted list-const entry is named like an edited one; new test files use the scratch root; a new crate runs agents_map + third_party_notices; the split golden regenerates once over the merged tree, judged gensym-normalized; verify every `--test` name against crates/<crate>/tests before a merge; never launch a merge before reading the previous one's outcome.
- LAUNCHED 2026-09-21: CI 35605476267 GREEN on 0fa109eb (all eleven jobs — the first run under 1.98.1); ten worktrees cut off origin/next = 0fa109eb in vilan/.claude/worktrees/<lane>; ten Opus agents launched (model passed explicitly), kolt + proposals READ-ONLY for every lane. Cross-lane handoff: reactive-38 writes `scratchpad/reactive-38/door2-measurement.md`, the one file papers-38 reads outside its own scratch. native-b-38 is rebased once after native-a-38 merges.

## papers-38 — FIRST to report (45 min, 141 tool uses): both papers written, eleven probes captured, worktree clean (nothing committed)
- `proposal/incremental-collections.md` (797 lines) COMMITTED from the lane's scratch; the door-2 section is STAGED at `sweeps/order38/papers-38/reactive-turns-order-within-a-wave.md` (spliced into reactive-turns.md as §7 at the sweep, after reconciling with reactive-38's measurement — theirs is a second opinion, not a replacement); probes + outputs at `sweeps/order38/papers-38/probes/`, the door-2 std patcher at `papers-38/door2/` (the seven patched std copies, 13 MB, NOT kept — rebuildable from `patch_door2.py`).
- HEADLINES: THE LAW holds — 400 turns, 1–4 random ops per batch, log limit 30, two lagging cursors: 555 checks / 0 failures, 1 notification per turn, `g` 1,223 calls vs 6,651 naive. Delta-driven `each`: one push into 1,000 rows = 22,347 Ir vs 358,454,848 Ir for today's pass = 16,040×, and today's pass is QUADRATIC (95.1 M / 358.5 M / 1,369.9 M Ir at 500/1,000/2,000 rows). Door 2: zero output differences over 130 runnable corpus programs; per settle at 50 effects 418,341 → 898,917 Ir (phase split, 2.15×) → 2,227,882 (naive id sort, 5.33×; the sort is 60 %).
- CONTRADICTIONS of the items as filed (stamp A112/A110 at the sweep): `Splice`'s `removed` must be `List<T>` (the group fold and `Delta::Remove` are unwritable from a count), `SetAt` carries both values; `Move` must be admitted at S1 (a later enum arm breaks every consumer's match); a per-element OWNER is not enough — a position is not an identity, the element needs a stable per-element cell (the index-capture probe crashes); A110's own probe is NOT a shape door 2 changes (inner created before outer) — the pin is the NESTING shape; door 2 ALONE still fires the disposed observer — it needs door 1's flag AND FIND-2.
- RULINGS the papers ask (recs): A112 §13 Q1 `map_each`/`filter_each`; Q2 two hand lists, no macro; Q3 `Move` an op at S1 (the wire translator lowers it to `Delta::Reset`); Q4 `KeyedCursor` → `DeltaCursor` in std::reactive with a deprecated re-export for one release; slice order S1 (lift) → S3 (`each`) first, S2 may wait. A110 door 2: make effect order within one settle a CONTRACT, build in Order 39 on top of door 1, FIND-2 first.
- FINDS for newitems38: FIND-1 `reconcile`'s key-matching scan is O(N²) per change (every `each` over a List source; A98 fixed the DOM half only) — MEDIUM–HIGH perf; FIND-2 `Subscription::dispose`'s scrub reads only `turn_scope.get_safe()`, so a dispose from inside a DRAIN scrubs nothing (four-line fix: fall back to `draining_turns.read().last()`) — RELAYED to reactive-38 by SendMessage (the tool exists in this build) to fold in beside door 1; FIND-3 a `$VILAN_STD` with no `macro_std` sibling is not refused by name (derive silently fails, `compare.vl` blamed) — B346's promise unkept on the user-configured path, adjacent to N105; FIND-4 = A112's recorded `ListCell::new()` note (B162's rule; intended).

## native-b-38 — SECOND to report (65 min, 153 tool uses): J6 BUILT — 86e1f79f (executor), 5b76a243 (the emitter's async arms); both signed; merge QUEUED after native-a-38 (one rebase)
- `vilan-rt/src/executor.rs` (1,693 lines incl. pins): the seven primitives, DEPENDENCY-FREE, `#![forbid(unsafe_code)]` intact, NO `RawWaker` — `Waker::noop()` + the current task's id written into the wake list of the thing awaited; a panic caught at the poll boundary (`catch_unwind` over `Future::poll`) latches a task's failure and gives the nursery join / `with_finally_async` their catch-across-an-await; a cancellation is its own payload TYPE. Pins: `-p vilan-rt` 25/25 (18 new, one per ordering rule; four proven RED against the rule removed — the drain's exhaustion 10 reds, one macrotask per turn 1, the eager spawn 1, the detached `__fail` override 1); vilan-cli `native_differential` +3 fns (`every_async_corpus_program_is_identical_or_named`, `the_spawn_and_sleep_ordering_is_byte_identical_on_both_backends`, `a_timers_verdict_is_byte_identical_on_both_backends`), new const `ASYNC_SUITE` (no row deleted from `PLATFORM_MODULES`; no existing fn/const edited there).
- ASYNC CORPUS: 1 byte-identical (`await-postfix.vl`), 6 refused BY NAME (host bindings `sleep`/`setTimeout` ×2 and `new Date` — F18's; generics ×3 incl. `reactive-turns.vl` — S1b's; `nursery.vl` — the context hidden parameter bound through a clause-typed parameter), 0 broken; two emitted probes byte-identical; `Pin`/`Send`/`Box::pin` appear 0 times in emitted code. Nursery helpers + `Promise.all`/`race` are IMPLEMENTED in the executor but NOT WIRED into `runtime_host_binding` (every path to them is generic) — ~20 lines at the rebase once S1b lands.
- Gates: core 5,999/5,999 (8 skipped); cli corpus/harness_scratch/native_differential/release_scripts 41/41; agents_map + third_party_notices 4/4; clippy; fmt. No `.vl` touched, no golden moved.
- THE WHOLE-SET DIFFERENTIAL IS RED AT THE BASE 0fa109eb (measured by checkout, not assumed): 117 enumerated / 29 identical / 84 refused / 4 BROKEN — §10's "17 verified, 0 differing, 0 rustc-refused" was the default suite's view; §11 carries the measured census. The four, all native-a-38's by ownership and RELAYED to it by SendMessage: (1) MISCOMPILE HIGH `side-effect-let.vl` `0\n0` vs `1\n2` — `value_of`'s `reads_a_loaned_parameter` copies a loaned parameter in argument position incl. a mutating intrinsic's receiver (`(xs).clone().push(..)`); (2) `mut-parameters.vl` — no `mut` on the binder (E0384/E0596); (3) `option-view.vl` — a view inside an enum payload loses its `&mut` + an E0425; (4) `module-dirs.vl` — `stage()` copies files only. Minor: PRELUDE allow list += `non_camel_case_types`, `unreachable_patterns`. LESSON for the seal: the seal runs `VILAN_NATIVE_DIFFERENTIAL=1`.
- FINDS for newitems38: the four above if native-a-38 does not close them; the executor's slab never reuses a settled slot (one word per spawn forever — a free list + generation before F18's per-request spawns); OQ-1 printing a host handle panics at runtime (rec: an emitter-side compile-time refusal); OQ-2 `settle_all` raises the first failure IN ORDER where `Promise.all` raises the first to SETTLE (rec: accept + document); OQ-3 real clock kept; OQ-4 a failing `async fun main` exits 101 with Rust's banner vs node's 1 (fold into a native exit-code item); OQ-5 (rule now, rec YES): `context.rs` mints hidden parameters record-less, so the emitter INFERS the `Option`-vs-bare flavour from call sites — record the flavour where the parameter is minted (~5 lines) and delete the inference; unblocks `nursery.vl`.
- CORRECTIONS to native-apps.md §10 for §11: (2) ONE timer per turn then the microtask queue again (firing every due timer is a different program — proven red); (3) a `Weak` cannot WAKE a parked sleep — the nursery body also carries a cancel-waiter list and a descendant list; the `RawWakerVTable` design is reversed (it would cost `unsafe`); (4) the body's own failure CANCELS the nursery before the drain; the brief under-scoped "the async arms" — three non-async gaps closed first (the context hidden parameter, a field read off a call with no recorded return type → the declaration is the fallback, the four host handles owing `PartialEq`/`Js`); `reactive-turns` cannot be the ordering pin this order (generic) — an emitted probe is.
- REBASE MAP (crates/vilan-rust/src/lib.rs): ADDED `ASYNC_MAIN_BODY`, `ContextFlavour`, fields `context_flavours`/`current_origin`, fns `compute_context_flavours`, `flavour_of_argument`, `context_parameter_declaration`, `context_parameter_type`, `awaited_type`, `declared_return_type`, `async_spawn`, `await_of`, `call_awaits`, the thin `call` wrapper, `runtime_host_binding`; TOUCHED `Emitter`/`new`/`run`, `nominal_struct` (+4 arms), `type_of`, `binding_name`, `ensure_function` (the `is_async` refusal DELETED), `parameter_declaration`, `expression` (+3 arms), `closure`, `call` → `call_expression` (the `awaited_calls` refusal DELETED).

## std-38 — THIRD to report (91 min, 223 tool uses): all five landed, 9f6a174c (five signed commits; +828/−25 over 20 files); no golden moved, no ledger row, no existing test fn/const edited; merge RUNNING (disjoint from hygiene/editor, so it does not wait for them)
- A104 b74a51cf — `Json`/`FromJson` for `Result<T, E>` as ONE new block at json.vl's end, externally tagged by variant NAME (`{"Ok":7}` / `{"Err":"nope"}`) — byte-identical to `encode_json` and to `[derive(Json)]`'s one-payload variant; the shape guard runs BEFORE the tag read. NOT kolt's `[0, v]`/`[1, e]` array shape (the brief said "the shape kolt hand-writes" — overruled by the lane for codec consistency; ACCEPTED): dropping kolt's hand pair CHANGES kolt's persisted/wire format — the sweep's kolt note says so; `[0,7]` is a pinned refusal so the transition fails loudly. Pins: vilan-core `inference` std_surface.rs ×3.
- A109 28bd50ba — `Result::expect_err(lazy message)`; the lazy differential re-run over 306 programs: ZERO differences (estate call sites: 0). Pins: inference lazy.rs ×2; a section COMMENT in lazy.rs edited (Four → Five).
- I4 454a8c94 — only THREE were missing (`List`, `Map<K: Hashable, V>`, `Set<T: Hashable>`; `str`/`bool`/`Option`/numerics already had one — the item over-counts and its heads omit `Hashable`); each impl in its own type's module; `[derive(Default)]` over a struct holding a container now admitted (kolt prefs.vl's shape). Pins: std_surface.rs ×4.
- I3 594d2e6a — `filter_map` twice: the ADAPTER (`FilterMapped<I, T, U>` in iterator.vl) and the EAGER `List` twin in option.vl beside `find` (list.vl has no `Some` in scope by design). Pins: inference iterators.rs ×5. I3 STAYS OPEN (S6/`Iterable` waits on B4). Q1: keep both (rec; as landed).
- A106 9f6a174c — `AlignItems`, `Cursor`, `TextAlign` join `std::style::prelude` (`Length` was ALREADY reachable — the item is wrong). R7 CORRECTION: `import std::style::prelude::*;` IS NOT VALID VILAN (no wildcard import); the documented idioms are the brace list and `import std::style::prelude as tokens;` (`as css` is refused — keyword). Pins: inference styling.rs ×4. DOOR-2 CENSUS: NOT ZERO — 6 file-level shadows (kolt lucide `divide`/`focus`/`space`; docs `struct Cursor` in std/collections.md, `scale_step` + `space` in spec/const.md), 40 local shadows, 122 redundant imports (website 64); door 2 NOT taken. A106 stays OPEN re-pointed at door 2 behind FIND-2.
- Gates: core 6,017/6,017 (8 skipped); cli corpus/examples/release_scripts 39 (every golden byte-identical), styling/split/service_layer/shared_census 72, ledger/harness_scratch/fmt_declines/book_mirrors 27; clippy; fmt; `vilan fmt` ×8 no declines.
- PROCESS — THE SHARED `.git/config` WAS BROKEN: `[commit] gpgsign = user.signingkey` (not a boolean → every `git commit` in every worktree dies while git iterates the config; `-c` cannot override). std-38 did NOT touch it and committed through plumbing (`write-tree`/`commit-tree -S`/`update-ref`, running `pre-commit` by hand). By the time the integrator looked (mtime 10:12) the line read `gpgsign = true` — SOME LANE wrote the bad value (a mistyped `git config`, likely answering "a lane that cannot sign says so") and some lane repaired it; both are edits to shared config no lane should make — find which in the reports; TEMPLATE RULE: "never run `git config` — signing is already configured globally; if a commit fails, REPORT the error text, do not reconfigure".
- FINDS for newitems38: FIND-1 HIGH `[derive(Json)]` on an enum CRASHES on a non-object document (`__json_tag` → `Object.keys(null)` throws; `Shape::from_json("null")` → TypeError) against `FromJson`'s never-crash contract — one line in transformer.rs's helper table + the interpreter.rs twin; moves every derive-using golden → its own Order 39 item; FIND-2 `std::style::prelude` publishes 21 internal ALIAS spellings (`export *;` re-exports aliased imports: `open_style`, `scale_step`, `*_condition` ×17, `value_piece`, `custom_property`) — 21 of its 67 exports; hide them, then re-census door 2; FIND-3 `json_boundary.rs` is a usage census, not a behaviour harness — fix the pin-location list. BRIEF CORRECTIONS: the website checkout is `vilan-website` (30 .vl), not `website`; `vilan-playground` has 6 .vl.

## hygiene-38 — FOURTH to report (118 min, 306 tool uses): all ten landed, 11 signed commits (plumbing — the shared config was broken, see std-38's note); merge QUEUED behind std-38's
- N100 ef8cce46 — check (2) searches EVERY fragment; 288 of 532 rows carry more than one (the item said 283/558). BRIEF CORRECTION: rows 494/499 needed NO re-key — the needle was un-normalized (`{{` in a key is the format-string spelling check (3) needs); 0 rows red. Pins (vilan-cli `diagnostics_ledger`): `n100_a_reword_of_a_fragment_that_is_not_the_longest_reds_its_row`, `n100_a_key_written_with_a_doubled_brace_is_searched_as_the_blob_spells_it`. EDITED existing fn: `every_indexed_row_still_lives_in_the_tree`.
- N99 ce0bbd24 — two `NEW` rows (`asset::staged`'s registry refusal; the derive load-ordering arm in macros.rs) + `ROWS_THE_ENUMERATION_CANNOT_REACH` +2 (list const EDITED, no deletions). Pin `n99_the_two_unreachable_messages_are_rowed_at_the_files_that_build_them`.
- N98 c4ae316d (+5b0faf43) — gate `no_prose_literal_swallows_a_line_continuation` + inverse `every_deliberate_run_is_still_written` over all 8 crates' src/ AND tests/; two rules (mid-sentence run ≥ 8, line-leading run ≥ 5), structural exclusions, `RUNS_THAT_ARE_DELIBERATE` = 3; 38 sites fixed in 9 files (module_resolution 6, check_scope_differential 9, lsp document.rs 12, hmr 3, build_hooks 1, grammar_ebnf 4, macros 1, formatter 2) — EVERY LATER LANE's new literals meet this gate at their merge.
- N101 244fda88 — parse frame re-measured 33.38 KiB/level debug, 8.12 release (recorded 71.8/20.3 not reproducible); re-keyed at deep_nesting.rs, parsing.rs `PARSE_DEPTH_LIMIT`, main.rs `COMPILER_STACK_SIZE`; canary `a_forty_five_level_nesting_still_parses_on_libtests_own_two_mib_thread` (planted red at 1 MiB). HANDED BACK: reactive_lifetimes.rs's headnote (reactive-38's file) — integrator's one-liner after that merge.
- N104 05032e16 — doc comments at `Expr::Number` / `Expr::StructInitializer` IN analyzer.rs (two hunks outside its ownership, named); ledger walkers derive the crate list from the manifest (5 → 8 crates; nothing reddened), pin `every_workspace_crate_is_walked`.
- N105 6bdbf94a — `std_package_dir` reads the manifest's location (bounded; two candidates); pins vilan-core --lib `n105_a_source_root_less_std_resolves_its_own_toolchain_root` (planted red), `n105_a_src_rooted_std_still_…`.
- N103 71df73c0 — `CompileGoal::Run`; pin `a_run_writes_no_build_directory_into_the_package` (vilan-cli `macro_expansion_cache`), planted red.
- N102 c931d39d — `BINARIES_STILL_ON_THE_SHARED_TMPFS` 38 → 3 (35 list entries DELETED, named in the report; the 3 left — reactive_lifetimes, ssr_differential, ui_rows — are reactive-38's this order: one line each AFTER its merge, integrator's); `PATHS_THE_BINARY_OWNS` +3 (install, module_paths, fs — each with its reason).
- N107 7a9f2e70 — the python generator's table mode DELETED (140 lines); the Rust gate is the one implementation. N107's second face (`Error.footnotes`) waits for B355's gate (solver-38).
- L23 53fc9913 — `release.yml` gains `workflow_dispatch` + five tag guards; the asset-list check; the published set unchanged. actionlint absent on this host (script executed against a staged dir: exit 0 / exit 1 on a missing archive). INTEGRATOR MUST TRIGGER after the merge: Actions → release → Run workflow on `next`; expect the four publishing jobs SKIPPED, the rest green — the precondition for the upload-artifact v7 / download-artifact v8 Dependabot PRs.
- Gates: core 6,002/6,002; cli corpus/diagnostics_ledger/release_scripts/harness_scratch 59; lsp 798; ide 13; wasm 41; clippy; fmt. No golden moved.
- FINDS for newitems38: FIND-1 HIGH-ish — analyzer.rs:11946 `Expr::StructInitializer(struct_id, _) => nominals.contains(struct_id)` compares an INITIALIZER id against struct DEF ids → always false (the drop planner's resource-reaching gate under-reports when the type is not yet recorded); fix via `struct_initializer_to_def`; FIND-2 `vilan build` exits before `depth_stats::report()` on a parse-bound refusal; FIND-3 analyzer.rs:28280's stale walk-frame figure; FIND-4 the shared git config (process).

## editor-38 — FIFTH to report (131 min, 316 tool uses): all eight landed, 8 signed commits; merge QUEUED (after std-38, hygiene-38)
- THE CONFIG: editor-38 is the lane that REPAIRED `commit.gpgsign` (`git config commit.gpgsign true`; backup at scratchpad/editor-38/git-config.before) because it could not commit at all — a strict repair, accepted after the fact, but still a shared-config edit; the lane that BROKE it is not yet known. Co-author trailer on lanes' commits reads "Claude Opus 5 (1M context)" — left as is (truthful).
- E198 55394ca0 — `Program::std_sources` split into RESIDENCE (`std_sources`) and FREEZING (new `frozen_sources`, the disk-only subset); consumers re-pointed one by one (`seal_frozen_ranges`, context.rs `library_spanned` → frozen; the A99 steer, chunks.rs ×3, the LSP rename refusal → residence). Pins NEW: vilan-wasm `compile` `a_retired_view_method_carries_the_a99_steer_in_the_playground` (planted red); vilan-core `check_scope_differential` `an_overlaid_std_module_stays_std_but_stops_being_frozen`. EDITED fns: `std_sources_are_recorded_and_the_entry_is_not`; the doc comment of `the_playground_compile_path_never_calls_the_split_emitter`. OPEN Q1: LSP rename over an OPEN std buffer now refuses (rec keep).
- N108 7329fc47 — THE ITEM'S DIAGNOSIS WAS WRONG: not a braced import list — `WouldChangeTheCode` has no span and names the file's FIRST item; the real bug is `needs_semicolon` forwarding through `Node::Const` unconditionally, so a `const { … };` statement printed with no terminator. Pin vilan-core --lib `formatter::const_declaration_printing::a_const_expression_statement_keeps_its_terminator`. kolt: `vilan fmt --check src` exit 2 → 1 (zero declines; one legitimate reformat at client.vl:77 — kolt's, at the owner's word).
- E197 a1147c08 — both editor front-ends on `reprint`; LSP `window/showMessage` once per file per cause (keyed on the decline sentence); vilan-wasm `format_checked` → `{ text, declined }` (`format` stays String-shaped for the deployed glue). Pins: vilan-lsp `formatting_decline_notice_tests` ×5; wasm `a_construct_the_printer_cannot_render_names_itself`. EDITED fns: `a_misindented_program_formats_to_the_canonical_layout`; RENAMED `a_program_that_does_not_parse_formats_to_itself` → `…_and_says_so`; `a_broken_source_formats_to_no_edit`. OWED in the WEBSITE repo: the playground calls `format_checked` (K-section item at the sweep).
- E201 e47143ce — the quick fix on `A_CSS_DECLARATION_IS_A_CALL` (the codemod's rules table-for-table; declines glued values/`!important`) + a FILE-WIDE bulk action (not per block — named deviation, rec keep). Pins vilan-lsp ×7. CONST EDITED: `A_CSS_DECLARATION_IS_A_CALL` in parsing.rs — visibility only (`pub`); rule-statement delta +0/−0. editor.md quick-fix count Twelve → Fourteen (N.B. hand-fold if another lane adds a quick fix — none briefed).
- E194 a632218b — `wordPattern` = VS Code's default WIDENED (hyphen-joined words, up to two leading dashes); pins vilan-cli `vscode_extension` ×2 (run through node's RegExp), wasm `element_head_completion_offers_a_hyphenated_attribute_under_its_own_prefix`. Named price: unspaced `a-b` reads as one word (rec accept).
- E195 9d748536 — `nominal_id_by_name` through the scope walk (`path_entity_id` shared); pins vilan-lsp ×3 — one differential (planted red), two regression guards BY THE LANE'S OWN ADMISSION (std walks first, so the old first-match was already right for `Style`).
- E196 d8a7d262 — the HMR overlay and the terminal read ONE `transformer_refusal_source(&Error)`; pins vilan-cli `transformer_refusal_source_tests` ×2; NO end-to-end HMR pin (no reachable transformer refusal with a non-entry note could be constructed).
- E200 e9dd3732 — one report for a lazy argument through a resource field (a recorded set `lazy_argument_resource_refusals`; R9's firing narrowed, text unchanged); pins inference lazy.rs ×2 (planted red).
- Gates: core 6,003; core --lib 803; lsp+ide+wasm 870; cli vscode_extension/grammar_sync/release_scripts/diagnostics_ledger/corpus/hmr/hmr_overlay 105; clippy (one new 1.98.1 `question_mark` in its own diff, fixed); fmt; wasm32 check. No golden moved.
- FINDS for newitems38: (1) `reprint` compares TOKEN STREAMS and never re-parses its output — a lost-and-gained pair would print a file that does not parse; rec: re-parse and decline; (2) `WouldChangeTheCode` names the file's first item — bisect the two token streams and report the first divergence's span; (3) B318 S4's ADMISSION MISS may be unreachable — a 4-module reproducer compiled clean (either dead code or a looser rule than visibility.md §3.5 says); neither source-carrying transformer note has a pin; (4) E194's non-VS-Code half: `Completion` carries no `filter_text`/edit range; (5) kolt owes one `vilan fmt` on client.vl.

## native-a-38 — SIXTH to report (130 min, 341 tool uses): F19 + F1 S1b + the host census; three commits built by plumbing UNSIGNED → RE-SIGNED by the integrator by rebase (tree identical): e0421335 (F19), 835c29d2 (S1b), c118d23b (census); merge QUEUED (std → hygiene → editor → … → native-a → native-b)
- F19 — `vilan-embedded-std` gains a second table (`RT_FILES`/`RT_CONTENT_HASH`/`RT_MANIFEST`; root `~/.vilan/rt-cache/<hash>/vilan-rt/`; the manifest GENERATED — the crate's own is a workspace member); `native.rs::runtime_crate` = three roots in B346's order (`$VILAN_RT` wins and never falls through; else the checkout carrying `vilan/std/vilan.toml`; else the embedded copy); `vilan cache prune` sweeps it. Pins: vilan-cli `install` `an_installed_binary_builds_natively_against_its_embedded_runtime`; vilan-embedded-std `embedded_std` ×2.
- S1b — monomorphisation by reproducing the JS emitter's MECHANISM (substitution threaded through the walk, structural instance key, reservation before the body); generic structs/enums one Rust item per instantiation; generic-parameter dispatch + trait-default specialization; module-level bindings as `thread_local!` `RefCell`s; `Weak<T>` + six Shared/Option intrinsics; `PartialEq`+`Js` for `Shared`/`Weak`; `if x is P(let y)` → `if let`; inferred return types; `*cell = v` → `Shared::set`. BRIEF CORRECTION (its Q1): the JS instance set CANNOT be consumed — `Transformer::instances` is minted during the JS walk with `js::Node` values; what is shared is `impl_select::*` + the dispatch helpers. Pins (vilan-cli `native_differential`): `one_declaration_at_two_types_emits_two_rust_items`, `a_string_literals_escapes_mean_the_same_thing_on_both_backends` (a LIVE escape miscompile found and fixed), `the_boxed_binding_count_is_reachable_and_counts_the_right_bindings`; `the_probes_board_program_names_its_own_gap` REWRITTEN; const `DEFAULT_SUITE` EDITED (+6); fn `stage` split (recursive copy).
- THE TABLE (whole set): emitted 33 → 47; refused by name 84 → 70; byte-identical 29 → 47; differing 1 → 0; rustc-refused 2 → 0; broken 4 → 0 (all four relayed base finds closed). Refusals after, by construct: `is`-capture outside an `if` 14; `lazy` parameter 12; overloaded operator 6; `for` over an `Iterator` impl 4; field read of an unresolved subject 4; `Hash` 3; `__emit_asset` 3; `Math.pow` 2; destructuring 2; backed enum 4; spread 2; singles.
- `board.vl` DID NOT FLIP — every wall S1a named is gone; ONE remains: an `is`-test capture outside an `if` condition (also 14 corpus refusals and one of kolt's nine). The order's native exit is NOT reached → a FOLLOW-UP slice after the native merges (the lane agent resumed on the merged tree): the `is`-capture restructure (`while`, `&&`, tail), with FIND 3 (`Option<&mut T>` payload view) closed FIRST, then N106.
- N106 NOT DONE (the escape half of the class was the live miscompile above; the f64-boundary and non-BMP halves untouched) — stays open, in the follow-up slice.
- C15 measured: 1 boxed binding over 47 programs (iterator.vl) — keep waiting. Compile CPU: emit mean 0.353 s, cargo+rustc +0.521 s mean (debug, loadavg 58–64).
- KOLT'S SERVER LEG (Order 39's brief; census kept at sweeps/order38/native-a-38/kolt-server-census.txt): 85 gaps — 55 host bindings (`createServer`, the `__db_*` eleven, `JSON.parse`/`stringify`, fs/socket/header accessors, coercions), 11 host types, 10 intrinsics (`Json*`, `CanonicalHash`, `HashEq`, `ListSortBy`, `Env`), 9 language constructs (await/async = J6's; `lazy`; the `is`-capture; destructuring; an unbound generic through `map`). Reproduce: `VILAN_NATIVE_HOST_CENSUS=1 vilan build --backend rust --stdout src/server.vl`.
- Gates: native_differential default AND whole-set 7/7; vilan-rt 7; core 5,999 (vilan-core untouched); cli corpus/diagnostics_ledger/agents_map/harness_scratch/third_party_notices/install/release_scripts; clippy; fmt. No golden moved; no ledger row (row 501's `{what}` tail only).
- REBASE MAP for native-b-38: signature changes `ensure_function` (→ `(id, &subst) -> Result<Reserved>`, body split into `function_body`), `ensure_struct`/`ensure_enum` (+`arguments`), `variant_path`, `struct_literal`, `pattern` (+`subject_type`); `expression` → thin wrapper over `expression_inner`; `rust_type` → `rust_type_inner`; `call` gained dispatch arms + `call_arguments`; `parameter_declaration` (+`mut`, + a MINIMAL `context_parameter_declaration` — PREFER native-b's `compute_context_flavours`, delete native-a's); `closure`'s parameter loop calls `parameter_declaration`; deleted `function_name`, `started`, `started_types`; `PRELUDE` +6 allows.
- FINDS for newitems38: (1) `analyzer::RENDER_MEMBER` + `transformer::unescape_string` are `pub(crate)` → copied into vilan-rust (widen to `pub`); (2) LIFT the monomorphisation RESOLUTION half out of transformer.rs into a shared vilan-core module (eight `&self`-over-`Program` fns now exist twice) — M; (3) latent wrong type `Option<&mut T>`; (4) the `is`-capture (M) and (5) `lazy` parameters (M, `Rc<OnceCell>`-shaped) are the two biggest blockers; (6) operators + backed enums are cheap wins; (7) `Map`/`Set` have no `impl Js` in vilan-rt; (8) rename `vilan-embedded-std` → `vilan-embedded`; Q2 a nominal's generic parameter arrives in TWO spellings (enum payload: the constraint id typed `Any`; struct field: `Generic(..)`) — solver hygiene; Q3 cross-link B357 (an open `Tree<any>` at the constructor site).

## reactive-38 — SEVENTH to report (131 min, 298 tool uses): A110 door 1, FIND-2, A105, A73 landed — four signed commits (tip 96726f96); door 2 MEASURED (file kept at sweeps/order38/reactive-38/door2-measurement.md + both instrument patches); merge QUEUED (goldens)
- THE CONFIG, CLOSED: reactive-38 ADMITS it — its first inspection command was `git config commit.gpgsign user.signingkey` (a read typed as a write) into the SHARED `.git/config`; editor-38 repaired it; std-38, hygiene-38, native-a-38 committed by plumbing around it (native-a's unsigned, re-signed by the integrator). All five reported code lanes verified by `gpgsig` header at the merge queue. TEMPLATE RULE stands: lanes never run `git config` — not even to read (use `git config --get`… no: use NOTHING; signing is global).
- A110 door 1 2f93d7a2 — one liveness cell SHARED between a `Subscriber` and its `Subscription`, lowered by `dispose` BEFORE any removal; `drain`'s wave loop and `notify`'s inline arm skip a lowered one. THREE faces closed: the inline snapshot; the WAVE COPY (`drain` takes a wave out of `pending` before running it); the scrub's blind spot (= FIND-2). Cost: one `Shared<bool>` per `observe` subscription (deferrals share a module-level `always_live`); std `Shared::new(` 128 → 131 (reactive.vl 26 → 29); goldens' `__shared_new` 129 → 140; +9.1 % Ir per 12-notification wave (152,387 → 166,325; ≈1,162 Ir per notification, the order of C14 S3's +590). Pins (vilan-cli): `reactive_lifetimes::a110_a_disposed_observer_does_not_fire_from_an_inline_notifys_snapshot`, `…_from_a_wave_the_drain_already_took_out`, `ui_rows::a110_nested_swaps_on_one_source_build_no_orphan_subtree_on_sign_out` — all planted red.
- FIND (load-bearing for kolt): A110 face 2 IN THE DOM IS A HARD CRASH on 0fa109eb — `TypeError: Cannot read properties of null (reading 'insertBefore')` (the inner form's `open_row` against an anchor the closed region removed). Kolt's `page_content` latch is what keeps the app from THROWING, not an optimisation — stamp A110.
- FIND-2 2b06202c — `dispose` resolves its turn as `notify`/`defer_to_turn` do (ambient, else draining); pin `reactive_lifetimes::find2_a_dispose_reached_from_inside_a_drain_scrubs_the_draining_turns_queue` reads the QUEUE LENGTH from inside the drain (two claims, red against two different plants). Rec taken: KEEP the scrub, fixed (it is what drops a torn-down subtree's closures out of a live queue). Needs a tracker id at the sweep (file-and-close).
- A105 c511ffe5 — `bind_attr` over a new `AttrBinding` trait (two impls: `Source<str>`, `Source<Option<str>>`; vilan admits no overloading — probed); both twins; pins `ui_rows::a105_…` ×2. FIND-3: A105's premise is WRONG — element syntax does NOT admit an `Option` attribute (A89 gave it to `Style::attribute`/`within` only); candidate item: `AttrValue` arms for `Option<str>` / `Source<Option<str>>`. Kolt: 1 definition (styles.vl:36) + 1 call site (views.vl:62) to retire.
- A73 96726f96 — `Storage::keys()` (snapshot; tolerant of a missing index), `Debounce::flush()`; pins `storage_handle::a73_…`, `debounce::a73_…`. Kolt: lib/storage.vl's `delete_unknown_keys` simplifies; theme.vl:141's debounce is NOT a flush customer.
- DOOR 2 MEASURED (nothing of it on the branch): +23.5 % Ir per wave built BUCKETED (229,042 vs a like-for-like control 185,507), 2.2× built as a naive partition-and-sort — the ruling reads the bucketed figure; NO corpus output changes (126 runnable); of 315 targeted tests exactly three move — the nested-`swap` pin IMPROVES (builds/teardowns 3/3 → 2/2), one sibling-observer reorder, and the no-cycle gate `unmounted cycles 0 → 1` which is the INSTRUMENT's wrapper closure (the build slice re-measures). Agrees with papers-38 on both corrections (A110's own probe is siblings, not nesting; door 2 does not subsume door 1). Rec: take the rule AS A CONTRACT ABOUT NESTED FORMS, built bucketed; effect order among independent observers of one source is explicitly NOT part of the contract. RECONCILE at the sweep: papers-38 measured 2.15×/5.33× at 50 effects (a different instrument) — the section carries both, the bucketed build as the design.
- Goldens moved: commit 1 — nine corpus (`blanket-impl`, `reactive-flatten`, `reactive-on-change`, `reactive-owner`, `reactive-selector`, `reactive-turns`, `reactive`, `signal-update`, `spread-parameters`) + split `app.js`; commit 2 — six corpus + all four split artifacts (gensym renumbering only). RUNTIME OUTPUT byte-identical both times. EDITED: `shared_census.rs` const `CENSUS` (reactive.vl row 26 → 29) + the literal 128 → 131 in `the_shared_census_matches_the_committed_table` — WIRE-38 EDITS THE SAME CONST AND LITERAL (json/binary rows; 128 → 118): at wire-38's merge the literal is 131 − 10 = 121, hand-fold; `vilan/test/reactive-on-change.vl:60`'s `// witness:` gensyms re-keyed — and will need RE-KEYING AGAIN after the merged-tree regeneration (FIND-4: the witness gate is a literal substring; normalize gensyms or admit `$*`).
- Gates: core 5,999; docs 11; cli reactive_lifetimes 9, reactive_channels 16, ui_rows 37, ssr_differential 1, service_layer 44, shared_census 2, corpus 11, split 13, examples 6, release_scripts 22, debounce 4, storage_handle 3; clippy; fmt; `vilan fmt` ×6.
- FINDS for newitems38: FIND-1 (the DOM crash — A110's stamp); FIND-3 `AttrValue` Option arms; FIND-4 gensym-naming witnesses; FIND-5 the drain's per-wave `NativeMap::new()` vs per-id `remove`: −35.9 % under the JIT, +11.5 % jitless, measured under load — re-measure on a quiet host. BRIEF CORRECTIONS: the item's turn-half probe is face 1 (door 2's), not door 1's; "builds zero times" holds only inline — the DOM pin asserts the creation/disposal PAIR.
- INTEGRATOR after this merge: reactive_lifetimes.rs's headnote (N101's hand-back), the three tmpfs lines (N102's remainder: reactive_lifetimes, ssr_differential, ui_rows).

## solver-38 — EIGHTH to report (147 min, 488 tool uses): B359, B354, B356, B357, B355 (+E191) landed — five signed commits (tip 488d404a); E199 NOT BUILT (no recoverable probe); merge QUEUED
- B359 f35e178b (MISCOMPILE; R1 = (a)) — the analyzer records the DECLARING trait at every default-body `self`-dispatch; all three `OnType(None, ..)` seams take it (method call, the `for` protocol member, an operator operand — the transformer's binary arm now reads `bound_dispatch_traits`). Two residues fixed that no pin reached: a SUPERTRAIT-declared member under a sub-trait-only clause (std's own `Source<T>::sub`; kolt's `StorageSignalCell`) via `impl_select::select_member`; and `List::new`/`List::push` — the two compiler-lowered externals with neither an `Intrinsic` row nor an `[extern]` binding — get `Dispatch::ListNew`/`ListPush`. CENSUS (corpus 131, examples 15 entries, every docs fence, kolt copy): TWO sites, ONE shape — `Ord::clamp`'s `self.min(max).max(min)` over an integer now reaches `Ord`'s own defaults instead of the inherent `Math.min`/`Math.max` externs: `vilan/test/number-math.mjs` golden MOVED (+34 lines; RUNTIME OUTPUT byte-identical, verified under node), docs std/numbers.md:128's fence (no golden), kolt one shape (no source change owed). Pins vilan-core `inference` traits.rs `b359_…` ×9 (7 planted red, 2 controls). OPEN Q1: `clamp` over integers costs two `compare` calls now — rec leave it (R1 as ruled); a std item for Order 39 if the host intrinsics are wanted back (overrides in each `impl <int> with Ord`).
- B354 1ec627ef (HIGH) — `Program::admitting_file` takes `note_source_of` (the sentinel made the refusal's own advice untakeable). Order 36's curation re-run: THREE of six re-privatized (`impl JsonWriter`, `impl JsonReader`, `impl BinaryReader` — the plain blocks); the three TRAIT-impl visitor blocks STAY exported and say why (a derived visitor's admitting file is the USER's; privatizing reds `vilan/test/crypto.vl`). Pins `module_resolution` `b354_…` ×2. OWNERSHIP OVERLAP: six header lines in json.vl/binary.vl (wire-38's files) — wire-38 TOLD by SendMessage.
- B356 82e486b2 — five path shapes, not six (`pkg::T<..>::f` does not parse); the ONE inert shape was the VARIANT CONSTRUCTOR (`seed_variant_subject_bindings`). Pins bounds.rs `b356_…` ×5. FIND: `Option<i32>::Some("y")` now refuses at the payload but the type still settles `Option<str>` → a second report downstream (E189 family).
- B357 60b65151 — a payload-less variant takes the landing constraint's arguments, else one `Unknown` per parameter; `binding_is_weaker`'s second arm DELETED (B352's three pins green without it). Pins `b357_…` ×2. (native-a-38's Q3 fallbacks can now go — cross-link.)
- B355 + E191 488d404a (R3) — `dispatch_refine::dispatch_fallbacks()` the fence's non-diagnostic face (five fallback paths now record themselves); the broad gate: with diagnostics already standing the context pass pushes nothing and ONE WARNING counting the checks that did not run — one ledger row `NEW`. KOLT COPY: `href(href())` restored at views.vl:276 (login.vl no longer exists): 35 errors → 1 + `Warning: 34 context checks did not run`; unplanted 0/0 either way. E191 CLOSES. EDITED fns (vilan-core `inference`): traits.rs `b279_an_unresolvable_dispatch_site_still_fences_its_candidates`, `b279_an_unresolvable_level_above_the_dispatch_still_fences` (onto the fallback set); macros.rs `b229_only_the_unresolved_run_s_own_context_stands_down`, `b241_a_genuinely_uncovered_read_beside_an_arity_error_still_fences` (restated as deferrals) + two NEW pins on programs that type.
- E199 UNFINISHED — solver-37's probe is not recorded anywhere; the lane located the seam (the `constructions` loop's declared-bound check, analyzer.rs ~6785–6860: `declared_bindings` should be grounded from constraints reachable through OTHER constraints' declared bounds first — B347's channel). Stays OPEN with that stamp; S once a repro exists.
- Gates: core 6,019; module_resolution 207; docs 11; lsp 798; cli corpus 11, examples 6, split 13, diagnostics 37, diagnostics_ledger 19, release_scripts 22; clippy; fmt.
- FINDS for newitems38: (1) a bindingless `external fun` emits a DANGLING mangled name at an ORDINARY call site (`external struct Thing; impl Thing { external fun poke(..) }` → `$b(t, "x")` undefined, clean at check) — MISCOMPILE-class; rec: refuse a user-written `external fun` with no `[extern]` and no lowering; (2) `List::new`/`push` should be `Intrinsic` rows (touches vilan-rust lib.rs:1652/1655 — after the native merges); (3) `Self<i32>` is an accepted spelling with no use — refuse; (4) B356's two-diagnostic cascade. PROCESS: `vilan build vilan/test/<x>.vl` OVERWRITES the corpus golden in place (the lane did it, restored it) — a Mechanics line; `vilan run <package>` writes `/tmp/vilan-run-<pid>.mjs` (N103's family — check hygiene-38's N103 covers it at the merged tree).
