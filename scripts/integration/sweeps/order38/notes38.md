# Order 38 — running record (integrator)

Opened 2026-09-21 off 0fa109eb (Order 37's sealed tip 31c13567 + 9e4ab461 Dependabot-against-next + 0fa109eb the 1.98.1 toolchain pin). Ten lanes — see go-items38.json. Rulings R1–R8 all as recommended ("Go with all recs (including the before go points)").

- GO: 0fa109eb pushed to origin/next (9e4ab461..0fa109eb); worktrees are cut only after its CI run reads green (first CI run under 1.98.1 — the rust-cache is cold). A110/A111/A112/B359 + briefs38 + this file committed at GO.
- Watch-outs from Order 37: lanes kill by PID only; a lane that cannot sign says so at its first commit; a rowed message is one `\`-continued literal; a deleted list-const entry is named like an edited one; new test files use the scratch root; a new crate runs agents_map + third_party_notices; the split golden regenerates once over the merged tree, judged gensym-normalized; verify every `--test` name against crates/<crate>/tests before a merge; never launch a merge before reading the previous one's outcome.
- LAUNCHED 2026-09-21: CI 35605476267 GREEN on 0fa109eb (all eleven jobs — the first run under 1.98.1); ten worktrees cut off origin/next = 0fa109eb in vilan/.claude/worktrees/<lane>; ten Opus agents launched (model passed explicitly), kolt + proposals READ-ONLY for every lane. Cross-lane handoff: reactive-38 writes `scratchpad/reactive-38/door2-measurement.md`, the one file papers-38 reads outside its own scratch. native-b-38 is rebased once after native-a-38 merges.

## papers-38 — FIRST to report (45 min, 141 tool uses): both papers written, eleven probes captured, worktree clean (nothing committed)
- `proposal/incremental-collections.md` (797 lines) COMMITTED from the lane's scratch; the door-2 section is STAGED at `sweeps/order38/papers-38/reactive-turns-order-within-a-wave.md` (spliced into reactive-turns.md as §7 at the sweep, after reconciling with reactive-38's measurement — theirs is a second opinion, not a replacement); probes + outputs at `sweeps/order38/papers-38/probes/`, the door-2 std patcher at `papers-38/door2/` (the seven patched std copies, 13 MB, NOT kept — rebuildable from `patch_door2.py`).
- HEADLINES: THE LAW holds — 400 turns, 1–4 random ops per batch, log limit 30, two lagging cursors: 555 checks / 0 failures, 1 notification per turn, `g` 1,223 calls vs 6,651 naive. Delta-driven `each`: one push into 1,000 rows = 22,347 Ir vs 358,454,848 Ir for today's pass = 16,040×, and today's pass is QUADRATIC (95.1 M / 358.5 M / 1,369.9 M Ir at 500/1,000/2,000 rows). Door 2: zero output differences over 130 runnable corpus programs; per settle at 50 effects 418,341 → 898,917 Ir (phase split, 2.15×) → 2,227,882 (naive id sort, 5.33×; the sort is 60 %).
- CONTRADICTIONS of the items as filed (stamp A112/A110 at the sweep): `Splice`'s `removed` must be `List<T>` (the group fold and `Delta::Remove` are unwritable from a count), `SetAt` carries both values; `Move` must be admitted at S1 (a later enum arm breaks every consumer's match); a per-element OWNER is not enough — a position is not an identity, the element needs a stable per-element cell (the index-capture probe crashes); A110's own probe is NOT a shape door 2 changes (inner created before outer) — the pin is the NESTING shape; door 2 ALONE still fires the disposed observer — it needs door 1's flag AND FIND-2.
- RULINGS the papers ask (recs): A112 §13 Q1 `map_each`/`filter_each`; Q2 two hand lists, no macro; Q3 `Move` an op at S1 (the wire translator lowers it to `Delta::Reset`); Q4 `KeyedCursor` → `DeltaCursor` in std::reactive with a deprecated re-export for one release; slice order S1 (lift) → S3 (`each`) first, S2 may wait. A110 door 2: make effect order within one settle a CONTRACT, build in Order 39 on top of door 1, FIND-2 first.
- FINDS for newitems38: FIND-1 `reconcile`'s key-matching scan is O(N²) per change (every `each` over a List source; A98 fixed the DOM half only) — MEDIUM–HIGH perf; FIND-2 `Subscription::dispose`'s scrub reads only `turn_scope.get_safe()`, so a dispose from inside a DRAIN scrubs nothing (four-line fix: fall back to `draining_turns.read().last()`) — RELAYED to reactive-38 by SendMessage (the tool exists in this build) to fold in beside door 1; FIND-3 a `$VILAN_STD` with no `macro_std` sibling is not refused by name (derive silently fails, `compare.vl` blamed) — B346's promise unkept on the user-configured path, adjacent to N105; FIND-4 = A112's recorded `ListCell::new()` note (B162's rule; intended).

## native-b-38 — SECOND to report (65 min, 153 tool uses): J6 BUILT — 86e1f79f (executor), 5b76a243 (the emitter's async arms); both signed; merge QUEUED after native-a-38 (one rebase)
- `vilan-rt/src/executor.rs` (1,693 lines incl. pins): the seven primitives, DEPENDENCY-FREE, `#![forbid(unsafe_code)]` intact, NO `RawWaker` — `Waker::noop()` + the current task's id written into the wake list of the thing awaited; a panic caught at the poll boundary (`catch_unwind` over `Future::poll`) latches a task's failure and gives the nursery join / `with_finally_async` their catch-across-an-await; a cancellation is its own payload TYPE. Pins: `-p vilan-rt` 25/25 (18 new, one per ordering rule; four proven RED against the rule removed — the drain's exhaustion 10 reds, one macrotask per turn 1, the eager spawn 1, the detached `__fail` override 1); vilan-cli `native_differential` +3 fns (`every_async_corpus_program_is_identical_or_named`, `the_spawn_and_sleep_ordering_is_byte_identical_on_both_backends`, `a_timers_verdict_is_byte_identical_on_both_backends`), new const `ASYNC_SUITE` (no row deleted from `PLATFORM_MODULES`; no existing fn/const edited there).
- ASYNC CORPUS: 1 byte-identical (`await-postfix.vl`), 6 refused BY NAME (host bindings `sleep`/`setTimeout` ×2 and `new Date` — F18's; generics ×3 incl. `reactive-turns.vl` — S1b's; `nursery.vl` — the context hidden parameter bound through a clause-typed parameter), 0 broken; two emitted probes byte-identical; `Pin`/`Send`/`Box::pin` appear 0 times in emitted code. Nursery helpers + `Promise.all`/`race` are IMPLEMENTED in the executor but NOT WIRED into `runtime_host_binding` (every path to them is generic) — ~20 lines at the rebase once S1b lands.
- Gates: core 5,999/5,999 (8 skipped); cli corpus/harness_scratch/native_differential/release_scripts 41/41; agents_map + third_party_notices 4/4; clippy; fmt. No `.vl` touched, no golden moved.
- THE WHOLE-SET DIFFERENTIAL IS RED AT THE BASE 0fa109eb (measured by checkout, not assumed): 117 enumerated / 29 identical / 84 refused / 4 BROKEN — §10's "17 verified, 0 differing, 0 rustc-refused" was the default suite's view; §11 carries the measured census. The four, all native-a-38's by ownership and RELAYED to it by SendMessage: (1) MISCOMPILE HIGH `side-effect-let.vl` `0\n0` vs `1\n2` — `value_of`'s `reads_a_loaned_parameter` copies a loaned parameter in argument position incl. a mutating intrinsic's receiver (`(xs).clone().push(..)`); (2) `mut-parameters.vl` — no `mut` on the binder (E0384/E0596); (3) `option-view.vl` — a view inside an enum payload loses its `&mut` + an E0425; (4) `module-dirs.vl` — `stage()` copies files only. Minor: PRELUDE allow list += `non_camel_case_types`, `unreachable_patterns`. LESSON for the seal: the seal runs `VILAN_NATIVE_DIFFERENTIAL=1`.
- FINDS for newitems38: the four above if native-a-38 does not close them; the executor's slab never reuses a settled slot (one word per spawn forever — a free list + generation before F18's per-request spawns); OQ-1 printing a host handle panics at runtime (rec: an emitter-side compile-time refusal); OQ-2 `settle_all` raises the first failure IN ORDER where `Promise.all` raises the first to SETTLE (rec: accept + document); OQ-3 real clock kept; OQ-4 a failing `async fun main` exits 101 with Rust's banner vs node's 1 (fold into a native exit-code item); OQ-5 (rule now, rec YES): `context.rs` mints hidden parameters record-less, so the emitter INFERS the `Option`-vs-bare flavour from call sites — record the flavour where the parameter is minted (~5 lines) and delete the inference; unblocks `nursery.vl`.
- CORRECTIONS to native-apps.md §10 for §11: (2) ONE timer per turn then the microtask queue again (firing every due timer is a different program — proven red); (3) a `Weak` cannot WAKE a parked sleep — the nursery body also carries a cancel-waiter list and a descendant list; the `RawWakerVTable` design is reversed (it would cost `unsafe`); (4) the body's own failure CANCELS the nursery before the drain; the brief under-scoped "the async arms" — three non-async gaps closed first (the context hidden parameter, a field read off a call with no recorded return type → the declaration is the fallback, the four host handles owing `PartialEq`/`Js`); `reactive-turns` cannot be the ordering pin this order (generic) — an emitted probe is.
- REBASE MAP (crates/vilan-rust/src/lib.rs): ADDED `ASYNC_MAIN_BODY`, `ContextFlavour`, fields `context_flavours`/`current_origin`, fns `compute_context_flavours`, `flavour_of_argument`, `context_parameter_declaration`, `context_parameter_type`, `awaited_type`, `declared_return_type`, `async_spawn`, `await_of`, `call_awaits`, the thin `call` wrapper, `runtime_host_binding`; TOUCHED `Emitter`/`new`/`run`, `nominal_struct` (+4 arms), `type_of`, `binding_name`, `ensure_function` (the `is_async` refusal DELETED), `parameter_declaration`, `expression` (+3 arms), `closure`, `call` → `call_expression` (the `awaited_calls` refusal DELETED).

## std-38 — THIRD to report (91 min, 223 tool uses): all five landed, 9f6a174c (five signed commits; +828/−25 over 20 files); no golden moved, no ledger row, no existing test fn/const edited; merge RUNNING (disjoint from hygiene/editor, so it does not wait for them)
- A104 b74a51cf — `Json`/`FromJson` for `Result<T, E>` as ONE new block at json.vl's end, externally tagged by variant NAME (`{"Ok":7}` / `{"Err":"nope"}`) — byte-identical to `encode_json` and to `[derive(Json)]`'s one-payload variant; the shape guard runs BEFORE the tag read. NOT kolt's `[0, v]`/`[1, e]` array shape (the brief said "the shape kolt hand-writes" — overruled by the lane for codec consistency; ACCEPTED): dropping kolt's hand pair CHANGES kolt's persisted/wire format — the sweep's kolt note says so; `[0,7]` is a pinned refusal so the transition fails loudly. Pins: vilan-core `inference` std_surface.rs ×3.
- A109 28bd50ba — `Result::expect_err(lazy message)`; the lazy differential re-run over 306 programs: ZERO differences (estate call sites: 0). Pins: inference lazy.rs ×2; a section COMMENT in lazy.rs edited (Four → Five).
- I4 454a8c94 — only THREE were missing (`List`, `Map<K: Hashable, V>`, `Set<T: Hashable>`; `str`/`bool`/`Option`/numerics already had one — the item over-counts and its heads omit `Hashable`); each impl in its own type's module; `[derive(Default)]` over a struct holding a container now admitted (kolt prefs.vl's shape). Pins: std_surface.rs ×4.
- I3 594d2e6a — `filter_map` twice: the ADAPTER (`FilterMapped<I, T, U>` in iterator.vl) and the EAGER `List` twin in option.vl beside `find` (list.vl has no `Some` in scope by design). Pins: inference iterators.rs ×5. I3 STAYS OPEN (S6/`Iterable` waits on B4). Q1: keep both (rec; as landed).
- A106 9f6a174c — `AlignItems`, `Cursor`, `TextAlign` join `std::style::prelude` (`Length` was ALREADY reachable — the item is wrong). R7 CORRECTION: `import std::style::prelude::*;` IS NOT VALID VILAN (no wildcard import); the documented idioms are the brace list and `import std::style::prelude as tokens;` (`as css` is refused — keyword). Pins: inference styling.rs ×4. DOOR-2 CENSUS: NOT ZERO — 6 file-level shadows (kolt lucide `divide`/`focus`/`space`; docs `struct Cursor` in std/collections.md, `scale_step` + `space` in spec/const.md), 40 local shadows, 122 redundant imports (website 64); door 2 NOT taken. A106 stays OPEN re-pointed at door 2 behind FIND-2.
- Gates: core 6,017/6,017 (8 skipped); cli corpus/examples/release_scripts 39 (every golden byte-identical), styling/split/service_layer/shared_census 72, ledger/harness_scratch/fmt_declines/book_mirrors 27; clippy; fmt; `vilan fmt` ×8 no declines.
- PROCESS — THE SHARED `.git/config` WAS BROKEN: `[commit] gpgsign = user.signingkey` (not a boolean → every `git commit` in every worktree dies while git iterates the config; `-c` cannot override). std-38 did NOT touch it and committed through plumbing (`write-tree`/`commit-tree -S`/`update-ref`, running `pre-commit` by hand). By the time the integrator looked (mtime 10:12) the line read `gpgsign = true` — SOME LANE wrote the bad value (a mistyped `git config`, likely answering "a lane that cannot sign says so") and some lane repaired it; both are edits to shared config no lane should make — find which in the reports; TEMPLATE RULE: "never run `git config` — signing is already configured globally; if a commit fails, REPORT the error text, do not reconfigure".
- FINDS for newitems38: FIND-1 HIGH `[derive(Json)]` on an enum CRASHES on a non-object document (`__json_tag` → `Object.keys(null)` throws; `Shape::from_json("null")` → TypeError) against `FromJson`'s never-crash contract — one line in transformer.rs's helper table + the interpreter.rs twin; moves every derive-using golden → its own Order 39 item; FIND-2 `std::style::prelude` publishes 21 internal ALIAS spellings (`export *;` re-exports aliased imports: `open_style`, `scale_step`, `*_condition` ×17, `value_piece`, `custom_property`) — 21 of its 67 exports; hide them, then re-census door 2; FIND-3 `json_boundary.rs` is a usage census, not a behaviour harness — fix the pin-location list. BRIEF CORRECTIONS: the website checkout is `vilan-website` (30 .vl), not `website`; `vilan-playground` has 6 .vl.
