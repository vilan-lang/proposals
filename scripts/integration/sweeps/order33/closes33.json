{
 "date": "2026-09-11",
 "order": "Order 33",
 "closes": [
  {"id": "A46", "lane": "slots-33", "commit": "7f911239", "text": "BUILT — `<>…</>` lowers to a `List<View>` literal: `ElementBody.tag` is `Option<Span>`, a nameless arm in `parse_element_inner`, `parse_element_children` closes on `</>`, `build_chain` returns `Node::List(children)`; NO lexer change (the pair is span-adjacent `Ctrl` tokens, so the three-place rule does not apply); a garbled fragment declines rather than running `recover_delimited`; nine inference pins, two `ui_rows` pins, four formatter pins (no new formatter rule was needed), three parser pins; spec/grammar.md's `fragment` production, guide/ui.md's Fragments section, std/browser.md. Documented limit: a fragment does not FLATTEN (`<><i/><>..</></>` is a list in a list, refused) — a lowering change if wanted, filed"},
  {"id": "A88", "lane": "slots-33", "commit": "511a33ae", "text": "BUILT (RULED: the boundary removes what it placed) — `Region` gains `live` + `hold(live)`; `close` removes every live node before the anchor; `hold` called where each form writes its bookkeeping; the two REACTIVE child arms (`Source<View>`, `Source<List<View>>`) had NO `defer` at all — content and anchor leaked into any surviving parent — and have one now; the `Source<str>` arm removes its own node too (one node is its run); the static arms keep no defer (a static child belongs to the parent); process twin accepts-and-drops `hold`. Pins: the portal containers empty after disposal, the anchors go with the content; `b268_every_child_arm_…` strengthened. Kolt's overlay `remove()` + `live_panel` are redundant now, at the owner's word"},
  {"id": "N73", "lane": "hygiene-33", "commit": "4d8af1ef", "text": "DONE — one shared DOM stub `tests/support/dom/stub.js` + one extras file per suite; NINE copies in EIGHT binaries consolidated (router.rs held two, one already drifted; the eighth is `dom_events.rs`); no test fn or assertion changed; three harness tails changed mechanically, the byte-for-byte SSR gate proving neutrality. Two stubs remain outside (`mount_missing_id.rs`, `hmr_swap.rs`) — filed"},
  {"id": "N71", "lane": "hygiene-33", "commit": "15a4a067", "text": "DONE — the seam lives in the HARNESS (`debounce.rs`'s `run_project_reporting` hands back both streams; a runtime collector would have moved every task-spawning corpus golden); pin `b277_a_debounce_survives_a_callback_that_throws_and_the_failure_is_reported` (sequence + stderr once), non-vacuous"},
  {"id": "N72", "lane": "hygiene-33", "commit": "3e56f67f", "text": "RULED BY THE LANE, recorded: the nursery-join ORDER is not promised (two independent timers started one statement apart); the pin asserts the set plus four causal invariants, proven not a weakening with the sleeps reversed"},
  {"id": "M61", "lane": "hygiene-33", "commit": "01684b36", "text": "MEASURED, left: both helpers 241 raw / 94 minified bytes; kolt min+gz +10 bytes (0.042%), the split fixture +27 bytes (0.75%); emission is already reachability-gated (a read-only program gets neither); folding would buy part of 94 bytes for a mode argument at every call"},
  {"id": "N69", "lane": "hygiene-33", "commit": "b75a29c2", "text": "DELETED, not narrowed — `element_view_import_note` could only fire for a std without `ui::view`, where its steer misses the same way (wrong-where-reachable); pin; guide/ui.md's stale sentence removed. Its twin `css_style_import_note` filed"},
  {"id": "N68", "lane": "hygiene-33", "commit": "ba0d3554", "text": "FIXED — the 2,500 ms handshake bound is now an ORDER claim (`said_yet(\"verifier:answered\")` after the 429; the 30 s liveness bound is a read bound a green run never pays; 2.8 s); proven red with `authorize_timeout(0)`"},
  {"id": "N65", "lane": "hygiene-33", "commit": "aab095bf", "text": "DONE — `CURATED_RULE_STATEMENTS` (18 named constants with their heads) held both ways by `every_rule_statement_constant_is_curated` and `every_curated_rule_statement_still_opens_as_recorded`; the helper-built resource-derive refusal rowed (ledger row 415); the TSV header's stale count removed. Consequence for every lane: a new named rule-statement constant needs TWO edits (`RULE_STATEMENT_SITES` + the curated list) — E157 met it at the merge"},
  {"id": "E160", "lane": "editor-33", "commit": "3c3c931d", "text": "BUILT — `CursorContext::StructInitializer` recognised in token space (innermost unclosed `{`, a head resolving to a struct bare/qualified/generic, no `=` in the cursor's comma-run; a RETURN-TYPE head `fun f(): KoltStore {` — kolt's own exhibit line — guarded on `Op(\":\")`); fields minus the written ones, the field being retyped kept; detail from `declaration_labels`, doc via `doc_first_paragraph_at`; insertion `name = ` or the bare shorthand over a same-named binding; `Completion::insert` in both front-ends; 21 pins in vilan-lsp `document.rs`"},
  {"id": "E162", "lane": "editor-33", "commit": "97ee2943", "text": "FIXED — a `[..]` sub-rule inside `meta.generic.vilan` (carries the list's `{`/`}` bail-out, not the `;`); no third place (the book's theme has no generic rule); regex pin non-vacuous; verified against the real tokeniser off-tree"},
  {"id": "E157", "lane": "editor-33", "commit": "f1deb784", "text": "FIXED — a curated rule `A_PATH_CANNOT_START_AT_A_BLOCK` with its own site in `refuse_block_like_continuation`'s `::` arm; `RULE_STATEMENT_SITES` 21 → 22 (a keyless row-229 statement — no ledger id); nine `borrows.rs` pins; joined N65's curated list at the merge"},
  {"id": "E159", "lane": "editor-33", "commit": "18395ced", "text": "BUILT — `docs_enum_parity.rs` (the `style_table_sync` shape): fences, enum-headed tables AND prose arm-counts, since A52's defect wore all three; 20 listings over 9 pages in parity; non-vacuous by replanting A52's regression"},
  {"id": "E154", "lane": "editor-33", "commit": "c64e5286", "text": "BUILT (RULED yes) — one line in `print_expr`'s `ForIn` arm; `vilan fmt --check .` a fixed point over the whole tree (no reformat); pins + two fixtures; the fmt appendix gains the block-bearing-head bullet it never had for any head"},
  {"id": "E156", "lane": "editor-33", "commit": "bee7fa0c", "text": "RULED accept-and-document, DONE — E151's rule itself was undocumented, so the appendix gains the element-head order and the bargain as its bullet; and PINNED: `sorting_an_element_head_moves_when_its_values_are_evaluated`, so a later purity check reds without a ruling"},
  {"id": "E158", "lane": "editor-33", "commit": "78c1a8af", "text": "DONE — invariant 1's pin reads EVERY loaded module (~30k rows) and found two real breaches (`pkg` and `Self` indexed against entities they are not, surviving by length coincidence — filed); the three derived-span tables each land on their identifier (pinned); the entry-attributed set is exactly the bodiless namespaces with no second member (the assumed `pkg::<entry>` alias arm does not exist) — a gate installed, no marker"},
  {"id": "B295", "lane": "rpc-smalls-33", "commit": "cba0826e", "text": "FIXED — generated stubs call std MODULE-QUALIFIED (`rpc::call`, `rpc::notify`, every route helper, `reactive::turn`); the generator's import line brings the modules beside the types only; `__`-prefixed `[rpc]`/`[client_service]` parameters refused at the attribute (ledger row 416); a socket round trip with methods named `call`/`arg`/`reply`/`turn`/`notified`/`notify` both directions. Residue: generated METHOD-name collisions (`verify`, `contract_hash`, `dispatcher`, `connect`…) are a duplicate definition qualification cannot help — filed"},
  {"id": "B303", "lane": "rpc-smalls-33", "commit": "a90a5a27", "text": "FIXED — `check_rpc_signatures` records the types it reported as not Wire (descending), and `check_generic_bound_satisfaction` stands a `Wire` bound failure in DERIVED code down at those types; 3 → 1 verified by planting; nested payloads keep one report per mistake; no row"},
  {"id": "B287", "lane": "rpc-smalls-33", "commit": "27d0b3e8", "text": "RULED refuse, BUILT — `async` + `&mut self` on an `[rpc]` method refused at expansion (ledger row 417); the guide caveat says refused; census 0. The premise was half stale: E3 already refused the shape when the body SUSPENDS; the live hole was the non-suspending `async fun f(&mut self)`. A suspending body now takes two refusals — filed"},
  {"id": "B301", "lane": "rpc-smalls-33", "commit": "9b45a253", "text": "FIXED — census first: 28 `[derive(..Wire..)]` sites, exactly ONE relying on the JSON half (std's own `RpcError`, whose `to_json()` the benchmark and ~12 pins call) → `[derive(Wire, Json, Debug, PartialEq)]` written in std, shipped surface unchanged; kolt's five in `shared.vl` use no JSON codec → no kolt edit. Both backends: std's `macro fun Wire` emits the Wire impls alone, `derive_impl_source` gates the JSON pair on `\"Json\"`, the Rust fallback's JSON prelude is Json-only; wire-32's two current-behaviour pins flipped to the admit face (one renamed) + three new; docs synced (E159's gate now holds the `RpcError` fence)"},
  {"id": "B307", "lane": "rpc-smalls-33", "commit": "240720ad", "text": "RULED refuse — and it was ALREADY SATISFIED at 5a0d0b49: B273's third recording site runs B188's arity check over the clause (`impl DogBox with Holder` refused; a defaulted parameter stays elidable). What remained: the padded clause was ALSO conformance-checked, an unsatisfiable second message — a refused clause is now checked no further; six pins; census 120 bare clauses, all naming defaulted traits, nothing newly refused"},
  {"id": "N70", "lane": "rpc-smalls-33", "commit": "97aa7bb1", "text": "DELETED — `service_impl_source` (269 lines), `service_contract_hash`, `RustFallback::any_service` and its prelude; the `Node::Service` `None` arm distinguishes a std WITH `rpc.vl` (B21's load-ordering message) from one WITHOUT (ledger row 418); pin drives a real std copy minus `rpc.vl` through `VILAN_STD`. Its derive twin (`derive_impl_source`'s fallback) filed"},
  {"id": "B276", "lane": "dx-33", "commit": "52842527", "text": "FIXED — ONE hash rule on both sides (`tracked_input_hash_of_bytes`: text where UTF-8 with the BOM dropped, bytes otherwise; `tracked_input_hash` for a path with the directory arm); `asset::read/bundle/bundle_as/digest` record through it and BOTH watch loops verify through it — `current_source_hash` and its unnamed twin in `hmr_round` are one function. Pins: a bundling leg (a `.woff2` AND an ASCII `.txt`) `Fresh` on an untouched round and recompiling on an edit; the record/verify pair over six input shapes. kolt's `build --watch` round: 5.150 s → 1.260 s median CPU (4.1×, at higher load after); the client leg goes from `Compiled` every round to `Fresh`"},
  {"id": "E106", "lane": "dx-33", "commit": "5d48c85b", "text": "MEASURED, the cause NAMED, the fix filed: over 2,000 keystrokes the deliberate per-analysis leak is RECLAIMED every window (the prime suspect refuted) and S5's re-parses are a per-request constant; what grows is one retained analysis per OPEN document (~48 MB per kolt file, 126 MB for views.vl, 864 MB with 18 open) plus glibc never trimming what a closed file gives back (738 → 320 MB in use, 421 MB retained free); the owner's live server read 4.13 GB after 8 h. Instruments `session_growth`/`open_documents` in lsp `document.rs`; gate `a_session_does_not_get_slower_or_leak_across_windows` (growth < 1.5× on the window MINIMUM, retention < 1.1×; non-vacuous) + two `#[ignore]`d 2,000-keystroke drivers. Bounding per-document retention and `malloc_trim` on close are filed as their own items"},
  {"id": "M58", "lane": "dx-33", "commit": "c9528180", "text": "PROFILED (kolt client.vl, warm, callgrind 3.635 G Ir): `post_analysis_passes` 51.07%, `analyze_cancellable` 30.24%, inside: `const_eval::evaluate` 22.77%, `context::thread_contexts` 12.90%, `async_infer::infer` 10.34%; then `call_graph` 9.82%, `dispatch_refine` 9.49%, `source_of` 7.22%, `semantic_tokens` 7.11% — and the last two were ONE row: `semantic_tokens` was 98% `source_of` asked 426,198×/analysis; M27's `source_lookup` hoisted into `semantic_tokens` and `inlay_hints`: 258 M → 25.5 M Ir, whole warm re-analysis −6.44%; pin answer-identical to `source_of`. The `CompletionIndex` twin (14,580×) filed"},
  {"id": "A89", "lane": "styles-33", "commit": "c64ac090", "text": "BUILT — `attribute(name, value: Option<str>, inner)` and `within` alike: `Some(v)` unchanged, `None` = presence (`.sX[data-selected]`, `[data-print] .sX`); `fun not(self, inner)` beside `hover` marks the inner's slots (a pending `!` vs an applied `!token` in the condition grammar, which is what lets a negation nest) and the enclosing combinator emits its own selector negated: `.sX:not([disabled]):hover`, `.sX:not(:hover)`, `:not([data-theme=\"dark\"]) .sX`; an unwrapped `not` is a loud RUNTIME refusal at `class_list` (no const-time plateau exists until B308); a double `not` and a `not` under a media condition fail the build; `pseudo` refuses a `!` head; `child_relation`'s message steers to `attribute`; `STYLE_CONDITION_METHODS` gains `not`. Thirteen pins in `inference/styling.rs`, 28 existing pins swept to `Some(..)`, the corpus `style.vl` gained `gated`; 4 ledger rows; guide + std docs. The mark moved from the slot KEY to the CLASS after `copy_elision_census` caught a per-application `keys()` snapshot (3.1 → 0.1 µs per `class_list`). The negated ancestor guard moves cascade BANDS (`:` vs `[`), pinned; §0bis.6 gains the row at the sweep"},
  {"id": "A90", "lane": "styles-33", "commit": "41b4122d", "text": "BUILT — `check_custom_property_name` (must start `--`, not bare `--`, no space/quote/`:`) called by `Color::var` and `Length::var`; `style_var` declines it deliberately (it appends a whole DECLARATION, and kolt `views.vl:193` writes `.style_var(\"width\", ..)` correctly); 2 ledger rows; nothing newly refused"},
  {"id": "A84", "lane": "styles-33", "commit": "measured", "text": "RULED (direction, 2026-09-11) and MEASURED — data-attribute rules for the STATE axis + custom properties for the COLOUR axis, `Style::when`'s computed merge kept for the local one-off. The numbers: same 25 live rules either way (the variant's 12% longer, and FOUR TIMES the dead intermediates until B308); the class attribute constant at 25 classes; and the deciding one — CPU per rendered button: `when` chain 26.8 µs, a const style 6.7 µs (4×), a const style with the class list hoisted to a `const` str 0.05 µs (~540×) — the attribute form makes the const class list possible, the computed form cannot; custom properties collapse kolt's colour-only rule duplicates (20 rules → 10, ~8% of the sheet). Two conditions carried into the build: B308 first or with it, and the guide says hoist the class attribute to a `const`. The build is kolt's `new_button_style` (A89 made it spellable) plus B308"}
 ]
}
