# Order 33 — running record (integrator)

Opened 2026-09-11 off 5a0d0b49 (Order 32's sealed tip). Eight lanes: solver-33 (TOP), styles-33, rpc-smalls-33, dx-33, editor-33, hygiene-33 (droppable, lands first), slots-33, rpc-33. Briefs: briefs33.md. Ledger next id 415. Rulings: ALL as recommended (owner) — stamped on A85/A46/A88/B299/B307/B287/A87/A84/E163/E154/E156/A79/B298 and transport-rpc §9.6; A90/A91/A92 filed at GO.
- PROCESS (GO): items filed (`--check` then file, own call) BEFORE the lanes launched (rpc-33 reads A92, styles-33 reads A90); the chronicle OPENING entry appended and grep-verified before the commit; worktrees created off origin/next = 5a0d0b49 (verified before `worktree add`); the integrator pushes lane branches at merge time — lanes never push.

## slots-33 — A46 BUILT (7f911239), A88 BUILT (511a33ae), A85 NOT BUILT (55bcc01b: four pins + a tooling entry), first to report (61 min, 189 tool uses); hold its merge until hygiene-33 lands (ui_rows.rs: new fns + one edited fn `b268_every_child_arm_…` + `parse_expr_regression.rs`'s `element_atoms_are_pinned`/`SNAP_ELEMENT` — `--allow-edit` those three)
- A85 — the §5 probe FAILS THREE WAYS: a struct field typed `(sync || View) context owner_scope` is refused ("a `context` clause is only supported on a parameter's closure type"; "a `sync` closure contract is only supported on parameters"; "an injected closure can only be called, forwarded to a parameter with the same clause, or passed to `run`"). Root: a clause is recorded side-band by PARAMETER id (`analyzer.rs:3589` `parameter_contexts`), `Type::Closure` carries none, `context.rs` keys `value_contexts` on locals — a clause flows through no field, generic argument or return. The brief's fallback (plain closure, owner entered in `place`) is NOT a fallback — MEASURED: a closure captures its context at creation, so after the `when` toggled off and its owner was disposed the body's effect still fired (`body sees two`) — every body/row/page would leak into the enclosing boundary; and the methods-as-sugar half is independently impossible (an injected `body` cannot be stored). Landed: `a85_a_context_clause_is_refused_off_a_parameter` (the CONTROL — goes red the day the restriction lifts), two `#[ignore = "A85: …"]` pins, `a85_a_plain_closure_field_answers_to_the_owner_it_was_built_under` (the capture-at-creation consequence as a program). No split-gate work. A85 NEEDS RE-RULING: (a) build contexts-on-fields first (the lane's rec and mine), (b) ship with the ownership divergence documented (no — `each` leaks per row), (c) a `Render` trait the app implements per body (works — context demand threads through calls — but a struct + impl per body). A91 inherits the same wall.
- A46 — `<>..</>` → `List<View>` literal: `ElementBody.tag: Option<Span>`, a nameless arm in `parse_element_inner`, `parse_element_children` closes on `</>`, `build_chain` returns `Node::List(children)`; NO lexer change (the pair is span-adjacent `Ctrl` tokens; the three-place rule does not apply — brief correction); a garbled fragment declines rather than running `recover_delimited` (buries the element's own note otherwise); no new message. Nine `inference/modules.rs` pins (5 of 9 red with the child list emptied), two `ui_rows.rs`, four formatter (NO new formatter rule needed — E150 was the wrong pointer), three parser. Docs: spec/grammar.md (`fragment` production), guide/ui.md (Fragments section; anchors golden regenerated), std/browser.md. Limit pinned: a fragment does not FLATTEN (`<><i/><>..</></>` is a `List<View>` in a `List<View>`, refused) — a lowering change if wanted.
- A88 — `Region` gains `live: Shared<List<View>>` + `hold(live)`; `close` removes every live node before the anchor; `hold` called where each form writes its bookkeeping; the two reactive Slot arms HAD NO `defer` AT ALL (content and anchor leaked into any surviving parent) — they have one now; process twin accepts-and-drops `hold`. WIDENING flagged: the `Source<str>` arm removes its own text node too (no region; one node is its run); the three STATIC arms keep no defer (a static child belongs to the parent). Pins `a88_a_disposed_boundary_empties_the_portal_containers_it_filled`, `a88_the_anchors_go_with_the_content` (non-vacuous twice); `b268_every_child_arm_…` STRENGTHENED (it froze "the last value stays in the DOM"; now asserts the reactive content and anchors are gone, the static arms stay). Split golden regenerated over the lane's tree (app.js only: `Region`'s third field, `hold`, `close`'s loop, `swap`'s `hold`).
- Gates: cli 133 (ui_rows/router/ssr_differential/ssr_fullstack/split/corpus/grammar_ebnf/grammar_sync/release_scripts/diagnostics_ledger/ci_ignored_pins/examples); WHOLE vilan-core 5453 passed; lsp+ide 687; std full-scan; clippy/fmt/`vilan fmt` 0.
- FINDS → candidate items: (F1) B/design: CONTEXT CLAUSES ON STRUCT FIELDS (and generic arguments / returns) — the A85 blocker; a clause in `Type::Closure` (core-enum change, the `_ =>` audit) or a `field_contexts` map + construction/field-read flow rules in `context.rs`; the four `bounds.rs` pins are the exhibits; (F2) a fragment does not flatten — pinned limit; (F3) kolt follow-up: `lib/overlay.vl:539–543`'s `remove()` half + `live_panel` + its per-open defer are redundant after A88 (`overlay.close()` stays); (F4) editor: `<>`/`</>` are painted by neither grammar's element rule (matches `<tag`); the LSP semantic pass paints them.
- OPEN Qs (owner): A85 re-ruling (a/b/c above; rec (a)); the `Source<str>` widening in A88 (in or out — rec IN: one node is its run, the rule has no exception to justify).
- Brief corrections: the §5 fallback line was wrong (both halves fail); "E150 rules for the empty head" was the wrong pointer (no formatter rule needed); the lexer gains no `<>`; the paper's §5 instruction ("come back for a ruling") was the one that held.

## hygiene-33 — all seven landed (4d8af1ef N73; 15a4a067 N71; 3e56f67f N72; 01684b36 M61; b75a29c2 N69; ba0d3554 N68; aab095bf N65), second to report (69 min, 200 tool uses), merge FIRST (started)
- N73: ONE shared DOM stub `crates/vilan-cli/tests/support/dom/stub.js` + one extras file per suite; consumers' consts become `concat!(include_str!(stub), include_str!(<suite>.js) [, tail])`. NINE copies in EIGHT binaries (router.rs held two — its second had drifted to `indexOf`; the eighth is `dom_events.rs`, brief correction). Three rules at the top of the base (harness-only methods in the base; program-reachable shape differences in the suite subclass via `installStubDocument`; `notifyMutation()` no-op hook). No test fn or assertion changed (diffed); three harness TAILS changed mechanically (router popstate via the shared window; ssr_fullstack's reflecting factory; ssr_differential's attribute table as an insertion-ordered map — its byte-for-byte gate proves neutrality). Split golden untouched (no std).
- N71: the seam is in the HARNESS (`debounce.rs`'s `run_project_reporting` hands back both streams) — a runtime collector would move every task-spawning corpus golden for a test's convenience; pin `b277_a_debounce_survives_a_callback_that_throws_and_the_failure_is_reported` (sequence + stderr carries `unhandled task error` with the callback's message ONCE); non-vacuous.
- N72 RULED BY THE LANE (recorded as such): the join ORDER is NOT promised — two independent timers (25/10 ms) started one statement apart; the pin now asserts the set + four causal invariants (holds with the sleeps reversed).
- M61: both helpers 241 raw / 94 minified bytes; kolt min+gz 24,073 → 24,063 (10 bytes, 0.042%); split fixture min+gz 3,577 → 3,550 (27 bytes, 0.75%) — LEFT as two helpers; emission is reachability-gated already (a read-only program gets neither).
- N69: `element_view_import_note` DELETED (wrong-where-reachable: it could only fire for a std without `ui::view`, where the steer misses too); pin `n69_an_unresolved_view_is_reported_without_an_import_steer`; guide/ui.md's stale sentence removed; lsp 679.
- N68: the 2,500 ms handshake bound → the claim asserted as ORDER (`StreamingServer::said_yet("verifier:answered")` after the 429; `VERIFIER_LIVENESS` 30 s is a read bound a green run never pays; 2.8 s); proven red with `authorize_timeout(0)`.
- N65: `CURATED_RULE_STATEMENTS` (18 named constants with their heads) held both ways by `every_rule_statement_constant_is_curated` (population found by asking the tree) and `every_curated_rule_statement_still_opens_as_recorded` (a reword reds the row to edit); `the_helper_built_resource_derive_refusal_is_rowed`; ONE NEW ledger row (the resource-derive refusal); the TSV header's stale "nine statements" count removed. NOTE FOR OTHER LANES: a NEW named rule-statement constant now needs TWO edits (`RULE_STATEMENT_SITES` + `CURATED_RULE_STATEMENTS`) — editor-33 (E157/E154) and styles-33 must know at merge.
- Gates: cli 135 (ui_rows/reactive_lifetimes/source_bindings/ssr_differential/ssr_fullstack/router/split/debounce/service_layer/diagnostics_ledger/release_scripts/dom_events/hygiene); core lib 726, inference 3846, docs; corpus 10 (goldens byte-identical); std full-scan; lsp 679; clippy/fmt 0.
- FINDS → candidate items: (F1) `css_style_import_note` is N69's identical twin (same `.or_else` chain, same false steer "add `import std::style::style;`") — delete the same way (styles-33's neighbourhood; a small); (F2) two DOM stubs remain outside N73: `mount_missing_id.rs` (a `format!` stub) and `hmr_swap.rs` — a follow-up; (F3) `ROWS_THE_ENUMERATION_CANNOT_REACH` is keyed by row NUMBER so a lane's `NEW` row cannot be listed there safely — key it on the row's KEY; (F4) process: a new rule statement costs two edits (above).
- OPEN Qs (owner): (a) should the curated rule statements get real PROSE ledger rows in proposals' diagnostics-ledger.md (the integrator can promote them at the sweep — rec: yes, 18 rows, mechanical); (b) N72's ruling is the lane's, recorded in its CHANGELOG entry.
- Merge hazard flagged: `vilan/docs/guide/ui.md` — hygiene-33 rewrote one sentence in the element-syntax section; slots-33's A46 docs land in the same file (a Fragments section) — resolve by hand if git cannot.

## editor-33 — seven landed (3c3c931d E160; 97ee2943 E162; f1deb784 E157; 18395ced E159; c64e5286 E154; bee7fa0c E156; 78c1a8af E158), E163 STOPPED on its CI half, third to report (87 min, 229 tool uses), merge after hygiene-33 (`--allow-edit every_indexed_span_covers_exactly_an_identifier,every_head_shape_is_a_fixed_point`)
- E160: `CursorContext::StructInitializer { struct_id, open }` recognised in token space (innermost unclosed `{`, a head resolving to a struct — bare/qualified/generic — no `=` in the cursor's comma-run); fields minus the written ones; detail from `declaration_labels`; doc via a new `Analysis::doc_first_paragraph_at`; insertion `name = ` or the bare shorthand when a same-named binding is in scope; `Completion::insert` mapped in both front-ends (lsp main.rs, wasm). A hole the brief did not name: a RETURN-TYPE head (`fun store_for(..): KoltStore {`, kolt store.vl:263 — the exhibit's own line) would have read as an initializer — guarded on `Op(":")` + declaration/control keywords after walking back over a `::` path. 21 pins in vilan-lsp `document.rs` (the brief's "vilan-ide `completion.rs`" was the wrong crate).
- E163 STOPPED: the ruling was not the blocker — CI is. Notices: FREE (`third_party_notices.rs` reads Cargo.lock only; the extension's notices file covers bundled packages, no gate). Cost measured: +2 lockfile packages, +1 MB node_modules, ~1 s warm `npm ci`, 0 advisories. The blocker: NOTHING creates `editors/vscode/node_modules` in `ci.yml`'s test job, `release.yml`'s gate job or `ignored-pins.yml` (only the vsix job and install-dev.sh run `npm ci`) → the scope pins would run nowhere; making them run = an `npm ci` step in two workflows (Windows included) and a fresh clone's `cargo nextest run --workspace` depending on an npm install — release machinery, an unruled second decision. PROTOTYPED off-tree anyway (`tokenize.js` + wasm onig works) and found two real defects on day one (F1, F2 below) — the price is worth paying; needs the owner's one line.
- E162: a `[..]` sub-rule inside `meta.generic.vilan` (carries the list's `{`/`}` bail-out, not the `;`); the book's theme has no generic rule (no third place); regex pin non-vacuous; VERIFIED against the real tokeniser off-tree.
- E157: new curated rule `A_PATH_CANNOT_START_AT_A_BLOCK` (parsing.rs:287) + its site in `refuse_block_like_continuation` (:1151, the `::` arm :1165) — the only parsing.rs lines touched; `RULE_STATEMENT_SITES` 21 → 22 (parser rule statements are row 229's KEYLESS population — NO ledger id minted; next id STILL 415 — brief correction); nine `inference/borrows.rs` pins. NOTE: hygiene-33's N65 now ALSO requires the new constant in `CURATED_RULE_STATEMENTS` — resolve at the merge (editor-33 branched before N65 existed): add the entry or the N65 gate reds.
- E159: `crates/vilan-core/tests/docs_enum_parity.rs` (the `style_table_sync` shape): fences, enum-headed tables AND prose arm-counts ("in six variants:") — A52's defect wore all three; 20 listings over 9 pages in parity; non-vacuous by replanting A52's regression.
- E154: one line in `print_expr`'s `ForIn` arm; `vilan fmt --check .` a fixed point over the whole tree (no reformat commit); pins + two fixtures in `every_head_shape_is_a_fixed_point` (edited fn); the fmt appendix gains the block-bearing-head bullet.
- E156: the doc paragraph — AND E151's rule itself was undocumented, so the appendix gains the element-head order; a pin `sorting_an_element_head_moves_when_its_values_are_evaluated` (`element_head_order.rs`) so a later purity check reds without a ruling.
- E158: invariant 1's pin now reads EVERY loaded module (~30k rows) — found TWO breaches (F3); `every_derived_span_shape_lands_on_its_identifier` (three derived tables); namespace half: `every_entry_attributed_module_is_a_bodiless_namespace` — the entry-attributed set is EXACTLY the bodiless namespaces with no second member (the assumed `pkg::<entry>` alias arm does not exist in either probed shape) — a gate installed, no marker.
- Gates: fmt/clippy 0; `vilan fmt --check .` 0 (non-vacuity proven); ide+lsp 710; cli 94 (grammar_sync/grammar_ebnf/vscode_extension/third_party_notices/release_scripts/diagnostics_ledger/ci_ignored_pins/ci_local_script/element_head_order); core 4667 (docs/markdown_golden/docs_enum_parity/inference/parse_expr_regression/parse_differential/parser_recovery/lib). NOT run: corpus, examples (nothing reaches emission). Flake seen once under load: `quickfix_offers_the_add_import_fix_for_the_view_type_beside_a_hygienic_element` (on-disk-workspace test).
- Touched beyond its surfaces (minimal): lsp `main.rs` (`to_completion_item` +11, fixtures +3), `keystroke.rs` (one field), wasm `lib.rs` (`from_completion` +5), lsp `document.rs` (new tests + `is_namespace_module`'s doc comment), `diagnostics_ledger.rs` (`RULE_STATEMENT_SITES` + one prose line) — none is dx-33's lifecycle.
- FINDS → candidate items: (F1) editor: a CLOSING TAG straight after text is highlighted as a generic list (`<span>hello</span>` → `</span>` gets `meta.generic` — the `generics` begin `(?<=[A-Za-z0-9_])<` and `hello` ends in an identifier char; the commonest markup shape, invisible to the regex pins; found by the E163 prototype); (F2) editor: the `>` closing an opening tag WITH attributes is painted `keyword.operator` (`elements.patterns[1]` is line-start only); (F3) editor/B: `pkg` and `Self` are indexed as references to entities they are not (`pkg` → the package ROOT module; `Self` → the trait) and survive `narrow`'s exact check by LENGTH COINCIDENCE (`pkg`/`std`, `Self`/`Wire`) — a rename of a 3-letter package or 4-letter trait rewrites them and breaks the build; ~40 rows in std; fix at the analyzer's recording site; (F4) editor: scope completion on a BLANK line inside a function offers only globals (`scope_at` falls back to the nearest entity before the offset) — the moment a user actually asks.
- OPEN Qs (owner): (1) E163's CI half — an `npm ci` step in ci.yml's test job and release.yml's gate job (a fresh clone's suite depends on npm), or the scope pins stay out? rec: ADD it — F1/F2 are the proof the regex pins are blind; (2) ledger row 229's prose ("twenty-two by Order 29") was one off before this lane and now coincidentally matches — re-read at the sweep.
- Brief corrections: the notices gate reads Cargo.lock only; E157 mints no ledger row; the `pkg::<entry>` alias arm does not exist; `member_completion_lists_fields_and_methods` lives in vilan-lsp's document.rs.
- MERGED a7c1db5f (hygiene-33, first): no conflicts; ledger row 415 (N65's resource-derive refusal; next 416); parity 310/310; gates green (cli ui_rows/reactive_lifetimes/source_bindings/ssr_differential/ssr_fullstack/router/debounce/service_layer/diagnostics_ledger/dom_events/hygiene/corpus, core inference+docs, std full-scan, lsp, release_scripts, split, fmt); pushed. Next: editor-33 (started; `--allow-edit` two fns; expect the N65 gate to want E157's constant in `CURATED_RULE_STATEMENTS`), then slots-33 (hand folds: guide/ui.md, ui_rows.rs b268 pin, parse_expr_regression SNAP_ELEMENT).
- MERGING c9a72294 (editor-33, second): CHANGELOG 1 hunk restored, parity 317/317, no ledger row; the two edited fns folded by name; the cli gate went RED as foreseen — `every_rule_statement_constant_is_curated` (N65) wanted E157's `A_PATH_CANNOT_START_AT_A_BLOCK` in `CURATED_RULE_STATEMENTS` → added at the merge (a fix commit on next; head = the constant's first source line); ledger+hygiene 19 green; the remaining gates (core docs/markdown_golden/docs_enum_parity/inference/parse_expr_regression; cli release_scripts/split/corpus; fmt) running, push after. RULE for the next order: a lane adding a named rule-statement constant ALSO lists it in `CURATED_RULE_STATEMENTS` (two edits) — put it in the mechanics block.

## rpc-smalls-33 — all six landed (cba0826e B295; a90a5a27 B303; 27d0b3e8 B287; 9b45a253 B301; 240720ad B307; 97aa7bb1 + 98061d1e N70), fourth to report (107 min, 268 tool uses), merge after slots-33 (`--allow-edit` in `inference/traits.rs`: `a_derive_wire_field_may_be_a_hand_implemented_wire_type` + the RENAMED `a_derive_wire_field_typed_map_meets_the_same_pre_existing_json_residue` → `a_derive_wire_field_may_be_a_map` (take the lane's traits.rs whole if the fold cannot express a rename — no other lane touched it since base); in `generics.rs`: `wire_derives_the_json_round_trip`, `derived_wire_visitor_matches_to_json_and_round_trips`)
- B295: generated stubs call std MODULE-QUALIFIED (`rpc::call`, `rpc::notify`, `rpc::arg/reply/reply_source*/notified/decode_failed/send_notification/session_of/bridge/dispose_on_close/reattach_mirrors/dial_for_service*/install_client_handlers`, `reactive::turn`) — the generator's import line (~5119) brings the modules beside the TYPES only, so no method can shadow a free fn; `__`-prefixed `[rpc]`/`[client_service]` parameters refused at the attribute (row NEW; `service_mut_self_refusals` → `service_method_refusals`). Pins: a socket round trip with methods named `call`/`arg`/`reply`/`turn`/`notified`/`notify` both directions; the `__` refusal. Census: 0 hits. OVERLAP with rpc-33 (textual `name(` → `rpc::name(` in the stub's handle arm and `route_block`/`route_outcome`) — the integrator resolves at rpc-33's merge.
- B303: `check_rpc_signatures` records the types it reported as not Wire (descending), `check_generic_bound_satisfaction` stands a `Wire` bound failure in DERIVED code down at those types (keyed on the type); 3 → 1 verified by planting; nested payloads 2 mistakes → 2 reports; no row.
- B287 (ruled refuse): refused at expansion on the written `async` + `&mut self` (row NEW); guide caveat says "refused"; census 0. CORRECTION: the premise was half stale — E3's signature rule already refused the shape when the body SUSPENDS; the live hole was an `async fun f(&mut self)` with no await, which compiled. A suspending body now takes TWO refusals (F2).
- B301: CENSUS FIRST — 28 `[derive(..Wire..)]` sites, none also `Json`; exactly ONE relied on the JSON half: std's own `RpcError` (`benchmarks/throughput.vl:61` and ~12 pins call `to_json()`) → `[derive(Wire, Json, Debug, PartialEq)]` written in std, shipped surface unchanged; kolt's five in `shared.vl` use no JSON codec → NO kolt edit owed. Both backends: std's `macro fun Wire` (json.vl) emits the Wire impls alone; `derive_impl_source`'s two arms gate the JSON pair on `"Json"`; the Rust fallback's JSON prelude is Json-only. Pins: wire-32's two flipped to the admit face (one RENAMED), + `a_derive_wire_field_may_be_a_result`, `a_derive_json_wire_type_carries_both_codecs`, `a_derive_wire_type_has_no_json_codec_of_its_own`; two more EXISTING fns edited (`[derive(Wire)]` → `[derive(Json, Wire)]`, what each tested). Docs: std/encoding.md, tour/data-and-traits.md, std/rpc.md (RpcError fence synced — E159's gate will hold it).
- B307: the RULING WAS ALREADY SATISFIED at 5a0d0b49 — B273's third recording site runs B188's arity check over the clause (`impl DogBox with Holder` refused "takes 1 type argument, 0 given"); the item's "accepted at the clause" was stale. What remained: the padded clause was ALSO conformance-checked ("`held` returns `Dog`, but `Holder` declares `T`" — unsatisfiable second message) → a refused clause is checked no further. Six `b307_*` pins; census 120 bare `with <parameterised trait>` clauses, ALL naming DEFAULTED traits — nothing newly refused.
- N70: DELETED (`service_impl_source` 269 lines + `service_contract_hash` + `RustFallback::any_service` + its prelude); the `Node::Service` `None` arm now distinguishes a std WITH rpc.vl (B21's load-ordering message) from a std WITHOUT one (row NEW); pin drives a real std copy minus rpc.vl through `VILAN_STD`. + a comment-only follow-up.
- Ledger: three NEW rows (the lane wrote 415–417; the integrator renumbers after hygiene-33's 415 → 416–418).
- Gates: clippy/fmt 0; core inference+docs 3830 (40 skipped); std full-scan; cli 123 (service_layer/rpc_http/examples/corpus/release_scripts/diagnostics_ledger/split/ci_ignored_pins — split golden did NOT move); lsp 679; `vilan fmt --check` rpc.vl/json.vl 0. Trailer: `Claude Opus 5 (1M context)` on all seven (left).
- FINDS → candidate items: (F1) B: generated METHOD-name collisions are unhandled and qualification cannot help (a duplicate definition): `[rpc] fun verify` → "'verify' is already defined for 'EchoClient<T>'" (1 error), `contract_hash`/`dispatcher` → 3 each, `connect`/`connect_with`/`with_handlers` → 1 each (`dispatcher_for` clean) — wants a refusal naming the reserved set; (F2) diagnostics: a suspending `async fun f(&mut self)` `[rpc]` now takes TWO refusals (the new one + E3's `async_view_parameter_message`) — deduping needs a channel from expansion-time diagnostics into the analyzer's stand-down set (no seam today); (F3) hygiene: the Rust DERIVE fallback (`derive_impl_source`'s Json/Wire split) is N70's twin, unpinned — tombstone the same way or a fixture-std pin; (F4) kolt: does not build on the branch for the two PRE-EXISTING reasons (the duplicate `Result` Wire impl; B275's `AttrValue`) — known migration debt.
- MERGED c9a72294 + fix 99e822b6 (editor-33, second): gates green after the curated-list fix (core docs/markdown_golden/docs_enum_parity/inference/parse_expr_regression 3893; cli release_scripts/split/corpus 44; fmt); pushed. Next: slots-33 (hand folds), then rpc-smalls-33.

## dx-33 — three landed (52842527 B276; 5d48c85b E106 measurement + gates, family TOOLING not fix; c9528180 M58 profile + one hoist), fifth to report (117 min, 243 tool uses), merge after rpc-smalls-33
- B276: ONE hash rule on both sides — `const_eval::tracked_input_hash_of_bytes` (text where UTF-8, BOM dropped; bytes otherwise) records; `tracked_input_hash` asks it of a path (+ the directory-listing arm); `asset::read/bundle/bundle_as/digest` record through it; BOTH watch loops verify through it — `current_source_hash` is a one-line call and an UNNAMED TWIN (an identical inline closure in `hmr_round`, main.rs:1273) is deleted, so `run --watch` and `build --watch` ask the same function. Pins in `watch_leg_reuse.rs`: a bundling leg (a non-UTF-8 `.woff2` AND an ASCII `.txt` — the text row makes it a hashing bug, not a decoding one) `Fresh` on an untouched round, recompiles on an edit; the recording/verify pair agree over six input shapes. NUMBERS (kolt copy, client + server, 12 bundled files, an edit only the server loads; `build --watch` rounds 2–4, process CPU): 5.150 s median → 1.260 s (4.1×, measured at HIGHER load after); the client leg went from `Compiled` every round to `Fresh` every round.
- E106: MEASURED, no fix — the prime suspect (the deliberate per-analysis leak) is REFUTED: `adopt_analysis` reclaims it every window for 2,000 windows (outstanding balance constant; RSS flat). S5's re-parses are a per-request constant (5.95 → 6.21 ms). WHAT GROWS: one retained analysis per OPEN document — kolt's 18 `src/*.vl` open: RSS 5.4 MB → 864 MB (~48 MB/file; views.vl/client.vl/channel.vl +126 MB each); closing all 18: RSS 802 MB (62 MB back to the OS), heap in-use 738 → 320 MB, free-RETAINED 3 → 421 MB (glibc never trims). FIELD READING: the owner's live `vilan-lsp` (8 h, under the VS Code host) at VmRSS 4.13 GB, VmHWM 4.40 GB, VmPeak 6.64 GB. Instruments: `session_growth` in lsp `document.rs` (per-100-keystroke medians/mins of analyze CPU, the burst, RSS, the leak tally) + `open_documents` (mallinfo2 in-use/free beside RSS); pins `a_session_does_not_get_slower_or_leak_across_windows` (every run; growth < 1.5×, retention < 1.1× — on the window MINIMUM, load-robust; median reported too), two `#[ignore]`d 2,000-keystroke drivers under `VILAN_PERF_KOLT`; non-vacuous (an `adopt_analysis` that keeps the outgoing analysis reds it in ten keystrokes). The kolt lifecycle run stopped at 900 keystrokes (loadavg 50–110, 24% of a core) with RSS and outstanding flat for nine windows.
- M58: warm profile (kolt client.vl, `profiling`, callgrind, 3.635 G Ir): `post_analysis_passes` 51.07% (not the item's 57–63% on this subject), `analyze_cancellable` 30.24%, inside (1): `const_eval::evaluate` 22.77%, `context::thread_contexts` 12.90%, `async_infer::infer` 10.34%; then `call_graph::Collector::walk` 9.82%, `dispatch_refine::refined_edges` 9.49%, `Program::source_of` 7.22%, `Document::semantic_tokens` 7.11%. LANDED (an hour): rows 8 and 9 were ONE row — `semantic_tokens` was 98% `source_of` (a linear scan asked 426,198×/analysis); M27's `Program::source_lookup` (binary search, answer-identical by construction) hoisted into `semantic_tokens` and `inlay_hints`: 258.3 M → 25.5 M Ir; `source_of` program-wide 262.4 M → 7.5 M; whole warm re-analysis −234.1 M Ir (−6.44%); `post_analysis_passes` unchanged in absolute Ir (the consistency check). Pin `the_hoisted_source_lookup_answers_exactly_what_source_of_answers` (planted off-by-one reds it). The `.callgrind` files are in the lane scratch.
- Gates: cli watch_leg_reuse/watch_lifecycle/hmr/perf_baseline/corpus/release_scripts + core phase_timing 56; lsp+ide 689; WHOLE vilan-core 5,435; clippy/fmt 0; no rows, no .vl, no goldens.
- FINDS → candidate items: (F1) perf S: `vilan_ide::completion::CompletionIndex::build` asks `source_of` 14,580×/7.5 M Ir per warm analysis — the identical hoist (editor-33's file, so filed); (F2) perf M/L + design: the server retains one full analysis per OPEN document (~48 MB/file on kolt, 864 MB for 18) — bound it (only the focused document keeps its `Program`); (F3) perf S + RULING: glibc never trims — `malloc_trim(0)` on document close returns ~400 MB but needs `libc` as a real dependency of vilan-lsp (dev-only today); (F4) tooling S: `Vilan: Show Language Server Status` reports cardinalities and no memory — `mallinfo2`'s split is attributable and is what located the memory; (F5) doc XS: `LegArtifact.bundled`'s doc claims a skipped round re-copies bundled files — both skip paths deliberately do not (unreachable before B276); (F6) method: CPU on this box is load-dependent by 2.1× between loadavg 11.7 and 35.2 — the per-window MINIMUM is the load-robust statistic (E121's quiet-box rule re-confirmed).
- OPEN Qs (owner): E106's gate on the window minimum (rec keep); E106's family tooling (rec keep); F2 and F3 need a word before anyone builds them (rec: F2 as an item for the next order — the 4 GB field reading is the case; F3 = a dependency ruling).
- Brief corrections: the `current_source_hash` twin; the leak was not the growth; M58's largest day's-work item was the LSP's token table, not inside `post_analysis_passes`; the kolt scratch copy needed the two known edits (B275's `.map`, the duplicate `Result` impl) and the `[[build.hook]]` removed (scripts/ not copied).

## styles-33 — A89 (c64ac090 + 99e1b746), A90 (41b4122d), Vec2::length_squared (6e4e10b1) landed; B308 NOT BUILT (measured, three designs priced); A84 MEASURED (the note below), sixth to report (122 min, 223 tool uses); FULL workspace suite 6989 passed; merge after dx-33 (`inference/styling.rs`: 28 existing fns swept to `Some(..)` + new fns — take the lane's file whole if the fold balks, no other lane touched it; `style_chain_order.rs` fixture `ORDER_SENSITIVE_CSS_BLOCK`; `css.rs` unit test `a_nested_head_with_arguments_keeps_them_before_the_chain`; goldens regenerated: `vilan/test/{css-block,preflight,style,style-when,theme,vec2}.mjs`, `style.css` (the new `gated` exhibit only), `markdown_anchors.golden` +1 line)
- A89: `attribute(name, value: Option<str>, inner)` AND `within` took the Option; `fun not(self, inner)` beside `hover` — marks the inner's slots (two `!` forms in the condition grammar: PENDING `!` as a leading token = what `not` leaves; APPLIED `!token` = what `render_rule` turns into `:not(..)`; the split is what lets a negation nest inside an already-negated condition); the enclosing combinator emits its own selector negated. Verified: `.sX[data-selected]`, `.sX:not([disabled]):hover`, `.sX:not(:hover)`, `:not([data-theme="dark"]) .sX`, `[data-print] .sX`. Thirteen pins in `inference/styling.rs` (NOT `style_when.rs` — that is where `collected_assets` and every attribute/within pin live; brief deviation); corpus `style.vl` gained `gated`. The UNWRAPPED-`not` refusal is a RUNTIME failure at `class_list` (application is runtime-legal code; no const-time plateau exists without B308 — OPEN Q1). `pseudo` refuses a name opening with `!` (the fence); `child_relation`'s message gains the `attribute(..)` steer; `STYLE_CONDITION_METHODS` gains `("not", Pseudo)` (an honest fifth axis would cost an exhaustive-match audit — Q2). Cascade note pinned: a negated ancestor guard moves BANDS (`:` vs `[`) — specificity unchanged; ui-styling.md §0bis.6 wants a row at the sweep (Q3). FOLLOW-UP 99e1b746: the first spelling read the mark off the slot KEY → a `keys()` snapshot per application, caught by `copy_elision_census` (preflight/theme 1 → 2 copies) and REAL: 3.1 µs per `class_list()` on a 25-slot style (78% of the call), once per element per render → the mark is written on the CLASS too; re-measured 0.1 µs; census green without regeneration. 4 NEW rows. Docs: guide/styling.md (presence + negation), std/style.md (`Option` prose, `not` row, `### Negation: not`).
- A90: `check_custom_property_name` (must start `--`, not bare `--`, no space/quote/`:`), called by `Color::var` and `Length::var`; `style_var` DECLINES the check deliberately — it appends a whole DECLARATION and kolt `views.vl:193` writes `.style_var("width", ..)` over a plain property (correct code the check would break). 2 NEW rows; nothing newly refused (kolt's dashless `var`s are inside the commented-out draft).
- `Vec2::length_squared` = `self.dot(self)`; `length` = `length_squared().sqrt()`; pin + doc row; `vec2.mjs` regenerated (witness lines moved with `length`'s body).
- B308 NOT BUILT — the ruled mechanism is NOT IMPLEMENTABLE without new compiler surface: `emit` is const-only and every application path (`styled`/`bind_styled`, the element class path) is runtime code through `class_list`; "the plateau" is `const_eval.rs`'s `results.insert` in Rust, which knows nothing of `Style` (no std TYPE is known to the compiler anywhere — checked); `render_rule` must stay the single source. MEASURED (dead = a class in the sheet absent from the bundle): kolt `dist/client.css` 162 rules/9,144 B → 11 dead/562 B (6.1% of the sheet, 10.7% of class-rule bytes); corpus style.css 27.6% dead; theme.css 17.2%; the exhibit reproduces exactly (a 4-level nest: 5 rule sets, 4 dead). THREE DESIGNS: (1) a Rust plateau flush — const-eval learns `Style` by type and re-enters the interpreter — L, a semantics decision (no precedent for the compiler knowing a std type); (2) RETRACTION at consumption (`__retract_asset`: each combinator re-renders the inner's exact line and retracts the LAST matching entry — correct by evaluation order per const site; ~20-line interpreter arm + a const-only extern + a `retract_inner` helper at five call sites) — M; the item's "impossible with a write-only channel" premise was false, but making the channel non-write-only is a ruling; (3) prune in the CLI collector (drop css lines whose classes appear in no emitted JS) — M with a real correctness surface (three assemble sites; the dev channel assembles css BEFORE the JS is known). REC: (2), landed BEFORE OR WITH A84's build. The split fixture emits NO CSS (nothing to measure; brief correction).
- A84 MEASUREMENT (kolt's `button_style` reconstructed; the two `pseudo("hover:not(:active)")` refinements replaced by plain hover/focus-visible in BOTH forms — see F1): sheet — current (`when`) 28 rules/1,267 B, 3 dead (14%); variant (attr + var) 38/1,982 B, 13 dead (38%) — same 25 LIVE rules, the variant's 12% longer and FOUR TIMES the dead intermediates (two combinators deep). Class attribute: current 15–22 classes/127–187 B per cell; variant 25/213 B constant. THE DECIDING NUMBER — CPU per rendered button (100k `class_list()`s, getrusage, node startup subtracted, loadavg ~82, median of 5): `when` chain 26.8 µs; const style 6.7 µs (4.0×); const style with the class list hoisted to a `const` str 0.05 µs (~540×) — the attribute form MAKES the const class list possible, the computed form cannot (a Map clone per `when`, a family sweep per key, a join, per element per render). Custom properties on kolt's real sheet: 10 rule SHAPES differ only in the `--color-*` token (the themed event family minted at primary-a AND danger): 20 rules/1,474 B → one colour-agnostic set + `--button-color` per element saves ~737 B (8%), scaling with the number of button colours; impossible in the `when` form (colour baked into the hash). RECOMMENDATION: confirms the GO's direction — data attributes for STATE, custom properties for COLOUR, `when` kept for the local one-off — with two conditions: (1) B308 first or with it (A84 alone trades a 4× runtime win for 14% → 38% dead rules); (2) the guide says to hoist the class attribute to a `const` (else only the 4× is collected). Footnote: kolt's `when(!selected && !disabled, ..)` compound is the tell that the states are not independent; `[data-selected]` and `:not([disabled])` dissolve it.
- FINDS → candidate items: (F1) MISCOMPILE: `Style::pseudo` accepts a name containing `:` (the slot key's own delimiter) and an ENCLOSING combinator then emits a WEAKER selector — `attribute("data-open", Some("true"), pseudo("hover:not(:active)", S))` → `.sX[data-open="true"]:hover` (the `:not(:active)` GONE; the property field is garbage, so `without_covered` resolves against `not(`); pre-existing; LIVE IN KOLT `styles.vl:48–49` (both `hover:not(:active)` and `focus-visible:not(:active)`) — not biting only because kolt never wraps them; fix = fence `:` in `pseudo`'s name (BREAKING for kolt) which needs F2 first; (F2) A89's `not` does NOT reach "hovered AND not active" (two pseudo-classes in one slot; `hover(not(active(S)))` means `:not(:hover):active`) — a second pseudo per slot or `pseudo_not(name, inner)` appending `:not(:name)`; pairs with F1; (F3) MISCOMPILE: `Map::entries()` hands out the GENERIC tuple layout for a `Map<K, (A, B)>` — `let (key, slot) = entry; let (class, declaration) = slot;` → `class=class,decl declaration=undefined` (`entry.1.0`/`.1.1` wrong the same way); `keys()`/`values()`/`get()` correct; only `entries()` (map.vl:73 `self.table.values()`) — a tuple built inside a GENERIC fn from type-parameter-typed values is not flattened while the caller (knowing `V = (A, B)`) reads it flat; silent wrong values; no other std caller; (F4) a family-COVERED slot leaves a dead rule (`without_covered` drops the longhand from the map but its line was already emitted) — B308's designs 1/2/3 each fix it; (F5) `child_relation` is a public raw-selector hatch and kolt uses it (`views.vl:165` `child_relation("not([hidden])", "display", ..)` → `.sX:not([hidden])`, skipping `@layer` and `> *`); A89 spells it (`attribute("hidden", None, not(..))` emits the identical selector, verified) — fence the token to `>*`/`>*+*` once views.vl:165 migrates.
- OPEN Qs (owner): (1) the unwrapped-`not` refusal is runtime until a plateau exists (B308 design 1/2 would move it); (2) `not`'s axis (`Pseudo` vs a fifth variant); (3) §0bis.6's band row; (4) do std const-time `panic` refusals carry ledger rows? (no existing style.vl panic does; 6 NEW rows added per the mechanics; drop them if the ledger's scope is compiler diagnostics — rec: KEEP, they are user-facing refusals with texts to hold).
- Brief corrections: the css desugar is `css.rs`, not `elements.rs` (name-blind as §5.3 promises — verified by a pin); the split fixture has no CSS; the completion tables carry names + axes, no arity; the combinators have NO free-function forms (`style/prelude.vl` aliases only `Length`/`Color` constructors and `s()`) — the real spellings are `style().attribute("disabled", None, style().not(style().hover(S)))` and the `css` block (both pinned); kolt census: 1 live `within` (styles.vl:13 → `Some("false")`), 2 `attribute` in the commented draft (→ `None`), 1 live raw `child_relation` (F5).
- PROCESS DEVIATION (disclosed by the lane): it stopped its own `cargo nextest run --workspace` with `pkill -f` (patterns `cargo nextest run --workspace`, `cargo-nextest`) at ~17:0x — a kill by PATTERN, forbidden; another lane's workspace run at that moment could have been hit (slots-33's merge gates ran in the integration worktree around then — they passed; solver-33/rpc-33 were live — watch their reports for an unexplained abort). Rule stands; recorded.
- MERGED 31592245 (slots-33, third): guide/ui.md auto-merged; the two named pins folded; parity 320/320; no ledger row; gates green (cli ui_rows/router/ssr_differential/ssr_fullstack/split/corpus/grammar_ebnf/grammar_sync/examples/diagnostics_ledger/ci_ignored_pins 115; core docs/markdown_golden/parse_expr_regression/inference/parse_differential/parser_recovery; std full-scan; lsp; release_scripts; split (the lane's regenerated golden merged clean); fmt); pushed. Next: rpc-smalls-33 (started), then dx-33, then styles-33.
- MERGING rpc-smalls-33 (fourth): merge_lane STOPPED at the ledger step (exit 2) — the lane wrote LITERAL ids 415–417 instead of `NEW`, and 415 collided with hygiene-33's row (renumber_ledger reads a numeric id HEAD also carries as an EDIT; base had neither → "both changed = stop"). Resolved by hand: the lane's `415` row → `NEW` inside the conflict hunk, then `renumber_ledger.py` (rows → 416–418 in file order) + `apply_row_mapping.py` + `fold_tests_by_name.py` on `inference/bounds.rs` (slots-33's a85 pins vs the lane's b307 pins, append-append); the CHANGELOG union had already resolved on disk. Gates running, push after. RULE (mechanics, next order): a lane writes `NEW` for every row it adds — never a number; a literal id that collides costs a hand resolution.
- MERGED 44ee8529 (rpc-smalls-33, fourth): ledger rows 416 (B295 `__` parameter), 417 (B287), 418 (N70 std without rpc.vl); bounds.rs folded (6 new b307 fns); gates green (core inference/docs 3887; cli service_layer/rpc_http/examples/corpus/diagnostics_ledger/ci_ignored_pins/hygiene 95; std full-scan; lsp 702; release_scripts + split 34; fmt); pushed. Next: dx-33 (started), then styles-33.
- MERGED 34933b5c (dx-33, fifth): no conflicts of note; parity 329/329; no ledger row; gates green (cli watch_leg_reuse/watch_lifecycle/hmr/perf_baseline/corpus/diagnostics_ledger 49; core phase_timing/inference/docs; lsp; ide; release_scripts; split; fmt); pushed. Next: styles-33 (started; 29 named edits allowed; expect hand folds on `css.rs`'s unit test, `markdown_anchors.golden` (both slots-33 and styles-33 regenerated it) and possibly the corpus goldens).

## rpc-33 — both landed (d868feb8 A92 the sync unleased handle stub; 74018c52 A79 the keyed return), seventh to report (143 min, 324 tool uses), merge LAST (after styles-33 and solver-33): 11 corpus `.mjs` goldens + `split/golden/app.js` + `markdown_anchors.golden` MOVED (the `SignalCell.id` stamp — `fresh_id()` per cell — reaches every program; regenerate over the merged tree); EXISTING fns edited (`--allow-edit`): `service_layer::{a_handle_returning_method_hands_the_client_a_mirror_and_hashes_as_the_mapped_type, a_hundred_handles_cost_ten_forwards_and_a_released_one_is_revoked_and_re_minted, a_handle_bridges_element_type_is_checked_against_the_annotation_at_the_call}`, `reactive_channels::{a_minted_mirror_remounting_inside_one_macrotask_sends_nothing, an_owner_and_a_reconnect_each_end_a_minted_mirror_the_lease_cannot}`, `rpc_http::a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method`, `inference/markdown::a25_a_second_subscribe_frame_opens_no_second_forward` (+ two `a25_` Status matches), `inference/traits::a78_a_handle_service_on_a_stamped_local_rpc_protocol_still_round_trips`, `examples::every_example_builds`; rpc.vl's generator region overlaps rpc-smalls-33's B295 qualification (hand-resolve: rpc-33's structure with `rpc::`-qualified calls; `reply_source_keyed` joins the import list)
- A92: the stub is SYNC and makes NO CALL — `fun get_message(self, id): RemoteSource<MessageBody>` mints the mirror unleased (channel −1, `released = true`); the first 0→1 lease runs the shipped `acquire → remint → rebind` path (the first mint IS the re-mint path); `Origin.reissue` is three-valued (`Result<Option<i32>, RpcError>`); `Status` gains `Absent` + `Failed(RpcError)`, `status()` derived over `cache` + a new `mint` cell (owner-optional); `close_now`/`release_unleased` guard on `minted()` (no `Unsubscribe(-1)`). Server: `expose_dynamic` dedups by `SignalCell.id`; the per-channel `Subscribe` count lives on `LiveForward.holds` (per DEMAND — `open_forward`/`close_forward`/`release_hold`). `SignalCell` gained `id: i32` (stamped at construction — the brief did not authorize the golden churn; flagged). `std/memo.vl`: `Memo<K: Hashable, V>` with `new/get_or/get/forget/clear/len` (home `std::memo` — there is no `std::collections` MODULE, only the docs page). NUMBERS (the demand pin, was → is): 100 handles minted: sources 101 → 1, calls 100 → 0; 10 leased: sources 11, calls 10; all released: sources 1; re-acquired: sources 2, calls 11. Dedup: two handles on one row = 2 calls, ONE capability, ONE forward; the first mirror's release leaves `sources=2 live=1` (no revoke under the second); the last release revokes; `find("nobody")` → `Absent`, `get()` `None`, table unmoved. Hashes: plain control 78bdada7 UNMOVED, plain handle da79e00b UNMOVED, the `Option` form MOVED 5f491b0a → 384b5434 — the surface writes it `->RemoteSource<T>?;` (DEVIATION from the brief's collapse: the two forms answer one client type but DECODE differently, `i32` vs `Option<i32>`; collapsing them would let a client read the other's `null` as a channel id — OPEN Q1). Pins: `an_unleased_mirrors_first_lease_is_its_mint_and_a_failed_one_retries` (unleased costs nothing · Failed · Absent · retry on the next 0→1 · the in-flight JOIN: two leases, one call); `a_memo_makes_a_value_once_per_key_and_forgets_one_on_request`.
- A79: `handle_element` reads `KeyedCell<K, T>`'s second argument, a new `handle_key` macro the first; kind `"keyed"` renders `KeyedSource<K, T>` in stub and surface, routes through `reply_source_keyed` over `expose_keyed_cell_dynamic` (identity `cell.elements.id`); `KeyedSource` gained `origin/released/leased/mint` + `remint/known/demand/minted/surrendered`; `acquire` AND `acquire_key` mint when released; surrender marked when total demand hits zero; `Option<KeyedCell<..>>` deliberately NOT a handle. Numbers: minted 1/0/0 Waiting → leased 2/1/1 Ready → `frames:2` (a `Reset` seed + ONE patch per insert) → released 1/0/1; a per-key lease mints and its release revokes. HAZARD found and closed: a `KeyedCell`'s `elements` IS a `SignalCell`, so a service answering both would dedup a `Patch` channel and an `Update` channel onto one id (the second mirror never seeds) → `dynamic_channel_for(identity, keyed)` discriminates on frame SHAPE (`Capability::keyed()`); pinned `shapes:sources=3 live=2`, red without it.
- Paper corrections (integrator, at the sweep): §9.2 mapping table (`SignalCell<T>` ⇒ `RemoteSource<T>` sync, `Option<SignalCell<T>>` ⇒ `RemoteSource<T>` with the `?` surface marker, `KeyedCell<K, T>` ⇒ `KeyedSource<K, T>`; "any `S: Source<T>`" stays unimplementable); the laziness paragraph (ten handles → ten CALLS only when leased; the first mint runs the body at the first lease, not the stub call); `Origin.reissue`'s type; §9.6 the as-built block; remote-sources §2d (four `Status` arms; "an unwatched handle mirror has not even asked"); §3 AMENDED (hold-counting: two `Subscribe`s need two `Unsubscribe`s — "the server does not rely on that" now holds for what it SENDS, not for when it STOPS).
- Gates: cli 142 (reactive_channels/service_layer/reactive_lifetimes/rpc_http/corpus/release_scripts/examples/split/diagnostics_ledger/ci_ignored_pins); WHOLE vilan-core 5436; lsp 679; clippy/fmt 0; `vilan fmt` clean over four .vl; no ledger rows; the markdown golden regenerated per commit.
- FINDS → candidate items: (F1) B: a keyed handle's KEY is not Wire-checked in the method's own vocabulary (`KeyedCell<NotWire, T>` passes the `[rpc]` return check and fails inside `reply_source_keyed`'s bound — generated code) — rpc-smalls-33's region (analyzer.rs ~14930), not built; (F2) perf/design: `SignalCell.id` is paid by every program (one `fresh_id()` per cell, 12 goldens moved) — a lazily-stamped `Shared::identity` intrinsic (`o.__id ??= next++`) gives reference identity at zero cost until asked and zero golden churn; (F3) design: HOLD-COUNTING vs remote-sources §3 — a raw client that asks twice and releases once holds its channel open until disconnect; the alternative restoring §3 exactly is client-side per-channel demand coordination in `ReactiveClient`; (F4) behaviour: the seed is one round trip LATER — `examples/rpc` lost its `note = (empty)` line (the mint and a concurrent write in flight together); no value lost, fewer intermediates observed; (F5) diagnostics: `Option<KeyedCell<K, T>>` falls to the generic non-Wire refusal naming the wrapper — a curated row ("a keyed handle has no `Option` form in v1"); (F6) doc/design: a handle method's BODY no longer runs at the stub call — a service relying on a call-time side effect changes behaviour silently (documented in the guide; no refusal).
- OPEN Qs (owner): (1) the `?` surface marker for the `Option` form (vs keeping `Option<RemoteSource<T>>` in the surface) — rec: the marker (the collision argument is right); (2) is `Absent` a retry or a final answer? (built: retry at the next 0→1, like `Failed` — rec keep); (3) `Memo`'s home `std::memo` (rec keep; the docs page is `std/collections.md`); (4) F3's client-side demand coordination — worth the plumbing?; (5) `reply_source_keyed` added to the generated import list — rpc-smalls-33's line; the integrator resolves.
- Brief corrections: step 3's Option collapse is a hash collision; `SignalCell` had no `id`; the keyed dedup needs a frame-SHAPE discriminator; `Origin.reissue`'s type changed (every `Origin` literal in tests moved); `cargo nextest run -p A -p B --test X` applies `--test X` to EVERY package (`-p vilan-core`/`-p vilan-lsp` run separately or silently run nothing); all rpc.vl anchors were stale as expected.
- Kolt migration sketch (the owner's word): `scratchpad/rpc-33/kolt-migration.md` — `lib/remote_signal.vl` and `lib/memo.vl` delete; the three memos become `Memo<i53, RemoteSource<..>>`; `find`/`messages` become `memo.get_or(id, || client.get_x(id)).or(fallback)`; the B288 FIXME annotations go; the `is_err → forget` retry pattern goes (a failed mint retries itself); check `get_channels()`'s `get_client().map(..).flatten()` — `.or()` needs an ambient owner and the closure runs in the caller's.
- MERGED 4a70f301 (styles-33, sixth): the 29 named edits folded; ledger rows 419–424 (A89 ×4, A90 ×2); parity 332/332; the markdown/corpus goldens merged clean; gates green (cli style_when/style_chain_order/hmr_css_matrix/ssr_differential/split/corpus/diagnostics_ledger/copy_elision_census/examples 56; core inference/docs/markdown_golden; std full-scan; lsp; release_scripts; split; fmt); pushed. Next: rpc-33 (started; rpc.vl resolved by `scratchpad/order33/resolve_rpc_vl.py` — the lane's structure with B295's qualification re-applied, next's module-import line kept), then solver-33 when it reports.
- MERGING ca5e0777 (rpc-33, seventh): merge_fold STOPped on rpc.vl (non-test) after folding service_layer.rs (three edited fns taken from the lane, `KEYED_HANDLE` const carried, one new fn); rpc.vl resolved by `scratchpad/order33/resolve_rpc_vl.py` (one import hunk kept from next — B295's module imports; two generator hunks took the lane's A92/A79 structure with `rpc::`/`reactive::` re-applied to 17 free names); no ledger row; parity 334/334; the split golden UNCHANGED over the merged tree and the corpus gate GREEN unregenerated (the lanes' goldens merged consistently); the markdown golden consistent. Gates running (cli reactive_channels/service_layer/reactive_lifetimes/rpc_http/examples/diagnostics_ledger/ci_ignored_pins/release_scripts/split; core inference/docs/markdown_golden; std full-scan; lsp; fmt; `vilan fmt`), push after.
- rpc-33 merge, round 2: two service_layer pins RED after the merge ("cannot match an enum variant against type RemoteSource<..>") — `fold_tests_by_name.py` took the lane's three edited FNs but kept next's EXISTING program CONSTS (`HANDLE_SERVICE`, `HANDLE_DEMAND`), so the programs still matched the old `Result` shape (the tool carries NEW top-level consts only). `git merge-file` was no better (it aligned the common opening lines of two different program consts as context and cut through raw strings). Resolved by an ITEM-LEVEL three-way merge (next's items; the lane's version of every item it changed where next kept the base's; the lane's new items appended; raw-string aware — vilan program text inside `r#"…"#` shares `struct`/`impl` with Rust): replaced HANDLE_SERVICE, HANDLE_DEMAND + the three fns; appended KEYED_HANDLE + the keyed pin; no both-changed item; service_layer 42/42. Script kept as `scripts/integration/merge_items_by_name.py`. RULE: a lane editing a program CONST beside its pin needs the item-level merge, not `--allow-edit` (fns only). The fix is STAGED; the commit waits for 1Password (signing + SSH both down since ~the styles-33 push).

## solver-33 (TOP) — all NINE built and gate-green (c8a639aa B300; ee62236c A86; d5cd510e B304; 3d7a19e7 B296; 8fae0eca B305; 9d9c8922 B306; B299 + B297 + B286 STAGED, UNCOMMITTED — 1Password), last to report (200 min, 369 tool uses); whole vilan-core 5487 + cli+lsp 1481, clippy/fmt/`vilan fmt` clean; merge after rpc-33 (`--allow-edit a52_an_rpc_mirror_is_not_a_list_source_and_binds_through_or` in `inference/markdown.rs`; the two un-ignored A86 pins in `traits.rs`; corpus golden `reactive-flatten.mjs` regenerated by BOTH solver-33 and rpc-33 → regenerate over the merged tree)
- B300: the receiver/subject reconciliation bound only what the SUBJECT writes, so a binder written inside a BOUND (`impl type S: Read<type T>`) stayed a hole → new `bind_subject_bound_binders` (the analyzer twin of `impl_select::bind_bound_binders`, B165) grounds them from the receiver's own impl of the bound trait as a WORKLIST — which closes the NESTED face too (binding `I` makes `I`'s bounds answerable, binding `U`); both A86 pins un-ignored + 5 `b300_*`.
- A86: `flatten` retired from the cell to two blankets (`impl type S: Source<type I: Source<type U>>` and the `Option` form), 7 pins, docs. Needed a CHECKER change (flagged): the duplicate-inherent-member rule compared subjects with `compare_type` (a bound's TRAIT, not its ARGUMENTS), so the two disjoint blankets read as one name twice → `subjects_collide` requires two BARE binders to be bound the same way (`same_generic_bounds`); constructor-headed subjects keep the old rule (tier 1 takes the first inherent candidate unranked). Corpus golden `reactive-flatten.mjs` regenerated (gensym shift only; runtime output unchanged); the split golden did NOT move (brief correction). The `or` census not done (rpc lanes').
- B304: THE ITEM'S FRAMING WAS WRONG — the discriminator is not "free fn vs inherent method" but WHETHER THE RECEIVER'S TYPE WAS KNOWN ON THE FIRST ATTEMPT: a late-landing receiver (a chain) makes the call DEFER and the retry binds `E`; a receiver that is a parameter or `self` resolves at once and the fill freezes the parameter at the callee's abstract `E` (a free fn with a parameter receiver fails identically — probed). Fix: both fills (`infer_closure_args_against_params`, `bind_callee_own_generics`) DECLINE while the expected closure's PARAMETER positions carry an unbound own generic (the RETURN position untouched — `map<U>`); `link_to` drops `|event: Event|`; 7 `b304_*` pins; the two red faces green. EXISTING fn edited: `markdown::a52_…` (its expected substring is now the INSTANTIATED `Source<List<Todo>>` — `bind_each`'s `T` binds from the key closure before the bound is checked; strictly better).
- B296: SHRUNK PAST THE ITEM — neither `[service]` nor `KeyedCell` is load-bearing: a plain struct with two such fields, or ONE CALL with two such arguments (`take(Bag::new([]), Bag::new([]))`). Root: both sibling loops (`resolve_call_subject`'s argument loop, the struct-literal field loop) recorded EVERY binding a reconcile handed back into the context the next sibling is checked under, so reconciling abstract `Bag<K,T>` against `Bag<str,Task>` wrote `Bag`'s own impl binders into `take`'s context (two instantiations → "Expected `Bag<i32, Row>`…"; one → silent, monomorphizing into the trait's bodiless requirement). Both loops filter by the callee's bindable generics / the literal's own parameters. 8 pins.
- B305: the `Expr::List` arm mints the same slot the empty path does (one shared `list_literal_element_slot`); `[event][0]` over an unfilled parameter now compiles; 3 pins; infer differential + 112 corpus goldens unmoved.
- B306: the brief's FIRST option is NOT viable — the candidate-diagnostic backstop was BUILT and MEASURED against the estate: 18 correct programs refused (`Doubler`'s supertrait defaults among them: "Expected Add, but got Doubler"), because this pass reconciles PARAMETER-first and a bare-trait parameter meets a concrete argument on `reconcile_type(Trait, Concrete)` — the one arm that refuses what every value-first position accepts (B4, method-resolution.md §10) — so a drop there is no evidence. The silence is safe because a later check always fires (`MethodArgCheck`, the free path's positional loop, the residual sweep); written at the site; 6 pins asserting EXACTLY ONE diagnostic with no later consumer.
- B299 (R5 DESUGAR; staged): `impl Trait<..> { .. }` means `impl type S: Trait<..>` — the implicit binder's constraint id IS the subject's type id, so `self` dispatches through the bound, the head's arguments bind from the receiver's impl (B300's step), the body re-dispatches via `GenericDispatch::OnConstraint`; the impl still REGISTERS under the trait subject (`Iterator::from_fn` keeps its name — pinned); a steer for a body naming the binder = ONE NEW ledger row; spec types.md §5.4; 6 pins incl. the owner's probe head over std's `Source`. OPEN Q: the FULL desugar (subject → `Generic(S)`, moving registration/conformance/`impl_binder_generics`) is a follow-up if the ruling meant the deeper form.
- B297 (staged): `Node::Impl` walked the subject twice; `impl_subject_args` stores the subject's type id alone; 4 pins (`impl Holder<Nope>` doubled identically — not trait-specific).
- B286 (staged): `abstract_trait_arguments` reads a bound's arguments off a PARAMETER's declared bounds (walking supertraits); CPU 802.4 vs 801.4 ms per compile over 30 builds — inside the spread; 4 pins.
- BLOCKED: `commit.gpgsign=true` over the 1Password ssh agent, which stopped mid-lane; the lane did NOT touch git config; the index holds the B299 snapshot; `scratchpad/solver-33/finish_commits.sh` lands all three in order (idempotent); the lane left two background retry watchers — the integrator runs the script AFTER the unlock and checks the log first (the watchers may have landed them).
- FINDS → candidate items: (F1) B: the duplicate-inherent-member rule now reads a bound's arguments only for two BARE binders — a blanket pair with OVERLAPPING but not identical bounds (`Source<type I>` vs `Source<Option<type I>>` on a hypothetical `Option: Source`) would both be admitted and tier 1 takes the first candidate UNRANKED (not reachable today; "no silent pick" is weaker than it was); (F2) B (B4/§10 territory): `reconcile_type(Trait, Concrete)` refuses what every value-first position accepts — 18 programs when a pass reports on it; any "report where the binder gave up" idea is unbuildable until B4 is settled; (F3) B: B304's real discriminator is receiver-resolution TIMING — a sweep of the `is_unknown_closure_parameter` doors for other schedule-dependent one-shot slots; (F4) kolt: 8 `|..: Event|` annotations (channel.vl:43/:56, views.vl:542/:754, lib/input_system.vl:39, lib/rotary.vl:22, interact.vl:76/:88) — B304 removes the need for the workaround ones (the owner's sweep).
- OPEN Qs (owner): B299's reach (body-level desugar landed; the full subject desugar is a follow-up — rec: file it, not now); B306's instrument (a counter for the silent drop vs prose + pins — rec: pins are enough).
- Brief corrections: B296's shape (sibling arguments/fields, not KeyedCell); B304's location; B306's first option; A86 moved the corpus golden, not the split one.
- MERGED ca5e0777 + fix 288ce830 (rpc-33, seventh): the service_layer fix committed once the agent returned; gates green (cli reactive_channels/service_layer/reactive_lifetimes/rpc_http/examples/diagnostics_ledger/ci_ignored_pins/release_scripts/split; core inference/docs/markdown_golden 3908; std full-scan; lsp 704; fmt; `vilan fmt`); pushed — origin/next = 288ce830 carries seven lanes. solver-33 pushed at 3c25fcc4 (nine commits; its three staged ones landed through its own watcher when the agent came back); merge started (allow-edit: the a52 markdown pin + the two un-ignored A86 pins; the preview conflicts only in bounds/generics/std_surface test files). NOTE: `ssh -T git@github.com` from the tool shell still says "Permission denied" while git signs and pushes fine — git reaches the 1Password agent by its own path; the bare-ssh watcher was the wrong probe.
- MERGED e4d192e3 (solver-33, eighth and last): bounds/generics/std_surface folded by name (13 new generics fns among them); the a52 markdown pin and the two un-ignored A86 pins taken from the lane; ledger row 425 (B299's steer; next 426); parity 343/343; the corpus gate GREEN unregenerated (reactive-flatten.mjs merged consistently); gates green (core inference/infer_differential/docs/markdown_golden 4094; cli corpus/service_layer/examples/reactive_lifetimes/reactive_channels/diagnostics_ledger/ci_ignored_pins 108; std full-scan; lsp 704; release_scripts; split; fmt); pushed. CANDIDATE SEAL TIP e4d192e3 — seal.sh started; CI watcher armed.
