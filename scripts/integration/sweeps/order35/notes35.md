# Order 35 — running record (integrator)

Opened 2026-09-13 off 36fb64ed (Order 34's sealed tip, the same day). Nine lanes: style-35 (G23 → B308 → A95 S1), visibility-a-35 (B318 S1+S2, B320, B321), visibility-b-35 (S3), slots-35 (A85+A91), solver-35, lang-35, editor-35, dx-35, hygiene-35 (droppable, first). Briefs: briefs35.md. Ledger next id 434. Rulings: ALL as recommended (owner) — R1–R8 stamped on B318/A95/G23/B308/A85/A91/B320–B332/D7/E170–E174/M67–M69/N77/N79/N80/L21.
- PROCESS (GO): no items to file or close at GO (every item exists); the chronicle OPENING entry appended and grep-verified before the commit; worktrees created off origin/next = 36fb64ed (verified); lanes never push; merges run DETACHED (merge_fold.sh under setsid nohup + an until-grep waiter) — a backgrounded tool call with a timeout SIGTERMs its gates.

## editor-35 — all five landed (E170 bb12b4bc, E171 d4e82a8a, E172 474971b5, E173 b091f593, E174 675ec9fd), first to report (57 min, 172 tool uses); merge now (regions disjoint from hygiene/lang/dx; allow-edit `e161_the_books_tag_rule_ignores_a_generic_head_too`)
- E170: one rule in E164's head region (there is NO `#element-head` key — the head is `#elements`' second pattern: brief correction) — `(?<![A-Za-z0-9_.:-])([a-z][a-zA-Z0-9_]*)(?=\()` → attribute-name; two scope pins (red-proved); blast radius 7 tokens / 38 chars over 257 .vl + 456 fences, all head-item names; the builder spelling does not move. FIND 5: only `vilan/test/element-syntax.vl` writes attributed markup in tracked .vl — the markup form is NOT the common one in the estate. FIND 2: a PARENLESS head item (`disabled`, `hidden.show(flag)`) still has no attribute vocabulary (one more rule, must decline the tag name and a hole).
- E171: the theme regex fixed; pin with `regex_match_positions` (global run); measured: the fix gains 10 matches in 6 files, all inside strings/comments which precede `ELEMENT_TAG` — nothing rendered moves.
- E172: `StyleSurface::extend` over the current file's `impl Style` bodies (`StyleMethodBody` carries its own source; std wins a name clash); kolt's `button_style` converts its `flex_row` prefix and splits at `.script_label()` (declared in theme.vl:369 — the PACKAGE boundary; FIND 1: widening needs the analyzed impl table + each file's text — its own item); two fixtures re-pointed at kolt's REAL `select_off` (the old `self.raw` shape converts now). Brief correction: "self-less-chain" → SELF-chain bodies.
- E173 (R8): `unused_import_spans() -> Vec<(Span, String)>`; `publish::diagnostic_groups` per-span message; the rewrite learned by RECORDING E168's `keep_module` through `organize_import_runs` (no formatter.rs edit) — INTEGRATOR NOTE: `rewritten_import_statements` calls `organize_import_runs` with a recording `keep_module`; visibility-b-35 re-points that predicate at selectors — same contract, different files. No ledger row (LSP paint is unrowed). Four pins incl. the wire one (HINT + Unnecessary unchanged).
- E174: `vilan.logSessionSummary` executeCommand (declared capability; undeclared command named at WARNING); `Backend::session_summary()` shared by the 500-request tick; the EXISTING palette entry `Show Language Server Status` prints the client tally then sends the command (rec built: extend, not a second command); `book_sync` gate `the_servers_declared_commands_are_the_ones_the_extension_sends` (the title gate needed nothing — brief correction); editor.md paragraph.
- Gates by the lane: lsp 749, cli grammar_sync 23 (CI=true, node_modules), style_chain_order 9, book_mirrors 2, vscode_extension 9, release_scripts 22, diagnostics_ledger 17, clippy, fmt, `npm run typecheck`. FIND 4: `keystroke::gate::the_entity_table_costs_the_edited_buffer_not_the_program` flaked once under load (32.2 s), green standalone and twice after — a load-aware bound or a flake item.
- Fold: grammar_sync.rs EDITED `e161_the_books_tag_rule_ignores_a_generic_head_too`; document.rs EDITED `css_spelling_conversion`, `StyleSurface::build`, `style_link_declarations`, `unused_import_spans` (+ tests `css_conversion_of`, two `refactor_*` fixtures, `faded`); publish.rs `diagnostic_groups`; main.rs `trace_request`, `server_capabilities`; book_sync.rs `editor_page_settings_are_the_extensions`; extension.ts `showServerStatus`.

## hygiene-35 — all four landed (N77 ca3613f0, N79 74f63511+b757c10f, N80 4f39df7f, L21 294d157f+71e947a0+41a3e137), second to report (74 min, 255 tool uses); merge after editor-35
- PUSH BLOCKED at first: the lane's seven commits carried author `Claude <noreply@anthropic.com>` (the worktree config is Reed's — the lane must have passed `--author`/env); the pre-push hook refuses a non-pseudonymous identity. Fixed by `git rebase 36fb64ed --exec 'git commit --amend --no-edit --reset-author'` in the finished worktree (shas MOVED — the table below is pre-rewrite); dx-35's two commits carry the same author and get the same fix at its report. Process rule for future briefs: commit as the worktree's configured identity, never `--author`.
- N77: the manifest comment → 664 declarations / 63 files @e4d192e3 (the recipe recovered exactly: top-level fun/external fun/struct/trait/enum/let; 665 at 36fb64ed) + the command; prelude.md §10.1 moved (proposals). FIND 1: §10.1's OTHER figure ("169 import statements, 46 would vanish") is stale too (193 today) — not re-derivable without redoing the census; item.
- N79 (R7): `derive_impl_source` + 729 lines deleted (all six derive names across five std modules — wider than R7's `json.vl` wording); the refusal names the missing module and the by-hand alternative (one row NEW); `RustFallback` → `SynthesizedMembers` (backed enums only); row 163 RE-KEYED (TSV + the proposals ledger paper); pin in service_layer.rs beside N70's (real std minus json.vl through `VILAN_STD`). FIND 2: a macro world registers no macros, so a derive inside one of the eleven force-loaded std modules would be refused with the load-ordering text — reach EMPTY today; documented at `IN_MACRO_WORLD`, no guard added. FIND 3: `RUST_DERIVES`' doc claimed a reach nothing exercised (fixed).
- N80: one `container_site_visited(site: ContainerSite)` (`File`/`Entity` arms), the census inside the predicate; differential pins 13/13. Brief correction: both predicates live in analyzer.rs, not the harness files. FIND 4: `take_reuse_record`'s inline `binary_search` is a third spelling asking a different question — left.
- L21: `materialize_into` prunes after a NEW hash lands (`STALE_AFTER`, `cache_entries`, `prune(root, age, dry_run)`; `prune_stale` never removes the current hash); `vilan cache prune [--all] [--dry-run]`; docs appendix/cli.md (+ releases.md §3 in proposals); six pins on temp roots; the owner's real cache untouched (4 entries >7 days remain). FIND 5: the vilan-cli test suite is the main FILLER of the real std cache (temp projects outside the repo have no `VILAN_STD`) — and now its pruner: the first post-merge suite run on a box deletes its >7-day std trees. Brief corrections: no CLI test enumerates subcommands; releases.md is the proposals paper.
- Gates by the lane: core 5649/5650 → the markdown anchor golden regenerated (the new `## vilan cache prune` heading; 534 anchors / 57 pages) then 2/2; differential 13, docs 9, embedded-std 13, cli release_scripts+diagnostics_ledger 39, upgrade 8, lsp 737, clippy --workspace, fmt.
- Ledger paper (proposals `diagnostics-ledger.md`, the audit record — rows end at 406, it is not a TSV mirror): the lane re-keyed row 163; the integrator mirrored Order 34's row 57 (RETIRED) and the 118/219/221 heads.
- MERGED c525d303 (editor-35, first merge; pushed): clean, no fold, no ledger rows; parity 381/381; gates green (lsp 749 in 604 s under load, cli grammar_sync/style_chain_order/book_mirrors/vscode_extension/diagnostics_ledger 60, release_scripts 22, split 12, fmt). Next: hygiene-35.

## lang-35 — all three landed (B331 b7ea1e41, B332 2bbea34e, D7 c4ed1169), third to report (114 min, 242 tool uses); merge after hygiene-35 (expect a textual conflict in `resolve_import` with visibility-a-35 — B332 edits the segment loop + miss arm, visibility-a the bit/reach: disjoint concerns, same fn; also `analyze_inner`'s module-registration block for B331)
- B331 (R3): `resolve_module_file` stops answering `a::lib` with `a/lib.vl` (root cause: one file analysed twice under two module names); the miss carries the curated steer to `pkg::a` (row NEW) via a new `modules_bodied_by_a_lib_file` field; a root-level `lib.vl` and a real `a/lib/` directory unaffected; A67's hand-skip of `lib` REMOVED and it was not inert (a declaration `lib` beside `a/lib/lib.vl` is now the ordinary A67 ambiguity — pinned); eight pins (three red-proved); names.md §4.1.
- B332 (R2): CENSUS ZERO — 287 .vl files (std 63, macro_std 3, examples 29, benchmarks 7, corpus 134, templates 8, cli test projects 2, website 11, playground 6, kolt 24) + 457 doc fences + the suites: no resolving path reaches a module member through a type-named prefix (every `Type::` import is `Option::{..}`/`Result::{..}`). Built: `ImportTypeSegment { subject_id, scope_id, name, brought_a_scope }` — an enum hands its variants scope, a struct hands nothing, `member_or_submodule` not asked; one rule. The breaking miss is CURATED (row NEW: "`px` is not a member of the type `Length`: it is an item of `pkg::style` … Write `pkg::style::px`") — the lane's judgement beyond the ruling (rec keep: it turns the worst half of the break into a fix-it; deletable in one arm); six pins (the shadowing pair asserts the RESULT TYPE); names.md §4.3/§4.6; CHANGELOG `breaking` with the census. FIND 2: import-path completion already agreed with B332 (never offered module members through a type prefix) and with B331 (`submodules_in_directory` always excluded `lib`).
- D7: `SignalCell::update`'s doc sentence; std/reactive.md bullet + a COMPILED fence (both spellings verified by running: write-back → 2, none → 0, take/replace works); A80's steer gains its second half; pin beside A80's three; no row (Rule statements keyless).
- Gates by the lane: core 5665 whole, module_resolution, inference, docs, std full-scan, cli module_paths/corpus/examples/release_scripts/diagnostics_ledger/hygiene/book_mirrors/grammar_ebnf, lsp 737, ide+wasm 46, clippy --workspace, fmt; final estate re-sweep with the shipped binary: neither refusal fires anywhere.
- FINDS → candidate items: F1 A67's message says "holds a `{name}.vl`" where a DIRECTORY-bodied child holds `{name}/` (pre-existing; B331's pin exhibits it); F3 A67's rule does not reach a pure-NAMESPACE child (`a.vl` declaring `c` beside `a/c/x.vl` with no lib — silent winner, verified pre-change); F4 `import pkg::a::lib::greet` also emits the standard cascade (steer names the right path — left).
- Nit left: the two refusal arms in `resolve_import` duplicate a five-line `spelled` builder (fold at the next touch).
- Brief corrections: B331 needed `analyze_inner`'s load loop (outside the listed loader fns; no other lane's); the website is `~/code/vilan-lang/vilan-website` + `vilan-playground` beside it (both censused).

## dx-35 — all three landed (M67 bdb30227, M68 b98e9ffa, M69 e1f93aad — pre-rewrite shas; the three commits carried author `Claude` and were rewritten with `--reset-author` before the push), fourth to report (119 min, 179 tool uses); merge after lang-35
- M67 (R6): MEASURED FIRST with a new ignored harness `base_cache_budget_walk_across_a_sibling_checkout` (19 kolt files open; a scan walk and a working-set walk; release; loadavg 14–30): unbounded 12 worlds / 266 MiB / RSS floor 374 MiB; 256 MiB → 11 worlds; 192 MiB → 7 worlds / 160 MiB / RSS held 443 MiB / FLOOR 305 MiB (M63's 300 MB target MET at the floor); 128 MiB → 5 worlds / floor 256 MiB. Miss cost PAIRED in one warm process: 21–295 ms (drag.vl 42 → 63 ms, views.vl 473 → 768 ms) — NOT M36's 3.5–5 s (that includes parsing std): brief correction. Built: `BASE_CACHE_RESIDENT_BUDGET` 512 → 192 MiB (the LRU budget ALREADY EXISTED — M24/M50; this lane set the default, added the live-key exemption and the bound pin: brief correction); `set_base_cache_live_entries`/`live_entries` (the LSP declares its retained set from `enforce_program_retention` and `did_close`); `evict_to_budget` never takes a live key; `CHECKED_CACHE_DEFAULT_BUDGET` decoupled (it was `/8` of the world budget — would have silently cut M46's 64 → 24 MiB). Pin `a_live_entrys_world_is_never_the_budgets_victim` (red-proved).
- M68: `mallinfo2` costs 266–990 ns; the arena walk 3.4–32.6 ms CPU per landing (the item's ~100 ms was WALL — correction) returning 14–135 MiB RSS each; threshold 4 MiB (a floor for a release that cannot return a page, not a saving); `trim_if_released`/`worth_trimming` at the two release seams; `did_close` keeps M64's unconditional trim; policy pin. (The harness landed in M67's commit — fold note.)
- M69: `Vec<Arc<ImplNamespace>>` (Arc: a stored world crosses threads); `Analyzer::clone` 66,881,118 → 66,833,191 Ir; no new pin (representation).
- Gates by the lane: core 5651, lsp 738, core lib 749, cli release_scripts 22, clippy, fmt.
- FINDS → candidate items: F1 (BIG) 7 of kolt's 19 files (`app_context, app_overlay, prefs, sidebar, store, styles, theme`) can NEVER be served by the base cache — an import cycle reaches the entry's own module, `entry_alias_module` is set and `entry_is_open_module` suppresses the pre-entry resolve AND the store — each pays 0.42–1.43 s CPU per analysis, per keystroke; dwarfs the budget question (M55/M19 territory) — file as its own M item, HIGH; F2 the LRU cliff (a budget one world below the session's cycle takes a scan from 7 to 17 misses); F3 the budget-constant coupling (fixed); F4 kolt today is 12 worlds / 271,915 KiB (not 13 / 276,267).
- OPEN Qs (recs): (1) add `base_cache_worlds/weight/budget` to E166's summary line (session_trace's pin text is E174's payload) — at the sweep or Order 36; (2) re-run the budget walk after M55 (192 MiB may then be moot); (3) the live-entry exemption never changed a miss count in the walk (the focused doc is also the MRU) — its value is the idle-focus case, pinned directly; kept as ruled.
- Fold: analyzer.rs `BaseCacheState` (+field), `evict_to_budget`, `base_cache_admit`, the two consts, `impl_namespaces` field + the push in `walk_expr_node_inner`'s `Node::Impl` arm; main.rs `enforce_program_retention`, `analyze_and_publish`, `Backend::focus`, `did_close`; memory.rs new fns; document.rs `mod perf_baseline` all NEW fns (no existing fn edited).

## visibility-b-35 — S3 landed in three commits (grammar+printer+sort key b71d6fdb; `file_impls` + admission 42598432; E168/E169 re-pointed + completion 7f58266d), fifth to report (130 min, 279 tool uses); merge AFTER visibility-a-35 (parsing.rs/analyzer.rs/document.rs/formatter.rs all edited — expect hand merges; allow-edit `organize_rewrites_an_emptied_import_whose_module_still_carries_an_impl`)
- Grammar: `only` as a trailing modifier (`parse_import_modifier`, contextual like `as`); `(impl TYPE)` as a brace-set element (`parse_impl_selector` + helpers; `parse_namespace_set_element`; ALSO an arm in `parse_namespace_path_inner` — paper correction: without it `import a::(impl T);` still failed at column 1); `Node::Import` gains `ImportModifier`, `ImportBranch::Selector`; `_` cost ZERO grammar — B294's anonymous binder already is the hole. The selector COMMITS at its two-token gate so a typo reports inside it. Six curated rule constants, NO ledger rows (row 229's population — brief correction: 'one row' for the `as` refusal was wrong); `RULE_STATEMENT_SITES` 23 → 29; `NON_KEYWORD_TERMINALS` gains `only`. Formatter `BranchKey::Selector(String, Box<BranchKey>)` after `Path` — the SHARED key; both the AST printer and the token net learn it. Spec grammar.md §3.2, names.md §4.3.
- `file_impls`: `Implementation.source: SourceId` STORED (not via `source_of` — M27); `ImportImplRestriction`/`ImportImplSelector`, `bank_import_restriction`, the post-build pass `check_impl_selector_admission` (selector subjects walked in a child scope so `_` registers and the name resolves in the importer's scope; admission unifies BOTH ways — `subject_applies(impl, selector)` concrete, `subject_applies(selector, impl)` placeholder: paper §3.4 correction); `impl_select.rs`/`candidates_of` family untouched (S4's); one row NEW (`'{member}' is provided by an impl in module `{module}`, and {claim}: {fix}`); ten pins.
- E168/E169 re-pointed: `keep_module` → `ModuleRescue::{No, Module, Selector(String)}`, the rewrite becomes `import a::{ (impl T) };` when one subject; `import_leaf_is_used` answers a selector span from `impl_selector_members`; completion after `impl ` and `)::` (`module_impl_blocks` from the parse cache); editor.md rows. EDITED existing pin `organize_rewrites_an_emptied_import_whose_module_still_carries_an_impl` (expectation `import pkg::a::{ (impl i32) };`). NOTE for the merge: editor-35's E173 records `keep_module` through `organize_import_runs` — the predicate's return type changed here; the fold must reconcile `rewritten_import_statements` with `ModuleRescue`.
- Gates by the lane: core 5674, lsp 742, ide 8, std full-scan, cli corpus/examples/release_scripts/diagnostics_ledger/grammar_ebnf, docs, clippy, fmt; zero golden movement, zero new firings (nothing in the estate writes `only` or a selector).
- SEAMS: S2 — `#(impl T)` not built (needs `Token::Hash`; widen `at_impl_selector`, a `reach: bool` on `ImplSelector`, the admission pass skips the export bit); S1 — `module_impl_blocks` and the member arm should filter on `Importable.exported`; S4 — `Implementation.source`, `import_impl_restrictions`, `impl_selector_members` are the inputs; lift `check_impl_selector_admission`'s `restricted` map into a `Program` field (it IS `file_impls` inverted), feed the candidate family, delete the post-hoc call check.
- FINDS → candidate items: F1 `source_of` on a module segment LIES when the module owns both `x.vl` and `x/` (the segment resolves to the directory NAMESPACE minted in the importer's id range → `source_of` answers the importer) — E168/E169's `import_module_is_used` makes the same lookup, so Organize Imports is probably wrong for such a module (probe); F2 `cargo test -p vilan-core --test inference -- modules::` fails two prelude-shaped tests that nextest passes (threaded shared state; reproduces with the lane's file reverted); F3 `vilan fmt` reprints a selector's subject verbatim (a comment inside makes the keys differ → the net bails, no-op); F4 go-to-definition/hover on a selector subject work for free (pinned).
- OPEN Qs (recs built): (1) a selector-only statement's module is looked up by FILE NAME (`module_source_by_name`) since `resolve_import` never walks a statement that binds nothing — rec: in S4 give `resolve_import` a `bind: bool` and route selectors through it; (2) the E168 rewrite may produce a RESTRICTING statement — kept per §7.2 (the knob is one arm of `rescuing_subject`); (3) a selector admitting NO implementation is silent — rec: a small diagnostics item.
- MERGED 492b1769 (hygiene-35, second merge; pushed): clean, no fold; ledger row 434 (N79) assigned, row 163 edited in place; parity 385/385; gates green (core inference 4024, module_resolution/markdown_golden/docs/check_scope_differential/replay_differential, std full-scan, embedded-std 13, cli upgrade/service_layer/diagnostics_ledger/examples/corpus 85, lsp, release_scripts, split, fmt). Next ledger id 435. Next merge: lang-35.

## slots-35 — A85 BUILT (6c75ab53), A91 BUILT (d5c129bf), sixth to report (140 min, 256 tool uses); merge LAST (split golden, ssr-render.mjs, markdown_anchors.golden, copy-elision-census.tsv all moved — regenerate the split golden once more over the merged tree; allow-edit `the_router_example_splits_into_its_three_routes` (chunks.rs), `the_index_form_keeps_the_rows_element_and_updates_through_its_cell`, `a88_the_anchors_go_with_the_content` (ui_rows.rs))
- A85: five free functions returning structs with `Slot` (`when → Conditional<S>`, `swap → Swap<T,S>`, `each → Each<T,K,S>`, `each_values`, `each_by`), each `place` opening its `Region` at the hole; the five View methods construct the struct literal (NOT literally sugar — FIND 1: a method name shadows a same-named free fn inside its own impl block and there is no qualified escape from inside std; paper correction); `@process` twin in lockstep; no `Group`; B253 unchanged. Split recognizer: `Gate.retarget` table (method arg 1 / free `swap` arg 0) + `std_free_function`; the value-form route `swap` splits with a transcript byte-identical to the method's (pinned). The `place` needs-context PROBE: `View::child<C: Slot>` inherits the requirement PER INSTANTIATION (a static child outside a boundary still compiles; a value form outside one is refused with the threaded diagnostic) — no design work needed, pinned twice. Emission trap found by a red pin: `swap → swap_split` retargeting reused the call's arguments after `thread_contexts` had made context an ordinary parameter → one argument short at runtime; designed around by making the gate a CLOSURE FIELD (`split_route` builds `wire: (|| void) context owner_scope`, `place` runs it); a flagless build emits no chunk machinery (router 17,471 → 18,005 bytes, zero `chunk`); each navigation's gate writes open their own turn (behaviour change in an unexercised corner — flagged).
- A91: `|T| C` with `C: Slot` on swap/swap_split/when/bind_each ×3 and the value forms; the reconciler's unit is a REGION: one leading empty-text marker per row, extent marker → next boundary, an order pass cuts live rows by span first then re-inserts; `Region::{Row, open_row, cut_row, insert_row, drop_row, hold_rows, host}` (`host()` reads `parentNode` off the anchor — a region that remembered a detached fragment host kept nesting into it: found by a red pin); `std::dom` gains `create_document_fragment`, `create_range` + `Range::{set_start_after,set_end_before,extract_contents}`, `insert_text_before`, `Text::parent_element`; three `ALLOWED_DIVERGENCES` in `std_twin_parity.rs`. FIND 3 (cost): +1 node per row and ~2× DOM mutation calls per reconcile (1,000 rows: nodes 1,005 → 2,005; insertions ~4,000 → ~8,000; the Ir figure is stub-dominated) — follow-up: skip cut/re-insert for a row whose index did not move (the plan knows `Keep(index)`).
- KOLT SKETCH (kolt untouched; unmodified kolt builds clean — non-breaking): row 1 channel.vl:28 removed by A85 (`{each_values(..)}`), row 6 views.vl:233 removed by A91 (the row closure returns `swap(..)` directly); row 4 views.vl:151 NOT removed — the outer swap's arms must agree on one `C` (`View` vs `Swap<..>`): B253's, not A91's (A91's item text named rows 4 and 6 — correction). 2 of 3.
- Paper corrections: §6's B253 spelling cost grows by one type argument (the row shape `C`; a defaulted parameter is applied eagerly at a struct literal, so it shadows inference); §3b "methods become sugar" not literally spellable (FIND 1); §3d's process-twin prediction held (`Row {}`); reactive.vl untouched, dom.vl needed a fragment/range/parentNode.
- FIND 2: `let wire = || { … };` does not carry a clause from the field it lands in (an annotated `let` does) — B325's neighbourhood; the shape std wants.
- Gates by the lane: core 5655 whole (an earlier attempt hit `StorageFull` ×3 — /tmp is a 12 G tmpfs shared by the lanes; re-run green), cli 836 whole, lsp+ide+wasm+embedded-std 792, std full-scan, inference, docs, std_twin_parity, markdown_golden, ui_rows/reactive_*/split/ssr_differential/examples/corpus/copy_elision_census/release_scripts/chunks, clippy --workspace, fmt, `vilan fmt` over three .vl. No ledger rows.
- OPEN Qs (built to rec): (1) the gate as a closure field (a second `SwapSplit` struct is unbuildable — retargeting is emission-time); (2) turn-per-navigation in the route gate (flagged, transcripts identical); (3) the per-row marker cost (the general path kept; a runtime discriminator would need an ambient flag).
- MERGED 871fd753 (lang-35, third merge; pushed): CHANGELOG 1 hunk mechanical, no fold; ledger rows 435 (B331) and 436 (B332) assigned; parity 388/388; gates green (core inference, module_resolution/docs, std full-scan, cli module_paths/corpus/examples/diagnostics_ledger, lsp, release_scripts, split, fmt). Next ledger id 437. Next merge: dx-35.

## style-35 — G23 BUILT (6c77482d), B308 BUILT on it (9e818c1f), A95 S1 BUILT (42908f11), seventh to report (162 min, 254 tool uses); merge after visibility-b (formatter.rs: `"on"` added to `STYLE_BARRIER_METHODS` — disjoint from visibility-b's organize region; goldens: `vilan/test/{style,css-block,theme}.css` moved by SUBTRACTION only, the markdown anchor golden +1)
- G23: `asset::schedule_at_end(finaliser)` — the const channel's sixth verb; a scheduled fn recorded by its EMITTED name (the name generator is the identity; an anonymous closure REFUSED — its scope dies with the run); `run_finalisers` after the last const expression re-enters each through a synthetic site seeded with the SCHEDULING site's emission record; a finaliser may schedule another; a panic fails the build naming the fn (one row NEW). "The end" = the end of ONE compile's const pass per leg; under `run --watch` the end of the ROUND (recompiled legs re-schedule and re-emit; untouched legs keep their sidecar via B276/M59). One carve-out: `schedule_at_end`'s ARGUMENT may name a compile-time-only fn. BRIEF CORRECTION: "a module accumulates into a global" is NOT achievable with the hook alone — a const pass gives every site its own interpreter scopes, no vilan global spans two `const` expressions → the registry is HOST-held (`ProjectReader`), exposed as two new channel verbs `asset::stage(kind, token, line)` / `asset::staged(kind)`. Five pins (styling.rs + assets.rs's HMR round pin: recompiled legs re-run, the rest retained). Docs const.md §9.2.
- B308: `Style::rule` stops emitting; stages under its class token and schedules `flush_stylesheet` (idempotent); LIVENESS = a token some const RESULT names (its strings, their words — `class_list` joins — or a substring): sound and over-approximate in the safe direction; `asset::staged` refuses before evaluation ends. DEAD SHARE before → after (controlled: `rule` temporarily reverted to `emit`, matching kolt's own dist at e4d192e3): kolt client.css 19/162 rules, 966/7,962 B = 12.1% → 0/143, 0.0% (11,986 → 11,020 B, −8.1%); corpus style.css 31.4% → 0, css-block.css 29.1% → 0, theme.css 24.1% → 0; style-when/preflight 0 → 0. Goldens moved by subtraction only, verified line by line. Five pins. The retraction design NOT built (withdrawn).
- A95 S1: `Condition` (a condition SET), `IntoConditions`, `+`, `.not()`, `.eq()`, the free constructors (hover/focus/active/disabled/first/last/pseudo/element/attribute/within/children/divide/sm/md/lg/xl/media), the canonicaliser (slot order + five refusals), `Style::on<C: IntoConditions>(conditions, inner)`, EVERY combinator re-expressed as a fence pass + one `on`; `render_rule` walks the canonical tokens once (two pseudo-classes on one rule and guard+child-relation now reachable); pseudo-element token carries `%`. GATE: 145 artifacts (131 corpus .mjs + 5 .css, two styling examples ×3, kolt ×3) BYTE-IDENTICAL before/after, run three times (`scratchpad/style-35/twinrun.sh`). 22 new pins; the 232 existing hold. 16 rows NEW. `"on"` → `STYLE_BARRIER_METHODS` (forced by style_table_sync).
- TWO FORCED DIVERGENCES from the paper (§13 as-built at the sweep): (1) the five condition KINDS are ONE type — `trait Add<B = Self>` returns Self, so `Pseudo + Attribute` cannot exist; `Condition` carries its kind in the token, `.eq()` on a non-attribute / `.not()` on a breakpoint are const-time refusals (§1.1's own precedent); `IntoConditions` has one implementor (kept as S2's seam); (2) `within(condition)` REFUSES a non-attribute condition in S1 — the guard's selector travels inside the slot key `media:condition:property` where a pseudo-class's `:` mis-aligns the split (B311's grammar); `within(hover())` refused naming the working form; widening needs a KEY change (S2 or later). §12's `within(hover())` example is not spellable yet.
- FINDS → candidate items: css-block §15.3's three exhibits are STALE (kolt has no `button_style_base`; `default_panel_style` is `const style::style()` — empty) — §8's re-measure plan needs re-anchoring (the number that moved is the sheet: −8.1%); kolt's four raw `pseudo("hover:not(:active):not([disabled])")` compounds are exactly S2's `.on(hover() + active().not() + attribute("disabled").not())`; `rpc_http.rs::a_handle_method_over_the_connectionless_post_leg_fails_naming_the_method` flaked once under load ("note err Waiting" — a lease answer read before the first lease failed) — timing race, item; interpreter `Failure` messages are consistently UNROWED (asset::emit outside const, check_emit_kind ×2, stage/staged) — check (3) enumerates only `Error { msg: <literal> }`: ruling wanted.
- OPEN Qs (built): `IntoConditions` kept with one implementor (S2's seam); the sugar's six nesting refusals KEPT verbatim in S1 (deleting them would make refused programs compile with no spelling to steer to; S2 deletes them with the codemod); `Condition::eq` vs a future `PartialEq::eq` name clash — rename to `.value(..)` then or never derive `PartialEq`.
- Gates by the lane: core 5675 full, lib 749, docs, markdown_golden (regenerated: +1 anchor `guide/styling.md h2 what-reaches-the-stylesheet`), style_table_sync, std full-scan, cli 830 full (corpus/release_scripts/examples/split/hmr_css_matrix/ssr_differential/style_when/style_chain_order/assets/diagnostics_ledger), lsp 737, ide+wasm, clippy, fmt, `vilan fmt --check` over std and test.
- Brief corrections: §15.3 is the CHARACTER measurement (the dead-share numbers are B308's own); G23's pins had no home in the PIN LOCATION list (styling.rs + assets.rs chosen).
- MERGED 03df116d (dx-35, fourth merge; pushed): CHANGELOG 1 hunk mechanical, no fold, no ledger rows; parity 391/391; gates green (core base_cache+inference 4055, lib, lsp, release_scripts, split, fmt). Next merge: style-35 (ahead of solver/visibility — its regions are disjoint; 17 NEW rows; three .css goldens + the markdown anchor golden moved — the anchor golden may conflict with hygiene-35's regeneration: regenerate over the merged tree if so).

## visibility-a-35 — B320 (4f786f8f), B321 (068ae50b), S1 grammar (d5178a6e), S1 the bit (c0a41264), S1+S2 warnings + `#` + `[doc(hidden)]` retired (fe542196), eighth to report (182 min, 585 tool uses); merge after style-35 (allow-edit: `doc_hidden_method_stays_callable`, `the_pub_steer_names_the_export_form_it_is_not`, `corpus_absent_constructs`, `a_non_entry_self_import_stays_clean`, `several_parse_errors_in_one_module_keep_distinct_spans`, `b76_the_parse_path_is_the_non_panicking_alternative`; consts `B239_FILES`/`helper` edited beside pins — merge_items_by_name if they conflict)
- B320: `Parser::import_path_failure` records how far the path grammar got; `emit_failure` consults it (the path is built from speculative `eat_*` probes, so `farthest_failure` never sees it); one row NEW. B321: `export_takes` admits an item / import / use / export / Error and peels attribute wrappers; `export (helper);` reported and KEPT (dropping it would unbind names); one row NEW.
- S1: `export *;` (`*` + `;` lookahead — `export * helper;` is a real deref) as `Node::ExportAll`; `export(in PATH)` GENERAL (`mod`/`pkg` reserved heads enforced exactly; a general `pkg::a` path parses/stores/reprints but is ADMITTED — the subtree test needs source paths on `Program`: FIND 8); `Importable.exported: Visibility { Private, Exported, Scoped }`; `exported_entities`/`export_all_modules`/`export_scopes`/`curated_modules`; add-import quickfix + the B4 steer filtered; access never gated. The two warnings: exposure (ONE per declaration, not per position — B5; `check_exposed_unexported_types`, post-build) and plain-reach (`check_plain_reaches`; a SECOND wording for the qualified path — no leaf to mark; a redundant-marker warning too). `#` = `Token::Hash`; row 335 DELETED and a NEW `flagship` row `HASH_IS_NOT_A_CSS_VALUE` (the head changed — an edit was not available); `[doc(hidden)]` RETIRED (`DOC_HIDDEN_IS_SUPERSEDED`; the fixed attribute ORDER changed: grammar.md EBNF, grammar_ebnf's marker gate, two doc fences, a formatter const, a backed_enums program). Ledger: 8 NEW + 1 edit (`visibility_marker_rule`) + 1 deletion (335); `RULE_STATEMENT_SITES` 23 → 28.
- WARNING CENSUS (final binary; all still compile): kolt 272 (64 distinct pairs; 30 qualified, all generated lucide), website 130, benchmarks 34, examples 56 (walkthrough 28, todo 14, …), std/macro_std/corpus/docs 0; exposure warnings 0 everywhere (by design — FIND 2).
- TWO ROLLOUT ADDITIONS the paper lacked (built, for §8): (F1) S1 as written turns `every_std_module_is_clean_under_full_scan` permanently red — std's 118 cross-file imports all fire (800 warnings in the `todo` example alone, ~780 about std internals) → SILENT when the importing file is a `std_sources` member (the A2 `check_deprecated` precedent); S6's std curation lifts it. (F2) On day one NO module has a marker, so a strict completion/add-import/steer gate hides every std name → `module_is_curated`: a module with no marker offers everything; gates the tooling and the exposure warning, deliberately NOT the plain-reach warning (which is what tells an author to curate). Also: a SELF-import never warns (F3); a REFUSED import gets no visibility warning on top (F4, span overlap).
- FINDS → candidate items: F5 `Document::QuickFix` is single-file → "Export `S`"/"Export `hidden`" ship as MESSAGE steers only (the two same-file fixes ship: `Import as #leaf`, `Delete the #`); give `QuickFix` a target path; F6 `resolve_world` calls `resolve_import` with `report=false` on the fixpoint pass (comment added); F7 vilan-ide's two completion filters (`OriginListing::completions`, `auto_import_completions`) NOT done — visibility-b's file (one forced line in `keyword_lexeme`'s exhaustive Token match); F8 `export(in <general path>)` admitted; F9 `export impl` RECORDS the bit — the ruled consequence (an invisible impl contributes no methods; no ambient impls) is S3/S4 enforcement, not built here; the exposure warning's dependency arm implemented but UNPINNED (needs a curated dependency package in the harness).
- Paper corrections (visibility.md §14 at the sweep): §9 S1's pin "`export(in pkg)` from a dependency warns at the consumer" contradicts the RULED no-diagnostic — built the ruling; §4 one warning per declaration; §5 needs the qualified-path wording; §7.1's `&& importable.exported` insufficient without the uncurated exemption; §4's workspace-edit fix inexpressible; §1's `import_steer_inner` is at :38792; §7.5 overridden by the ruling (full retirement).
- Gates by the lane: core 5665 (std full-scan inside), lsp 747 + ide 8, cli corpus/examples/module_paths/release_scripts/diagnostics_ledger/grammar_ebnf/book_mirrors/hygiene/diagnostics/document 144, docs 9, clippy, fmt; no .vl touched.
- MERGED 7f05e26f (style-35, fifth merge; pushed): CHANGELOG 1 hunk mechanical, no fold; ledger rows 437–453 assigned (G23's finaliser panic + A95 S1's sixteen canonicaliser/condition refusals); the markdown anchor golden merged clean beside hygiene-35's line; the three .css goldens by subtraction; parity 394/394; gates green (core inference, style_table_sync/docs/markdown_golden 21, std full-scan, cli corpus/examples/assets/hmr_css_matrix/ssr_differential/style_when/style_chain_order/diagnostics_ledger 62, lsp, release_scripts, split, fmt). Next ledger id 454. Next merge: visibility-a-35 (expect `resolve_import` to conflict with lang-35's merged B332 edits — hand fold).

## solver-35 — all eight landed (B323 b979cbfa, B324 4d09b402, B325 edd4075d, B326 c15ad25e, B327 347c0540, B328 95a5eafd, B329 297a8929, B330 0464161d), ninth and last to report (183 min, 244 tool uses); merge after visibility-a (allow-edit `an_injected_closure_cannot_escape` — macros.rs `mod tests` in src, so no fold needed; one row NEW (B324); row 3's TEXT edited but its truncated key does not move — the proposals ledger paper's prose wants the new wording)
- B323: `Program::struct_literal_expectations` (the walk's `expected_types` landing on a struct initializer); `field_type_at` resolves an instance's generic arguments through `instance_types_of` — annotation first, the literal's type second; `saw 9` → `saw 3`; pin + the constructor control.
- B324: `unfollowable_clause` walks one level in from every position the pass knows, stopping at a closure type, following a struct's generic argument where `field_type_at` does; refuses the written POSITION (not each literal — the map-value case has no literal the pass can associate; strictly wider) — one row NEW; three pins; nothing in the estate fires it.
- B325: the binding landing rule runs over every binding whose TYPE carries a clause; an unannotated binding is a forward by construction; EDITED `macros.rs::an_injected_closure_cannot_escape` (its escape moves to a clause-less parameter); two pins; a message stops firing for one form (lsp re-run green). FIND 1: an ANNOTATED binding whose initializer is a clause-returning CALL is still refused (`let body: (|| void) context c = make();`) while the unannotated form runs — the call's `expr_type_ids` entry does not carry the clause; a widening attempt did not fire and was reverted; rec: record the clause on a call's own type or read the callee's declared return at the landing — small solver item.
- B326 (R4): `handle_return_element`'s `Option` arm declines a keyed inner (mirrors rpc.vl's `handle_key(inner) == ""`), `resolved_handle_return_element` too; two generated-code reports → one Wire refusal in the method's vocabulary; two pins.
- B327: `check_hashable_boundary` asks `resolved_type_is_hashable`; `is_hashable_type` DELETED; message gains "or a type with an `impl .. with Hashable`" (row 3 text edit, key unmoved); two pins.
- B328: `expr_type_id`'s `Expr::Local` arm falls back to the parameter table; `drop_argument_type_id`'s private copy deleted; corpus byte-identical; pin non-vacuous (removing the arm trips the sink's guard).
- B329: `written_source_element` reads the element off the annotation; the `[expose]` refusal renders via `render_type`; the `expose_refused_elements` stand-down key deliberately keeps `pretty_print_type` (routing everything would un-suppress the mirror's duplicate); the sweep found NO other message printing a written annotation (B302 covered them); two pins.
- B330 (R5): doc-only — "What this rule DOES NOT refuse" at `generic_bounds_overlap`; two pins (declaration order picks the body for a receiver satisfying both: `debug side` / `tagged side` when swapped).
- Gates by the lane: core 5665, cli 829, lsp 737, ide+wasm 46, clippy, fmt; corpus byte-identical.
- FIND 2: `/tmp` is a 12 GiB tmpfs and the inference harness writes its `.mjs` runs to `temp_dir()` — three `No space left` failures once under nine lanes (also seen by slots-35): harness item (write under `target/` or clean eagerly). Housekeeping: `vilan build` over `vilan/test/*.vl` in place emitted four asset artifacts (`.vilan-bundled`, `icons/close.svg`, `logo.<hash>.svg`, `robots.txt`) that `git add -A` swept into a commit — removed and amended; those paths are NOT gitignored: item.
- MERGING visibility-a-35 (sixth merge): merge_fold committed 83c00d0b (ledger rows 454–461 assigned; row 335 deleted) but its by-name fold of module_resolution.rs broke the file THREE ways — (1) the lane's `B239_FILES` const (an EDITED existing const: the fold carries new consts only) lost its two `export *;` entries; (2) hygiene-35's RENAMED test came back under its old name (`rust_fallback_derives_parse_through_the_content_cache` — absent from HEAD, so 'new'); (3) the lane's multi-line string const `EXPOSURE_MODULE` (`const … = "\` continuation) was SPLIT: its three header lines were dropped inside the `use vilan_core::{ … }` block and its 32-line body vanished — hence `prefix std is unknown` at line 22 and `\` tokens at line 100 (the gate's build error, exit 6, NOT pushed). `merge_items_by_name.py` reproduced (2) and (3). Hand-reconstructed (fix commit f48ebd7e): the lane's const inserted once before its first user, the B239 lines restored, the resurrected test dropped; module_resolution+docs+parse_differential 181/181; remaining gates running detached via gates_and_push.sh. PROCESS: both item tools mis-split a multi-line string const whose first line ends in `"\` — a splitter defect to fix in the tools (Order 36 hygiene).
- visibility-a-35 gates after the repair: core lib 755, std full-scan, cli set 122 green; lsp 751/752 — editor-35's E173 pin `a_rewritten_imports_leaf_publishes_its_own_text_at_the_same_severity` failed because its fixture imports an UNEXPORTED `b` and under S1 the plain-reach WARNING now publishes beside the hint; fixed by the estate's own `export *;` line in the fixture (b62c8bf8); lsp 752/752; the last gates (ide, diagnostics_ledger, book_mirrors, release_scripts, split, fmt) + push running detached.
- MERGED visibility-a-35 (sixth merge) = 83c00d0b + fix f48ebd7e (the fold repair) + fix b62c8bf8 (E173's fixture); PUSHED at b62c8bf8; ledger rows 454–461 (B320, B321, the exposure warning, the plain-reach warning, the redundant-marker warning, the qualified-reach warning, `HASH_IS_NOT_A_CSS_VALUE` flagship, `DOC_HIDDEN_IS_SUPERSEDED`), row 335 DELETED, `visibility_marker_rule` edited; parity 401/401; gates green (core inference 4050, module_resolution/docs/parse_differential 181, lib 755, std full-scan, cli set 122, lsp 752, ide 8, diagnostics_ledger/book_mirrors 19, release_scripts 22, split 12, fmt). Next ledger id 462. Next merge: solver-35, then visibility-b-35, then slots-35.
- MERGED 592e45d3 (solver-35, seventh merge; pushed): CHANGELOG 1 hunk mechanical, no fold; ledger row 462 (B324) assigned; parity 409/409; gates green (core inference, lib, std full-scan, cli service_layer/corpus/examples/diagnostics_ledger, lsp, release_scripts, split, fmt). Next ledger id 463. Next merge: visibility-b-35 (expect src conflicts with visibility-a in parsing.rs/analyzer.rs/document.rs and with editor-35's E173 `keep_module` recording — `ModuleRescue` contract; hand resolve).
- MERGING visibility-b-35 (eighth merge): merge_fold STOPPED — nine unresolved files (analyzer.rs 3 hunks, formatter.rs 7, lib.rs 2, macros.rs 1, node.rs 3, parsing.rs 4, document.rs 2, grammar.md 1; module_resolution.rs folded by name, resurrecting hygiene-35's renamed test a THIRD time). HAND MERGE (db350e3d): every hunk was the two visibility lanes adding adjacent arms/variants/productions — resolved by union in written order, with three shapes to watch: (a) the `Node::Import` arity (`Import(ImportBranch, ImportModifier)` from S3 kept; HEAD's `Export(Option<Box<ExportScope>>, ..)` and `ExportAll` kept; every one-field `Node::Import(_)` pattern → `(..)`); (b) a hunk whose closing brace sits BELOW the markers (a shared tail) — union leaves the first block unclosed: formatter.rs's token-set loop and `emit_branch_tokens`' `Reach` arm, and document.rs's two test hunks (each fn's `);`/`remove_dir_all`/`}` tail duplicated for both sides); (c) five exhaustiveness holes where one lane's new fn matched the other's new variant (`collect_impl_selectors`/`collect_branch_selectors` gain a `Reach` arm delegating to the inner branch, `import_root`/`collect_reach_marked_spans` gain `Selector`). Then E173 (editor-35) vs S3's `ModuleRescue` contract: `rewritten_import_statements` records `(span, rescue)` and renders the SELECTOR form (`import pkg::a::{ (impl i32) };`) when the rescue is one subject — the fade names what the action now does; `ModuleRescue` derives `Clone`; the two E173 pins' expectations updated. Workspace builds; lsp 757/757; `cargo fmt --all`; gates + push running detached.
- visibility-b-35 gates after the hand merge: inference 4071 green; module_resolution/docs/markdown_golden 186 green; the vilan-core LIB TEST build failed on a one-field `Node::Import` pattern in visibility-a's reach-marker pin (fixed), then two of visibility-a's B320 pins failed because they pinned the PRE-S3 world (a selector and `only` as malformed imports): updated to the merged grammar — the four selector shapes parse clean, `::*` and `!hidden` keep B320's rule, `use a::{ (impl T) }` reports S3's `USE_TAKES_NO_IMPL_SELECTOR`, `import pkg::a merely;` is the missing-`;` control; lib 763/763; the remaining gates + push running detached. LESSON: two lanes in one order can pin contradictory premises about the same grammar — the integrator's merge must re-read the first lane's pins against the second lane's landing.
- visibility-b-35, one more: merge_lane's `.rs` conflict-UNION step kept BOTH lanes' `RULE_STATEMENT_SITES` lines (28 and 29 off a base of 23) → the diagnostics_ledger test binary would not compile; set to 34 (the sum), `the_keyless_row_still_counts_the_rule_sites` confirms; 17/17. Fix commit on top; the cli set + lsp + ide + wasm gates and the push running detached.
- MERGED visibility-b-35 (eighth merge) = db350e3d (hand merge) + cf88e171 (B320 pins re-read) + fdaea14b (RULE_STATEMENT_SITES 34); PUSHED at fdaea14b; ledger row 463 (S3's admission refusal); parity 412/412; gates green (core inference 4071, module_resolution/docs/markdown_golden 186, lib 763, std full-scan, cli corpus/examples/diagnostics_ledger/grammar_ebnf/module_paths 59, lsp 757, ide 8, wasm 38, release_scripts 22, split 12, fmt). Next ledger id 464. LAST merge: slots-35 (allow-edit `the_router_example_splits_into_its_three_routes`, `the_index_form_keeps_the_rows_element_and_updates_through_its_cell`, `a88_the_anchors_go_with_the_content`; the markdown anchor golden will conflict with hygiene-35/style-35's regenerations — regenerate over the merged tree).
- MERGED baaa7911 (slots-35, ninth and LAST merge; pushed): CHANGELOG 1 hunk mechanical, the three edited pins folded by name, the split golden / ssr-render.mjs / copy-elision census / markdown anchor golden merged clean; no ledger rows; parity 414/414; gates green (core inference, docs/markdown_golden/std_twin_parity, std full-scan, cli ui_rows/reactive_lifetimes/reactive_channels/ssr_differential/examples/corpus/copy_elision_census/diagnostics_ledger, lsp, release_scripts, split, fmt). (The first launch was refused at the pre-check: `chunks` is a vilan-CORE test binary — the union covers it at the seal.)

## ALL NINE LANES MERGED on origin/next @baaa7911 (2026-09-13): editor c525d303, hygiene 492b1769, lang 871fd753, dx 03df116d, style 7f05e26f, visibility-a b62c8bf8, solver 592e45d3, visibility-b fdaea14b, slots baaa7911; ledger rows 434–463 (335 deleted; 163 edited; 118 unchanged); seal started; CI watched.
- SEAL on baaa7911: union 7409/7411 (two red: `workspace::a_post_build_violation_in_a_module_renders_in_that_module` — its entry now showed the plain-reach warning for an unexported `keep`; `benchmarks::benchmarks_run_and_report_the_deterministic_counts` — the benchmark packages' 34 reach warnings on stderr), clippy 0, windows 0, audit 0, fmt 0, parity 414/414. Fix 3473a550 (pushed): the fixture's `store.vl` and the six benchmark modules carry `export *;` (S6's codemod applied to one package early); workspace+benchmarks+release_scripts 76/76. Neither binary was in any lane's gate list — the seal's union is what caught them; CI on 3473a550 is the last word.
- CI on 3473a550: Ubuntu legs green, BOTH Windows legs red — two of visibility-b's selector pins (`b318_a_concrete_selector_serves_its_own_type_only`, `b318_a_method_selector_takes_only_the_member_it_names`): `module_source_by_name` matched a `/`-joined suffix against canonical paths as STRINGS, which never matches a `\`-separated Windows path, so a selector-only statement's module was not found and no admission refusal fired. Fix 9b22ec36 (pushed): match by path components (`Path::ends_with`); module_resolution 175/175 locally; CI on 9b22ec36 watched. LESSON: the seal's Windows leg is a cross-COMPILE, not a test run — a separator-dependent string compare passes it; L20 (the native Windows test leg on the WSL host) is the gap.

## SEALED 2026-09-13 at 9b22ec36 — CI green on all eleven jobs (Ubuntu ×2, Windows ×2, changes, fmt, vilan-fmt, wasm, audit, clippy, check); toolchain refreshed both locations (9b22ec364); worktrees/branches reaped; chronicle sealed paragraph appended. Order 36 draws from: B318 S4 (per-importer namespace — monomorphization; `resolve_import` `bind: bool`; lift the admission map into `Program`; `export impl` enforcement; `#(impl T)`), S5, S6 (the `export *;` sweep + std curation, then drop the std suppression); A95 S2 (breaking; kolt's migration at the owner's word), S3, S5 (+ the sugar's nesting refusals, `Condition::eq`); M70 + M55 (perf); A98; N81 (the merge tools), N82–N85; B333/B334 (rulings); E175–E179; the 86 open.
