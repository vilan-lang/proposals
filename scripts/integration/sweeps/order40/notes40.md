# Order 40 — running record (integrator)

Opened 2026-09-22 off 49de3915 (Order 39's sealed tip; origin/next == local; CI 35683785688 green 10/10; nothing landed since). Twelve lanes — go-items40.json. The owner: "Go with all recs" — R-a (dyn this order, in parallel with the paper), R-b (the v0.41.0 cut HOLDS through Order 40), R-c (B382 = a papers-40 recommendation, no build) RECORDED AS RECOMMENDED; every other ruling was made 2026-09-22 and stamped before the draft (rulings40.json).

- Standing integrator rules (Orders 38–39): state an outcome only AFTER reading the log; every merge gate list includes `-p vilan-cli --test native_differential` (+ the whole set before a native merge pushes); `regen_goldens.sh` after every golden-moving merge; verify every lane's commits by `gpgsig` header; a breaking/refusing lane's estate sweep re-runs over the MERGED tree; no edit in the integration worktree while a merge helper runs; poll a log's `^pushed` line, never `pgrep -f` the helper; tracker writes go through the helpers; `-p <crate>` with zero tests exits non-zero; no rebuild during a whole-crate run; `node_size` for node.rs lanes; `markdown_golden` + `book_sync` for docs lanes.
- Merge order: hygiene-40, editor-40, ui-40, std-40, solver-40, reactive-40, collections-40 (S3 rebased after solver-40), dyn-40 (BREAKING field refusal — estate re-sweep), native-a-40, native-b-40 (one rebase by its resumed agent), fmt-40 LAST. papers-40 → proposals only.
- LAUNCHED 2026-09-22: twelve worktrees cut off origin/next = 49de3915 (`commit.gpgsign = true` read intact before launch; `vilan/.claude/worktrees/<lane>`, branch `<lane>`); twelve Opus agents in the background (model passed explicitly), a shared preamble (`scratchpad/lane40_preamble.md`) instantiated per lane carrying the hard rules inline; kolt + proposals READ-ONLY for every lane except reactive-40 (its paper + sweeps/order40/reactive-40/), collections-40 (reactive-turns.md) and papers-40 (index-type.md, sweeps/order40/papers-40/), which commit in proposals and never push. Integrator work while they run: nothing in the integration worktree; merges begin as reports arrive in the recorded order.

## papers-40 — FIRST to report (44 min, 198 tool uses): I5 paper `proposal/index-type.md` (785 lines) + B382 page; three signed commits 4cde759/8afedbf/8b5179a (pushed by the integrator); NO vilan tree change
- I5: 105 index positions by parse (86 signatures + 19 fields), all `i32`; `i53` = fs offsets (STREAM kind, stays); WIRE proof: `usize` must write via `i32_value` (naive u53 moves the frame 104 → 108 bytes); the contract hash does NOT see a frame-width change (rule is load-bearing); FIVE literal-inference positions B370 did not close (left operand, annotated list elements, match pattern, generic call return, bare `let n = 0`) = S1's real content → new item at the sweep (F-c); rec S1 and S2 in SEPARATE orders. Q1–Q5 with recs in §11.
- B382 premise TWO-THIRDS FALSE: `export import pkg::x::DeltaCursor as KeyedCursor;` works today (transparent, over generic struct/trait/enum/fn/impl); only `type X = Y;` and `[deprecated]` on a type are missing → rec build `[deprecated]` on a type; restamp B382 and A112 (the ruled deprecated re-export WAS expressible; the hard rename was avoidable).
- FINDS: F-a HIGH MISCOMPILE `xs[at]` with `at: f64`/`str`/`bool` type-checks clean and crashes at runtime (write form too; native refuses with an unrelated rustc error) — RELAYED to solver-40 as an add-on; F-b native: an assignment RHS literal is emitted at the default width (`i -= 1` on a `u53` → E0308) — RELAYED to native-b-40 as an add-on (blocks I5 natively); F-d `Expected u53, but got i32` lacks the `as_*` steer; F-f the sized numeric family has NO `Display` (u53 refused by `T: Display`); the CLI's rustc `arithmetic_overflow` wording becomes a false accusation under I5 ruling 2.

## reactive-40 — SECOND to report (112 min, 147 tool uses): A123 e162ddfa + 358a6274 (8 pins, non-vacuous by plant; no goldens); A124 paper `proposal/reactive-pipeline.md` (806 lines; picks `Resource<T>`, `.or` resets/`.latest` holds; dyn = two vtable slots, no drop slot); S1 probe c0683da5 `std/src/reactive_pipeline.vl` (imported by nothing; 4 pins; measured cold 0.23–0.41× of five map cells in Ir). Proposals commits f9adece/5addd38/6d106e7 (pushed by the integrator).
- NOTE: f9adece swept collections-40's then-UNCOMMITTED reactive-turns.md edit (FIND-3/Q3 in §7.11) into reactive-40's commit alongside its §7.12 — both wanted; collections-40 told. Kolt A123 sites: SEVEN `map_safe` calls (model.vl:36,46,76,84,97,111,120) + channel.vl:35 — census in sweeps/order40/reactive-40/kolt_census_2026-09-22.md.
- PREMISE CORRECTIONS to A124/briefs40: kolt has ZERO bare-trait struct fields (Searchable/InputLayer are structs) — dyn-40 told; combine 5 (not 15), flatten 2, signal params 11, maps 55, gets 49, memo tables 6 (not 1) = kolt hand-rolling `.cell()`; 69 % of derivations single-reader; only 3 of 19 fields need `dyn`.
- FINDS: a refused impl subject (one type argument too many) still enters the candidate set as `unknown` and cascades into every dispatch of its trait ("both `Root` and `unknown` provide it") — repro sweeps/order40/reactive-40/unknown_impl_subject_cascades.vl → file; R2's one-id-per-leaf-chain needs `observe` to TAKE an id (S2a, paper §5); `on_settle` cannot be a blanket (triples work) — a trait requirement. Open Q1–Q5 in the paper §9 (Q1 rec keep `SignalCell`, not `Initial`; Q5 the combinator flip waits one order behind I5).
- MERGE reactive-40 (c1ef2692): clean merge, parity 590/590, markdown golden regenerated; gate RED at `shared_census` — A123's two rolling-subscription cells + the S1 probe's one moved the std `Shared` count (reactive.vl 33 → 35, reactive_pipeline.vl 1, total 132 → 135); the lane's battery never named `shared_census`. Integrator fixed the table (one commit on top), `gates_push.sh` re-running. RULE for the remaining std lanes (relay if they touch `Shared::new(`): `shared_census` is in every std gate list. Stray untracked `notes.db` (Sep 21 probe residue) removed from the integration worktree.

## fmt-40 — THIRD to report (120 min, 135 tool uses): E215 e68ee261 (the key; four-file set; book) + 11fd28ec (std opts in `wrap_comments = true`/`comment_width = 84`; the ONE reflow: 61 of 64 files, comment lines 11,495 → 11,233, zero non-comment lines changed, zero files whose comment WORDS changed, fixed point; 21 lines remain over 84 = the book paragraph) — LAST commit on the branch; E217 ce55652b (a hand-wrapped generic `impl` header PRINTS: tail list one arg per line; one estate line moved rpc.vl:4173). Six + six pins, non-vacuous by plant. Gates green incl. natdiff full, clippy, book_sync.
- PREMISE NOTE for the owner: std's comments are written to ~80, not 84 (census p50 75, p90 79, p95 80, p99 82; 11 lines at exactly 84) — the ruled 84 is WIDER than the written margin, so the reflow JOINS lines (net −262). Built as ruled; the owner may prefer 80.
- FINDS: docs.rs `RETIREMENT_NOTES` keyed on a long run of a std comment line broke under the reflow (repaired in the reflow commit; lesson: no test keys on a long substring of a std comment); E217 splits a one-argument list (consistent, odd-looking); parse_differential duplicates drop_trailing_commas on purpose (both must move together).
- OPEN Q → editor-40 (relayed): E216's Reflow action has no public paragraph-level entry (`comment_reflow::reflow` is pub(super)); build on `formatter::reprint_with(src, FormatOptions{wrap_comments, comment_width})` (whole buffer, agrees with format-on-save).
- MERGE PLAN: fmt-40 LAST; 11fd28ec will conflict with every std-editing lane — take the OTHER side for vilan/std/src and re-run `cargo run -p vilan-cli -- fmt vilan/std` over the merged tree (fixed point); then `fmt --check vilan crates` for E217's headers (dyn-40, collections-40 add `impl` headers); the vilan-fmt CI leg polices std prose from then on — re-run BEFORE the seal. Likely textual conflict with editor-40 in vilan-lsp/src/manifest_completion.rs (the `fmt` keys table).

## ui-40 — FOURTH to report (134 min, 294 tool uses): A121 S1+S2 + the DOM reads + N113 fourth edge; five signed commits a4f780dc/30c20147/9a0bed40/e4570ffb/182fe505; 12 pins non-vacuous by plant; markdown_anchors.golden +2; one NEW ledger row (`check_global_property_externs`); Shared census browser/ui 24→27, process/ui 3→6 (total 132→138 on ITS base; +3 over A123's 135 → 141 after both merges — hand fold expected in shared_census.rs); CHANGELOG hand fold (three entries). Gates green whole-crate (vilan-core 6193, vilan-cli 949+1 fixed, natdiff both).
- Kolt owed (the owner): overlay.vl:514 latch, :531–542 the `[x-autofocus]` block → `scope.focus_initial()` in relayout after `visibility: visible`; `focus_scope(panel_element, ..)` install; comment :510–513; six markup sites → the standard `autofocus` attribute (channel.vl:182,318,390; views.vl:461,726; theme.vl:173).
- Open Qs (recs): process twin's `autofocus` does NOT write the attribute (a served autofocus would focus before the client build) — rec leave; the focus module lives INSIDE browser/ui.vl + process/ui.vl (a new twin would need TWINNED_MODULES = hygiene-40's file) — rec keep; S3 (`focusout`) not built — rec close A121 on S1+S2, file S3 LOW.
- FINDS: the shared DOM stub's `focus()` accepted ANY element and dispatched nothing (fixed here — focusable-area + visibility rules + `focusin`); B271's suite keeps a private override that could retire onto the stub (hygiene item); stub.js is shared by eight suites — behaviour change noted. Outside its map: lib.rs one line + analyzer.rs one new fn at the end (N113's refusal).
- MERGED reactive-40 → next cc72ff92 (c1ef2692 merge + the census commit): gates green (184 cli, 4388 core, 842 lsp, release_scripts 22, split 13), pushed. ui-40 merging next (expect a hand fold in shared_census.rs: total 135 + ui-40's +6 = 141).

## editor-40 — FIFTH to report (142 min, 500 tool uses; two heavy legs unrun under loadavg 65–100): F27 f40ab8ba+90bd779a (R6: the E119 note names the twin that DECLARES the member, also on the `has no method` site; R2: `infer_platform` weighs member evidence when no import settles it), E213 b4021651 (built the ITEM's ruled spelling `[internal("reason")]` — the brief's `[doc(internal)]` was stale; fn/method/external + struct FIELD; completion hides unless a ≥3-char exact prefix, sorts last; `internal` semantic modifier dims; hover leads with the reason; std's `Region.anchor` + five row methods carry it in both twins; S2 owed: nominals/variants/traits/module bindings, docs "Internal" heading, `[lints] internal_use`), E216 850c9114+30d0bc6d (format-on-save reads `wrap_comments` via `wrap_comments_covering`; `reprint_with` twice as the "would it change anything" test; playground `format_checked_with`; NO dependency on fmt-40's names; at fmt-40's merge: pass `comment_width` at vilan-lsp main.rs `formatting` + `Document::comment_reflow`'s two calls). E214 STOPPED — PREMISE FALSE: `autoClosingPairs.notIn` takes only string/comment/regex and decides on the token LEFT of the caret (identifier for `List<`, whitespace for `a < b` — both Other), so no grammar scope can separate them; editor-39 had pinned exactly this (`e202_the_angle_pair_surrounds_but_does_not_auto_close`). Rec (priced S–M, needs a RULING): a client-side `type` override behind `vilan.autoClosing.generics` asking the server `opens_a_generic_list`, inserting `<>` and swallowing the next `>` (that map IS the overtype); the extension declares the capability in initializationOptions and `on_type_edits` stands down for `<` when set.
- Open Q: R2 cannot fix kolt's own case (a file no entry reaches is coloured by `default-entry`, which outranks inference) — rec leave the coloring rule; F27 R1's module-level `[platform(..)]` is the real fix. `Reflow this comment` edits the WHOLE buffer — rec narrow after fmt-40 or rename.
- FINDS: a long attribute line makes the signature below it split (pre-existing; fmt); `cargo fmt` joins a `\`-continued literal and leaves the indentation inside the string (AGENTS.md line wanted); `Document::semantic_tokens` returns spans while helpers assume line/char.
- Outside its map: lib/analyzer/parsing/node/macros.rs (F27 note sites + E213 records), formatter.rs (the two `[internal]` printing arms — fmt-40 overlap, additive), browser/ui.vl + process/ui.vl (six labels on `Region` — ui-40 fold likely). Unrun: natdiff full, examples, check_scope_differential, whole `-p vilan-lsp` — the merge battery covers them.

## SESSION RESTART 2026-09-22 ~13:30 — the integrator's process exited mid-order: seven lanes (hygiene, solver, collections, dyn, native-a, native-b, std) stopped mid-work with their worktrees intact (hygiene 10 commits all items; solver 6 — B371/B372/B381 left; collections 1 + S2 uncommitted; dyn 2; native-a 4 incl. the exit; native-b 6 + F26 uncommitted; std 10 — A120 S4/S5 left); ui-40's `gates_push` died after its first battery (209 green). 27 orphaned nextest/vilan processes killed by PID (loadavg 112 → falling). The owner asked for Opus 5.5 on all lanes: relaunched all seven FRESH on the Agent tool's `opus` alias (the tool exposes no version; a resumed agent cannot change model) with resume prompts (`scratchpad/resume_<lane>.md`: read your commits and uncommitted diff first; finish the remaining items; report the WHOLE lane). NEW QUIRK: this session's non-interactive shell lacks `~/.cargo/bin` on PATH — `gates_push.sh` exited 5 on `cargo: command not found`; every command now prefixes `export PATH="$HOME/.cargo/bin:$PATH"`; all seven lanes told. ui-40 gates re-running.

## native-a-40 — SIXTH to report (RESUMED; 13 min this session): five signed commits 735a0813 (std::json natively: vilan_rt::json, nine seams, three emitter gaps), a0ac30dc (Math/number labels/charCodeAt/process.env/fs/SHA-256; a void closure that awaits FLOATS), 0e816010 (F32: BigInt = i128; refusal past the limit, one NEW ledger row; two programs flip), 54d42be9 (`vilan-rt-sqlite` crate, rusqlite 0.37 bundled, linked only when std::db is reached; vilan-rt still dependency-free; `any` in the bind list; `for` over an Iterator impl; Bytes read accessors; five latent defects incl. a WRONG ANSWER in derive-enum.vl), b9f8d3ab (AGENTS.md repo map: nine crates). EXIT NARROWER THAN BRIEFED: a login server with kolt's SHAPE via the `Server` struct literal answers three exchanges over a real socket byte-for-byte with node (pin red with a planted wrong column reader); `Server::builder()` does NOT build natively, so the rpc server natively + keyed pins against it did NOT land — blockers: F22 (native-b-40: rpc_server.vl's `turn(.., || protocol.respond(..))` with an awaiting closure = an adapted instance), a RULING on mutable `Bytes` (alloc/fill/copy_into; native Bytes is an immutable refcounted buffer — rec a shared RefCell buffer), two seams (`now_millis`, SHA-1 digest). Whole set: 119 → 82 identical / 37 refused / 0 / 0 / 0. Host census on the exit: 0 gaps; on a bare builder: 6. Gates green (natdiff both modes, 79 goldens, 68 rt/rust/sqlite, clippy, audit 152 crates). §12 table updated in proposals d62adcf (pushed). Overlap with native-b-40 expected in vilan-rust/src/lib.rs ~4630 and native_differential.rs. ENVIRONMENT: `node` is ALSO missing from this session's PATH (`~/.nvm/versions/node/v24.2.0/bin`) — natdiff fails "failed to launch node" without it.

## hygiene-40 — SEVENTH to report (RESUMED; 27 min this session): 12 signed commits, all nine items. N119 13e44b7c (the 128 MiB thread + catch_unwind + join-Err arm ALREADY EXISTED, unpinned — now pinned, the internal-error diagnostic names the file; a STACK OVERFLOW aborts the process (exit 134), no Err to observe — rec: a stack-remaining probe in the analyzer's recursive walks + a pin at the playground fence lib.rs:472); N113 fc16c3aa+f39d489d (edges 1–3; RULE_STATEMENT_SITES 46 → 48; `g<()>()` moved to the parse decliners); N114 2292a7f7; N115 8b401ddd (five in-crate pins); N116 03d83f9b (no assertion reads a clock; spawn deadlines 5/500 ms); N118 0c37536b (surface = EXPORTED names; 14 of 26 allowlist entries retired — re-run std_twin_parity on the merged tree against ui-40's focus module); B376 8f420b47 (in macros.rs not analyzer.rs; unknown derive refused listing the six derivable names; NEW ledger row); M85 1901e91c+e0581e87+a58605fe (field table with bisect: 6.5–12.7 µs → 0.02–0.09 µs at 5,070 fields; whole hover 10–13 → 3.5 µs); B384 e4070eef (counter + vacuous pin deleted; arm kept with its reasoning). Gates green (lsp+ide 864, core 5522, cli 96/97 → fixed, natdiff both). Outside its map: analyzer.rs (B384's arm), parsing.rs (N113), spec docs. Trial merge: CHANGELOG + ledger only; B376's NEW → 551.
- MERGED ui-40 → next c2f605f5 (hand fold: shared_census total 141; ledger row 550; gates green after the PATH fix: 209 cli, core, 842 lsp, release_scripts, split), pushed. hygiene-40 merging next.
- MERGED hygiene-40 → next b9a48ac0 (CHANGELOG union parity 603/603; ledger row 551 = B376, row 292 edited in place = N119; markdown golden regenerated; gates green: cli, core, lsp 845, ide 19, release_scripts, split), pushed. editor-40 merging next.

## collections-40 — EIGHTH to report (RESUMED; 43 min this session): A125 c11a6edc (BREAKING; `K: Hashable` on reconcile + each*; 1,000-row reversal 738 M → 14.6 M Ir, linear; pins red at 31,375 comparisons with the scan planted back; goldens reactive-keyed + reconcile-index moved, runtime identical; census 349 → 351; estate: one site, ui_rows.rs B255 Row hand-impl). KOLT RULING WORDING CORRECTION: kolt's two keys `Theme` (theme.vl:285) and `Command` (views.vl:546) define `==` by id, so `[derive(Hashable)]` would hash every field and contradict equality (Command holds a closure + a SignalCell) — the owner writes hand impls `fun hash(self): Hash { self.id.hash() }`. A112 S2 f917da4e (`ListCell`, the two sequence traits, `map_each`; corpus `list-cell.vl` + golden; census 351 → 393; a 300-turn random walk; 1,081+1,081 calls vs 4,196 naive; four plants red; positions i32 with `// I5` markers). A112 S3 STOPPED — THIRD GAP, a COMPILER STACK OVERFLOW: a trait whose supertrait is the bound of its own blanket impl (`trait DeltaFeed<T> with Source<List<T>>` + `impl type S: Source<List<type T>> with DeltaFeed<T>`) aborts every program importing std::reactive; pre-existing on 49de39157; a std-free sibling is refused wrongly ("missing read") — RELAYED to solver-40 as its top item (B385-class); the held patch was also incomplete (needs a forwarding blanket over `DeltaSource<List<T>, SeqOp<T>>`, which B378 enables) and predates A125; evidence + repros preserved at sweeps/order40/probes/a112-s3/ (a no-supertrait candidate WORKS: one push into 1,000 rows builds 1). Rec: fix the solver gap and land the held shape (Order 41). FINDS: a `ListCell` write still deep-copies the whole list (`size()` via `SignalCell::get`; every notify hands each observer a copy) — matters for S3's Ir claims; held-patch drift. Merge: regen census + markdown_anchors (+3) after; reactive.vl re-export lines near reactive-40's hunks.

## std-40 — NINTH to report (RESUMED; 63 min this session): ALL TEN items in 13 signed commits; nothing slipped. B374 c0eb0d07 (`__guarded_async` glue; Err(Transport) with the host's cause), B375 e2358ed9 (+ 9b84d93c follow-up: `service_method_refusals` also read through `export impl` — four checks were silenced by `export`), B373 1becc367 (+ff90ada3: four goldens moved, identical prints), B383 5d8fcd98/94ae491b (arity via `decode_args_failed`; list closed), N117 bd1d4fe7 (three real holes), A120 S2 633d5567 (`over_http`), S3 39f993bb (status table: 405, 501 as envelope, content type required; `Response::header`, `post_bytes_typed`; CORS recipe), S4 269231ac (`Service::authorize_request(async |Request| Result<Session, Reject>)` after the 405/content-type checks; 401/403 → Unauthorized envelope, 503 → Unavailable, 429 bare; `Session` moved to std::rpc (re-exported); `RpcRequest`/`RpcProtocol` carry a session stamp; rpc.vl/rpc_server.vl re-fmt'd), S5 c7127a23 (`[service(Client, http)]` MARKER refusing an `[expose]` field, a SignalCell/KeyedCell return, `client = H`; node.rs/parsing.rs/formatter.rs touched — fmt-40: `http` prints between the client name and `client = ..`). Six NEW ledger rows; markdown anchors +4; gates green (natdiff both, inference 4374 in three partitions, census unchanged). FIND: a spurious "provided by an impl … that main does not export" error on an `export impl Door` beside a plain `impl Peer` with `[rpc] fun ping` (pre-existing on 49de39157). Open Qs (recs): a generated `[rpc]` method cannot read the POST session (record on A120); `HttpTransport` cannot send a header credential → `over_http_with(mount, codec, headers)` next order; SSE legs follow the socket column; 403 rides the Unauthorized arm; the paper's S5 = kolt's rewrite (§9.7.10) still at the owner's word.
- MERGED editor-40 → next b3d01e63 (CHANGELOG parity 606/606; no ledger rows; markdown golden regenerated; gates green: cli, core 4637, lsp 859, ide 19, wasm, release_scripts, split), pushed. std-40 merging next (four corpus goldens moved → regen_goldens after; six NEW ledger rows).
- MERGE std-40 (2122a1c6; parity 616/616; ledger 552–557; markdown golden regenerated): build RED — a SEMANTIC conflict with editor-40: E213 widened the struct-field tuple to four elements and std-40's new `service_http_refusals` (macros.rs:3217) destructured three; folded by the integrator (one commit), gates re-running. LESSON: two lanes touching the same record shape merge textually clean and fail at build — the helper's build step is the tripwire, as designed.
- MERGE std-40, second red: `grammar_ebnf::every_word_the_grammar_quotes_is_one_the_compiler_knows` — S5 added `"http"` to the spec EBNF (service-arg) without the NON_KEYWORD_TERMINALS row; the lane's own gate list never named grammar_ebnf. Folded (18204dbd), gates re-running. RULE for briefs41: any lane that edits `vilan/docs/spec/grammar.md` gates `grammar_ebnf` + `grammar_sync`.

## solver-40 — TENTH to report (RESUMED; 80 min this session): 11 signed commits, all items + two add-ons. B377 f7a08dab (unused-`let` effect predicate exhaustive over Expr; side-effect-let golden moved, runtime-judged), B378 42323dda (specificity compares supertrait closures, analyzer + impl_select), B379 5c9a554c, B362 dispatched half c86db7e3 (one lazy convention per trait), B380 3078a259, subscript index add-on 8258b73c (one NEW ledger row), B372 62b52ff8 (PREMISE PARTLY FALSE: not B162 — the inner call bound `T` to Unknown and kept it; the argument now waits for the closure parameter's type; `fixpoint_stalled` gives one more round for let-bound closures; a silent WRONG-TYPE program (`i32` into a `str`) is now refused), B381 ecdcf9b3 (PREMISE FALSE — it was B379; four pins, two controls), B371 bf0c806e (`flatten`'s `I` came back as the caller's own generic and was rejected as unresolvable; fixed — the generic `switch` body no longer needs its result annotated), SUPERTRAIT-BLANKET OVERFLOW a6bf7c43 (gdb: provides_trait → subject_applies → provides_trait; a thread-local in-progress set answers a self-reaching question "no"; the std-free sibling's wrong refusal fixed — a blanket subject's bound supplies the supertrait's members; spec/types.md §5.5 states the rule; four pins abort with the guard out). A112 S3'S HELD SUPERTRAIT FORM NOW CHECKS AND BUILDS (tested on collections-40's std + the candidate patch with the supertrait line restored; `logged=true plain=false ops=1` at runtime) → S3 lands THIS order after solver-40 + collections-40 merge (a new branch off merged next). Gates green (core 4638, cli 78, natdiff both, clippy core). FINDS: a let-bound closure whose unannotated parameter feeds a generic position is refused "type of variable f could not be resolved" (pre-existing); the missing-member sentence for a supertrait reached with a constructed argument shows the wrong argument. Merge notes: impl_select.rs + spec/types.md outside its map; re-run the reactive goldens after (B371 changes what blanket calls from generic bodies record); pins named `switch_to`/`and_then_to` to avoid A123's names.
- MERGED std-40 → next 18204dbd (two folds: E213's four-tuple; the `http` EBNF terminal; ledger 552–557; gates green), pushed. solver-40 merging next; `regen_goldens.sh` after it (B373 moved four, B377 one, B371 may move the reactive goldens).
- MERGE solver-40 (parity 626/626; ledger 558 = the subscript refusal): two test files conflicted in the append-at-end shape (std_surface.rs: std-40's B373/N117 vs the subscript pins; tuples.rs: hygiene-40's N113 vs B377) — folded with `fold_tests_by_name.py` (6 + 8 lane fns appended whole), gates re-running.

## native-b-40 — ELEVENTH to report (RESUMED; 81 min this session): seven signed commits, F26 last and KEPT (the half-work had one real bug: the shared `resolve_inherited_default` passed `None` for impl admission on BOTH backends — JS used to pass `current_admitting_file` (B318 S4); admission is now a parameter, JS passes its file, native `None`; the estate cannot see that change — nothing restricts an import — so it is UNPINNED: rec a B354-style multi-file pin). F30 2fce56b4 (module-level mut aggregate mutated in its cell; 7 spellings; args of a mutating call hoisted), F29 ed60ff90 (census walks refused args/closures/enum-payload views; kolt server.vl: 69 gaps / 50 rows vs 53 / 34 before = 16 bindings low; Order 38's "55" was a different measure), F31 beb51d83 (native liveness: move at last use unless deeper in a loop/closure/spawn/comprehension; 111.7 M → 56.5 M Ir on a 200k loop; `native-copy-census.tsv`), 940e5b97 (papers-40's F-b: a literal assigned to a narrow binding takes its width; 23 rustc errors without it), F21 c6192fb2 (option-view.vl flips; arena.vl + transparent-references.vl still refused by name; phantom-parameter struct literal grounded from the return type), F22 f48979cf (monomorphise on asyncness; adapt.vl identical; COVERS native-a-40's `turn(.., || protocol.respond(..))` blocker — two probes print the same on both backends), F26 e03d7e98 (`vilan_core::mono`: six shared fns; `type_key`/substitution resolvers left per-emitter with reasons; 384/384 emits byte-identical before/after; a planted drift IS detected). Whole set 119 → 70 / 49 / 0 / 0 / 0 on ITS base. Gates green (natdiff both 25/25, clippy workspace, fmt). FINDS: JS `inherited_substitution` may not bind a callee's generic parameters its signature never mentions (native does) — probe item; admission unpinned. Open: F26's rename `vilan-embedded-std` → `vilan-embedded` NOT done (22 files across cli/lsp/wasm/release) — Order 41 item; F26 stays partial. Overlap with native-a-40 in vilan-rust/src/lib.rs + native_differential.rs — the integrator rebases.
- MERGED solver-40 → next c132cafb (two by-name folds; ledger 558; gates green: core, cli, lsp 859, release_scripts, split), pushed. collections-40 merging next; `regen_goldens.sh` after it.

## dyn-40 — TWELFTH and last to report (RESUMED; 100 min this session): five signed commits. 20949031 (`dyn Trait` is a type: keyword/grammar/formatter/editor; object-safety check naming the member; explicit one-way coercion; resource refusal; JS pair `[value, table]`; native refused by name; 24 pins), 9e6d2b34 (BREAKING: bare trait at a struct FIELD refused with the `dyn Trait` steer; `resolve_hidden_struct_parameters` RETIRED, 191 lines; B184's pins replaced by five), 3d414903 (EIGHT HOLES fixed, each a program that ran wrong: blankets did not reach the object; a generic over the bound refused as ambiguous with two impls; an erased value's OTHER members leaked then crashed; a foreign bound accepted; copies ALIASED (`2 2` for `2 1`); an async impl under a sync declaration printed a Promise — now refused per trait-objects §5(i); an async declaration not awaited; a table-only body lost module state; JS table MEASURED: slots on the table's prototype 1.4–2.9× a direct call vs 5.3–7.0× as a literal; a wrapper would be 1.1–1.8× but moves every golden — not taken), 89b5e3fe (native: `vilan_rt::Dyn<dyn ObjectX>`, a counted pointer + Rust's vtable, no unsafe; `&mut self`/async/context-threaded slots refused by name; new corpus `dyn-objects.vl` byte-compared both backends), f2076e89 (a call through a dyn passes the hidden context arguments). 34 `dyn_objects` pins; the `.cell()` pin `#[ignore]` until merge (needs next's reactive_pipeline.vl) — UN-IGNORE AT THE MERGE. Census 349 → 361. Six NEW ledger rows + one reworded. ESTATE: vilan-playground `todo/src/store.vl:14` `[expose] notes: Signal<List<Note>>` → `dyn Signal<..>` (runs, pinned); kolt main tree NONE; kolt's worktree `kolt/worktrees/connection-v3/vilan/common/src/lib.vl:41–42` two fields (the owner's); std/website none. Gates green (inference 4381, natdiff both — 120 enumerated / 70 / 50 / 0; lsp 842; clippy). FINDS (native): `SignalCell::new(1).map(..)` refused "unbound generic type parameter (parameter 1 of map)"; `flatten` refused on a `?` lift. OPEN (recs): `map<U>` through a dyn stays refused until A124 S2 makes it a blanket (a default over Self = dyn would bypass overrides); table carries FOUR slots (get/on_change/sub/effect — defaults reached through a bound become slots so `SignalCell::sub`'s override still runs) — rec accept; keep the prototype-table pair; RATIFY §5(i) (async impl under a sync declaration refused at the coercion). Outside its map: async_infer/call_graph/impl_select/dispatch_refine.rs (a dyn call counts as a dispatch), interpreter.rs (`Vtable`), formatter.rs, vilan-ide completion.rs, vilan-lsp document.rs. Expect code conflicts in analyzer.rs, transformer.rs, vilan-rust lib.rs, vilan-rt lib.rs, DEFAULT_SUITE.
- MERGED collections-40 → next 985dfaf3 (parity 628/628; markdown golden regenerated; gates green: cli, core, lsp 859, release_scripts, split), pushed. Seven lanes on next. Next: regen_goldens over the merged tree, then native-a-40, native-b-40 (rebase), dyn-40 (BREAKING; expect code folds), fmt-40 LAST, then the S3 lane off merged next.
- regen_goldens over 985dfaf3: 0 corpus goldens moved. native-a-40 merging (whole-set native differential in the gates via `VILAN_NATIVE_DIFFERENTIAL=1` exported around the helper).
- MERGED native-a-40 → next 128e7b24 (parity 633/633; ledger 559 = F32's refusal; markdown golden regenerated; gates green incl. the whole-set native differential, rt/rust/sqlite/embedded, core 4459, lsp 859), pushed. native-b-40 merging (direct merge first; code folds in vilan-rust lib.rs / native_differential.rs expected).
- native-b-40 REBASED by its resumed agent onto 128e7b24 → tip 9f2e317f (seven conflicts folded keeping both lanes: the declared type still passed for a borrowed argument + F30's writable place; native-a's void-position async closure + F29's census walk (also into `NativeDispatch::Host` args); one `current_return_type` field kept; both DEFAULT_SUITE row sets; F22's rule applied before native-a's; the native copy census regenerated for native-a's four programs). Gates on the tip: natdiff 28/28 both modes — WHOLE SET 120 / 82 identical / 38 refused / 0 broken; 33 goldens; 68 rt/rust/sqlite; core 4449; clippy/fmt clean; F26 re-checked 386/386 byte-identical. NOTE: F29's CHANGELOG figures (53 → 69) were the old base; on the rebased tip kolt's server.vl census reads 19 gaps / 15 host bindings (native-a made json + the server surface native) — the 69 stays as the comparison against Order 39's 55; add the new figure at the sweep. Merging.
- MERGED native-b-40 → next cf965730 (clean after the rebase; parity 640/640; gates green incl. the whole native set, lsp 859, release_scripts, split), pushed. Ten lanes on next. dyn-40 merging (BREAKING field refusal; expect code conflicts).
- MERGE dyn-40: direct merge conflicts in impl_select.rs (2, B378 vs dyn dispatch), vilan-rust lib.rs (4), native_differential.rs (2, DEFAULT_SUITE), copy-elision-census.tsv (1) — ABORTED; dyn-40's agent resumed to rebase onto cf965730 (un-ignore the `.cell()` pin, regenerate the census, re-sweep the field refusal on the rebased tree).
