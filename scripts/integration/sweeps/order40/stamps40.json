{
 "prefix": "- 2026-09-22 (Order 40 sweep): ",
 "stamps": {
  "A124": "PAPER LANDED (reactive-40, proposals f9adece/5addd38/6d106e7): `proposal/reactive-pipeline.md` (806 lines) — the five parts with the node structs in vilan, the push-pull protocol re-derived against door 2, `Resource<T>` picked (pending → settled → pending again; `.or` RESETS, `.latest(default)` holds; `failed` a third state), the `dyn` delta (two vtable slots, no drop slot), kolt's classification (69 % single-reader, 15 % shared, 15 % stored; only 3 of 19 fields need `dyn`; SIX hand-rolled memo tables = `.cell()` in the wild), the R4 migration, a cost table; S1 PROBE c0683da5 `vilan/std/src/reactive_pipeline.vl` (imported by nothing; `Cold<T>` beside `Source`, `Map`/`Switch`/`Combine`, `.cell()`, a `watch` leaf; four pins; MEASURED: a five-deep chain 0.41× to construct, 0.23× per pulled frame, 0.29× per pushed frame of five `map` cells, callgrind Ir). Paper's OPEN Q1–Q5 (§9, recs): Q1 keep `SignalCell` as the root's name, not `Initial` (written over its whole life; the rename moves every annotation); Q2 `.or` resets; Q3 `.cell()` does NOT compare — a separate `.distinct()` carries `T: PartialEq`; Q4 `on_settle` public as the node author's requirement (it CANNOT be a blanket — triples the work); Q5 the combinator flip (S2c) waits one order behind I5 (two breaking trains in one cut cannot be bisected). Also: R2's one-id-per-leaf-chain needs `observe` to TAKE an id (S2a, §5). dyn-40 (this order) built R3 — see B4's reopening note and its four open Qs: `map<U>` through a `dyn` stays refused until S2 makes it a blanket; the table carries FOUR slots (get/on_change/sub/effect — defaults reached through a bound become slots so overrides run) rec accept; the JS pair-with-prototype-table kept; §5(i) (an async impl under a sync declaration refused at the coercion) wants RATIFYING. NEXT: S2 the surface, after the owner rules Q1–Q5 + the dyn four.",
  "A112": "S2 BUILT (collections-40 f917da4e): `ListCell`, the two sequence traits, `map_each`; corpus `list-cell.vl` + golden (census 351 → 393); a 300-turn random walk with a second `map_each` chained; 1,081 + 1,081 calls where a naive rerun makes 4,196; four plants red; positions `i32` with `// I5` markers. S3 STOPPED at the lane on a THIRD gap — B387 (a supertrait-blanket stack overflow, pre-existing) — then FIXED by solver-40 a6bf7c43 in the same order: the held supertrait form now checks and builds (`logged=true plain=false ops=1`). The held patch was also incomplete (nothing answered `Some`; needs the forwarding blanket `impl type S: DeltaSource<List<type T>, SeqOp<T>> with DeltaFeed<T>`, which B378 enables) and predates A125 (`+ Hashable`). Evidence + repros at sweeps/order40/probes/a112-s3/. S3 then LANDED by the s3-40 lane off the merged tree — ON THE TWO-BOUND SPELLING (`S: Source<List<T>> + DeltaFeed<T>`) as the DECLARED fallback: the ruled supertrait form first proved UNSOUND on 3e4e6c51 (B394, fixed by solver-40b the same day) and then, sound, BROKE existing programs (B395: a supertrait's default member on a type implementing it at other arguments is resolved through the subtrait's blanket) — B395 is Order 41's first solver item and the declaration switches to the ruled spelling in one std commit after it. FIND on the way: M86 (a `ListCell` write still deep-copies the list). S3 as landed (s3-40 6c4a8065 + fada6489): `place_each` applies Splice/SetAt directly, Reset and Move fall back to the pass; 1,000 rows `built=1 keys=1`; the 300-turn walk; M86 measured (6.30 M Ir per push, 6.28 M of it the ListCell WRITE's copies, 20 K the op path). NOT wired: `each_by` (still the full pass) — S3b. The ruled-shape switch is a saved patch (sweeps/order40/probes/a112-s3/s3-supertrait-switch.patch).",
  "F18": "SLICE 2 as reached (native-a-40 735a0813/a0ac30dc/0e816010/54d42be9/b9f8d3ab): `std::json` natively (`vilan_rt::json`, nine seams, three emitter gaps), the host seams (Math, number labels, charCodeAt, process.env, fs, SHA-256; a void closure that awaits FLOATS), `std::db` through the SEPARATE `vilan-rt-sqlite` crate (rusqlite 0.37 bundled; vilan-rt still dependency-free; linked only when a program reaches `std::db` — pinned), and THE EXIT NARROWER THAN BRIEFED: a login server with kolt's SHAPE built from the `Server` struct literal answers three exchanges over a real socket byte-for-byte with node (`the_kolt_server_shape_answers_a_login_over_a_real_socket`, red with a planted wrong column reader). `Server::builder()` does NOT build natively — so the rpc server natively and the keyed rpc pins against it did not land. Blockers: F22 (native-b-40 f48979cf — now COVERS the `turn(.., || protocol.respond(..))` shape), the mutable `Bytes` RULING (F33), two seams (`now_millis`, SHA-1 `digest`). Whole set after native-a: 119 → 82 identical / 37 refused / 0 / 0 / 0 (native-b adds option-view; the merged number at the seal). SLICE 3 = F33 + the seams + the builder + the rpc server + kolt's `server.vl` as written — Order 41.",
  "F26": "PARTIAL (native-b-40 e03d7e98): `vilan_core::mono` shares `collect_type_generics`, `signature_generics`, `function_has_body`, `trait_default_member`, `resolve_inherited_default`, `trait_parameter_substitution` (the last two take impl ADMISSION as a parameter — the inherited half-work had passed `None` on BOTH backends, silently changing JS; JS passes its file again, native `None`; unpinned → N124); `RENDER_MEMBER` + `unescape_string` now `pub`, the backend's copies deleted; 384/384 emits byte-identical before/after on both backends and a planted drift IS detected. Left per-emitter with reasons in the module header: `type_key`, the active-substitution resolvers, `inherited_substitution`, `call_substitution` (they DIFFER — B393 probes whether JS is wrong). The `vilan-embedded-std` → `vilan-embedded` rename NOT done → N125. Stays open on those.",
  "I5": "PAPER LANDED (papers-40, proposals 4cde759): `proposal/index-type.md` (785 lines): by parse, 105 index-kind positions (86 signatures + 19 fields), ALL `i32`; `i53` = fs offsets (a STREAM kind, stays); three boundaries the item did not name (WIRE 61, STREAM 10, SEQUENCE 5); estate 251 explicit namings in 56 files, 207 inside Rust-test consts, 0 on website/playground, 3 in kolt; 7 `-1` sentinels; 8 downward loops that break. WIRE PROOF: a naive `u53` moves a `Patch` frame 104 → 108 bytes; `usize` writing via `i32_value` is byte-identical — the rule is load-bearing because the contract hash does NOT see a frame-width change (a user rpc parameter respelled DOES move it — ruling 5 holds with that sentence). Emission: an `i32` loop and a `u53` loop compile byte-identically, so corpus goldens measure S2. FIVE literal-inference positions B370 did not close → B389 (S1 depends on it). Underflow: JS prints -1 and runs on, native panics in debug — the differential cannot cover such a program; `__at`'s `index >= 0` guard is what makes 'never memory-unsafe' true. Q1–Q5 with recs (§11): file offsets stay `i53`; a `usize` in a contract surface lets the hash move; `usize::max_value()` = the JS guarantee on every backend; the native refusal wording (N120); a `42usize` suffix yes. Rec S1 and S2 in SEPARATE orders. Also from the census: A126 (no `Display` on the sized family), E218 (the missing `as_*` steer), B388 (native RHS width — fixed by native-b-40).",
  "B382": "PREMISE TWO-THIRDS FALSE (papers-40, `sweeps/order40/papers-40/b382-type-alias.md`, probes/b382_aliases.sh): `export import pkg::inner::DeltaCursor as KeyedCursor;` COMPILES, runs, is transparent (a `KeyedCursor` value satisfies a `DeltaCursor` parameter) and works over a generic struct, a trait, an enum with variant paths, a function and an `impl` on the alias — so A112's ruled deprecated re-export WAS expressible and the hard rename was avoidable. Genuinely missing: `type X = Y;` (no customer) and `[deprecated]` on a TYPE (admitted only on a function). REC: build `[deprecated]` on a type; leave the alias open with no customer. Re-titled by this stamp; the next lane must not build aliasing the language already has.",
  "E214": "STOPPED by editor-40, PREMISE FALSE — `autoClosingPairs.notIn` takes only string/comment/regex and decides on the token LEFT of the caret (an identifier for `List<`, whitespace for `a < b` — both `Other`), so no grammar scope can separate them (the grammar ALREADY scopes them apart: `punctuation.definition.generic.begin.vilan` vs `keyword.operator.vilan`); editor-39 had pinned exactly this (`e202_the_angle_pair_surrounds_but_does_not_auto_close`). The priced alternative is E222 (a client-side `type` override behind a setting, with the server standing down for `<` when the client declares the capability) — a RULING.",
  "F27": "R6 + R2 BUILT (editor-40 f40ab8ba + 90bd779a): PREMISE PARTLY FALSE — E119 already printed two of R6's three facts; the missing third is that the OTHER twin declares the member: the E119 note now ends 'The `browser` twin of `std::ui` declares `anchor` — an entry of that platform (or `--platform`) is what puts this file under it' (read off the twin file, cached), and the same note rides `MethodLookup::NoMethod`; R2: `infer_platform` weighs the MEMBERS a file uses against what each twin declares when no import settles it, and an inferred platform carries its own reason clause; six pins. NOTE: R2 cannot fix the owner's own case — a package file no entry reaches is coloured by `default-entry` (E113), which OUTRANKS inference (three shipped pins depend on the fallback); R6 answers there and R1 (module-level `[platform(..)]`) is the real fix for the annotated case. Kolt no longer has `lib/conditional_value.vl` (the shape reproduced in a scratch package). STILL OPEN: R1 (module-level/impl/nominal `[platform(..)]` as the analysis platform + the function fence's resolution meaning), R3 (platform-fenced twin items), the parity gate learning fields."
 }
}