# Order 31 — running record (integrator)

Opened 2026-09-09 off c3ed9239. Lanes: handles-31 (TOP), reverse-31, mutself-31, keyed-31 (droppable), rpc-smalls-31. Briefs: briefs31.md. Ledger next id 407.
- PROCESS (GO): the three-item filing ran with an unescaped pipe in B281 (INVALID, exit 1), but the commit after it still ran — the `&&` chain was broken by a heredoc terminator (the line after `EOF` starts a new command). Rule: a heredoc ends a chain; put heredocs in their own call or first. Filed at 94b9903 after the escape.

## rpc-smalls-31 — all three landed (a092d246 A56(i) the keyed-map mismatch refusal — span on the attribute argument, `Exposure::Keyed` now carries `Option<Spanned<&str>>`, four generated `Keyed<i32>` bound failures stand down behind it via `expose_refused_key_bounds`; f03ee14d A56(ii) 429 kept — the rationale was ALREADY at rpc_server.vl:639–647 (A52's lane), the lane added the ruling record + a four-arm `Reject` status table in guide/services.md; eeca2e8d the §9 guide sweep — eight edits incl. `RpcError::Unavailable` reaching the docs for the first time), merged FIRST (landed first). Ledger: row 407 NEW (contested — renumber at each merge). Pins in `crates/vilan-core/tests/inference/traits.rs` (three a56_* fns).
- Brief corrections: (1) the 429 rationale existed at the site; (2) attribute refusals pin in vilan-core `inference`, wire behaviour in vilan-cli `service_layer` — ADD to the mechanics block for future orders.
- FINDS → items: (F-A) `RpcError::Unavailable` (A52) reached no reader-facing page — `--test docs` compiles fragments, so a stale enum LISTING still compiles → candidate: an enum-arm parity check for doc fragments (the `style_table_sync` idiom). (F-B) the pin-location rule above (mechanics, not an item).
- Open for the integrator: guide/services.md's Traps bullet (hand-wired channels) is written to survive either reading of "DYNAMIC" — re-read after handles-31 merges (if handles-31 marks EVERY reply-time export dynamic, a hand-wired channel's Unsubscribe is a revoke too and the bullet should say so); `appendix/glossary.md`'s "mirror" entry becomes incomplete once handles-31 lands (that lane's).
- MERGED 09d8e78a (rpc-smalls-31, first): no conflicts; ledger row 407 = A56 mismatch refusal; gates green (cli 113, core 3794 incl. inference/docs/markdown_golden, std full-scan, lsp 678, parity 22, split 12). PROCESS: the validator refused `--test inference` (a `tests/inference/main.rs` multi-file target) — merge_lane now accepts the `tests/<name>/main.rs` layout; one ssh timeout to GitHub on the proposals push, retried clean.

## mutself-31 — both landed (4a9cb0ec B272 fix, 71570b77 `mut self` refusal), merge second
- Shape: NOT a `Shared<S>` cell — the generated `dispatcher()` declares `mut self` and the routes capture the BINDING (spec §6.9); one binding per connection under `Service::factory`. The cell was built, measured and rejected: a copy per connection + a prelude import, C9's resource pin regressed (`Shared` cannot hold the resource, no code frame), and a reflection field it forced. Both route call sites moved (arity 0 and the decode-gated arm). Pins: `a_mut_ref_self_rpc_write_survives_the_next_call_on_that_connection_alone` (the sketch over two live connections, `Shared<bool>` control), `an_rpc_method_taking_mut_self_is_refused_at_the_attribute_naming_the_method` (must-fail); the old lost-write pin DELETED (allowed by the brief). Ledger NEW row. Docs: "Mutable session state" in guide/services.md (+1 anchor). Side fix: `construct_function_item` writes all five `meta::Field` slots (`keyed`/`key` were `undefined` in the macro world).
- Brief corrections: routes are 3184–3193 (two call sites); nothing in `vilan/test/` uses `[service]` so `--test corpus` cannot catch a generator change — `--test examples` + `--test docs` do; add `--test diagnostics_ledger` to every NEW-row lane's gate list (mechanics).
- FINDS → items: (1) an `[rpc]` parameter named `request` shadows the route closure's `request` → "Expected RpcRequest, but got str" on the struct — gensym the closure parameter (touches `route_block`, three lanes' territory; B item). (2) `Shared::read().bump(5)` writes through (a `&mut self` call on a value return) = B274's shape, the owner's ruling pending — no new item. (3) `analyzer::service_impl_source`, the Rust fallback generator, is a stale twin (no `turn`, no keyed, still `fun dispatcher(self)`); only fixture stds without rpc.vl reach it — tombstone-or-sync N item.
- OPEN Qs (owner): once B281 lets one connection's handlers interleave, two routes share the one `self` binding and an `async fun f(&mut self)` that awaits can be mutated underneath — Q9's "a `&mut self` method does not await" promise becomes load-bearing and NOTHING enforces it (an async `&mut self` rpc compiles today and its write persists). Enforce or document? → sent to reverse-31 as a guide sentence; the enforcement is the owner's. `[client_service]` needs the same `mut self` guard → sent to reverse-31.
- Q9 amendment text in the lane's report (apply at the sweep).
- PROCESS: no SendMessage tool in this build — a running lane cannot be steered; the two reverse-31 items become MERGE-TIME CHECKS: (a) the `[client_service]` expansion refuses `mut self` like `[service]` (same arm or the same guard); (b) the guide says a `&mut self` handler that awaits may be interleaved with another handler on the same connection once B281 lands (Q9's promise, unenforced). Missing → a small follow-up lane before the seal.

## reverse-31 — all landed (3267426c A75 feature; 83d8cf53 the two refusals; 83af3f52 B281 the receive loop, red-first: reverted answers `Success:-1` after 400 retries, fixed on the first; f6439d33 docs; e005e406 grammar sync for the new `service-item` production; 809971f1 `vilan fmt` over rpc.vl; 80cc5fd4 the 0x73 binary lane pinned), merge after mutself-31
- Shapes: `Node::Service` carries `ServiceAttr { client_name, handler_name, server_side, client_side }`; `ServiceItem` (meta.vl) +3 fields at the END; the typed proxy is a std TRAIT (`ClientProxy::for_connection(connection)`) reached by `Connection::client<P: ClientProxy>()` — P resolves from the field's declared type (rejected: a generated `Connection` member — two services' handler types collide; a typed factory parameter — changes `Service::factory` for every service); the handler instance via `.with_handlers(instance)` on the CONNECTED client (connect verifies the contract first → "refused before any `s:` frame" holds by construction) with an `early_serve` queue for a server notifying at `on_connect`; `s:` id 0 on the wire (4 bytes reserved for v2); a per-connection `client_channels` registry in std::rpc, `DuplexEnd` UNTOUCHED (paper §9.3/§9.4 correction). Void `[rpc]` methods legal ONLY on a `client_side` struct. Hashes: plain d1d5fba0 byte-identical to c3ed9239's; with `client =` c6c3956e; server == client on a live connection. Pins: 5 in service_layer.rs + 1 vilan-core lib + the refusals + the receive-loop + the binary lane.
- Deliberate conflicts (rpc.vl): route statements now `route_outcome(returns_void, i"self.{method.name}(…)")` (mutself's `mut self` receiver drops in — the substring is verbatim); the stub's `.returns(...)` is inside an `if render()=="void" / else` — handles-31's mapped return goes in the `else` arm; the surface builder's `client:` loop is a suffix after `expose:`/`keyed:`.
- FINDS → items: (1) `[expose]` on a `[client_service]`-only struct is a silent no-op (compiles, hashes, never mints) — refuse at the attribute (B). (2) the SSE/`SplitDuplex` leg has no reverse lane — `self.client.ping()` on an SSE connection drops silently (A/B). (3) a peer struct's browser-side instance has no honest proxy value (`for_connection(-1)` sentinel) — `ClientProxy::detached()` or an `Option` field (A). (4) `notify` on a closed connection is silent by design — a v2 sentence.
- OPEN Qs (owner): (1) `client = H` requires H in the SAME module (module-wide reflection; fits kolt) — accept for v1? (2) the forward stub of a void method is sync void (drops the reply); want `Result<bool, RpcError>` for an awaitable ack? (3) a `[client_service]`-only struct still generates `__contract` — keep (symmetry) or drop?
- Paper corrections (§9.3/§9.4): no pending table / `fresh_id` / `DuplexEnd` extension shipped or needed under R4 (row → "unchanged"); the proxy method is sync void (the async-coloring paragraph is v2's); the `s:` id bytes exist and are reserved; `connection.client()` is generic over a std trait; the `client:` entries are a SUFFIX (what makes byte-identical mechanical).
- Merge-time checks (from mutself-31): `[client_service]` shares the `Node::Service` arm → the `mut self` guard should apply after the merge — PROBE it; the interleave caveat sentence for `&mut self` handlers that await — ADD at integration if absent.
- MERGED f53d999c (mutself-31, second): CHANGELOG + ledger resolved mechanically; ledger row 408 = the `mut self` refusal; gates green (cli 90, core 3782 incl. inference, std full-scan, lsp 678, parity 22, split 12).
