{
  "date": "2026-09-22",
  "order": "Order 39",
  "closes": [
    {"id": "A110", "lane": "reactive-39", "commit": "e59e1413", "text": "door 2 — effect order within one settle is a CONTRACT: derivations bucketed and drained to a fixpoint, then effects ascending by subscriber id (an ordered insert in `enqueue`, no sort); the class decided by `observe` from a bare `as_derivation()` mark it SPENDS; two nesting pins red under door 1's routing planted back; +23.5 % Ir per wave against 2.2× for partition-and-sort; nine goldens runtime-identical; kolt's page_content latch retired in full; door 1 landed Order 38"},
    {"id": "A113", "lane": "reactive-39", "commit": "381c5567", "text": "every row MEASURED by subscriber count — owned forms 0 after dispose; a dropped `sub` handle stays subscribed; four combinators inside a boundary 0; `map` OUTSIDE every boundary stays 1 (pinned as the contract); a plain effect body accumulates 3 where `scoped_effect` holds 1; guide 'Who cleans up what'"},
    {"id": "A114", "lane": "reactive-39", "commit": "51efbeb0", "text": "`Source::scoped_effect` / `scoped_effect_on_change` over a private `scoped_runner`, an Owner per run released before the next; free `on_cleanup` (strict — no ambient owner is a compile error); the forms NOT rebased on it (R2); three pins incl. a throwing body and a mirror lease released at the run's end"},
    {"id": "A115", "lane": "std-39", "commit": "5a17769e", "text": "`impl Option<str> with AttrValue` + `impl type S: Source<Option<str>> with AttrValue`, both twins; `None` writes nothing / removes; three pins"},
    {"id": "A116", "lane": "std-39", "commit": "3ccbf2da", "text": "`__json_tag` answers `\"\"` for a non-string/non-object (null/number/boolean/array/`{}`), the interpreter twin in step; six pins (the `null` JS pin the true differential)"},
    {"id": "A117", "lane": "std-39", "commit": "bf2e4ad7", "text": "BREAKING (empty in practice): `std::style::prelude` exports 67 → 46 — the 21 alias spellings moved into the one body each is for via FUNCTION-SCOPED imports (`#` is B318's reach marker, not a hide); four pins; door-2 re-census: what collides is words — A106 stays open"},
    {"id": "A118", "lane": "std-39", "commit": "60794a42", "text": "BREAKING (R7 — the doc wins): `random::range` is `[low, high)`; three estate sites lose their upper value (file.vl, watch.vl, arena's brand draw); four pins, 400 draws each"},
    {"id": "A119", "lane": "std-39", "commit": "80ea4159", "text": "`when_some` on `place_swap`'s machinery, both twins, in `std::web`'s prelude — presence decides structure, `Some → Some` writes the row's `SignalCell<T>` and the row STANDS (node identity pinned); the HMR premise did NOT reproduce (two real watch rounds, row live); second commit 2a1d7ff4 names the server twin's body for `std_twin_parity`; kolt's conditional_value.vl deleted"},
    {"id": "B360", "lane": "solver-39", "commit": "ab4ee1af", "text": "R4 — `check_unlowered_externals` post-pass; census by the compiler: 647 `external fun`, 154 with no `[extern]`, every one a compiler lowering; 0 estate sites fire"},
    {"id": "B361", "lane": "solver-39", "commit": "c69fb566", "text": "BREAKING — `Self<i32>` was NOT coherent (nonsense diagnostics in both positions); one parser rule; one estate site (a `.vl` const in bounds.rs)"},
    {"id": "B363", "lane": "void-result-39", "commit": "0976fd19", "text": "PREMISE FALSE and corrected everywhere: `void` IS the unit value (`Ok(void)` builds and runs); only the `()` spelling fails; S3 ruled and built — A107's `rpc::call_ack` answers `Result<void, RpcError>`, generator `.returns(\"Result<void, RpcError>\")`, docs/pins/CHANGELOG corrected, kolt's two lines migrated; the `()` type + EBNF + shadowing holes filed as N113"},
    {"id": "B365", "lane": "solver-39", "commit": "9eaf5320", "text": "repaired via `struct_initializer_to_def`, counter wired; the arm is UNREACHABLE from a clean analysis (seven shapes, `literal_evidence()` = 0 — the type check refuses first) → the warm-path question is B384"},
    {"id": "B366", "lane": "hygiene-39", "commit": "b6c62f2e", "text": "PREMISE WRONG — there is no second spelling: 44 declared parameters, 131 `Generic(..)` nodes, 0 bare constraint ids over the loaded world; the invariant stated at `Type::Generic` and `impl_select::bind_subject` with a gate (`nominal_generic_spelling` ×2); native-a-39's 4-line dead branch deleted in ee9edd4e"},
    {"id": "B368", "lane": "solver-39", "commit": "21b24d00", "text": "R5 — `parse_for` shares `let`'s `parse_binder`; `Node::ForIn` carries `Box<Spanned<Pattern>>` (the inline form broke `node_size`'s 96-byte ceiling — caught); lowers to a hidden element + one `Destructure` prelude; EBNF (+ the array binder form), formatter, ten pins; native inherits `Expr::Destructure`"},
    {"id": "B369", "lane": "solver-39", "commit": "99d83953", "text": "PREMISE WRONG — the inferred return type keeps `C`; the CALLER handed an inferred one through unsubstituted (`infer_type_path` before substitution): one `callee_return_type` + one `substitute_type`; six pins incl. kolt's exact shape; kolt's workaround file deleted with A119"},
    {"id": "B370", "lane": "solver-39", "commit": "d0911b74", "text": "MISCOMPILE (the owner's `rem(4 / 16)` → 0): the emission verdicts (`integer_division`, `bitwise_u32`) were recorded from the left operand re-inferred at `Type::Unknown` = the default width; now taken from the typed position; a second one (`1 << 31` as u32) with it; kolt's 77 `Nf / M` sites dropped the suffix and the emitted JS constant-folds"},
    {"id": "E202", "lane": "editor-39", "commit": "e3c63ef9", "text": "R9 — vilan-lsp's first `onTypeFormatting` provider (`<` alone, the retained program decides: a type/trait/generic-fun name or a declaration head before it); `<>` in `surroundingPairs`; `autoCloseBefore` declared; 18 pins; the overtype VS Code cannot give a server edit is E214"},
    {"id": "E203", "lane": "editor-39", "commit": "699b891d", "text": "the backtick auto-closes globally with `notIn: [\"string\"]` (it means nothing to the lexer — a grep pin holds that premise); joins `surroundingPairs`"},
    {"id": "E204", "lane": "editor-39", "commit": "cad84bdb", "text": "census MEASURED then fixed: field-read hover dropped the doc, field-declaration and initializer-key hover answered the whole struct, member completion had no detail — four consumers agree; seven pins; Q2 (the struct block shows each field's first paragraph) built as recommended: no"},
    {"id": "E205", "lane": "fmt-39", "commit": "066dfabb", "text": "R8 — `[fmt] wrap_comments` (default OFF) → `FormatOptions` → `reprint_with`; `formatter/comment_reflow.rs` (703 lines); ten never-reflow classes each with a byte-identity pin (incl. section banners the brief omitted); the words-in-order net pinned with a planted broken filler; the two counts: std 6,719 → 5,573 comment lines / kolt 235 → 198, 0 declines, 0 words changed; the width ruling is E215, the editor half E216"},
    {"id": "E206", "lane": "editor-39", "commit": "ea9a7ee1", "text": "`Program::call_signature_labels` (the signature each generic call site solved, off `method_call_substitution`); hover renders declaration and computed lines in one fenced block; seven pins; there is NO signature help in this server and the playground has no hover (the book pins both)"},
    {"id": "E207", "lane": "editor-39", "commit": "f99330c2", "text": "ROOT CAUSE — a per-parameter append of the context clause; deleted, planted red; six counting pins; the existing E9 pin was VACUOUS (`hover_at_cursor` strips every `|`) — repaired onto `hover_at_marker` (the trap is N114)"},
    {"id": "E208", "lane": "solver-39", "commit": "b2ae74c3", "text": "`written_variant_arguments` reads B356's bindings as a list; `Option<i32>::Some(\"y\") … unwrap_or(3)` reports ONCE; papers-39's variant-constructor `internal:` was literally the same fix; five pins"},
    {"id": "E209", "lane": "fmt-39", "commit": "9035aac9", "text": "MECHANISM corrected: the hole is that three of `normalize`'s canonicalizations are DELETIONS, so a reprint with a different raw stream normalizes equal; `verify_reprint` is two checks (tokens, then a re-read of the printer's own output); `DeclineReason::ReprintDoesNotParse`; five pins (two red with the check removed)"},
    {"id": "E210", "lane": "fmt-39", "commit": "80b948e9", "text": "the divergence bisect runs over the NORMALIZED streams and maps back via common prefix/suffix/first moved token, printing the source LINE — verified against the real N108 bug with its fix reverted (line 3 / line 9 where the old code said line 1); five pins"},
    {"id": "E211", "lane": "editor-39", "commit": "629171e7", "text": "`Completion` gains `filter_text` + `replace_span`; the server emits a `textEdit`; the playground carries both; six pins"},
    {"id": "E212", "lane": "editor-39", "commit": "788400d4", "text": "PREMISE PARTLY WRONG — B346's refusal existed and named both paths; it fired TEN times anchored inside std after a first diagnostic blaming user code; now asked ONCE at the registry build, attributed to the entry, leading (11 → 2 diagnostics); the silently-accepted `[derive(PartialOrd)]` behind the original report is B376"},
    {"id": "N109", "lane": "hygiene-39", "commit": "a8998ce8", "text": "`Intrinsic::ListNew`/`ListPush` rows; three `World` fields, two `Dispatch` variants, two `Transformer` fields and six arms deleted; corpus + native byte-identical; solver-38's `Dispatch::ListNew/ListPush` were UNREACHABLE (0 hits over 131 programs); `list_new_fn_id`/`list_push_fn_id` kept for vilan-rust"},
    {"id": "N110", "lane": "hygiene-39", "commit": "027fe10c", "text": "a `$`-prefixed identifier in a `// witness:` is a HOLE bound one-to-one in both directions; 37 witnesses in 10 programs, none rewritten; six-direction pin; retires the re-keying chore at every golden regeneration"},
    {"id": "N111", "lane": "hygiene-39", "commit": "b0cd9dce", "text": "4 of 5 held (site analyzer.rs:28561, three stale frame figures); `report_depth_stats()` on a parse-bound refusal; `write_run_script` — unlink then `create_new` — for all three temp-script writers: the real defect was SYMLINK-FOLLOWING in a shared temp dir plus a leak across an abnormal exit; four pins (the symlink one planted red)"},
    {"id": "N112", "lane": "hygiene-39", "commit": "839f50e4", "text": "DECIDED: keep B355's WARNING; `footnotes: Vec<Note>` not built; the rule 'a second fact that is not a second LOCATION for THIS error is its own diagnostic' stated at `error::Note` and B355's gate; promoted into diagnostics-standard.md C3 at the sweep"},
    {"id": "D9", "lane": "hygiene-39", "commit": "—", "text": "PREMISE STALE — all three paragraphs were already in editing-dx.md (proposals 8d4c73c, Order 37); re-verified claim by claim against c3f7d1a3; closed without work"},
    {"id": "M82", "lane": "collections-39", "commit": "de237ee1", "text": "`reconcile` keeps a per-canonical-key chain of old positions + `low`; the plan IDENTICAL for every `K`, no bound added; callgrind Ir 500/1,000/2,000 rows: 95.1 M / 355.1 M / 1,369.9 M → 14.8 M / 29.1 M / 58.5 M — per doubling 3.7–3.9× → 1.97–2.01×, QUADRATIC → LINEAR; the reorder case (`K: Hashable`) is A125"},
    {"id": "M83", "lane": "reactive-39", "commit": "—", "text": "MEASURED, DECLINED: door 2's two-map design removes the forcing function and per-id `remove` is WORSE on both tiers (Ir +9.8 %, thread CPU +22.1 %); reactive-38's JIT −35.9 % does not survive"},
    {"id": "F20", "lane": "native-a-39", "commit": "8f9b8038", "text": "`board.vl` FLIPPED — byte-identical on both backends; the whole set 49 → 67 identical / 68 → 50 refused by name / 0 differing / 0 rustc-refused / 0 broken; `vilan_rt::Hash` + a `Json` trait beside every `impl Js` + `Js` for Map/Set (ec1811bd — NINE general defects behind it incl. a live boxed-binding miscompile), `vilan_rt::Lazy<T>` (fcf62f2e), the four classes from native-b's note FIXED (ee9edd4e), overloaded operators (cb97f323), backed enums + `List::sort_by` (8f9b8038); PREMISE CORRECTION: five gaps behind the one wall named — the census runs on the exit program first (F29)"},
    {"id": "F23", "lane": "native-b-39", "commit": "TBD-after-rebase", "text": "`context.rs` records the hidden parameter's flavour where it mints it (`Program::context_optional_hidden_parameters`, `Plan::param_nodes` carries the bool); `compute_context_flavours` + two helpers DELETED (98 lines); mint order unchanged → corpus byte-identical"},
    {"id": "F24", "lane": "native-b-39", "commit": "TBD-after-rebase", "text": "`TaskId {index, generation}`; a free list at reap; the generation bumped as the slot is freed; three executor pins (200 rounds bounded; two stale-handle pins red without the generation check)"},
    {"id": "F25", "lane": "native-b-39", "commit": "TBD-after-rebase", "text": "`main` inside `vilan_rt::main_guard`: exit 1 with the message alone on stderr (was 101 + Rust's banner), sync and `async fun main`; printing a host handle or a function-holding value is a COMPILE-TIME refusal"}
  ]
}
