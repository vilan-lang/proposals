{
  "items": [
    {
      "id": "A123",
      "section": "A",
      "kind": "feature (std, two blanket methods)",
      "area": "Reactive core & UI",
      "status": "OPEN — filed 2026-09-21 (the owner's question, mid-Order 39); Order 40 candidate, S",
      "title": "NEW — `and_then` on `Source<Option<T>>` (Kleisli composition for a source of options: `\\|T\\| I: Source<Option<U>>` → `SignalCell<Option<U>>`) and `switch` on `Source<T>` (`\\|T\\| I: Source<U>` → `SignalCell<U>`, = `map(f).flatten()`) — the two DYNAMIC-DEPENDENCY combinators every kolt chain hand-rolls: channel.vl:41 is `message.map(\\|x\\| x.map(\\|x\\| x.author.user())).flatten().map(\\|x\\| x.flatten())` and model.vl:130's `map_safe` is `and_then` specialised to the client. PROBED: the generic `and_then` blanket impl compiles and runs correctly TODAY on 4f990aa2c; the generic `switch` hits B371",
      "discussion": "the owner (2026-09-21, 'are we missing some primitives for signals?'); A124 (the read-tracked derivation — the general answer; this is the immediate one); A86 (the two `flatten` blankets these compose); B300 (the nested-bound blanket, closed — the shape these ride on); B371 (the generic `switch` body's internal error); B372 (`Signal::new` in a closure's result does not fix `T`)",
      "body": "**The finding.** Every 'ugly' signal chain in kolt is a DYNAMIC dependency (which inner source to follow depends on the current value) spelled through STATIC combinators plus the join. `Source` has one dynamic primitive, `flatten`, in two shapes (A86), and the `Option` shape is `sequence`-then-join — `Source<Option<Source<U>>>` → `Source<Option<U>>` — not the join of the composite `Source∘Option` (that would take `Source<Option<Source<Option<U>>>>` straight to `Source<Option<U>>`). Which is why kolt needs the trailing `.map(|x| x.flatten())` every time: the inner source is itself a source of options. The owner is right that it is 'not really a flatten'; it is a legitimate op with an overpromising name, and a third `flatten` for the composite shape cannot be added — its subject `Source<Option<I: Source<Option<U>>>>` overlaps the existing `Source<Option<I: Source<U>>>` and there is no overloading to pick between them.\n\n**The two methods** (probed in the scratchpad on 0.40.0 @4f990aa2c):\n\n```vl\n/// Follow the source `f` answers for the current `Some`; `None` while the outer is `None`.\nexport impl type S: Source<Option<type T>> {\n\tfun and_then<U, I: Source<Option<U>>>(self, f: sync |T| I): SignalCell<Option<U>> {\n\t\tself.map(|x| x.map(f)).flatten().map(|x| x.flatten())\n\t}\n}\n/// A dynamic dependency with no option in it — Rx's `switchMap`.\nexport impl type S: Source<type T> {\n\tfun switch<U, I: Source<U>>(self, f: sync |T| I): SignalCell<U> {\n\t\tself.map(f).flatten()\n\t}\n}\n```\n\n`and_then` RUNS correctly today as a userland blanket (alice → `?` on a switch to an empty inner → `bob` when that inner fills → `?` on outer `None` → `alicia` on re-follow, five prints as expected). `switch` compiles to the internal error B371 in the generic body (`self.map(f).flatten()` at an abstract `I: Source<U>`), while the same expression at a concrete type with the result annotated runs — the std lane either lands B371 first or writes `switch` directly over `on_change` (which is what the total `flatten` does anyway, so the direct form is also the cheaper one: ONE derived cell, not two).\n\n**Why `and_then` is the right primitive and not a special case.** Signals are TOTAL — `get()` always answers — so the partial operations (filter, `.some()`) are the ones that cannot exist on a `Source<T>`: there is no value to hold while the predicate is false. `Option`/`Result` solve partiality by making absence a value, and `Source<Option<T>>` is exactly the total encoding of a partial signal — which is why kolt's model layer is `SignalCell<Option<X>>` throughout. `and_then` is the Kleisli composition of that encoding, the same reason `Option::and_then` exists; the owner's `.some().and_then(fetch)` is `and_then(fetch)` on the total encoding with nothing lost.\n\n**Kolt after.** channel.vl:41 → `let author = message.and_then(|m| m.author.user());`. model.vl:130's `map_safe` → `get_client().and_then(|client| client.get_message(id))` (the impl block on `SignalCell<Option<KoltClient<…>>>` keeps `get_safe` only). model.vl:110 `get_channels` likewise.\n\n**Doc work.** The `Option` `flatten`'s doc comment says what it is (`sequence` of `Option` over `Source`, then join) and points at `and_then` as the everyday spelling; the guide's derivations section gets 'static dependency: `map`/`combine`; dynamic: `switch`/`and_then`; `flatten` is what they are built on'.\n\n**Pins.** `and_then` over a source of options: switch of inner, inner update, outer `None` detaches (a later write to the abandoned inner does NOT reach the result), re-follow; `switch` the same without the option; both DERIVATIONS under A110 door 2 (an effect downstream sees the settled value in one wave — pin it with a diamond); the rolling inner subscription released at the ambient owner's disposal (A28's story, inherited from `flatten`); one derived cell per call in the direct form (count via `cell_identity`/`fresh_id` deltas, or the shared census if std's own use grows)."
    },
    {
      "id": "A124",
      "section": "A",
      "kind": "paper + ruling",
      "area": "Reactive core & UI",
      "status": "OPEN — filed 2026-09-21 (the owner's question, mid-Order 39); paper Order 40, a RULING before any code",
      "title": "PAPER — a READ-TRACKED derivation, `computed(\\|\\| body)`: one derived cell whose dependency set is re-recorded from the `get()`s its body makes on each run — the single primitive that subsumes `map`, `combine`, `flatten`, `switch`/`and_then` (channel.vl:41 becomes `computed(\\|\\| message.get()?.author.user().get())`). This is the state/tracking split the owner asks about, located: CELLS hold state; a computed holds a FUNCTION and a dependency set — not a lazy pipeline builder (the iterator analogy breaks on totality). Reverses `reactive-batching.md`'s 'vilan tracks explicitly' stance, which A110 door 2 already half-reversed by adopting Solid's compute→apply split",
      "discussion": "the owner (2026-09-21, 'should state and tracking be split apart?'); `reactive-batching.md` §'What we take from Solid 2.0' (the explicit-tracking stance and its table row 'N/A — explicit/structural tracking has no ambient scope to police'); `reactive-turns.md` §7 (A110 door 2: derivations to fixpoint before effects — the discipline auto-tracking needs, now built); A123 (the immediate fix, ships regardless); A112 (delta derivatives are NOT this — a computed recomputes whole values); A28 (owner-tied derivations); C14 (cell representation — a computed's cache is a cell); F1/native (the tracker is std code, ports)",
      "body": "**The owner's question, answered in two parts.** (1) Are we missing primitives? Yes, two, and they are the same missing thing: a DYNAMIC dependency spelled directly — A123 ships them as `switch`/`and_then`. (2) Should state and tracking be split? The intuition is right and the iterator analogy locates the split in the wrong place.\n\n**Why the lazy pipeline (`.mapper()…to_signal()`) is not it.** Three reasons, recorded so the alternative is not re-proposed without them: (a) TOTALITY — the pseudo-code's `.some()` (`map_filter`) has no value to hand `to_signal()` when the source is currently `None`; the example works only because `count` starts `Some(0)`. Iterators have no 'current value', so filter is free there; a signal's `get()` must answer, so a filtered signal needs either a default (`to_signal_or`) or the `Option` back (`SignalCell<Option<U>>`) — at which point it IS `Source<Option<T>>` and `and_then` is the composition (A123). (b) `and_then`/`switch` are NOT pure folds — the inner source changes and the result must re-subscribe — so a lazy pipeline containing one still holds a rolling subscription; fusion saves nothing there. Pure `map∘map` chains are the only thing it fuses, and those cost one cell each, no DOM, no effect. (c) It doubles the vocabulary (every `Source` method needs a `Mapper` twin kept in parity), which is the 'keep adding special methods' problem the owner wants out of, squared.\n\n**Where the split actually is.** In today's design a derivation is a `SignalCell` (state) PLUS a subscription whose dependency is STRUCTURAL — `map` depends on exactly its receiver, `combine` on exactly its tuple, and the only way to depend on something chosen at runtime is `flatten`'s rolling inner subscription. A read-tracked computed splits differently: the CELL is still the state; TRACKING moves out of the combinator's shape into the body's reads. `computed(|| message.get()?.author.user().get())` — one derived cell; on each run the body's `get()`s are recorded (an ambient 'currently computing' frame that `SignalCell::get` consults), the dependency set is diffed against the last run's, subscriptions added/dropped. `map` = a computed reading one source; `combine` = a computed reading a tuple; `flatten` = a computed reading `outer.get().get()`; `switch`/`and_then` = a computed with a `?`. One primitive, and every 'is there a combinator for…' question ends. This is Solid's `createMemo`, Preact's `computed`, Angular's `computed`, and the TC39 Signals proposal's `Computed` — every modern signals library converged here.\n\n**What we said before, and what changed.** `reactive-batching.md` chose explicit tracking to AVOID Solid's discipline (compute→apply split, `untrack`, tracking-scope rules) and get glitch-freedom from dedup-on-enqueue. A110 door 2 (Order 39, landed) then adopted the compute→apply split anyway — derivations to fixpoint, effects after, `as_derivation()` marks — because nested forms needed it. The discipline auto-tracking requires is therefore BUILT; what remains is the ambient frame and the per-`get` branch. The stance should be re-ruled with that on the table, not inherited.\n\n**Costs, honestly.** (i) One branch on every `SignalCell::get` (is a frame active?) — measure on the reactive benchmarks and kolt's `each` rows; Solid pays it everywhere and the JIT handles a monomorphic check well; the native backend pays a thread-local or a `Cell<Option<…>>` in vilan-rt. (ii) The classic footgun: a `get()` inside a callback the body REGISTERS (not runs) is not tracked — `sync ||` bodies and a `[must_use]`-style lint on `.get()` inside a nested closure in a computed body would catch most of it; async reads are correctly untracked. (iii) Conditional reads change the dependency set per run — a feature (the `?` above), and door 2's fixpoint handles the re-wiring inside one wave. (iv) Ordering by creation id (door 2's bucketed ascending run) stays valid: a computed is created after its sources. (v) A112 is untouched — delta derivatives recompute PARTS; a computed recomputes a WHOLE value; both live side by side.\n\n**What to build if ruled yes.** S1 PROBE in std over existing primitives (no compiler change): `computed<T>(body: sync || T): SignalCell<T>` with a `Shared<List<Frame>>` stack, `SignalCell::get` pushing its id into the top frame when one is active, diff-and-resubscribe on run, `as_derivation()` marked, owner-tied like `map`; pins = the four combinators re-expressed and byte-equal in behaviour under the existing turn pins; measure the `get` branch. S2 if the probe holds: `map`/`combine`/`flatten` keep their names as thin bodies over `computed` (or stay as they are — they are correct and cheaper for the static case; measure first). S3 the guide: 'reach for `map` when the dependency is one source; `computed` when the body decides'.\n\n**Rulings asked.** R1 — do we WANT read-tracking at all (reverses the recorded stance; the cost is the per-`get` branch and the untracked-callback footgun; the gain is one primitive)? R2 — if yes, does `computed` REPLACE the structural combinators' bodies or sit beside them? Rec: R1 yes, probe first; R2 beside, with `map`/`combine` unchanged until the benchmark says the branch is free.\n\n**What is NOT a derivation, and stays an effect.** theme.vl:128/145, views.vl:707, channel.vl:370 each pair a source with `.effect(|x| other.set(…))` where `other` is ALSO written by the user (a selection index reset when the filter changes; a form field seeded from data). That is a state machine with two writers, not a transform — no combinator library expresses 'derived default, user-overridable' except as an effect writing a cell (Solid's `createWritableMemo` is the same thing with a name). Those sites are already spelled right; a `reset_on(source, |x| initial)` helper would be sugar, not a primitive. Recorded so 'every effect+signal pair is a missing combinator' is not the reading."
    },
    {
      "id": "B371",
      "section": "B",
      "kind": "bug (internal error — a compile-time guard fires on a legal program)",
      "area": "Solver / emit",
      "status": "OPEN — filed 2026-09-21 (found probing A123 on 4f990aa2c)",
      "title": "INTERNAL ERROR — a generic blanket method that calls the TOTAL `flatten` at an abstract inner source type — `impl type S: Source<type T> { fun switch<U, I: Source<U>>(self, f: sync \\|T\\| I): SignalCell<U> { self.map(f).flatten() } }` — fails at emit with `internal: a call resolved to Source's requirement get, which has no body … first reached while emitting flatten (reactive.vl:1538); please report this program`, while the OPTION-shaped twin (`and_then` over `Source<Option<T>>` calling the optional `flatten` at an abstract `I: Source<Option<U>>`) compiles and RUNS. Inlined at a concrete type with the result annotated, `n.map(\\|m\\| Signal::new(m * 2)).flatten()` runs — so the gap is the abstract `I` reaching the total join's `self.get().get()`",
      "discussion": "A123 (the `switch` body this blocks — std can route around it via `on_change` directly, but the userland spelling must work); B300 (closed — the SAME message at a fully concrete nested `SignalCell<SignalCell<i32>>`, fixed by `bind_subject_bound_binders` grounding bound binders from the receiver's impl as a worklist; this is that machinery at a receiver whose inner type is the CALLER's type parameter `I`, not a concrete impl); A86 (the two flattens)",
      "body": "Repro (`scratchpad/andthen/sw6.vl`; the result annotated, so B300(a)'s inference gap is out of the picture):\n\n```vl\nimport std::reactive::{ Signal, SignalCell, Source, run_with_owner, Owner };\nexport impl type S: Source<type T> {\n\tfun switch<U, I: Source<U>>(self, f: sync |T| I): SignalCell<U> {\n\t\tself.map(f).flatten()\n\t}\n}\nfun main() {\n\tlet n = Signal::new(1);\n\trun_with_owner(Owner::new(), || {\n\t\tlet doubled: SignalCell<i32> = n.switch(|m| Signal::new(m * 2));\n\t\tprint(i\"{doubled.get()}\");\n\t});\n}\n```\n\n→ the `internal:` above, pointed at std's `flatten` (reactive.vl:1538). The free-function spelling `fun switch<T, U, S: Source<T>, I: Source<U>>(source: S, f: sync |T| I)` fails the same way once its result is annotated. The HYPOTHESIS (verify): `flatten`'s subject is `S: Source<type I: Source<type U>>`; at the `switch` call site the receiver is `SignalCell<I_switch>` where `I_switch` is `switch`'s own bound type parameter — `bind_subject_bound_binders` grounds `flatten`'s `I` to `I_switch`, but the emitter then needs `I_switch.get()` resolved to a concrete impl at `switch`'s INSTANTIATION (`SignalCell<i32>` here), and the substitution from the outer generic call is not applied to the inner blanket's bound binders — the requirement `get` is reached with no body. The `Option` twin succeeds, which narrows it: the difference is `self.get().get()` (a chained dispatch on the freshly-bound `I`) versus `match self.get() { Some(let inner) => inner.get() }` (a dispatch on a PATTERN-BOUND local) — so the chained-receiver path is the suspect.\n\nFix direction: whatever grounds `I` in the inner blanket must carry the OUTER call's substitution (the same lesson as B369: the caller substitutes, the callee's inferred types are handed through verbatim). Pins: `switch` as above runs and prints `2` then `10`; the free-function form; the `Option` twin unchanged; a `b371_*` in the solver's pins beside `b300_*`. Until fixed, A123's std `switch` is written over `on_change` directly (which is the better body anyway — one cell)."
    },
    {
      "id": "B372",
      "section": "B",
      "kind": "bug (inference)",
      "area": "Solver",
      "status": "OPEN — filed 2026-09-21 (found probing A123 on 4f990aa2c)",
      "title": "INFERENCE — `Signal::new(expr)` as a CLOSURE's result does not fix `T` from its argument: `let inner = n.map(\\|m\\| Signal::new(m * 2));` → `the type of 'inner' is never fully determined: SignalCell<SignalCell<T>> keeps its callee's type parameters` — while `n.map(\\|m\\| SignalCell::new(m * 2))` and `n.map(\\|m\\| Some(m * 2))` both infer, and `let x = Signal::new(0)` at statement level infers. The B162 receiverless trait-default path (`Signal::new` resolves statically to the default body) loses the argument→`T` binding when the call is a closure's return expression",
      "discussion": "A123 (kolt writes `Signal::new` everywhere — `switch(\\|m\\| Signal::new(…))` is the natural spelling and it does not infer); B162 (`Signal::new` with no receiver resolves to the trait default — the path at fault); B300(a) (closed — a different 'never fully determined': the trait argument not bound from the RECEIVER; this one is the ARGUMENT not binding the callee's `T` in return position); B369 (closed — the caller handing an inferred type through unsubstituted; check whether the same seam is involved)",
      "body": "Repro (three files, `scratchpad/andthen/inf{1,2,3}.vl`, on 4f990aa2c):\n\n```vl\nlet n = Signal::new(1);\nlet inner = n.map(|m| SignalCell::new(m * 2));   // inf1: compiles, prints 2\nlet inner = n.map(|m| Some(m * 2));              // inf2: compiles, prints 2\nlet inner = n.map(|m| Signal::new(m * 2));       // inf3: 'never fully determined: SignalCell<SignalCell<T>>'\n```\n\nSo the closure-return path is fine (inf1, inf2) and `Signal::new`'s argument binds `T` at statement level (`let n = Signal::new(1)` in the same file); the intersection — the B162 receiverless default in return position — is what fails. Annotating the binding (`let inner: SignalCell<SignalCell<i32>> = …`) makes it compile and run, which is the workaround and the diagnostic's own advice, but `Signal::new` is the documented everyday spelling (the `Signal` trait doc: 'the everyday spelling … resolves statically to this default body') and it must infer wherever `SignalCell::new` does.\n\nHypothesis (verify): the B162 static resolution substitutes the default body's signature `fun new(value: T): SignalCell<T>` with a FRESH `T` when the call has no expected type (a closure body's return has none until the closure's `U` is solved from it), and that fresh `T` is never unified with the argument's `i32` — `SignalCell::new` goes through the ordinary inherent-call path which does unify. Pins: inf3 compiles and prints `2`; the same inside a `combine(...).map(|(a, b)| Signal::new(a + b))`; the existing B162 pins unchanged."
    }
  ]
}