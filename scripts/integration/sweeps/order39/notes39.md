# Order 39 — running record (integrator)

Opened 2026-09-21 off c3f7d1a3 (Order 38's sealed tip; origin/next == local; CI 35642858068 green; nothing landed since). Ten lanes — go-items39.json. The owner's "Go" answered "waiting for R1–R9, or a 'go with all recs'": R1–R9 RECORDED AS RECOMMENDED.

- Standing integrator rules from Order 38: state an outcome only AFTER reading the log; every merge gate list includes `-p vilan-cli --test native_differential` (+ the whole set before a native merge pushes); `regen_goldens.sh` after every golden-moving merge; verify every lane's commits by `gpgsig` header before merging; a breaking/refusing lane's estate sweep is re-run over the MERGED tree; one merge at a time; relay cross-lane finds by SendMessage; resume native-b-39's agent for its rebase; the seal runs the whole-set native differential.
- LAUNCHED 2026-09-21: ten worktrees cut off origin/next = c3f7d1a3 (`commit.gpgsign = true` verified intact before launch); ten Opus agents (model passed explicitly), kolt + proposals READ-ONLY for every lane. Each prompt carries the hard rules inline: never `git config`; verify the premise; a passing probe must build something; declare return types around B369; goldens judged by runtime output; `native_differential` in every std-touching lane's gates. native-b-39 will be RESUMED for its rebase after native-a-39 merges. Cross-lane notes to relay when reports land: hygiene-39's N109/B366 vilan-rust lines → native-a-39; solver-39's B368 printing → fmt-39 and its `for` pattern arm → native-a-39.

## papers-39 — FIRST to report (33 min, 126 tool uses): three papers, 22 probe captures, every package artifact asserted (no vacuous pass)
- COMMITTED: `proposal/unit-literal.md`, `proposal/focus-scope.md`; A120's is a SECTION for transport-rpc.md (§9.7), staged at `sweeps/order39/papers-39/service-as-http-api.md`, spliced at the sweep; probes at `sweeps/order39/papers-39/probes/` (dist/ stripped).
- B363's PREMISE IS FALSE — VERIFIED by the integrator on c3f7d1a38: `Ok(void)` builds and runs; `void` is the unit value (spec/types.md:27, lexical.md:194; std's option.vl builds `Verdict::Bad(void)`); only the `()` SPELLING fails. The falsehood came from wire-38's probe (`Ok(())` only), was ACCEPTED by the integrator, reported to the owner as fact, stamped into A107's close, transport-rpc §9.6c, native… and kolt's `model.vl` wrapper. CORRECTED today: B363 rewritten, §9.6c annotated, INDEX row; still stating it: vilan `rpc.vl` ~1434, `guide/services.md` ~198, CHANGELOG ~161 (B363 S1's to fix in the tree). RULING ASKED: A107's stub → `Result<void, RpcError>` (breaking, unreleased; two source lines, two kolt lines, no wire byte) — rec YES, Order 40.
- A120 — HALF WRONG as filed: the generated client's transport is a TYPE PARAMETER and every plain stub lives on the unconstrained impl, so a hand-built `AuthClient<HttpTransport>` ALREADY WORKS today over `POST {mount}rpc` (login both arms, a plain method, the awaited void, `verify()`; browser leg too). What is missing: a constructor (`over_http(mount, codec)` — not `connect_http`: there is no connection), four refusals (`client = H` compiles clean today — the one hole), a status table (200 for everything the protocol decides; 401 typed envelope; 501 for a factory service is PLAIN TEXT), and TWO bugs first (below). Rulings asked Q1–Q5 (recs: `[service(http)]` an opt-in marker; a second hook `authorize_request`; std does no CORS but requires a non-safelisted content type; `over_http`; an application `Err` is never a non-200).
- A121 — right and understated: kolt's fallback `panel_element.focus()` is a NO-OP (a bare `<div>` with no `tabindex`). Design: a tabbable WALK (the DOM stub's `matches` throws), keydown wrap + capture-phase `focusin` guard, a scope STACK in std (an overlay is a portal — DOM ancestry is wrong), the trap at mount and `focus_initial()` called by the SHOW (B271's ruling); prototyped in userland, builds for a browser. NOT PINNABLE until the DOM stub grows `tabIndex`, `parentElement`, `getComputedStyle` and `focus()` dispatching `focusin`. Rulings Q1–Q6 asked.
- FINDS for newitems39b: HIGH the JSON codec READER never poisons on a type/arity mismatch (`["x",2]` reads `NaN`, `[1.5,2]` reads 1.5 as i32, a short list reads NaN; over HTTP `args: []` → `{"Success":NaN}`) — RELAYED to std-39 as an add-on; blocks A120. HIGH `HttpTransport` CRASHES the process on an unreachable host (unhandled `fetch failed`) against `Transport`'s `Err(reason)` contract. MEDIUM `[service]` over an `export impl` → a dispatcher with ZERO routes and the empty hash `00001505`, silently — RELAYED to solver-39. MEDIUM `HttpTransport` sends `text/plain;charset=UTF-8` (CORS-safelisted — a cross-site form POST is wire-indistinguishable; not exploitable today, the hazard the day a cookie gates a service). MEDIUM written type args on a variant constructor + an unsatisfied bound → `internal:` — RELAYED to solver-39 (beside E208). LOW ×6: the uninhabited `()` type; `void` missing from the EBNF literal; `let void = 3`; `GET {mount}rpc` answered 200 + Decode (nothing reads the method); `Element::focus`'s doc omits "focusable"; `[extern("document.activeElement")]` emits a CALL to a property (a `[extern(get, …)]` is the spelling — doc it).

## OWNER, mid-order (2026-09-21)
- I5 FILED (`usize` for every index) and RULED the same day: a distinct type (u53 on JS unless revisited); underflowing subtraction unspecified (never memory-unsafe); literal inference a GENERAL law; the rest as proposed; a paper lane in Order 40.
- B370 FILED then found to be a MISCOMPILE from the owner's kolt report: an expression of literals in a typed position is re-typed AFTER operator dispatch — `rem(4 / 16)` against an `f64` parameter prints 0 silently (`Math.trunc` emitted); five positions reproduced; RELAYED to solver-39 as its priority after B369.
- B363 S3 RULED and BUILT by the integrator: A107's stub answers `Result<void, RpcError>` — lane `void-result-39` 63161d83 → next 0976fd19 (pushed; gates green incl. native_differential); toolchain 0976fd194 installed both locations; kolt's two lines migrated (check 0/0, build clean, uncommitted). NOTE for the merges: every Order 39 lane branched at c3f7d1a3 — 0976fd19 touches rpc.vl's `call_ack` + the generator's one `.returns(..)`, guide/services.md, two pins, two test comments and CHANGELOG; collections-39 edits rpc.vl's `KeyedCell` (a different region).
- E213 + F27 FILED and RULED (all recs) the same day. F27: module-level `[platform(..)]` that is also the analysis platform; member-name evidence; platform-fenced twin items (not app-level layers); the parity gate learns fields. The owner corrected the integrator's framing — the editor's handling of kolt's unconsumed `conditional_value.vl` was CORRECT; the item is about legibility — so F27 gained R6: the diagnostic names the platform in force, the twin that HAS the member, and WHY the platform was chosen, with a quick fix and a status-line note. E213: `[internal("reason")]`, required reason. Both Order 40 candidates (R6 + R2 first).

## native-b-39 — SECOND to report (95 min, 202 tool uses): six signed commits (verified by `gpgsig`); branch pushed; merge QUEUED after native-a-39 (one rebase, by its resumed agent). THE HTTP EXIT IS NOT REACHED.
- F23 f192c6f4 — `context.rs` records the hidden parameter's flavour where it mints it (`Program::context_optional_hidden_parameters`; `Plan::param_nodes` carries the bool = `holds_bare`); `compute_context_flavours` + two helpers DELETED (98 lines); mint order deliberately unchanged → corpus byte-identical, `nursery.vl` still identical. Pin `native_differential::a_context_threaded_parameter_is_typed_from_the_recorded_flavour` (both readings in one program; flipping the bool swaps them).
- F24 77e0628b — `TaskId {index, generation}`; a free list at reap; the generation bumped AS the slot is freed. Pins vilan-rt `executor::tests` ×3 (200 rounds bounded — 201 slots with the list disabled; two stale-handle pins red without the generation check).
- `vilan-rt::http` 307ca259 (~1,050 lines + 15 pins; dependency-free, `forbid(unsafe_code)` intact): R1's design AS BUILT — `std::net` offers `set_nonblocking` and nothing else, so the executor gained `trait IoSource { poll, is_live }` + `register_io` and `advance_macrotasks()`: with NO source registered it IS `advance_timers` (every J6 ordering rule unchanged for programs that do no I/O); with one: poll the sources (a `true` is a macrotask), else fire the earliest DUE timer, else sleep `min(1 ms, next deadline)`. PRICE, written at the site: an idle server wakes ~1000×/s and a request waits ≤ 1 ms longer than under readiness notification. HTTP/1.1: request line, lowercased fields (repeats joined), `Content-Length` bodies, `Connection: close`; four wire refusals 431/400/411 (any `Transfer-Encoding`)/413; streaming responses; the UPGRADE handover (socket + head reach the handler; no RFC 6455 key yet — Order 40). Writes are queued and flushed by the poll (closing at `destroy()` discarded a just-written handshake — found by the upgrade pin). The one impossible comparison, named: node adds `Date` and keep-alive headers, so pins compare status + the program's headers + body. 7 pins over a REAL socket on an OS-chosen port, clients as joined threads, no sleeps for ordering.
- The emitter reaches `std::http` 0501cac2 (+2ebe5cb7): 23 of 25 raw `node:http` bindings native (the 2 left answer a `JsonValue`). PREMISE CORRECTION: the brief's "55 host bindings" census UNDER-REPORTS — a refused call's ARGUMENTS are never walked, so `createServer` hid nine bindings behind it. FOUR GENERAL emitter fixes fell out: `Expr::Destructure` emitted (`destructuring.vl` flips identical); a destructure of a PLACE copies (two destructures of one binding were a use-after-move); a closure's TUPLE parameter emits `parameter_destructures`; `scan_closure` treats `for` binders / destructure patterns / `match` legs / `is` captures as DECLARED. WHOLE SET on its branch: 50 identical / 67 refused / 0 differing / 0 rustc-refused / 0 broken.
- F25 6f81e6dd — `main` inside `vilan_rt::main_guard`: exit 1, the message alone on stderr (was 101 + Rust's banner), sync AND `async fun main`; printing a host handle or a function-holding value is a COMPILE-TIME refusal (the runtime panic stays as the nested-case backstop).
- WHY THE EXIT IS NOT REACHED: a `std::http` server built from the `Server` STRUCT LITERAL now reports 0 host gaps, then rustc refuses in FOUR lowering classes, all in native-a-39's half — (1) an `async` closure TYPE in a declared-async position (`Server.request_handler`): asyncness is per SITE (`async_fields`/`async_values`/`async_returning`), not in `Type::Closure` — NOT F22's hard half; (2) derived `PartialEq` through `Option<Rc<dyn Fn>>`; (3) an enum payload holding a closure (`ResponseBody::Stream`) — `ensure_enum` lacks `ensure_struct`'s guard; (4) a non-`Copy` field read off a loaned receiver is MOVED (E0507). Through `Server::builder()` four more: backed enum `Ordering` + `ListSortBy` (native-a-39's F20 tail), `JsonValue` (Order 40). RELAYED to native-a-39 by SendMessage with the rule that a rustc-refused emission must at least become a refusal BY NAME (an http program is not in the corpus, so the whole-set gate cannot see it). Whatever native-a does not reach, the RESUMED native-b agent takes at its rebase.
- NOT REACHED from its brief: `now_millis`, `queue_microtask`, the `Number`/`String`/`Boolean` coercions. Order 40's list by name is in the report (JSON five, coercions, the Bytes/codec set, `with_finally`, `random_bytes`, six `__db_*`, seven fs).
- FINDS for newitems39b: the four lowering classes if unfixed at the seal; the host census under-reports (walk a refused call's arguments anyway); OPEN Q1 the declared-async closure type as its own item split from F22 (rec yes); Q2 `IO_POLL_INTERVAL = 1 ms` kept; Q3 WebSocket upgrade is Order 40's — and `std::http` declares `upgrade_handler` SYNC, so A40's suspending upgrade handler is the adapted-instance shape (F22).

## hygiene-39 — THIRD to report (111 min, 311 tool uses): five signed commits; D9 needs none; merge RUNNING (a7cc20f4, clean)
- N111 b0cd9dce — PREMISE: 4 of 5 held (the site is analyzer.rs:28561 not :28280, and THREE stale frame figures, incl. deep_nesting.rs's own header); `report_depth_stats()` on a parse-bound refusal (a 600-paren program prints `parse 501 16.26MiB`); `write_run_script` — unlink then `create_new` — for ALL THREE temp-script writers (`vilan-run/-watch/-test-<pid>.mjs`): the real defect was SYMLINK-FOLLOWING in a shared temp dir and a leak across an abnormal exit, NOT N103's family; json_boundary.rs's header says what it is. Pins: cli `diagnostics` `n111_a_parse_bound_refusal_still_prints_the_depth_line`; cli `--bin vilan` `write_run_script_tests` ×3 (the symlink pin planted red).
- N109 a8998ce8 — `Intrinsic::ListNew`/`ListPush` rows; three `World` fields, two `Dispatch` variants, two `Transformer` fields and six arms DELETED; corpus + native differential byte-identical. `Program::list_new_fn_id`/`list_push_fn_id` KEPT — `vilan-rust` is their last reader (handed to the native side at the rebase). FIND: solver-38's `Dispatch::ListNew`/`ListPush` (B359) were UNREACHABLE — 0 hits over all 131 corpus programs; R1 closed the route they guarded. Pin core `intrinsic_binding` `n109_…` (red with the rows removed).
- N110 027fe10c — a `$`-prefixed identifier in a `// witness:` is a HOLE bound one-to-one in both directions; 37 witnesses in 10 programs, 23 quote a generated name, none rewritten. EDITED fn: cli `corpus` `every_declared_witness_is_in_its_golden` (one line). Pin `n110_a_generated_name_in_a_witness_is_a_hole_and_nothing_wider` (six directions). This retires the re-keying chore for reactive-39's / collections-39's golden regenerations.
- B366 b6c62f2e — PREMISE WRONG: there is NO second spelling — 44 declared parameters, 131 `Generic(..)` nodes, 0 bare constraint ids over the whole loaded world; the Rust emitter's `bind_parameters` took the `Generic` arm both times it was entered over 131 programs; planting the bare spelling PANICS the analyzer. Landed: the invariant stated at `Type::Generic` and `impl_select::bind_subject`, and a gate (`tests/nominal_generic_spelling.rs` ×2, the detector proven on synthetic input). native-a-38's "~40 lines of double handling" measures as ONE 4-line branch + a doc paragraph — relayed to native-a-39 to delete.
- N112 839f50e4 — DECIDED: keep B355's WARNING; `footnotes: Vec<Note>` not built. Rule: "a second fact that is not a second LOCATION for THIS error is its own diagnostic" — at `error::Note` and B355's gate. Rec: promote it into diagnostics-standard.md C3 (integrator, at the sweep).
- D9 — PREMISE STALE: all three paragraphs are ALREADY in editing-dx.md (the integrator wrote them at proposals 8d4c73c, Order 37); re-verified claim by claim against c3f7d1a3 (E114's identity; E69's 31/71/59/1,645/104; E193). CLOSE without work.
- Gates: core 6,062; cli 233 across nine targets; native_differential 11 + 11 (whole set); clippy; fmt. No golden moved, no `.vl`, no ledger row.
- Hand-backs kept: `sweeps/order39/hygiene-39/handbacks.md` (N112's text, D9's verification, the two vilan-rust notes — RELAYED to native-a-39).
- SCORE so far on "your item's text is a hypothesis": papers-39 3 items → 1 false + 1 half wrong; hygiene-39 6 items → 1 wrong (B366), 1 stale (D9), 1 mis-sited and mis-classified (N111); native-b-39 → the census under-counted. The rule is paying for itself.

## fmt-39 — FOURTH to report (112 min, 220 tool uses): E209, E210, E205 landed — three signed commits; merges LAST
- E209 9035aac9 — PREMISE verified, MECHANISM corrected: "a lost-and-gained pair" IS caught by the token net; the real hole is that three of `normalize`'s canonicalizations are DELETIONS (`drop_trailing_commas`, `drop_anonymous_binder_keywords`, `drop_redundant_import_aliases`), so a reprint with a different RAW stream normalizes equal — `fun main() {,}` does not parse and normalizes to `fun main() {}`. `verify_reprint` is now two checks (tokens, then a re-read of the printer's own output); `DeclineReason::ReprintDoesNotParse`. Pins core --lib `formatter::reprint_safety_net` ×5 (2 go red with the check removed). Cost NOT measured (one extra parse per successfully formatted file).
- E210 80b948e9 — the bisect runs over the NORMALIZED streams (raw ones would name the import run again); normalized → written index via common prefix, else common suffix, else the first moved token; the tool prints the source LINE. Verified against the REAL N108 bug with its fix reverted: names line 3's `};` (and line 9 in the kolt-shaped file, where the old code said line 1). Pins `formatter::divergence_location` ×5.
- E205 066dfabb (R8) — `[fmt] wrap_comments` (default OFF) → `FormatOptions` → `reprint_with`; new `formatter/comment_reflow.rs` (703 lines); `manifest::wrap_comments_covering(path)` climbs to the nearest manifest with an opinion. TEN never-reflow classes each with a byte-identity pin (fences, lists with hanging indents, tables, headings, block quotes, interior runs of 2+ spaces, SECTION BANNERS / rules — found by the census, the brief's list omitted them — toolchain directives, commented-out code, license headers) + trailing comments (a different emission path); long URLs and code spans are atoms; the words-in-order net pinned with a PLANTED broken filler. With the key off `vilan fmt --check vilan crates` is exit 0 — byte-for-byte today's.
- THE TWO COUNTS (neither tree opted in): `vilan/std` 61 of 63 files, 6,719 → 5,573 comment lines, 0 declines, 0 words changed, second pass clean; kolt `src` 14 of 27 files, 235 → 198 lines, zero em dashes introduced (plain hyphens kept). `vilan/test`: all 37 witness lines byte-identical, no golden moved.
- EDITED consts/fns (for the merge): core `manifest.rs` `KNOWN_SECTIONS` (+"fmt"); lsp `manifest_completion.rs` `TABLES`, `HEADERS`, fn `schema_and_listing_agree`; `editors/vscode/schemas/vilan-toml.schema.json`; formatter.rs test fns `a_printer_gap_is_recorded_with_the_span_of_what_it_met`, `a_probe_leaves_no_decline_behind`; lsp document.rs one comment in `a_broken_source_formats_to_no_edit` (EDITOR-39 edits document.rs heavily — fold by name); cli main.rs fn `fmt`. OWNERSHIP CORRECTION: a new manifest section touches FOUR files held together by one test, three outside the lane's map.
- ONE RED found and fixed in-lane: a docs line BEGINNING with backticks reads as a fence and swallowed nine headings → `markdown_golden` + lsp `book_sync`; TEMPLATE: any lane touching `vilan/docs` gates `-p vilan-core --test markdown_golden` and `-p vilan-lsp -E 'test(book_sync)'`.
- NOT DONE (by ownership): R8's "Reflow this comment" code action and the LSP/playground READING the key (format-on-save will not wrap for an opted-in package; safe — reflow only adds wrapping) — vilan-lsp is editor-39's this order → Order 40 (recipe in the report). No B368 hand-off arrived from solver-39 (`print_expr`'s `ForIn` arm untouched).
- FINDS for newitems39b: (1) std's comment width (~80–88) is not the code width (100) — a `[fmt] comment_width` key or a ruling that std never opts in; THE thing between the key and a default flip; (2) the LSP/playground wiring; (3) `wrap_comments = true` does not mean "no comment over the budget" (hanging-indent lists stay). OPEN Qs (recs): the default does NOT flip at v0.41.0; std does not opt in this order; the commented-out-code heuristic is tuned to bail (keep).

## MERGES (2026-09-21 →)
- (pre-order) void-result-39 → 0976fd19 (B363 S3; the integrator's one-commit lane).
- hygiene-39 → a7cc20f4 (pushed; clean merge; gates green incl. native_differential, corpus, copy_elision_census, vilan-wasm). Waiting on: editor-39 (next in order), then std → solver → reactive → collections → native-a → native-b (resumed for its rebase) → fmt LAST. Reported and queued: native-b-39 (branch pushed), fmt-39 (branch pushed).

## INTEGRATOR'S OWN BUG, found via reactive-39's report ("A113.md is 3 lines… the GO stamp trimmed it"): 25 TRACKER ITEMS WERE TRUNCATED by the ad-hoc stamping one-liner `open(p,'w').write(open(p).read().rstrip('\n') + stamp)` — Python opens for WRITING (truncating) before it evaluates the read, so every item stamped that way since the Order 38 SWEEP kept only its newest stamp: A106 A110 A111 A112 A113 A114 A118 A119 A72 A8 B360 B361 B362 B368 C14 C15 E199 E202 E203 E205 F1 F18 I3 M79 N106. RESTORED from git history at proposals 2e02d8a by replaying each file's commits (the last full version, then every stamp the truncating writes left behind, in order) — NO content lost; multi-stamp files verified (A110, A112: 3 stamps; F18: 5). Order 39's lanes had READ truncated bodies at launch (the briefs carried the substance): the five still running — editor-39 (E202, E203), std-39 (A118, A119, A72), solver-39 (B360–B362, B368, E199), collections-39 (A112), native-a-39 (N106, F1, C14, C15) — were told by SendMessage to re-read and to check their pins against the restored text; reactive-39 had gone to git history for A113 itself; papers-39/hygiene-39/fmt-39/native-b-39's items were not among the 25. GUARD: `scripts/integration/stamp_items.py` (reads everything first, validates each heading, writes, re-reads and asserts the heading and old body survive; `--check`); no committed helper ever had the pattern. RULE: tracker writes go through the helpers (`file_items.py`, `close_batch.py`, `stamp_items.py`) — never an inline `open(p,'w').write(open(p)…)`.

## reactive-39 — FIFTH to report (128 min, 232 tool uses): A110 door 2, A114, A113 landed; M83 measured and DECLINED; three signed commits; merge QUEUED (goldens)
- A110 DOOR 2 e59e1413 — `Subscriber.derived` (a plain bool); `Turn.pending_derived`/`queued_derived`; `enqueue` routes by class — a derivation to its bucket, an effect INSERTED so `pending` stays ascending by id (a backward scan: one comparison in the common case, no sort per wave); `drain` = derivations to a fixpoint, then effects; `dispose` scrubs both queues. The class is decided by `observe` from a mark the derivation site leaves and `observe` SPENDS — a BARE statement `as_derivation()`, never a closure wrapper (reactive-38's wrapper was the instrument's `cycles 0 → 1`): THE NO-CYCLE GATE READS `cycles=0` ON THE REAL BUILD. Seven sites marked (`map`, `combine`, `selector`, both `flatten`s ×2). COST: 167,236 → 206,714 Ir/wave = +23.6 % (reactive-38 predicted +23.5 %); thread CPU +29.9 % at loadavg 86 (loud). A TRAP worth the template: `Shared::read()` of a list in an EXPRESSION emits no copy, but BINDING it to a local emits `__clone` of the whole list — the lane's first version took A44's 2,000-observer pin from 80 ms to 7,587 ms; rule: never bind the queue to a local (§7.5 mislocates it).
- Pins (cli `reactive_lifetimes`): `a110_door2_a_parent_forms_effect_runs_before_anything_its_render_created`, `a110_door2_an_effect_reads_a_derivation_chains_final_value_once` (both red under door 1's routing planted back). EDITED: `ui_rows` fn `a110_nested_swaps_…_on_sign_out` (3/3 → 2/2); `reactive_lifetimes` consts `A110_DISPOSED_OBSERVER_IN_A_TURN` (disposer moved above the victim; expectation unchanged) and `FIND2_A_DISPOSE_FROM_INSIDE_A_DRAIN` (RESTRUCTURED — under door 2 the old program went VACUOUS; proven red against its own plant); `reactive_selection` fn `combine_attaches_…` (+1 assertion); `shared_census` `CENSUS` reactive.vl 29 → 33 and the literal 121 → 125 (COLLECTIONS-39 may move the same literal — hand-fold by MEASURING the merged tree); `copy-elision-census.tsv` 310 → 325 (three new exclusive-branch sites, not an elision regression); two witnesses re-keyed (N110 makes the next re-key unnecessary).
- Goldens: 9 corpus + 4 split artifacts, all RUNTIME-IDENTICAL (judged with regen_goldens.sh).
- A114 51efbeb0 (R2) — `Source::scoped_effect` / `scoped_effect_on_change` over a private `scoped_runner`; free `on_cleanup` (strict: no ambient owner is a compile error); std's form prints byte-identically to the user-code probe. Pins: `a114_a_scoped_effect_releases_each_runs_registrations_before_the_next_run` (runs=3 cleanups=3), `a114_a_throwing_scoped_effect_body_does_not_leak_the_run_it_started`, `reactive_channels::a114_a_mirror_lease_taken_in_a_scoped_effect_is_released_at_the_runs_end` (3 Subscribe / 3 Unsubscribe; the plain-`effect` control leaks three observers). Docs: std/reactive.md + guide/reactive.md.
- A113 381c5567 (R3) — every row MEASURED by subscriber count (owned forms → 0 after dispose; a dropped `sub` handle stays subscribed; four combinators inside a boundary → 0; `map` OUTSIDE every boundary stays 1 — pinned as the contract; a plain effect body accumulates 3 where `scoped_effect` holds 1); guide "Who cleans up what". markdown_anchors.golden +3 folded into this commit (51efbeb0 alone would red `markdown_golden`). BRIEF CORRECTIONS: the guide file is `guide/reactive.md` (not `reactivity.md`).
- M83 — MEASURED, DOES NOT LAND: door 2's two-map design removes its forcing function, and per-id `remove` is WORSE on both tiers here (Ir +9.8 %, thread CPU +22.1 %); reactive-38's JIT −35.9 % does not survive. CLOSE as declined.
- KOLT'S LATCH CAN GO IN FULL once this merges (kolt's route changes are all inside a turn): views.vl:157–162 comment → one line; delete :164–169 (`page` cell + the `effect_on_change` latch); :173 `swap(page, …)` → `swap(get_route(), …)`; drop `is_login_route` from the import at :33.
- FINDS for newitems39b: FIND-1 MISCOMPILE HIGH — a mapped-tuple comprehension whose element is a BLOCK expression is DROPPED from the emitted JS, statements and side effects included, clean at check: `(value in values => { note(1); value })` emits nothing (`block=0`) where the call form emits `values.map(..)` (`call=3`) — REPRODUCED by the integrator on the installed 0976fd194; probe kept at `sweeps/order39/probes/tuple_comprehension_block_dropped.vl`. FIND-2 MEDIUM — `RemoteSource::map` and `KeyedSource::map` (rpc.vl ~3376/~4134) attach through `self.lease(..)` and are classed as EFFECTS under door 2, so a lower-id effect on the same mirror reads the mapped cell STALE (`seen=…7/2` vs the local control's `7/14`); fix = `as_derivation();` above each `get_owner().take(self.lease(..))` + EXPORT `as_derivation` — the integrator's at the merge of collections-39 (which owns rpc.vl) or Order 40. FIND-3 LOW — the inline cadence's good behaviour on the nested shape is subscription order + door 1, not a contract (write into reactive-turns.md). OPEN Q3: `at_settle` deferrals are effect-class ordered by a possibly very low held id — no pin moved; record, do not change.

## collections-39 — SIXTH to report (130 min, 214 tool uses): A112 S1 + M82 landed (two signed commits, tip de237ee1); A112 S3 WRITTEN BUT NOT LANDED — blocked by two solver gaps; patches + repros kept at `sweeps/order39/collections-39/`; merge QUEUED
- S1 b850d369 — new `vilan/std/src/delta.vl` (340 lines): `DeltaCursor`, `SeqOp<T>` (`Splice`/`SetAt`/`Reset`/`Move`), `MapOp`, `SetOp`, `DeltaLog<O>` (+ `with_limit`), `trait DeltaSource<C, O> with Source<C>`; reactive.vl RE-EXPORTS the seven names (an import cycle between the two files works), so `std::reactive::DeltaCursor` is the ruled spelling. `KeyedCell` re-based on it — THE WIRE DID NOT MOVE: the paper's `probe_keyed_cell_today` prints byte-for-byte what the shipped cell printed (incl. `since=Reset(1104) base=1031 version=1106`), every keyed rpc pin green unchanged. ONE DESIGN CORRECTION the gates forced: the log is over `KeyedElement<K, T>` (element + its key, projected once per WRITE), not the paper's bare `SeqOp<T>` — projecting at DRAIN time reds `a_keyed_cell_re_keys_nothing_…` at (20, 20) vs (0, 0).
- THE LAW is in the suite: new corpus program `vilan/test/delta-law.vl` + runner cli `delta_log::the_derivative_law_holds_across_four_hundred_randomized_turns` — reproduces the paper's arithmetic EXACTLY (400 turns, 555 checks, 0 failures, 845 ops, 56 resets, 1,223 vs 6,651 calls); asserts the artifact exists; NON-VACUOUS (a planted `since → Some([])` for a lost cursor reds at turn 12). The lane notes the corpus gate ALONE would not have caught it (two identical panics pass a byte gate) — hence the dedicated runner. Also `the_logs_two_off_by_one_behaviours_are_what_the_doc_says`, `std_reactive_imports_nothing_from_std_wire`.
- M82 de237ee1 — `reconcile` keeps a per-canonical-key chain of old positions + `low`; the plan is IDENTICAL for every `K` (a coarse `==` still scans `[low, candidate)` — empty for every key type in the estate); no bound added. NUMBERS (callgrind Ir, `--jitless`, before measured on THIS tree): 500 rows 95,126,314 → 14,773,633; 1,000 rows 355,064,495 → 29,069,240; 2,000 rows 1,369,863,837 → 58,504,196 — per doubling 3.73×/3.86× → 1.97×/2.01×: QUADRATIC → LINEAR. Pin `delta_log::reconciles_index_answers_exactly_what_the_scan_answered` over `vilan/test/reconcile-index.vl` (the old scan carried verbatim; 1,015 cases incl. 400 coarse-`==` pairs; its first version reddened it). PAPER CORRECTION: one push on the delta path is 3,517 Ir at 1,000 rows, not 22,347 (the paper's 20-change slope sat in noise) — ~101,000× the old scan, ~8,265× the indexed one.
- OWNERSHIP NOTE: `reconcile` lives in reactive.vl:1360 (NOT the ui twins, as the brief said) — one hunk in reactive-39's file, far from the scheduler; expect it at the merge.
- S3 NOT LANDED — the delta path in `place_each` is written and formats clean (held patches), but `each` cannot REACH it on this compiler: the dispatch must be an optional-capability trait and both spellings are refused — FIND 1 (HIGH): the specificity order does not rank a SUBTRAIT bound above its supertrait bound (`impl type S: Narrow<T>` vs `impl type S: Base<T>` where `Narrow with Base` → "neither impl subject is more specific"); FIND 2 (HIGH): a generic parameter reachable only through a BLANKET impl's trait argument does not bind through a struct-RETURNING constructor call ("`T` is missing the bound `: PartialEq`"; the nominal-impl route and a non-struct-returning call both work) — with a control. The lane REVERTED rather than ship a weakened `each`, a breaking bound, or an unratified third form. Rec (Q-B): rule both as defects, fix, land the held patch unchanged; alternative an explicit `each_delta(..)` form.
- EDITED: `shared_census.rs` `CENSUS` (rpc.vl 52 → 47; new delta.vl row 9) and the literal 121 → 125 — REACTIVE-39 ALSO wrote 125 (+4 in reactive.vl): the merged literal is 121 + 4 + 4 = 129? NO — MEASURE the merged tree (collections' delta is rpc −5, delta.vl +9 = +4); `copy-elision-census.tsv` 310 → 333 (reactive-39: 310 → 325) — regenerate over the merged tree; `corpus_manifest!` +2 rows; `reactive-keyed.vl`'s witness re-spelled; docs std/reactive.md + std/rpc.md; markdown_anchors.golden +1 (reactive-39 +3 — regenerated at the merge). Goldens: `reactive-keyed.mjs` moved, runtime-identical; two new.
- Q4 (ruled: a deprecated `KeyedCursor` re-export) IS NOT EXPRESSIBLE — no type alias, no `import as` for a re-export, `[deprecated]` is a function attribute; the lane did the HARD RENAME with a `breaking` note. → the owner is told; a type-alias item is filed at the sweep.
- FINDS for newitems39b: FIND 1, FIND 2 (above); FIND 3 indexing a `Shared<List<K>>::read()` TEMPORARY at a generic element type is refused ("element type is never determined"; `.get(at)` works); FIND 4 `==` on a `T: PartialEq` loses its bound inside a nested match arm within a closure; FIND 5 `vilan fmt` DECLINES a hand-wrapped generic `impl` header; FIND 6 the M75/M77 pin `m75_a_collisions_refusal_does_not_scale_…` is WALL-CLOCK and reds under load ("-10 ms" — the instrument) — perf-38 made its COUNT half, the wall half must go; FIND 7 the inexpressible deprecated re-export. OPEN Q-A: add `K: Hashable` to `reconcile`/`each*` so reorders go linear too (BREAKING: kolt's two `each_values` over structs need `[derive(Hashable)]`) — rec yes, Order 40, with I5's train; alternative leave it.

## 1PASSWORD LOCKED mid-order (~15 min): pushes and SIGNED commits both failed (`sign_and_send_pubkey: … communication with agent failed`; `1Password: failed to fill whole buffer / fatal: failed to write commit object`). Handled per the standing rule: stopped, merged from LOCAL lane branches (`merge_lane_local.sh`, a variant of merge_lane.sh that tolerates a failed fetch/push), the editor-39 merge stayed STAGED with MERGE_HEAD until the vault unlocked, then committed signed (b5958d7f) and gated. Four lane branches pushed after unlock. No commit was made unsigned.

## editor-39 — SEVENTH to report (136 min, 332 tool uses): all seven landed, eight signed commits; merged b5958d7f (CHANGELOG-only conflict), gates running
- E207 f99330c2 — ROOT CAUSE CONFIRMED and planted red; the per-parameter append deleted; `parameter_contexts` KEPT (context.rs is its consumer). Six pins that COUNT the substring. FOUND on the way: the existing E9 pin `hover_renders_a_parameters_context_clause` was VACUOUS — `hover_at_cursor` strips every `|`, so it analyzed `body: ( void)` and asserted a clause only the deleted append could produce; repaired onto a new `hover_at_marker`.
- E206 ea9a7ee1 + 5899ce4e — `Program::call_signature_labels` (the signature each generic call site solved, off `method_call_substitution`); hover renders both lines in one fenced block, blank line between — exactly the owner's exhibit. Seven pins. CORRECTION: there is NO signature help in this server (the book pins its absence) and the wasm playground has NO hover — hover + completion `detail` are the whole surface.
- E204 cad84bdb — census MEASURED: field-read hover dropped the doc; field-declaration and initializer-key hover answered the WHOLE STRUCT; member completion had no detail; four consumers now agree. Seven pins. There is no docs generator (the book is hand-written) so that row has no consumer. Q2 (the struct block showing each field's first paragraph): rec NO — built as recommended.
- E202/E203 e3c63ef9 + 699b891d (R9) — vilan-lsp's FIRST `onTypeFormatting` provider (`<` only); `<`/`>` and the backtick join `surroundingPairs`; the backtick auto-closes with `notIn: ["string"]`; `autoCloseBefore` declared. Pins: 11 typed shapes incl. the `Option<|` path head, 7 comparison shapes incl. `if a <|`, the lexer grep pin naming E203 — the last three came FROM THE RESTORED ITEM BODIES. OPEN Q1: "typing `>` over the placed one does not double it" is NOT achievable from the server (VS Code overtypes only what it inserted itself) — a client-side `type` command override in extension.ts is the follow-up (file at the sweep). The element-head half (`<div`) not built, per R9's scope.
- E211 629171e7 — `Completion` gains `filter_text` + `replace_span`; the server emits a `textEdit`; the playground carries both. Six pins.
- E212 788400d4 — PREMISE PARTLY WRONG: B346's refusal EXISTED and named both paths; the defect was that it fired TEN times (once per macro-defining std file), anchored inside std, AFTER a first diagnostic blaming the user's code — now asked ONCE at the registry build, attributed to the entry, leading: 11 diagnostics → 2. Pins in `relocated_std` (which already existed).
- EDITED: lsp `hover_renders_a_parameters_context_clause` (was vacuous); lsp `completion_item_tests` fixtures (+2 fields); core `relocated_std::messages_against`; lsp `book_sync.rs` `CAPABILITY_CLAIMS` — one row DELETED (`"there is no range or on-type formatting"`) and two added (named; the merge's union must not resurrect it); docs appendix/editor.md.
- FINDS for newitems39b: (1) `[derive(PartialOrd)]` is SILENTLY ACCEPTED and expands to nothing (`STD_DERIVE_MACROS` lists six names; compare.vl declares only a `PartialEq` macro) — a derive name std declares no macro for should be refused BY NAME at the attribute; likely the shape behind the owner's original E212 report; (2) `hover_at_cursor` mangles its fixture (a trap; `hover_at_marker` beside it); (3) vilan-ide has no in-crate tests for the completion engine; (4) `field_at_offset` is a linear scan per hover; (5) E202's overtype needs a client-side `type` override. HANDED to fmt-39 (now moot — fmt-39 has reported): the fmt idempotency pin over a documented field — verified by hand only.
- Gates: lsp+ide+wasm 899; core --lib 807; core whole 6,061; cli vscode_extension/grammar_sync/diagnostics_ledger/release_scripts/corpus/native_differential 111 + whole-set 11; wasm32 check; clippy; fmt. No golden moved.

## std-39 — EIGHTH to report (149 min, 285 tool uses): all six items + the relayed JSON-reader add-on landed; eight signed commits (tip 2a1d7ff4); branch pushed; merge QUEUED (goldens: six regenerations over five files, all runtime-identical)
- A116 3ccbf2da — `__json_tag` answers `""` for a non-string/non-object (null/number/boolean/array/`{}`); the interpreter's twin (which reported an INTERNAL error) moves in step. Six pins; only the `null` JS pin is a true differential (the other shapes already fell to the `_` arm — regression guards by the lane's admission). BRIEF CORRECTION: only TWO goldens carry `__json_tag` (`derive-enum`, `time`), not "every derive-using golden".
- A118 60794a42 (BREAKING) — a CONTRADICTION, not just a bug: the book said `[low, high)`, random.vl's own header said inclusive, `__random_int` implemented inclusive; R7: the doc wins. Estate sweep: THREE sites lose their upper value — `vilan/test/file.vl`, `watch.vl` (`range(100000, 999999)`) and `std::arena`'s brand draw. Four pins (two planted red; 400 draws per case). The book fence now PANICS out of range but no gate runs it — N47's.
- A117 bf2e4ad7 (BREAKING) — export count 67 → 46. CORRECTION: `#hidden` is NOT a hide marker (`#` is B318's REACH marker); the mechanism is a FUNCTION-SCOPED import (probed both directions). Zero estate importers of any alias. DOOR 2 RE-CENSUS with the 46-name set (std-38's script was not kept; reconstructed): file-level shadows 7 → 5, scope-local 22 → 22, redundant imports 75 unchanged — only ONE colliding name was an alias (`scale_step`); what collides is words (`space`, `divide`, `focus`, `first`, `last`, `s`, `active`, `within`, `hex`). REC: door 2 stays untaken — A106 stays open on that.
- A115 5a17769e — `impl Option<str> with AttrValue` + `impl type S: Source<Option<str>> with AttrValue`, both twins; `None` writes nothing/removes; three pins; std/browser.md's stale A33 bullet fixed in passing.
- A119 80ea4159 + 2a1d7ff4 (R6) — `WhenSome` + `when_some` on `place_swap`'s machinery, both twins, in `std::web`'s prelude; presence decides structure, a `Some → Some` writes the row's `SignalCell<T>` and the row STANDS (node identity pinned). THE HMR PREMISE DOES NOT REPRODUCE: kolt's `conditional_value.vl` impl transcribed into `hmr_swap.rs` and driven through two real `run --watch` swap rounds, incl. with the row LIVE — clean; std's `when_some` too. Second commit: the server twin's body became a named `place_when_some` because `std_twin_parity` COUNTS PRIVATE DECLARATIONS (reddened only on the whole core run) — a briefs40 Mechanics line. Pins: ui_rows ×2, ssr_differential (from the restored item text), hmr_swap ×3, module_resolution (the prelude surface).
- A72 a66fed94 — `navigate_replace(path)` (not a parameter — no overloading, no defaults; breaking at every call); pin in `router` planted red twice. The item's other two faces stay open.
- ADD-ON (papers-39's JSON reader find) 2321790e — every typed read checks the JSON kind and POISONS naming both sides; integer lanes want a whole number, unsigned a non-negative one; the openers gated; `end_list` holds a list to being read whole; `field`'s `has_json_field` THREW on `null` (a null frame reaching a struct decode took the process). Ten inference pins ALL planted red + `service_layer::a_malformed_rpc_argument_answers_a_decode_failure_rather_than_success` over a real route. The wire does not move. Goldens `crypto`, `time` moved (a new `__json_kind` helper), runtime-identical. Style: `found_kind` ends in `_ =>` because an exhaustive match over a backed enum emits `__enum_trap` into every JSON-decoding bundle.
- EDITED: cli `ssr_differential.rs` const `COMPONENT` + fn `ssr_process_render_matches_browser_dom_tree`; core `module_resolution` fn `the_web_preludes_surface_is_its_members_and_its_ambient_modules`; `tests/support/dom/stub.js` (additive: `historyEntries`, `replaceState`); `vilan/std/src/web.vl` (one import list — outside the map, nobody else's). MERGE NOTE: void-result-39 (0976fd19) edited `service_layer.rs` in place; std-39 APPENDED to it — trivial at worst; re-run `service_layer` + `rpc_http` after.
- KOLT RECIPES (at the owner's word, after the merge): A119 — delete `lib/conditional_value.vl` whole + its import; `channel.vl:32`'s body now receives a `SignalCell<(Message, User)>` — derive `message`/`author` with `.map`, make the two text holes reactive reads, `message_menu_subject.set(Some(message.get()))` — the row then updates IN PLACE (kolt's second FIXME answered); A72 — delete `history_replace` + `navigate_in_place`, import `navigate_replace`, two call sites.
- FINDS for newitems39b: (1) kolt's `BUG: This breaks on HMR` is NOT reproducible — ask the owner what the symptom was (A96, closed Order 38, the likely original cause); (2) `FromJson`'s SCALAR impls have the other half of the hole (`i32::from_json("1.5")` → `Ok(1.5)`); (3) the rpc route's LONG argument list is unchecked (`open_request` never closes the list; `RpcRequest.arity` ignored) — rpc.vl, Order 40; (4) `std::binary`'s reader unaudited for the class; (5) `std_twin_parity` counts private declarations — write it down. OPEN Q: integer MAGNITUDE (range) in the reader — rec leave, its own item.
