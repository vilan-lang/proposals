{"prefix": "- 2026-09-22 (the owner's rulings before Order 40): ", "stamps": {
"A124": "R2 RULED as recommended — a notification carries NO payload and the leaf pulls through `get()`, one subscriber id per leaf chain; WITH the owner's note: cells are welcome MID-CHAIN when a consumer wants them — a complex mapping finished with `.cell()` (the owner's name for the materialising node; use it instead of `memo()`) gets an efficient cached value; cells at EVERY step is what is inefficient. Non-`Source` returns that later converge (the pending family) and dynamic subscriptions are both composable, so the paper writes `.cell()` as one more node, composable anywhere in a chain. R3 RULED — `dyn Source<T>` over the object-safe core, explicit keyword, no resources; AND a bare `Trait` at a struct FIELD is REFUSED for now with a steer to `dyn Trait` (B184's hidden generic at fields is withdrawn at that position; parameters (B186) and bindings (B161) unchanged). R4 RULED — kolt migrates (same names; the cut may come BEFORE the migration if that works better; not a blocker on the cut). R5 RULED — `Pending<T>` or `Resource<T>` (the paper picks; rec `Resource<T>` — it can go from settled BACK to pending when an upstream source changes, which is Solid's resource exactly, and the paper states that state machine: pending → settled → pending again), with BOTH `.or(default)` → `Source<T>` and `.optional()` → `Source<Option<T>>`; sub-question for the paper: on re-pend, does `.or` fall back to the default or hold the last settled value (rec: `.or` resets; a `.latest(default)` holds).",
"A125": "RULED YES — `K: Hashable` on `reconcile` and the `each*` family, Order 40 beside I5 (one signature move per `each` caller); kolt's two `each_values` over structs derive `Hashable`; pin the 1,000-row reverse linear in Ir.",
"I5": "RULED (the last open question) — a length or position that crosses rpc KEEPS its wire width: frames byte-identical, the contract hash does not move; the paper proves it (a `Patch`/`Delta` frame before and after, byte-compared).",
"F32": "RULED (b) — an `i128` LIMIT natively: a literal past it is refused at compile time, an overflow traps at runtime, the book states the limit; (a) a bignum only if a real program asks. The two programs flip or their refusal names the rule.",
"E215": "RULED R1–R3 as recommended — R1 a `[fmt] comment_width` key, default = the code width; R2 std opts in at the measured 84 (prose stays as written); R3 `wrap_comments` does not flip on by default before std has run under it for one order.",
"B362": "RULED (the dispatched half) — THUNK at the dispatched call site through the callee's recorded convention (a `lazy` parameter is part of the signature and impls must match, so the convention is known at the bound); the direct-call pins stay true; un-ignore `b362_a_lazy_parameter_reached_through_a_bound_is_still_unsound` red-first.",
"A120": "RULED Q1–Q5 as written in §9.7.12 — Q1 an opt-in `[service(http)]` MARKER; Q2 a second `authorize_request` hook; Q3 no CORS in std (guide recipe; the content-type check stays); Q4 `over_http`; Q5 an application `Err` is ALWAYS 200. The two decided-not-asked points stand (`over_http` makes no call; envelope and status never disagree). S1–S5 may be briefed, after B374/B375.",
"A121": "RULED Q1–Q6 as written in focus-scope.md §10 — Q1 a WALK; Q2 `Contain` + `Wrap`, opt-out = not calling; Q3 the SHOW calls `focus_initial`; Q4 `View::autofocus()` sets the attribute (kolt's `[x-autofocus]` retires); Q5 no `inert` arm; Q6 the stack lives in std. Trap at mount, focus at show, as two acts. The DOM stub grows `tabIndex`/`parentElement`/`__dom_active_element`/`__dom_computed_style` FIRST (S1's prerequisite), then S1–S3."
}}
