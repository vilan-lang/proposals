# Vilan Backlog — the archive

Append-only. When an item in the live tracker
([`backlog.md`](backlog.md)) ships or closes, the
same sweep that closes it moves its tombstone paragraph here — dated,
under a dated heading — and retires the number. The live tracker never
accumulates history; this file never holds open work.

The eras before this file:

- **Alpha capture, through 2026-07-18**: [`backlog.md`](backlog.md) —
  every shipped item's full body, context, and lessons (including the
  bodies relocated there by the 2026-08-03 restructure).
- **Cycles 15–19, 2026-07-18 → 2026-08-18**:
  [`backlog-2026-07-18.md`](backlog-2026-07-18.md) — frozen at the
  2026-08-18 re-baseline with its tombstones in place, including the
  E49/E56 charters' full arcs and the v0.33.0/v0.34.0 trains.

---

## 2026-08-18

- **L1. Ratify beta.md — CLOSED 2026-08-18** (ratified as recommended
  the same day it was drafted: Q1 the clean-train count starts at
  v0.35.0; Q2 B73 blocks trigger (c) — beta-critical; Q3 message-head
  identity, no numeric codes; Q4 reactive/ui Tier 2, canvas Tier 3; Q5
  the annex ratified. The owner's follow-up "should we defer beta?" is
  answered in the status block: the ratified trigger already defers the
  declaration; pre-switch work proceeds as low-regret hygiene. Record:
  beta.md status block / process.md §5.)

- **M3. `DevChannel.clients` accumulates dead SSE streams (S; found by the survey)** — SHIPPED 2026-08-18. The HMR dev channel pushed every SSE connection into
`clients: Arc<Mutex<Vec<TcpStream>>>` unconditionally and pruned
only as a side effect of a broadcast's write failure — a long dev
session with many reconnects and sparse rebuilds accumulated dead
streams unboundedly. Confirmed at one leaked fd per disconnect (100
cycles → 100 fds, no rebuild involved), with two corrections to the
survey's write-up: the per-connection **threads never leaked** (the
handler returned after registering the socket), and a rebuild did
**not** reap — a write to a cleanly closed peer succeeds, so a dead
client survived its first broadcast and left only on the second.
Fixed with the read-side liveness check: the connection's own thread
now stays on it, blocked reading, and unregisters on end-of-stream;
the broadcast prune stays as a backstop. After: 100 cycles, zero fd
and zero thread growth. Pinned in `hmr.rs`'s unit tests (the
registry's size is not observable on the wire, and `vilan-cli` is
bin-only). M2's tier-2 soak remains the long-horizon validation. (Record: hmr.md appendix; merged 931171dc.)

- **N1. vilan README refresh** (S–M) — SHIPPED 2026-08-18 (diagram now lists all five crates + npm/homebrew, test count updated to ~3,700). The repository-structure diagram omits two of five workspace crates (`vilan-embedded-std`, `vilan-wasm` — the playground's own engine) and both packaging trees (`npm/`, `homebrew/`) — README.md:176-187 vs Cargo.toml:1-7; and the "~1,500 tests" claim is far under the ~3,000+ the suite actually runs (process.md §8.5 measured 3,046 on ubuntu at v0.32.0). The alpha status line STAYS (see §L note). (Merged 08704bc1.)

- **N3. The lone transformer TODO** (S) — SHIPPED 2026-08-18 (verdict: dead, comment deleted — see transformer.rs:7788 blame). `crates/vilan-core/src/transformer.rs:7788` ("consider extracting identifiers into a separate lookup table for late identifier substitution", 2026-05-04) is the single genuine TODO in the whole Rust workspace outside bindgen's vocabulary. Resolve it into a real item or delete it — a three-month-old open question in codegen is either work or noise. (Merged 08704bc1.)

- **N4. Homebrew staged-formula header honesty** (S) — SHIPPED 2026-08-18 (generator's heredoc + its own top comment + the test's doc comment all reworded, formula regenerated byte-identical). `homebrew/Formula/vilan.rb` pins v0.14.0 deliberately (the test's seed fixture, per crates/vilan-cli/tests/brew_formula.rs:26-40) but its header comment claims it mirrors "the current release" — reword the comment to say seed fixture, so the 20-release gap stops reading as rot to every future audit. (Merged 08704bc1.)

- **B124. An exhaustive `if`/`else` of bare `ret`s draws a false type error** (S–M) — SHIPPED 2026-08-18 (merged f94cfeac; record: editing-dx.md §17.7). A function whose tail is an `if`/`else` where EVERY branch is a bare `ret expr;` still reports "Expected T, but got void" on the whole `if`: the branches unify on their own synthesized void tails, not on the `ret`s that actually leave. The fix likely teaches tail unification that a branch ending in `ret` contributes no tail value. History: backlog-2026-07-18.md §B item 124. Root cause: the analyzer had the divergence vocabulary (`expr_diverges`/`block_diverges`) and the two merges that type a branching expression never asked it — `if` collected trailing ids only, `resolve_match` the same one level down; a `ret`-terminated branch's synthesized void tail was unified as if reachable. Fix: a branch that leaves contributes `Type::Never` at both merges, `check_return_position` treats a diverging body as Matched, the walk-time ret-only dedup deleted in favor of the general question. 17 pins (13 shapes + 4 negatives), plant-proven three ways; closures keep ret-checking.md §4's conservative rule by design.

- **M1. Benchmark harness + baseline** (M) — SHIPPED 2026-08-18 (merged 7a4dd6b0; record: perf-baseline.md + perf-baseline.jsonl). Four-phase library-call benchmarks (no criterion dependency — `Instant`, per house minimal-deps taste) over std (cold) and kolt/website (warm), with **explicit cold/warm control** via `analyzer::base_cache_clear()` — the process-global `BASE_CACHE` and `parse_clean_cached` otherwise conflate the two, the exact drift suite-speed.md §2.1/E26 got burned by. End-to-end `vilan build` timings gate only relative to a freshly measured reference compile (the existing liveness-bound convention), never absolute seconds. Plus an LSP edit-latency benchmark: p50/p95/p99 over thousands of synthetic single-keystroke edits via `Document::analyze_on_this_ thread`, reusing `leak_measurement`'s warmup/measured shape. G2's measured 7–9% warm-analysis const cost is the first known candidate. Outcome: three sections (four-phase library calls with forced cold/warm per iteration and priming to kill the ordering artifact; `vilan check` end-to-end in reference units; LSP edit latency p50/p95/p99), no threshold in the PR gate (E32/E39/E40 precedent), four cheap pins incl. two proving the percentiles are the numbers they name. Headline: `const_eval::evaluate` is 64% of the website's wall (68× kolt for 3× the source); post-passes are the one phase warmth doesn't help; page.vl edit latency 321ms p50 vs the 150ms debounce. Filed M4/M5/M6.

- **K1. Playground findability** (S; owner's 2026-08-18 list, item 3) — SHIPPED 2026-08-18 (web-tokens lane; vilan-lang/website@561dcff, deployed; live: href="/playground" in top_bar and the footer). The playground is linked from no user-facing surface: `top_bar()` renders exactly three nav links, none of them the playground (vilan-website `src/masthead.vl:86-90`), and the footer's three link columns skip it too (`src/page.vl:680-708`). Add it to the primary navigation.
- **K2. Template selector — track the dirty state** (S) — SHIPPED 2026-08-18 (web-fix lane caac9d48 → website@561dcff; the select derives its state from the buffer on every change — placeholder reads "Modified — <Template>"; browser-verified). The selector's value is written once at init (`wirePicker`, `playground/editor-src/editor.mjs:375-390`) and never revisited; the update listener only persists and lints (`editor.mjs:347-352`), so a modified buffer still shows its template as plainly selected. Mark the selected entry as modified once the buffer diverges.
- **K3. Template selector — reselecting the active template resets it** (S) — SHIPPED 2026-08-18 (web-fix — the placeholder holds the committed value while dirty, so a same-template pick is a real change and native `change` fires; reset routes through K4's confirm). The pick handler hangs off the native `change` event (`editor.mjs:381-385`), which never fires on a same-value pick — "load the pristine template again" is unreachable today. Route it through K4's confirmation when the buffer is dirty.
- **K4. Template selector — confirm before replacing modified code** (S) — SHIPPED 2026-08-18 (web-fix — an inline confirm bar rendered in vilan, no window.confirm; cancel snaps the select back before the page hears the pick; 55 browser checks). `pick` replaces the whole document unconditionally (`src/playground.vl:204-214` → `editor_set_doc(example_source(name))`) — a dirty buffer dies with no confirmation. Gate the replacement on an explicit confirm.
- **L7. The three security settings** (S) — DONE 2026-08-18 (orchestrator, under Order 2's Go: `secret_scanning`, `secret_scanning_push_protection`, `dependabot_security_updates` PATCHed to enabled on vilan-lang/vilan and read back). `secret_scanning`, `secret_scanning_push_protection`, `dependabot_security_updates` — all still off. The one deferred item with a real failure mode behind it (a credential in a public repo); should not wait for beta.
- **N5. Website copy drift trio** (S; all one commit) — SHIPPED 2026-08-18 (web-tokens lane → website@561dcff; live copy reads "one package, three entries"; vilan.toml comment and README `dist/server.mjs` fixed). The live page's dogfood section says "one package, two entries" (vilan-website `src/page.vl:616`) and `vilan.toml:3-4`'s header comment agrees — both stale since commit 04730cc added the third (playground) entry; and README.md:39 documents `dist/server.js` where the toolchain has emitted `dist/server.mjs` since v0.33.0 (deploy.yml already launches the `.mjs`). Three strings, one fix.
- **N6. Website stale lane worktree** (S) — DONE 2026-08-18 (orchestrator: playground-upgrades was fully merged — worktree removed, branch deleted; the vilan repo's three foreign worktrees pruned the same day at the owner's ask). `vilan-website/.claude/worktrees/playground-upgrades/` is a full leftover checkout from a merged lane. Verify merged (`git branch --merged`), then `git worktree remove` + branch delete, per the lane convention.

- **N9. AGENTS.md is stale in the parts that steer lanes** (S; found by the M3 lane 2026-08-18) — SHIPPED 2026-08-18 (merged 2391d307; five crates, `parsing.rs`, nextest as the gate, "Git is scoped to your worktree"; `lexer.rs`→`lexing.rs` in the keyword bullet; scar tissue kept). The agent briefing says "four crates" (five — AGENTS.md:10), routes the pipeline through `parser.rs (chumsky…)` (the handwritten frontend replaced chumsky 2026-07-22, frontend.md — it's `parsing.rs`; AGENTS.md:13), names `cargo test` as the gate where CLAUDE.md names `cargo nextest run --workspace` (AGENTS.md:37), and its "Mutating git is not yours — do not commit" rule (AGENTS.md:94) contradicts the lane convention every cycle runs on (lanes commit in their worktrees; the orchestrator merges) — the M3 lane flagged the tension rather than silently resolving it, which is the right instinct and the wrong default. Rewrite those four passages to the tree as it is; keep the scar-tissue list, which is still gold.
- **E63. NEW — the consumer sweep S1 promised: `examples/todo` and `examples/walkthrough` onto the builder** (S; found 2026-08-18 answering the owner's status question) — SHIPPED 2026-08-18 (merged adfa1b3d; record: fullstack-dx.md §16; drafts in proposal/e63-drafts/). fullstack-dx.md §13.3 moved `examples/fullstack`, `examples/ssr`, and the init template to the ladder and said todo/walkthrough "close the gap when S1 lets them move to the builder" — S1 shipped the same day and the follow-up never happened: both still `serve_service` with the hand-read shell (`examples/todo/src/server.vl:13,26`, `examples/walkthrough/src/server.vl:10,22`). Move both to `Server::builder().with_service(...).serve_build(build)` (rung 1) or `Document::of` (rung 2); the docs guides already teach the idiom. Beyond the repo: the owner's `vilan-playground/todo` and kolt's `vilan-migration` branch are on the old idiom too — the lane drafts those diffs for the owner to apply (a second field test of the ladder by the person who set its bar), never touches them itself. Outcome: both examples on rung 1 + rung 0+ (`with_service` + `serve_build` + `require_shell`), served pages byte-identical, ceremony 5 → 7 per §1.2's line rule — which the record shows was flattered by the old 258-char mega-line (§16.1: count per call); rung 2 blocked for examples by two gates (→ E65); `build_handler` orphaned (→ E64); the two owner drafts apply cleanly and `vilan check` green under v0.34.0.
- **L2. Script the train** (M) — SHIPPED 2026-08-18 (merged 7435de47; record: releases.md §7.1–§7.2). `scripts/cut-release.sh` (sweep (a) + retitle/order + bump) and `scripts/fold-release.sh` (merge, fast-forward, both dispatches, manifest, toolchain), both with `--dry-run`; neither tags or pushes a tag. releases.md §7.2 stays the authority, the scripts execute it — §7.1 documents how the sweep derives an entry's commit, §7.2 step 3 the `<!-- family: ... -->` convention the ordering refuses to guess at. Pinned by `crates/vilan-cli/tests/release_scripts.rs`; proven against the real v0.34.0 cut. Ready for the 2026-08-22 train. Outcome: `scripts/cut-release.sh` (sweep by `git log --all -S` on the entry's bold head + ancestry; `<!-- family: -->` markers classify, unmarked entries REFUSED never guessed; retitle/order; never tags or pushes) and `scripts/fold-release.sh [--dry-run]` (asserted preconditions, clean-merge check via `git merge-tree` since main has always carried commits the tag lacks; resumable). Replayed byte-for-byte on v0.34.0's 43 entries; 5 fixture-repo pins, plant-proven. First live use: the 2026-08-22 cut.
- **D15. NEW — docs currency audit against v0.34.0** (M; owner's 2026-08-18 list, item 9) — SHIPPED 2026-08-18 (merged db83df0c; record: docs-audit-2026-08-18.md). All 57 pages swept. No page taught a removed form — the docs gate had held that line. The drift was in the ` ```vilan,fragment ` fences the gate does not compile: thirteen printed signatures wrong against std, nine of them the same dropped/invented `sync` closure marker. Fixed, plus three prose claims that outlived their mechanism (the CSS hot-swap, the view rule on the `await` keyword, a closed tracked-gap note in `spec/types.md`) and `guide/persistence.md`, where the fullstack-dx sweep left both boot-sequence sections on the pre-ladder shape. New page `appendix/editor.md`; the v0.34 diagnostics wave indexed in `appendix/errors.md`. Keyword drift (§4 of the record): keyword axis clean, `i64`/`u64` still highlighted as primitives and `i53`/`u53` absent, `platform` missing from both attribute lists — all fixed. Filed behind it: D16, D17, D18. Feeds beta bar #6 (beta.md §2); the train's docs gate holds the line from here. Outcome: 55 pages verdicted (22 clean, 20 STALE, 19 GAP, 1 NEW); no page taught a removed form — the drift lived in `fragment` fences the gate doesn't compile (13 of 20, nine of them a dropped/invented `sync` marker); persistence.md rewritten onto the ladder; a new appendix/editor.md; the TextMate grammar highlighted `i64`/`u64` (hard errors) and not `i53`/`u53` — fixed, keyword axis clean; filed D16 (glossary dead anchors), D17 (gate the three-place keyword rule), D18 (keep editor.md honest).
- **B73. A blanket trait impl beats a user's specific one by declaration order** (M) — SHIPPED 2026-08-18 (R1 eafe5a3e / R2 4e086a5d / R3 9d72f2e6; record: method-resolution.md §13.8; BETA-CRITICAL, closes trigger (c)'s live blocker). `impl type T with Into<T>` matches every subject, so a user's own `impl Foo with Into<Bar>` loses to the blanket by declaration order — a wrong resolution from a clean compile, which the owner ruled counts as a miscompile for beta trigger (c). §13's 24-row probe table found the filing under-describes it: the headline shape is a false REJECTION, the silent miscompile is the unannotated call, and a second type-confusion miscompile (a `T: Conv<Baz>` bound emitting the `Conv<Bar>` body) involves no blanket at all — **so deleting std's blanket would not close trigger (c)**. Root cause splits in two: resolution keys on a bare trait id where B98's pair key and bound satisfaction both key on `(trait, arguments)`; and a genuine overlap falls back to declaration order. §13.5 recommends a specificity ordering (R1 arguments in the key, R2 expected-type selection, R3 subsumption ranking) over overlap rejection; §13.6 asks six questions, Q2 (may an annotation steer impl selection) and Q3 (should the std blanket exist) being the load-bearing ones. 13 `#[ignore]`d pins stand in `tests/inference.rs` under `b73_*`. History: backlog-2026-07-18.md §B item 73. Outcome: R1 puts the trait's effective arguments into the resolution key (analyzer + a one-sided transformer filter, goldens byte-identical) — the two silent miscompiles (rows 2 and 20) closed by R1 alone; R2 lets the expected type pick among instantiations, `Into::into` reaches §3.1's re-point (row 5 needed two repairs); R3 ranks a genuine overlap by subject subsumption then bound strength, bounds part of applicability (Q6), row 17 takes the trait default (Q5), unrankable maxima a call-site ambiguity (Q4). All 24 rows closed; four divergences from §13.2's predictions recorded (18/19 need R1+R2; 6/7 close under R2; row 5's mechanism; spec/types.md §5.4's second note was already stale). Ledger rows 213/214. One residue deferred → B128.
- **E66. NEW — no completion after a call expression: `client.add("blah").|`** (S–M; owner's 2026-08-18 playground notes, item 2) — SHIPPED 2026-08-18 (merged 6229ef5c; record: editing-dx.md §18; root causes: `receiver_nominal_id` resolved complex receivers through `hover_label`, whose `Call` arm names the CALLEE's signature; and the analyzer records no type on a call's own expr id (B85/B70's silence) — fixed by `Document::expression_type_id` resolving value types structurally (binding/call/block, depth-bounded); 8 pins + a protocol pin). At `vilan-playground/todo/src/client.vl:42` — inside `print(client.add(note_name.get()))` — typing `.` after the call's closing paren offers nothing; the receiver is a call expression whose result type (`RpcOutcome`/`Result` of the service method) the completion path evidently doesn't resolve mid-edit, where a bound name's would. Same family as E52/E57's completion work (editing-dx.md); pin per receiver shape (call result, chained call, call inside an argument list, `?`/`!`-lifted call).
- **E67. NEW — no completion for element-syntax attributes: `<div |>` / `<div .|>`** (S–M; owner's 2026-08-18 playground notes, item 3) — SHIPPED 2026-08-18 (merged 6229ef5c; record: editing-dx.md §18; root causes: elements desugar before analysis so completion had no head context; AND the parser flattened `<div .>` to an error atom (nested: the whole statement) — fixed: the dot commits the item to the chain form (failure reported, item dropped, element kept), and `in_element_head` reads the live parse at the head's own bracket depth; `<div .|>` → View methods, `<div |>` → dotted links + `on:`; attribute NAMES deferred → E69; 7 pins + a protocol pin + a parser pin). vocabulary is deliberately not offered: the desugar is name-blind by ratified design, §18.2) Inside an element's opening tag, neither the bare attribute position nor the `.method` position offers anything — element syntax lowers to the `view(...)` chain (element-syntax.md), so `<div .|>` is exactly the chain's method-completion site (`.bind_each`, `.on`, `.text`, `.child`, styling methods) and `<div |>` the attribute names the desugar accepts (`name(..)`, `type(..)`, `on:event(..)`, `.method`). Completion should answer both from the desugar's own knowledge; the recovery-tolerant parse of an unfinished tag (E49's synchronizer) should already keep the tree alive at that point — verify.

- **M2. Memory-leak soak** (M; owner's item 8) — SHIPPED 2026-08-18 (merged aae4d7ad; record: leak-soak.md; findings: 1 filed — M7). Tier 1 extends `leak_measurement` to both real corpora at 2,520 analyses each, in two byte-compared windows, through BOTH allocation lifetimes — the inline one and `Document::analyze`'s thread-per-analysis, whose tally must be read INSIDE each thread (a read after the join is zero, which looks exactly like a clean plateau; the new gate pin `leak_soak_harness_smoke` asserts the two drivers agree to the byte so that cannot ship). Tier 2 is `scripts/soak.sh`: a `run --watch` session over 40 rebuild rounds with 160 SSE connect/disconnect cycles, plus the compiled Node server under 40,000 requests across two heap configurations. Results: every window byte-identical to its partner, and `total` equals the named sites on every row — every other `leak_tally` site contributed ZERO over 5,000 measured real-file keystrokes; the watcher's fds/threads ran 4 → 8 → 4 on all forty rounds (M3's fix validated in the field — pre-fix the same run ends at 164 fds); the Node server's RSS plateaus and survives a 64 MiB old-space cap. The one finding is a magnitude rather than a bug → M7. dhat-rs was NOT added (a new dependency is a stop condition); leak-soak.md §6 is the plug-in sketch and what it would answer. Clears beta.md §2's leak question. Outcome: tier 1 (`leak_measurement` on kolt's views.vl and the website's page.vl, moving single-char edit, two disjoint windows byte-identical, both `analyze` lifetimes — the thread-local tally read after a join reads zero, guarded by a gate smoke) — every non-entry site 0 B over 5,040 analyses; tier 2 `scripts/soak.sh` — 40 watch rounds with 4 SSE browsers churned each: fds/threads 4→8→4 every round (M3 validated; pre-fix 164 fds); 20,000 server requests: RSS bounded (proven under `--heap-cap 64`). THE FINDING: the by-design per-analysis leak is linear in the session — 3.12 MiB RSS per keystroke on page.vl, 97% the entry AST (`Program<'static>`) → M7. dhat not added (dependency = stop condition; §6 sketches it).

- **M4. `const_eval::evaluate` is two thirds of a style-heavy compile** (M; found by M1's baseline) — SHIPPED 2026-08-18 (M4 hoists 466820a5 + M4-A d6b38fc7; record: const-eval.md §10.1–§10.6; B stays deferred). Profiled 2026-08-18 (const-eval.md §10). The pass is **linear** in const sites at a flat ~0.75 ms/site release; the website's gap over kolt is site count times chain length — 483 evaluations against at most 18 — and nothing degrades with scale, so the filing's super-linear suspect was wrong except for one 3.4 % term (`in_const_subtree`'s O(sites × call sites) span scan). Two thirds of the per-site cost is the interpreter computing the style itself (0.094 ms per property, zero intercept — irreducible within one analysis); one sixth was a per-site rebuild of program-wide state, chiefly a 4,184-entry source-name map built once **per const site**. Those hoists SHIPPED: const_eval on the website is 3408 → 2799 ms debug and 429 → 308 ms release, `vilan check` −12.7 %, and `page.vl`'s keystroke p50 321.5 → 252.4 ms (still 1.68× the 150 ms debounce, was 2.14×). Byte-identical output; three pins. **Live remainder**: §10.5's two options — (A) one shared const world per analysis, recommended, ~25–30 % of what is left; (B) cross-analysis memoization, §8.3's deferred cache-key question, the only thing that helps the editor's *second* keystroke. RULED 2026-08-18: A, as recommended; **A SHIPPED 2026-08-18** (const-eval.md §10.6). The distinct-function count the ruling asked for first: **10,245 function emissions across the website's 513 sites for 106/94/111 distinct per entry** — a 36–43× overlap — and the re-lowering it prices is **17.1 %** of the pass, not the ruling's 25–30 % (which sized the prize from the emission count against a transform share that §10.4's hoists had already cut to 19.4 %). The pass lowers ONE world per analysis and evaluates every site against it; each site keeps its own reach (prelude, `unresolved`, helpers, imports), its own prelude, and its own interpreter scope, so nothing a site evaluates can reach another. const_eval on the website is 341 → 273 ms release (−19.9 %) and 3062 → 2617 ms debug (−14.6 %), `vilan check` −9.5 %, and `page.vl`'s keystroke p50 252.0 → 237.1 ms (1.58× the 150 ms debounce, was 1.68×). Byte-identical output on all three real corpora; six new pins, each planted red. **B stays deferred** behind §8.3's cache-key question — it is the only thing that helps the editor's *second* keystroke, and it is the interpreter's 77 %. History: the const pass cost **4.21 s of the website's 6.60 s `vilan check`** — 64% of the wall, and 89% of everything `post_analysis_passes` does (attributed by patching per-pass marks into `post_analysis_passes`, debug profile, 2026-08-18; perf-baseline.md §4). It is not proportional to source size: kolt is a third of the website's lines and pays 62 ms, todo pays 20 ms, the `std::print` reference pays 1.2 ms — the website pays **68× kolt** for **3× the source**. What separates them is `const`-marked style work (`std::style`'s compiled atomics; 186 `const` sites across the website's sources, 81 in `art.vl` alone), so the suspect is per-site evaluation cost or a missing memo, not the pass's fixed overhead. G2's measured "7–9% warm-analysis const cost" (const-eval.md §8) was measured on a corpus that does not look like this one and should not be read as the general figure. `check` pays it in full despite emitting nothing, and so does every language-server keystroke: the post-passes are the one phase warming the caches does not help (204.9 ms cold → 206.3 ms warm on the website, perf-baseline.md §4.2), and editing `vilan-website/src/page.vl` costs **321.5 ms p50 per keystroke** against a 150 ms debounce budget. That is what makes it the first thing to profile rather than an emission-time curiosity. Outcome: the pass is LINEAR per site (the super-linear suspect was wrong); §10.4 hoisted the per-site name/program/span rebuilds (−18–28% on the pass); §10.6 built Option A after measuring the prize at 17.1% (not the ruling's 25–30% — honestly recorded, above the 10% floor): one `ConstWorld` per pass lowers each reached function once, while each site keeps its own interpreter scope, prelude and REACH (one site's refusal never spreads); a real regression the lane caught by probe — shared naming lost the failing function's name in the const-failure diagnostic — fixed at the root (`resolve_trace` maps frames to entities; the note now points at the RIGHT `helper`). Cumulative: website check −13% then −9% more; page.vl keystroke p50 321 → 252 → 217 ms (2.14× → 1.58× the debounce); byte-identical output on all three corpora.

## 2026-08-19

- **E65. NEW — a rung-2 example cannot exist under two shipped gates** (S–M; found by E63 2026-08-18) — SHIPPED 2026-08-19 (merged 3fd14ecf; record: fullstack-dx.md §16.7). `unlinked_stylesheets` and `init.rs`'s blessed-layout gate read the leg's SERVER source through the real lexer for `Document::of(<build>)` resolved to a leg (`tests/support/ladder.rs`); `examples/todo` climbed to rung 2 (`src/app.html` deleted, page written from the build; served bytes measured against the old shell: only `<html lang>`, the viewport meta, two self-closing tags); the `vilan init` scaffold stays at rung 1 + validator by the §6.3/§10.6 ruling, the gate distinguishing scaffold from example. Finds: E70 (filed), stale `hmr.rs` comments, `examples.rs` header count.

- **A25. NEW — remote sources: subscribe by demand, unsubscribe at zero** (M; owner's 2026-08-18 playground notes, item 1) — SHIPPED 2026-08-19 (merged 5cb95b0a + goldens 372122e1; record: remote-sources.md §8; paper RATIFIED as recommended the same morning). S1: `Subscription.release` one-shot hook + `at_settle(id, action)` in reactive.vl; `RemoteSource` `count`/`closing`/`settle_id`/`codec` replacing `wanted`, one counted `lease` under `sub`, `Subscribe` at 0→1 (eager), `Unsubscribe` at 1→0 deferred to the ambient settle, `rebind` drops a pending close and re-subscribes iff count > 0, `ReactiveServer::start` idempotent. S2: `map<U>(|Option<T>| U): Signal<U>` (counted, owner-scoped — a compile error without an owner), `or(initial)` as sugar, passive `status(): Signal<Status>` (`Waiting`/`Ready`), `sub` keeps `|T|`. S3: walkthrough + todo examples, benchmarks dispose their leases, five doc sites + `std/reactive.md:87` (both reference pages wrongly said `sub` fires only later), README. Pins A/B/C un-ignored + nine new. Six corpus goldens and the split fixture regenerated at merge (std inlined; stdout identical). Playground-todo and kolt diffs written in §8, not applied (await v0.35.0 on the installed toolchain). Finds: B129 (filed), E68 widened.

- **D17. NEW — gate the three-place keyword rule** (S–M; found 2026-08-18 by D15) — SHIPPED 2026-08-19 with D18 and D19 folded in (merged 56486ea5; record: docs-audit-2026-08-18.md §5). `crates/vilan-cli/tests/grammar_sync.rs` holds `lexing::KEYWORDS`, `type_::SCALAR_PRIMITIVE_NAMES`/`NUMERIC_SUFFIXES`, `parsing::KNOWN_ATTRIBUTE_MARKERS` (the tables themselves, now `pub` consts the lexer/parser/analyzer read) against the TextMate grammar and the book's highlight.js language, both directions; drift found and fixed: the TextMate attribute lookahead lacked `platform`. AGENTS.md names the tests.

- **D18. NEW — nothing keeps `appendix/editor.md` honest** (S; found 2026-08-18 by D15) — SHIPPED 2026-08-19 inside D17 (merged 56486ea5; record: docs-audit-2026-08-18.md §5). `crates/vilan-lsp/src/book_sync.rs` (a `#[cfg(test)]` module — the server is a bin crate) pins the page's quick-fix titles against every `QuickFix`/`CodeAction` constructor, its 24-row capabilities table against a factored `server_capabilities()`, settings and the restart command against `package.json`, and `BOOK_BASE` against the extension's homepage. Drift fixed: "Five" quick fixes → four.

- **D19. NEW — an LSP keyword-hover deep link is already broken** (S; found 2026-08-18 by K6) — SHIPPED 2026-08-19 inside D17 (merged 56486ea5; record: docs-audit-2026-08-18.md §5). mdBook 0.5's heading-id algorithm reimplemented (it DROPS non-alphanumerics — `if / else` → `if--else`; whitespace → `-`; no collapse) and every `KEYWORD_DOCS` link checked page + anchor with no `mdbook` at test time; an `#[ignore]`d test compares against a real build when `mdbook` is on PATH (447 headings / 56 pages, 0 mismatches). The `impl` anchor fixed.

- **K7. Run buttons and "Open in playground" on doc examples** (M) — SHIPPED 2026-08-19 (merged 05b4785b; record: docs-port.md §7; K6 S5). Every complete fence runs in place: one shared lazily-spawned module worker at `/playground/worker.js` (FIFO queue, recycle after 32 / on crash, 3-attempt load with in-panel message), the playground's srcdoc-iframe runner ported (sandbox, console/error forwarding, ResizeObserver height), an output panel (status, result, diagnostics, console), `Check` for process-leg fences; no `&v=` pin; one injected `<style>` on the role tokens with mdBook fallbacks. Gate against the real published wasm: 156 fences carry controls, 153 compiled fences settle (138 ran, 15 checked, 0 crashed); 156/156 ▶ links round-trip, 15 `&mode=node`. Codec re-verified identical (de-dup = K15). Finds: K14 (fence-tag shim blind under mdBook 0.5), K15 (filed).

- **M7. The LSP's per-analysis leak is linear in keystrokes, and it is megabytes** (M; found by M2's soak) — SHIPPED 2026-08-19 (merged 23178daf; record: leak-soak.md §7). `leak_tally::Leaked<T>` (a `Box::leak` whose site keeps the handle; `unsafe fn reclaim` frees and `release`s the same bytes; gross `bytes` unchanged, `outstanding` = signed net; new `MacroWorldAst` site so `EntryAst` is the top-level entry only); `analyze_source_reclaimable` hands the tree back; the Document's `AnalyzedProgram` drops the program then reclaims text + tree (explicit ordering). Soundness: every process-global audited (§7.2 table; `BASE_CACHE` is stored scrubbed — S3c's invariant now load-bearing), vilan-lsp's 317 tests clean under ASan with a planted use-after-reclaim caught as the positive control. Soak after: outstanding 0 B at both sites on every row; RSS/keystroke page.vl 3.19 → 1.52 MiB, views.vl 0.75 → 0.05 MiB. The remainder is M8 (filed): the const pass's interpreter Rc cycles, +1.52 MiB/analysis on page.vl. Also filed: M9.

- **L6. Branch protection — the deferred half of process.md** (S–M) — APPLIED 2026-08-19 (owner: "L6 approved"; record: process.md §9.2, §2.3/§2.4). Ruleset 18887216 `Protect default` amended by `PUT` and read back: active; `deletion` + `non_fast_forward` + `pull_request` (0 approvals, merge/squash) + `required_status_checks` (`check`); Admin bypass `always`; no `required_signatures`. `Protect next` (21052052) created: `deletion` + `non_fast_forward` on `refs/heads/next`, nothing else. process.md 8.3 and the second half of 8.1 closed. Stated consequence: the Saturday fold's direct push to `main` is admitted as an admin bypass; a PR-based fold is a `fold-release.sh` change if wanted.

- **N11. NEW — `print.html` is 712 KB of served dead weight** (S; found 2026-08-18 by K6) — SHIPPED 2026-08-19 (k6-book, merged 0eaa38c0; record: docs-port.md §6 S1). `[output.html.print] enable = false` in `book.toml`; verified absent from a local build; live at the v0.35.0 fold (`docs.yml` builds from `vilan@main`).

- **E64. NEW — `build_handler` has no consumer outside the docs** (S; owner call; found by E63 2026-08-18) — RETIRED 2026-08-19 (owner: "Retire `build_handler`"; merged 7e66a4eb; record: fullstack-dx.md §16.8; a `breaking` CHANGELOG entry). `std::http::build_handler` deleted (nothing else went dead); `std/process.md`, `guide/routing.md` (its deep-link fence is now a compiled builder program), `guide/services.md`, `guide/persistence.md`, `std/rpc.md:188` re-taught on `ServerBuilder::serve_build` / the `with_service` + `serve_build` chain. Out-of-repo grep: the playground has no caller; kolt still runs `serve_service` (E71's business). The `serve_rpc`/`serve_service`/`serve_connected` trio is E71, owner-ruled "soon".

- **N10. NEW — `theme/vilan.js` is served twice** (S; found 2026-08-18 by K6) — SHIPPED 2026-08-19 (pages repo web-chrome → main 2fa70e4; record: docs-port.md §1.3, the `docs.yml` comment). `docs.yml` now removes EVERY file under the book's source `theme/` from the served tree after the build (the pages link only hashed copies) plus `book.toml` — one loop, so a theme source the book grows later is covered. The first run under it succeeded (32280177879).

- **N12. NEW — the Pages repo has no README** (S; found 2026-08-18 by K6) — SHIPPED 2026-08-19 (pages repo 2fa70e4). `README.md` at the repo root: the who-writes-what table (`docs/**` by `docs.yml`; `index.html`/`client.*`/`playground/**`/`chrome/**` by the website's `deploy.yml` as the GitHub App; `assets/`, `CNAME`, `404.html`, `robots.txt`, `sitemap.xml` hand-owned), the chrome contract (`chrome/header.html`/`header.css`: one root element, no braces, absolute paths, role tokens only — the token list named), what the book does with it, and the N13 note.

- **K6. Docs on the vilan framework, integrated with the site** (L; owner's item 10) — SHIPPED 2026-08-19 as docs-port.md's option B (paper RATIFIED the same morning; record: docs-port.md §6 (S1–S4, S6), §7 (S5); the port proper is K13). The book wears the design language: `theme/css/variables.css` maps mdBook's 42 variables onto the role tokens declared on `html.light`/`html.navy` (design-language.md §2.5 — the light palette, authored here), Inter + CommitMono V143 from the pages repo's `assets/fonts/` with the §2.3 feature set, `.light`/`.navy` as the two vilan themes with the stock picker entries removed (`picker.js`), the tool-register stylesheet, no `print.html`; `head.hbs` links `/chrome/header.css` and `header.hbs` is a fallback that `docs.yml` replaces with `chrome/header.html` — the masthead the website's deploy exports from its own `top_bar` (mechanism (i): one nav, generated by the site, never hand-copied; `chrome/` is a new bot-owned prefix, named in the pages README). K7's run button rides S5. The site's half: every token carries a dark and a light value behind `prefers-color-scheme`, the home-page art re-lit onto the roles (owner: "Approved"). Compiler side merged to `next` 0eaa38c0 (live at the v0.35.0 fold — `docs.yml` builds from `vilan@main`; the cp is gated on `head.hbs` being present there); pages 2fa70e4; website cb3752a deployed. Owner reviews: "The book changes are approved"; the site "look good except for the home page graphics" → restyled → "Approved". Follow-ups filed: K14, K15, N14, K10 widened (three token mirrors now). Screenshots: `/mnt/c/Temp/vilan-shots/k6-*`, `web-*`, `art-*`.

## 2026-08-20

- **E71. NEW — retire the pre-builder boot functions: `serve_rpc`, `serve_service`, `serve_connected`** (M; owner ruling 2026-08-19: "I'd like the others retired soon as well") — SHIPPED 2026-08-20 (merged dfce0cd1; record: fullstack-dx.md §16.9; a `breaking` entry in v0.35.0's train). The trio deleted from `rpc_server.vl` plus `rpc_response` (its only extra death — `serve_rpc`'s text-only mount); the builder's layer (`Service`, `with_service`, the folds, the registry, `register_session`/`drop_session`) all stays. Consumers moved: rpc_http (6 servers), transport_robustness, streaming, the inference contract pin, both benchmarks, services/walkthrough guides, std/rpc + std/process references. `service_layer.rs`'s wire pin reframed as recorded literals with provenance — not one byte moved. ONE deliberate wire change beyond spelling, stated in the CHANGELOG: `serve_rpc`'s mount answered rpc on every path, the service answers on `{mount}rpc`, so bare-origin `HttpTransport` clients dial `/rpc`. Kolt's migration branch still calls `serve_service` — `e63-drafts/kolt-server.patch` + the A25 kolt diffs are REQUIRED for kolt on v0.35.0 (the owner applies).

- **K14. NEW — the fence-tag shim is blind under mdBook 0.5** (S; found by K7's lane 2026-08-19) — SHIPPED 2026-08-20 (merged 669af908; record: docs-port.md §7 addendum). The shim reads both mdBook class forms and normalizes to the comma form; pinned in `grammar_sync` (planted red first). Swept: 319 fences, tags survive the source census exactly; the 3 mistagged `fun main` fragments lost their controls; 155 carry controls, 138 ran / 16 checked / 1 failed — the failure is v0.34.0-wasm-vs-new-book skew (the services guide's A25 `or([])` fence) and heals at the cut.

- **E68. NEW — a cascade: the host-await secondary beside an `owner_scope` coverage error** (S; found by A25's lane, widened 2026-08-19) — FIXED 2026-08-20 at the root (merged bbafd351; record: async-polymorphism.md §A.4; a `diagnostics` entry). `Context::run` is `external` only as a type-checking fiction — the context pass erases accepted `run` calls before `async_infer`; on ANY context error it left the tables untouched and `async_infer` judged std's own async-into-`run` bodies as host-await misuses. Both arms now skip the intrinsic (`context_run_fn_id`): a surviving `run` call implies the context pass already refused, so the check's premise is false in every reachable state — an exemption, not a demotion. Three pins planted red pre-fix; the genuine-misuse pins still hold.

- **L5. Ledger recheck batch** (S) — SHIPPED 2026-08-20 as batch 8 (merged bbafd351; record: diagnostics-ledger.md). 162/214 recorded heads matched verbatim; ~20 re-keyed (the 2026-07-28 de-AI-ism pass had reworded them with no ledger update — the exact failure beta §3.1 will forbid); rows 215–239 added (context.rs's 11 never-audited sites, the `Rule` family, the process layer's runtime refusals); E68 recorded; A25's coverage anchor verdicted RE-ANCHOR (→ E74). The owed tail is L9.

- **B129. NEW — an empty `[]` through a `T`-typed parameter does not take `T` from the receiver** (S–M; found by A25's lane, owner-verified 2026-08-19) — FIXED 2026-08-20 (merged b97e8134; record: remote-sources.md §9; a `feature` entry). Two-step root cause: the empty-list arm minted its element slot ignoring its constraint, then reconcile's generic arm re-bound the receiver-known `T` with the raw `List<unknown>` — the reported hover. Fix: an empty `[]` whose expectation is `List<E>` grounds its slot to `E` iff the slot is open and `E` is fully determined (exhaustive-match helper, no catch-all). A25's pin un-ignored; five new pins + two negatives; annotations dropped at four teaching sites — `examples/todo` keeps one, correctly: that residue is B125's ordering gap (widened there), not B129's. No golden moved.

- **M5. `VILAN_PHASE_TIMING` cannot see inside `post_analysis_passes`** (S; found by M1) — SHIPPED 2026-08-20 (merged 4624aefa; record: perf-baseline.md §6.1). The aggregate line moved into the pass sequence itself and gained the ten-bucket breakdown incl. the `const-lower`/`const-interp` sub-split; prints from the one shared definition, so the CLI pipeline shows it too. First-vs-repeat emission deliberately skipped (post-§10.6 there is no repeat lowering to time; the surgery is named). Pin planted red.

- **M6. The in-process cold measurement cannot clear the parse cache** (S; found by M1) — SHIPPED 2026-08-20 (merged 4624aefa; record: perf-baseline.md §6.2). `parse_clean_cache_clear()` beside its two siblings (clears the map AND the broken set; the memory stays leaked); cold now means parse-cold — old-vs-new recorded side by side (+27% std_wide cold), the jsonl's cold rows declared incomparable rather than overwritten. Bonus: the ratio pin repaired at the measurement (interleaved rounds; the 6× bound kept — an 8× bound would forgive a real cheap-quadratic at 7.6–8.0×, measured).

- **E72. NEW — a member hover shows the bare, pre-house-style type label** (S; owner report 2026-08-19) — SHIPPED 2026-08-20 (merged 2e9d7420; record: editing-dx.md §19). Member reads answer the fenced `name: T` (tuple members free); the bare fallback gained the fence; and `get_safe`'s `enum Option` answer died at its root — `function_target` now falls back to the surviving `function_calls` record when the context pass overwrote `entity_map[call]`, so the method answers its real signature + std doc. editor.md updated under D18's gate.

- **E73. NEW — hovering a member whose binding never got a type CRASHES the server** (S–M; owner report 2026-08-19, core-dumped to the line) — FIXED 2026-08-20 (merged 2e9d7420; record: editing-dx.md §19; a `tooling` entry with E72). `hover_label`, `function_target`, `definition_of` walk their chains iteratively with a seen-list and answer an honest `None` on a cycle; the other resolvers audited one-step-safe. Plant-proven: the recursive form aborts with the owner's exact stack-overflow SIGABRT. Correction to the filing: the enabling condition is the CONTEXT LOWERING's records-less hidden parameter (present in clean files), not errors-in-file — the visibility gap is E75.

- **M8. NEW — the const pass's evaluator leaves `Rc` cycles every analysis** (M; found by M7's lane 2026-08-19) — FIXED 2026-08-20 (merged 5271ebb5; record: leak-soak.md §7.8, const-eval.md §10.7; a `tooling` entry finishing M7's sentence). The per-run scope registry: `Scope`'s raw constructors deleted, construction moved inside `Interpreter` (a scope cannot be born unregistered), `Vec<Weak<..>>` so the registry never extends a life, teardown as each run's last act — error paths and the MACRO path included (its caches hold only lowered trees and text, so cleared-too-early is structurally excluded). Three pins, each planted red (the planted census matched the prediction exactly: 4 stranded scopes). The soak after: in-use per analysis views.vl +45.8 KiB → +1.0 B, page.vl +1,523.9 KiB → +0.3 B; the LSP's session memory is now flat on const-heavy files. Residual, recorded: a panic that unwinds out of a run still leaks that run's cycles (M7's outer-fence caveat's class).

- **K16. NEW — the playground becomes a full-window app: the K5 refinement pass, specified** (M; owner's 2026-08-20 review notes) — SHIPPED 2026-08-20 (website@1e2875d, owner: "K16 approved"; record: design-language.md §2.7). The six points landed: viewport-fixed shell, slim app bar absorbing the brand link, Program | Result over Diagnostics | Console butting the window edges with the grid's own 1px `stroke-hard` seams, no cards (the confirm rail excepted), per-panel internal scrolling (page scroll asserted dead at two window sizes), flat `appearance:none` selects (hover moves the border, never the fill — the native flood and Firefox's popup recolor die with the widget; Firefox reasoned, not driven — no firefox.exe on the box), balanced gutter insets with the lint strip at VS Code's glyph position, vertical stack under lg. CDP sweep 19/19 (Run/Check, Share round-trip, Format, dirty-guard both paths, server template, diagnostics, version pin).

- **E70. NEW — `Document::head()`/`body()` promise a check that `html()` never runs** (S; found by E65's lane; RULED yes 2026-08-20) — SHIPPED 2026-08-20 (merged e1655512; record: fullstack-dx.md §16.10; a `feature` entry saying plainly a boot that served a wrong page yesterday can refuse today). `html()`'s generated arm runs the `check_shell` rules exactly when a hatch was used; hatch-less path proven byte-identical and check-free. Four refusal pins each planted red against the pre-change std (F2/F3/F4/F6); F1 unreachable through `html()` by construction; the Boot harness factored to `tests/support/boot.rs` and shared with shell_check. Find: E77 (supplied-shell hatches inert).

- **E74. NEW — the `owner_scope` coverage primary anchors in std when reached through a helper** (S–M; RE-ANCHOR verdict, batch 8) — SHIPPED 2026-08-20 (merged with cycle 25's train; record: ledger rows 222/223, async-polymorphism.md §A.4b, spec/contexts.md §8.3). The mechanism reused, not invented: async_infer's origin discipline — the primary anchors at the earliest user-written originating call (a new `user_entry_of` walk over direct, dispatch-narrowed, top-level, and capture edges through unbound callers only), std's read demoted to a cross-source note. Six pins, four proven red on a planted revert; a direct user read still anchors at itself, note-free. Residuals recorded in-row (OnConstraint union-admission; row 223's std flavor structurally covered, unprobed). Find: E76.

- **E75. NEW — the context lowering is invisible to tooling** (M; found by E73's lane) — SHIPPED 2026-08-20 (merged with cycle 25's train; record: editing-dx.md §19.3). The small "what I erased" record: `Program` gains `context_erased_subjects` + `context_hidden_parameters`, filled at the three erasure sites; the hidden parameter is a MARKER, not fabricated records (a fabricated `parameters` entry would surface it as a completion tab stop — the concrete lie that decided it); hover and go-to-definition resolve every call→subject step through the record and land on the source callee. Six pins; plant-proven three ways; E73's guards untouched and green.

- **L9. NEW — ledger batch 8's owed tail** (S–M) — CLOSED 2026-08-20 (merged with cycle 25's train; record: the ledger's "L9 closed" paragraph under batch 8). 13 pins, no wording touched: the four context rows (215/216/219/220, with 216 discriminating its sibling arm), `BuildError::Unreadable` both arms + `require_build`'s boot stop, `require_shell`'s header words, E67's exact render, three unpinned arms the empty-row sweep surfaced; the ten empty heads refilled from current sites (none retired). Find: batch 8's `require_shell` flag was stale — the boot path was always driven, only the header's words were unasserted.

- **K10. One token source for the editor theme** (S–M; widened 2026-08-19) — SHIPPED 2026-08-20 (website b4eac72 vilan-half merged 2b694ef9; record: design-language.md §2.6, docs-port.md §8). State correction first: the tracker's "three mirrors" was ONE — the CodeMirror theme consumes `--code-*` slots since K5 slice 2 (a token edit re-themes the committed bundle with no rebuild), so the ordered "generated editor theme module" was correctly NOT built (it would have re-coupled a rebuild-free mechanism). What shipped: theme.vl's palette in one const `themed_values` table (a missing name fails the build at const eval, probed red); the chrome leg emits `chrome/tokens.css` in both forms + a fixture; the book's `variables.css` stays the local source but is GATED byte-equal to the committed fixture (`book_mirrors.rs`, non-vacuous four ways). Site proven pixel-identical (before/after screenshots byte-equal).

- **K15. NEW — the share codec exists twice** (S) — SHIPPED 2026-08-20 (with K10). The codec's one home is `playground/codec.js` (imported by the editor bundle, esbuild-inlined, build fails if missing); the BOOK keeps its inline copy by the order's contingency — vilan.js is a classic script and its ▶ links are eager and serverless, so a dynamic import would kill every link on local books — but the copy is rewritten byte-equal to canonical and PINNED against the committed `codec-fixture.js`. Round-tripped in the served playground and against a real built book's ▶ link.

- **K12. NEW — `mdbook` is installed by unverified `curl`** (S) — SHIPPED 2026-08-20 (pages 5e58b6a/684b091). The v0.5.4 tarball's sha256 pinned (first-download trust, stated — upstream publishes no manifest) and `sha256sum -c` gates the untar; tamper path verified locally under `bash -e`.

- **D16. NEW — the glossary's internal cross-references are dead links** (S; RULED "update or drop" 2026-08-20) — SHIPPED 2026-08-20 as UPDATE (merged with cycle 25's train; record: docs-audit-2026-08-18.md §3). All 49 terms front explicit anchors, the 25 links resolve, rendered page otherwise unchanged; pinned in `book_sync` (`glossary_cross_links_resolve_to_a_term_anchor`, which caught the lane's own mid-task revert — the pin's first save predates its merge). A `tooling` entry (links now work).

- **N13. NEW — two sitemaps, no story** (S; owner delegated to convention 2026-08-20) — SHIPPED 2026-08-20 (pages 5e58b6a/684b091). The root `sitemap.xml` is a sitemap INDEX over the hand-kept `sitemap-root.xml` and the book's generated `docs/sitemap.xml`; robots.txt names the index; the README tells the story.

- **N14. NEW — the book's search index is 3.0 MB, 45% of the served tree** (S) — CLOSED 2026-08-20 as ACCEPTED, measured (record: docs-port.md §4 Q4 additions). Baseline 3,180,555 B / 448 sections, ~80% inverted index; `limit-results`/`teaser-word-count` are query-time only; `heading-split-level=1` saves 18.5% but collapses "signal" to page tops; the appendix opt-out saves 10% but delists pages that rank in the top-5 for real probes. The declined knobs live as comments beside book.toml's search block.

- **K11. Playground wasm retention policy** (M; RULED 2026-08-20: prune to ~6, as recommended) — SHIPPED 2026-08-20 (website fe902c6, pages a2d75b9). `deploy.yml` prunes `site/playground/` to the newest 6 version dirs (VER held out of the candidate list, ≤6 prunes nothing; simulated at <6/=6/>6/current-not-newest and against the real 21-dir inventory); the one-time pages prune deleted 15 dirs (~9.8 MB — v0.19.0 through v0.28.0, each named with size in the commit body; survivors v0.29.0–v0.34.0); the manifest regenerated byte-format-exact and validated. Bonus fix: a share link pinned to a pruned version no longer dead-ends in a "compiler crashed" retry loop — it falls back to the current compiler and says so ("Ready (vilan …)").

- **N7. Branding pipeline honesty** (S–M umbrella) — SHIPPED 2026-08-20 (branding 50e7e7d). The README's asset tables complete (bloom.png/bloom.webp were the two unlisted files, exactly as surveyed; the Regenerating comment now names all four bake families); `SHADOWS.md` registers the three downstream derivatives with recipes verified BYTE-EQUAL by running them (footer_mark.webp is sharp q85 — not the pipeline's q80, found by sweep; the two hero wordmarks are a 3-line shell recipe with a warning that the viewBox literal rounds outward — reproduce, don't recompute); `manifest.sha256` pins all 34 sources + 11 vendored copies with the deliberate unpins recorded, and `scripts/verify-vendoring.sh` runs clean against today's checkouts ("clean: 45 file(s) match — all checkouts seen"; drift path proven loud). No CI workflow — the repo has none; script + README is the v1. The 2026-08-18 byte-identity HOLDS — no drift.

- **E76. NEW — the CLI's cross-source note sub-header prints a line:col that disagrees with the label** (S; found by E74's lane) — FIXED 2026-08-20 (merged fe54223e; record: diagnostics-standard.md §3 C3, the ledger's conventions paragraph). Root cause in ariadne 0.6.0 itself (labels converted bytes→chars for drawing, the secondary group's header derived from the char offset AS IF bytes — any earlier multibyte character drags it; no upstream release fixes it). The CLI now hands ariadne ONE index space: every span converted to chars exactly once at the boundary (`char_range`), so header and label derive from the same number. Both live note flavors verified fixed and pinned red-first; same-file rendering byte-identical.

- **E77. NEW — `head()`/`body()` on a SUPPLIED shell are silently inert** (S; RULED compose 2026-08-20) — SHIPPED 2026-08-20 (merged f4fb4aa6; record: fullstack-dx.md §16.12; a `feature` entry saying plainly that previously-ignored calls start doing something). The hatches splice before the shell's own closing tags — read the way the check reads markup, so a `</head>` in a comment is no splice point; a shell lacking the tag a USED hatch needs refuses with both fixes named (ledger row 240) — and the composed page runs the check_shell rules exactly when a hatch was used, uniform with E70 (the envelope's slot names the shell + the hatch provenance). Hatch-less supplied path pinned byte-identical with the check's ABSENCE observed. Six of seven pins planted red against pre-change std.

- **E78. NEW — the unprovided-context error underlines the whole call chain** (M; the owner's 2026-08-20 ask, example-as-contract) — SHIPPED 2026-08-20 (merged d2b7489b; record: spec/contexts.md §8.3, appendix/errors.md, ledger rows 241–243, async-polymorphism.md §A.4b). E74's walk now keeps EVERY user entry (each gets its own primary, chain, and std note; the least-id entry still leads) and a BFS collects every uncovered user-written call as a labeled hop, entry → read, capped at 6 with an honest tail; covered callers stop the trace (the owner's example is pinned verbatim — `c` clean); dispatch hops say "may flow through" (E74's union-admission residual, honestly). Rendered as multi-label ariadne in the CLI and `DiagnosticRelatedInformation` in the LSP. `Error` gained the `trace` channel beside the one-note C3 contract (256 literals swept mechanically). At merge: unified with E76's one-index-space renderer (every label converted per its own file text) — E76's 21 pins and E78's 9 all green over the combined renderer. Residue: E80 (HMR overlay + wasm playground don't surface the trace).

- **E81. NEW — E78's chain reached the editor only as the read's tooltip: related information draws no underline** (S; the owner's same-day report — "There is something in the `context.get()` error tooltip, but the call sites themselves aren't underlined") — FIXED 2026-08-20 (1c21fa0f, direct on next post-cycle-26; record: ledger rows 241–243 amended, the E78 CHANGELOG entry corrected in place — it claimed "each hop underlines in place", which `DiagnosticRelatedInformation` cannot deliver: VS Code renders related locations as Problems-panel children and hover links only; rust-analyzer gets multi-site underlines by publishing secondary spans as their own diagnostics). Each CALL hop of the trace now publishes as its own error diagnostic at the call: message = the hop's pinned label (rows 241/242), related information = the rest of the story (the other hops entry → read, then the read, then the C3 note), routed to the file the CALL is in; two reads reached through one shared upstream call merge into ONE hop diagnostic with the union of stories; the elision tail labels but never underlines (its span is the last kept hop's). `Error::trace` restructured to `Vec<TraceHop>` (note + `call` flag) so the tail is distinct by structure, not string prefix; CLI rendering byte-identical. Four wire pins in publish.rs, plant-proven red with the expansion disabled; the owner's todo client verified at the CLI (both closure-borne hops label; the wire consumes the same trace). Riders: row 242's cross-reference corrected (said "row 240" — merge-renumber remnant) and an orphaned empty family-tooling marker deleted from Unreleased (union debris; dry-run stayed green either way). Residue: E80 unchanged (overlay/wasm still message + note only).

- **L11. NEW — `cut-release.sh` accepts a family marker with no entry** (S; found 2026-08-20 during the E81 records check) — FIXED 2026-08-21 (lane l11-cut-orphans 74ab5d6d, merged; record: releases.md §7.2 step 3 — the orphan shape named as refused, the directly-above rule, the anchored parity one-liner as the manual cross-check). The awk parser strands a `family:`/`commit:` marker that reaches a blank line, a `---`, a second marker of its kind, prose, or the section's end before a bold head ("marker `<!-- family: tooling -->` at line N opens no entry"), exit 3 beside the existing refusals; the REFUSED banner reworded (the old one described only the missing-marker shape). Blank between marker and head REFUSED on evidence: 33/33 markers at HEAD adjacent, and every historical marker→blank was the 2026-08-20 debris itself. Four pins in release_scripts.rs through the `--out` seam (the real orphan shape + commit orphan + doubled marker + section-cut + EOF; the blank-parted marker; marker→prose — the pre-existing "text under Unreleased" RED, previously unpinned; commit/family in either order still cuts), three of them red against the unmodified script. Suite 3896/3896. Opens v0.36.0's `## Unreleased` with the first entry.

- **E80. NEW — the requirement trace stops at the CLI and the editor: HMR overlay and the wasm playground surface message + note only** (S; found by E78's lane 2026-08-20) — SHIPPED 2026-08-22 (lane e80-trace-surfaces: compiler ac0287f7 merged 7148be50; website db1690c pending the owner's visual OK — inert on the live site until a wasm carrying the field ships at v0.36.0, the worker maps `?? []`; record: hmr.md §2 amendment, guide/dev-loop.md, ledger rows 241/243 annotated). OVERLAY: text protocol kept (the SSE frame pin stands); per diagnostic the chain renders between message and note as `    via <file>:<line>:<col> — <label>` lines entry → read, the elision tail verbatim on its own line; each hop located at capture in ITS file (E16 per hop); the shim's new TRACE_LINE class is tested before LOCATION_LINE so the badge counts a traced diagnostic ONCE (pinned in the shim e2e: "1 error" not "4 errors"). PLAYGROUND: wasm `Diagnostic.trace: Vec<TraceEntry { file, line, column, message, call }>` (a bindgen getter — a Vec of exported structs crosses only as a return; `call` kept as the fifth field, the bit every surface keys on since E81); worker maps it, editor appends `via` lines to the lint message, the vilan-written pane renders dim children. Six pins plant-proven red (two exact-string overlay pins, the shim e2e badge + class guards, the live cross-module watch pin, the wasm owner's-example pin + the file's FIRST note pin). Suite 3901/3901 on the union. Found in passing: I4 (List has no PartialEq impl), E82 (derive refusal misanchored in the entry). Owner questions carried: the diagnostics-standard.md C3a paragraph (rec yes; draft in the lane report).

- **K9. Playground autocomplete: imported, never wired** (M) — SHIPPED 2026-08-22 (lane k9-playground-completion: design 1ad5473d + build 3c677a57, merged; website e8b2729 pending the owner's visual OK; record: playground-completion.md (new — §9 latency, §10 ship record, §11 owner questions), design-language.md §2.7 WIRED + §2.8, editing-dx.md §20 pointer). Design-first, and the seam was CLEAN so it built: the completion engine (68 functions / 1235 lines) shares only 9 primitives with hover and nothing with rename — hover/definition/rename stayed in the LSP. New crate `crates/vilan-ide` (core-only, builds for wasm32): the ONE `LineIndex` (both forks unified, 8 pins), `Analysis` (the brief's CompletionContext — program, analyzed + live indices, entity spans, platform requirements, import roots; E52's live→analyzed mapping as a method), the engine moved verbatim; vilan-lsp stays a bin depending on it (document.rs −1859 lines; all 65 completion pins green through the moved code, wire byte-identical); vilan-wasm retains the last analysis in a thread-local (replaced per compile, cleared when no tree, zero leak — pinned) and exports `complete(source, line, character)`; core's `modules_in_root` lists overlay entries (pinned). Playground: worker `canComplete` probe + `complete` action, CodeMirror `autocompletion` source (identifier chars, `.`/`:`, Ctrl-Space; snippets; auto-import as one transaction), smoke claim extended. Seven wasm pins, six plant-proven (one found VACUOUS under its plant and tightened). Latency: member completion 2.6 ms in-process / 2.9 ms worker round trip; scope position 51 ms → E83. At merge: unioned with E80 in vilan-wasm lib.rs by hand (k9's Retained/emit structure carrying e80's Locator with the entry index borrowed; one converter for both paths so parse-error diagnostics carry their trace too) — wasm+lsp 370/370, suite 3909/3909. Owner questions carried: crate name (`vilan-ide`), cold-instance completion (built: empty until the first check), keyword/snippet noise, popup trigger set.

- **B125. NEW — the P21 standing pin: generic propagation through an annotated `.map<U>`** (M; widened 2026-08-20 by B129's lane) — FIXED 2026-08-22 (lane b125-solver-ordering: 055183be + 20bf10ed + 76bbd9e6 + records 4b4d8276, merged b1764013; record: type-solver.md "The expectation is an input of generic call resolution — P21 closed", editing-dx.md §17.6 closed, remote-sources.md §8 corrected). Design-first with a spike; the bar was met so the code merged. Verdict: NEITHER (a) nor (b) as the papers framed them — the expectation (`expected_types[call]`, seeded at walk time for the annotated `let`, the declared return's tail and `ret`) was in the map before the call resolved and only B73 R2's home selection read it; it is now a third binding source inside the existing two-phase resolver with a fixed precedence: receiver → non-closure arguments → expectation → closure returns, at both call paths (B90 keeps them one rule); argument-first pinned (`fold(0, ..)` under `let s: str` still reports at the `let`); `type_is_ground` unchanged and load-bearing. B5: exactly one diagnostic, at the closure's brace, the `let` never doubles it — eight `b125_*` pins with `assert_fails_once_with` + `assert_fails_without`. KEY FINDING: B129's pin was misdiagnosed — never this family and never about the `let`: `resolve_for_each_item` (priority 8) and `resolve_subscript` committed `any` on sight of an Unknown iterable while the `.map` call deferred; fixed by the C′ defer rule the other four resolvers apply (its own `miscompile` CHANGELOG entry, split out at merge; one edge stated plainly: a never-called closure iterating its untyped parameter now reports → B131). Both ignored pins un-ignored (the P21 pin's `"}"` snippet had matched the struct brace — latent, fixed with `_nth`); the nested shapes (block/if-branch/match-leg tails) closed too; todos.vl's last annotation dropped. Gates: inference 2396/0/1, corpus byte-identical, docs, benchmarks; suite 3913 on the branch, 3930 on the union; perf within noise (alternating ×2, deterministic constraint-attempt counts 1–3 FEWER); plant-proofs 8/17, 5/5, 3/3 red. Residue filed: B131 (the never-called closure's anchor/head), B132 (bare-expression bodies report at the argument).

- **B126. NEW — an unannotated function's `ret` never reaches its inferred return type** (S–M; found by B124's lane 2026-08-18) — FIXED 2026-08-22 (lane b126-ret-inference: de16702d + 28b1be7c + 77a11a9b, merged 3b83d7e5; OWNER NOD 2026-08-22 with Q3 `any` and Q4 `never` confirmed; record: ret-checking.md rule 3 amended + "Rule 3, amended (B126)" section, spec/types.md §5.6, tour/functions-and-closures.md, ledger rows 244–245). The rule: an unannotated function's return type is the unification of its REACHABLE tail and every `ret`; disagreement = one refusal per disagreeing `ret` at that `ret` (function types `any`, nothing cascades — B5); bare `ret` = `ret <void>`; self-calls contribute nothing (only-self-calls = `never`, the panic precedent); async infers the same; closures stay on rule 4. The lane OVERTURNED the orchestrator's value-ret+void-tail clause on soundness (`fun f(x: bool) { if x { ret 1; } }` would hand `undefined` back) — reachability is the line, B124's own question asked of the same positions. Probed holes the amendment closes: `{ if x { ret "s"; } 2 }` typed `i32` and printed `s`; an exhaustive if/else of `ret`s typed `never` and accepted `let y: str`; `ret "wide"` passed conformance against `: i32`. Mechanism: ReturnFrame::VoidFunction deleted (Inferred generalizes Closure), Function.rets + FunctionReturns constraint, ONE helper (`inferred_return_type`) serving all five readers incl. conformance (body_tail_id deleted). 22 pins + 1 #[ignore]d residue (b126_a_let_bound_self_call_read_in_the_tail_resolves); three plants 15/9/6 red. Suite 3913 branch / 3951 union. At merge: the ledger's conflict block was committed by a blind `git add -A` and repaired one commit later (aaaf4d2a) — resolve every UU file explicitly, assert none remain. Follow-ups filed: B133 (closure asymmetry), B134 (return_sites join).

- **B127. NEW — delete the std `Into<T>` blanket?** (S; split from B73's Q3; census DONE 2026-08-20, RULED DELETE 2026-08-22) — SHIPPED 2026-08-22 (lane b127-blanket-delete d5360dbc, merged bacff3a7; record: method-resolution.md §14 status + §14.2 ship record; beta.md Tier 3 → 0 modules, `std::into` tabled Tier 2 pending the owner's glance, §5.1 Q3 collapsed). `into.vl` 5–9 deleted; the trait stays. Three B73 pins re-staged with two user impls (message bytes unchanged where pinned); rows-3/5 pins deliberately gained a first-declared decoy so R2's return-position leg stays exercised; b98's pin repurposed as the reflexive-impl legality/migration pin; probe B pinned (a bare `.into()` with no impl is a missing method, was silently compiling); the annotation tax repealed (single-user-impl `.into()` selects it, pinned). Corpus 7/7 byte-identical as the census measured; docs example moved to a user overlap; breaking CHANGELOG entry with the migration line. Suite 3957.
- **B128. NEW — R2 selecting an unrankable home takes its first maximum instead of the ambiguity** (S; deferred residue of B73, §13.8) — FIXED 2026-08-22 (lane b127-blanket-delete 615864fa, merged bacff3a7; record: method-resolution.md §14.2, ledger row 214 annotated). The probe the tree lacked was built and the fix was contained: `rank_member_candidates` hands R2 each home WITH its R3 verdict (`RankedHome`: representative + unranked maxima); R2 selecting an unrankable home reports `AmbiguousImpls` for that home alone. Pin red pre-fix (first maximum answered, compiled clean) → green; the ranked-home-beside-unrankable complement green both sides. A `miscompile` CHANGELOG entry (a program admitted where the ambiguity should have reported).
- **B130. NEW — a `T: Into<Foo>` bound fed a `Foo` dies in an internal error through the blanket** (S–M; found by B127's census 2026-08-20; live on released 0.34.0/0.35.0) — FIXED 2026-08-22 by the deletion (lane b127-blanket-delete d5360dbc, merged bacff3a7). The owner's check ("looks like a bug a user could recreate; should be fixed") answered as predicted: with the blanket gone the shape is the clean steered refusal `'Foo' does not implement trait 'Into<Foo>', required by a generic bound of this call` with the C3 note at the bound — pinned RED against the pre-deletion tree (the internal-error text recorded in the lane report verbatim), green after; probe G's reflexive-impl bound path green in both worlds. No transformer work exists on the delete path (D3's debt dies unfunded, §14.1).

- **L4. Deprecation mechanism** (M; design PROPOSED 2026-08-20, all four Qs RULED as recommended 2026-08-22) — SHIPPED 2026-08-23 (lane l4-deprecation c471b285 + 84125c1a + 6c9d4c20 + 18e748a9, merged 8fae76be; record: deprecation.md §7 ship record; ledger rows 246–247). The `[deprecated("steer")]` attribute (functions free/member/external, leading the ordered chain, steer required verbatim), honored std and user code alike per Q2; the use-site warning `` `{name}` is deprecated; {steer} `` on the non-fatal channel (calls + passed-as-value; import alone silent; deprecated-inside-deprecated still warns; A2 keeps std-internal uses silent — dependency packages ride L10/E84), surfaced by the LSP as a warning diagnostic; the CHANGELOG lifetime markers with cut-release.sh REFUSING a removal whose deprecation never shipped a released minor (same-train does not count; patch cuts refuse both markers; stranded lifetime markers refuse by line — 7 pins beside L11's through the --out seam); the CLI rename device on a synthetic hidden alias pair (warn + fold, hidden from help, conflict refusal). Q4's voluntary two-train window recorded PENDING for the next real retirement, not simulated. Every positive plant-proven (producer removed → five red; A2 skip removed → red; sweep/rewrite/strand/fold arms each planted). Suite 3969 branch / 3975 union. The book: grammar §3.3 production + attribute list; both highlighting grammars (grammar_sync green).

- **M9. NEW — `parse_clean_cached` / `ERROR_CACHE` is a slower session leak when an edited file is imported by another open document** (S–M; found by M7's lane 2026-08-19; §7.9.4 NOD 2026-08-22) — SHIPPED 2026-08-23 (lane m9-overlay-loads c06fcd93, merged ad3af3b3; record: leak-soak.md §7.10 with every number). Built as designed, eviction never resurrected: overlay-served module loads during an opted-in LSP analysis bypass the global caches and parse into analysis-owned `Leaked` allocations (`owned_modules.rs`: RAII CollectionScope, non-reentrant, drop-without-drain keeps the leak — the M7 panic story; one parse serves clean and broken alike, retiring §7.9.1's double-leak), drained into `AnalyzedEntry.owned_modules`, reclaimed by `AnalyzedProgram::drop`; bound = the open set. The base-world STORE GATE built and plant-proven — the disabled-gate run visibly reproduced §7.9.2's UAF ("cannot find 'greeting' in the imported path"); ASan positive control (planted use-after-reclaim → heap-use-after-free READ 26) then a clean 348-test run. CLI/wasm/tests/macro-world byte-for-byte unchanged (pinned: the non-opted path still caches globally). Soak: all four global sites read 0 B in every phase; outstanding 0 once the document drops. Perf: ≈ +0.8 ms/analysis (the edited file's parse, the only systematic delta) after fixing the hook's own accidental double-canonicalize (~1.1 ms flat, caught by the anchor). Suite 3964 branch / 3988 union. What stays open, restated in §7.10: base-world caching pauses while a toolchain file is open (resumes on close, pinned); macro-DEFINING buffer content still leaks per distinct content via the world caches.

- **E79. NEW — the declined `Document` head helpers have their first real customers: the §10.1 review is due** (S; filed by K13 step 3) — CLOSED 2026-08-22 (paper fullstack-dx.md §16.13, lane e79-head-helpers-review 1a192281 merged 46efafd2; ALL SEVEN RULED as recommended 2026-08-22). The census: across 12 documents the most common candidate appears in two — under §15.2's rule as written nothing graduates; the bound redrawn instead (the intersection plus the identity lines the document is the sole author of). The builds ride E85 (description graduates), E86 (the hatch separator), K17 (site-side theme_metas); icons/social card stay hatches; generic meta/CSP/`<base>` stay declined.
- **L10. NEW — std vs official packages: the distribution shape** (L; the owner's 2026-08-20 question) — RULED 2026-08-22, all five as recommended (std-shape.md §6 carries the ruling block). The NAMESPACE model is recorded direction with zero construction now; nothing splits before a registry exists; `std::markdown` builds package-shaped when K13 reaches it (Order 10's k13-markdown-design starts that); the one code item — reserve `std`/`pkg`/`macro_std` — is L12.
- **N2. `proposal/archive/` consolidation** (M) — ABSORBED and EXECUTED 2026-08-24 by N15: the dead generations, the frozen backlogs and roadmap.md live under `archive/` in vilan-lang/proposals, forwarding banners fixed at the rename; citations keep landing via the `proposal/` stubs.
- **N15. NEW — the proposals move to their own vilan-lang repo** (M; the owner's 2026-08-20 ask; six §8 rulings 2026-08-22) — EXECUTED 2026-08-24 (lane n15-proposals-cutover prepared: extraction 967 commits/117 files over THREE historical roots, byte-identical tip, runbook persisted; the orchestrator + owner ran it: vilan-lang/proposals live with full history, hygiene gate green, ruleset 21295500; the compiler freeze f0095d21 on next — banner stamps freeze sha 18f24cdf, suite 3988; sibling checkout + conventions re-pointed; the tracker renamed `tracker/backlog.md` same day at the owner's ask, history following). vilan main receives the freeze at the v0.36.0 fold.

- **L12. NEW — reserve `std`/`pkg`/`macro_std` as package names** (S; L10 Q5 RULED 2026-08-22) — SHIPPED 2026-08-24 (lane l12-reserved-names: vilan be0de705 merged 4294220b, proposals cfd6daa merged 6aaa582; record: std-shape.md §6 ship note, ledger row 248). The probe made the case better than the ruling: a dependency named `std` SILENTLY REPLACED the standard library (`import std::print` → "cannot find" while the dep answered `std::*`), `pkg` was silently dead (the own-package early return wins), and `macro_std` satisfied macros.rs's hermeticity check by spelling alone; `[package] name = "std"` was accepted. Now refused in `Manifest::validate` — the choke point both frontends ride (CLI invalid-manifest path; LSP F5 S5 manifest diagnostic, no new plumbing) — for `[package] name` and dependency keys in all three tables; `[library] name` deliberately exempt (std/macro_std ARE those libraries; a library's own name never binds an import root — the permanent line pending the owner's Q1 glance). Head: `` `std` is a reserved package name: the standard library owns it (`std`, `pkg`, and `macro_std` are all reserved); rename the package ``. Ten pins red under the plant + three complements + the LSP publish pin; the macro_std e2e fixture's dep key renamed `reflection`. CHANGELOG `breaking` (the probe: today-legal manifests stop compiling — all silently wrong or dead). Suite 4002. Residue: E88 (the compiler/IDE disagreement on a std-named dep, unreachable via manifests now).

- **E83. NEW — the scope-position completion re-parses the buffer once per auto-import candidate** (S–M; found by K9's lane 2026-08-22) — FIXED 2026-08-24 (lane e83-completion-parse-once: vilan 29aa6c21 merged ca6f122f, proposals 3e284a6 merged 3e5003c; record: playground-completion.md §9 dated amendment, original numbers kept). The seam: `auto_import_completions` called string-input `insert_import` per surviving candidate — a whole-buffer parse each (21 parses per scope request, measured); `doc_comment_of` read module text per candidate. Now: `formatter::ParsedSource` (parse once, probe many; the string entry stays, pinned, delegating), a per-query `source_texts` cache on `Analysis`, and thread-local count probes (`buffer_parse_count`/`source_read_count`, the E23 tally style — kept unconditional per the orchestrator's call). COUNT pins, not time pins: parses==1 (plant: 21≠1 red), reads==1 (plant: 3≠1 red), the parsed-vs-string equivalence probe. Numbers (release wasm, median of 20): scope 60.8 → 5.8 ms (136 items, now ~2× member — residue recorded, no follow-up); member/import unchanged. Wire byte-identical; all 65 LSP + 7 wasm + 8 ide pins green; suite 3992 on the branch. E89 filed (commit the measurement fold + harness). Union-gate note: the first union suite run was SIGTERM-killed at load-47 (four sibling lanes' suites); the quiet-box re-run is the gate of record.

- **E82. NEW — a derive refusal on a module's struct anchors at a comment line in the ENTRY** (S–M; found by E80's lane) — FIXED 2026-08-24 (lane diag-anchors 53d60e1f, merged 55e2f851; record: ledger batch cycle 28, row 137 RE-ANCHORED). Root cause was a CLASS: every diagnostic `finalize_build` (the post-fixpoint pass) pushed claimed the entry file while its span indexed the writing file — ~15 push sites (operator refusals, the for-each family, literal/range, residual sweeps) now route through `push_anchored` (the E1/E16/B112 channel; DERIVED_SOURCE re-anchors at the generating attribute, provenance prefixed). Seven module-attribution pins red-first. AT MERGE: the three derive pins' repro (`List` field) was neutralized by I4's new impl — re-staged on a derive-less `Opaque` (union commit), anchoring assertions unchanged.
- **B131. NEW — a never-called closure's untyped parameter reports at the use, through the leftover sweep** (S; found by B125's lane) — FIXED 2026-08-24 (lane diag-anchors, same merge; ledger row 249 — renumbered from the lane's 248 under l12's landed row). Head, parameter-anchored: `` `{name}` is never given a type: this closure is never called and its parameter is unannotated; annotate it (e.g. `|xs: List<i32>|`) ``; the sweep's residuals become its cascade (B5, pinned silent); a closure with a STALLED call is excluded (a pre-existing pin caught the false positive mid-build). Rows 250–254: the never-rowed for-each/backed-enum heads verdicted with their existing pins.
- **E84. NEW — the trace/demotion contract widens to any external/linked package** (S–M; the owner's C3a ruling) — SHIPPED 2026-08-24 (lane e84-package-demotion 7749f234, merged 92378547; record: C3a DONE, ledger rows 222/223/241 widened, spec/contexts.md §8.3). The probe confirmed the gap verbatim (primary IN the dependency's file, internals labeled as hops); the fix keys on the loader's `Origin` seam (`dependency_sources` mirroring `std_sources`, BOTH load sites — the lib.vl surface load was the trap); git deps demote identically. Six pins red under the std-only plant + a double-planted boundary control; a new dependency-package harness in inference.rs. Owner questions parked: open-dep-buffer behavior, std's own lib.vl, the workspace-sibling demotion (Q3 — the sharp one), the definition-site A2 sweep, the fallback holder name.
- **E85. NEW — `description(text)` graduates to a `Document` builder method** (S; E79 Q2 RULED) — SHIPPED 2026-08-24 (lane std-doc-smalls 6f5476da, merged 4bdcc5de; §15.2's bound amended per the ruling). `title`'s twin: attribute-escaped, emitted iff non-empty beside `<title>`, inert on a supplied shell (the sole-author rule). The escaping plant reddened exactly the escaping pin.
- **E86. NEW — repeatable `head()`/`body()` calls concatenate with no separator** (S; E79 Q7 RULED) — SHIPPED 2026-08-24 (same lane/merge). Consecutive hatch calls land one per line (composed-output pins, both arms); the site's per-item conversion + `joined()` deletion ride the HELD website commit (a359612) until v0.36.0 ships.
- **E87. NEW — data-* and aria-* attributes: `data-foo-bar(v)` already works — bless, pin, document** (S) — BLESSED 2026-08-24 (same lane/merge; element-syntax.md §2 dated sentence; docs line). The owner's probe pinned end to end; behavioral pin, no plant possible — no bug exists.
- **I4. NEW — `List<T: PartialEq>` has no `PartialEq` impl** (S; found by E80's lane) — SHIPPED 2026-08-24 (lane std-doc-smalls 68917bb7, merged 4bdcc5de). `impl List<T: PartialEq> with PartialEq` (element-wise, length first; the body written with `==` because the explicit `.eq()` spelling trips B135's operator-path ICE — recorded in compare.vl's doc comment); six pins incl. the derive-on-struct-field plant (the exact old refusal). DiagRow's derive return rides the HELD website commit.
- **K17. NEW — site-side `theme_metas(ground)` reads `themed_values`** (S; E79 Q5 RULED) — DEPLOYED 2026-08-24 (website 61c316f merged e18eb0c; served head byte-equal on installed v0.35.0 — landing 40082 B, playground 8187 B, diff empty). The hand-restated hexes are gone; one palette home holds.

- **B132. NEW — a bare-expression closure body reports its type disagreement at the argument, not the expression** (S; found by B125's lane) — FIXED 2026-08-24 (lane closure-ret-family f1727da4, merged 12dfa484; record: type-solver.md — P21's anchor residual closed). Bare bodies now report ON the expression with S3's regime wordings; six b132_ pins (5 + the re-pinned generics-binding pin red under the route plant; the agreeing-body complement green by design); b125's eighth B5 pin re-pinned to the new anchor with `assert_fails_without` proving the old whole-value message is gone, the other seven untouched and green.
- **B133. NEW — lift rule 4's closure refusal to the reachable-tail rule** (S–M; B126's Q1, owner-approved follow-up) — SHIPPED 2026-08-24 (lane closure-ret-family cc811d92, same merge; record: ret-checking.md "Rule 4, amended (B133)" — every clause carries its pin). Closures and async blocks infer by the SAME reachable-tail unification as functions (`return_evidence` + `unify_return_evidence`, one rule not two copies); a closure of rets binds a caller's return generic; a KNOWN return type (annotation, or the ground target B125's expectation held) checks the rets rule-2 style; the conservative steer survives exactly where a genuine disagreement remains. Sixteen b133_ pins under three plants (10/15, 2/2, 1/1 red; the five greens pin surviving behavior deliberately); one pin added post-plant, stated honestly. Residuals recorded: the held-target head wording (owner Q1), the ordering residual (Q2), never-called ret closures now quiet (Q3, pinned).
- **B134. NEW — the borrow/crossing seams do not see an unannotated function's `ret`s** (S; B126's Q2, owner-approved follow-up) — FIXED 2026-08-24 (lane closure-ret-family 08baf0cf, same merge; record: ret-checking.md — rule 3's Q2 closed; `family: miscompile`, the orchestrator confirms the reclassification). The join completes: unannotated `ret`s reach infer_borrows/crossings/clone seams. THE FIND: an unannotated tail of a loaned place ALIASED live storage in every released version (printed 99 where the annotated twin prints 3) — no corpus program had the shape; now copies like the twin (7 pins, 6 red under the declared-only plant). Acceptance change recorded: unannotated `ret &self.x` compiles-with-copy (was refused); the tour states the corollary (an unannotated function returns a value, never a view).

- **E90. NEW — workspace members are carved out of the E84 demotion** (S; e84 Q3, RULED 2026-08-24) — SHIPPED 2026-08-24 same day (lane e90-sibling-carveout: vilan 74f85c33 merged, proposals 1edf2f1 merged; record: C3a ruling note BUILT, ledger row 222's E90 paragraph, spec/contexts.md §8.3). Membership = the entry's enclosing `[project]`'s DECLARED `packages` (manifest.rs Project.packages), flagged on PackageSpec at the manifest resolver, excluded from `dependency_sources` at both load sites; `member=` rides the base-cache workspace fingerprint. The plant runs vindicated "never a path heuristic" empirically: a containment-keyed predicate swallowed even git checkouts. Ten pins (manifest four incl. the unlisted-path-under-root external; analyzer/CLI five red pre-carve; the external control red under an over-carve plant). E84's Unreleased entry amended in place ("your own code includes your workspace"). Suite 4070. Three edge semantics CONFIRMED by the owner 2026-08-24: a broken ancestor [project] now refuses resolution for depending packages; membership is the ENTRY's project only; nearest enclosing project wins.

- **B135. NEW — the operator path skips monomorphization for all-native bindings and hits the no-body guard** (S–M; found by std-doc-smalls 2026-08-24; B127's family, the OPERATOR half) — FIXED 2026-08-26 (Order 11, lane compiler-tooling-smalls 54744a12, merged edf8eb0b). The all-native skip assumed the body uses only operators on `T`; a user impl spelling `.eq()` explicitly ICEd at `Pair<i32> == Pair<i32>` on the bodyless-requirement guard. Fixed at the general path: the analyzer records the operand's type-arg bindings unconditionally (the same rule an explicit method call gets), and the transformer specializes exactly when the body transitively reaches a call that needs the substitution — a memoized walk over the program's call graph (generic helpers, trait-default fallbacks, `for`-loop `next`, lexical closures). The shared generic emission and every corpus golden stand byte-identical. Pins red-first in all three shapes (direct, generic-helper, closure — inference.rs `b135_*`). compare.vl's `==` idiom is now a codegen-size choice, recorded as such in its doc comment.
- **B136. NEW — an `is` test in a loop CONDITION compiles against a hoisted copy of its subject** (M; found by markdown-build 2026-08-25; MISCOMPILE, live in releases) — FIXED 2026-08-26 (Order 11, lane b136-loop-is 2f64b8ec + 6b9318d9, merged 131d449c; record: markdown.md §10.7's finding closed). Every conditional-position `is` form was wrong (probe matrix: the §10.7 repro printed 3 for 1; the unbounded form hung; nested, binding, double-`is`, and `Result` forms all stale; `if` position clean). `Expr::For` now walks its condition into a per-iteration prelude — `while (true) { <prelude> if (!cond) break; <body> }` when the condition emits statements, byte-identical emission otherwise; `jump continue` re-enters at the prelude. Pinned red-first per loop form (8 inference pins + a release-preset twin); the `std::markdown` bool-flag workaround removed; corpus goldens untouched. A release-notes line is due at the next cut — live in released toolchains through v0.36.0. Out of scope, noted: a second uncommented flag site in `parse_inline`'s `_emphasis_` branch remains (trivial follow-up).
- **B137. NEW — book_sync's `normalize_id` diverges latently from std::markdown's anchor rule** (S; found by markdown-build 2026-08-25) — FIXED 2026-08-26 (Order 11, lane compiler-tooling-smalls 85836c2b, merged edf8eb0b). The LSP docs-gate twin lowercased ASCII-only and skipped the post-tag-drop trim — both empirically wrong vs mdBook v0.5.4, both latent because no book heading exercised them. Aligned with the package's rule (full Unicode fold, post-tag-drop trim), both shapes pinned red-first, and `mdbook_heading_ids_reproduces_the_anchor_golden` now holds the twin to the same 456-id `markdown_anchors.golden` std::markdown answers to — one golden, two implementations, neither drifts without its own gate going red.
- **B138. NEW — the analyzer's recursion depth needs 64 MiB of headroom** (M; surfaced by the v0.36.0 gate's SIGABRT 2026-08-24) — CLOSED 2026-08-26 (Order 11, lane b138-depth 906c4a49, merged 37787a39). Measured first: `VILAN_DEPTH_STATS` (the phase-timing way — per-family peak depth + SP-measured bytes per top-level analysis) showed every realistic fixture peaks at 20 levels and the deep path is the phase-1 expression walk (~36 KiB/frame dev, ~4.5 KiB release). That walk is now bounded at 500 levels (25× realistic) with one clean steering diagnostic ("lift inner expressions into `let` bindings"); plant: a 5000-deep chain SIGABRT'd a 64 MiB worker unbounded, refuses at 17.5 MiB bounded. The 256 MiB spawns and wasm 64 MiB `-zstack-size` keep their values but lose their folklore status — measured rationale written at every site; the shrink is blocked by the unbounded return-inference chain, filed as B139 (with the exponential-time nested-arithmetic bug the plants surfaced, B140). Constrained proof green under RUST_MIN_STACK=1MiB (31/31 on the incident binaries).
- **D15. NEW — `std::crypto`'s promoted primitives are undocumented; the docs still teach the extern workaround** (S; found verifying the kolt dogfood list, 2026-08-26) — FIXED 2026-08-26 (Order 11, lane compiler-tooling-smalls 63e0b8ee, merged edf8eb0b). docs/std/misc.md documents `hmac_sha512`/`pbkdf2_sha512` and keeps the extern lesson scoped to what it still covers — std's surface is async WebCrypto, so the walkthrough's sync rpc dispatch still correctly binds Node's `pbkdf2Sync`. Same sweep: `content_type_of`'s doc comment no longer claims `std::fs` cannot read bytes (stale since F13's `read_bytes`). Docs gate green.
- **E88. NEW — a dependency named `std` resolves opposite ways in the compiler and the IDE** (S; found by L12's probe 2026-08-24) — FIXED 2026-08-26 (Order 11, lane compiler-tooling-smalls 076a2b15, merged edf8eb0b). Refusal chosen over resolver alignment: recon found a THIRD resolver (macros' `scope_for`, std-first) beside the filed two, and aligning three resolvers around a shape L12 already ruled illegal was maintenance for a contradiction. `analyze` — the funnel every entry path passes through — reports a `std`/`pkg` dependency edge with the manifest's own reserved-name refusal and DROPS it rather than failing the program; `macro_std` stays a legal edge at this layer (the macro world itself stages it). Pins red-first (module_resolution.rs; pre-fix the std imposter demonstrably swallowed `std::print`).
- **E91. NEW — grammar_sync generates the token tables, so the tree-sitter grammar is born gated** (S–M; the owner's grammar-strategy ask, filed 2026-08-25) — SHIPPED 2026-08-26 (Order 11, lane e91-grammar-gen 4d3aa887, merged a802e126). The grammars' word lists are generator output byte-gated to the compiler's exported tables (`KEYWORDS` split by a gated `KEYWORD_ROLES`, the newly exported `TWO_CHARACTER_OPERATORS` — the lexer itself now consumes the table — `SCALAR_PRIMITIVE_NAMES`, `NUMERIC_SUFFIXES`, `KNOWN_ATTRIBUTE_MARKERS`); structural rules stay hand-written. Regen: `VILAN_REGENERATE_GRAMMARS=1 cargo test -p vilan-cli --test grammar_sync generated` (named by every red and both files' headers). Plant-proven three ways (roleless keyword, hand-dropped fragment, fake operator). NOTE: the retired entry doubled as the GRAMMAR-STRATEGY RECORD (compiler = the one truth via semantic tokens; static grammars skeleton-grade; tree-sitter written once at/after the beta switch as one more consumer under this same gate) — E62 readers start at this tombstone and grammar_sync.rs's E62 seam comment until that lane opens its paper.
- **E92. NEW — `vilan build` leaves a superseded process artifact in place after a rename** (S; found refactoring kolt to 0.36.0, 2026-08-25) — FIXED 2026-08-26 (Order 11, lane compiler-tooling-smalls 78e576ba, merged edf8eb0b). The WARNING chosen over the record-scoped sweep, deliberately: pre-rename builds recorded nothing (process legs leave no manifest at all), so a manifest-scoped sweep ships blind to every dist/ already carrying the residue, and deletion in a directory users also write into is only sound with a record. `vilan build` now warns after each bundle write when a same-stem sibling survives under the other script classification's extension (stale `.js` beside `.mjs`, and the stranded-`.mjs` reverse), workspace dist/ and lone-package outputs alike — named files, named fix, never swept. Pins red-first (workspace.rs). A whole-build emission record (renamed-leg/dropped-leg shapes) remains open for a future item.
- **L13. NEW — a mechanical re-key scan over the whole ledger** (S; diag-anchors Q4, RULED file 2026-08-24) — DONE 2026-08-26 (Order 11, lane audit-1 as the diagnostics dimension; ledger updated in the same records push). All 254 rows mechanically scanned (fragment-wise fixed-string over crates/ + std + macro_std), every miss hand-triaged against source and `git log -S`. Re-keyed 13 rows (29, 30, 54, 70, 162, 170, 173, 175, 176, 178, 186, 199, 200) — all one class: 93d73f57's dash→colon/semicolon reword sitting outside batch 8's re-key list, the exact row-135 failure class that motivated this item, times thirteen. Zero dead rows (every site verified live); row 92 annotated with the held-target REWORD park; Order 10's batch-note row numbers corrected for the merge renumber. Scan-artifact lesson recorded in the new batch note: source-side `\`-continuations split format strings — fragment-wise matching plus hand triage is mandatory; a single-fragment scan silently masks drift (it did here).

- **E93. NEW — the HMR dev channel answers any origin and takes unauthenticated refresh POSTs: re-affirm or gate** (S; N16 audit run 1, 2026-08-26) — CLOSED 2026-08-26 (Order 12, lane hmr-token b8904853, merged b72d88e4). The owner asked for detail and the detail narrowed it honestly: never a network surface (127.0.0.1 only) and never a file-read primitive (the artifact routes always refused anything but a bare `<name>.<ext>`) — what was open was the BROWSER. Any other page the developer visited while `run --watch` ran could reach the fixed default port, read `/bundle/<leg>.js`, hold `/events` open and watch every compile failure arrive as it was typed (the overlay carries file:line:col, the diagnostic text, trace hops), and fire `POST /refresh` for a reload flood — the last needing no CORS permission at all, which is why an origin allowlist was never the answer. Shipped: 128 bits minted per run (SHA-256 over `RandomState`'s OS-seeded keys, wall-clock nanos, pid, a heap address — no RNG crate, the watcher's dependency-free posture), substituted into the shim beside `__VILAN_HMR_PORT__`, and required by a gate ABOVE the routing so no future route can forget it; a bare `403`, byte-identical for absent, malformed and wrong. Query parameter on every route because `EventSource` cannot set headers and one parser beats two. `ACAO: *` and the traversal guard deliberately unchanged. `std::watch::force_refresh()` — the one legitimate non-browser caller — learns it from `VILAN_HMR_TOKEN`, inherited at spawn. Three plants (gate removed, leaky 403, tokenless force_refresh). One residual recorded in the module doc and in guide/dev-loop.md rather than papered over: a tab from a previous watch session carries that session's token and must be reloaded once.
- **E96. NEW — `[build] run` hooks are arbitrary shell by design: record the trust model** (M; N16 audit run 1, 2026-08-26) — TIER 1 SHIPPED / TIER 2 RULED-AND-CARRIED 2026-08-26 (Order 12, lane build-trust 97090a0e, merged 373b691a; record: the new `proposal/build-trust.md`, index row landed). The audit's finding held and SHARPENED: the execution was deliberate and thoroughly code-documented (shell choice, before-not-after ordering, fail-loud rule all reasoned out on `BuildHooks`) while NO user-facing page stated the consequence — the strongest sentence in the corpus described capability ("pipes, globs and `&&` work"), never authority. Three places read SAFER than the truth: the spec's `[build]` bullet introduced `run` one sentence after "Build options never change program semantics, only the emitted text" (the same adjacency in the LSP hover); the tour said `run` "names a command" while the book called a hook "a step Vilan doesn't do"; and appendix/cli.md opened with "`vilan upgrade` is the only command that touches the network" as THE blast-radius statement, on the page documenting `vilan build`. Tier 1 landed in guide/dev-loop.md, spec/platform.md §11.4 (the codegen-knobs reassurance now scoped so it cannot bleed onto `run`), tour/projects.md, appendix/cli.md ×2, the `[build] run` hover, and a doc comment on `BuildHooks::run` recording the DECLINED first-run gate so it is not rediscovered as an oversight. The already-unconditional `Running <command>` echo is documented for the first time — the one honesty improvement, deliberately not a gate. TIER 2 is a RULING CARRIED FORWARD with no enforcement point and no need of one yet (dependencies cannot carry hooks): dependency-authored build code does not run by default and needs an explicit per-dependency opt-in recorded in the manifest, with three ruled properties (per-dependency not per-project, since a blanket switch reconstructs the default it replaces; recorded in the manifest, so granting execution is something a review sees and a revert undoes; absent means no, silently). WHO INHERITS IT: kolt.local 027 (`build.vl` / `vilan install` — its Trust question is ANSWERED and closed by build-trust.md §2/§3; its hook-point, caching and what-the-script-sees questions remain its own) and D5's registry (the opt-in's manifest syntax, resolution rules, transitive behavior). **The owner requires strict security here BEFORE beta, not at it** — standing bar in beta.md §2's "Before" list with the sibling boundary findings (E93/E94/E95, L14/L15); the reason is structural: beta is the posture in which somebody other than the author runs `vilan build` on code they did not write, so tier 2 stops being hypothetical exactly when the promise is made.

- **B139. NEW — the return-inference chain recurses once per call link, unbounded** (M; filed by B138's lane 2026-08-26 — the margin-shrink unlocker) — CLOSED 2026-08-26 (Order 13, lane depth-tail 9864e45f, merged ea9201bd). Both halves fixed. TIME: `inferred_return_types` already recorded each exact answer but only a read-only coercion path read it, so every link re-derived every link beneath it — reading it in `infer_function_returns`, gated on an EMPTY substitution context (the record is keyed by function alone; an answer shaped by a caller's generic bindings is that caller's), took a 500-link chain from 390,984 inference entries to 14,730, linear in both source orders; where the callee is defined first the recursion flattens away entirely (502 levels / 6.26 MiB → 6 / 0.07 MiB). DEPTH: that fast path is order-dependent, so the chain is also bounded at 500 links, the companion to `WALK_DEPTH_LIMIT` — past it the answer is `never` exactly as a re-entrant ask is answered and for the same reason, every frame beneath is marked inexact so nothing truncated is recorded, and one diagnostic steers to the annotation that ends the chain (1200 links: 503 frames, 6.54 MiB). MARGINS DID NOT SHRINK, and B138's premise turned out incomplete: every ANALYZER family is now bounded (~3.4 MiB together at release frame sizes), but extending `VILAN_DEPTH_STATS` with a `parse` family found the parser at ~20.3 KiB/level release — 4× the bounded walk, unbounded, and running FIRST, so it reaches the cliff before any analyzer bound can refuse. Filed as B142, which is now the actual unlocker. Rationale comments updated at both sites that own one. Constrained proof green under RUST_MIN_STACK=1MiB (2580/2580).
- **B140. NEW — nested arithmetic is exponential-TIME in analysis** (M; found by B138's plants 2026-08-26) — CLOSED 2026-08-26 (Order 13, lane depth-tail 5c8fb9df, merged ea9201bd). **MISFILED CAUSE — it was not the analyzer at all.** The filed diagnosis said re-computation through constraint wakes; `VILAN_PHASE_TIMING` showed every analyzer phase flat at ~200 ms while `analyze_source` took 8.4 s, and infer depth never moved. All of it was the PARSER: `parse_assignment` runs before the operator tower and decided whether an assignment operator follows a place by parsing the whole precedence chain SPECULATIVELY, discarding it when none did — after which the tower parsed the identical text again, so every expression re-entering `parse_expression` inside a bracket paid for its subtree twice, C(n) = 2·C(n−1). Fixed by `assignment_reachable`, a per-position table built in one backward pass over the tokens (a place is a balanced token run, so its operator sits at the bracket depth the place began at); `,` is deliberately NOT a barrier, since generic args put one at bracket depth zero inside a place — missing a barrier costs a declined attempt, a wrong one loses a parse. 20 levels 9.01 s → 0.24 ms; `vilan check` N=22 33.8 s → 0.28 s; N=60 never finished → 0.31 s; N=320 analyzes in 263 ms. Nested list literals are the same class and the same fix. PINNED ON A COUNT, not a clock (`parsing::atom_parse_count`, the E83 `buffer_parse_count` shape, ceiling `8·depth + 64`): a generous time ceiling separates linear from exponential but would NOT catch a regression to merely quadratic — and the obvious cheap fix for this bug is a quadratic lookahead scan. Verified non-vacuous: with the guard disabled the pin fails in 1.5 s at the smallest depth (524,284 atoms vs ceiling 192) rather than hanging. Nothing about what parses, or what any diagnostic says, changed.
- **B141. NEW — a field or method access directly off an implicitly-awaited call reads the PROMISE, not the value** (M; found probing the owner's async-transparency question 2026-08-26; MISCOMPILE, live in released toolchains) — CLOSED 2026-08-26 (Order 13, lane b141-await-parens d8bc9a85, merged, THE ORDER'S PRIORITY ITEM at the owner's ask). The emitter rendered an await as the prefix form `await (<operand>)` — parenthesising the OPERAND, never the whole await-expression — while the postfix arms rendered their subject with no parens of their own, so `await (f()).x` parsed as `await ((f()).x)` and the member was read off the promise. Silent: `vilan check` clean, exit 0, no diagnostic, because nothing was wrong with the PROGRAM — the type system, inference and call graph all had it right and only the printing was wrong, a defect a type checker cannot see by construction. BLAST RADIUS, wider than filed: field, built-in method, postfix chain and the async-call-in-argument-position spelling all went silently `undefined`; calling an async-returned callable (`make()()`) THREW `is not a function`; and the EXPLICIT `(await p).f` was broken too — the author's own parentheses were dropped at parse and never reprinted, so writing the await by hand did not save you. Neither of the last two was in the filed probe. Subscript and user-method calls were correct only BY ACCIDENT (both land the await in argument position, via `__at` and via free-function lowering) and are now pinned against exactly that accident being removed. ROOT CAUSE AND THE LESSON: the printer already decided parentheses at the CONSUMER — `operand()` + `js_binary_precedence()` answer "does this child survive unwrapped in binary-operand position" — and simply had no counterpart for postfix-subject position; the one case anybody had hit, a directly-called closure, sat open-coded in the call arm as a lone `matches!`, which WAS that missing helper at width one. Generalised to `postfix_subject`, wrapping any subject binding looser than a postfix (await, unary, binary, assignment, closure), and the special case is subsumed. The blanket alternative `(await x)` was rejected AND MEASURED: 88 lines across 7 goldens for no semantic gain, leaving every non-await loose-binding node unguarded; the context-aware rule moved ZERO goldens — which is the same fact as "std and the corpus always bind to a `let` first", the reason this survived to release. A new corpus program `vilan/test/await-postfix.vl` now makes the parentheses a byte gate. A release-notes line is due at the next cut.

- **B142. NEW — the parser has no depth bound, and it is the pipeline's deepest stack consumer** (M; measured by the depth-tail lane 2026-08-26 — the ACTUAL margin-shrink unlocker) — CLOSED 2026-08-27 (Order 15, lane b142-parser-depth 8c403fb8, merged 48169ea7). **Scope was six grammars, not one, and finding that was the lane's main result.** The bound looked like it belonged in `parse_atom` — that is where the depth instrument hangs and where the bracketed forms re-enter — but within expressions the BLOCK-BEARING forms (`{ }`, `if`, `for`, `match`, a closure body) reach a nested expression without ever touching an atom, and so do the unary and `const` prefixes, which recurse into themselves. Past expressions, five further recursive grammars are closed cycles reaching no expression rule at all, so no bound anywhere in the expression grammar could have seen them: types, binders/patterns, items (nested `fun`/`mod`/`impl`/`trait`, chained `export`), import paths, and elements. Not theoretical — `fun a() { fun a() {…} }` at 5000 levels SIGABRT'd a 64 MiB worker with no diagnostic while the other four parsed 5000 levels with ZERO refusals, so shipping an expression-only bound and then shrinking margins on it would have been a false claim. The guard sits on one shared funnel all six call through, counting ONE budget of 500 rather than six (six per-family counters would admit 3000 nested frames between them — no bound at all for the purpose). Two details arrived at the hard way: the refusal is held OFF the error list until the parse ends, because the backtracking combinator truncates that list when a branch declines and would have rolled the refusal back with it, leaving a file refused for depth reporting only a puzzled "expected `)`" (how deep the input went is a fact about the INPUT, not a claim by whichever branch was exploring); and past the bound each grammar returns a stand-in rather than declining, because declining sends 500 open frames back through their alternatives — measured, that took nested `fun` at 505 levels from 9 ms to not finishing in 25 s. Two funnels whose callers loop on success must also CONSUME a token as they refuse, or those loops spin. The constant is backed by a sweep: 211 compilable corpus entries peak at 23 levels, median 14, so 500 is 21.7× — stated conservatively (the same corpus reads 15/33× on the older expression-only counter). **THE PAYOFF COLLECTED**: the compiler-thread spawns drop 256 → 128 MiB (CLI and LSP both), ~3.6× the measured 35.2 MiB worst case with room for a macro-world compile which nests a whole pipeline inside the running analysis; the wasm `-zstack-size` drops 64 → 16 MiB, measured on the profile that SHIPS (`opt-level="z"` + fat LTO built to wasm32 and run under node against the plants: 8 MiB compiles every one, 4 MiB traps on the two worst), freeing 48 MiB of linear memory the playground no longer reserves. Second-order win now documented: a parser that will not descend past the limit cannot BUILD a tree deeper than it, so every later AST walk is bounded by construction. 21 door pins + a formatter-parse-mode pin (a second entry into the same parser — a bound wired into one entry point would have left `vilan fmt` overflowing on a file `vilan build` refuses) + a limit/text pin; non-vacuity proven by raising the limit to 5,000,000 and watching all three parser pins SIGABRT while the eight analyzer pins stayed green. B138's and B139's rationale comments are now current at every site, the LSP spawn included, which neither earlier item had updated.
- **E94. NEW — `asset::emit`'s kind becomes an output-path segment unsanitized** (S; N16 audit run 1, 2026-08-26) — FIXED 2026-08-27 (Order 15, lane security-tail b81301db). The `(kind, line)` pairs reached `output_js.with_extension(kind)` raw, so `"../evil"` steered the build's write outside `dist/` and `"a/b"` named a file in a directory the build never made. The SIBLING IN THE SAME CHANNEL had the fence all along — `asset::read` refuses an absolute path and any non-`Normal` component, lexically and before any filesystem look — so the input direction was fenced and the output direction was not, which is the whole of the defect. `check_emit_kind` applies the same shape at const time where the kind is finally known: structural (one path segment) rather than a charset allowlist, so it refuses every path a kind could become while leaving every real kind untouched. Both Order 14 pins un-ignored; the green negative holds, because a fence that refused a legitimate kind would be worse than the hole.
- **E95. NEW — a manifest dependency `path` lacks the `..` containment check entry paths get** (S; N16 audit run 1, 2026-08-26) — DECIDED, NOT GUARDED, 2026-08-27 (Order 15, lane security-tail 38db42e3). The asymmetry is correct and is now written where the code is. An ENTRY path names a file inside the package being built, becoming `dist/<name>.js`, so climbing out is meaningless and that guard is well-formedness, not a security boundary. A DEPENDENCY path names another package, and pointing outside the declaring directory is the form's entire purpose: `common = { path = "../common" }` is how every workspace spells it, this repo's own `examples/fullstack` legs included, `manifest.rs`'s dedup test uses `path_dependency("../lib")`, and std resolution runs through `../../vilan/std` — so `..` is not the hostile case, it is the ONLY case, and a guard would refuse the correct spelling of the common one. The security argument does not survive `build-trust.md` §2 (RULED 2026-08-26): the manifest is the developer's own file and building already runs its `[build] run` hooks with full privileges, no sandbox and no prompt, so against a file free to execute arbitrary shell a rule about which DIRECTORY it may name buys nothing — and worse, advertises a containment property the ruling explicitly declines to offer. Tier 2's enforcement point is D5's registry, not this path. Pinned by `a_dependency_path_may_leave_the_declaring_directory` so it cannot be quietly converted into the entry rule by a later reader who sees only the asymmetry, with a cross-reference at the entry guard explaining why the two differ.
- **L14. NEW — pin the workflows' third-party actions to commit SHAs** (S–M; N16 audit run 1, 2026-08-26) — FIXED 2026-08-27 (Order 15, lane security-tail fbc46ba1). All 32 `uses:` lines across ci.yml and release.yml pinned to full commit SHAs with the tag in a trailing comment; `.github/dependabot.yml` added so the pins are tended rather than frozen (a pin with nothing maintaining it stops receiving the action's own security fixes). Every SHA resolved from real upstream and cross-checked two ways — `git ls-remote` with tags peeled, and the API with annotated tags dereferenced; none taken from memory. **The item's inventory was STALE**: it named `Swatinem/rust-cache`, `actions/checkout@v5` and "the pages families", none of which exist in this tree, and MISSED two high-value actions it never mentioned — `azure/login` (the OIDC exchange) and `actions/create-github-app-token` (app-token minting) — both now pinned. One trap checked and dismissed with evidence: pinning `dtolnay/rust-toolchain@stable` looks like it freezes the Rust version and does not, because its `action.yml` defaults the toolchain input to the literal string `stable`, a rustup CHANNEL resolved at run time — so the pin freezes the action's code only. Verified by reading the branch, not assumed.
- **N18. NEW — the proposals repo's hygiene.yml declares no `permissions:` block** (S; N16 audit run 1, 2026-08-26) — FIXED 2026-08-27 (Order 15, lane security-tail; landed proposals d928fda). `permissions: contents: read` declared, and its `actions/checkout` SHA-pinned as L14's class, which the item named as owed here too.
